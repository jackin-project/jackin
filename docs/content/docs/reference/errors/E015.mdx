---
title: "E015 — Worktree materialization conflict"
---

**E015**: Worktree materialization conflict

## What to try

Run `jackin prune isolation` to clean up stale worktree state, then re-run the launch.

## See also

- Run `jackin doctor` for a full pre-flight health check.
- Pass `--debug` to any `jackin` command to enable additional operator output.
