---
title: "E012 — GitHub CLI authentication failed"
---

**E012**: GitHub CLI authentication failed

## What to try

Run `gh auth login` to authenticate the GitHub CLI, then re-run the jackin❯ command.

## See also

- Run `jackin doctor` for a full pre-flight health check.
- Pass `--debug` to any `jackin` command to enable additional operator output.
