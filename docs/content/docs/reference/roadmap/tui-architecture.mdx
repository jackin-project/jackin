---
title: "TUI architecture - Ratatui application patterns"
---

**Status**: Partially implemented. The shared `jackin-tui` component crate exists, the launch cockpit is substantially extracted, and the capsule renders through Ratatui. The remaining work is to make `jackin-console`, `jackin-launch`, and `jackin-capsule` all follow one source layout and one Elm Architecture boundary.

## Goal

jackin' has three terminal applications:

- `jackin-console` - the host workspace console.
- `jackin-launch` - the launch progress cockpit.
- `jackin-capsule` - the in-container multiplexer.

All three should read like the same kind of Ratatui application. A contributor changing TUI behavior should know where to put state, messages, update logic, effects, subscriptions, rendering, terminal wiring, screen modules, and local components without searching through unrelated runtime, Docker, config, credential, or protocol code.

The target is not a visual redesign. The target is a clearer architecture: TUI code is grouped under the TUI boundary, non-visual product work stays outside it, and external work returns to the TUI through typed messages.

This is primarily a refactor. Existing operator workflows must keep working the same way unless a later PR explicitly calls out and justifies a user-visible behavior change. Structural moves may tune internal behavior and TUI rendering consistency, but they must not silently change command semantics, launch flow, capsule attach behavior, saved state, keyboard/mouse contracts, or error handling.

## Architecture rule

Each surface crate has one TUI boundary:

```text
crates/<surface-crate>/src/tui.rs
crates/<surface-crate>/src/tui/
```

`src/tui.rs` is the thin module root. It contains module declarations, intentional re-exports, and short `//!` orientation docs only.

`src/tui/` contains the whole TUI application layer for that surface, organized with The Elm Architecture:

| Layer | Owns |
|---|---|
| Model | Visible terminal state: active screen, focus owner, modal stack, scroll offsets, hover/click state, picker state, animation state, render-safe snapshots |
| Message | Semantic TUI events and results: key/mouse intent, resize, tick, background completion, dialog outcome, screen transition |
| Update | Deterministic state transitions: apply `Message` to the model and return typed effects |
| Effect / Subscription | Typed descriptions of non-TUI work and long-lived event sources; they describe work but do not execute external commands inside TUI code |
| View | Pure Ratatui/raw-ANSI frame composition from the current model |
| Terminal/run loop | Terminal lifecycle, backend adapter, event-loop wiring, and routing of completed effects back into messages |
| Components | Surface-local visual components that are not reusable enough for `jackin-tui` |

The shared component/design-system crate stays separate:

```text
crates/jackin-tui/src/
```

`jackin-tui` owns reusable visual primitives, tokens, theme adapters, geometry helpers, shared runtime abstractions, and lookbook-backed components. It must not contain console-, launch-, capsule-, Docker-, workspace-, credential-, or command-execution logic.

## Boundary rule

`src/tui/` is for terminal interaction. It is not a place for business logic or external command execution.

**Belongs in `src/tui/`:**

- TUI model structs and enums.
- TUI `Message` / `Action` enums.
- Update functions that mutate only TUI state.
- Effect and subscription type definitions.
- Event-loop wiring that maps terminal events and completed background work into messages.
- Terminal lifecycle/backend adapters, including raw mode, alternate screen, and capsule socket backends.
- Ratatui `Widget` / `StatefulWidget` implementations.
- Raw-ANSI rendering only where a surface still requires it.
- Frame composition, layout, geometry, hitboxes, focus rendering, hint/footer rendering, and local UI components.

**Stays outside `src/tui/`:**

- Docker/runtime command execution.
- Git, GitHub CLI, 1Password CLI, shell command, filesystem, and network operations.
- Config/schema loading, migration, and persistence.
- Workspace resolution, role loading, mount inspection, and durable state cleanup.
- Credential handling and token/domain logic.
- Capsule PTY/session/control-plane authority.
- Capsule wire protocol definitions.
- Pure non-visual product rules that do not depend on terminal interaction.

The allowed connection is typed effects:

```text
tui/update.rs
  -> returns Effect::RefreshInstances

effect executor / run loop
  -> calls non-TUI service/runtime code

non-TUI code
  -> performs Docker/runtime work
  -> returns typed data or typed error

tui/message.rs
  <- Message::InstancesRefreshed(result)

tui/update.rs
  -> updates the TUI model
```

No renderer, component, or update function should call Docker, git, `op`, `gh`, the filesystem, the network, or config persistence directly.

## Final layouts

These examples are the target shape. They intentionally use the Rust 2018 `foo.rs + foo/` module-root style used elsewhere in the repo.

### Shared TUI crate

```text
crates/jackin-tui/src/
|-- lib.rs
|-- components.rs
|-- components/
|   |-- button_strip.rs
|   |-- confirm_dialog.rs
|   |-- container_info.rs
|   |-- error_dialog.rs
|   |-- hint_bar.rs
|   |-- panel.rs
|   |-- scrollable_panel.rs
|   |-- select_list.rs
|   |-- status_footer.rs
|   |-- tab_strip.rs
|   `-- text_input.rs
|-- geometry.rs
|-- runtime.rs
|-- scroll.rs
`-- theme.rs
```

`jackin-tui` is the only place for cross-surface visual primitives. A surface-specific component starts in that surface's `src/tui/components/`; it graduates to `jackin-tui` only when more than one surface should consume the same implementation.

### Single-screen surface

Use this for `jackin-launch` and for `jackin-capsule` unless a surface gains multiple distinct screens later.

```text
crates/<surface>/src/
|-- lib.rs
|-- domain/                 # optional, pure non-visual rules
|-- services/               # optional, side-effect adapters
|-- tui.rs                  # thin module root
`-- tui/
    |-- app.rs              # TUI model
    |-- message.rs          # TUI message/action vocabulary
    |-- update.rs           # state transition + effect requests
    |-- effect.rs           # typed effect requests only
    |-- subscriptions.rs    # timers, background completions, long-lived streams
    |-- run.rs              # event-loop wiring
    |-- terminal.rs         # terminal lifecycle/backend/capabilities
    |-- view.rs             # top-level frame composition
    |-- components.rs       # thin local-component module root
    `-- components/
```

Concrete `jackin-launch` target:

```text
crates/jackin-launch/src/
|-- lib.rs
|-- progress.rs             # non-visual launch orchestration / host adapter boundary
|-- build_log.rs            # build-log capture/storage when not visual
|-- tui.rs
`-- tui/
    |-- app.rs
    |-- message.rs
    |-- update.rs
    |-- effect.rs
    |-- subscriptions.rs
    |-- run.rs
    |-- terminal.rs
    |-- view.rs
    |-- components.rs
    `-- components/
        |-- rain.rs
        |-- progress_rail.rs
        |-- failure_dialog.rs
        |-- build_log_dialog.rs
        |-- container_info_dialog.rs
        `-- prompts.rs
```

Concrete `jackin-capsule` target:

```text
crates/jackin-capsule/src/
|-- lib.rs
|-- main.rs
|-- protocol/               # wire protocol and frame types
|-- daemon/                 # PTY/session/control-plane authority
|-- runtime_setup.rs        # non-visual container setup
|-- tui.rs
`-- tui/
    |-- app.rs              # visible multiplexer model
    |-- message.rs
    |-- update.rs
    |-- effect.rs
    |-- subscriptions.rs
    |-- run.rs              # attach/client TUI loop when applicable
    |-- terminal.rs
    |-- socket_backend.rs
    |-- view.rs
    |-- components.rs
    `-- components/
        |-- chrome.rs
        |-- status_bar.rs
        |-- branch_context_bar.rs
        |-- pane.rs
        |-- dialog.rs
        |-- palette.rs
        `-- container_info_dialog.rs
```

### Multi-screen surface

Use this for `jackin-console`.

```text
crates/jackin-console/src/
|-- lib.rs
|-- domain/                 # optional, pure non-visual console/product rules
|-- services/               # optional, command/IO adapters
|-- tui.rs                  # thin module root
`-- tui/
    |-- app.rs              # ConsoleApp, active screen, modal stack
    |-- message.rs          # top-level messages
    |-- update.rs           # top-level routing/update
    |-- effect.rs           # typed effect requests only
    |-- subscriptions.rs
    |-- run.rs
    |-- terminal.rs
    |-- view.rs             # top-level frame/chrome/modal composition
    |-- components.rs       # thin console-local component module root
    |-- components/         # console-local shared components
    |-- screens.rs          # thin screen module root
    `-- screens/
        |-- workspaces/
        |   |-- model.rs
        |   |-- message.rs
        |   |-- update.rs
        |   |-- effect.rs
        |   |-- view.rs
        |   `-- components/
        |-- editor/
        |   |-- model.rs
        |   |-- message.rs
        |   |-- update.rs
        |   |-- effect.rs
        |   |-- view.rs
        |   `-- components/
        `-- settings/
            |-- model.rs
            |-- message.rs
            |-- update.rs
            |-- effect.rs
            |-- view.rs
            `-- components/
```

Do not create `screens/` for a single-screen crate. Do not create nested `tui/screens/<screen>/tui/`; once code is inside `src/tui/screens/<screen>/`, the screen's TEA files already express the TUI layers.

## Current gap

The current repo is transitional. The target above should guide all remaining moves.

| Surface | Current shape | Remaining cleanup |
|---|---|---|
| `jackin-tui` | Shared components and helpers already live under <RepoFile path="crates/jackin-tui/src/lib.rs" />, <RepoFile path="crates/jackin-tui/src/components.rs" />, and <RepoFile path="crates/jackin-tui/src/components/panel.rs" /> style modules. | Keep it visual/shared only. Do not add surface-specific product semantics. |
| `jackin-launch` | The thin module root is <RepoFile path="crates/jackin-launch/src/tui.rs" />. The launch model/message/update/effect/input-subscription/terminal/run-loop pieces live under <RepoFile path="crates/jackin-launch/src/tui/app.rs" />, <RepoFile path="crates/jackin-launch/src/tui/message.rs" />, <RepoFile path="crates/jackin-launch/src/tui/update.rs" />, <RepoFile path="crates/jackin-launch/src/tui/effect.rs" />, <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" />, <RepoFile path="crates/jackin-launch/src/tui/terminal.rs" />, and <RepoFile path="crates/jackin-launch/src/tui/run.rs" />. The background rich render driver now also lives in `tui/run.rs`; <RepoFile path="crates/jackin-launch/src/progress.rs" /> remains the launch progress orchestration boundary. Top-level frame composition lives in <RepoFile path="crates/jackin-launch/src/tui/view.rs" />, and launch-local visual pieces live under <RepoFile path="crates/jackin-launch/src/tui/components.rs" /> and `crates/jackin-launch/src/tui/components/`. Docker-build output remains captured outside the TUI in <RepoFile path="crates/jackin-launch/src/build_log.rs" />; the run loop snapshots it into <RepoFile path="crates/jackin-launch/src/tui/app.rs" /> so the build-log overlay renders from the model instead of reading the sink directly. | Keep non-visual launch orchestration and host adapters outside `src/tui/`; grow typed launch effects only in `effect.rs` when update needs side effects. |
| `jackin-capsule` | The thin module root is <RepoFile path="crates/jackin-capsule/src/tui.rs" />. Visible multiplexer model vocabulary such as mux mode, hover targets, pointer shape, visible agent state, visible pane layout snapshots and derivation, drag-resize state, mux-mode derivation, pointer-shape derivation, and cursor-visibility derivation now lives in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />, and visible pane tree/tab geometry, content-row and content-rect derivation, plus pane-local mouse coordinate mapping now live in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. Terminal input parsing lives in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />, visible redraw/update vocabulary, partial-frame invalidation policy, and drag-resize ratio calculation live in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />, attach-client effect and subscription vocabulary lives in <RepoFile path="crates/jackin-capsule/src/tui/effect.rs" /> and <RepoFile path="crates/jackin-capsule/src/tui/subscriptions.rs" />, interactive attach-loop wiring lives in <RepoFile path="crates/jackin-capsule/src/tui/run.rs" />, and attach-client raw-mode, reset handling, terminal size normalization, and OSC 22 pointer-shape encoding live in <RepoFile path="crates/jackin-capsule/src/tui/terminal.rs" />. Full Ratatui frame composition plus raw-ANSI status bar, pane chrome, pane body, dialog overlay, dialog footer, bottom chrome, and selection-highlight render helpers live in <RepoFile path="crates/jackin-capsule/src/tui/view.rs" />, status-bar prefix-mode mapping lives in <RepoFile path="crates/jackin-capsule/src/tui/components/status_bar.rs" />, command-palette close-label derivation lives in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" />, and mouse text-selection state, start, motion, and copy helpers live in <RepoFile path="crates/jackin-capsule/src/tui/selection.rs" />. Semantic TUI actions live in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />, Ratatui/raw-ANSI rendering helpers live under <RepoFile path="crates/jackin-capsule/src/tui/render.rs" /> and siblings, and capsule-local visual components live under <RepoFile path="crates/jackin-capsule/src/tui/components.rs" /> and `crates/jackin-capsule/src/tui/components/`; callers use those canonical `tui::` paths instead of flat root aliases. Pure pull-request render snapshots live outside session authority in <RepoFile path="crates/jackin-capsule/src/pull_request.rs" />. Daemon/session/protocol code remains outside and calls TUI composition helpers for visible frames. | Keep daemon, protocol, PTY/session authority, runtime setup, and control-plane code outside `src/tui/`; keep moving only visible multiplexer app state and TUI event/update code into the TUI boundary where it is currently still mixed with daemon modules. |
| `jackin-console` | The thin module root is <RepoFile path="crates/jackin-console/src/tui.rs" />. Top-level generic app model plus message/update carriers and effect/subscription vocabulary now live in <RepoFile path="crates/jackin-console/src/tui/app.rs" />, <RepoFile path="crates/jackin-console/src/tui/message.rs" />, <RepoFile path="crates/jackin-console/src/tui/update.rs" />, <RepoFile path="crates/jackin-console/src/tui/effect.rs" />, and <RepoFile path="crates/jackin-console/src/tui/subscriptions.rs" /> while root-only product message/update now lives under <RepoFile path="src/console/tui/message.rs" /> and root-only typed effect requests live under <RepoFile path="src/console/tui/effect.rs" />. Root-only app constructors live under <RepoFile path="src/console/tui/app.rs" />, root-only model state and create-prelude model transitions live under <RepoFile path="src/console/tui/state.rs" />, root-only launch dispatch lives under <RepoFile path="src/console/tui/launch.rs" />, agent/role prompt flow lives under <RepoFile path="src/console/tui/prompts.rs" />, input dispatch lives under <RepoFile path="src/console/tui/input/mod.rs" />, and debug-log naming lives under <RepoFile path="src/console/tui/debug.rs" /> so keyboard/mouse handling stays inside the root TUI boundary while it still depends on root types. Root-only footer hint composition lives under <RepoFile path="src/console/tui/render/footer.rs" /> and `src/console/tui/render/footer/`; the root-dependent op-picker adapter lives under <RepoFile path="src/console/tui/op_picker/mod.rs" /> until its `operator_env` types move behind generic adapters. Root-only effect execution and background polling live outside the TUI boundary in <RepoFile path="src/console/effects.rs" /> so `message.rs` stays focused on semantic messages and deterministic update. Event-loop shell helpers now live in <RepoFile path="crates/jackin-console/src/tui/run.rs" />, terminal lifecycle abstractions live in <RepoFile path="crates/jackin-console/src/tui/terminal.rs" />, and top-level header/footer composition helpers plus footer-height calculation live in <RepoFile path="crates/jackin-console/src/tui/view.rs" />. The root application crate's host debug-buffering adapter for that terminal lifecycle lives outside the TUI tree in <RepoFile path="src/console/terminal.rs" />. Screen model/message/update/effect/view helpers now live under <RepoFile path="crates/jackin-console/src/tui/screens.rs" /> and `crates/jackin-console/src/tui/screens/`, with screen model state in each screen's `model.rs`, root-independent screen message/effect vocabulary in each screen's `message.rs` and `effect.rs`, tab label plus environment label composition in the editor/settings `view.rs` files, and workspaces disclosure rendering, running-instance summary rendering, and new-workspace description pane rendering in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />; local visual components live under <RepoFile path="crates/jackin-console/src/tui/components.rs" /> and `crates/jackin-console/src/tui/components/`, including the file-browser state/input/render modules under <RepoFile path="crates/jackin-console/src/tui/components/file_browser/mod.rs" />. File-browser Git URL lookup now leaves input as `ManagerEffect::ResolveFileBrowserGitUrl`, and file-browser directory navigation plus commit validation leave input as `ManagerEffect::ApplyFileBrowserOutcome` requests executed by the root file-browser service adapter <RepoFile path="src/console/services/file_browser.rs" /> through <RepoFile path="crates/jackin-console/src/services/file_browser.rs" />. Browser launches and workspace deletes queue `ManagerEffect::OpenUrl` and `ManagerEffect::RemoveWorkspace`; the root TUI run loop executes those root effects through <RepoFile path="src/console/effects.rs" />, which calls <RepoFile path="src/console/services/browser.rs" /> and config persistence outside the TUI boundary. Manager mount-info, instance-refresh, settings-save, auth-form 1Password read-validation, role repository registration, trusted-role persistence, workspace-save drift gates, isolation-record scans, cleanup workers, workspace writes, create-prelude file-browser opens/reopens, editor add-mount browser opens, global mount browser opens, file-browser Git URL resolution, file-browser navigation/commit validation, URL opens, workspace deletes, and op-picker metadata-load requests leave update/input/picker state as typed effects or pending load requests; <RepoFile path="src/console/effects.rs" /> executes those effects and calls <RepoFile path="crates/jackin-console/src/services/mount_info.rs" />, <RepoFile path="src/console/services/instances.rs" />, <RepoFile path="src/console/services/config.rs" />, <RepoFile path="src/console/services/op.rs" />, <RepoFile path="src/console/services/op_picker.rs" />, <RepoFile path="src/console/services/role_load.rs" />, <RepoFile path="src/console/services/agents.rs" />, <RepoFile path="src/console/services/token_setup.rs" />, and <RepoFile path="src/console/services/workspace_save.rs" /> and return results as `ManagerMessage` values, state updates, or pending subscriptions. Root-console side effects that still depend on the root application crate are isolated under <RepoFile path="src/console/services.rs" /> and its children: browser launch, config persistence for role/workspace/settings saves, Docker-backed instance discovery, 1Password probing/read validation, op-picker metadata loading/cache invalidation, role repository registration, trusted-role persistence, agent resolution, Claude token minting, and workspace-save drift/cleanup workers. Pure auth-kind/config-mode derivation, role-source derivation, launch provider derivation, instance-refresh snapshot data, workspace-choice construction, shared mount draft construction, synthetic current-directory mount construction, global-mount naming and scope parsing, workspace-save prospective mount merging, save-preview validation message text, and workspace-edit delta construction live in <RepoFile path="src/console/domain.rs" />. | Keep moving the remaining root-console app/message/update/view plumbing into the `jackin-console` TUI boundary and replace the remaining modal/input service bridges with typed effects/messages. |

## Remaining work

Latest console cleanup: agent-picker choice resolution now runs through the root effect executor before `src/console/tui/prompts.rs` opens inline prompt state. The prompt helper owns terminal drawing and state transitions only; Docker-backed agent resolution stays behind `src/console/effects.rs` and `src/console/services/agents.rs`.

The shared confirm dialog in <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" /> now exposes only generic prompt/detail/note rendering. Console and launch role-trust flows build their role/repository text in their owning surface code instead of storing role-specific product semantics in `jackin-tui`.

Reusable console mount-row formatting and width/height calculations now live in <RepoFile path="crates/jackin-console/src/mount_display.rs" />. The root renderer keeps only adapters that translate root `MountConfig`, global-mount rows, and mount-info cache labels into the crate-owned display inputs.

Modal size and placement rules now flow through the surface-local rect-mode and rect-spec helpers in <RepoFile path="crates/jackin-console/src/tui/components/modal_rects.rs" />. Root modal layout code maps root `Modal` variants to crate-owned modes and supplies only dynamic facts such as picker counts, confirm height, and error height instead of owning generic centered-rect decisions or fixed modal dimensions itself.

Settings screen view geometry now owns the reusable global-mount scroll-frame calculation in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />; root settings geometry supplies only root mount rows and cache-derived content width.

Settings frame layout plus General-tab, Mounts-tab, Environment-tab, Auth-tab, and Trust-tab line composition now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root rendering passes only selected/hovered row state, pending toggle values, formatted mount rows, pending environment/trust rows, root value lookup adapters, auth mode/source display facts, and focus visibility instead of owning the screen-specific row styling itself.

Settings environment key text-input construction now lives in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input passes only the pending environment map, target scope, label, and initial value instead of owning duplicate-key validation labels for settings-env modals.

Global-mount confirm state, global-mount scope-picker state, global-mount text-input state, and settings environment value text-input state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input wraps those crate-owned state objects in root modal variants instead of owning the product-specific prompt/title/input-state construction itself.

Settings environment source-picker state, scope-picker state, and delete-confirm state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input keeps routing key commits and pending picker values, while the screen helper owns the reusable component state for the environment modal chain.

Workspace-list row sizing now includes the generic list-name content-width fold in <RepoFile path="crates/jackin-console/src/tui/list_geometry.rs" />; root list geometry maps manager rows to widths and delegates the viewport floor calculation.

Workspace-list split geometry and list-name scroll clamping also live in <RepoFile path="crates/jackin-console/src/tui/list_geometry.rs" />. The root list geometry keeps selection/state mapping and delegates the reusable column allocation and scroll-limit mechanics.

Workspace-list mouse split geometry, seam hit-region rules, mouse-wheel step policy, scrollbar drag offset planning, point-in-rect hit-testing, mouse-wheel scroll application, tab-strip hit-testing, and tabbed content-area geometry now live in <RepoFile path="crates/jackin-console/src/tui/layout.rs" />. Root mouse input keeps root stage/modal hit-testing and delegates minimum drag width, seam columns, seam proximity, Ratatui-matched pane dimensions, drag percentage math, rectangle containment, scrollbar track-position math, scroll-delta viewport math, tab-row geometry, and header/tab/footer rectangle math to the console TUI boundary.

Workspace-list body rectangle and visual-row mouse hit-testing now also live in <RepoFile path="crates/jackin-console/src/tui/layout.rs" />. Root key/mouse input maps the crate-owned geometry result back to root `ManagerListRow` values but no longer owns the generic list-content border math.

Sidebar scroll-focus liveness now lives in <RepoFile path="crates/jackin-console/src/tui/sidebar_layout.rs" />. Root list geometry maps its `MountScrollFocus` enum into the crate-owned focus vocabulary and no longer owns the generic "is the focused sidebar area still scrollable?" rule.

Capsule initial attach-frame labels now live with attach-client effect vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/effect.rs" /> instead of daemon internals. The daemon still assembles and sends the actual protocol frames, but the visible first-attach burst names are owned by the TUI boundary.

Capsule visible multiplexer mode vocabulary (`Normal`, prefix-await, dialog, drag, selection) now lives with the visible TUI model in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" /> instead of a root crate module.

Capsule mux-mode priority, chrome-hover priority, pointer-shape priority, cursor-visibility priority, and drag-resize ratio math now live under <RepoFile path="crates/jackin-capsule/src/tui/app.rs" /> and <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. Daemon mouse/dialog code supplies the current protocol/session facts and applies the resulting action, but no longer owns those visible-state decisions.

Capsule dialog copy-feedback timeout now lives with visible redraw/update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still owns the timer deadline and clears dialog feedback when it expires, but the UI-visible duration is no longer daemon-owned.

Capsule dialog state construction for rename-tab, split-direction, close-target, confirm-action, container-info, and GitHub-context dialogs now lives with the dialog component in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" />. The daemon still gathers session/container/PR facts and executes terminal actions, but no longer owns those reusable visible dialog defaults.

Capsule content-row reservation, content-rect derivation, split-spawn pane sizing, and main-view hint row reservations now live with visible pane geometry in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. The daemon still owns capacity checks, PTY/session spawn, and tab-tree mutation, but the future half-pane size calculation is a TUI layout helper.

Capsule pane-local mouse coordinate mapping now lives with visible pane geometry in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. The daemon still gates mouse forwarding on the focused session's protocol mode and writes encoded mouse bytes to the PTY.

Capsule mouse text-selection start-state construction now lives with selection state, motion, and copy helpers in <RepoFile path="crates/jackin-capsule/src/tui/selection.rs" />. The daemon still finds the target pane and gates selection on the session's mouse protocol mode.

Capsule visible pane-list derivation now lives with the visible multiplexer model in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />. The daemon still supplies active tab, focus, zoom, and content-rect facts before rendering.

Capsule parsed-input and prefix-command to semantic-action mapping now lives with action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon still supplies live context such as dialog-captures-input and branch-bar hit-testing, then executes the returned action against PTY/session state.

Capsule input binding data, key-binding parsing, escape-time defaults, and SGR passive-motion vocabulary now live with terminal input parsing in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />. The environment/logging resolver for those TUI-owned binding definitions lives under the non-TUI services boundary in <RepoFile path="crates/jackin-capsule/src/services/input_bindings.rs" />.

Capsule tab double-click timing now lives with terminal input parsing in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />. The daemon still stores the previous tab click timestamp and opens the rename dialog, but the TUI-owned gesture threshold is no longer daemon-owned.

Capsule render, state, branch-context, and pull-request-context ticker intervals now live with subscription vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/subscriptions.rs" />. The daemon still owns the runtime timers and background context workers, but their TUI tick cadence is no longer daemon-defined.

Op-picker account/vault/item/field display metadata, query/filter helpers, render trait and renderer, selected-index routing, naming-stage input routing, filter-reset selection routing, pane-refresh planning, account-stage navigation planning, vault-stage navigation planning, item-stage navigation planning, field-back navigation planning, field-stage commit routing, field-label commit routing, existing-field commit routing, section-stage navigation planning, section-collapse routing, naming-stage transition planning, field-row derivation, and the session cache type now live with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. Root `operator_env` keeps compatibility aliases while the root state adapter implements the crate-owned render trait, and `op` CLI execution, blocking subscriptions, and cache invalidation remain in non-TUI service/root adapter code.

Console debug key-name formatting now lives in <RepoFile path="crates/jackin-console/src/tui/debug.rs" />. The root debug adapter still owns root-state location labels and supplies whether a focused widget is consuming text input.

Editor screen frame/body layout, General/Mounts/Roles/Secrets/Auth-tab line composition, row-width primitives, and scroll clamping now flow through <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor geometry still derives root-specific content widths/heights, General-tab values, root-formatted mount rows, role allowed/default state, secret value/registry lookups, and auth mode/source display facts from workspace config and mount-info cache data, then delegates viewport/body geometry and screen-specific row styling to the screen view helper.

Editor secret key text-input construction now lives in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input derives forbidden keys from root workspace/role data, then delegates label, duplicate-validation, and scope-specific forbidden-label setup to the editor screen helper.

Editor name, secret value, role-load, and mount-destination text-input state plus secret delete confirm state, secret scope-picker state, workdir picker state, and mount-destination choice state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input keeps choosing which modal variant opens and carrying root targets, while screen-owned helpers build the reusable prompt labels and component state.

Editor new-secret value text-input state and secret source-picker state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input still routes the selected secret scope/key and op-availability flag, but no longer constructs the source picker or "Value for ..." text input directly.

Editor isolated-state save confirm state now lives in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root save input still detects drift and carries the cleanup plan, while the screen helper owns the operator-facing preserved-state prompt text.

Workspace editor auth/secret and settings environment flat-row adapters now live in <RepoFile path="src/console/tui/state.rs" /> while generic row derivation, editor role allow/default toggles, editor-secret key lookup/mutation plus secret row action/Enter planning, and settings-env key lookup/mutation plus row action/Enter planning, mask toggling/delete-row mutation stay in <RepoFile path="crates/jackin-console/src/tui/screens/editor/update.rs" /> and <RepoFile path="crates/jackin-console/src/tui/screens/settings/update.rs" />. Root input, geometry, render, and footer code share those adapters instead of carrying duplicate wrappers.

Workspace-list, create-prelude, destructive-confirm, editor/settings row, workspace/settings mount-row, and modal footer hint composition now live with console-local footer hint components in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root footer code derives row facts such as picker mode, instance snapshot availability, saved-workspace state, scroll focus, GitHub URL availability, and active modal mode, then delegates shared hint ordering and labels to component helpers.

Workspace-secret and settings-environment footer hint fragments now also live in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root footer code decides whether the focused row is plain, `op://`, add sentinel, or role header, then delegates the shared hint labels and ordering to crate-owned helpers.

Root modal footer rendering now maps root modal variants to the crate-owned `ModalFooterMode` vocabulary in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root code keeps only root-state inspection such as file-browser state, auth token-generation availability, and modal payload facts.

Shared editor/settings auth-source display vocabulary now lives with the shared row helpers in <RepoFile path="crates/jackin-console/src/tui/components/editor_rows.rs" /> so screen views can render masked literals, `op://` breadcrumbs, unset credentials, and "not required" rows from root-supplied display facts.

Shared auth source-picker state and credential text-input state helpers now live with the auth-panel component in <RepoFile path="crates/jackin-console/src/tui/components/auth_panel.rs" />. Root editor/settings auth input keeps carrying root modal parent chains and token-generation markers, but no longer duplicates the shared `generated token`, environment-variable, and `Credential` input construction.

Workspace-list mount row rendering for workspace mounts and global mounts now lives with console-local mount row components in <RepoFile path="crates/jackin-console/src/tui/components/mount_rows.rs" />. Root list rendering still gathers root workspace/global mount data and mount-info labels, then passes crate-owned display rows into the component helpers.

Workspace and settings save-confirm preview line composition now lives with the console-local save-preview component in <RepoFile path="crates/jackin-console/src/tui/components/save_preview.rs" />. Root save-preview code maps root workspace/settings state, mount-info labels, and `EnvValue` breadcrumbs into display-only snapshot structs before calling the component renderer. Save input handlers still decide when to open the preview modal and which save effect to request, but no longer own save-preview Ratatui line construction.

Workspace delete confirm state and instance purge confirm state now live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root manager update selects the next stage and carries root identifiers, while the screen view helper owns the workspace-list confirm prompt text.

Create-prelude destination text-input state, workspace-name text-input state, mount-destination choice state, and workdir-picker state now live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root prelude input still owns the wizard transition order and root mount draft construction, but delegates reusable prompt labels and component state construction to the workspaces screen helper.

Workspace-list running-instance summary rendering and the `+ New workspace` right-pane description now also live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root list rendering still selects the active row and supplies instance count/expanded state, but no longer owns those workspace-screen visual panes directly.

1. **Classify existing modules.** For each root-level file in the three surface crates, decide whether it is TUI, pure non-visual domain logic, or side-effect/service/runtime logic.
2. **Capture the current behavior before moving code.** Identify the existing tests, snapshots, and manual-smoke checks that prove the affected workflow. Add focused coverage first if a workflow has no useful guard.
3. **Move TUI files under `src/tui/`.** Preserve behavior while moving files into `app.rs`, `message.rs`, `update.rs`, `effect.rs`, `subscriptions.rs`, `run.rs`, `terminal.rs`, `view.rs`, `components/`, and `screens/` as appropriate.
4. **Keep non-TUI code outside.** External commands, filesystem/network/config/credential work, workspace resolution, and capsule protocol/session authority stay in root modules or future domain/service modules outside `src/tui/`.
5. **Replace direct work with effects.** If TUI update/render/component code needs Docker, git, `op`, `gh`, filesystem, network, config, or credential work, it returns a typed effect instead.
6. **Verify behavior after each move.** Run the affected unit tests, render snapshots/lookbook checks, docs link checks when docs changed, and focused manual terminal smoke for behavior a `TestBackend` cannot prove, especially console -> launch -> capsule handoff, raw-mode restore, mouse/focus behavior, and capsule attach/resize.
7. **Promote reusable visuals deliberately.** Surface-local components stay under the owning surface. Cross-surface components move to `jackin-tui` with lookbook coverage and snapshot/test coverage.
8. **Update docs as files move.** Keep [TUI Design Decisions](/reference/tui-design-decisions/) and [Codebase Map](/reference/codebase-map/) aligned with the new module shape.

## Acceptance criteria

- Each TUI surface has a thin `src/tui.rs` module root and the TUI application layer under `src/tui/`.
- `jackin-console` uses `src/tui/screens/<screen>/` only because it has multiple meaningful screens.
- `jackin-launch` and `jackin-capsule` do not grow `screens/` unless they gain multiple distinct screens.
- Existing operator-visible behavior is preserved unless an implementation PR explicitly documents and justifies a behavior change.
- TUI update/render/component code performs no external command execution, filesystem/network/config persistence, credential handling, or capsule protocol/session authority.
- External work requested by the TUI travels through typed effects/subscriptions and returns as typed messages.
- Shared visual primitives live in `jackin-tui`; surface-specific visual composition stays in the owning surface crate.
- Module-root files stay thin; no `lib.rs`, `tui.rs`, or other module root becomes a home for rendering, dispatch, state machines, or domain logic.
- Every implementation slice identifies the pre-existing behavior it protects and verifies it after the structural move with tests, snapshots, link checks, or manual terminal smoke as appropriate.

## Documentation impact

This is contributor-facing architecture work. Keep the following docs synchronized as the implementation moves:

- [TUI Design Decisions](/reference/tui-design-decisions/) for binding TUI conventions.
- [Codebase Map](/reference/codebase-map/) for current module ownership.
- Component lookbook pages when reusable `jackin-tui` components are added or changed.

No operator- or role-author-facing page needs to change unless a later implementation changes visible behavior or commands.

## Host-side effects

None. This roadmap item describes in-repo TUI structure only. It introduces no new host writes, no new container paths, and no launch-time host mutation.
