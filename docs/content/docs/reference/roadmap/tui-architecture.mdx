---
title: "TUI architecture — Ratatui application patterns"
---

**Status**: Substantially implemented — Phases 0–9 and key Phase 5 goals are complete on `feature/tui-architecture`; Phases 8c, 8d, and 8e are complete on 2026-05-31. Phase 10 (console/launch crate extraction) and Phases 8b and 8f remain as planned follow-up work.

**Completed:**
- All host `ManagerMessage` update paths are wired: list navigation, editor/settings tabs, mouse selection, modal open/dismiss, inline picker dismissals, instance action errors.
- Render paths are immutable: scroll geometry prepared before draw, modal spinner/confirm-save geometry computed in update, `MountInfoCache` consumed read-only during render.
- Async throughout: `EventStream` + `tokio::select!` replaces the 50ms blocking `event::poll`; deploy-hold sleep is `tokio::time::sleep`; all background work uses `spawn_blocking` + oneshot — no nested Tokio runtimes.
- Modal stack convergence: `AuthFormReturnPath` stash slots retired; `EditorState.pending_env_key` retired (env_key embedded in `Modal::SourcePicker { env_key }`); auth parent stack is `Vec<SettingsAuthModal>`.
- Module-root files thin: `console/mod.rs` (1729→628 lines) via `console/terminal.rs`, `console/prompts.rs`, `console/run.rs`; `jackin-tui/lib.rs` (1040→740 lines) via `geometry.rs`.
- Capsule fully Ratatui: `SocketBackend` custom backend serializes `Buffer::diff` to the attach socket; `compose_ratatui_frame()` renders chrome, pane bodies, and ALL dialog overlays (via `dialog_widgets.rs` using shared `jackin-tui` components) — raw-ANSI path is fallback only.
- `daemon.rs` (8496→3777 lines) decomposed into 9 submodules under the Rust 2018 `foo.rs + foo/` layout, each owning one responsibility.
- ADR-004 written: custom cell widget chosen over tui-term (fork incompatibility); benchmark ~378µs vs ~118µs raw-ANSI baseline.
- `criterion` frame-time bench + capsule bytes-per-frame bench guard CI.
- Lookbook: 14 stories, real `TestBackend`-backed SVG previews, CI drift check; `unstable-widget-ref` feature removed.
- `insta` snapshot tests for 4 key render paths.
- Terminal resize correctness: capsule resize now resets the Ratatui double-buffer and `SocketBackend` style tracker before repaint; resize regression tests cover the repaint path.
- Focus audit progress: editor General and settings Mounts now use the same focused expression for green borders and `▸` cursor visibility; focused editor General content has a regression test.
- Container info is unified across surfaces: shared `jackin-tui` state/render/hit-test/OSC 8 overlay helpers back the host console and launch cockpit; capsule status-bar chip click opens the matching container-info dialog; OSC 52 copy works for copyable rows.
- Launch failure paths now render as full wrapped plain text and receive OSC 8 `file://` hyperlink overlays for supporting terminals; overlay tests assert both diagnostics and docker-output links.

**Resolved (Phase 5):** Typed session spine substantially complete. `run_console` accepts `parent_session: Option<&TerminalSession>` and uses `is_active()`. `RichRenderer` stores `entered_alt_screen: bool` at construction, eliminating two global reads. The `host_screen_owned` global remains as the ambient signal for four deep-call-stack subsystems (`runtime/attach.rs`, `tui/animation.rs`, `docker.rs`, `operator_env.rs`) where threading a typed reference would change ~12 function signatures for minimal benefit. Accepted as permanent ambient state. — `run_console` now accepts `parent_session: Option<&TerminalSession>` and uses `is_active()` instead of the global for its own screen-ownership decision. `RichRenderer` now stores `entered_alt_screen: bool` at construction and uses the stored flag in `Drop`/`with_raw_mode`, eliminating two global reads from the renderer lifecycle. The `host_screen_owned` global remains as the ambient query mechanism for subsystems that don't receive a session reference (`runtime/attach.rs` for `JACKIN_HOST_ALT_SCREEN`, `tui/animation.rs` animation helpers, `docker.rs`, `operator_env.rs`). `TerminalSession` owns the terminal lifecycle and manages the global; eliminating the global entirely would require threading the reference through the full launch stack.

## Goal

jackin' renders three connected terminal surfaces: the host operator console (`jackin console`), the launch progress cockpit, and the in-container multiplexer (`jackin-capsule`). The first two now use Ratatui, and the console-triggered launch path now keeps one host alternate screen across console → launch → capsule attach. That shipped backbone is the right product direction, but the architecture behind it is still not the Ratatui application pattern this codebase should standardize on: the host console remains a procedural draw-everything-every-frame loop over god-structs, the launch cockpit owns a parallel mini-app under <RepoFile path="src/runtime/progress.rs" />, and the capsule still uses **no Ratatui at all** — it hand-rolls ANSI escape writes and a row diff that re-implements Ratatui's own `Buffer::diff`.

This item defines the **architectural target** for all three surfaces: a clear Model / Update / View separation, an explicit `Action`/`Message` type instead of inline state mutation in event handlers, a pure view layer with no I/O, state decomposed into domain vs. UI vs. ephemeral concerns, `StatefulWidget`-based reusable components, and background I/O that feeds the update loop through a channel rather than blocking the draw. It also commits `jackin-capsule` to **rendering through Ratatui** instead of its current hand-rolled ANSI, so the in-container multiplexer shares the same component library as the host. Throughout, the organizing goal is **reusability**: a single, recognizable component vocabulary that every terminal surface shares, instead of the parallel copy-paste implementations the codebase accumulates today.

This is a north-star design item, not a big-bang rewrite. The work lands incrementally, one surface and one layer at a time, behind the snapshot-test regression net, with **no operator-visible behavior change** at any step.

## What this roadmap must deliver

This item exists to make future TUI work obvious to place, easy to read, and hard to block at runtime. Every phase below should be judged against these three outcomes:

| Outcome | Target | How an implementation proves it |
|---|---|---|
| **Clear structure** | Every TUI concern has one obvious owner: app session/runtime, app model, update/effects, reusable components, surface-specific domain state, terminal capability handling, and tests/lookbook. | A contributor changing a footer, modal, launch stage, capsule dialog, or async refresh knows which module owns it without searching unrelated surfaces. `src/runtime` no longer imports `src/console/widgets`; capsule UI code no longer lives as raw ANSI copies beside daemon business logic. |
| **Understandable, idiomatic Ratatui code** | The host console, launch cockpit, and capsule all read like Ratatui applications: typed messages, central update, pure view, `StatefulWidget` components, explicit effects, decomposed state, and snapshot-backed render tests. | Event handlers do not directly mutate god-structs; render functions do not take `&mut state`; repeated visual patterns are named `jackin-tui` components; module boundaries follow Model / Update / View and component seams rather than file-size-only splits. |
| **Non-blocking UX** | The TUI keeps responding and animating while Docker, git, 1Password, filesystem, capsule socket, diagnostics, and launch work is in flight. | Long-running work runs as typed effects or subscriptions and reports back through messages. The only draw owner is the TUI runtime. No disk, socket, subprocess, or sleep runs inside render or on the update path unless it is demonstrably trivial and bounded. |
| **One consistent way** | Every TUI surface is built with the same architecture, vocabulary, and components — there is exactly one way to do each TUI thing, project-wide. | A new dialog, event route, border style, or animation uses the project's single existing pattern; no surface carries a second concept for something already done once. Consistency is a hard rule, not a preference (see Design principles). |

The intended operator-visible result is not a redesign. The screens should look the same unless a later UX roadmap item deliberately changes them. The visible improvement from this item is responsiveness and consistency: no frozen progress cockpit, no event loop blocked by refresh work, no host/capsule drift in common components, and no terminal handoff flash introduced by the refactor.

One UX behavior is part of the architecture, not optional polish: every surface must have a single visible focus owner. The active interaction container gets the bright `PHOSPHOR_GREEN` border; background and sibling containers use the dark border. Opening a dialog moves the bright border to that dialog; closing it restores the bright border to the underlying focused block; clicking a scrollable detail block moves the bright border from the workspace list to that block. The component library and state model must make this focus-visible rule the default, matching [TUI Design Decisions](/reference/tui-design-decisions/#focus-visible-border-one-active-container-owns-bright-green/).

## Scope — one continuous Ratatui experience

This item covers **every terminal surface jackin' renders**, unified under Ratatui and one shared component library:

1. **The operator console (`jackin console`)** — the full flow from console start through workspace / role / agent selection. Already Ratatui; this item restructures it (see the host target below).
2. **The loading flow (console → launch → container).** The [Launch Progress TUI](/reference/roadmap/launch-progress-tui/) work shipped the product backbone for this: a Ratatui cockpit, structured stages, diagnostics run IDs, a console-owned host alternate screen, and a capsule handoff flag that prevents nested alt-screen toggles. The remaining target is to make that shipped backbone architectural rather than ad hoc: one app-level TUI session model, one update/effect boundary, one shared component library, no direct imports from `src/console/widgets` into runtime launch code, and no split where launch progress is a separate mini-app beside the console.
3. **Ratatui inside the container.** The `jackin-capsule` multiplexer daemon renders the in-container experience. It moves from hand-rolled ANSI to **rendering through Ratatui** (the attach client stays a thin byte relay — see the capsule target below).
4. **The capsule, fully Ratatui.** Every capsule surface — pane chrome, status bar, tab strip, dialogs, pickers, confirms — becomes a Ratatui widget, and those widgets are the *same* shared components the host console uses.

The connective tissue across all four is the shared design-component crate (`jackin-tui`): one `ConfirmDialog`, one `FilterList`, one `TabStrip`, one `Panel`, one `HintBar`, reused everywhere rather than reimplemented per surface — catalogued and rendered in a browsable **lookbook** (the design-system section below).

## Design principles

**Reusability is the first principle of this item, not one of several — every other goal here is downstream of it.** The single biggest source of TUI entropy today is copy-paste: the same confirm dialog, picker, tab strip, scrollbar, and palette wrapping exist as parallel implementations across the host console and the capsule, kept consistent only by the procedural rules in [TUI Design Decisions](/reference/tui-design-decisions/). Ending that failure mode is the whole point of this item. The non-negotiable rule: **no code is duplicated and no component is copied. Anything — widget, block, dialog, layout, or UI logic — that can appear more than once is implemented exactly once and instantiated everywhere it is needed, with one source of truth, so a change in that one place changes every instance.** When new functionality would repeat existing code, the work is to refactor the existing thing into a reusable component *first* and then use it — never to paste a second copy and "keep them in sync." We do not tolerate multiple places where the same thing works slightly differently. Every TUI improvement under this item begins with the question "what is the reusable shape?", and that includes the whole **transition experience** (console → loading → capsule → exit), which must itself be composed from shared components, not per-surface copies. The structural fix is to make reuse the path of least resistance — a real design-component crate, not a rulebook.

- **Consistency is a hard rule — one approach, project-wide.** Every TUI surface is built the *same way*: the same Model/Update/View shape, the same `Message` / `Effect` vocabulary, the same `jackin-tui` component library, the same token-based styling path, and the same focus / scroll / modal / animation semantics. There is exactly one way to add a dialog, route an event, style a border, or animate a surface — not a host way and a capsule way and a launch way. Reintroducing a second concept for something the project already does once is a defect even when it works, because it is precisely the entropy this item exists to remove. When two surfaces need the same behavior they share the one implementation; when a new need appears the existing pattern is extended, never forked. A genuinely new pattern requires operator sign-off, exactly as a new component does. This is a hard rule, not a preference: an inconsistency is a bug to be fixed, not a style to be tolerated.
- **One pattern, one component — zero duplication.** If a visual pattern, widget, block, or piece of UI logic can appear twice, it is a single shared implementation with one source of truth, instantiated per use site — never a second copy kept in sync by hand. Copying code to produce a variant is forbidden; the correct move is to parameterize or extend the one implementation so all instances advance together. This operationalizes the existing "Component Reuse — Strict Rule" in [TUI Design Decisions](/reference/tui-design-decisions/) structurally rather than by review discipline: with the shared component crate in place, the duplication becomes hard to introduce, not merely discouraged.
- **One design system, every terminal surface.** The host console, launch cockpit, and capsule render the same brand language (PHOSPHOR palette, tight chrome, footer-only hints, identical confirm / picker / modal shapes). They must consume the *same* components from the *same* crate, so a confirm dialog is literally the same code everywhere — not two copies a rule says must match.
- **Frontend-team workflow, terminal edition.** Agents should work like a frontend team using Storybook and a component library: pick an existing component from the catalog, pass props/state, wire messages/effects, and ship. They should not invent a new dialog, picker, footer, list row, or panel while implementing a feature.
- **Recognizable, not bespoke.** A contributor (or agent) reading the code should recognize the component vocabulary immediately: `Panel`, `ScrollableBlock`, `ConfirmDialog`, `FilterList`, `TabStrip`, `HintBar`, `Modal`. New screens compose existing components; a genuinely new component is added only when none fits, with operator sign-off per the strict-reuse rule.
- **Idiomatic Ratatui.** Components are `StatefulWidget` / `WidgetRef` implementations with their own `State`, the shape the Ratatui ecosystem recognizes and the [component template](https://ratatui.rs/templates/component/project-structure/) is built around — so the codebase reads like a Ratatui app, not a one-off renderer.
- **One source of truth for tokens.** Color values, spacing, tab gap, and hint formatting are defined once and converted per surface (raw-ANSI `Rgb` today, `ratatui::Color` for widgets), never re-declared inline.
- **Async-first rendering — never block the loop.** Every animated or progress-bearing surface keeps drawing on its own cadence *while work is in flight*. Rendering and long-running work (Docker calls, image pulls, network waits, git, daemon round-trips, disk inspection) run on independent async tasks: an interval-/`Message`-driven loop repaints shared state, and the work mutates that state from a `tokio::select!` arm, a `tokio::spawn`d task, or `spawn_blocking` — never inline with the draw. A surface that only repaints *between* sequential blocking steps freezes the instant a step is slow: the digital rain stalls, the active-stage pulse stops, the spinner hangs, and the operator cannot tell whether jackin' is working or wedged. Steady motion regardless of how long the underlying step takes is what makes the TUI feel native, snappy, and alive — and that "snappy and alive" feel is an explicit operator goal for this item, not a nice-to-have side effect. The acceptance test for any new long-running step on an animated surface: *does the animation keep moving smoothly while the step runs?* If it stutters, the step is on the render loop and must move off it. The concrete sites to migrate are enumerated in the [blocking-work inventory](#blocking-work-inventory--async-migration-targets) below.

## Rust engineering practices this refactor must follow

The architecture above is only as strong as the Rust it is written in, so this item is explicit about the idioms every phase is reviewed against — "restructure for readability" then has a concrete standard, not a taste argument. These mirror the project's Rust-best-practices guidance and extend, never weaken, the repo-wide rules in <RepoFile path="AGENTS.md" /> (DRY / reuse-before-writing, prefer maintained crates over hand-rolled parsers and format handlers, comment only the non-obvious *why*). They are grouped so a reviewer can scan by concern.

**Ownership & allocation**

- **Borrow by default; own only when you keep it.** Take `&str` over `String`, `&[T]` over `Vec<T>`, `&Path` over `PathBuf`, and `Option<&T>` over `&Option<T>`. Take ownership only when the function stores, moves, or sends the value — let the caller decide where a clone happens.
- **`.clone()` is a design decision, not a borrow-checker escape.** The pure view takes `&Model` / `&UiState` and builds from borrowed data; avoid per-frame `String` / `Vec` / `Span` churn. Use `Cow<'_, str>` and pre-derived rows where a clone is otherwise implied every draw — the current host renderers rebuild picker `ListItem` vectors and mount-row `format!` chains on every frame, which is exactly what this removes.
- **No needless intermediates, but clarity wins over cleverness.** Skip throwaway `Vec` / `String` allocations where an iterator, slice, or borrowed view is enough; but prefer plain control flow over a combinator chain that hides branching, errors, ownership, or side effects.

**Type-driven design — make illegal states unrepresentable**

- **Enums over boolean soup.** The capsule's scattered `dialog_open()` / `drag.is_some()` / `selection.is_some()` checks collapse into one `MuxMode`; the host's three coexisting modal shapes converge on a single `Vec<Modal>`; focus is one `Focus` enum. If two fields can never be true at once, they are one enum.
- **Newtypes over primitive obsession.** Typed ids — `ComponentId`, `SessionId`, `PaneId`, and a `Generation` / `RequestId` for cancellation — replace bare `u64` / `usize`, so a stale background result cannot mutate a newer screen and a pane id cannot be passed where a session id is expected.
- **Props are validated data with `Default` + builders.** Component `Props` structs implement `Default` and are populated by a builder or struct-update syntax, so a call site sets only what differs — no eight-positional-argument render functions.
- **Implement the common traits where they carry meaning** — `Debug`, `Clone`, `Default`, `Display`, `Error`, `From` / `TryFrom`, `AsRef` — and follow the `as_` / `to_` / `into_` conversion conventions.

**Errors & panics**

- **`Result` for expected failure, `Option` only for detail-free absence.** The shared `jackin-tui` crate exposes typed (`thiserror`) errors; `anyhow`-style errors stay at the binary / app boundary — the `jackin` and `jackin-capsule` mains and the CLI dispatch. Propagate with `?`, adding context at IO / socket / subprocess / parse boundaries; background failures become typed `Message`s routed to the one shared error component.
- **The loop never panics.** No `unwrap` / `expect` / `panic!` / `todo!` on any render, update, or effect path. Reserve them for tests and genuinely-unreachable invariants, and only with a precise `expect` message. Public component and effect APIs document `# Errors` (and `# Panics` / `# Safety` where relevant).

**API design & boundaries — the `jackin-tui` surface**

- **Narrow the public surface.** `pub(crate)` is the default; the only stable `pub` API is the design crate's component contract. Keep struct fields private unless the representation is part of the contract, and use sealed traits where a downstream impl would freeze future evolution. Dovetails with [pub(crate) visibility](/reference/roadmap/pub-crate-visibility/).
- **No accidental compatibility commitments.** No public dependencies, re-exports, blanket generic impls, or `serde` derives on the shared crate unless they are intentional and documented as stable.
- **Traits define the seams.** Components implement `StatefulWidget` plus a small `Component` trait (`update` / `handle_event` / `render`) so the renderer, the wider ecosystem, and the [Ratatui component template](https://ratatui.rs/templates/component/project-structure/) all recognize the shape. Prefer generics with bounds for zero-cost reuse (`FilterListPicker<T>`); reach for `dyn` only where heterogeneity is real, such as the modal stack. `#[must_use]` on `Effect` / `Outcome` types so a side-effect request cannot be silently dropped.
- **Reuse the canonical crate; never re-implement what one owns.** Per <RepoFile path="AGENTS.md" />, the capsule's hand-rolled `PaneBodyCache` row diff re-implements `ratatui::buffer::Buffer::diff` and is deleted in favor of it; scroll math already moved behind `tui-scrollbar` (the merged <RepoFile path="crates/jackin-tui/src/scroll.rs" /> wraps it) and is the model to follow; text input consolidates on the in-house `TextField`, dropping the pre-1.0 `ratatui-textarea`.

**Concurrency**

- **One runtime, explicit cancellation.** Effects are spawned tasks that carry a cancellation handle or generation id; a `tokio::select!` cancellation branch beats ad-hoc "is this result still relevant?" checks. No mutex is held across an `.await`, and no code path builds a nested runtime to run a single future (see the [performance anti-patterns](#async-anti-patterns-the-grounding-pass-found)).

**Tests & documentation**

- **Tests ship with the component, at the stable boundary.** No component is canonical without `TestBackend` snapshot renders of every state and unit tests that drive `update` from messages — behavior tests at the boundary, not tests that freeze incidental helper signatures. Snapshots stay deterministic and redacted.
- **Docs that stay true.** Doc-test examples on public components propagate with `?`, not `unwrap`. Each promoted module opens with a `//!` line stating its one responsibility; public items carry `///` docs. Comments name the non-obvious *why* — invariant, ordering, platform quirk — never narrate the code (repo rule).

**Lints & suppression**

- **Clippy as a design-review assistant, enforced in CI, not by reviewers' memory.** CI runs `cargo fmt --check` and `cargo clippy --all-targets --all-features -- -D warnings`. Fix a warning before suppressing it; a justified suppression uses `#[expect(clippy::lint_name, reason = "…")]` so it is flagged when it goes stale.
- **No blanket strictness on the reusable crate.** Do not enable the `restriction` / `pedantic` / `nursery` groups wholesale, and do not add crate-level `#![deny(warnings)]` to `jackin-tui` — a future compiler warning must not break the library build. Apply strict lints selectively: the "loop never panics" rule is enforced by selectively denying the unwrap / panic lints in the TUI crates via the CI flag, not by a blanket group.

This grouped checklist is the review standard for every phase below; a phase that lands code violating a rule without an `#[expect]`-style documented exception is incomplete.

## Product goal — component library first

The end state should feel like a mature frontend codebase:

- A contributor opens the **TUI lookbook**, finds `ConfirmDialog`, `FilterListPicker`, `StatusFooter`, `ScrollablePanel`, or `TabStrip`, and uses it.
- Component pages show real rendered states, accepted props/state, emitted messages, keyboard/mouse contract, layout constraints, accessibility notes, screenshots/SVGs, and usage examples.
- The same lookbook is usable inside a terminal through `tui-lookbook --terminal`, with realistic sample content for every shared component and state so contributors see exactly how the widget looks on the black terminal background operators normally use.
- New feature work starts from "which existing component expresses this?" not "draw a block and spans."
- Design-system work has its own review path: add or change a component once, update its stories/tests, then consume it in product screens.
- Product screens compose components; product screens do not own palette constants, border glyphs, hint rendering, button styling, modal chrome, list-row highlight rules, scrollbar math, or text-input behavior.

The practical rule: **feature PRs may use components; component PRs may create components.** If a feature needs a new reusable widget, split that into a component-library step first unless the feature is a tiny one-off with a written reason. This mirrors frontend teams that keep design-system primitives separate from app screens, and it makes agents less likely to create near-copy UI.

### Component maturity ladder

Every repeated TUI pattern should move through this ladder:

| Level | State | Allowed use |
|---|---|---|
| 0 — scattered | Inline spans/layout in product code | Existing debt only |
| 1 — local helper | Helper in `src/console/widgets` or capsule raw ANSI | May be used by one surface, not copied |
| 2 — shared model | Shared state/input model in `jackin-tui`, per-surface renderer | Good for capsule-before-Ratatui transition |
| 3 — shared Ratatui widget | `StatefulWidget` / pure renderer in `jackin-tui` | Default for host + launch |
| 4 — catalogued component | Widget has stories, snapshot/SVG lookbook, usage docs, tokens, states | Required before broad reuse |
| 5 — cross-surface canonical | Host, launch, capsule all consume one implementation | Target state |

This lets the migration be incremental without pretending partial sharing is complete.

### Component contract

Each `jackin-tui` component should expose the same shape:

| Contract piece | Purpose |
|---|---|
| `Props` | Immutable inputs from product model: labels, rows, mode, disabled state, severity |
| `State` | Local UI state: focus, filter text, scroll offset, hover, caret, selected row |
| `Message` / `Outcome` | Semantic output: `Selected(id)`, `Cancelled`, `FilterChanged`, `Scrolled`, `Copied` |
| `Effect` hooks | Optional typed requests: clipboard copy, diagnostics note, async load trigger |
| `render(&Props, &State, area)` | Pure Ratatui render, no I/O, no mutation |
| `update(&mut State, ComponentEvent) -> ComponentOutcome` | Key/mouse/focus handling local to the component |
| focus-visible styling | Active component/container uses `PHOSPHOR_GREEN` border; inactive/background containers use `PHOSPHOR_DARK`; passive scroll blocks only claim the bright border when overflow makes scroll focus meaningful |
| stories/tests | Render all states through `TestBackend`; pin keyboard/mouse behavior |

Current code already hints at this contract: many widgets have `State`, `handle_key`, `render`, and `ModalOutcome`. The refactor should preserve that useful shape and promote it into `jackin-tui` instead of replacing it with a vague abstraction.

### Local audit — what is already reusable but misplaced

The codebase is not starting from zero. The useful shape already exists in fragments:

| Existing surface | Reusable thing hiding there | Migration target |
|---|---|---|
| <RepoFile path="src/console/tui/widgets/role_picker.rs" />, <RepoFile path="src/console/tui/widgets/github_picker.rs" />, <RepoFile path="src/console/tui/widgets/workdir_pick.rs" /> | filtered list picker with local state, filter input, selected row, `ModalOutcome` | generic `FilterListPicker<T>` with typed row IDs and section headers |
| <RepoFile path="src/console/tui/widgets/agent_choice/mod.rs" />, <RepoFile path="src/console/tui/widgets/source_picker.rs" />, <RepoFile path="src/console/tui/widgets/scope_picker.rs" /> | small option chooser with button/list focus | `ChoiceDialog<T>` / `SegmentedChoice<T>` |
| <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" />, <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" /> | shared host/launch confirm dialog plus capsule confirm action | extend `ConfirmDialog` with `severity`, `body`, and capsule-compatible variants once capsule renders through Ratatui |
| <RepoFile path="crates/jackin-tui/src/components/text_input.rs" />, <RepoFile path="crates/jackin-tui/src/lib.rs" /> | text-entry model plus rendered dialog | consolidate on `TextField` + Ratatui `TextInputDialog`; retire `ratatui-textarea` unless a feature gap remains |
| <RepoFile path="crates/jackin-tui/src/components/scrollable_panel.rs" /> | scroll math, selected-line render helpers, proportional scrollbars | extend the shared `ScrollablePanel` / `Scrollbar` adapters as new surfaces adopt Ratatui |
| <RepoFile path="crates/jackin-tui/src/components/hint_bar.rs" />, <RepoFile path="crates/jackin-tui/src/components/status_footer.rs" />, <RepoFile path="crates/jackin-capsule/src/tui/statusbar.rs" /> | footer hints, status bars, tab/chrome tokens | `HintBar`, `StatusFooter`, `TabStrip`, `BrandHeader` |
| <RepoFile path="crates/jackin-tui/src/components/error_dialog.rs" />, <RepoFile path="src/runtime/progress.rs" /> | shared red-border error modal for host and launch | add capsule usage once capsule renders through Ratatui |
| <RepoFile path="src/console/tui/widgets/file_browser/mod.rs" />, <RepoFile path="src/console/tui/widgets/op_picker/mod.rs" />, <RepoFile path="src/console/tui/widgets/auth_panel/mod.rs" /> | larger composed flows built from list, text input, status, footer, and modal primitives | keep app/domain state local; replace private drawing with shared primitives |

The current file layout already proves the frontend-style component model is compatible with the codebase: most widgets have a state object, a key handler, a renderer, and narrow tests. The roadmap should therefore prefer **promotion and generalization** over replacement. The first refactor pass should move those pieces into `jackin-tui` with minimal behavior change, then make product screens consume them.

### Refactor targets — remove these drift sources first

The same audit found concrete places where a component library would pay off immediately:

| Drift source | Evidence | Fix |
|---|---|---|
| Local palette constants | `Color::Rgb(0,255,65)` is re-declared in save/source/scope widgets, file browser, save input code, capsule dialogs/status bar, and runtime progress animation code | only token modules may define RGB values; all product code imports semantic tokens |
| Runtime imports console widgets | <RepoFile path="src/runtime/progress.rs" /> uses console widget modules for launch UI | move reused widgets into `jackin-tui`; runtime imports the design crate only |
| Capsule raw ANSI copies dialog chrome | <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" /> has its own `render_box`, filter input, row, hint row, confirm, and picker code | share models/tokens first; replace renderer once capsule has a Ratatui frame |
| Multiple picker implementations | host role/github/workdir pickers, launch select list, capsule palette/agent/split pickers | one generic picker primitive plus app-specific row adapters |
| Multiple button-strip styles | host confirm, confirm-save, scope, source, mount-destination, and save/discard buttons now consume `ButtonStrip`; capsule menus still carry raw-ANSI strips until Ratatui lands | Continue routing remaining button rows through `ButtonStrip` until no surface declares focused/unfocused button styles locally |
| Render mutates state | scroll clamping and geometry cache writes happen during draw | components expose `layout_snapshot`/`Geometry` outputs from update or post-render measurement, not hidden mutation in view |

### Proposed `jackin-tui` package layout

Keep the existing crate and grow it deliberately:

```text
crates/jackin-tui/src/
  tokens.rs          # Rgb, semantic tokens, spacing, glyphs, timing
  theme.rs           # Ratatui + ANSI adapters, terminal capability fallback
  geometry.rs        # centering, clamp, scrollbar, tab layout, text measurement
  runtime/
    message.rs       # shared event/message traits and common input events
    effect.rs        # typed side-effect vocabulary used by host runtime
    dirty.rs         # Dirty flags and redraw scheduler types
    subscription.rs  # ticks, terminal events, long-lived external streams
  components/
    panel.rs
    modal.rs
    hint_bar.rs
    status_footer.rs
    button_strip.rs
    text_input.rs
    filter_list.rs
    scrollable.rs
    tab_strip.rs
    confirm.rs
    error_dialog.rs
    file_browser.rs  # composed, only after primitives are stable
```

The `runtime/` module can start as small shared traits and types if moving the full host loop into the crate is too much at first. The important boundary is that product crates own domain data and effects, while `jackin-tui` owns reusable UI state machines, rendering, tokens, and layout math.

### Target ownership map

The refactor should leave a contributor-facing map like this. The exact filenames can evolve, but the ownership boundaries should not:

| Owner | Lives in | Owns | Must not own |
|---|---|---|---|
| App TUI session | `src/app/` plus shared runtime types in `jackin-tui` | terminal lifecycle, raw mode, alternate screen, mouse capture, diagnostics routing, suspend/resume, console → launch → attach handoff | product state, component drawing, Docker/git/1Password work inline |
| Host console app | `src/console/` | console domain model, workspace/editor/settings state, host-specific messages/effects, adapters from shared component outcomes to console actions | shared visual primitives, palette literals, blocking refresh work in render/update |
| Launch app state | <RepoFile path="src/runtime/launch.rs" /> / <RepoFile path="src/runtime/progress.rs" /> until folded into the app session | launch-stage domain state, launch-specific effects, forced choices, failure acknowledgement, build-log state | its own private TUI runtime, direct dependency on `src/console/widgets` |
| Capsule app | `crates/jackin-capsule/src/` | PTY/session model, mux mode, attach protocol, capsule-specific messages/effects, pane-body rendering decision | duplicated confirm/picker/tab/status implementations, raw ANSI chrome once Ratatui frame lands |
| Shared design system | `crates/jackin-tui/src/` | tokens, themes/adapters, geometry, reusable component state machines, Ratatui widgets, common runtime contracts, lookbook stories | host workspace semantics, container session lifecycle, direct terminal writes |
| Tests/lookbook | `tests/`, crate-local tests, generated docs assets | message/update tests, snapshot renders, component stories, Buffer/SVG generation, terminal handoff smoke notes | hand-drawn component previews that can drift from real widgets |

This map is the answer to "where does TUI code go?" New work under this roadmap should either move code toward one of these owners or explain why a temporary bridge is still needed.

### Component extraction order

1. **Freeze the component inventory.** Add a tracked table of components, owners, current call sites, states, and maturity level. New TUI work must name the component it uses or the reason it cannot.
2. **Promote tokens and geometry.** Move every inline RGB value, spacing constant, border glyph, scrollbar helper, tab layout helper, and centering helper behind `jackin-tui` tokens/adapters.
3. **Promote pure models.** `TextField`, `HintSpan`, `TabCell`, `ModalOutcome`, picker row IDs, button focus, and scroll state become reusable model types before renderers move.
4. **Promote primitives.** `Panel`, `ModalFrame`, `HintBar`, `StatusFooter`, `ButtonStrip`, `TextInput`, `FilterInput`, `ScrollablePanel`, `Scrollbar`, and `TabStrip` land in `jackin-tui` with `TestBackend` snapshots.
5. **Promote dialogs.** Rebuild confirm, error, text-input, filter-list, choice, save/discard, and status popups from primitives. Existing app-specific dialogs become thin props/state adapters.
6. **Consolidate launch.** Launch progress stops importing `src/console/widgets`; it consumes `jackin-tui` components and emits app messages/effects.
7. **Bridge capsule.** Capsule consumes shared tokens/models with raw-ANSI adapters until Phase 6 gives it a Ratatui frame.
8. **Replace capsule renderers.** Once capsule renders through Ratatui, delete raw dialog/picker/status renderer copies and consume canonical components.
9. **Generate lookbook.** Every canonical component gets stories, snapshots, generated SVG previews, and usage examples. The first renderable story registry lives at <RepoFile path="crates/jackin-tui-lookbook/src/stories.rs" />, and the first generated previews are published in per-component [TUI Lookbook](/reference/tui-lookbook/) pages with CI drift checking; complete component coverage still needs to be added as more patterns move into `jackin-tui`.

### Guardrails that make reuse enforceable

These are implementation rules for future PRs under this item:

- No new direct `Color::Rgb(...)` in product code. RGB values belong in `jackin-tui` tokens or a documented adapter test.
- No new raw ANSI color literals for jackin-owned chrome. Capsule may have an adapter, but it consumes shared `Rgb` tokens.
- No product screen hand-rolls a bordered modal, button row, footer hint line, tab strip, filter row, scrollbar, or selectable list once the matching component exists.
- No component invents its own focus-border semantics. The shared components own the "one active bright border" rule so host, launch, and capsule cannot drift.
- No new `src/runtime` dependency on `src/console/widgets`; both surfaces import `jackin-tui`.
- No component becomes canonical without keyboard/mouse tests, `TestBackend` snapshots, a docs lookbook page backed by generated SVG, and a terminal lookbook story visible through `tui-lookbook --terminal`.
- No app-specific component is promoted until its domain data is separated from UI state. Example: the 1Password picker can keep vault/item domain loading locally, but list selection, filter text, loading/error chrome, and button behavior must be shared.

## Proposed crate topology — one obvious home per surface

A developer should be able to answer "where is this implemented?" from the crate name alone. Today the host lives in one large root crate (`src/`, ~97K lines), `jackin-tui` holds shared tokens and pure helpers, and `jackin-capsule` / `jackin-protocol` are separate. The size and the god-structs make "where does this state live?" hard to answer. The target topology gives each terminal surface its own crate and makes `jackin-tui` the shared design system every surface consumes:

```text
crates/
  jackin-tui/        # shared design system — tokens, theme/adapters, geometry, runtime traits, StatefulWidget components, lookbook. No surface-specific domain.
  jackin-console/    # host operator console — workspace/editor/settings Model, console Messages/Effects, console screens composed from jackin-tui
  jackin-launch/     # the loading experience — launch cockpit, stage vocabulary, digital rain, failure model, composed from jackin-tui
  jackin-capsule/    # in-container multiplexer binary — PTY/session/mux model + attach backend, chrome rendered with jackin-tui widgets
  jackin-protocol/   # attach wire protocol (exists)
src/                 # the thin `jackin` binary — CLI dispatch, runtime/launch orchestration, wiring the surfaces together
```

The decision rule for *what moves into `jackin-tui`* versus *what stays in a surface crate* is mechanical, and it is the answer to "rethink what we can put as reusable":

- **Goes in `jackin-tui`:** anything that renders or behaves identically — or should — on more than one surface. Tokens, geometry / scroll math, the `StatefulWidget` components (`Panel`, `ScrollableBlock`, `ConfirmDialog`, `FilterListPicker`, `TabStrip`, `HintBar`, `StatusFooter`, `ButtonStrip`, `TextInput`, `ErrorDialog`), and the shared runtime contracts (`Message` / `Effect` / `Dirty` / `Subscription` traits). If a thing is reused across console, launch, and capsule today — the launch cockpit now imports shared `jackin-tui` hints, status footer, select list, text input, confirm, scrollable, and error dialogs instead of `src/console/widgets`, while the capsule still re-draws confirm and picker flows in raw ANSI — that *is* the signal it belongs in `jackin-tui`, not in any one surface.
- **Stays in the surface crate:** the domain state and semantics only that surface has. Workspace / role / settings editing lives in `jackin-console`; launch-stage progression and failure acknowledgement live in `jackin-launch`; PTY / session / mux lifecycle lives in `jackin-capsule`. These crates *compose* `jackin-tui` components against their own `Model`; they never re-implement a visual primitive.

The litmus test: a component or helper that two surfaces need is promoted to `jackin-tui` *before* the second surface uses it — never copied. The dependency direction is one-way: surface crates depend on `jackin-tui`; `jackin-tui` depends on no surface crate and contains no `src/console` / `src/runtime` / capsule domain types.

This topology extends the deferred [Cargo workspace split](/reference/roadmap/cargo-workspace-split/), which already targets `jackin-tui` and `jackin-console` crates but folds launch into a `jackin-runtime` crate and gates the whole conversion on a ~150K-LOC trigger. The split of ownership between the two items:

- **This item owns the TUI-facing crate boundaries** — growing `jackin-tui` into the real design-system crate now, and defining the `jackin-console` / `jackin-launch` module seams (Model/Update/View, per-component files) so the surfaces become cleanly separable.
- **The Cargo workspace split owns the non-TUI tiers** (`jackin-core`, `jackin-config`, `jackin-runtime` minus the launch UI, `jackin-shell`) and the virtual-manifest conversion, plus the two `console → runtime` production dependencies (`register_agent_repo`, `RepoError`) that must be broken before any console crate can compile standalone.
- **Whether the physical crate extraction lands inside this item's branch or waits for the workspace-split trigger is an open decision** (see [Open questions & risks](#open-questions--risks)). The legibility win — module boundaries that map one-to-one onto these crates — lands here regardless, because it is achievable inside the current single crate and is the prerequisite either way; pre-release status means the split can be pulled forward without migration cost if the operator chooses.

## Module & file structure — thin roots, flat trees, one file per thing

A direct rule this item commits to, because the codebase's biggest readability problem is the opposite of it: **module-root files (`mod.rs`, `lib.rs`) are thin wiring files, never homes for logic.** A root file holds only module declarations, the intentional `pub use` re-export facade, `//!` orientation docs, and minimal glue — no rendering, dispatch, state machines, or domain logic. Today <RepoFile path="src/console/mod.rs" /> (~1,500 lines), <RepoFile path="src/console/tui/widgets/mod.rs" />, and the capsule's <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> (~6,900 lines) are the inverse — god-files that happen to sit at a module root — and they become orientation files over focused submodules.

The conventions, all standard Rust practice:

- **Thin roots.** A `lib.rs` or module-root file is a table of contents plus a curated facade: `mod` / `pub mod` declarations and intentional `pub use` re-exports, kept separate from ordinary `use` imports. Callers write `jackin_tui::ConfirmDialog`, not `jackin_tui::components::dialogs::confirm::ConfirmDialog` — the re-export flattens the public *path* without flattening the file *tree*. ([Rust Reference — Modules](https://doc.rust-lang.org/reference/items/modules.html); [matklad — Notes on the Module System](https://matklad.github.io/2021/11/27/notes-on-module-system.html).)
- **No `mod.rs`.** New TUI modules use the 2018 `foo.rs` + `foo/` layout, not `foo/mod.rs`. `mod.rs` has not been required since Rust 1.30, and a tree full of identically-named `mod.rs` files is hard to navigate in an editor; `foo.rs` beside `foo/` keeps the file path equal to the module path. Existing `mod.rs` files convert as the modules around them are split. ([Rust Reference — Modules](https://doc.rust-lang.org/reference/items/modules.html).)
- **One responsibility per file.** Each file owns one component, one state machine, or one cohesive concern — the [`jackin-tui` package layout](#proposed-jackin-tui-package-layout) below is one component per file, and the host / capsule splits follow the same rule. This is what makes the readability program's file-size tiers (800 / 1000 / 2000 lines) a *symptom* check: a file is over the threshold because it holds more than one responsibility, so the fix is separating concerns, not just cutting the file in half.
- **Flat over deep.** Prefer a shallow module tree; reach for a nested submodule only when a group of files genuinely shares private internals. Deep `a::b::c::d` nesting that exists only to mirror a directory tree is discouraged — the flat-with-re-exports shape is easier to read and to rename.
- **Flat `crates/` workspace.** The same instinct at the crate level: a flat `crates/<name>/` layout under a virtual workspace manifest, internal crates at `version = "0.0.0"`, folder names never prefix-stripped — matklad's recommendation for workspaces in the 10K–1M-line range, which this codebase is squarely inside. Owned by the [Cargo workspace split](/reference/roadmap/cargo-workspace-split/); see the [crate topology](#proposed-crate-topology--one-obvious-home-per-surface) above. ([matklad — Large Rust Workspaces](https://matklad.github.io/2021/08/22/large-rust-workspaces.html).)
- **Legible for humans and AI agents alike.** The structure is tuned so an AI agent — and a first-time reader — can locate the one file that owns a thing without loading the rest: predictable, greppable names (`confirm.rs` holds `ConfirmDialog`, `mux_mode.rs` holds `MuxMode`), one concept per file, tests and stories co-located with the component they cover, and a short `//!` header plus a per-directory orientation note (the readability program's per-directory `README` / `AGENTS.md`) at each level. Many small, single-purpose files are explicitly preferred over a few large ones: a smaller file is less irrelevant context an agent must read before it can act, and a predictable path is something it can open by name instead of hunting for with a search. When the choice is "one big file" or "many small files," this item chooses many small files — agent and reviewer performance is a first-class goal of the restructuring, not a side effect.

The payoff is the operator's stated goal — a developer or an AI agent finds where any piece of state or rendering lives from the path alone: the crate names the surface, the directory names the concern, the file names the component, and the root file is a one-screen map of what is inside. Restructuring every TUI file to this shape is in scope here (the file splits land in the [readability-program TUI slice](#which-readability-program-work-this-item-absorbs)); files outside the TUI keep their current shape until the readability program reaches them.

## Relationship to existing roadmap work

This item is deliberately scoped to the *architectural pattern*. The neighbouring [Codebase readability program](/reference/roadmap/codebase-readability/) items are scoped to *size and duplication*. They are complementary and must be sequenced together:

| Item | Owns | This item adds |
|---|---|---|
| [Codebase readability program](/reference/roadmap/codebase-readability/) | File size, copy-paste duplication, `//!` coverage, mega-functions | The *target shape* the extractions should produce (components, not just smaller files) |
| [Snapshot tests for TUI render](/reference/roadmap/snapshot-tests-tui/) | `insta` + `TestBackend` regression net | The safety net every phase below depends on — it must land first |
| [Launch Progress TUI](/reference/roadmap/launch-progress-tui/) | The launch-flow UX — cockpit, stage vocabulary, rain lifecycle, failure model, diagnostics run IDs, rich/compact/test renderers, and the first console → launch → capsule continuity pass | The durable app-level TUI architecture under that shipped surface: unified update/effect routing, shared components in `jackin-tui`, and removing the runtime → console-widget coupling |
| [Split `input/editor.rs`](/reference/roadmap/split-input-editor/) · [Split `app/mod.rs`](/reference/roadmap/split-app-mod/) | Mechanical file splits of the largest TUI files | The module boundaries those splits should target (Model/Update/View seams, per-component files) |
| [Architecture Decision Records](/reference/roadmap/architecture-decision-records/) | ADR-003 "ratatui selection" | The decision record that must be extended to cover the capsule's no-Ratatui status and the adoption decision driven here |
| [Cargo workspace split](/reference/roadmap/cargo-workspace-split/) | Deferred crate boundaries | This item elevates the existing <RepoFile path="crates/jackin-tui/Cargo.toml" /> crate into the shared design-component crate every terminal surface consumes (see the design-system section below) |

The key sequencing rule: **the file splits and DRY extractions in the readability program should aim at the target architecture defined here**, so that a 4,000-line input file becomes a set of focused `update`/component modules rather than four smaller files that still mutate a shared god-struct inline.

### Which readability-program work this item absorbs

The [Codebase readability program](/reference/roadmap/codebase-readability/) is codebase-wide; this item is where its **TUI-specific** work actually happens, so that when the readability program runs its remaining passes the terminal surfaces are already done. The boundary:

**Owned here (the TUI slice of the readability program):**

- **TUI rendering deduplication** — the readability program's Phase 1.5 "TUI rendering deduplication" (the `titled_block` helper, the named style constants `BOLD_WHITE` / `DIM` / `GREEN` / `BORDER` / `DANGER` replacing ~296 `Style::default().fg(...)` chains, the centralized palette import that kills inline `Color::Rgb(...)` across <RepoFile path="src/console/manager/input/save.rs" /> / <RepoFile path="src/console/tui/widgets/scope_picker.rs" /> / <RepoFile path="crates/jackin-tui/src/components/save_discard_dialog.rs" /> / <RepoFile path="src/console/tui/widgets/source_picker.rs" /> / <RepoFile path="src/console/tui/widgets/file_browser/render.rs" />, the `app_layout` split helper, the breadcrumb-span dedup) *is* this item's token-and-primitive promotion into `jackin-tui`. It lands here, against the component seams — not as a standalone boilerplate pass.
- **TUI file splits** — the Phase 2 splits of the terminal-surface files land against this item's Model/Update/View seams: <RepoFile path="src/console/manager/input/editor.rs" />, <RepoFile path="src/console/tui/render/editor.rs" />, <RepoFile path="src/console/tui/render/list.rs" />, <RepoFile path="src/console/manager/input/save.rs" />, <RepoFile path="src/console/manager/state.rs" />, <RepoFile path="src/console/tui/widgets/op_picker/mod.rs" />, <RepoFile path="src/console/manager/input/global_mounts.rs" />, <RepoFile path="src/console/manager/input/mouse.rs" />, <RepoFile path="src/console/manager/input/auth.rs" />, <RepoFile path="src/console/manager/input/list.rs" />, <RepoFile path="src/console/tui/render/mod.rs" />, <RepoFile path="src/console/tui/render/global_mounts.rs" />, <RepoFile path="crates/jackin-tui/src/components/scrollable_panel.rs" />, and the launch UI in <RepoFile path="src/runtime/progress.rs" />. The dedicated [Split `input/editor.rs`](/reference/roadmap/split-input-editor/) and [Split `app/mod.rs`](/reference/roadmap/split-app-mod/) pages describe each per-file shape; this item supplies the *target* (component modules, not arbitrary smaller files).
- **TUI `//!` coverage and magic numbers** — the `tui/` module is at 0% `//!` coverage and <RepoFile path="src/tui/animation.rs" /> carries 12+ inline millisecond values; the timing tokens move into `jackin-tui` and the modules earn orientation docs as they are promoted.
- **The snapshot-test net** — [Snapshot tests for TUI render](/reference/roadmap/snapshot-tests-tui/) is the shared prerequisite (Phase 0 here) and the readability program's Phase 2 safety net; it is one body of work, not two.

**Stays with the readability program (everything outside the TUI):** the non-TUI file splits and DRY passes — <RepoFile path="src/runtime/launch.rs" />, <RepoFile path="src/operator_env.rs" />, <RepoFile path="src/instance/auth.rs" /> (the 5 copy-pasted provision functions), <RepoFile path="src/config/editor.rs" />, <RepoFile path="src/isolation/materialize.rs" />, the CLI under `src/cli/`, the shared string constants (`MANIFEST_FILENAME`, `DOCKERFILE_NAME`, the construct `FROM` line), the agent match-arm dispatch dedup, the Docker-command dedup, and the test-infrastructure consolidation. The dispatch half of <RepoFile path="src/app/mod.rs" /> (the 1,239-line `fn run`) is a readability-program split; only the slice of `app/mod.rs` that owns the host terminal session (the `TerminalSession` guard and the console → launch → attach handoff) is reshaped here.

The practical result the operator asked for: by the time the readability program reaches its TUI rows, this item has already closed them — and this item never touches the CLI / config / instance / isolation duplication, which stays the readability program's job.

<Aside type="note">
Exact line counts drift week to week and are tracked (and re-baked) in the [Codebase readability program](/reference/roadmap/codebase-readability/). This page cites approximate sizes to make the *pattern* legible, not to be the source of truth for LOC.
</Aside>

## How this work is executed — one branch, one pull request

This roadmap is implemented as **a single long-lived branch and a single pull request**, not one PR per phase. The implementing agent keeps adding **small, self-contained commits** to that one branch until the entire item is delivered, and only then is the PR marked ready. This is a deliberate exception to the usual one-PR-per-change rhythm, made because the phases are tightly coupled: a half-promoted component, a `Message` boundary that covers only some handlers, or a `jackin-tui` move the launch surface has not yet adopted are intermediate states that only make sense read together.

The rules for that branch:

- **Small commits, always green.** Each commit is a bounded, reviewable step — one promoted component, one renderer made pure, one blocking site moved behind an effect — and must compile and pass tests on its own. The half-migrated state (some handlers on `Message`, some not) is explicitly allowed but must never break the build. The [goal-ready work slices](#goal-ready-work-slices) are the natural commit boundaries.
- **One branch for the whole item.** Work does not fan out into parallel feature branches. New phases continue on the same branch; the agent picks up where the last commit left off. The branch is rebased on `main` as needed, never split.
- **The PR grows until the item is complete.** The pull request accumulates commits across all phases and is not merged phase-by-phase; it lands once the acceptance criteria are met, the snapshot tests show no render diff, and the lookbook is generated. Because every commit keeps the tree green, the PR stays reviewable even mid-flight.
- **A behavior change leaves the branch.** The one exception to "everything on one branch": if a slice reveals a feature-sized, operator-visible behavior change, that behavior is split into its own roadmap item and its own PR. This branch stays a pure no-behavior-change refactor.

## Scope & sequencing

This is a large item — eight phases across the host console, launch cockpit, capsule, session spine, and lookbook tooling. It is scoped as a single roadmap item for now, but if it grows it should graduate to a multi-leaf program like the [Codebase readability program](/reference/roadmap/codebase-readability/), with one leaf per track (host refactor, launch/session consolidation, capsule Ratatui, design system / lookbook).

Critical-path dependencies:

- **Phase 0 (snapshot tests) blocks every refactor phase** — without the regression net, no behavior-preserving claim is verifiable.
- The host track (Phases 1–4) and the capsule track (Phases 6–7) are largely independent and can proceed in parallel.
- The [file-split items](/reference/roadmap/split-input-editor/) land *against* the Phase 1/4 component seams, not before them — splitting first just produces smaller files that still mutate a god-struct.
- Phase 5 (session-spine consolidation) depends on Phase 1 (the `Message` loop) and starts from the `TerminalSession` / launch-cockpit / `JACKIN_HOST_ALT_SCREEN` behavior shipped by [Launch Progress TUI](/reference/roadmap/launch-progress-tui/), not from a blank slate.
- Capsule Ratatui (Phase 6) blocks full widget sharing (Phase 7) and the lookbook's capsule coverage (Phase 8).

## Current state — host operator console

The host console lives under `src/console/` (~28K lines of the production tree) and is built on `ratatui` 0.30 (with the `unstable-widget-ref` feature enabled) and `crossterm` 0.29. Text entry is now backed by the shared `TextField` / `TextInput` path in <RepoFile path="crates/jackin-tui/src/components/text_input.rs" /> rather than `ratatui-textarea`.

### Module map (the load-bearing files)

| File | Approx. size | Responsibility |
|---|---|---|
| <RepoFile path="src/app/mod.rs" /> | ~3,900 | Top-level command dispatch; `jackin console` now owns the host alt-screen guard before entering `run_console` and keeps it through launch + attach |
| <RepoFile path="src/console/mod.rs" /> | ~1,500 | `run_console` entry point, standalone terminal ownership fallback, the single console event loop |
| <RepoFile path="src/console/manager/state.rs" /> | ~3,100 | `ManagerState`, `EditorState`, `SettingsState`, all `Modal` enums, instance refresh |
| <RepoFile path="src/console/tui/render/editor.rs" /> | ~3,300 | Workspace editor render — all five tabs |
| <RepoFile path="src/console/tui/render/list.rs" /> | ~3,100 | List/details render, the `sidebar_inputs_for_*` split |
| <RepoFile path="src/console/manager/input/editor.rs" /> | ~4,300 | Editor key dispatch — the largest TUI file |
| <RepoFile path="src/console/manager/input/mouse.rs" /> | ~2,200 | Mouse handling, seam-drag, scroll-focus |
| <RepoFile path="crates/jackin-tui/src/components/scrollable_panel.rs" /> | ~800 | shared `render_scrollable_block`, selected-line helpers, scrollbar geometry |
| <RepoFile path="crates/jackin-tui/src/components/select_list.rs" /> | ~300 | shared launch/host simple picker |
| <RepoFile path="crates/jackin-tui/src/components/hint_bar.rs" /> · <RepoFile path="crates/jackin-tui/src/components/status_footer.rs" /> | ~470 combined | Host footer/status primitives now reused by launch progress, proving the component seam |

### Anti-patterns vs. Ratatui best practice

1. **God-structs that mix four concerns.** `ManagerState` (≈30 fields) holds domain data (`instances`, `workspaces`, `current_dir`), layout geometry (`cached_term_size`, `list_split_pct`, eight separate scroll-offset fields), navigation/focus state (`selected`, `expanded_workspaces`, `list_scroll_focus`, `preview_focused`), and infrastructure (`op_cache`, `instances_last_refresh`) in one struct. Adding one scrollable block touches the state struct, a render signature, the mouse handler, and a focus enum across four files with no single seam to audit.

2. **No `Action`/`Message` type.** Event handling mutates state directly inside the handler. `handle_key` in <RepoFile path="src/console/manager/input/mod.rs" /> is a ~240-line `if`-precedence chain that returns an `InputOutcome` only for routing decisions; all state changes are side effects before the return. Nothing is replayable, loggable, or testable without constructing a full `ManagerState` and driving a real terminal.

3. **The view is not pure.** Render functions now take immutable model state for the manager body, with terminal-size, footer-height, list/editor/settings content geometry, modal animation/preview prep, and scroll-clamp preparation running before `render`. The remaining pure-view work is auditing nested widget renderers and non-manager surfaces, then tightening APIs so mutation cannot be reintroduced accidentally.

4. **Filesystem I/O on the event loop.** Visible mount rows now read only cached `MountInfoCache` facts during render, with synchronous refreshes moved to state construction and input/update paths, but those refreshes are not yet typed background messages. Instance index/manifest reads and the per-container snapshot fan-out now run on a worker thread and report back through `ManagerMessage::InstancesRefreshed`; the remaining gap is promoting the spawn/poll side into the shared typed-effect/subscription layer.

5. **Unconditional full-frame redraw.** The loop draws every tick regardless of whether anything changed. No dirty flag, no skipped frames.

6. **No `StatefulWidget`/`WidgetRef`.** Every renderer is a free `fn(frame, area, &mut state)`. The `unstable-widget-ref` feature is enabled in <RepoFile path="Cargo.toml" /> but unused — there are no reusable widget components implementing the Ratatui widget traits.

7. **Three coexisting modal-stack shapes.** — *resolved:* `AuthFormReturnPath` stash slots (`pending_auth_form_return` on both `EditorState` and `SettingsAuthState`) retired; `EditorState.pending_env_key` retired (now embedded in `Modal::SourcePicker { env_key: … }`); auth parent stack now `Vec<SettingsAuthModal>`. `SettingsEnvState.pending_env_key` for the settings env-add flow is a distinct field that remains intentionally.

8. **DRY drift from symmetric variants.** `sidebar_inputs_for_workspace` vs. `sidebar_inputs_for_current_dir` in <RepoFile path="src/console/tui/render/list.rs" /> are near-identical bodies; the same workspace-vs-current-dir distinction is repeated in `focused_block_still_scrollable`, `clamp_list_scroll_for_area`, and the mouse `update_scroll_focus` handler — four call sites that must stay in lock-step.

9. **Component reuse was happening in the wrong layer.** The launch cockpit used to import host console widgets such as `select_list`, `status_bar`, `hints`, `scrollable`, and `error_popup`. This branch is moving those pieces into <RepoFile path="crates/jackin-tui/Cargo.toml" /> so every terminal surface consumes the shared component crate instead of reaching sideways into `src/console/widgets`.

## Current state — `jackin-capsule` multiplexer

The in-container control plane (the [jackin' Capsule](/reference/roadmap/jackin-capsule/) program) is a small tmux-like PTY multiplexer. It now renders fully through Ratatui via a custom `SocketBackend` that serializes `Buffer::diff` diffs to the attach socket; the `crossterm` dependency remains only for `enable_raw_mode`/`disable_raw_mode` and `terminal::size()` in <RepoFile path="crates/jackin-capsule/src/client.rs" />.

### Anti-patterns (initial state — now resolved or in progress)

1. **All rendering is hand-rolled raw ANSI** — *resolved:* chrome, pane bodies, and ALL dialog variants now render through Ratatui widgets. The raw-ANSI `compose_full_frame` is a fallback only. The hand-rolled SGR serializer in `session.rs` remains for the PTY output relay (not jackin-owned chrome).

2. **A hand-rolled row diff re-implements `Buffer::diff`.** — *resolved:* `PaneBodyCache` has been replaced by `PaneBodyWidget` + Ratatui's `Buffer::diff` via the `SocketBackend`; see ADR-004.

3. **A ~6,900-line god-struct.** `Multiplexer` in <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> owns rendering, input parsing, network I/O, git/PR context polling, session lifecycle, the dialog stack, and mouse drag/selection in one file and one compilation unit.

4. **Top-level mode state is only partially extracted.** <RepoFile path="crates/jackin-capsule/src/mux_mode.rs" /> now names `Normal`, `PrefixAwait`, `Dialog`, `Drag`, and `Select`, and the daemon's debug/input gates start reading through it. The underlying dialog/drag/selection fields still live separately inside `Multiplexer`, so the next step is making the enum the owner rather than a derived adapter.

5. **`compose_full_frame` vs. `compose_partial_frame` duplication** — ~160 lines of near-duplicate pane-loop / box-draw / scrollbar code that must be kept symmetric by hand.

6. **No tests** for `handle_input` dispatch, `apply_dialog_action`, the dialog `handle_key`/`handle_click`, the drag/selection state machines, or the compositor. The parser, protocol, and `render_pane` are tested in isolation; the daemon's decision layer is not.

<Aside type="note">
The capsule now has the first pieces of the target in place: `Dialog::handle_key`/`handle_click` return a `DialogAction` enum (`Command`, `SpawnAgent`, `RenameTab`, `Dismiss`, …), and <RepoFile path="crates/jackin-capsule/src/action.rs" /> starts the daemon-wide `Action` wrapper for palette, prefix, resize, and dialog actions. The remaining work is routing every dispatch arm through that boundary instead of mutating state inline.
</Aside>

## Current state — the console → launch → container flow

The three surfaces are still built differently, but #452 removed the biggest product seam for the console-triggered launch path. `jackin console` now creates a `TerminalSession` in <RepoFile path="src/app/mod.rs" /> before entering `run_console`; `run_console` detects that an outer guard owns the terminal and draws into it; `runtime::LaunchProgress` draws the cockpit into the same alternate screen; and `runtime::attach` passes `JACKIN_HOST_ALT_SCREEN=1` into the capsule client so the child skips its own alternate-screen enter/leave. The remaining problem is no longer the old plain-output launch premise; it is that the shipped continuity is implemented as several local contracts and globals instead of one TUI application architecture.

| Segment | Technology | Terminal state | Anchor |
|---|---|---|---|
| Console command wrapper | `TerminalSession` guard + Ratatui | raw mode, host alternate screen, mouse capture | <RepoFile path="src/app/mod.rs" />, <RepoFile path="src/console/mod.rs" /> |
| Console manager | Ratatui, **stdout** | draws into the inherited host screen when present; standalone `run_console` still owns its own guard | `run_console` in <RepoFile path="src/console/mod.rs" /> |
| Rare interim prompts | dialoguer / cooked terminal | `TerminalSession::suspend` leaves and re-enters the alt screen for sensitive-mount confirm or agent choice | <RepoFile path="src/app/mod.rs" /> |
| Launch progress | Ratatui rich cockpit with compact fallback | rich renderer draws into the inherited host screen; non-rich / non-interactive paths emit compact deterministic lines | <RepoFile path="src/runtime/progress.rs" />, <RepoFile path="src/runtime/launch.rs" /> |
| → attach | `docker exec -it … /jackin/runtime/jackin-capsule` | host screen remains owned; env flag tells capsule client not to nest alt-screen | <RepoFile path="src/runtime/attach.rs" /> |
| In-container client | raw byte relay | raw mode; skips its own alt-screen only when `JACKIN_HOST_ALT_SCREEN` is set | <RepoFile path="crates/jackin-capsule/src/client.rs" /> |
| Daemon render | hand-rolled ANSI | — | <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> |

This is a real improvement and should be preserved. The architectural debt is what the improvement exposed:

- `TerminalSession` is currently a terminal-lifecycle guard, not a full app session model. It still coordinates sub-surfaces through `crate::tui::host_screen_owned()` and `set_rich_surface_active()` globals instead of typed ownership.
- The launch cockpit has its own `LaunchView`, renderer task, picker loop, input drain, and failure modal rather than sharing the console's eventual `Message` / `update` / `Effect` loop.
- The launch cockpit imports reusable pieces from `src/console/widgets`, so the first cross-surface reuse exists but lives below the wrong module boundary.
- `jackin load` still creates the launch cockpit as a standalone rich surface; only the console path has the outer `TerminalSession` that spans console → launch → attach.
- The capsule render path is still independent raw ANSI. The host can preserve a single alt-screen across handoff, but the in-container UI is not yet part of the shared Ratatui component system.

## Ratatui application patterns (the target)

The Ratatui project documents three application patterns and recommends one for immediate-mode apps:

- **The Elm Architecture (TEA)** — [Model / Update / View](https://ratatui.rs/concepts/application-patterns/the-elm-architecture/). State lives in a Model; events map to a `Message` enum; an `update` function applies a message to the model; a **pure** `view` function renders the model. Ratatui calls TEA "the best architectural approach for immediate-mode rendering."
- **[Component Architecture](https://ratatui.rs/concepts/application-patterns/component-architecture/)** — components own their local state and event handlers. The [recommended project structure](https://ratatui.rs/templates/component/project-structure/) is `app.rs` · `action.rs` · `event.rs` · `tui.rs` · `components/`, with the guidance that "you shouldn't need to change anything outside the `components` folder."
- **[Flux](https://ratatui.rs/concepts/application-patterns/flux-architecture/)** — unidirectional data flow; an alternative to TEA for larger apps.

Supporting practices:

- **`StatefulWidget` / `WidgetRef`** separate a widget's drawing logic from a persistent `State` object — the idiomatic way to build reusable, testable components.
- **Immediate-mode discipline**: the view is a pure function of state, redrawn each frame; mutation and I/O belong in `update`, never in `view`.
- **I/O off the render/update thread**: long-running work (disk, network, subprocess) runs in background tasks that send `Message`s back to the loop, so the draw never blocks.
- For PTY rendering specifically, [`tui-term`](https://github.com/a-kenji/tui-term) renders a `vt100::Screen` into a Ratatui `Buffer` (the same approach [used by Turborepo](https://github.com/vercel/turborepo/pull/9123) and [Ratty](https://blog.orhun.dev/introducing-ratty/)) — which means the capsule *could* keep its `vt100` model while letting Ratatui own the buffer diff and the chrome.

<Aside type="caution">
Dogmatic TEA clones the whole model on every update. jackin's state is large, so a clone-per-keystroke model is impractical. The realistic target is Ratatui's documented **mutable** variant: `fn update(&mut self, msg: Message)` mutating the model in place, paired with a strictly **pure** `fn view(&self, frame)`. The win is the `Message` boundary and the pure view, not literal immutability.
</Aside>

## Additional research pass — stronger architecture ideas

The post-#452 re-analysis should not stop at "move files into components." A broader research pass across Ratatui, Bubble Tea, Textual, `tui-realm`, and `tui-term` suggests a better target: **a small jackin-owned TUI runtime**, not a generic framework dependency and not a loose convention.

### Source ideas worth adopting

| Source | Useful idea | jackin' adaptation |
|---|---|---|
| [Ratatui application patterns](https://ratatui.rs/concepts/application-patterns/) | Ratatui explicitly frames TEA, component architecture, and Flux as app-level patterns rather than renderer features. | Treat Ratatui as the renderer; jackin' owns the app runtime around it. |
| [Ratatui FAQ — async](https://ratatui.rs/faq/#when-should-i-use-tokio-and-asyncawait) | `terminal.draw` is the only Ratatui call that must touch the terminal; async mainly matters for input and app work. | Enforce one draw owner; all Docker, 1Password, filesystem, capsule, and diagnostics work is an effect that returns a message. |
| [Ratatui component template](https://ratatui.rs/templates/component/project-structure/) | `action.rs`, `app.rs`, `tui.rs`, and `components/` keep update, terminal lifecycle, and components apart. | Use the same separation, but adapted to the existing crate layout instead of importing a template wholesale. |
| [Bubble Tea](https://github.com/charmbracelet/bubbletea) | `Model.Init`, `Model.Update`, `Model.View`, plus `Cmd` for async side effects, gives a concise runtime contract; Bubbles separates shared components from apps. | Add typed `Effect`s returned by `update`; put reusable UI in `jackin-tui`, with app-specific state in host/capsule modules. |
| [Textual reactivity](https://textual.textualize.io/guide/reactivity/) | Smart refresh: state changes mark widgets dirty and coalesce refreshes; watch hooks react to changes. | Do not add magic descriptors in Rust; do add explicit invalidation: messages declare `Dirty::Layout`, `Dirty::View(ComponentId)`, or `Dirty::Animation`. |
| [`tui-realm`](https://github.com/veeso/tui-realm) | A React/Elm-inspired ratatui framework proves the shape is viable: components, event updates, declarative rendering. | Use as a design reference, not a dependency: jackin' needs container attach, diagnostics, and host-suspend effects that are too project-specific. |
| [`tui-term`](https://docs.rs/tui-term/) | A Ratatui pseudo-terminal widget already maps `vt100::Screen` into Ratatui. | Keep it as the benchmark baseline for capsule pane bodies; decide with throughput and allocation measurements. |

### The higher bar: `TuiRuntime`, not just `Message`

The roadmap should target an internal runtime with these contracts:

1. **One draw owner.** A `TuiRuntime` owns the terminal backend, raw mode, alternate screen, mouse capture, diagnostics routing, and `terminal.draw`. No component, launch step, or capsule adapter may call `draw` directly.
2. **Messages are facts, not commands.** `Message` values describe what happened (`KeyPressed`, `StageDone`, `InstancesRefreshed`, `CapsuleDetached`, `BuildLogClicked`). They do not perform I/O.
3. **Effects are the only side-effect door.** `update(&mut Model, Message) -> Effects` returns typed effects such as `DockerBuild`, `OpLoad`, `InspectMounts`, `SuspendForPrompt`, `AttachCapsule`, `WriteDiagnostics`, `SetTerminalTitle`, `ClipboardCopy`, `TickAfter`, or `Quit`. Effects run outside the draw/update path and return messages.
4. **Subscriptions own long-lived inputs.** Keyboard/mouse/focus/resize events, animation ticks, build-log updates, capsule socket events, Docker instance refresh, and diagnostics lifecycle updates are `Subscription`s. The runtime starts/stops them based on the active view and component tree.
5. **Dirty state is explicit.** Every message/update marks what changed. The runtime coalesces dirty flags and redraws only when state, layout, or animation actually changed. Animations register their own tick subscription; idle views do not redraw at 20 Hz.
6. **Components are pure renderers plus local update.** A component owns its local UI state, handles component-scoped messages, emits effects, and renders from immutable input. Cross-component mutation happens only through parent messages.
7. **Terminal suspension is an effect.** Cooked prompts, token generation, external editors, and `docker exec -it` attach are `SuspendTerminal` / `AttachTerminal` effects with explicit restore semantics, not inline calls sprinkled through event loops.
8. **Diagnostics are a runtime concern.** Since a rich surface owns the terminal, debug output routes through the active diagnostics run from the runtime. Components emit structured diagnostics events; they never print.
9. **Replay is possible by construction.** A recorded sequence of messages plus effect results should replay the state machine in a headless `TestBackend`. This unlocks regression tests for console → launch → attach transitions that screenshots alone cannot cover.

### Why not adopt a framework?

The external frameworks validate the pattern, but jackin' should not depend on one wholesale:

- The host flow has project-specific effects: sensitive host prompts, container attach, diagnostics run files, Capsule socket events, host-alt-screen inheritance, and no-debug-spew guarantees.
- The capsule needs a custom backend that serializes Ratatui buffer diffs to an attach socket, not a normal local terminal.
- The shared `jackin-tui` crate already exists and already owns tokens and pure layout helpers. The right move is to grow it into the project-specific runtime/component layer rather than route through a generic framework and then fight its assumptions.

The implementation can still borrow proven vocabulary: Bubble Tea's `Cmd` becomes jackin's typed `Effect`; Textual's smart refresh becomes explicit dirty invalidation; Ratatui's component template supplies the file boundary; `tui-term` supplies the pane-body benchmark baseline.

### Async architecture contract

"Async-first" does not mean every function becomes `async fn`. It means the render/update path never waits for unbounded work, and the terminal has one clear draw owner. The implementation contract:

- **Only the runtime draws.** `terminal.draw` and Ratatui buffer flushing happen in the app runtime / capsule backend, never inside components, background tasks, launch steps, or domain helpers.
- **Update is fast and bounded.** `update(&mut model, msg) -> Vec<Effect>` may mutate memory and enqueue effects; it must not call Docker, git, 1Password, the filesystem for unbounded inspection, the capsule socket, network APIs, or `thread::sleep`.
- **Effects own work.** Blocking subprocesses, filesystem walks, Docker CLI calls, `op` calls, GitHub/PR context loading, diagnostics writes, and retry loops run as effects. Async-capable work uses `tokio::spawn`; CPU-bound or blocking library work uses `spawn_blocking`; already-threaded capsule PTY work reports back through the same message boundary.
- **Subscriptions own streams.** Terminal input, resize/focus, animation ticks, launch logs, instance refresh cadence, capsule protocol events, and diagnostics lifecycle updates are subscriptions that send messages. They can be started/stopped based on the active view.
- **Cancellation is explicit.** Effects that outlive the view that requested them carry a generation id, request id, or cancellation handle so stale results cannot mutate a newer screen.
- **Backpressure is deliberate.** High-volume streams (PTY output, build logs, animation ticks) coalesce or drop superseded redraw messages instead of growing an unbounded queue.
- **Locks never cross await points.** Shared view snapshots may use short-lived locks, but no update/effect path may hold a mutex while awaiting Docker, socket, filesystem, or channel work.
- **Errors are messages.** Background failures return typed messages and render through the normal error component. They do not panic, print over a rich surface, or disappear into logs only.

The practical test is simple: while a long image build, instance refresh, git/PR context load, or 1Password lookup is slow, key input still queues, animations still tick, diagnostics still record, and the terminal can still redraw or show cancellation/error state.

## Gap analysis

| Ratatui best practice | Host console today | `jackin-capsule` today |
|---|---|---|
| `Message`/`Action` enum, central `update` | Partial — `ManagerMessage` wraps list selection, mouse list/tab/content-row selection, scroll, tree expand/collapse, preview focus/cursor mutations, editor/settings tab focus/navigation, editor cursor/scroll/auth-kind/header/toggle state, and simple settings content cursor/toggle/header state, returning `jackin_tui::runtime::UpdateResult`; most handlers still mutate inline | Partial — `Action` wraps dialog/prefix/palette/resize/focus-report/pane-data paths; many mouse/session arms still mutate inline |
| Pure `view` (no `&mut`, no I/O) | Violated — `&mut state`, scroll-clamp + FS reads in draw | Violated — render writes ANSI + diffs in the compose path on the loop |
| State decomposed (domain / UI / ephemeral) | Violated — god-structs mix all three | Violated — one `Multiplexer` god-struct |
| Reusable `StatefulWidget` components | Absent — free `fn(frame, &mut state)` | Absent — raw ANSI free functions |
| I/O off the loop thread | Partial — instance index/manifest/snapshot refresh uses a worker-thread result channel and mount rows do not inspect during render; `mount_info::inspect` still refreshes synchronously on state/input paths | Mostly OK — PTY/git use background tasks; some `std::thread` vs `spawn_blocking` inconsistency |
| Explicit mode state machine | Partial — focus/modal flags scattered | Partial — `MuxMode` exists, but some state is still derived from scattered fields |
| Framework buffer diff | OK — Ratatui owns it | Violated — `PaneBodyCache` re-implements `Buffer::diff` |
| Render regression tests | Medium-grained `TestBackend`, no snapshots | None for the compositor/dispatch |

## Performance is the driver — async everywhere, nothing blocks the reactor

Responsiveness is the primary reason this refactor is shaped the way it is, not a side benefit of it. A terminal UI that stutters, freezes, or drops input while a Docker image builds, a 1Password vault loads, or an instance fan-out runs reads as broken even when the underlying work is progressing normally. The `Message` boundary, the pure view, typed effects, subscriptions, and dirty redraw all exist to keep the draw loop free so the surface stays smooth under load. The organizing rule: **performance comes first, and that is precisely why everything possible is async — nothing that can block ever runs on the thread that draws.** This section is the *why*; the [async architecture contract](#async-architecture-contract) is the *rules* and the [blocking-work inventory](#blocking-work-inventory--async-migration-targets) is the *sites*.

### The runtime reality (grounded 2026-05-30)

Both binaries already run on Tokio, and both use the **single-threaded** flavor:

- `jackin` (host CLI — console and launch) — `#[tokio::main(flavor = "current_thread")]` in <RepoFile path="src/main.rs" />, awaited through <RepoFile path="src/app/mod.rs" />.
- `jackin-capsule` (daemon **and** attach client, PID-routed) — `#[tokio::main(flavor = "current_thread")]` in <RepoFile path="crates/jackin-capsule/src/main.rs" />.

`current_thread` is a reasonable default for a TUI — one reactor, no work-stealing, no `Send` tax on a large model — but it makes the non-blocking rule **load-bearing rather than optional**: a single thread services async I/O *and* the draw, so one synchronous blocking call (a subprocess wait, an uncached filesystem walk, a `thread::sleep`, a socket read without a timeout) freezes input, animation, and redraw at the same instant. "Use Tokio everywhere" therefore has a precise meaning here:

- Async-capable I/O (sockets, child pipes, timers) is `.await`-ed on the reactor — it yields, so other tasks and the draw keep running.
- Genuinely blocking or CPU-bound work (synchronous `std::process::Command`, blocking FS walks, `op` CLI calls, git shell-outs) moves to `tokio::task::spawn_blocking` — whose pool is separate from the reactor thread — or a dedicated thread, and reports back through the `Message` channel.
- `terminal.draw` is never `.await`-ed on anything; it reads an already-prepared snapshot and returns immediately.

### Async anti-patterns the grounding pass found

| Site | Problem | Target |
|---|---|---|
| Blocking `event::poll(50 ms)` + `event::read` | the console input loop in <RepoFile path="src/console/mod.rs" /> blocks the reactor up to 50 ms per tick and wakes 20×/s even when idle | `tokio::select!` over a crossterm `EventStream` and the `Message` channel; redraw on a message or animation tick, not a fixed 20 Hz spin |
| Nested runtime per background action | <RepoFile path="src/console/manager/input/save.rs" />, <RepoFile path="src/console/manager/input/editor.rs" />, and <RepoFile path="src/app/mod.rs" /> each `std::thread::spawn` a thread that builds a fresh `Builder::new_current_thread().build()` and `block_on`s one future | reuse the app runtime: `tokio::spawn` / `spawn_blocking` an effect that returns a `Message`; never stand up a second runtime to run one call |
| Unconditional full redraw | the console and the launch cockpit recompose every row and pane every tick with no dirty flag | explicit `Dirty` invalidation so an idle view does zero work |
| Per-frame allocation | host renderers in <RepoFile path="src/console/tui/render/list.rs" /> rebuild picker `ListItem` vectors and mount-row `format!` chains on every draw | derive rows in `update`, render from borrowed prepared state |
| `thread::sleep` on render-adjacent paths | shimmer / deploy-hold in <RepoFile path="src/tui/output.rs" />, retry pacing in <RepoFile path="src/runtime/snapshot.rs" />, poll pacing in <RepoFile path="src/runtime/logs.rs" />, worker spins in <RepoFile path="src/console/tui/widgets/op_picker/mod.rs" /> | `tokio::time::sleep` / interval ticks / move off the loop (detailed in the inventory below) |

The nested-runtime pattern deserves emphasis: standing up a whole `current_thread` runtime inside a detached `std::thread` to run one `block_on` is both wasteful and a structure smell — it exists only because there is no effect system to hand the work to. Once `update` returns typed effects, that work becomes a `spawn_blocking` returning a `Message`, on the one runtime the app already owns.

### Frame budget and a performance regression guard

The codebase has **no benchmarks today** — no `criterion`, no `#[bench]`, no frame-time assertion; only ad-hoc `cdebug!` duration logs in the capsule compositor and the launch renderer. Async structure alone does not prove the UI is fast, so this item adds a measurable target and a guard:

- **A frame-time budget.** Set an explicit per-draw budget — the host console should compose a representative workspace frame well inside one 60 Hz frame (~16 ms) — and assert it with a `criterion` bench over the compose path, so a regression fails CI instead of being noticed by feel.
- **Allocation awareness.** The compose path must not grow allocations with frame count; the bench tracks per-frame allocation, because the heaviest host renderers rebuild row vectors every draw today.
- **Capsule bytes-over-socket.** The capsule renders to a Unix socket, not a local TTY: a full frame is ≈ 65 KB on first paint and a partial frame ≈ 16 KB, already logged per render in <RepoFile path="crates/jackin-capsule/src/daemon.rs" />. Bytes-per-frame is a first-class metric a local terminal does not have; the move to Ratatui's `Buffer::diff` (Phase 6) should shrink it, and the bench must confirm it does not regress.
- **Bounded memory.** Capped buffers stay capped (build log 5,000 lines in <RepoFile path="crates/jackin-launch/src/build_log.rs" />, PTY scrollback 10,000 rows in <RepoFile path="crates/jackin-capsule/src/session.rs" />). The uncapped but lifecycle-bound maps — instance snapshots in <RepoFile path="src/console/manager/state.rs" />, and `sessions` / `pane_body_caches` in <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> — are either documented as lifecycle-bound or given a cap, so a long-running console that refreshes instances for hours does not grow without bound.

The felt acceptance test from the async principle stays — *animations keep moving and input keeps queuing while slow work runs* — and this item adds the measured one: *a named frame-time budget enforced by a bench, plus a capsule bytes-per-frame figure the Ratatui move must not regress.*

## Blocking-work inventory — async migration targets

The async-first principle becomes actionable as a concrete list. Every site below either blocks the render/update loop today or sleeps synchronously on a render-adjacent path; the migration target for each is the same — move it off the loop so the draw keeps ticking. Phases 2, 3, and 5 are where these land.

**Host console — work on the event-loop thread:**

| Site | What blocks | Target |
|---|---|---|
| `mount_info::inspect` (<RepoFile path="src/console/manager/mount_info.rs" />) | visible mount rows read only cached facts during render, but cache refresh still runs synchronously on state construction/input paths | typed `MountFacts`, refreshed by a background `MountInspected` message |
| instance refresh (<RepoFile path="src/console/manager/state.rs" />) | index/manifest reads + per-container socket fan-out now run on a worker thread and land through `ManagerMessage::InstancesRefreshed`; the spawn/poll side is still bespoke instead of a shared effect/subscription | typed subscription/effect producer for `InstancesRefreshed` |
| 1Password loads | synchronous `OpRunner` calls invoked from input handlers | background task → result message |
| `event::poll(50 ms)` loop | wakes 20×/s even when idle; full-frame redraw every tick | `tokio::select!` over an `EventStream` + the `Message` channel; dirty/animation-tick redraw |

**Launch flow — the cockpit render cadence:**

The launch cockpit now has a background render task in <RepoFile path="src/runtime/progress.rs" />. It ticks every ~33 ms, snapshots a shared `LaunchView`, drains cockpit-local input, and draws the Ratatui frame while <RepoFile path="src/runtime/launch.rs" /> awaits Docker/network/workspace futures. `LaunchProgress::while_waiting` is now deliberately a thin await wrapper because the renderer no longer needs each launch step to interleave drawing manually. That means the old "wrap every step in a select loop" plan is stale.

The remaining migration target is different: fold the cockpit's parallel mini-app into the same app-level message/effect architecture as the console. Launch stage mutations now have an initial `LaunchMessage` boundary in <RepoFile path="src/runtime/progress.rs" />; forced-choice pickers and failure acknowledgement still need to become effects/input states, and the background render task still needs to become the shared animation-tick source instead of a launch-only thread. Any synchronous work that still runs before `LaunchProgress::new`, between stage mutations, or during rich-surface input handling must move behind effects so the cockpit and future shared session stay responsive.

**Synchronous `std::thread::sleep` on render-adjacent paths** — each should become `tokio::time::sleep`, an interval tick, or move off the render task:

| Site | Sleep | Note |
|---|---|---|
| <RepoFile path="src/tui/output.rs" /> | 25 ms shimmer · 1500 ms deploy hold | animation/pacing on the launch path — drive from an interval, not a blocking sleep |
| <RepoFile path="src/runtime/snapshot.rs" /> | 20 ms · 10 ms | retry pacing |
| <RepoFile path="src/runtime/logs.rs" /> | 250 ms | poll pacing |
| <RepoFile path="src/console/tui/widgets/op_picker/mod.rs" /> | 2 ms ×2 | worker poll pacing |

**Capsule:** PTY and git/PR context already run on background tasks; the gap is the `std::thread` vs. `spawn_blocking` inconsistency the gap-analysis table notes, plus the compose path running on the loop. The Ratatui-frame move (Phase 6) puts the buffer diff on the framework and the compose work behind the same `Action`/update boundary.

Line references and timings drift — re-grep (`rg 'thread::sleep' src/`, `rg 'while_waiting' src/`) before acting on any row.

## Target architecture — host operator console

Introduce the Model/Update/View seam without changing behavior:

1. **One dispatch type + central `update`.** Define the dispatch `enum` — `Message` on the host, the generalized `Action` on the capsule, **converged on a single name** so the shared vocabulary stays recognizable — covering input intents (`MoveSelection`, `OpenModal`, `CommitSave`, `LaunchWorkspace`, …), background results, and their failures (`InstancesRefreshed`, `MountInspected`, `OpLoadFailed(e)`, …) so errors flow as messages routed to the `ErrorPopup`, never as `unwrap`/panic. Map every `crossterm` event — key, mouse, **paste, focus, and resize** — to messages in a thin layer; route every state change through `fn update(&mut self, msg: Message) -> Vec<Effect>` — one update may emit several effects (redraw, spawn a task, quit), so it returns a list, not a single `Option`. Handlers stop mutating state directly.

2. **Decompose state.** Split the god-structs along the seams that already exist conceptually: a domain `Model` (workspaces, instances, pending config), a `UiState` (selection, focus, scroll offsets, open modal), and ephemeral per-frame scratch. Geometry that the renderer derives (content width/height) becomes computed-and-cached state owned by `update`, not written from inside `view`.

3. **Pure view.** Convert renderers to `fn(frame, area, &Model, &UiState)` with no `&mut` and no I/O. Move scroll clamping into `update` (on resize / scroll messages). Replace per-frame `mount_info::inspect` with a cached `MountFacts` map refreshed by a background `MountInspected` message.

4. **Background I/O → channel.** Move the remaining bespoke/background work into typed effects: instance refresh already runs on a worker-thread result channel and reports through `InstancesRefreshed`, but its spawn/poll side still needs to become a shared effect/subscription; 1Password loads and mount inspection still need to move behind result messages. `run_console` is already an `async fn`, so the target loop is a `tokio::select!` over a crossterm `EventStream` and the background `Message` channel — replacing today's synchronous `event::poll(50 ms)` — so the draw never blocks on disk/socket work and no longer wakes 20×/s just to spin. (Fallback if `EventStream` proves awkward: a dedicated input thread feeding the same `Message` channel via `std::sync::mpsc`.)

5. **`StatefulWidget` components.** Extract the list, the editor (per tab), settings, and the modal family into components implementing the Ratatui widget traits, each with its own `State`. This is where the [Split `input/editor.rs`](/reference/roadmap/split-input-editor/) and [Split `app/mod.rs`](/reference/roadmap/split-app-mod/) work should land — the splits target component boundaries, not arbitrary smaller files.

6. **Converge the modal stack** onto the single `Vec<Modal>` shape and retire the stash slots and bare `Option<Modal>`, satisfying the existing [TUI Design Decisions](/reference/tui-design-decisions/) sub-dialog-stack rule by construction.

Every existing rule in [TUI Design Decisions](/reference/tui-design-decisions/) (W3C ARIA tabs, the focusability/affordance contract, the shared `render_scrollable_block`, modal backdrop dimming, the PHOSPHOR palette, settings↔editor parity) is preserved — the refactor changes the *structure behind* those rules, never the rules themselves.

## Target architecture — `jackin-capsule`

Two questions are independent and should be decided separately:

**(1) Structural cleanup — clear win, do regardless of rendering backend.**

- Add a `MuxMode` enum (`Normal`, `PrefixAwait`, `Dialog`, `Drag`, `Select`) so invalid combinations are unrepresentable and dispatch reads from one place.
- Generalize the existing `DialogAction` into a single `Action` type for the whole `handle_input` dispatch — the *same dispatch type the host converges on* (see the host target) — so the daemon's decision layer is testable without a live PTY.
- Split <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> into modules: event loop, session lifecycle, render/compose, input dispatch, dialog. Merge `compose_full_frame`/`compose_partial_frame` onto a shared pane-composite helper.
- Add dispatch/dialog/drag tests once the `Action` boundary exists.

**(2) Render through Ratatui — the decided direction.**

The capsule moves to **full Ratatui**: the daemon builds frames in a Ratatui `Buffer` and renders chrome, status bar, tab strip, dialogs, pickers, and confirms as Ratatui widgets — the *same* shared widgets the host console uses (see the shared design system below). The attach client in <RepoFile path="crates/jackin-capsule/src/client.rs" /> stays a thin byte relay; Ratatui renders inside the **daemon** via a custom backend that serializes the `Buffer` diff to the attach socket instead of a real TTY, replacing the hand-rolled `PaneBodyCache` row diff with Ratatui's own `Buffer::diff`.

The one open question is the **pane-body** mechanism — the cells that mirror a child program's `vt100::Screen`, the perf-critical hot path:

| Pane-body option | Shape | Trade-off |
|---|---|---|
| `tui-term` | render the `vt100::Screen` into the Ratatui `Buffer` via the pseudo-terminal widget | maximal reuse of Ratatui's buffer/diff; `tui-term` is self-described "work in progress" with a known per-cell allocation cost on high-throughput PTYs |
| thin custom cell widget | a small `Widget` that blits the `vt100::Screen` into the `Buffer` directly | keeps the proven fast path while still living inside the Ratatui frame; a little custom code, no new dependency risk |

**Resolve the pane-body question with a throughput spike** (a later phase) — both options keep the capsule fully inside a Ratatui frame, so chrome / dialog / picker reuse lands regardless. That benchmark is the only part of the capsule that stays a measurement question; everything else is decided. The shared widgets live in the existing <RepoFile path="crates/jackin-tui/Cargo.toml" /> crate, which now has Ratatui adapters for host/launch components while still exposing backend-neutral tokens for the capsule.

## Target architecture — one owned terminal session

The first version of this exists now, so the target is no longer "move ownership up" in the abstract. The target is to formalize the shipped `TerminalSession` / launch renderer / capsule handoff into one app-level TUI session spine:

- Keep the #452 behavior: the console manager renders into an outer host screen, the launch cockpit draws into that same screen, and the capsule handoff preserves the host alternate screen with `JACKIN_HOST_ALT_SCREEN` so the child does not enter a nested alt-screen.
- Replace the global coordination flags (`host_screen_owned`, `rich_surface_active`) with typed session ownership: an `AppTuiSession`/`TerminalSession` object owns raw mode, alt-screen, mouse capture, debug buffering, rich-surface diagnostics routing, suspend/resume, and the handoff contract.
- Make console and launch views siblings in one app model. "Operator picked a workspace" → "watching the launch cockpit" becomes a `Message`/`Effect` transition, not `run_console` returning an outcome to a separate launch mini-app.
- Convert launch progress updates (`stage_started`, `stage_done`, failures, build-log overlay, forced-choice picker, failure acknowledgement) into the same message/effect vocabulary as the console. The existing background render task is preserved conceptually as the animation tick source, but it is not launch-private.
- Move reusable launch widgets out of `src/console/widgets` and into <RepoFile path="crates/jackin-tui/Cargo.toml" />. Runtime code must not depend on console internals just to render a status bar, hint row, select list, scrollable build log, or error popup.
- Keep the rare cooked prompts as explicit `SuspendForPrompt` effects. The current `TerminalSession::suspend` behavior is useful, but the reason for leaving the rich surface should be typed and visible in the update loop.
- Preserve panic-safe restore. The RAII guard remains the ultimate owner of terminal restoration; the refactor changes where ownership is modeled, not the guarantee that raw mode and alternate screen are restored on every exit path.

This item now owns the consolidation of the shipped backbone. The [Launch Progress TUI](/reference/roadmap/launch-progress-tui/) item owns the operator-facing launch cockpit and diagnostics behavior that already landed; this item owns making that behavior a reusable architecture rather than a special case.

## Shared design system & lookbook (`jackin-tui`)

A shared crate already exists: <RepoFile path="crates/jackin-tui/Cargo.toml" />, and it is the seam between two otherwise separate binaries — the `jackin` host CLI (Ratatui + `crossterm`) and the `jackin-capsule` multiplexer (raw ANSI today). The crate now has two layers: <RepoFile path="crates/jackin-tui/src/lib.rs" /> keeps backend-neutral tokens/models (`Rgb`, ANSI helpers, `TextField`, `HintSpan`, `TabCell`, unicode-width helpers), while <RepoFile path="crates/jackin-tui/src/components.rs" /> exposes Ratatui adapters for shared host/launch components. The capsule still consumes the backend-neutral layer directly; host and launch consume the Ratatui component layer. The `scroll.rs` and `scrollable_panel.rs` split is the pattern this item generalizes: maintained math wrapped once in `jackin-tui` and consumed by every surface, instead of hand-rolled scroll behavior per surface.

That token layer is the model to follow: one source of truth for the values, a thin per-surface conversion. The gap is that **every component above the token layer is reimplemented per surface or borrowed through the wrong module boundary** — the host has its own tab strip, scrollbar, text input, hint model, and confirm dialog; the launch cockpit imports host widgets directly from `src/console/widgets`; and the capsule hand-rolls each again in raw ANSI, kept in sync only by the procedural rules in [TUI Design Decisions](/reference/tui-design-decisions/). **Target: elevate `jackin-tui` into the canonical TUI design-component crate** every terminal surface consumes; the crate that already owns the tokens grows to own the components. Do not create a new crate.

This is a design-system problem, and the web ecosystem has a mature playbook for it. An audit found **roughly 45 repeatable visual patterns** — an approximate figure; the enumerated drift table below is the verifiable subset — about a dozen implemented twice and several with measurable cross-surface drift. The fix is a single token set, a single component library, and a browsable **lookbook** that renders every component and its states so the operator can point at one and say "use this." The canonical *rules* stay in [TUI Design Decisions](/reference/tui-design-decisions/); the lookbook renders the *components* and never restates a rule — its Do / Don't rows link to the relevant rule anchor. The catalog below, organized by atomic-design layer, is the seed inventory; each row becomes a lookbook page.

### The web playbook, mapped to a TUI

| Web concept | What it is | jackin' equivalent |
|---|---|---|
| [Design tokens](https://dev.to/psqrrl/managing-design-tokens-using-storybook-5975) | the smallest brand primitives (color, spacing, timing) in a tech-independent source | the `Rgb` palette + spacing/timing constants in <RepoFile path="crates/jackin-tui/src/lib.rs" /> |
| [Atomic design](https://github.com/danilowoz/react-atomic-design) (Brad Frost) | atoms → molecules → organisms → templates → pages | tokens → primitives → components → chrome → screens |
| Component library | the implemented, reusable components | the widget set, consolidated into `jackin-tui` |
| [Storybook](https://storybook.js.org/) | an interactive explorer that renders each component in isolation with its states ("stories") | the **lookbook** — a docs surface that renders each TUI component and its variants |
| Living style guide | docs that stay in sync with the real components | lookbook pages generated from real `TestBackend` renders, not hand-drawn |

### Catalog — collected evidence

The audit's patterns, grouped by atomic layer. This is the seed inventory the lookbook pages are built from; each row becomes a component page (page shape below).

**Tokens (atoms)** — defined once in <RepoFile path="crates/jackin-tui/src/lib.rs" />, wrapped per surface:

| Token group | Values | Notes |
|---|---|---|
| Core palette | `PHOSPHOR_GREEN` `Rgb(0,255,65)` · `PHOSPHOR_DIM` `Rgb(0,140,30)` · `PHOSPHOR_DARK` `Rgb(0,80,18)` · `WHITE` · `BLACK` · `INPUT_BG_DIM` `Rgb(20,24,22)` | host wraps as `ratatui::Color`; capsule emits SGR |
| Semantic | danger red `Rgb(255,94,122)` · warning yellow · action-accent · disclosure-accent · cyan (live state) | several are host-only locals today — candidates to promote into the shared token set |
| Spacing | tab gap `1` · cursor indent `2` · role marker `5` · button gap `4` · text-input band pad `1` | |
| Glyphs | borders `┌─┐│└┘` · selection `▸ ` · scrollbar thumb `█` / track `·` · filter placeholder `░` · caret `█` | |
| Motion timing | step shimmer 25 ms · deploy hold 1500 ms · rain age→color ramp | <RepoFile path="src/tui/animation.rs" />, <RepoFile path="src/tui/output.rs" /> |

**Primitives (molecules):**

| Component | Represents | Look & feel | States |
|---|---|---|---|
| `Panel` | bordered titled region | `Borders::ALL`; title `" Title "` `WHITE+BOLD`; green border only when focused **and** scrollable | focused-scrollable / focused-fit / unfocused |
| `ScrollableBlock` | scrollable content + scrollbars | wraps `Panel`; proportional thumbs; clamps offsets each frame | horizontal / vertical overflow |
| `Scrollbar` | overflow indicator | thumb `█` `PHOSPHOR_DIM`, track `·` `PHOSPHOR_DARK` | vertical / horizontal |
| `HintBar` | footer key hints | `Key`=`WHITE+BOLD`, `Text`=`GREEN`, `Dyn`=`DIM`, `Sep`=` · ` `DARK` | wraps to width |
| `TabStrip` | W3C tabs | active focused = `WHITE+BOLD` + `━` underline; content-focused = `GREEN` bg / black fg | tab-bar-focused / content-focused |
| `TextInput` | single-line entry | band bg `INPUT_BG_DIM`; text `GREEN`; inverse caret; red border on duplicate | normal / invalid |
| `FilterInput` | type-to-filter row | `"Filter: "` `DIM`; empty = 20×`░`; caret `█` | empty / typing |
| `ListRow` | selectable item | selected = `▸ ` + `GREEN`; unselected color is a **drift point** (host `WHITE` vs capsule `GREEN`) | selected / idle |
| `ButtonStrip` | 2–3 buttons | focused = `WHITE` bg / black fg / bold; unfocused = `GREEN+bold`; disabled = dark/dim; gap 4 | `jackin_tui::components::button_strip`; host confirm, confirm-save, scope, source, mount-destination, and save/discard consume it |

**Components (organisms):**

| Component | Represents | Look & feel | Reuse status today |
|---|---|---|---|
| `ConfirmDialog` | destructive Yes/No | title `" Confirm "`; question ends `?`; default focus **No** | host and launch share <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" />; capsule `render_confirm_action` lives in <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" /> |
| `FilterListPicker` | filter + list pick | filter row 0, spacer, items 2+; box `items + 4` | host <RepoFile path="src/console/tui/widgets/role_picker.rs" /> + launch `select_list` + capsule palette / agent / split pickers — many impls |
| `ErrorPopup` | the only error surface | red border + title; scrollable; dismissible | host and launch share <RepoFile path="crates/jackin-tui/src/components/error_dialog.rs" />; capsule has none |
| `ConfirmSaveDialog` | diff-preview save | scrollable diff; default focus Save | host only |
| `SaveDiscardStrip` | 3-button dirty-exit | standard strip; default Cancel | shared <RepoFile path="crates/jackin-tui/src/components/save_discard_dialog.rs" /> |
| `TextInputDialog` | modal text entry | 60% width clamp `[40,100]`, 5 rows | **already shared** via `render_text_input_dialog` in <RepoFile path="crates/jackin-tui/src/lib.rs" /> |
| `InfoDialog` | read-only label/value | right-aligned labels; copy-target row emphasized + hover lift; `✓ Copied!` | capsule container-info / github-context in <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" /> |

**Chrome & motion (templates):**

| Component | Represents | Look & feel |
|---|---|---|
| `AppLayout` / `TabbedLayout` | screen scaffolds | header / (tab strip) / body / footer constraints |
| `ConsoleHeader` | top brand bar | `▓▓▓▓ ` green + `jackin'` `WHITE+BOLD` + dim title |
| `StatusBar` (capsule) | rows 0–1 in container | brand pill `GREEN` bg / black + tab strip + menu button; `━` underline row |
| `PaneBox` (capsule) | per-PTY frame | active border `GREEN`; inactive gray; `┌─ title ─┐` |
| `DigitalRain` | boundary ritual | age→color fade; centered reveal banner |
| `Spinner` / `StepShimmer` | progress | braille spinner; per-char shimmer |

### How deep the sharing goes depends on the capsule's rendering decision

- **Tokens and pure layout math** (palette, `lay_out_tabs`, `vertical_thumb`, text measurement, `box_rect` centering) can be shared **today**, regardless of the capsule's pane-body rendering decision. This is the floor — already partly in place — and unifying the remaining token wrappers and layout helpers is an early, low-risk win that needs no rendering-backend change.
- **Full `StatefulWidget` components** (`Panel`, `ScrollableBlock`, `ConfirmDialog`, `FilterList`, `TabStrip`, `HintBar`) require the capsule to render through a Ratatui frame (Phase 6). Until that lands, the capsule shares only tokens and layout math and keeps separate render code — the weakest form of reuse, which is why moving the capsule into a Ratatui frame is committed scope here, not an optional follow-up.

### What each lookbook page contains

Each component gets one page with a fixed "story" shape so the operator reads them all the same way:

1. **Name + one-line purpose** — what it represents and when it appears.
2. **Anatomy** — a labelled diagram of the rendered component (border, title, content, scrollbar, buttons).
3. **Tokens used** — which palette / spacing tokens, linked to the token page.
4. **States / variants gallery** — focused / unfocused / scrollable-fit / error / default-focus / hover, each rendered.
5. **Where used** — the screens that compose it, so a change's blast radius is visible.
6. **Do / Don't** — links to the [TUI Design Decisions](/reference/tui-design-decisions/) rule anchors that apply (never restated inline).
7. **Live preview** — the actual rendered component (mechanism below).

### Rendering TUI components in the docs

The lookbook is only trustworthy if its previews are the *real* components, not hand-drawn art that drifts. The plan, strongest first:

- **Primary — render the real widget to SVG.** Draw each component (and each state) through `ratatui::backend::TestBackend` into a `Buffer`, then serialize the buffer's cells (char + fg/bg/modifiers) to inline **SVG** (or styled HTML). A small `cargo run --bin tui-lookbook` emits one SVG per component-state from the actual widget code; MDX embeds the generated SVGs; CI regenerates them so they never drift. This reuses the exact `TestBackend` + `insta` infrastructure from [Snapshot tests for TUI render](/reference/roadmap/snapshot-tests-tui/) — the same render that guards regressions also produces the lookbook art. [`term-transcript`](https://docs.rs/term-transcript) (Rust; renders terminal output to SVG/HTML) is a candidate serializer.
- **Motion and raw-ANSI surfaces** — for the digital rain, spinners, and (until they move to Ratatui) the capsule's raw-ANSI dialogs, record an asciicast and convert with [`vhs`](https://github.com/charmbracelet/vhs), [`termsvg`](https://github.com/MrMarble/termsvg), [`svg-term`](https://github.com/marionebl/svg-term-cli), or [`termtosvg`](https://nbedos.github.io/termtosvg/) to an animated SVG embeddable in MDX.
- **Interim bridge** — a small MDX `<TuiPreview>` component that paints hand-authored Unicode mockups using the docs theme's existing `--jk-brand` PHOSPHOR CSS tokens. Fast to write but hand-drawn and drift-prone, so it is a bridge to the generated-SVG approach, not the destination.

### Drift the lookbook would eliminate

Concrete inconsistencies the audit found between host and capsule for the "same" pattern — the evidence that one shared component set is needed:

| Pattern | Host | Capsule |
|---|---|---|
| Unselected list-row color | `WHITE` | `PHOSPHOR_GREEN` |
| Tab strip | two modes (tab-bar / content focus) | one always-on mode |
| Confirm dialog | dynamic height, structured trust variant | fixed 9 rows, single question + explanation |
| Inactive tab background | terminal default | explicit `Rgb(30,30,30)` |
| Agent-choice picker | internal hint row (pre-rule artifact) | hints in footer |
| `SaveDiscardStrip` palette | re-declares `Color::Rgb(0,255,65)` locally | n/a |

Plus the documented modal-backdrop-dim gap: the rule mandates dimming the whole surface behind a modal; neither implementation fully does today.

### The component selector (end state)

The lookbook is the operator's component catalog. The intended flow: the operator browses it, points at a component — "use `ConfirmDialog` here", "use `FilterList` with section headers", "this should be a `Panel`, not a bare block" — and the agent composes the screen from the shared `jackin-tui` widgets instead of hand-rolling a near-copy. That is the Storybook "pick the component" experience brought to jackin's TUI: a named, visible, reusable vocabulary instead of ~45 patterns reinvented per surface.

## Terminal capability & degradation

A single token layer makes graceful degradation a property of the tokens, not of every call site:

- **Color depth.** Tokens resolve through Ratatui's color handling for truecolor; on 256- or 16-color terminals the palette maps to the nearest supported color, and `NO_COLOR` / `COLORTERM` are honored so the UI stays legible without a redesign.
- **Glyph fallback.** The box-drawing, scrollbar, and selection glyphs (`┌─┐│└┘`, `█`, `·`, `▸`, `░`) get an ASCII fallback set for terminals that cannot render them, selected once at startup rather than per call site.
- **Size.** Components clamp to a minimum usable size and degrade (hide chrome, collapse columns) on narrow terminals instead of panicking or overflowing.
- **Reduced motion.** Animations (digital rain, spinners, shimmer) honor the reduced-motion handling owned by [Launch Progress TUI](/reference/roadmap/launch-progress-tui/); the design system exposes the same gate so static fallbacks stay consistent.

**Non-goals (this refactor).** Screen-reader / assistive-technology support and alternate themes (light, high-contrast) are out of scope — but the single token set makes both a future *configuration* change rather than a rewrite, which is the point of centralizing them.

## Constraints and non-goals

- **No behavior change — this is restructuring, not a redesign.** The whole item is refactoring, file restructuring, and reuse; the running app must look and behave the same. Snapshot tests gate every step, and the operator's screens, key bindings, mouse behavior, and muscle memory are unchanged. Visual/UX changes are out of scope and belong to items like [Launch Progress TUI](/reference/roadmap/launch-progress-tui/). The one allowed exception is a genuine pre-existing bug surfaced *while* restructuring — a stale-geometry scroll glitch, a focus border that was already wrong, a missing call in one of two symmetric paths — which may be fixed, but each such fix is called out explicitly and justified against its snapshot diff. That is the rare "we forgot something" case, not license to change how the app works; the default and the expectation is byte-identical behavior.
- **Pre-release freedom.** Internal types may change freely; no migration shims or compatibility layers (these are not versioned schemas).
- **Incremental only.** No big-bang rewrite. One surface, one layer, behind tests. A half-migrated state (some handlers on `Message`, some not) must compile and pass tests at every commit.
- **DRY.** Extend the shared scrollable/widget helpers and the `jackin-tui` crate; do not fork parallel implementations.

## Goal-ready work slices

The phases below describe the long-range order. For `/goal` execution, use slices like these so each run has a bounded diff and a concrete exit condition:

| Slice | Depends on | Deliverable | Exit condition |
|---|---|---|---|
| Snapshot baseline | none | TUI render snapshots for the screens/components about to move | The next structural refactor can prove "same render" with `TestBackend` or a documented real-terminal smoke check. |
| Component inventory | none | A tracked inventory of repeated TUI patterns, current owners, call sites, maturity levels, and drift notes | New TUI work can name the component it uses; missing components are visible as debt instead of discovered ad hoc. |
| Token/geometry promotion | snapshot baseline | Move palette, spacing, glyph, tab, scrollbar, centering, and text-measurement helpers into `jackin-tui` | Product code imports semantic tokens/helpers; new `Color::Rgb(...)` and raw ANSI chrome literals disappear from touched code. |
| Host message boundary | snapshot baseline | `Message` plus central `update` around one host-console path, then expanded across keys/mouse/resize/focus | State changes flow through messages; tests can drive update without a terminal. |
| Host pure-view pass | host message boundary | Remove `&mut` and I/O from one renderer family at a time | The renderer under migration accepts immutable model/UI state and cannot stat the filesystem or mutate scroll geometry during draw. |
| Host async refresh | host message boundary | Move one blocking refresh source at a time (`mount_info`, the instance-refresh worker result, 1Password) behind effects/subscriptions | Slow work reports back as messages; redraw/input remain live while the work runs. |
| Shared primitive extraction | token/geometry promotion | Promote one primitive (`HintBar`, `ButtonStrip`, `ScrollablePanel`, `TabStrip`, etc.) into `jackin-tui` | Host/launch use the shared primitive; snapshots prove unchanged output. |
| Launch consolidation | host message boundary, shared primitives | Replace launch-private widget/runtime seams with app messages/effects and `jackin-tui` components | `src/runtime` no longer imports `src/console/widgets`; launch stage updates are messages, with picker/failure effects still pending. |
| Terminal session spine | host message boundary, launch consolidation | Replace global rich-surface/host-screen coordination with typed `TerminalSession` ownership | Console → launch → capsule still handoffs cleanly; diagnostics/debug routing and panic-safe restore still work. |
| Capsule action/mode split | none | `MuxMode`, generalized `Action`, daemon module split, dispatch/dialog tests | Input dispatch is testable without a live PTY and invalid mode combinations are unrepresentable. |
| Capsule Ratatui frame | capsule action/mode split, shared primitives | Custom Ratatui backend over attach socket; chrome/dialogs/pickers rendered as shared widgets | Raw ANSI chrome copies are deleted; pane-body rendering remains the only measured choice. |
| Pane-body benchmark | capsule Ratatui frame | `tui-term` vs custom widget benchmark and ADR decision | ADR records the chosen pane-body path with throughput/allocation evidence. |
| Lookbook | shared components, snapshot infrastructure | `tui-lookbook` generator plus MDX pages from real renders | The generator now emits SVG previews from widget code into per-component [TUI Lookbook](/reference/tui-lookbook/) pages and CI checks drift; complete component coverage remains. |

Each slice should update this page's status notes if it changes scope, closes a risk, or invalidates a later slice. If a slice reveals a feature-sized behavior change, split that behavior into its own roadmap item instead of hiding it inside the architecture refactor.

## Implementation phases

### Phase 0 — Safety net and decision record

Land the [Snapshot tests for TUI render](/reference/roadmap/snapshot-tests-tui/) item (the regression net) and extend [ADR-003 "ratatui selection"](/reference/roadmap/architecture-decision-records/) to record the capsule's current no-Ratatui status and the pane-body rendering decision. This page is the design artifact.

### Phase 1 — Host: `Message` boundary

Introduce the `Message` enum and central `update`; route existing key/mouse handlers through it without moving the renderers. Behavior identical; the win is a testable update layer. The first slice is <RepoFile path="src/console/manager/message.rs" />, which routes list selection, mouse list/tab/content-row selection, scroll, tree expand/collapse, preview focus/cursor mutations, editor/settings tab focus/navigation, editor cursor/scroll/auth-kind/header/toggle state, and simple settings content cursor/toggle/header state through `ManagerMessage` and returns the shared <RepoFile path="crates/jackin-tui/src/runtime.rs" /> `UpdateResult` contract; remaining modal, effect-producing, mouse, edit, and complex settings content handlers still need to move.

### Phase 2 — Host: pure view

Remove `&mut` and I/O from renderers. Terminal-size, footer-height, and top-level scroll-clamp prep now runs before `render`; the remaining target is moving the rest of the scroll/geometry writes into `update` on resize / scroll messages. Visible mount rows now consume pre-refreshed `MountInfoCache` facts, which removes `.git` reads from render; the remaining target is to replace synchronous update-path refresh with cached facts refreshed by a message. Introduce event-driven (dirty) redraw — draw on a `Message` or an animation tick, not unconditionally every 50 ms — so an idle console does no work.

### Phase 3 — Host: background I/O

Move the remaining refresh/load paths onto background tasks that send `Message`s. Instance refresh no longer blocks draw and reports through `ManagerMessage::InstancesRefreshed`; next promote its spawn/poll side to the shared effect/subscription layer alongside 1Password loads and mount inspection.

### Phase 4 — Host: components + modal-stack convergence

Extract `StatefulWidget` components (list, editor-per-tab, settings, modals) into the shared <RepoFile path="crates/jackin-tui/Cargo.toml" /> design crate; land the [Split `input/editor.rs`](/reference/roadmap/split-input-editor/) and [Split `app/mod.rs`](/reference/roadmap/split-app-mod/) work against these seams; converge the modal stack onto `Vec<Modal>`. Token and layout-helper unification (the option-independent floor) can land earlier, in Phase 1.

### Phase 5 — App: one owned terminal session

Consolidate the `TerminalSession` behavior that [Launch Progress TUI](/reference/roadmap/launch-progress-tui/) shipped into a typed app-level session spine. The console manager and launch cockpit already share one host alternate screen on the console path; this phase removes the ad hoc globals and return-to-app handoff, models console and launch as sibling views in one app model, makes launch updates messages/effects, and moves the launch-used widgets from `src/console/widgets` into <RepoFile path="crates/jackin-tui/Cargo.toml" />. The visible behavior stays the same: console → launch → capsule remains continuous, with cooked prompts represented as explicit suspend effects.

### Phase 6 — Capsule: structural cleanup + Ratatui frame

Add `MuxMode`; generalize `Action`; split <RepoFile path="crates/jackin-capsule/src/daemon.rs" />; merge the full/partial compositors; add dispatch/dialog tests. Then move the daemon to render into a Ratatui `Buffer` via a custom attach-socket backend, replacing `PaneBodyCache` with `Buffer::diff`, and render chrome / dialogs / pickers as the shared <RepoFile path="crates/jackin-tui/Cargo.toml" /> widgets.

### Phase 7 — Capsule: pane-body spike + final reuse

Benchmark `tui-term` against a thin custom cell widget for pane bodies on a high-throughput PTY and pick the mechanism. Land the shared confirm / picker / tab-strip / panel / hint-bar widgets so the host console and the capsule render identical components.

### Phase 8 — Design-system lookbook

Stand up the lookbook: a `tui-lookbook` binary that renders each `jackin-tui` component and its states to SVG from the real widget code, provides a real-terminal browser through `tui-lookbook --terminal`, one MDX page per component following the story shape above, and CI regeneration so previews never drift. The first generator lives at <RepoFile path="crates/jackin-tui-lookbook/src/main.rs" /> and renders <RepoFile path="crates/jackin-tui-lookbook/src/stories.rs" /> stories from `TestBackend` buffers into committed assets under <RepoFile path="docs/public/tui-lookbook/panel-focused.svg" />, consumed by per-component [TUI Lookbook](/reference/tui-lookbook/) pages. The generated SVGs always carry a black terminal background so docs previews match real terminal use. CI runs `tui-lookbook --check` against those committed previews; every new shared TUI component must appear in both the docs lookbook and the terminal browser in the same PR. Reconcile the host/capsule drift table as components consolidate into shared widgets.

### Phase 8f — Enforce remaining design-decision hard rules

Several rules in [TUI Design Decisions](/reference/tui-design-decisions/) have no corresponding implementation phase. This phase audits and enforces them:

**Navigation W3C ARIA Tabs pattern** — verify every tabbed surface (`ManagerStage::Editor`, `ManagerStage::Settings`) implements the full pattern: `←`/`→` cycle tabs when tab bar focused, `⇥`/`↓` enters content, `⇧Tab`/`Esc` returns to tab bar. Verify `tab_bar_focused: bool` exists on every tabbed state struct. Write a test for each surface that asserts the key bindings route correctly.

**Space vs Enter semantics** — audit every toggle row (keep_awake, git_pull, trusted role, allowed role, DCO sign-off, co-author trailer). Each must respond to `Space` only (not `Enter`). Audit every action button (Save, Cancel, Yes, No, OK): `Enter` only. Add a test that asserts `Space` on a toggle row toggles the value and `Enter` does not.

**Hints footer-only** — no widget may render its own internal hint row. Audit all files under `src/console/tui/widgets/` for `Constraint::Length` rows containing hint text. Every hint must route through `render_hint_bar` / `render_wrapped_hint_bar` in the footer, never inside the widget body.

**Error surface** — every error must use `ErrorPopup`. Audit all `eprintln!` calls, any `status_overlay` that surfaces errors, and any inline error text in render functions. Route all to `ManagerMessage::OpenListErrorPopup` or equivalent.

**Dialog button spacing** — every dialog button row must have exactly one empty row above it. Audit the existing dialogs against the `content_rows + 5` formula. Write a `TestBackend` test per dialog that asserts the button row index.

**Acceptance:** All five audits produce zero violations. New tests covering each rule are green in CI.

### Phase 8e — Long paths in dialogs: OSC 8 hyperlinks

**Complete on `feature/tui-architecture` (2026-05-31).** The launch failure popup now wraps long path values instead of silently truncating them, keeps the full path visible in non-OSC-8 terminals, and emits OSC 8 `file://` overlays for the diagnostics and docker-output rows after the Ratatui draw. The OSC 8 open/close helpers live in `jackin-tui::ansi` and are reused by the capsule container-info dialog.

The launch failure popup renders "run diagnostics" and "docker output" paths that are truncated when they exceed the dialog width. This violates the "never silently truncate" rule.

**Fix:** Render file paths in the failure popup as OSC 8 terminal hyperlinks (`file://` scheme). The visible text can be the shortened path; the href is the full absolute path. The operator clicks to open the file in their system default viewer. This is consistent with how `ContainerInfo` in the capsule already renders the "Run log" row.

**Also audit:** Any other dialog row that displays a file path, URL, or long string. Apply the same OSC 8 approach to:
- `failure_popup_rows` in <RepoFile path="src/runtime/progress.rs" /> — "run diagnostics" and "docker output" rows.
- Any future info panel that shows a path.

**Detecting truncation violations:** search for `.chars().take(N)`, `.truncate(`, `&s[..width]`, or any substring slice on a path/URL value being rendered into a dialog row. Each of those is a truncation site.

**OSC 8 fallback:** terminals that do not support OSC 8 simply display the visible text verbatim — the escape sequences are ignored and the path appears as plain text. This is safe and already how `ContainerInfo` works in the capsule. Use the full absolute path as the visible text (no abbreviation) so it is readable even without hyperlink support.

**Acceptance:** The failure popup rows for file paths render as clickable OSC 8 links in supporting terminals. Terminals that do not support OSC 8 show the full plain-text path. No silent truncation anywhere in the failure popup.

### Phase 8d — Unified container info dialog across all surfaces

**Complete on `feature/tui-architecture` (2026-05-31).**

The container info dialog now opens from every status-bar/status-footer chip surface. Host and launch use the shared <RepoFile path="crates/jackin-tui/src/components/container_info.rs" /> `ContainerInfoState` / `ContainerInfoRow` / `render_container_info` component, plus shared value hit-testing and OSC 8 hyperlink-overlay helpers. The capsule status-bar container-chip click opens the matching `Dialog::ContainerInfo` in <RepoFile path="crates/jackin-capsule/src/daemon/input_dispatch.rs" />. Copyable rows emit OSC 52, linked rows receive OSC 8 overlays, and hover uses the OSC 22 hand pointer.

**Required work:**

1. **`jackin-tui` shared component** — create `ContainerInfoState` (rows: label + value + optional OSC 8 href + copy-target flag) + `render_container_info(frame, area, state)` in `crates/jackin-tui/src/components/` (new file alongside existing components). Same layout contract as capsule's existing `render_container_info` in <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" />: PHOSPHOR_GREEN border, label column + value column, emphasised rows clickable via OSC 52, debug rows conditional on `debug_enabled()`. Done.

2. **Host console** — replace the `ErrorPopup` opened by the debug bar chip click in <RepoFile path="src/console/run.rs" /> with a proper `ContainerInfoState` filled with: run ID, diagnostics log path (OSC 8 link). Done, including click-to-copy and OSC 8 overlay behavior for that modal.

3. **Capsule** — wire direct click on the instance ID / combined chip in the status bar row to push `Dialog::ContainerInfo` onto the dialog stack. Done.

4. **Launch progress** — wire `StatusFooter` chip click to open `ContainerInfoState` with the fields available at that stage: container ID (if known), jackin version, run ID (debug). Done, including click-to-copy and OSC 8 overlay behavior for linked rows.

**Non-debug vs debug rows:** Non-debug: container ID, role, agent, workdir, versions. Debug: same + run ID (bold, copyable) + diagnostics path (OSC 8 link) + docker output path (OSC 8 link, if build failed).

**Implementation details:**
- `debug_enabled()` is `crate::logging::debug_enabled()` in `crates/jackin-capsule/src/logging.rs`. On the host side use `crate::tui::is_debug_mode()`.
- Column layout: label column is left-aligned, fixed at the longest label width; value column fills the remainder. Use the `render_info_rows` pattern already in <RepoFile path="crates/jackin-capsule/src/tui/dialog.rs" />.
- OSC 8 and OSC 52 can coexist on the same row: render the OSC 8 link around the display text, then separately track the row as a copy target for OSC 52 on click.

**Acceptance:**
- Clicking any status bar chip on any surface opens the container info dialog.
- The dialog renders identically (same border, column widths, row spacing) on every surface.
- In non-debug mode: no run ID or log path rows visible.
- In debug mode: run ID and log paths appear; paths are OSC 8 clickable links.
- Hovering the chip shows OSC 22 hand pointer on all surfaces.
- OSC 52 copy works on the container ID row and the run ID row.

### Phase 8c — Terminal resize visual correctness

**Complete on `feature/tui-architecture` (2026-05-31).**

**Problem:** Resizing the terminal (especially the capsule) produces visual corruption — wrong background colors (gray tint), stale cell artifacts from the previous size, broken layout geometry. The capsule's Ratatui double-buffer and SocketBackend cursor-tracking state are not reset on resize.

**Root cause:** On `ClientFrame::Resize` in <RepoFile path="crates/jackin-capsule/src/daemon.rs" />, `compose_full_frame` is called but `ratatui_terminal.clear()` is not. The diff then compares against a previous-buffer sized for the OLD geometry, leaving stale content at cells that no longer exist in the new layout. Additionally, `SocketBackend`'s `cursor_row`/`cursor_col` tracking carries stale coordinates across the resize boundary, causing the consecutive-cell optimization to skip position escapes that are now needed.

**Completed fix — three steps:**

1. **Reset Ratatui buffer on resize** — in the `ClientFrame::Resize` handler in <RepoFile path="crates/jackin-capsule/src/daemon.rs" />, add `let _ = mux.ratatui_terminal.clear();` before `compose_full_frame`. `SocketBackend::clear()` already does NOT emit `[2J` (from a prior fix), so this resets the diff buffer without a flash.

2. **Reset SocketBackend cursor state on resize** — in <RepoFile path="crates/jackin-capsule/src/tui/socket_backend.rs" />, the `resize` method currently only updates `self.size`. Add `self.current_style = CellStyle::default();`. The `draw()` method tracks `cursor_row`/`cursor_col` as local variables (not struct fields) — verify whether stale cursor position can carry over between resize and the next `draw()` call; if so, add state fields. `SocketBackend::clear()` does NOT emit `[2J` (verified: it only resets `current_style`).

3. **Host console** — verify `run_console` in <RepoFile path="src/console/run.rs" /> correctly handles `Event::Resize` by calling `terminal.autoresize()` (already done) and that the debug-bar area is recomputed on the next frame. The existing `split_debug_area(full_area)` already re-reads `frame.area()` each tick, so this should be correct — add a regression test with `TestBackend::resize`.

**Files:** <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> (resize handler), <RepoFile path="crates/jackin-capsule/src/tui/socket_backend.rs" /> (clear/resize), <RepoFile path="src/console/run.rs" /> (verify).

**Acceptance:** Dragging the terminal window to a smaller or larger size while the capsule multiplexer is running must produce a correct full repaint with no gray background, no stale cells, and no layout tears within one animation tick. The regression tests cover `SocketBackend::resize` resetting style state and a `Multiplexer` resize followed by `compose_full_frame(FullRedrawReason::Resize)` producing a non-empty repaint.

### Phase 8b — Focus correctness audit

**Partially implemented on `feature/tui-architecture` (2026-05-31):** fixed the editor General and settings Mounts border/cursor expression mismatches and added a focused editor General regression test. Remaining work: exhaustive single-green-border cluster tests for every host console state.

Systematically audit every screen in the host console and settings for focus violations:

1. **Single-owner rule.** No screen may show two PHOSPHOR_GREEN borders simultaneously. Verify by rendering each screen state in `TestBackend` and asserting exactly one green-bordered cell cluster.

2. **Content block border gated on focus state.** Every `render_scrollable_block_at` / `render_scrollable_block` call must pass a dynamic `focused` expression using this pattern: `let focused = !state.tab_bar_focused && state.<tab>_scroll_focused && state.modal.is_none();`. Never a hardcoded literal `false`. Detect violations with: `grep -rn "render_scrollable_block_at.*,\s*false\s*,"` — each match is a bug.

   The `show_cursor` variable in the corresponding line-builder function must use the **identical boolean expression** as `focused`. Any mismatch (border dark but cursor visible, or border green but cursor hidden) is a design violation.

3. **Tab click sets `tab_bar_focused = true`.** Every `SelectEditorTab` / `SelectSettingsTab` dispatch must ensure the tab bar becomes the focus owner. The underlying `select_editor_tab` / `select_settings_tab` message handlers must set `tab_bar_focused = true`.

4. **Cursor `▸` matches border color.** The cursor must appear if and only if the content block border is green. Any mismatch (cursor visible but border dark, or border green but cursor hidden) is a violation.

5. **Modal steals focus from content block.** When a modal opens, all underlying content blocks must render with `focused = false` regardless of their scroll state — the modal is the sole focus owner.

**Known violations already fixed on this branch:** settings General tab hardcoded `false`, tab click not setting `tab_bar_focused`, sidebar focus clearing on resize.

**Known remaining gaps to audit:** all editor tabs under modal, all settings tabs under modal, list-view detail panels after workspace change, any surface added after this audit passes.

### Phase 9 — TUI source directory convention migration

**Complete on `feature/tui-architecture`.** Capsule-specific rendering files live under `crates/jackin-capsule/src/tui/`; console-specific rendering files live under `src/console/tui/`; `src/console/manager/` now owns the manager model/update code and re-exports the console render module as a compatibility facade.

**Industry reference: Elm Architecture / Model-Update-View.**

The TUI community (Ratatui, tui-rs, Elm, The Elm Architecture, Cursive, builtins in terminal frameworks) has converged on one structural pattern for complex TUI applications: **Model / Update / View** (MUV). jackin' follows this pattern explicitly:

- **Model** — the application state structs (`ManagerState`, `EditorState`, `SettingsState`, `Multiplexer`). Pure data, no rendering, no I/O.
- **Update** — the message handlers (`update_manager`, input dispatch, `handle_mouse`). Pure functions: take a model + event, return a new model + effect requests.
- **View** — the rendering layer (`render`, widgets, raw ANSI paint functions). Pure reads of immutable model; no mutations, no I/O, no side effects.

This pattern is the industry standard for TUI correctness and testability. The `tui/` directory enforces the View boundary: it contains ONLY the View layer. Model lives in `state.rs`; Update lives in `input/` and `message.rs`. Mixing them is a design violation.

**Hard rule for the tui/ directory: rendering only, no business logic.**

The `tui/` directory is a strict rendering boundary. Every file inside it must satisfy this test: *"If this file were deleted, would any workspace resolution, role loading, Docker command, network call, config write, or data-model mutation be affected?"* If yes, the file does not belong in `tui/`. The separation maps directly to the Model/Update/View architecture: `tui/` is the View layer only.

**What belongs in `tui/`** (from [TUI Design Decisions](/reference/tui-design-decisions/#tui-source-code-location--mandatory-directory-convention)):
- Ratatui widget implementations (`Widget` / `StatefulWidget` impls)
- Raw ANSI escape rendering functions
- Screen layout and geometry helpers
- Color token application, border rendering, cursor management
- Focus state rendering, hint bar rendering, lookbook stories

**What must NOT be in `tui/`** (stays alongside the business layer):
- Business logic — workspace resolution, role loading, Docker commands, config parsing
- Input event dispatch and key binding handlers
- Network and I/O code
- State mutation logic (update layer)

The Model/Update/View contract guarantees this: the update layer (`input/`, `message.rs`, `state.rs`) mutates state in response to events; the view layer (`tui/`) reads immutable state and produces pixels. These two must never mix in the same file. A file that both handles input events and renders output is a violation.

**Completed work:**
- `src/console/manager/render/` moved to `src/console/tui/render/`.
- `src/console/widgets/` moved to `src/console/tui/widgets/`.
- `src/console/terminal.rs` moved to <RepoFile path="src/console/tui/terminal.rs" />.
- `mod` declarations and `use` paths updated after the moves.
- `cargo check` and `cargo fmt --check` pass locally after the migration.

Use `git mv` for every file move to preserve history. New TUI files added before migration completes must go into the target directory immediately.

Move all terminal-rendering source files into designated TUI directories so the location of any visual component is unambiguous. The rule: TUI code lives in exactly three homes — `crates/jackin-tui/` (shared), `crates/<crate>/src/tui/` (crate-specific), or `src/console/tui/` (host console). A developer or agent changing a visual behavior must know the exact directory to open without searching.

**Capsule migration** (`crates/jackin-capsule/src/tui/`): move `statusbar.rs`, `chrome_widget.rs`, `dialog.rs`, `dialog_widgets.rs`, `pane_widget.rs`, `socket_backend.rs`, `render.rs`, `branch_context_bar.rs` from the crate root into `src/tui/`. The `daemon/compositor.rs` render methods stay in `daemon/` since they are render+state boundary code, but their rendering primitives (helpers they call) move into `tui/`. Add a `tui/mod.rs` that re-exports what `lib.rs` currently exposes.

**Console migration** (`src/console/tui/`): move `manager/render/` → `tui/render/`, `widgets/` → `tui/widgets/`, `terminal.rs` → `tui/terminal.rs`. Update all `use` paths. The manager input handlers and state structs remain in `manager/` — they are not rendering code.

**Done when:** complete. All surface-specific rendering files live under the designated `tui/` subdirectory; CI still needs to run the normal full matrix before merge.

**Tracked in:** `TODO.md` → `tui-directory-migration`.

### Planned crate landscape (full TUI ecosystem)

When all phases complete, every TUI surface lives in its own crate with explicit MUV layers. This table is the target state:

| Crate | Screens | **Model** | **Update** | **View** | Status |
|---|---|---|---|---|---|
| `jackin-tui` | 0 — shared lib | — | — | `components/`, `theme.rs`, `scroll.rs`, `geometry.rs` | ✅ done |
| `jackin-tui-lookbook` | 1 browser | `stories.rs` | `interactors.rs` | `main.rs` (terminal), `svg.rs` | ✅ done |
| `jackin-capsule` | 1 multiplexer | `session.rs`, `mux_mode.rs` | `action.rs`, `input.rs`, `daemon/` | `tui/` (statusbar, chrome, dialog…) | ✅ done |
| `jackin-console` | 3 screens | per-screen `state.rs` | per-screen `update.rs` | per-screen `tui/` | Phase 10 |
| `jackin-launch` | 1 cockpit | `state.rs` (LaunchView) | `update.rs` (stages, pickers) | `tui/` (rain, rail, failure) | Phase 10 |
| `jackin` binary | 0 | — | — | — routes CLI → crates | ongoing |

**The one rule:** if it is about what the operator sees and interacts with → `tui/`. If it is about data, Docker, or disk → outside `tui/`.

`tui/` contains ALL THREE layers (Model, Update, View) plus event-loop wiring. The boundary is TUI experience vs. business logic, not View vs. Update+Model. For single-screen crates, `tui/` is flat. For multi-screen crates (console), screens are sub-folders inside `tui/screens/` — see [TUI Design Decisions](/reference/tui-design-decisions/#tui-source-code-location--mandatory-directory-convention) for the full layouts.

**`jackin-tui`** is the shared component library — consumed by every crate; never contains surface-specific code.

### Phase 10 — Extract `jackin-console` and `jackin-launch` crates

Split the console and launch surfaces into dedicated crates, each following the per-screen Option B structure described in Phase 9.

**`jackin-console`** — the workspace manager TUI (workspace list, editor, settings):

```
crates/jackin-console/src/
├── lib.rs
├── run.rs              ← event loop (wiring only)
├── state.rs            ← ConsoleState (top-level model wrapping all screens)
├── workspaces/         ← workspace list screen
│   ├── state.rs        ← Model
│   ├── update.rs       ← Update (message handlers, key dispatch)
│   └── tui/            ← View (render fns, pure &State reads)
├── editor/             ← workspace editor screen
│   ├── state.rs
│   ├── update.rs
│   └── tui/
└── settings/           ← settings screen
    ├── state.rs
    ├── update.rs
    └── tui/
```

**`jackin-launch`** — the launch progress cockpit (digital rain, stage rail, failure popup):

```
crates/jackin-launch/src/
├── lib.rs
├── run.rs              ← event loop (wiring only)
├── state.rs            ← LaunchView, stage model
├── update.rs           ← stage transitions, forced-choice picker logic
└── tui/                ← View (cockpit, rain, progress rail, failure popup)
    ├── cockpit.rs
    ├── rain.rs
    └── failure.rs
```

Currently `jackin-launch` content lives in <RepoFile path="src/runtime/progress.rs" /> — one 2000+ line file mixing model, update, and view. Extracting it separates those layers and gives the cockpit its own `cargo build -p jackin-launch` fast path.

**MUV contract reminder:** `tui/` in every crate is the View layer only. `run.rs` is the wiring loop. `update.rs` / `message.rs` is the Update layer. `state.rs` is the Model. Nothing crosses the boundary.

**Rule for multiple screens (console):** split by screen first (`workspaces/`, `editor/`, `settings/`), then put `tui/` inside each screen folder. **Rule for single-screen crates (capsule, launch):** one `tui/` at the crate root is sufficient.

**Prerequisite:** <RepoFile path="src/runtime/progress.rs" /> and `src/console/` must not import each other's types; verify before extraction. Check how much `src/console/` currently imports from `src/runtime/`, `src/config/`, `src/instance/` — those deps must become explicit crate dependencies or trait abstractions.

**Tracked in:** `TODO.md` (add entry `jackin-console-jackin-launch-extraction`).

## Open questions & risks

- **Handoff feasibility.** The single-host-alt-screen approach (the host keeps its alt-screen across `docker exec -it`; the capsule client skips its own `?1049` toggle via `JACKIN_HOST_ALT_SCREEN`) is implemented by the [Launch Progress TUI](/reference/roadmap/launch-progress-tui/) work and must keep being confirmed across real terminals — alt-screen survival, raw mode, SIGWINCH, and the stale `Buffer` full-redraw on resume. If a terminal mishandles the shared screen, the fallback is to tear the host backend down before the exec and re-initialize on return, with the flash minimized.
- **Global session flags.** `host_screen_owned` and `rich_surface_active` encode real product state but are not a durable architecture. Phase 5 must replace them with typed ownership/effects without regressing diagnostics routing, compact fallback output, or panic-safe terminal restore.
- **Async loop shape.** ✅ Resolved: `EventStream` + `tokio::select!` is implemented in the console event loop, replacing the 50ms `event::poll` blocking call.
- **Tokio runtime flavor.** Both binaries run `#[tokio::main(flavor = "current_thread")]` today. Decide whether the host keeps `current_thread` (simplest, no `Send` tax on a large model) once all blocking work is behind `spawn_blocking`, or moves to a small `multi_thread` pool if `spawn_blocking` contention under heavy Docker / `op` fan-out proves measurable. The capsule daemon stays `current_thread` — it is PID 1 in a container with a deliberately tiny footprint.
- **Nested runtimes.** ✅ Resolved: role registration and workspace isolation-drift checks use `spawn_blocking` on the single app runtime; the Stop/Purge in-place Docker handler is `async fn` on the existing reactor; the `save.rs` force-cleanup path now spawns a dedicated OS thread with its own single-threaded runtime and a fresh `ShellRunner`, eliminating the "Cannot start runtime within runtime" panic. The editor literal role registration (test-seam only, `FakeRunner`) remains an isolated test helper with no production impact. No production code path creates a nested runtime.
- **Crate-extraction timing.** Whether the `jackin-console` and `jackin-launch` crate extractions land inside this item's branch or wait for the [Cargo workspace split](/reference/roadmap/cargo-workspace-split/) ~150K-LOC trigger. The Model/Update/View module boundaries that map onto those crates land here regardless; only the physical `crates/` move is in question.
- **Pane-body rendering cost.** ✅ Resolved: ADR-004 chooses the custom cell widget approach. tui-term is incompatible with the vt100 fork; the `PaneBodyWidget` at `crates/jackin-capsule/src/tui/pane_widget.rs` renders `vt100::Screen` cells into the Ratatui Buffer. Benchmark: ~378µs per frame at 200×50 vs ~118µs for the raw-ANSI baseline — acceptable since Ratatui's Buffer::diff eliminates redundant writes at steady state. The capsule bytes-per-frame benchmark at `crates/jackin-capsule/benches/pane_body.rs` measures ~484µs for the full SocketBackend pipeline (first frame includes full diff; subsequent idle frames emit near-zero bytes).
- **`ratatui-textarea` vs. `TextField`.** *Decision:* consolidate the host text input onto the in-house `TextField` (already shared via `jackin-tui`) and drop the third-party, pre-1.0 `ratatui-textarea`, so there is one text-input model. Revisit only if `TextField` lacks needed editing features.
- **`unstable-widget-ref`.** ✅ Resolved: the feature was never used (no `WidgetRef` implementations in the codebase); it has been removed from all three `ratatui` dependency declarations (<RepoFile path="Cargo.toml" />, <RepoFile path="crates/jackin-tui/Cargo.toml" />, <RepoFile path="crates/jackin-capsule/Cargo.toml" />).
- **Lookbook SVG toolchain.** ✅ Resolved: custom `Buffer`→SVG serializer ships in `crates/jackin-tui/src/bin/tui-lookbook.rs`; `vhs`/`termsvg` for motion remains a future option for animated stories.
- **Dirty redraw vs. animation.** Event-driven redraw must still drive spinner / rain animation ticks, so the redraw scheduler needs an animation-timer source, not only input `Message`s.

## Documentation impact

When implementation lands, update the contributor-facing internals: [TUI Design Decisions](/reference/tui-design-decisions/) (add the architecture / Model-Update-View section), [Codebase Map](/reference/codebase-map/) (new module shape), and the relevant ADR. The lookbook adds a new set of Internals pages — one per `jackin-tui` component, with previews generated from real renders. No operator- or role-author-facing page changes, because there is no operator-visible behavior change.

## Host-side effects

None. This is a pure internal refactor of in-process TUI code. It introduces no new writes to the operator's host machine, no new files, and no new container paths.

## Acceptance criteria

- Host event handling routes through a `Message` enum and a central `update`; no renderer takes `&mut state` or performs disk/socket I/O.
- Per-frame filesystem inspection is eliminated; mount facts and instance state are refreshed by background tasks that message the loop.
- Reusable screens are `StatefulWidget` components; the modal stack is a single `Vec<Modal>` shape.
- The console → launch → handoff remains one continuous Ratatui session on the console path, but the continuity is owned by a typed app-level `TerminalSession` rather than global flags; the capsule handoff still preserves the host alt-screen with `JACKIN_HOST_ALT_SCREEN`, and the in-container experience continues the same visual language. *(Manual-smoke criterion — a real-terminal flash is not catchable by `TestBackend` snapshots.)*
- The capsule has a `MuxMode` state machine, a generalized `Action` dispatch with tests, and a decomposed `daemon.rs`; the full/partial compositors share one helper.
- The capsule renders through Ratatui; the pane-body mechanism (`tui-term` vs a custom cell widget) is recorded in an ADR with benchmark evidence.
- Every rule in [TUI Design Decisions](/reference/tui-design-decisions/) still holds; snapshot tests now exist for 4 key render paths (list, settings, editor-general, editor-mounts) via `insta` in <RepoFile path="src/console/tui/render/snapshot_tests.rs" /> — any render regression fails CI.
- Reusable patterns live in <RepoFile path="crates/jackin-tui/Cargo.toml" /> as single implementations; no visual pattern that appears on multiple terminal surfaces has two copies or a runtime dependency on `src/console/widgets`. The confirm dialog, picker, tab strip, scrollbar, panel, hint bar, status/footer bar, and modal frame each have exactly one implementation all surfaces consume.
- Token and layout-helper unification lands without a rendering-backend change; full widget sharing follows the capsule's move to a Ratatui frame.
- Every repeatable visual pattern is a named `jackin-tui` component with a lookbook page rendering its states from the real widget code; no host/capsule drift remains for shared patterns.
- Lookbook previews are generated from `TestBackend` renders (or recorded SVG for motion) and regenerated in CI, so they cannot drift from the shipped components.
- Every TUI surface is built one way: the same Model/Update/View shape, the same `Message` / `Effect` vocabulary, the same `jackin-tui` components, and the same token-based styling. No surface reintroduces a second concept for something the project already does once; an inconsistency between surfaces is treated as a bug.
- Reusable UI lives in the `jackin-tui` crate and the surface crates (or surface modules) compose it; a developer can name the crate or module that owns any given piece of state or rendering from its location alone.
- Module-root files (`mod.rs` / `lib.rs`) are thin — declarations, the `pub use` facade, and `//!` docs only; no module root holds rendering, dispatch, or domain logic, and new TUI modules use the `foo.rs` + `foo/` (no-`mod.rs`) layout.
- A `criterion` frame-time bench guards the host compose path against a named budget, and a capsule bytes-per-frame bench guards the move to Ratatui's `Buffer::diff`; both run in CI.
- No render, update, or effect path can `unwrap` or `panic`; `clippy` (with the no-panic lints) and `rustfmt` run in CI for the TUI crates.
- Every tabbed surface (`ManagerStage::Editor`, `ManagerStage::Settings`) implements the full W3C ARIA Tabs pattern: `←`/`→` cycle tabs, `⇥`/`↓` enters content, `⇧Tab`/`Esc` returns to tab bar.
- Every toggle row responds to `Space` only; every action button responds to `Enter` only. No toggle responds to `Enter`; no action button responds to `Space`.
- No widget renders an internal hint row; all hints appear in the shared footer via `render_hint_bar` or `render_wrapped_hint_bar`.
- Every error surfaces through `ErrorPopup`; no `eprintln!` or status overlay is used for error display on any TUI surface.
- Every dialog with a button row has exactly one empty separator row above the buttons (formula: `content_rows + 5` for outer height).
- Clicking either status bar chip (instance ID or run ID) shows the correct dialog with all expected fields on every surface where the chip is visible.
- Long values (file paths, URLs) in any dialog are either OSC 8 hyperlinks, word-wrapped, or accompanied by a scrollbar — never silently truncated.
- Every screen has exactly one PHOSPHOR_GREEN border visible at any time; no screen state produces two green-bordered containers simultaneously. Focus transfers only via keyboard, mouse click, or scroll-hover — never via resize, content fitting, or navigation keys within a focused container.
- Every `render_scrollable_block_at` / `render_scrollable_block` call passes a dynamic `focused` expression; no hardcoded `false` exists for content that can receive focus.
- All surface-specific TUI rendering files live in designated `tui/` subdirectories: `crates/jackin-capsule/src/tui/` for capsule, `src/console/tui/` for host console. No terminal-rendering code exists outside these paths or `crates/jackin-tui/`.
- No background action stands up a nested Tokio runtime; all blocking work runs as a `spawn_blocking` effect on the single app runtime and returns a `Message`.
- The whole item ships as one branch and one pull request of small, always-green commits; no phase is merged on its own.
- No phase introduces an operator-visible behavior change.
