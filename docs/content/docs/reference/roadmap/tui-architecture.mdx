---
title: "TUI architecture - Ratatui application patterns"
---

**Status**: Partially implemented. The shared `jackin-tui` component crate exists, the launch cockpit is substantially extracted, and the capsule renders through Ratatui. The remaining work is to make `jackin-console`, `jackin-launch`, and `jackin-capsule` all follow one source layout and one Elm Architecture boundary.

## Goal

jackin' has three terminal applications:

- `jackin-console` - the host workspace console.
- `jackin-launch` - the launch progress cockpit.
- `jackin-capsule` - the in-container multiplexer.

All three should read like the same kind of Ratatui application. A contributor changing TUI behavior should know where to put state, messages, update logic, effects, subscriptions, rendering, terminal wiring, screen modules, and local components without searching through unrelated runtime, Docker, config, credential, or protocol code.

The target is not a visual redesign. The target is a clearer architecture: TUI code is grouped under the TUI boundary, non-visual product work stays outside it, and external work returns to the TUI through typed messages.

This is primarily a refactor. Existing operator workflows must keep working the same way unless a later PR explicitly calls out and justifies a user-visible behavior change. Structural moves may tune internal behavior and TUI rendering consistency, but they must not silently change command semantics, launch flow, capsule attach behavior, saved state, keyboard/mouse contracts, or error handling.

## Architecture rule

Each surface crate has one TUI boundary:

```text
crates/<surface-crate>/src/tui.rs
crates/<surface-crate>/src/tui/
```

`src/tui.rs` is the thin module root. It contains module declarations, intentional re-exports, and short `//!` orientation docs only.

`src/tui/` contains the whole TUI application layer for that surface, organized with The Elm Architecture:

| Layer | Owns |
|---|---|
| Model | Visible terminal state: active screen, focus owner, modal stack, scroll offsets, hover/click state, picker state, animation state, render-safe snapshots |
| Message | Semantic TUI events and results: key/mouse intent, resize, tick, background completion, dialog outcome, screen transition |
| Update | Deterministic state transitions: apply `Message` to the model and return typed effects |
| Effect / Subscription | Typed descriptions of non-TUI work and long-lived event sources; they describe work but do not execute external commands inside TUI code |
| View | Pure Ratatui/raw-ANSI frame composition from the current model |
| Terminal/run loop | Terminal lifecycle, backend adapter, event-loop wiring, and routing of completed effects back into messages |
| Components | Surface-local visual components that are not reusable enough for `jackin-tui` |

The shared component/design-system crate stays separate:

```text
crates/jackin-tui/src/
```

`jackin-tui` owns reusable visual primitives, tokens, theme adapters, geometry helpers, shared runtime abstractions, and lookbook-backed components. It must not contain console-, launch-, capsule-, Docker-, workspace-, credential-, or command-execution logic.

## Boundary rule

`src/tui/` is for terminal interaction. It is not a place for business logic or external command execution.

**Belongs in `src/tui/`:**

- TUI model structs and enums.
- TUI `Message` / `Action` enums.
- Update functions that mutate only TUI state.
- Effect and subscription type definitions.
- Event-loop wiring that maps terminal events and completed background work into messages.
- Terminal lifecycle/backend adapters, including raw mode, alternate screen, and capsule socket backends.
- Ratatui `Widget` / `StatefulWidget` implementations.
- Raw-ANSI rendering only where a surface still requires it.
- Frame composition, layout, geometry, hitboxes, focus rendering, hint/footer rendering, and local UI components.

**Stays outside `src/tui/`:**

- Docker/runtime command execution.
- Git, GitHub CLI, 1Password CLI, shell command, filesystem, and network operations.
- Config/schema loading, migration, and persistence.
- Workspace resolution, role loading, mount inspection, and durable state cleanup.
- Credential handling and token/domain logic.
- Capsule PTY/session/control-plane authority.
- Capsule wire protocol definitions.
- Pure non-visual product rules that do not depend on terminal interaction.

The allowed connection is typed effects:

```text
tui/update.rs
  -> returns Effect::RefreshInstances

effect executor / run loop
  -> calls non-TUI service/runtime code

non-TUI code
  -> performs Docker/runtime work
  -> returns typed data or typed error

tui/message.rs
  <- Message::InstancesRefreshed(result)

tui/update.rs
  -> updates the TUI model
```

No renderer, component, or update function should call Docker, git, `op`, `gh`, the filesystem, the network, or config persistence directly.

## Final layouts

These examples are the target shape. They intentionally use the Rust 2018 `foo.rs + foo/` module-root style used elsewhere in the repo.

### Shared TUI crate

```text
crates/jackin-tui/src/
|-- lib.rs
|-- components.rs
|-- components/
|   |-- button_strip.rs
|   |-- confirm_dialog.rs
|   |-- container_info.rs
|   |-- error_dialog.rs
|   |-- hint_bar.rs
|   |-- panel.rs
|   |-- scrollable_panel.rs
|   |-- select_list.rs
|   |-- status_footer.rs
|   |-- tab_strip.rs
|   `-- text_input.rs
|-- geometry.rs
|-- runtime.rs
|-- scroll.rs
`-- theme.rs
```

`jackin-tui` is the only place for cross-surface visual primitives. A surface-specific component starts in that surface's `src/tui/components/`; it graduates to `jackin-tui` only when more than one surface should consume the same implementation.

### Single-screen surface

Use this for `jackin-launch` and for `jackin-capsule` unless a surface gains multiple distinct screens later.

```text
crates/<surface>/src/
|-- lib.rs
|-- domain/                 # optional, pure non-visual rules
|-- services/               # optional, side-effect adapters
|-- tui.rs                  # thin module root
`-- tui/
    |-- app.rs              # TUI model
    |-- message.rs          # TUI message/action vocabulary
    |-- update.rs           # state transition + effect requests
    |-- effect.rs           # typed effect requests only
    |-- subscriptions.rs    # timers, background completions, long-lived streams
    |-- run.rs              # event-loop wiring
    |-- terminal.rs         # terminal lifecycle/backend/capabilities
    |-- view.rs             # top-level frame composition
    |-- components.rs       # thin local-component module root
    `-- components/
```

Concrete `jackin-launch` target:

```text
crates/jackin-launch/src/
|-- lib.rs
|-- progress.rs             # non-visual launch orchestration / host adapter boundary
|-- build_log.rs            # build-log capture/storage when not visual
|-- tui.rs
`-- tui/
    |-- app.rs
    |-- message.rs
    |-- update.rs
    |-- effect.rs
    |-- subscriptions.rs
    |-- run.rs
    |-- terminal.rs
    |-- view.rs
    |-- components.rs
    `-- components/
        |-- rain.rs
        |-- progress_rail.rs
        |-- failure_dialog.rs
        |-- build_log_dialog.rs
        |-- container_info_dialog.rs
        `-- prompts.rs
```

Concrete `jackin-capsule` target:

```text
crates/jackin-capsule/src/
|-- lib.rs
|-- main.rs
|-- protocol/               # wire protocol and frame types
|-- daemon/                 # PTY/session/control-plane authority
|-- runtime_setup.rs        # non-visual container setup
|-- tui.rs
`-- tui/
    |-- app.rs              # visible multiplexer model
    |-- message.rs
    |-- update.rs
    |-- effect.rs
    |-- subscriptions.rs
    |-- run.rs              # attach/client TUI loop when applicable
    |-- terminal.rs
    |-- socket_backend.rs
    |-- view.rs
    |-- components.rs
    `-- components/
        |-- chrome.rs
        |-- status_bar.rs
        |-- branch_context_bar.rs
        |-- pane.rs
        |-- dialog.rs
        |-- palette.rs
        `-- container_info_dialog.rs
```

### Multi-screen surface

Use this for `jackin-console`.

```text
crates/jackin-console/src/
|-- lib.rs
|-- domain/                 # optional, pure non-visual console/product rules
|-- services/               # optional, command/IO adapters
|-- tui.rs                  # thin module root
`-- tui/
    |-- app.rs              # ConsoleApp, active screen, modal stack
    |-- message.rs          # top-level messages
    |-- update.rs           # top-level routing/update
    |-- effect.rs           # typed effect requests only
    |-- subscriptions.rs
    |-- run.rs
    |-- terminal.rs
    |-- view.rs             # top-level frame/chrome/modal composition
    |-- components.rs       # thin console-local component module root
    |-- components/         # console-local shared components
    |-- screens.rs          # thin screen module root
    `-- screens/
        |-- workspaces/
        |   |-- model.rs
        |   |-- message.rs
        |   |-- update.rs
        |   |-- effect.rs
        |   |-- view.rs
        |   `-- components/
        |-- editor/
        |   |-- model.rs
        |   |-- message.rs
        |   |-- update.rs
        |   |-- effect.rs
        |   |-- view.rs
        |   `-- components/
        `-- settings/
            |-- model.rs
            |-- message.rs
            |-- update.rs
            |-- effect.rs
            |-- view.rs
            `-- components/
```

Do not create `screens/` for a single-screen crate. Do not create nested `tui/screens/<screen>/tui/`; once code is inside `src/tui/screens/<screen>/`, the screen's TEA files already express the TUI layers.

## Current gap

The current repo is transitional. The target above should guide all remaining moves.

| Surface | Current shape | Remaining cleanup |
|---|---|---|
| `jackin-tui` | Shared components and helpers already live under <RepoFile path="crates/jackin-tui/src/lib.rs" />, <RepoFile path="crates/jackin-tui/src/components.rs" />, and <RepoFile path="crates/jackin-tui/src/components/panel.rs" /> style modules. | Keep it visual/shared only. Do not add surface-specific product semantics. |
| `jackin-launch` | The thin module root is <RepoFile path="crates/jackin-launch/src/tui.rs" />. The launch model/message/update/effect/input-subscription/terminal/run-loop pieces plus semantic prompt context styling live under <RepoFile path="crates/jackin-launch/src/tui/app.rs" />, <RepoFile path="crates/jackin-launch/src/tui/message.rs" />, <RepoFile path="crates/jackin-launch/src/tui/update.rs" />, <RepoFile path="crates/jackin-launch/src/tui/effect.rs" />, <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" />, <RepoFile path="crates/jackin-launch/src/tui/terminal.rs" />, and <RepoFile path="crates/jackin-launch/src/tui/run.rs" />. The background rich render driver and terminal hyperlink-overlay emission now also live in `tui/run.rs`; <RepoFile path="crates/jackin-launch/src/progress.rs" /> remains the launch progress orchestration boundary. Top-level frame composition and pure hyperlink-overlay byte derivation live in <RepoFile path="crates/jackin-launch/src/tui/view.rs" />, and launch-local visual pieces live under <RepoFile path="crates/jackin-launch/src/tui/components.rs" /> and `crates/jackin-launch/src/tui/components/`, with standalone prompt context styling and role-trust confirm dialog state constructed in <RepoFile path="crates/jackin-launch/src/tui/run.rs" />. Docker-build output remains captured outside the TUI in <RepoFile path="crates/jackin-launch/src/build_log.rs" />; the run loop snapshots it into <RepoFile path="crates/jackin-launch/src/tui/app.rs" /> so the build-log overlay renders from the model instead of reading the sink directly. | Keep non-visual launch orchestration and host adapters outside `src/tui/`; grow typed launch effects only in `effect.rs` when update needs side effects. |
| `jackin-capsule` | The thin module root is <RepoFile path="crates/jackin-capsule/src/tui.rs" />. Visible multiplexer model vocabulary such as mux mode, hover targets, pointer shape, visible agent state, visible pane layout snapshots and derivation, drag-resize state, mux-mode derivation, pointer-shape derivation, and cursor-visibility derivation now lives in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />, and visible pane tree/tab geometry, content-row and content-rect derivation, plus pane-local mouse coordinate mapping now live in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. Terminal input parsing lives in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />, visible redraw/update vocabulary, partial-frame invalidation policy, and drag-resize ratio calculation live in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />, attach-client effect and subscription vocabulary lives in <RepoFile path="crates/jackin-capsule/src/tui/effect.rs" /> and <RepoFile path="crates/jackin-capsule/src/tui/subscriptions.rs" />, interactive attach-loop wiring lives in <RepoFile path="crates/jackin-capsule/src/tui/run.rs" />, and attach-client raw-mode, reset handling, terminal size normalization, and OSC 22 pointer-shape encoding live in <RepoFile path="crates/jackin-capsule/src/tui/terminal.rs" />. Full Ratatui frame composition plus raw-ANSI status bar, pane chrome, pane body, dialog overlay, dialog footer, bottom chrome, selection-highlight render helpers, spawn-failure visible message/banner formatting, and spawn-capacity failure wording live in <RepoFile path="crates/jackin-capsule/src/tui/view.rs" />, status-bar prefix-mode mapping and supplied visible identity-label storage live in <RepoFile path="crates/jackin-capsule/src/tui/components/status_bar.rs" />, command-palette close-label derivation lives in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" />, and mouse text-selection state, start, motion, and copy helpers live in <RepoFile path="crates/jackin-capsule/src/tui/selection.rs" />. Semantic TUI actions live in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />, Ratatui/raw-ANSI rendering helpers live under <RepoFile path="crates/jackin-capsule/src/tui/render.rs" /> and siblings, and capsule-local visual components live under <RepoFile path="crates/jackin-capsule/src/tui/components.rs" /> and `crates/jackin-capsule/src/tui/components/`; callers use those canonical `tui::` paths instead of flat root aliases. Pure pull-request render snapshots live outside session authority in <RepoFile path="crates/jackin-capsule/src/pull_request.rs" />. Daemon/session/protocol code remains outside and calls TUI composition helpers for visible frames. | Keep daemon, protocol, PTY/session authority, runtime setup, and control-plane code outside `src/tui/`; keep moving only visible multiplexer app state and TUI event/update code into the TUI boundary where it is currently still mixed with daemon modules. |
| `jackin-console` | The thin module root is <RepoFile path="crates/jackin-console/src/tui.rs" />. Top-level generic app model plus message/update carriers and effect/subscription vocabulary now live in <RepoFile path="crates/jackin-console/src/tui/app.rs" />, <RepoFile path="crates/jackin-console/src/tui/message.rs" />, <RepoFile path="crates/jackin-console/src/tui/update.rs" />, <RepoFile path="crates/jackin-console/src/tui/effect.rs" />, and <RepoFile path="crates/jackin-console/src/tui/subscriptions.rs" /> while root-only product message/update now lives under <RepoFile path="src/console/tui/message.rs" /> and root-only typed effect requests live under <RepoFile path="src/console/tui/effect.rs" />. Root-only app constructors live under <RepoFile path="src/console/tui/app.rs" />, root-only model state and create-prelude model transitions live under <RepoFile path="src/console/tui/state.rs" />, root-only launch dispatch lives under <RepoFile path="src/console/tui/launch.rs" />, agent/role prompt flow with crate-owned role-resolution, no-eligible-launch, and workspace-list instance and instance-refresh index popup wording lives under <RepoFile path="src/console/tui/prompts.rs" />, input dispatch lives under <RepoFile path="src/console/tui/input/mod.rs" />, and root-state debug location labels live under <RepoFile path="src/console/tui/debug.rs" /> while generic key/modal debug naming lives in <RepoFile path="crates/jackin-console/src/tui/debug.rs" /> so keyboard/mouse handling stays inside the root TUI boundary while it still depends on root types. Root-only footer hint composition lives under <RepoFile path="src/console/tui/components/footer.rs" /> and `src/console/tui/components/footer/`; the root-dependent op-picker adapter lives under <RepoFile path="src/console/tui/op_picker/mod.rs" /> until its `operator_env` types move behind generic adapters. Root-only effect execution and background polling live outside the TUI boundary in <RepoFile path="src/console/effects.rs" /> so `message.rs` stays focused on semantic messages and deterministic update. Event-loop shell helpers plus global quit/Q-intercept policy, confirm-state construction, and Claude token-generate status wording plus in-place instance-action busy/failure wording now live in <RepoFile path="crates/jackin-console/src/tui/run.rs" />, terminal lifecycle abstractions live in <RepoFile path="crates/jackin-console/src/tui/terminal.rs" />, and top-level header/footer composition helpers plus footer-height calculation live in <RepoFile path="crates/jackin-console/src/tui/view.rs" />. The root application crate's host debug-buffering adapter for that terminal lifecycle lives outside the TUI tree in <RepoFile path="src/console/terminal.rs" />. Screen model/message/update/effect/view helpers now live under <RepoFile path="crates/jackin-console/src/tui/screens.rs" /> and `crates/jackin-console/src/tui/screens/`, with screen model state in each screen's `model.rs`, root-independent screen message/effect vocabulary in each screen's `message.rs` and `effect.rs`, tab label plus environment label composition in the editor/settings `view.rs` files, and workspaces disclosure rendering, running-instance summary rendering, instance session empty/error wording, and new-workspace description pane rendering in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />; local visual components live under <RepoFile path="crates/jackin-console/src/tui/components.rs" /> and `crates/jackin-console/src/tui/components/`, including the file-browser state/input/render modules under <RepoFile path="crates/jackin-console/src/tui/components/file_browser/mod.rs" />. File-browser Git URL lookup now leaves input as `ManagerEffect::ResolveFileBrowserGitUrl`, and file-browser directory navigation plus commit validation leave input as `ManagerEffect::ApplyFileBrowserOutcome` requests executed by the root file-browser service adapter <RepoFile path="src/console/services/file_browser.rs" /> through <RepoFile path="crates/jackin-console/src/services/file_browser.rs" />. Browser launches, container-info clipboard writes, and workspace deletes queue `ManagerEffect::OpenUrl`, `ManagerEffect::CopyContainerInfoValue`, and `ManagerEffect::RemoveWorkspace`; the root TUI run loop executes those root effects through <RepoFile path="src/console/effects.rs" />, which calls <RepoFile path="src/console/services/browser.rs" />, writes terminal clipboard sequences, and performs config persistence outside the TUI input/update boundary. Manager mount-info, instance-refresh, settings-save, auth-form 1Password read-validation, role repository registration, trusted-role persistence, workspace-save drift gates, isolation-record scans, cleanup workers, workspace writes, create-prelude file-browser opens/reopens, editor add-mount browser opens, global mount browser opens, file-browser Git URL resolution, file-browser navigation/commit validation, URL opens, container-info clipboard writes, workspace deletes, and op-picker metadata-load requests leave update/input/picker state as typed effects or pending load requests; <RepoFile path="src/console/effects.rs" /> executes those effects and calls <RepoFile path="crates/jackin-console/src/services/mount_info.rs" />, <RepoFile path="src/console/services/instances.rs" />, <RepoFile path="src/console/services/config.rs" />, <RepoFile path="src/console/services/op.rs" />, <RepoFile path="src/console/services/op_picker.rs" />, <RepoFile path="src/console/services/role_load.rs" />, <RepoFile path="src/console/services/agents.rs" />, <RepoFile path="src/console/services/token_setup.rs" />, and <RepoFile path="src/console/services/workspace_save.rs" /> and return results as `ManagerMessage` values, state updates, or pending subscriptions. Root-console side effects that still depend on the root application crate are isolated under <RepoFile path="src/console/services.rs" /> and its children: browser launch, config persistence for role/workspace/settings saves, Docker-backed instance discovery, 1Password probing/read validation, op-picker metadata loading/cache invalidation, role repository registration, trusted-role persistence, agent resolution, Claude token minting, and workspace-save drift/cleanup workers. Pure auth-kind/config-mode derivation, GitHub auth env-preserving mode-change construction, role-source derivation, launch provider derivation, instance-refresh snapshot data, workspace-choice construction, shared mount draft construction, synthetic current-directory mount construction, global-mount naming and scope parsing, workspace-save prospective mount merging, save-preview validation message text, and workspace-edit delta construction live in <RepoFile path="src/console/domain.rs" />. | Keep moving the remaining root-console app/message/update/view plumbing into the `jackin-console` TUI boundary and replace the remaining modal/input service bridges with typed effects/messages. |

## Remaining work

Launch rich-terminal capability checks and the canonical "requires a rich terminal" error message now live in <RepoFile path="crates/jackin-launch/src/tui/terminal.rs" />. Launch orchestration and rich renderer entry call that TUI terminal helper instead of carrying duplicate terminal requirement wording in progress/run code.

Launch terminal-size-to-frame-area fallback now also lives in <RepoFile path="crates/jackin-launch/src/tui/terminal.rs" />. The rich render task and cockpit input subscription ask the terminal helper for the current area instead of duplicating `crossterm::terminal::size()` fallback logic around build-log scrolling.

Launch rich-dialog-required fallback wording now lives in <RepoFile path="crates/jackin-launch/src/tui/run.rs" />. The progress orchestration boundary in <RepoFile path="crates/jackin-launch/src/progress.rs" /> still decides whether a rich renderer is active, but it asks the TUI run boundary for the operator-facing error message when a prompt requires that renderer.

Latest console cleanup: workspace-screen top-level frame geometry, modal-overlay visibility facts, modal backdrop rendering, destructive-confirm modal placement, settings-error modal placement, and status-overlay placement now live in <RepoFile path="crates/jackin-console/src/tui/view.rs" />. The root frame renderer supplies root state and concrete widgets, but no longer owns those generic console frame layout rules.

Create-workspace prelude destination/name default derivation now lives with the workspaces screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root create-prelude state still stores the concrete selected source/destination/workdir facts, but it asks the screen helper for visible default strings before opening destination/name inputs or mount-destination choices.

Workspace-list header title wording now also lives with top-level console view helpers in <RepoFile path="crates/jackin-console/src/tui/view.rs" />. Root frame rendering asks that helper for the workspace screen title instead of carrying root-side copy.

Root-console list pre-render preparation now also uses the crate-owned workspace frame geometry from <RepoFile path="crates/jackin-console/src/tui/view.rs" /> before clamping list scroll, so render and pre-render state preparation share the same body rectangle.

Console startup `op` CLI availability probing now happens before `src/console/tui/run.rs` enters the TUI loop. The run loop receives the already-probed availability fact and initializes visible state from it instead of probing 1Password from inside the TUI boundary.

Launch prompt agent-choice lookup now calls the non-TUI agent service boundary in <RepoFile path="src/console/services/agents.rs" /> directly from the root run loop before `src/console/tui/prompts.rs` opens inline prompt state. The TUI prompt code owns only prompt drawing and state transitions; Docker-backed role/agent resolution remains outside `src/console/tui/`.

The shared confirm dialog in <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" /> now exposes only generic prompt/detail/note rendering. Console and launch role-trust flows build their role/repository text in their owning surface code instead of storing role-specific product semantics in `jackin-tui`.

Reusable console mount-row formatting and width/height calculations now live in <RepoFile path="crates/jackin-console/src/mount_display.rs" />. The root component adapter in <RepoFile path="src/console/tui/components/mount_display.rs" /> translates root `MountConfig`, global-mount rows, and mount-info cache labels into the crate-owned display inputs.

Modal size and placement rules now flow through the surface-local rect-mode and rect-spec helpers in <RepoFile path="crates/jackin-console/src/tui/components/modal_rects.rs" />. The root component adapters in <RepoFile path="src/console/tui/components/modal_layout.rs" /> and <RepoFile path="src/console/tui/components/modal.rs" /> map root `Modal` variants to crate-owned modes/widgets and supply only dynamic facts such as picker counts, confirm height, and error height instead of owning generic centered-rect decisions, fixed modal dimensions, or widget dispatch in the root view itself; settings-modal render adapters now call the crate-owned rect specs directly instead of carrying duplicate root wrapper helpers.

Console status-popup, error-popup, role-load/editor-action/GitHub-preview/save/1Password-read/workspace-list instance/instance-refresh index/in-place instance-action error-popup, in-place instance-action failure message formatting, debug run-info container popup, and editor exit save/discard prompt state construction now live under <RepoFile path="crates/jackin-console/src/tui/components/status_popup.rs" />, <RepoFile path="crates/jackin-console/src/tui/components/error_popup.rs" />, <RepoFile path="crates/jackin-console/src/tui/components/container_info.rs" />, and <RepoFile path="crates/jackin-console/src/tui/components/save_discard.rs" />; root prompt/message/input/run adapters pass only title, message, run ID, log-path facts, and concrete errors.

Role repository availability, cached-remote mismatch, invalid role-repository, generic role-repository failure, and missing role-file wording now also live in <RepoFile path="crates/jackin-console/src/tui/components/error_popup.rs" />. Root state still classifies root-only `RepoError` and `RoleRepoValidationError` values, but it maps them to crate-owned human text instead of carrying the popup copy itself.

Token-generation failure, open-URL failure, delete failure, and create-prelude file-browser failure titles now also live in <RepoFile path="crates/jackin-console/src/tui/components/error_popup.rs" />. The root effect executor still performs the side effects, but it uses crate-owned popup helpers/titles when surfacing failures back into editor, settings, or workspace-list modals.

Role-registration loading, workspace-save drift-check, and isolated-state cleanup status popup construction now lives in <RepoFile path="crates/jackin-console/src/tui/components/status_popup.rs" />. The root effect executor still starts the background workers and stores the pending subscriptions, but it asks the console TUI component boundary for the visible status state.

Root-console editor and mouse tests now also seed role-loading status and editor-exit save/discard modals through the same crate-owned helpers used by runtime. This keeps audit searches from reintroducing direct root-side popup/dialog constructors as test-only fixtures.

Test-only role-input registration helpers now live with <RepoFile path="src/console/effects.rs" /> instead of <RepoFile path="src/console/tui/input/editor.rs" />. Editor input tests still use fake runners, but service registration and trusted-role persistence plumbing stay outside the TUI input boundary.

Editor save-flow preserved-state confirm wording and running-isolated-state block wording now live with editor screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root save input passes only the affected container names and keeps drift detection, cleanup requests, and workspace-write effects outside the crate-owned screen view.

Editor General-tab content-width calculation now uses <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" /> for the same row labels and enabled/disabled wording used by rendering. Root layout code supplies only the current workspace name, shortened workdir, keep-awake flag, and git-pull flag before applying the crate-owned width helper.

Editor header title wording now also lives with editor screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root rendering passes the editor mode/name facts and no longer owns the `edit workspace` / `create workspace` title copy.

Editor workspace-name fallback derivation now also lives with editor screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input and root auth-panel state pass only edit/create mode, optional pending name, and the caller-specific create fallback before opening the rename input or resolving the workspace key used by the auth panel.

The root-dependent console op-picker adapter now lives under <RepoFile path="src/console/tui/op_picker/mod.rs" /> instead of `src/console/tui/components/`, so the remaining root-bound load/input/state bridge is no longer stored as a visual component.

The op-picker background-worker disconnect message now lives in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. The root adapter still owns the `operator_env` runner, subscriptions, and cache binding, but visible recoverable-error wording stays with the component boundary.

Console op-picker load-error state shaping now also lives with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. The root adapter still receives concrete `op` runner failures and subscription closure facts, but it asks the component boundary to classify probe failures, preserve recoverable messages, and construct the standard worker-disconnect state.

Console op-picker blocked-load key policy now lives with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. The root input adapter still maps concrete crossterm keys to `Esc`/not-`Esc` facts and returns `ModalOutcome`, but the rule that loading and fatal picker states cancel on `Esc` and consume all other keys is picker-owned.

Console op-picker commit selection shaping now lives with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. The root adapter still supplies concrete `operator_env` reference and field-target types, but the picker-owned `Existing`, `NewItem`, and `EditItemField` selection variants are assembled through crate-owned helpers instead of inline root input matches.

Console subscription-worker disconnect fallback messages now live with the console TUI subscription vocabulary in <RepoFile path="crates/jackin-console/src/tui/subscriptions.rs" />. Root polling in <RepoFile path="src/console/tui/state.rs" /> and root test helpers still own concrete worker receivers, but they no longer hard-code the operator-facing disconnected-worker text.

Console editor/settings file-browser URL opens now return typed modal outcomes and are queued as `ManagerEffect::OpenUrl` by input dispatch. The modal handlers no longer side-channel browser requests through mutable URL slots.

Console GitHub auth mode changes now preserve existing `[github.env]` maps through a non-visual helper in <RepoFile path="src/console/domain.rs" />. Editor Auth-tab commit/reset mutation for workspace and role auth layers also lives in that domain boundary: root auth input commits the shared auth form, then delegates mode-block updates, Github env insertion, and Zai ignore cleanup. Settings auth state also reads global `[github.env]` defaults through <RepoFile path="src/console/domain.rs" />, and settings auth source lookup, credential-env commit routing, and credential-env clear routing ask the same domain boundary whether a kind reads `[github.env]` or normal environment maps. Root settings state stores only pending/original maps for dirty tracking, form mounting, and save-preview adapters. Editor auth row/form adapters also ask <RepoFile path="src/console/domain.rs" /> for effective-mode credential requirements, role-key eligibility, explicit workspace modes, display source-layer lookup, and workspace/role mode-plus-credential derivation before passing those facts into crate-owned row planning or shared auth forms.

Capsule tab-pane visible-kind derivation now lives with the capsule TUI app vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />. The daemon pane-layout adapter still reads concrete session agent/provider facts from <RepoFile path="crates/jackin-capsule/src/daemon/pane_layout.rs" />, but it delegates the shell-vs-agent visible tab classification and agent label formatting to the TUI boundary before computing the tab strip auto-label.

Capsule pane-motion and mouse-release gesture routing now lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon input dispatcher still owns the concrete drag/selection state mutations and PTY forwarding in <RepoFile path="crates/jackin-capsule/src/daemon/input_dispatch.rs" />, but it asks the TUI message boundary whether a motion/release should become drag motion, selection motion/finalization, resize end, or mouse forwarding.

Capsule pane-data redraw planning now lives with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon dispatcher still sends the operator bytes to the focused PTY and updates session-side scrollback/input state, but the visible reason for a redraw after pane data (`scrollback-movement`, `explicit-redraw`, or no redraw) is derived by the TUI update boundary.

Capsule status-bar click routing now lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon still computes concrete hit facts such as tab index, double-click timing, and menu-hit state in <RepoFile path="crates/jackin-capsule/src/daemon/input_dispatch.rs" />, then the TUI message boundary decides whether the click is a tab switch, rename request, palette open, or no-op.

Capsule branch-context bar click routing now also lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon still asks the branch-context bar component for concrete hit facts, but the TUI message boundary maps those facts to GitHub-context, container-info, or no-op actions.

Capsule protocol-agent-state to visible-agent-state mapping now lives with capsule TUI app vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />. The daemon compositor still reads concrete session protocol state from <RepoFile path="crates/jackin-capsule/src/daemon/compositor.rs" />, but it delegates the status-bar visible state mapping to the TUI boundary.

Capsule command-palette route planning now lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon input dispatcher still opens dialogs, switches tabs, toggles zoom, clears panes, and redraws concrete terminal state, but the palette command to sub-dialog/action route, including single-pane close confirmation versus split-pane close-target drill-down, is TUI-owned.

Capsule confirmed-action route planning now lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon input dispatcher still clears dialog state and performs the destructive terminal action, but the mapping from confirmation kind to close-pane, close-tab, or exit-all route is no longer daemon-owned.

Capsule palette-toggle routing now lives with semantic TUI action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon input dispatcher still opens or clears concrete dialog stack state, but the "palette key closes an open dialog, otherwise opens the palette" rule is TUI-owned.

Capsule dialog-action redraw planning now lives with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still applies concrete dialog actions and clipboard writes, but the distinction between copy-feedback overlay redraw and full dialog-change redraw is TUI-owned.

Capsule selection-start redraw planning now lives with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still detects concrete pane selection state from live geometry/session facts, but the "selection began, repaint highlight" invalidation rule is TUI-owned.

Capsule focus-change, drag-resize, and selection-change redraw planning now also live with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still mutates concrete pane focus, split ratios, and selection state, but the visible invalidation reasons for those changes are TUI-owned.

Capsule palette-route redraw planning now lives with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still executes palette commands against tabs, panes, and dialogs, but the route-to-redraw rule for terminal actions is TUI-owned.

Capsule direct-action frame planning now lives with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still validates action preconditions and mutates concrete tabs, panes, dialogs, zoom, and detach state, but the visible full-frame versus overlay-frame reason for those direct actions is TUI-owned.

Capsule wheel scrollback, unsafe partial fallback, and pane-cache-miss redraw reasons now live with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon/compositor still computes scrollback availability, Ratatui fallback, and pane-body cache validity, but the visible invalidation reasons are TUI-owned.

Capsule lifecycle redraw reasons for first attach, resize, session exit, status change, dialog-change overlay, and explicit redraw now also live with visible update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still owns attach handshakes, client resize messages, session exit removal, status-context workers, and prefix-mode mutation, but it asks the TUI update boundary for the visible full-frame reason.

Settings screen view geometry now owns the reusable global-mount scroll-frame calculation in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />; root settings geometry supplies only root mount rows and cache-derived content width.

Settings header title wording now lives with settings screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root rendering asks the screen helper for the title instead of carrying settings-screen copy.

Settings global-mount scroll clamping now uses the same crate-owned settings frame geometry helper that settings rendering uses, so scroll preparation and frame rendering cannot drift on header/tab/body/footer row allocation.

Settings tab content-height rules for Mounts, Auth, Environment, and Trust tabs now live with the settings screen view helpers. Root layout code supplies row counts and error-state facts instead of owning the tab-specific height math; Auth height uses the same detail-row-count helper that settings input uses for focus.

Console modal letter-consumption policy for the global quit intercept now lives in <RepoFile path="crates/jackin-console/src/tui/run.rs" />. The root run loop maps concrete root modal variants into generic `LetterInputState` facts instead of owning which modal shapes consume typed letters.

Settings error-popup title, sensitive-path abort wording, global-mount missing-GitHub-URL and add/edit validation wording, global-mount scope text fallback, and settings frame layout plus General-tab, Mounts-tab, Environment-tab, Auth-tab, and Trust-tab line composition now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root rendering/input passes only selected/hovered row state, pending toggle values, formatted mount rows, pending environment/trust rows, root value lookup adapters, root environment values mapped into display-value enums, auth mode labels, open-URL availability facts, add-draft presence, optional global-mount scope, and focus visibility instead of owning the screen-specific row styling or inline screen copy itself.

Settings environment key text-input construction now lives in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input passes only the pending environment map, target scope, label, and initial value instead of owning duplicate-key validation labels for settings-env modals.

Global-mount confirm state, global-mount scope-picker state, global-mount text-input state, and settings environment value text-input state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input wraps those crate-owned state objects in root modal variants instead of owning the product-specific prompt/title/input-state construction itself.

Global-mount text-target labels now also live with settings screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input supplies the target and current value, then uses the crate-owned label for add/edit text modals instead of hard-coding duplicate prompt strings.

Settings environment source-picker state, scope-picker state, inline error-message wording, role-picker default/launch confirm labels, and delete-confirm state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" /> and <RepoFile path="crates/jackin-console/src/tui/components/role_picker.rs" />. Root settings input keeps routing key commits and pending picker values, while the screen/component helpers own the reusable component state for the environment modal chain.

Settings environment value-edit, current-value fallback, new-key, and empty-key text labels now live with settings screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input supplies only the selected environment key, selected scope, and optional persisted value before mounting the crate-owned text-input state.

Settings environment add-cancel wording now also lives with settings screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/view.rs" />. Root settings input still decides when a cancelled modal chain should surface an error, but no longer hard-codes that operator-facing message.

Settings tab-bar focus planning, tab-move focus planning, direct tab-select planning, auth-row selection planning, Auth-tab detail row-count planning, global-mount selection/scroll planning, environment-row selection/scroll planning, trust-row selection/scroll planning, trust-row direct selection focus planning, horizontal-scroll offset planning, mouse scroll-focus routing, and auth-kind entry/clear focus planning now live with settings screen update helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/update.rs" />. Root update/mouse applies the returned plans to concrete settings state instead of owning the generic tab cycling, focus assignment, auth row movement, source-row presence, global-mount row movement/scrolling, environment row movement/scrolling, trust row movement/scrolling, trust row focus, horizontal scroll clamping, tab-to-scroll-focus routing, and auth detail-row reset rules inline.

Settings global-mount confirm outcome planning now lives with settings screen update helpers in <RepoFile path="crates/jackin-console/src/tui/screens/settings/update.rs" />. Root settings input keeps save/remove/sensitive-abort side effects, but consumes crate-owned commit/cancel/continue plans instead of matching confirm component outcomes inline.

Workspace-list row sizing now includes the generic list-name content-width fold in <RepoFile path="crates/jackin-console/src/tui/list_geometry.rs" />; the root list layout adapter in <RepoFile path="src/console/tui/layout/list.rs" /> maps manager rows to widths and delegates the viewport floor calculation.

Workspace-list split geometry and list-name scroll clamping also live in <RepoFile path="crates/jackin-console/src/tui/list_geometry.rs" />. The root list layout adapter keeps selection/state mapping and delegates the reusable column allocation and scroll-limit mechanics.

Workspace-list root rendering now also uses the crate-owned split-column helper from <RepoFile path="crates/jackin-console/src/tui/list_geometry.rs" /> instead of duplicating Ratatui percentage layout in the root display adapter. Render and pre-render preparation now share the same list/preview column allocation boundary.

Workspace-list mouse split geometry, seam hit-region rules, mouse-wheel step policy, scrollbar drag offset planning, point-in-rect hit-testing, mouse-wheel scroll application, tab-strip hit-testing, and tabbed content-area geometry now live in <RepoFile path="crates/jackin-console/src/tui/layout.rs" />. Root mouse input keeps root stage/modal hit-testing and delegates minimum drag width, seam columns, seam proximity, Ratatui-matched pane dimensions, drag percentage math, rectangle containment, scrollbar track-position math, scroll-delta viewport math, tab-row geometry, and header/tab/footer rectangle math to the console TUI boundary.

Workspace-list body rectangle and visual-row mouse hit-testing now also live in <RepoFile path="crates/jackin-console/src/tui/layout.rs" />. Root key/mouse input maps the crate-owned geometry result back to root `ManagerListRow` values but no longer owns the generic list-content border math.

Sidebar scroll-focus liveness now lives in <RepoFile path="crates/jackin-console/src/tui/sidebar_layout.rs" />. Root list geometry maps its `MountScrollFocus` enum into the crate-owned focus vocabulary and no longer owns the generic "is the focused sidebar area still scrollable?" rule.

Root-console pre-render preparation now lives in <RepoFile path="src/console/tui/layout/prepare.rs" /> instead of `src/console/tui/view/`; it caches terminal size, footer heights, scroll clamps, and modal geometry immediately before draw while render modules stay focused on frame composition.

Capsule initial attach-frame labels now live with attach-client effect vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/effect.rs" /> instead of daemon internals. The daemon still assembles and sends the actual protocol frames, but the visible first-attach burst names are owned by the TUI boundary.

Capsule visible multiplexer mode vocabulary (`Normal`, prefix-await, dialog, drag, selection) now lives with the visible TUI model in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" /> instead of a root crate module.

Capsule mux-mode priority, chrome-hover priority, hover redraw policy, pointer-shape priority, cursor-visibility priority, and drag-resize ratio math now live under <RepoFile path="crates/jackin-capsule/src/tui/app.rs" /> and <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. Daemon mouse/dialog code supplies the current protocol/session facts and applies the resulting action, but no longer owns those visible-state decisions.

Capsule dialog copy-feedback timeout now lives with visible redraw/update vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/update.rs" />. The daemon still owns the timer deadline and clears dialog feedback when it expires, but the UI-visible duration is no longer daemon-owned.

Capsule dialog state construction for rename-tab, split-direction, close-target, confirm-action, container-info, provider-picker, and GitHub-context dialogs now lives with the dialog component in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" />. The container-info dialog carries a TUI-owned diagnostics display snapshot, the provider picker carries TUI-owned visible provider choices, and GitHub-context branch/PR loading facts map through a TUI-owned `GithubContextView` helper; the daemon still resolves runtime diagnostics, gathers session/container/PR/provider facts, maps provider labels back to env overrides, and executes terminal actions, but no longer owns those reusable visible dialog defaults or PR-status display mapping.

Capsule content-row reservation, content-rect derivation, split-spawn pane sizing, and main-view hint row reservations now live with visible pane geometry in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. The daemon still owns capacity checks, PTY/session spawn, and tab-tree mutation, but the future half-pane size calculation is a TUI layout helper.

Capsule pane-local mouse coordinate mapping now lives with visible pane geometry in <RepoFile path="crates/jackin-capsule/src/tui/layout.rs" />. The daemon still gates mouse forwarding on the focused session's protocol mode and writes encoded mouse bytes to the PTY.

Capsule mouse text-selection start-state construction now lives with selection state, motion, and copy helpers in <RepoFile path="crates/jackin-capsule/src/tui/selection.rs" />. The daemon still finds the target pane and gates selection on the session's mouse protocol mode.

Capsule visible pane-list derivation, visible agent-label formatting, shell session launch labeling, and tab auto-label derivation now live with the visible multiplexer model in <RepoFile path="crates/jackin-capsule/src/tui/app.rs" />. Capsule outer-terminal title, pane-title, pane-agent-label, and OSC window-title helpers now live under <RepoFile path="crates/jackin-capsule/src/tui/title.rs" /> as pure fact-based helpers. The daemon still owns `Session` access and supplies active tab, focus, zoom, content-rect, and session agent/shell/provider/title/cwd/label facts before rendering, spawning, or refreshing tab chrome.

Capsule branch-context default-branch suppression now lives with the branch-context bar component in <RepoFile path="crates/jackin-capsule/src/tui/components/branch_context_bar.rs" />. The daemon still supplies Git/default-branch facts and executes GitHub/container actions.

Capsule parsed-input and prefix-command to semantic-action mapping now lives with action vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/message.rs" />. The daemon still supplies live context such as dialog-captures-input and branch-bar hit-testing, then executes the returned action against PTY/session state.

Capsule input binding data, key-binding parsing, escape-time defaults, and SGR passive-motion vocabulary, mouse protocol gating/encoding now live with terminal input parsing in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />. The environment/logging resolver for those TUI-owned binding definitions lives under the non-TUI services boundary in <RepoFile path="crates/jackin-capsule/src/services/input_bindings.rs" />.

Capsule tab double-click timing now lives with terminal input parsing in <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />. The daemon still stores the previous tab click timestamp and opens the rename dialog, but the TUI-owned gesture threshold is no longer daemon-owned.

Capsule render, state, branch-context, and pull-request-context ticker intervals now live with subscription vocabulary in <RepoFile path="crates/jackin-capsule/src/tui/subscriptions.rs" />. The daemon still owns the runtime timers and background context workers, but their TUI tick cadence is no longer daemon-defined.

Capsule attach-spawn, spawn-capacity, and spawn-failure agent-label wording now lives with spawn-failure visible formatting in <RepoFile path="crates/jackin-capsule/src/tui/view.rs" />. The daemon still enforces tab/session limits, spawns PTYs, and sends the banner frame, but the operator-facing attach-spawn, capacity, and shell fallback text is no longer daemon-owned.

Op-picker account/vault/item/field display metadata, query/filter helpers, render trait and renderer, selected-index routing, selected label fallback, naming-stage input routing, filter-reset selection routing, pane-refresh planning, account/vault/item/field load-completion planning, selected account/vault/item cache-key derivation, concealed-first field-load sorting policy, account-stage navigation planning, vault-stage navigation planning, item-stage navigation planning, field-back navigation planning, field-stage commit routing, field-label commit routing, existing-field commit routing, section-stage navigation planning, section-collapse routing, naming-stage transition planning, naming text-input state construction, field-row derivation, pending-load carrier, and the session cache type now live with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs" />. Root `operator_env` keeps compatibility aliases while the root state adapter implements the crate-owned render trait, and `op` CLI execution, blocking subscriptions, and cache invalidation remain in non-TUI service/root adapter code.

Console debug key-name formatting and modal debug-name vocabulary now live in <RepoFile path="crates/jackin-console/src/tui/debug.rs" />. Console debug-bar chip geometry and run-id empty fallback now live with the event-loop shell helpers in <RepoFile path="crates/jackin-console/src/tui/run.rs" />, so rendering and click hit-testing share one right-aligned chip calculation. The root debug adapter still owns root-state location labels and maps concrete root modal variants plus focused text-input facts into crate-owned debug facts.

Editor screen frame/body layout, General/Mounts/Roles/Secrets/Auth-tab line composition, row-width primitives, Roles-tab width planning, Secrets-tab width planning, Auth-tab row width planning, and scroll clamping now flow through <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Editor tab-bar focus planning, field-selection row/scroll planning, mount-row direct selection focus planning, horizontal-scroll focus/offset planning, and mouse scroll-focus routing now lives with editor screen update helpers in <RepoFile path="crates/jackin-console/src/tui/screens/editor/update.rs" />. Root editor geometry still derives root-specific content widths/heights, General-tab values, root-formatted mount rows, role allowed/default state, role counts/names, secret value/registry lookups, and auth mode/source display facts from workspace config and mount-info cache data, then delegates viewport/body geometry, screen-specific row styling, role-row width calculation, secret-row width calculation, auth-row width calculation, and field-selection row/scroll calculation to screen helpers.

Editor tab-move reset planning and direct tab-select planning and auth-kind entry/clear focus planning now live with editor screen update helpers in <RepoFile path="crates/jackin-console/src/tui/screens/editor/update.rs" />. Root update applies the returned plans to root state and keeps only concrete root set-clearing for auth selection, secret expansion, and unmasked rows.

Editor secret key text-input construction, new-key/empty-key labels, and pending-draft duplicate-key derivation now live in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input passes pending workspace/role env maps into the screen helper instead of owning duplicate-key validation labels for editor-secret modals.

Editor secret value-edit current-value fallback now also lives in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input supplies only the optional persisted environment value before mounting the crate-owned text-input state.

Editor name, secret value, role-load, and mount-destination text-input state plus secret delete confirm state, secret scope-picker state, role-picker default/launch confirm labels, workdir picker state, and mount-destination choice state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" /> and <RepoFile path="crates/jackin-console/src/tui/components/role_picker.rs" />. Root editor input keeps choosing which modal variant opens and carrying root targets, while screen/component helpers build the reusable prompt labels and component state.

Workspace-list current-directory row, new-workspace row, instance row-label formatting, instance purge-confirm label formatting, list-width labels, live-pane default agent label, picker sidebar title formatting, global-mount title formatting, and role-global-mount title formatting now live with workspaces screen view helpers in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root workspace-list adapters and layout code supply expansion, active-instance, selection, hover, instance ID, container base, role key, optional agent, workspace-name, role-name, and role-label facts instead of owning those visible defaults.

Editor new-secret value text-input state and secret source-picker state now also live in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root editor input still routes the selected secret scope/key and op-availability flag, but no longer constructs the source picker or "Value for ..." text input directly.

Editor isolated-state save confirm state now lives in <RepoFile path="crates/jackin-console/src/tui/screens/editor/view.rs" />. Root save input still detects drift and carries the cleanup plan, while the screen helper owns the operator-facing preserved-state prompt text.

Workspace editor auth/secret and settings environment flat-row adapters now live in <RepoFile path="src/console/tui/state.rs" /> while generic row derivation, editor role allow/default toggles, editor auth-kind focus planning, editor-secret key lookup/mutation plus secret row unmask-target and label-free action/Enter planning, settings auth-kind focus planning, and settings-env key lookup/mutation plus label-free row action/Enter planning, mask toggling/delete-row mutation stay in <RepoFile path="crates/jackin-console/src/tui/screens/editor/update.rs" /> and <RepoFile path="crates/jackin-console/src/tui/screens/settings/update.rs" />. Root input, geometry, render, and footer code share those adapters instead of carrying duplicate wrappers.

Workspace-list, create-prelude, destructive-confirm, editor/settings row, editor/settings contextual row-mode planning, workspace/settings mount-row, workspace-list footer-mode planning, op-picker modal footer-mode routing, and modal footer hint composition now live with console-local footer hint components in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root footer code derives row facts such as picker presence, instance snapshot availability, saved-workspace state, scroll focus, GitHub URL availability, editor/settings selected row modes, and active modal facts, then delegates footer-mode planning, shared hint ordering, and labels to component helpers.

Editor/settings save-footer base labels and pick-list modal commit labels now also live with console-local footer hint components in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root footer adapters pass dirty/change facts, row modes, and modal variants, but no longer carry the `save workspace`, `save settings`, `select`, or `confirm` copy.

The root frame renderer asks for ready footer items and no longer resolves GitHub mount availability while composing the frame.

Workspace-secret and settings-environment footer hint fragments now also live in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root footer code decides only the focused row mode (`plain`, `op://`, add sentinel, role header, auth action, trust row, etc.), then delegates the shared hint labels and ordering to crate-owned helpers.

Root modal footer rendering now maps root modal variants to the crate-owned `ModalFooterMode` vocabulary in <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />. Root code keeps only root-state inspection such as file-browser state, auth token-generation availability, and modal payload facts; op-picker naming/section/filtered footer classification is crate-owned.

Shared editor/settings auth-source display vocabulary and display derivation from supplied values now lives with the shared row helpers in <RepoFile path="crates/jackin-console/src/tui/components/editor_rows.rs" /> so screen views can render masked literals, `op://` breadcrumbs, unset credentials, and "not required" rows from root-supplied display facts.

Shared auth source-picker state, auth panel title formatting, credential text-input state helpers, auth-form mode cycling, mode-to-next-focus planning, and full auth-form key/focus planning now live with the auth-panel component in <RepoFile path="crates/jackin-console/src/tui/components/auth_panel.rs" />. Auth-form target kind access lives with the shared settings screen model in <RepoFile path="crates/jackin-console/src/tui/screens/settings/model.rs" />. Root editor/settings auth input keeps carrying root modal parent chains, token-generation markers, credential-source side trips, save/reset persistence, selected auth-kind facts, and other root side effects, but no longer duplicates the shared `generated token`, environment-variable, `Credential` input construction, auth panel title padding, mode cycling, auth-form target-kind lookup, or auth-form focus transitions.

Generated-token source-picker label construction now also lives with the auth-panel component in <RepoFile path="crates/jackin-console/src/tui/components/auth_panel.rs" />. Editor and settings token-generation input code still decides when generation starts, but it asks the component helper for the visible source-picker state.

Generated-token 1Password item-name default construction now also lives with the auth-panel component in <RepoFile path="crates/jackin-console/src/tui/components/auth_panel.rs" />. Editor and settings token-generation input code still supplies the configured template and concrete scope label, but the component helper owns applying the scope label to the visible item-name default. Auth form literal-buffer derivation also lives with the auth-panel component, so editor/settings root adapters no longer inspect credential enum variants when mounting the shared form.

Root editor auth-row display mapping now lives in the root auth-panel adapter at <RepoFile path="src/console/tui/components/auth_panel.rs" />. The editor view passes synthesized config facts and auth rows to that adapter instead of owning auth-mode inheritance and credential-source display derivation inline.

Settings auth-row display mapping also lives in the root auth-panel adapter at <RepoFile path="src/console/tui/components/auth_panel.rs" />. The settings view asks for ready auth lines instead of owning auth mode/source display derivation inline.

Settings tab-strip, tab-body, modal, global-mount, environment, and trust display mapping now lives in the root settings component adapter at <RepoFile path="src/console/tui/components/settings.rs" />, and root `EnvValue` to secret-display mapping lives under <RepoFile path="src/console/tui/components/env_value.rs" /> so the root component module stays declaration-only. The settings view keeps frame chrome while tab-strip rendering, modal rendering, global-mount row formatting, mount-info labels, environment source lookup, unmask checks, role counts, and trust cursor facts are derived outside the view module.

Editor tab-strip plus General/Mounts/Roles/Secrets/Auth tab-body display mapping now lives in the root editor component adapter at <RepoFile path="src/console/tui/components/editor.rs" />. The editor view keeps frame chrome while delegating tab-strip rendering, tab body rendering, tab line construction, mount row formatting, role allow/default row construction, and secret value/unmask lookups before drawing the selected tab.

Root workspace-list body split, name-row, instance-detail, picker-sidebar, mount-row, environment-row, role-row, and left-list, right-detail, and sidebar-body display mapping now lives in the root workspace-list adapter at <RepoFile path="src/console/tui/components/workspace_list.rs" />. The root frame renderer imports that adapter directly while the adapter supplies workspace/config/runtime snapshot facts and delegates left-sidebar selection, right-pane detail selection, sidebar orchestration, `WorkspaceListDisplayRow`, `WorkspaceInstancePane`, provider/role/agent picker labels, mount display rows, `WorkspaceEnvRow`, and `WorkspaceRoleRow` construction before calling the crate-owned workspaces screen render helpers.

Workspace-list mount row rendering for workspace mounts and global mounts now lives with console-local mount row components in <RepoFile path="crates/jackin-console/src/tui/components/mount_rows.rs" />. Root list rendering still gathers root workspace/global mount data and mount-info labels, then passes crate-owned display rows into the component helpers.

Workspace and settings save-confirm preview line composition now lives with the console-local save-preview component in <RepoFile path="crates/jackin-console/src/tui/components/save_preview.rs" />. Root save-preview code maps root workspace/settings state, mount-info labels, and `EnvValue` breadcrumbs into display-only snapshot structs before calling the component renderer; workspace mount summaries are formatted from crate-owned preview rows instead of preformatted root strings. Save input handlers still decide when to open the preview modal and which save effect to request, but no longer own save-preview Ratatui line construction.

Create-workspace save-preview fallback naming now also lives with the console-local save-preview component in <RepoFile path="crates/jackin-console/src/tui/components/save_preview.rs" />. Root save-preview mapping supplies only the optional pending workspace name; the component owns the visible `"(unnamed)"` fallback used by create-mode previews.

Workspace delete confirm state and instance purge confirm state now live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root manager update selects the next stage and carries root identifiers, while the screen view helper owns the workspace-list confirm prompt text.

Workspace instance action/status acceptance planning, list-selection movement/direct-select clearing plans, selected-tree disclosure planning, preview-pane focus/key routing and cursor movement, workspace-list mouse scroll-focus routing, workspace-list unclamped scroll offset planning, and destructive-confirm outcome planning now live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/update.rs" />. Root list input maps concrete `ConsoleInstanceAction` and instance manifest status values into crate-owned TUI facts before selecting a container, supplies mouse-region facts before applying list-name and mount-scroll focus, supplies pane counts to the preview key planner, and keeps only root container/session lookup, preview focus/cursor storage, concrete picker fields, concrete scroll slots, tree expansion storage, plus message dispatch. Root destructive-confirm handlers map the shared confirm component outcome into crate-owned commit/return/continue plans before queuing root effects or instance actions.

Create-prelude destination text-input state, workspace-name text-input state, mount-destination choice state, and workdir-picker state now live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root prelude input still owns the wizard transition order and root mount draft construction, but delegates reusable prompt labels and component state construction to the workspaces screen helper.

Workspace-list name row/block/scrollbar rendering, instance-detail pane rendering, running-instance summary rendering, the `+ New workspace` right-pane description, the reusable provider/role/agent picker sidebar widget, and the General/Mounts/Global-mounts/Environments/Roles subpanel rendering now also live in <RepoFile path="crates/jackin-console/src/tui/screens/workspaces/view.rs" />. Root list rendering still selects the active row and supplies instance count/expanded state, picker titles, labels, selection, shortened workdir text, mount display rows, environment display rows, and role display rows, but no longer owns those workspace-screen visual panes directly.

1. **Classify existing modules.** For each root-level file in the three surface crates, decide whether it is TUI, pure non-visual domain logic, or side-effect/service/runtime logic.
2. **Capture the current behavior before moving code.** Identify the existing tests, snapshots, and manual-smoke checks that prove the affected workflow. Add focused coverage first if a workflow has no useful guard.
3. **Move TUI files under `src/tui/`.** Preserve behavior while moving files into `app.rs`, `message.rs`, `update.rs`, `effect.rs`, `subscriptions.rs`, `run.rs`, `terminal.rs`, `view.rs`, `components/`, and `screens/` as appropriate.
4. **Keep non-TUI code outside.** External commands, filesystem/network/config/credential work, workspace resolution, and capsule protocol/session authority stay in root modules or future domain/service modules outside `src/tui/`.
5. **Replace direct work with effects.** If TUI update/render/component code needs Docker, git, `op`, `gh`, filesystem, network, config, or credential work, it returns a typed effect instead.
6. **Verify behavior after each move.** Run the affected unit tests, render snapshots/lookbook checks, docs link checks when docs changed, and focused manual terminal smoke for behavior a `TestBackend` cannot prove, especially console -> launch -> capsule handoff, raw-mode restore, mouse/focus behavior, and capsule attach/resize.
7. **Promote reusable visuals deliberately.** Surface-local components stay under the owning surface. Cross-surface components move to `jackin-tui` with lookbook coverage and snapshot/test coverage.
8. **Update docs as files move.** Keep [TUI Design Decisions](/reference/tui-design-decisions/) and [Codebase Map](/reference/codebase-map/) aligned with the new module shape.

## Acceptance criteria

- Each TUI surface has a thin `src/tui.rs` module root and the TUI application layer under `src/tui/`.
- `jackin-console` uses `src/tui/screens/<screen>/` only because it has multiple meaningful screens.
- `jackin-launch` and `jackin-capsule` do not grow `screens/` unless they gain multiple distinct screens.
- Existing operator-visible behavior is preserved unless an implementation PR explicitly documents and justifies a behavior change.
- TUI update/render/component code performs no external command execution, filesystem/network/config persistence, credential handling, or capsule protocol/session authority.
- External work requested by the TUI travels through typed effects/subscriptions and returns as typed messages.
- Shared visual primitives live in `jackin-tui`; surface-specific visual composition stays in the owning surface crate.
- Module-root files stay thin; no `lib.rs`, `tui.rs`, or other module root becomes a home for rendering, dispatch, state machines, or domain logic.
- Every implementation slice identifies the pre-existing behavior it protects and verifies it after the structural move with tests, snapshots, link checks, or manual terminal smoke as appropriate.

## Documentation impact

This is contributor-facing architecture work. Keep the following docs synchronized as the implementation moves:

- [TUI Design Decisions](/reference/tui-design-decisions/) for binding TUI conventions.
- [Codebase Map](/reference/codebase-map/) for current module ownership.
- Component lookbook pages when reusable `jackin-tui` components are added or changed.

No operator- or role-author-facing page needs to change unless a later implementation changes visible behavior or commands.

## Host-side effects

None. This roadmap item describes in-repo TUI structure only. It introduces no new host writes, no new container paths, and no launch-time host mutation.
