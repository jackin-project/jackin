---
title: "Runtime Restore and Image Architecture"
---

**Status**: In progress — most of the runtime-restore / image-architecture work has landed as code in this branch, but it is **not operator-verified**, and the dirty-exit confirmation flow is **confirmed not working**. This page tracks only the remaining must-do work: the confirmed defect, the agreed in-capsule redesign, and the verification still owed. Do not treat any item here as done until the operator has manually confirmed it (see the verification list at the end of this page). Landed code is not the same as a working, signed-off feature, and a green CI run is not operator verification.

## Confirmed defect — the dirty-exit confirmation never fires

The headline behavior of this work — "if you exit with dirty or unpushed work, jackin' asks what to do" — does not happen. Reproduced three times: with a dirty workspace and the default `ask` policy, exiting the agent normally shows **no dialog**; the container just disappears and the work is silently left preserved. The operator is never asked.

Root cause (code-confirmed):

- The `jackin load` foreground path, `launch_role_runtime` in <RepoFile path="crates/jackin-runtime/src/runtime/launch.rs" />, **never calls `finalize_foreground_session`** at all. On success it returns `Ok(())`; on the attach result being an error it returns a `capsule attach failed` error. There is no dirty-exit dialog on the load path, on any exit.
- The `jackin hardline` reconnect path, `finalize_reconnected_foreground_session` in <RepoFile path="crates/jackin-runtime/src/runtime/attach.rs" />, runs `attach_outcome?` **before** it calls finalize, so a clean agent exit short-circuits past the dialog.
- On a clean last-session exit the in-container capsule client (`client::run_client`) hits the daemon's socket close as `early eof` and **exits non-zero**, so `docker exec … jackin-capsule` returns non-zero. The host then treats a normal exit as a failed attach.

Net effect: the host-side exit dialog is unreachable on the everyday "quit the agent" flow. The feature, as a thing an operator can see and use, is not implemented.

## Decision — the dirty-exit confirmation moves inside the capsule

The confirmation is **redesigned to run inside the capsule** (the in-container multiplexer), not on the host after the container exits. This supersedes the old host-side post-exit exit dialog for the dirty case.

Why in-capsule (operator's call, accepted):

- **Ask before teardown.** A host-side dialog can only prompt *after* the container has exited — at which point "want to resume?" means rebuild/restart to get back to work. In-capsule, the container is still warm; starting a new agent is instant.
- **Decision at the place of work.** The operator is already inside the container; the capsule already owns the TUI and a full set of dialogs. No new surface, no second binary.
- **Less resource churn.** No stop → ask → recreate cycle.

The split of responsibility: the **capsule** detects dirty/unpushed state and **asks** the operator before any teardown; the **host** still **executes** the filesystem keep/discard, because it owns the isolated worktrees. The capsule decides; the host carries it out.

## Architecture — share the dirty-detection logic

The detection logic must be shared, not duplicated, so the capsule and the host can never disagree on what "dirty" means.

- Today the detection lives only host-side, in `assess_cleanup` in <RepoFile path="crates/jackin-runtime/src/isolation/finalize.rs" /> — pure git work (`git -C <path> status --porcelain` for uncommitted/untracked, `rev-list <upstream>..<branch>` for unpushed), coupled only to a worktree path and a command runner.
- Both `jackin-runtime` (host) and `jackin-capsule` (in-container) already depend on `jackin-core`, which already holds the shared `CleanupStatus` and `IsolationRecord` types in <RepoFile path="crates/jackin-core/src/isolation_record.rs" />. `jackin-core` is the natural shared home — no new crate, no new binary.
- Plan: extract the path-based assessment into `jackin-core` as a function over a repo path returning `Clean | Dirty | Unpushed` (plus the `PreservedReason` it already produces). The host's `assess_cleanup` becomes a thin wrapper over it; the capsule calls the same function in-container against its workdir at last-session-close. One source of truth.

## Target workflow — in-capsule dirty-exit

The hinge is the last-session-close branch in <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> (`no_live_sessions()`, which today calls `drain_and_exit` immediately):

```text
last live session exits (agent /exit, Ctrl-D, or crash)
  │  run the shared dirty assessment on the workspace mount(s) in-container
  ├─ clean / pushed ─────────────► drain_and_exit  (unchanged — silent exit)
  └─ dirty / unpushed ───────────► push in-capsule modal; container stays alive
        ├─ Start a new agent ────► open the existing agent picker (same as "New tab")
        │                          → spawn session → back to work, container kept warm
        └─ Exit ─────────────────► drain_and_exit → host finalize executes keep/discard
```

"Start a new agent" reuses the capsule's existing `new_agent_picker` (`PickerIntent::NewTab`) in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" /> — the exact dialog "New tab" already opens. The confirmation reuses the capsule's existing dialog / modal-stack infrastructure; no new chrome.

## Remaining work — must do

1. **Share detection (`jackin-core`).** Extract the dirty/unpushed assessment out of `finalize.rs::assess_cleanup` into a path-based `jackin-core` function and shared result type; rewire the host `assess_cleanup` to call it. Unit-test the function directly against a temp git repo (clean / uncommitted / untracked / unpushed).
2. **In-capsule confirmation (`jackin-capsule`).** At `no_live_sessions()` in `daemon.rs`, run the shared detection on the workdir(s); if clean, `drain_and_exit` as today; if dirty, push the confirmation modal (`Start a new agent` / `Exit`) and keep the daemon alive with no live panes. Wire "Start a new agent" → `new_agent_picker` → `spawn_session`; wire "Exit" → `drain_and_exit`. Test that a dirty last-session exit pushes the modal (not shutdown) and that each choice routes correctly.
3. **Host executes cleanup after Exit (`jackin-runtime`).** Make a clean capsule exit actually reach `finalize_foreground_session` so the host applies `dirty_exit_policy` keep/discard: the capsule client must exit `0` on a clean last-session shutdown (so `docker exec` returns success), and the host load/hardline paths must run finalize on a clean exit instead of treating it as `capsule attach failed`. This is the fix for the confirmed defect above.

## Open design questions — for operator review (TUI)

Not yet settled; the operator will walk the TUI display and decide. Captured here so the design conversation has a home:

- Exact modal content and labels — wording of "Start a new agent" / "Exit", the title, and whether a short dirty summary (repo / file count) shows in the modal.
- Whether the in-capsule modal offers an **Inspect** (read-only diff) view of the dirty changes before deciding, or only the two choices.
- Where the **keep vs discard** *choice* is made: pulled into the in-capsule modal (so the operator makes every decision before teardown and the host only executes), or left to host `dirty_exit_policy` after Exit. The operator's "ask before teardown" preference leans toward in-capsule; the tentative default is host-executes-per-policy until decided.
- Uncommitted-vs-unpushed messaging, and how multiple dirty repos / mounts are presented.

## Operator verification — pending sign-off

The following landed as code in this branch but is **not operator-verified**. None of it may be marked implemented until the operator manually confirms it works. This list deliberately replaces the previous page's "shipped ✓" claims, which asserted completion without operator verification. CI passing does not move an item off this list — only the operator's manual confirmation does.

- Image-baked agent binaries, Claude plugins, and default-home state; image-validity by Docker labels; agent-version-changed rebuild trigger.
- Empty-dir home seeding, first-seed-only auth handoff, atomic first seed; durable Amp / OpenCode config roots.
- Container identity: `--user $HOST_UID:$HOST_GID --group-add 0` with `libnss-extrausers` passwd/group; group-0 image-home writability; an arbitrary-UID/GID launch (read + write + first seed + `git config --global` + role-hook tool installs) without permission-denied.
- Restore ladder: Tier 1 `docker start`, Tier 2 reuse of the stored image, Tier 3 pinned rebuild from `role_git_sha` (original commit, not current `HEAD`); launch never reattaches to a live instance.
- Schema `v1alpha7` migration and `dirty_exit_policy` config (global + per-workspace; `ask` / `keep` / `discard`).
- Console instance surfaces; clean-exit purge and stale-row / orphaned-lock reconciliation on launch / prune.
- Launch cockpit hard-exit behavior (`Ctrl+Q` → confirm → immediate exit; `Ctrl+C`; double `Ctrl+C` mid-build) with the terminal restored and `gc_orphaned_resources` reclaiming abandoned resources on the next launch.
- Local image-build performance changes (no recursive permission repair, single-layer plugin install, uncompressed local export, construct `shellfirm` prebuild) — and the arbitrary-UID/GID correctness this permission rework depends on.

When the operator confirms a group above, move it out of this list with an explicit note that it was operator-verified; until then it stays here regardless of test or CI status.

## Related files

- <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> — multiplexer daemon; the `no_live_sessions()` last-session-close branch is the in-capsule hinge.
- <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" /> — capsule dialog system and `new_agent_picker` reused for "Start a new agent".
- <RepoFile path="crates/jackin-runtime/src/isolation/finalize.rs" /> — host `assess_cleanup` (detection to extract) and terminal-outcome / keep-discard execution.
- <RepoFile path="crates/jackin-core/src/isolation_record.rs" /> — shared cleanup types; target home for the extracted detection.
- <RepoFile path="crates/jackin-runtime/src/runtime/launch.rs" /> — `launch_role_runtime` load path that must run finalize on clean exit.
- <RepoFile path="crates/jackin-runtime/src/runtime/attach.rs" /> — hardline reconnect path that short-circuits finalize on clean exit.
