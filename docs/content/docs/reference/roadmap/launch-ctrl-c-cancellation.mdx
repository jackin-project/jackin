---
title: "Launch Screen Exit Keybindings (Ctrl+C / Ctrl+Q)"
---

**Status**: Partially implemented — Phase 1–3 shipped; three design corrections identified in operator testing: terminal freeze on Ctrl+C, Q→Ctrl+Q migration, and hints in wrong slot

## Problem

Pressing `Ctrl+C` during the launch screen has no effect. While the rich TUI cockpit is active — covering image pulls, Docker builds, DinD network setup, workspace materialisation, and every other pre-attach pipeline step — `Ctrl+C` is silently swallowed. The operator cannot abort a long-running launch without resorting to `kill` from another terminal window or closing the terminal tab entirely. Neither is acceptable UX for a CLI tool.

The bug has two layers:

1. **Raw-mode signal suppression.** `crossterm::terminal::enable_raw_mode()` on Unix clears the `ISIG` termios flag. This disables the kernel's Ctrl+C → SIGINT translation, so `tokio::signal::ctrl_c()` never fires. The key event reaches the process as `KeyCode::Char('c') + KeyModifiers::CONTROL` instead. No signal means no signal handler helps.

2. **Input polling ignores the key event during pipeline phases.** `handle_cockpit_input()` in <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" /> drains queued terminal events every 33 ms with `event::poll(Duration::ZERO)`, but it dispatches only mouse events, scroll keys, build-log toggles, and similar cockpit interactions. `Ctrl+C` falls through unhandled during every non-dialog phase of the launch pipeline.

`read_pressed_key()` in <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> correctly detects and reacts to `Ctrl+C` — but it is called only when an interactive dialog is on screen (role picker, text prompt, confirm prompt). During the much longer stretches when the pipeline is doing I/O (git clone, Docker pull, image build, container startup), nothing reads or acts on the key event.

## Why It Matters

`Ctrl+C` is the universal exit convention for interactive CLI processes. Every major terminal application — `docker`, `git`, `cargo`, `kubectl`, and the agents themselves — honours it. Operators reach for `Ctrl+C` by reflex. Finding it inert on the launch screen erodes trust in the tool and forces disruptive workarounds (`kill -9`, closing the terminal).

The impact is highest during slow operations: large image pulls on a metered connection, Docker builds that should have used `--rebuild`, and DinD network setup stalls. These are exactly the moments when operators most want to bail.

Fixing this also unblocks a clean cancellation path for the pipeline generally: error propagation, `LoadCleanup` (container, DinD, certs volume, network teardown), and terminal-state restoration on abort are all already implemented — they just need a signal to trigger on.

## Design

### Root-cause analysis

The relevant code paths:

| Location | Role |
|---|---|
| <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" /> `handle_cockpit_input()` | Polls events every 33 ms during pipeline phases — does not check `Ctrl+C` |
| <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> `read_pressed_key()` | Correctly handles `Ctrl+C` but is only called during interactive dialogs |
| <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> `RichDriver::spawn()` | Owns the background render task; the render loop needs access to a shared cancel signal |
| <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs" /> | Hosts `load_role_with()` — the top-level pipeline future; needs to await cancellation in parallel with each pipeline step |
| <RepoFile path="crates/jackin-runtime/src/runtime/launch/progress_helpers.rs" /> | `StepCounter::while_waiting()` wraps long-running docker/git futures — the choke point for wiring cancellation into individual steps |

### Cancellation primitive: `tokio_util::CancellationToken` ✓ settled

`tokio_util::CancellationToken` is the primitive. It is:

- Clone-able and `Send + Sync` — safe to share between the render task and the pipeline task.
- Awaitable — `token.cancelled()` is a future that resolves when `token.cancel()` is called.
- Composable with `tokio::select!` without wrapping every future in an `Option`.
- Provides sync `is_cancelled()` for the render loop's per-tick check.

`tokio-util` is not a current direct dep in any crate. Add it to the workspace `[workspace.dependencies]` in the root `Cargo.toml`, then reference it from `jackin-launch/Cargo.toml` with the `sync` feature. Only `jackin-launch` needs it directly — `LaunchProgress`, `RichDriver`, and `handle_cockpit_input()` all live there; callers in `jackin-runtime` just call `progress.while_waiting(fut).await?` with no token type visible to them.

### Keybinding model — revised after operator testing

Two bindings, both cause immediate exit without confirmation:

| Key | Label | Behaviour | Phase |
|---|---|---|---|
| `Ctrl+C` | `abort` | Immediate hard cancel — no dialog, no double-press | All pipeline phases |
| `Ctrl+Q` | `quit` | Immediate clean exit — no dialog | All pipeline phases |

**Single-press Ctrl+C** is the established standard for pipeline contexts. Double-press Ctrl+C is an emerging pattern for *interactive AI sessions* (Codex CLI, OpenCode) where an accidental Ctrl+C on an idle prompt is a real risk. The launch screen is a pipeline watcher (analogous to `docker build` or `cargo build`), not an interactive session — single-press abort is correct and matches operator expectations.

**Ctrl+Q now exits immediately** — the previous design planned a "Ctrl+Q → confirmation dialog → pipeline keeps running" flow. Operator testing feedback: the dialog is unnecessary; Ctrl+Q should just quit. Both Ctrl+C and Ctrl+Q result in immediate pipeline abort and terminal restore; the semantic difference is the label ("abort" vs. "quit") only.

**Bare `q` must be migrated to `Ctrl+Q` everywhere it was used** in the launch surface — the previous design note that said "q is already used for closing dialogs and must not be repurposed" was reversed by operator feedback. Any dismiss binding that was `Esc | q` (build log, container info) must become `Esc | Ctrl+Q`, and hint text must update accordingly. `Ctrl+Q = XON` (flow control resume) is inert in raw mode, so no conflict.

**Both bindings must be available on all screens** within the launch surface — cockpit, build log overlay, container info overlay, and every interactive dialog during the pipeline.

`Ctrl+C` in the capsule TUI is forwarded to the running agent's PTY as SIGINT — this must not be intercepted there. Capsule-level `Ctrl+Q` is a related but separate concern tracked as a follow-on.

### Signal flows

**Ctrl+C — hard cancel:**

```
Operator presses Ctrl+C during pipeline
        │
        ▼
handle_cockpit_input() detects KeyCode::Char('c') + KeyModifiers::CONTROL
        │
        ▼
cancel_token.cancel()
        │
        ├──► while_waiting() tokio::select! fires cancelled branch
        │    → Err("launch cancelled by operator")
        │    → LoadCleanup::run() — removes container, DinD, certs volume, network
        │
        └──► RichDriver tick checks cancel_token.is_cancelled() → breaks
             RichRenderer::drop() — cursor, raw mode, alt screen, mouse restored
```

**Ctrl+Q — exit confirmation (non-blocking):**

```
Operator presses Ctrl+Q during pipeline
        │
        ▼
handle_cockpit_input() detects KeyCode::Char('q') + KeyModifiers::CONTROL
        │
        ▼
update_launch_view(ExitConfirmOpened)     ← pipeline KEEPS RUNNING
        │
        ▼
cockpit renders "Abort launch? [Y / N]" modal overlay (ConfirmState)
        │
        ├── Y pressed → cancel_token.cancel() → same path as Ctrl+C above
        └── N / Esc pressed → update_launch_view(ExitConfirmClosed) → modal gone
```

The confirm modal follows the same overlay pattern as `container_info_open` / `build_log_open` — a boolean flag on `LaunchView`, rendered as a `ConfirmState` widget on top of the cockpit. The pipeline does not pause; if the launch completes before the user answers, the modal is irrelevant and torn down with the cockpit.

### Implementation plan

**Phase 1 — Wire the cancellation token and exit-confirm state through the render driver (jackin-launch)**

1. Add `cancel_token: CancellationToken` field to `LaunchProgress`. Expose via `cancel_token() -> CancellationToken` (returns a clone).
2. Thread the token into `RichDriver::spawn()`; the render task checks `cancel_token.is_cancelled()` as an early-break guard alongside the existing `stop` atomic.
3. Add `exit_confirm_open: bool` to `LaunchView` with `ExitConfirmOpened` / `ExitConfirmClosed` `LaunchMessage` variants.
4. In `handle_cockpit_input()`: detect `Ctrl+C` → `cancel_token.cancel()`. Detect `Ctrl+Q` → dispatch `ExitConfirmOpened`. When `exit_confirm_open` is true, route Y/Enter → `cancel_token.cancel()`, N/Esc → `ExitConfirmClosed`. Add `cancel_token: &CancellationToken` parameter to `handle_cockpit_input`.
5. Also update `read_pressed_key()` to bail on `Ctrl+Q` in addition to `Ctrl+C`, so interactive dialog phases handle it consistently.

**Phase 2 — Propagate cancellation into the pipeline (jackin-runtime)**

1. In `StepCounter` (or wherever `while_waiting()` lives), add a `cancel_token: Option<CancellationToken>` field.
2. Rewrite `while_waiting()`:

```rust
pub async fn while_waiting<T>(
    &self,
    future: impl Future<Output = anyhow::Result<T>>,
) -> anyhow::Result<T> {
    if let Some(token) = &self.cancel_token {
        tokio::select! {
            result = future => result,
            _ = token.cancelled() => {
                anyhow::bail!("launch cancelled by operator")
            }
        }
    } else {
        future.await
    }
}
```

3. Thread the `CancellationToken` from `RichDriver` → `StepCounter` → every `while_waiting()` call site in `launch_role_runtime()` and the DinD sidecar path.
4. For pipeline stages that do not use `while_waiting()` (e.g., `resolve_restore_candidate`, trust prompts), wrap the await with `tokio::select!` directly or extract a `cancellable()` helper.

**Phase 3 — Coverage audit and cooperative cancellation guarantee**

A `tokio::select!` wrapping `load_role_with()` at the top level is NOT the right guard. Dropping the pipeline future abruptly skips `LoadCleanup::run()` (called inside the existing error-propagation chain, not via `Drop`). The correct model is **cooperative cancellation**: `while_waiting()` returning `Err` propagates via `?` through the existing error handlers, which call `cleanup.run()`.

The slow pipeline operations (image build, docker pull, DinD startup probe, workspace materialisation) all go through `while_waiting()`. Fast operations (docker API calls, git identity, container name claim) do not — but they complete in <1 s, so cancellation is felt at the next `while_waiting()` boundary. This latency is acceptable.

Phase 3 work: audit every pipeline step to confirm it either (a) goes through `while_waiting()` or (b) is fast enough that waiting for the next `while_waiting()` boundary is fine. If any non-`while_waiting()` step can block for seconds (e.g., a slow docker API call), wrap it in a direct `tokio::select!` that returns the same cancellation error — still cooperative, still hits `?`.

**Phase 4 — Cleanup and terminal restoration (BUG: terminal freezes on Ctrl+C)**

Operator testing revealed that pressing `Ctrl+C` during a running pipeline causes the screen to freeze for 10–30 seconds before the terminal is restored. The root cause:

`LaunchProgress` owns `RichDriver` which owns `Arc<Mutex<RichRenderer>>`. `RichRenderer::drop()` is the mechanism that restores the terminal (disables raw mode, leaves the alternate screen). The Arc ref count reaches zero — and `RichRenderer::drop()` fires — only when **both** the render task and the `RichDriver` struct release their Arc clones. `RichDriver` is held inside `LaunchProgress`. `LaunchProgress` is dropped **after** `LoadCleanup::run()` completes (Docker container stop, network teardown — typically 10–30 s). The render task breaks its loop on the next tick (~33 ms after cancel fires), releasing its Arc clone, but that only lowers the count from 2 → 1. The count reaches 0 only after `LaunchProgress` drops, which happens post-cleanup.

Fix required: terminal must be restored **before** cleanup runs, not after. Two approaches:

1. **Explicit early restore** — add a `restore_terminal_now()` method to `LaunchProgress` that (a) calls `stop_detached()` on the driver (aborts the render task, releasing its Arc clone immediately), (b) drops the `renderer` field explicitly (the driver's Arc clone goes to 0 → `RichRenderer::drop()` fires). Call this in the pipeline the moment the cancellation error is detected, before `LoadCleanup::run()`. Cleanup then runs with the terminal in normal mode; any output goes to stderr.
2. **Separate terminal handle** — extract terminal ownership from `LaunchProgress` into a `TerminalGuard` with explicit scope, so the terminal is restored when the guard drops at the cancel boundary, independently of when `LaunchProgress` drops.

Either approach must not break the case where `LaunchProgress` is dropped on an error path that does **not** go through cancellation (e.g., an actual Docker error), where the terminal should also be restored promptly before the error is printed.

Also verify `RichRenderer::drop()` fires correctly on non-cancel error paths (it always fires on drop; confirm no path panics before drop, which would skip `Drop`). Add unit test stub for `while_waiting()` cancellation path.

**Phase 5 — Hints in the correct location (BUG: hints on status bar, not hint bar)**

Operator testing revealed that `Ctrl+C abort · Ctrl+Q exit?` was added to the **status bar** (white bar) via a `hint: Option<&str>` parameter on `render_status_footer`. This is wrong. Per the [TUI chrome design](/reference/tui/chrome): the status bar is the white instance/branch bar; hints belong in the **hint bar** (the separate row above the chrome separator, not on the white bar).

Fix required:

1. Revert `hint: Option<&str>` from `render_status_footer` / `StatusFooter::new()` in `jackin-tui` and all callers — remove this slot entirely.
2. Add `Ctrl+C cancel · Ctrl+Q exit?` to the **launch hint bar** (the row that currently shows e.g. `Q close · ↑↓ scroll` when the build log or container info is open, and the activity-level hint row during pipeline phases). The hint bar is the canonical place for all keybinding hints per `/reference/tui/navigation`.
3. Update the chrome docs page (`docs/content/docs/reference/tui/chrome.mdx`) to remove the incorrect status-bar hint slot description added in the previous implementation pass, and document the correct hint-bar placement instead.

### Visual feedback on cancellation — revised

Immediate teardown. When Ctrl+C or Ctrl+Q fires, the render task immediately calls `restore_terminal()` in the same tokio task step as the cancel (before the pipeline task can resume), then `while_waiting()` returns `Err`, the error propagates via `?`, `LoadCleanup::run()` removes Docker resources with the terminal already in normal mode. No "Cancelled" cockpit state is rendered — the cockpit simply disappears, matching `docker build`, `cargo build`, `git clone` behaviour. If cleanup emits any output, it goes to stderr after the terminal is restored.

The previous design relied on `RichRenderer::drop()` for terminal restoration, which fires only after `LaunchProgress` drops (post-cleanup). This caused the 10–30 s freeze. The fix: explicitly call `restore_terminal()` in the render task when cancel is detected, before breaking the render loop, making `Drop` a no-op via `entered_alt_screen = false`.

### What does NOT change

- `tokio::signal::ctrl_c()` is not the right fix (blocked by `ISIG` suppression in raw mode) but can be added as defence-in-depth for the narrow path where the launcher runs without raw mode active (e.g., non-interactive CI invocations).
- The cancellation error message matches the existing `"launch cancelled by operator"` string used in `read_pressed_key()` and the cancel branches of `update_text_prompt` / `update_select_prompt`.
- `exit_confirm_open` state and the `ExitConfirmOpened` / `ExitConfirmClosed` `LaunchMessage` variants should be removed — the exit confirm dialog is no longer part of the design.

### Dependency check

Before starting implementation, confirm `tokio_util` appears in `Cargo.toml` as a direct dependency in the relevant crates (`jackin-launch`, `jackin-runtime`). If absent, add it with the `sync` feature only (`tokio-util = { workspace = true, features = ["sync"] }`).

## Tasks

**Phase 1–2 (shipped):**
- [x] Add `tokio-util` to `jackin-launch/Cargo.toml`
- [x] Add `cancel_token: CancellationToken` to `LaunchProgress`; expose via `cancel_token() -> CancellationToken`
- [x] Thread token into `RichDriver::spawn()` — add `cancel_token: &CancellationToken` param to `handle_cockpit_input()`
- [x] `handle_cockpit_input()`: detect `Ctrl+C` → `cancel_token.cancel()`
- [x] `read_pressed_key()`: add `Ctrl+Q` to the bail condition alongside `Ctrl+C`
- [x] RichDriver tick loop: check `cancel_token.is_cancelled()` as early-break guard
- [x] `LaunchProgress::while_waiting()`: rewrite with `tokio::select!` on `cancel_token.cancelled()`

**Phase 3 — pipeline audit (shipped):**
- [x] Wrap `docker build` await in `build_agent_image()` with `while_waiting()` — `build_log::end()` always called via local variable ordering
- [x] Wrap binary download `tokio::try_join!` in `prepare_runtime_binaries()` with `while_waiting()`
- [x] `resolve_agent_repo_with()` git fetch — assessed as acceptable latency (fast enough to wait for next `while_waiting()` boundary)
- [x] `RoleState::prepare` spawn_blocking — assessed as low-latency, acceptable

**Phase 3b — Ctrl+Q redesign corrections (operator feedback):**
- [ ] Remove `exit_confirm_open: bool` from `LaunchView`; remove `ExitConfirmOpened` / `ExitConfirmClosed` from `LaunchMessage`; remove confirm overlay from cockpit view
- [ ] `handle_cockpit_input()`: change `Ctrl+Q` from "dispatch ExitConfirmOpened" to `cancel_token.cancel()` (same as Ctrl+C)
- [ ] Change `Esc | q` dismiss in container-info dialog to `Esc | Ctrl+Q`
- [ ] Change `Esc | q` dismiss in build-log overlay to `Esc | Ctrl+Q`
- [ ] Verify both `Ctrl+C` and `Ctrl+Q` are handled inside every interactive dialog during the pipeline (role picker, text prompt, confirm prompt) — `read_pressed_key()` must bail on both

**Phase 4 — Immediate terminal restore on cancel (BUG FIX):**
- [ ] Add `restore_terminal()` method to `RichRenderer` — same body as `Drop`, sets `entered_alt_screen = false` so `Drop` is a no-op
- [ ] In render task loop: after `handle_cockpit_input()`, if `cancel_token.is_cancelled()`, call `rr.restore_terminal()` immediately and break — this fires BEFORE the pipeline task can resume (no tokio yield between cancel and restore)
- [ ] Remove `exit_confirm_open` render path from cockpit view (covered by Phase 3b above)

**Phase 5 — Hints in correct location (BUG FIX):**
- [ ] Revert `hint: Option<&str>` from `render_status_footer` / `StatusFooter::new()` in `jackin-tui`; update all callers to remove the hint arg
- [ ] Add `Ctrl+C abort · Ctrl+Q quit` to the launch hint bar (not the status bar) — the hint bar is the row rendered above the chrome separator, per `/reference/tui/chrome`
- [ ] Update hint text when build-log or container-info overlays are open: include `Ctrl+Q close` instead of `Q close`
- [ ] Update `docs/content/docs/reference/tui/chrome.mdx`: remove the incorrect status-bar hint slot description; document correct hint-bar placement

**Validation:**
- [ ] Unit tests for `while_waiting()` in `crates/jackin-launch/src/progress/tests.rs` (separate file per crate layout rules)
- [ ] Manual smoke test: `Ctrl+C` mid-pull → TUI disappears immediately, cleanup to stderr, clean terminal restore
- [ ] Manual smoke test: `Ctrl+Q` → same immediate exit behavior as Ctrl+C
- [ ] Manual smoke test: `Ctrl+C` / `Ctrl+Q` during interactive dialog — both bail correctly
- [ ] Manual smoke test: build-log open, press `Ctrl+Q` → closes overlay; press `Ctrl+Q` again → exits pipeline

## Related Files

- <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" /> — `handle_cockpit_input()`, the 33 ms event-polling loop
- <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> — `RichDriver`, `RichRenderer`, `read_pressed_key()`
- <RepoFile path="crates/jackin-launch/src/progress.rs" /> — `LaunchProgress`, `while_waiting()`
- <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs" /> — `load_role_with()`, top-level pipeline future
- <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch.rs" /> — `launch_role_runtime()`, `LoadCleanup`
- <RepoFile path="crates/jackin-tui/src/components/status_footer.rs" /> — `render_status_footer()`, `StatusFooter` widget
- <RepoFile path="crates/jackin-launch/src/tui/components/footer.rs" /> — `render_footer()`, launch-specific footer wrapper
