---
title: "Agent Runtime Status Authority"
---

**Status**: **Implemented on `feature/agent-runtime-status` (Phases 0–12, June 25, 2026).** The evidence-arbitration authority is wired and live, and every claim here is backed by a green CI gate (this page once asserted "shipped + smoke-tested" over code that did not compile; that is the failure this work corrected, and the guardrails below stop it recurring). What landed: the `agent_status` tree is declared in <RepoFile path="crates/jackin-capsule/src/lib.rs" /> and its tests run in CI (was zero); the protocol surface exists (`jackin_protocol::agent_status` types, `AgentState::Unknown`, `ReportRuntimeEvent`/`Ack`, `StatusCapture`, `PaneSnapshot.agent_status_report`); the byte-driven flap engine in <RepoFile path="crates/jackin-capsule/src/session.rs" /> is deleted (PTY/keystrokes update recency only, sessions start `Unknown`); the daemon tick is the sole state path — assemble `EvidenceSnapshot` → `arbitrate` → `apply_watchdog` (Linux-gated) → `debounce` → publish; the semantic channel runs end to end (`report-event` CLI → control socket → gating → `AuthorityEvidence`, Claude/Codex identity-only per Decision 0a); `/proc` physics, OSC evidence (title/notify/OSC 9;4/OSC 133), and the screen rule-pack engine (with `line_regex`/`forbids_regex`/prompt-caret regions, fixture harness, semver-bounded `validated_versions`) all feed the snapshot; seen/ack, flap/stuck telemetry, `status explain`/`status capture`, per-launch reporter installation, and derived-image asset copying have landed. CI guardrails: `cargo shear --deny-warnings` makes an unlinked source tree a hard error, and `scripts/ci/check-agent-status-truthful.sh` fails if this page claims "Implemented" while the tree is unwired. **Tracked refinements** (functional without them, recorded in the checklist / Future Decisions): runtime pack-vs-CLI co-versioning enforcement (agents are bind-mounted, not baked, so there is no build-time pinned version — `RulePack::accepts_cli_version` is the comparison, runtime enforcement pending); the dirty-session faster-recheck cadence (the 700 ms idle cap governs at the 1 Hz floor); BEL capture (needs a jackin-term passthrough variant); recursive nested `all`/`any`/`not` gates beyond regex alternation; `min_engine_version`. The **Implementation Checklist** is the live per-phase record; the **Implementation Blueprint** and **Architecture Specification** remain the design reference.

## Problem

jackin' needs to know what each in-container agent is actually doing: actively working, waiting on the operator, finished but unseen, idle and reviewed, crashed, or suspiciously stuck. A quiet agent can be thinking, waiting for network, running a long tool, sitting at an input box, or dead — and a noisy pane can be a blocked dialog repainting itself. Treating activity as `working` and silence as `blocked` creates false notifications, noisy dashboards, bad queue dispatch, and weak operator trust. This authority must exist before agent attention prompts, autonomous task queues, Desktop Agent Hub, and cost/resource correlation can be reliable.

## Target

One consistent status vocabulary across `jackin console`, `jackin hardline`, the daemon, Desktop Agent Hub, and scripts:

| Public status | Meaning |
|---|---|
| `working` | Actively generating, running tools, processing — not ready for operator input. |
| `blocked` | Waiting on an operator decision: approval prompt, question, auth prompt, interactive input. |
| `done` | Returned to idle after work; the operator has not yet reviewed that pane. |
| `idle` | Idle and already seen, or a shell pane with no active agent. |
| `unknown` | No reliable evidence. Unknown is safer than pretending. |
| `stuck` | Diagnostic overlay: progress was expected, no reliable transition arrived in time. Never replaces `blocked`. |

Raw detector states are `unknown | working | blocked | idle` plus per-pane `seen`; `done` is derived as raw `idle + !seen` and is never reported by any detector or reporter.

**Goals**: lowest achievable false-positive rate; every wrong state debuggable from one diagnostics dump; detection keeps working when an agent vendor ships no API, breaks their API, or changes their TUI; cheap to extend to new agents. **Non-goals**: probabilistic state estimation (no field evidence it beats precedence+freshness+debounce), Windows ConPTY, LLM-provider traffic interception, autonomous recovery actions after stuck detection (this authority reports and routes attention only), per-tool/per-command status granularity, and built-in support for agents beyond the five runtimes (custom roles use the reporter contract and the `unknown` fallback).

## Why No Single Signal Works — Skeptical Review of Every Approach

Every approach in the field fails somewhere. The design below is a deliberate combination chosen so that each approach's failure mode is covered by another's strength. This table is the rationale; keep it current when adding signal sources.

| Approach | Field users | What it gets right | How it fails (be skeptical) | Role in jackin' |
|---|---|---|---|---|
| Runtime hooks / plugin events | workmux, tmux-agent-status, agent-deck, zellaude, Herdr (full-lifecycle agents only) | True semantic transitions from inside the runtime: prompt submitted, permission requested, turn ended. The only source that knows *why* the agent is waiting. | No event fires on operator interrupt (Esc/Ctrl-C) in Claude Code or Codex → stuck `working`. Agent crash mid-turn → no Stop event. Hook config can drift or be wiped. Vendors rename/retire events (contract churn). Async events can arrive late or out of order. A sync hook can stall or alter the agent. Some surfaces miss permission *results*. | **Primary authority where the surface exists**, graded by completeness, TTL-bound, identity-checked, and always cross-checked by the physical watchdog. Reporters forward events, never states; mapping/gating lives in daemon Rust. |
| Visible-screen pattern matching | Herdr (manifest engine), CCManager, Agent Session Manager, claude-squad, unitmux | Always available — jackin' owns the grid, so the live UI is readable for any agent, including ones that ship no API (the explicit fallback for runtimes like Kimi where waiting for upstream is not acceptable). Catches states hooks cannot see (interrupt returns to prompt box, crash screens). | TUI wording churns weekly; strings localize; single mis-sampled frames mid-redraw flip state; scrollback text contaminates window-wide matches; overlays (transcript viewers, pickers) hide the live prompt; per-version drift is permanent maintenance. | **Universal fallback and validator**, made survivable by: structural regions (not joined text), rules as data co-versioned with the image-pinned agent CLI, conservative `unknown` fallback, executed false-positive fixtures, and debounce. |
| Agent-authored OSC emissions (title, OSC 9 notify, OSC 9;4 progress, BEL) | Herdr (title/progress regions), cmux (terminal-side interception), VibeTunnel (BEL + attribution) | Authored by the agent itself, immune to screen-redraw races, already flowing through the parser jackin' runs — zero new I/O. Codex's title carries a spinner/status by default; Claude emits OSC 9-family notifications and OSC 9;4 progress. | Not all agents emit anything. Semantics are ambiguous: a notification means "done OR needs attention" without saying which; Claude's progress bar keeps animating during approval prompts, so progress-active is not working-proof. Any child process the agent shells out to can emit OSC bytes (spoofable). Vendors change emission formats silently. | **Evidence edges, never states**: notification edge feeds attention arbitration; progress-clear is a done-ish hint; title spinner is a busy/idle channel for agents that document it (Codex). Attributed to the foreground agent; wiped on identity change. |
| Process physics from `/proc` (CPU-jiffy deltas, child-process tree, foreground pgid) | WezTerm Agent Deck (fg info), VibeTunnel (process-tree attribution), Herdr (identity only) | Unfakeable by redraws: tool execution implies children under the agent PID; generation implies recurring CPU increments; `tcgetpgrp` answers "agent or shell owns the pane". Free inside the container. | CPU is not semantics: spinner timers burn CPU while idle-ish, network waits burn none while genuinely working. Children are absent during pure generation. Nothing here distinguishes blocked from idle. A wchan-style "blocked in tty read = waiting" check is invalid for every built-in runtime (event-loop TUIs sit in `epoll` in both states) — also invalidating cursor-probe-timeout as a blocked detector. | **Corroborator and watchdog fuel only.** Demotes lying authorities ("working" with zero physics for too long → `unknown` + stuck). Never promotes to `blocked`. |
| PTY output activity / pane-hash recency | claude-squad (hash), VibeTunnel (5 s recency), tmux `monitor-*`, Herdr's deleted June 2026 experiment | Trivial to implement; output genuinely correlates with work *sometimes*. | The decisive negative result: Herdr shipped PTY-activity-first detection (output authored `working`, with keystroke-taint causality, leases, and confirmation debounces stacked on top) and **deleted all of it within ~48 hours** — keystroke echo, resizes, redraw nudges, and agent-spawned background processes are indistinguishable from thinking at the byte level, and every compensating mechanism added latency to correct transitions. jackin's current branch wired exactly this shape, with fewer defenses. | **Never authors state.** Output/input recency exists only as timestamps feeding the watchdog and re-evaluation scheduling. |
| Cooperative state files / agent-written sentinels | multicode autonomous-state skill, claude_code_agent_farm heartbeats, Codeman done sentinels | Lets custom/role agents participate with zero integration. | The agent can crash, forget, or race stale state; a file proves the writer wrote it, not that the runtime is healthy. | Accepted as reporter evidence with freshness + process validation; never proof. |
| LLM-as-classifier | coder/mux sidebar, Claude agent-view row summaries | Handles novel UI states no rule anticipated. | Cost, latency, nondeterminism — wrong tool for a badge that must be instant and reproducible. | Future display-layer descriptions only; never the badge. |
| Native supervisor state | `claude agents --json` (working/blocked/done/failed/stopped, research preview) | Exact in-process truth — own the process, get the truth. | Only for supervisor-hosted background sessions; not available for arbitrary PTY panes, which is what jackin' runs. | Tracked as a future higher-grade authority if supervisor-hosted sessions become launchable inside role containers. |

**Design thesis** that follows from the table: accuracy comes from *redundant disagreeing witnesses plus explicit trust rules*, not from any one channel. Use the semantic channel when a runtime offers one (most accurate when fresh); always run the screen/OSC engine (because jackin' is the multiplexer and can always see the live UI — no waiting for vendors); always run the physics watchdog (because both of the above can lie); and when witnesses cannot be reconciled, say `unknown` rather than guessing.

## Research Base And License Discipline

Re-validated June 10, 2026: Herdr head `1fc5e84` (source review), vendor hook/plugin docs for all five built-in runtimes, and an orchestrator field survey (claude-squad, VibeTunnel, workmux, cmux, tmux-agent-status, zellaude, claude-code-zellij-status, Crystal, claude_code_agent_farm, coder/mux, CCManager, agent-deck, Codeman, ccmux, multicode, pi-mono). Herdr's June 8–10 sequence — hand-coded matchers → PTY-activity-first (deleted in ~48 h) → manifest rules over structural regions + OSC evidence with one asymmetric idle debounce — is the strongest external validation of this design. multicode (Apache-2.0) contributes descendant-session roll-up and runtime-API-first ordering.

Copyright protects expression, not ideas or methods (17 U.S.C. §102(b); EU Software Directive 2009/24/EC art. 1(2)): every concept here is free to reimplement as original Apache-2.0 Rust. Per-project code constraints, verified June 10, 2026 against repository license files: Herdr (dual AGPL-3.0-or-later/commercial), claude-squad (AGPL-3.0), coder/mux (AGPL-3.0), cmux (dual proprietary) — **concepts only; never copy, never translate line-by-line, never write jackin' code with their source open**; ccmux (FSL-1.1-MIT) — concepts; tmux-agent-status and claude-code-zellij-status (no license; code all-rights-reserved, ideas free); VibeTunnel, workmux, zellaude, Crystal, pi-mono, CCManager, agent-deck, Codeman, claude_code_agent_farm (MIT) and multicode (Apache-2.0) — concepts suffice. Vendor docs are published for integration. Reference these projects in PRs as concept sources only.

### Where Herdr falls short — the advantages jackin' must actually cash in

1. **Co-versioned rule packs.** Herdr chases whatever agent version a host has installed, hence its 30-minute over-the-air manifest updates (a remote trust surface it must cap and validate). jackin' pins agent CLI versions in role images, so a rule pack and the TUI it targets are validated together at image build and cannot drift. No OTA machinery needed: repo-shipped packs + operator override.
2. **Identity is known, not discovered.** Herdr scans `/proc` on a few-hundred-millisecond tick with wrapper unwrapping and miss hysteresis to learn what owns a pane. jackin' spawned the process: child PID and agent slug are facts; probing covers only the exceptions (manual launches inside a pane, agent exits to shell).
3. **No physical cross-check.** Herdr disables screen detection while a full-lifecycle hook authority is live; a hung plugin reports `working` forever. jackin' keeps the watchdog always on and lets it demote any authority.
4. **Optimistic fallback.** Herdr's unmatched-screen fallback is `idle`, so unknown UI states become false `done` downstream. jackin' falls back to `unknown`.
5. **No physics signals at all** (no CPU deltas, no child-tree). 6. **No descendant/subagent roll-up.** 7. **Hand-curated fixtures** (jackin' adds capture-from-live and PTY-transcript replay). 8. **No accuracy self-measurement** (jackin' adds flap-rate telemetry).

### Herdr re-review (June 25, 2026 — head `58b9fecc`, two weeks past the original `1fc5e84` review)

Re-read because Herdr ships frequently and the original review was the design's strongest external validation. Findings, all from source inspection (AGPL — concepts only, no code read into jackin'):

- **The PTY-activity negative result still holds.** Herdr's current code still does **not** author `working` from PTY bytes: output activity only increments a scan-scheduling counter that lets an already-idle pane skip re-scanning; `working` is published exclusively from the manifest/OSC engine. (There is stale comment drift in their source claiming "PTY activity is the normal working authority," but no code path implements it.) Conclusion unchanged: the *Why No Single Signal Works* table's treatment of PTY recency as "never authors state" remains correct, and Herdr never re-adopted byte-authored working.

- **New, substantive divergence — Herdr demoted Claude and Codex hooks to identity-only.** Herdr's installers now **strip every lifecycle→state hook** for Claude and Codex (and copilot/devin/droid/qodercli/cursor) and register only `SessionStart`, which reports a resumable **session id with no state**; the screen detector owns `idle`/`working`/`blocked` for these runtimes. The load-bearing reason, stated in their hook script, is that completion-class events are unreliable for these CLIs — `SubagentStop`/recap/away-summary can fire *after* the main turn's `Stop` and would revive an idle pane, and the event stream cannot faithfully observe terminal state. This page currently treats Claude/Codex hooks as **Partial-grade state authority** (the gating table maps their events to states). jackin' is partly ahead of the failure Herdr is reacting to — the gating table already holds `working`/`blocked` through `turn-complete` when `subagents_active > 0` or a permission is pending (`StopSuppressed`), which neutralizes the naive SubagentStop-revives-idle case. **But the open question Herdr's newer work raises is real and must be answered explicitly here:** should Claude/Codex hook events author state at all, or only supply session identity + freshness while the always-on rule pack owns state? jackin's pinned CLI versions and always-running screen engine make the identity-only stance cheap to adopt, and it removes a whole class of "vendor event arrived late/out of order → wrong state" failures that the watchdog can only catch slowly. **Decision needed (tracked as Future Decisions):** keep Partial-state Claude/Codex hooks with the subagent/permission gates as the sole defence, or downgrade them to identity+freshness and let the pack + physics watchdog decide, matching Herdr's field-validated conclusion. Either way, OpenCode (Complete) and Amp/Kimi handling is unaffected.

- **Borrow — count-token "waiting on its own background tasks = working" rules.** The June 24 commit (`58b9fecc`) generalized Antigravity's detection so a footer like `· N task` (nonzero count) classifies the pane as `working` even when the older `/tasks` slash-command hint is absent; the same shape recurs in Cursor (`N background tasks`) and Kimi (`[N agents running]`). jackin' has no rule of this class and — more importantly — no encoded principle that *an agent visibly waiting on background tasks it spawned must read as `working`, not `idle` or `blocked`*. This is screen-derived and covers the no-hook / hook-stripped case; it complements (does not replace) the subagent roll-up. The rule-pack schema already expresses it via a `regex` matcher on a `bottom_nonempty:N` region; add count-token working rules for the runtimes that surface such counts.

- **Borrow — bounded idle-hold cap and fast-recheck cadence.** Herdr's working→plain-idle hold is `3` confirmations like this design's `IDLE_CONFIRMATIONS`, but it is additionally **capped at ~700 ms** with a ~100 ms recheck while pending, so a stuck confirmation loop can never hold a stale `working` indefinitely and a real idle transition is sub-second. This design has the confirmation count but no upper time bound; add an `IDLE_HOLD_CAP` to `policy.rs` so the inferred-idle debounce is bounded, and use a faster recheck while a confirmation is pending instead of waiting for the 1 Hz floor.

- **Borrow — explicit identity-miss hysteresis and a short freshness re-publish.** Herdr clears an agent identity only after **6 consecutive process-probe misses** (sticky against transient `/proc` gaps) and re-publishes a *persisting* visible blocker about every **800 ms** so downstream freshness/notification consumers stay live. This design clears identity implicitly and has only the 5-minute operator `RENOTIFY_INTERVAL`, which is a different mechanism. Add an explicit miss-confirmation count before identity teardown, and consider a short blocker freshness re-publish distinct from operator re-notification (it keeps `WaitSessionStatus` and the aggregate `WorkspaceStatusChanged` consumers honest without re-alerting the operator).

- **Borrow — engine-version floor on packs.** Herdr manifests carry a `min_engine_version`, and the loader refuses a manifest that needs a newer engine than it implements. This design's packs carry only `schema_version = 1`. When regions/matchers grow, an engine-version floor lets an older capsule safely reject an operator-override pack it cannot evaluate, instead of silently mis-matching — cheap insurance for the override surface under `~/.jackin/`.

- **Borrow — rule-engine expressiveness (the single largest design gap).** A June 25 deep read of Herdr's manifest engine versus this design's flat `requires_all`/`requires_any`/`forbids` + one whole-region regex found three primitives this design lacks that are load-bearing across Herdr's packs, each enabling correct matches the flat model *cannot* express:
  - **Existential per-line regex (`line_regex`)** — "some line matches this anchored pattern." It is Herdr's most-used disambiguator (codex/claude/cursor/droid/grok/kimi/antigravity): a spinner-at-line-start `^\s*[braille]`, a numbered-choice line, a non-zero count token `\b[1-9][0-9]*\s+tasks?\b`, a bare-caret idle line `^\s*❯\s*$`. This design's single whole-region regex anchors only to the joined blob's start/end, so "a spinner currently on its own line" is inexpressible. This subsumes the earlier count-token borrow.
  - **Nested `all`/`any`/`not` gates** — a shared positive prefix that needs its own sub-OR (e.g. Claude's bash-permission prompt: `contains "do you want to proceed?"` AND `any[bash markers]` AND `any[numbered choices]`). Flattened into one `requires_any`, the sub-conditions leak across branches and produce false matches.
  - **`not` inside an OSC-title rule → idle-by-subtraction** — Codex derives idle from the title bar alone as "title has content AND is neither the spinner-prefixed working title nor `Action Required`." `forbids` (substring) cannot forbid an *anchored* spinner pattern, so this design cannot replicate the cheapest possible detection (state from the title before touching the screen).

- **Borrow — prompt-caret region family and structural geometry.** Herdr has 14 region kinds to this design's 8; the five missing are all prompt-caret-relative (`after_last_prompt_marker`, `before_current_prompt_marker`, `whole_recent_without_current_prompt_marker`, `current_prompt_block_marker`, `after_current_prompt_block_marker`) and exist to discriminate a **live** prompt from the same text echoed in scrollback — e.g. a region that collapses to empty when a fresh caret is present, so a rule keyed there fires only when the agent is *not* idle at a prompt. Equally important is *how* Herdr finds chrome: a single `is_horizontal_rule` primitive (≥3 leading `─` or a pure dash run, U+2500 only, trim-aware) is reused to (a) implement `after_last_rule` and (b) localize the prompt box by counting **two** rules up from the bottom (bottom border, then top border) — one detector, not two divergent ones. This design names `prompt_box_body`/`above_prompt_box`/`after_last_rule` as regions but does not specify the rule tolerance or the box-localization heuristic, and has no caret/block-marker (`›`, `•■✗✓`) vocabulary. Claude and Codex draw their boxes with plain `─` rules and no corner glyphs, so a corner-glyph box finder would miss them — the rule-counting approach is the correct primitive.

- **Borrow — debounce/state-machine mechanics the policy table omits.** Herdr's working→idle hold is a *seed call plus 3 increments* sampled at a **100 ms cadence while pending** (vs 300 ms idle), hard-capped at **700 ms** wall-clock so a stuck confirmation loop cannot hold stale `working` forever; a persisting visible blocker is **re-published every 800 ms** so downstream freshness/notification consumers do not decay a long-lived blocker; an idle pane **skips re-scanning** until a content-sequence counter (bumped only on non-empty PTY reads) changes; and a screen blocker overrides a non-full-lifecycle hook **only when the screen observation is fresher than the hook report**. This design's table specifies the count but not the cap, the dual cadence, the stable-blocker re-publish, the idle-scan dedupe, or the fresher-than-hook comparison. (Note a transcription check: this design must list the stable-blocker re-publish at 800 ms and the idle cap at 700 ms — distinct values.)

- **Borrow — hook-authority machinery for the Complete-grade reporters (OpenCode).** Even if Claude/Codex go identity-only, OpenCode remains a Complete-grade authority, and Herdr's authority bookkeeping is more defensive than this design's: per-**source** monotonic sequence rejection (`seq <= last_seq` dropped); a suppression-reason taxonomy where a process-exit suppresses **all** session refs while other reasons suppress only the matching ref; stale-session memory with fresh-session re-anchoring; a **session-replacement allowlist keyed on the lifecycle event** that caused the new session (e.g. replace only on `clear`/`resume`/`compact`/`new`, never on a stray duplicate); and a strict **exit-clears-authority-before-recompute** ordering so an exit can never leave a hook claiming a dead process. These belong in the gating/authority layer regardless of the Claude/Codex decision.

- **Confirmed jackin' advantages that remain real (once built).** Herdr still has **no CPU/process-physics signals at all** — its only defence against a hung authority or the Esc/Ctrl-C interrupt hole is screen re-scan plus the 6-miss identity hysteresis. This design's CPU-jiffy + child-tree **watchdog is a genuine edge** for demoting a lying `working` quickly; it is also currently unwired dead code, so the edge is unrealized until the Blueprint lands. Likewise the `unknown` (not `idle`) unmatched-screen fallback, co-versioned image-pinned packs (no OTA trust surface), and subagent-gated `turn-complete` remain correct differentiators versus Herdr's idle fallback and 30-minute OTA manifests.

- **Not a gap — breadth.** Herdr now ships manifests for 18 runtimes (adds gemini, cursor, devin, droid, copilot, grok, hermes, kilo, kiro, cline, qodercli, antigravity, pi, omp). jackin's five-runtime scope is a deliberate non-goal, not a miss. The manifest patterns there — spinner alphabets (braille `U+2800–28FF`, moon phases, clock glyphs, block runs), box-drawing approval chrome, count tokens — are a ready translation library for when the custom-role rule-pack contract lands.

**Net answer to "did we implement it sensibly / miss anything":** the *design* is sound and in several respects ahead of Herdr (physics watchdog, `unknown` fallback, co-versioned packs, subagent-gated stop). The miss is twofold: (1) none of it is built — see Current Implementation State; and (2) one design assumption (Claude/Codex hooks as state authority) is contradicted by Herdr's newer field experience and needs an explicit, recorded decision rather than an implicit "Partial grade".

## Current Implementation State

> **Historical pre-implementation audit (June 25, 2026, morning).** The conditions below — the orphaned tree, the live flap engine, the absent protocol surface — describe the branch *before* Phases 0–12 landed. They have all been corrected; the **Implementation Checklist** is the current per-phase record and the **Status** line at the top is the present state. This audit is retained because its root-cause analysis is what the guardrails (CI unlinked-source hard error + the status truthfulness gate) and the execution plan were built to prevent recurring — delete it only if it stops being instructive.

Audited **June 25, 2026** by direct source inspection of branch `feature/agent-runtime-status`. The previous revision of this section (dated June 11) claimed the entire authority had landed and passed a live smoke test. **That claim was false.** Source inspection showed the implementation present as a self-contained module tree that was never compiled, never tested, and never called, while the shipping behaviour was the pre-design flap engine this page set out to delete. The state at that point:

**Written but not wired (dead on disk)** — the `agent_status/` tree under <RepoFile path="crates/jackin-capsule/src/agent_status.rs" /> contains, coded close to this spec: the evidence model (<RepoFile path="crates/jackin-capsule/src/agent_status/evidence.rs" />), the seven-step arbitration (<RepoFile path="crates/jackin-capsule/src/agent_status/arbitrate.rs" />), debounce + watchdog constants (<RepoFile path="crates/jackin-capsule/src/agent_status/policy.rs" />), the event gating table (<RepoFile path="crates/jackin-capsule/src/agent_status/gating.rs" />), the rule-pack engine (<RepoFile path="crates/jackin-capsule/src/agent_status/process.rs" /> and the rules module), and the hook installer. Verification facts:

- **It is not in the crate module graph.** <RepoFile path="crates/jackin-capsule/src/lib.rs" /> declares 24 modules; `agent_status` is not among them, and `git log -S'mod agent_status'` on that file returns no commit. Nothing under the tree is referenced anywhere outside the tree itself.
- **It cannot compile as written.** Every file `use`s `jackin_protocol::agent_status::{AgentRawState, AgentStatusConfidence, AgentStatusReport, AgentStatusSource}`, but <RepoFile path="crates/jackin-protocol/src/lib.rs" /> exposes only `control`, `provider_adapter`, `snapshot` — there is no `agent_status` module in the protocol crate, and those types are defined **only inside the orphaned capsule tree**. The capsule builds green solely because rustc never sees these files.
- **Zero tests run.** Because the module is undeclared, `cargo nextest` discovers none of the arbitration/policy/gating/process/rules tests. Every assertion the prior section credited ("tested as…", "fixtures pass…", the soak test, the transcript replay) is inside `#[cfg(test)]` blocks in uncompiled files. The fixture harness, `status explain`/`capture`, and the smoke test described on June 11 are not reachable from any binary.

**Protocol surface absent** — <RepoFile path="crates/jackin-protocol/src/control.rs" /> defines `AgentState` as `{Working, Blocked, Done, Idle}` (no `Unknown`), and `ClientMsg` as `{Status, Snapshot, Agents, Unknown-sink}`. None of `ReportAgentState`, `HeartbeatAgentAuthority`, `ClearAgentAuthority`, `ReportRuntimeEvent`, `ReportChildAgentState`, `EventsSubscribe`, `WaitSessionStatus`, `SessionReadVisible`, `SessionStatusExplain` exist; neither does `AgentStateChanged` or `WorkspaceStatusChanged`. The console already carries an `agent_status_report` field on its snapshot type, but it is only ever `None`.

**The live state machine is the flap engine this page forbids** — in <RepoFile path="crates/jackin-capsule/src/session.rs" />: `mark_operator_input` sets `state = Working` (operator keystroke authors state); `state_after_pty_output` maps `Working | Idle → Working` on every PTY byte (output authors `Working`); `state_after_refresh` publishes `Blocked` after `BLOCKED_AFTER = 3 s` of output silence (silence authored as `Blocked`). <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> broadcasts this raw `session.state` directly. This violates three *Do Not Do* rules on this page simultaneously: PTY output authors state, operator keystrokes author state, and silence is treated as `blocked`. It is the precise behaviour Appendix A catalogued as the bug to remove.

**Reporter assets present, inert** — the hook/plugin scripts under `docker/runtime/agent-status/` (Claude/Codex/OpenCode shell pipes, Amp/OpenCode `plugin.js`, five rule packs, fixtures including the three Claude false-positive classes and `codex/blocked_auth.txt`) exist and the Claude hook is a correct dumb pipe (no python, no `nc`, no Stop-blocking). But they invoke `jackin-capsule report-event`, a subcommand that does not exist in <RepoFile path="crates/jackin-capsule/src/main.rs" />, and **no Dockerfile or build step copies `runtime/agent-status/` into any image**, so the June 11 "derived image copied the assets" smoke result is not reproducible from the tree. `validated_versions` appears in the packs but nothing parses or enforces it at image build (no `docker/` or `.github/` reference).

**What operators actually see today** — the live two-state flap signal is operator-visible, not just internal. The host console paints a per-pane `[working]`/`[blocked]` label (<RepoFile path="crates/jackin-console/src/tui/input/list.rs" /> via the snapshot `state`), and the in-container capsule paints a tab glyph from the same field; both flip to `[blocked]` after any 3 s output gap while the agent is actively thinking and back to `[working]` on the next byte. `idle`, `done`, and the focus-`seen` acknowledgment are unreachable in the compiled build (`Done` is never authored — exited sessions are removed instead), so the `Done` tab-glyph branch is dead. The intended richer carrier — a `PaneSnapshot.agent_status_report: Option<AgentStatusReport>` — was half-merged: a console test constructed that field against a `PaneSnapshot` and `jackin_protocol::agent_status::AgentStatusReport` type that were never added, leaving `cargo check -p jackin-console --tests` failing with `E0560` (the **only** build break in the workspace). It was the same deferred-feature seam as the orphaned tree, and is corrected by matching the test to the real four-field `PaneSnapshot`; re-adding the field belongs with the Slice 2/8 wiring that produces it. The structural lesson reinforces the root cause below: the project already ships a guard for exactly this (`cargo shear` is PR-blocking and flags **unlinked Rust source files**, per <RepoFile path="crates/AGENTS.md" />), so the orphaned tree should have tripped CI — that it did not is itself a signal to chase.

### Root cause (architecture, not a typo)

Per this project's bug-fixing discipline: a correct architecture would not let "fully implemented, smoke-tested" be asserted over code that cannot compile. Two structural conditions allowed it, each generating a whole class of latent failures:

1. **A new module can exist on disk without being in the build, and the build stays green.** Rust only compiles modules reachable from a `mod` declaration; an entire subsystem can be authored, "self-tested", and forgotten with no compiler signal. The class: *any* feature added as a fresh module tree without the one-line `mod` wire-up ships as inert code that reviewers and docs mistake for live. The project *does* run a guard for exactly this — `cargo shear` is a CI step and reports `src/agent_status.rs` as an "unlinked file" — but it emits that as a **non-blocking warning**, so it never failed the build. (Verified June 25: `cargo shear` flags the unlinked tree as ⚠ only.) The structural fix is to make unlinked-source a hard error — a build/CI gate that fails when any `src/**/*.rs` is not reachable from a crate's module graph — not a one-time re-wiring. Note a second-order tell the existing gates *did* catch but that was shipped anyway: because the tree is dead, the dependencies it pulled in read as unused, so `cargo shear` exited non-zero on this branch — `rustls` and `toml_edit` in `jackin-capsule`, `semver` and `toml` in `jackin-image` — i.e. the branch was not actually merge-green, contradicting the "shipped" claim a second way. Because wiring is being handed to a fresh implementation pass, those four premature dependencies were removed (June 25) to restore a green gate — standard add-the-dep-in-the-slice-that-uses-it hygiene, not a paper-over; the execution plan re-adds each one in the phase that introduces its consumer (`rustls`/`toml_edit` with the hook installer and reporter transport, `semver`/`toml` with image-build pack version validation). The rule that still holds: never delete a dependency that *compiled* code needs — only a premature dep for code not yet wired.
2. **Doc status is asserted from intent, not from an executed acceptance gate.** The *Acceptance Criteria* here are concrete and runnable, but nothing binds the page's *Status* to them actually passing in CI. The class: any roadmap item can drift to "shipped" while its acceptance tests are unrun. The structural fix is to gate "Implemented" status on the acceptance suite executing in CI — for this item, on the `agent_status` tests being discovered and green, which is impossible until condition 1 is fixed.

The remediation is therefore not "re-run the smoke test"; it is: add `mod agent_status` (and the protocol `agent_status` module the tree imports), make it compile, let CI discover the tests, delete the `session.rs` flap authoring per Slice 1, and wire the daemon tick per Slice 2 — i.e. the Implementation Blueprint below, which was written but never landed. Until then this page is a design, not a description.

### Defects inside the unwired tree (must be fixed as part of wiring, not after)

Wiring the tree is necessary but not sufficient — a June 25 read of <RepoFile path="crates/jackin-capsule/src/agent_status.rs" /> and its children found correctness defects that would ship the moment it compiles. Ordered by severity (wrong logic first):

- **Second compile blocker:** `policy.rs` uses `Duration::from_mins(5)` for `RENOTIFY_INTERVAL`; `from_mins` is unstable and does not build on the pinned toolchain. Use `from_secs(300)`. (Independent of the missing-protocol-types blocker.)
- **OSC progress-clear promoted from hint to authoritative idle:** `arbitrate` step 5 maps a single `progress_cleared_at` edge to `Strong` idle, which `debounce` then publishes immediately — contradicting this page's own mapping discipline ("progress-clear = done-ish hint", never authoritative). A done-ish hint must enter at a confidence that still requires idle confirmation, not bypass it.
- **The watchdog, debounce, and startup-grace never run.** `apply_watchdog`, `debounce`, and `should_publish_candidate` have no caller outside their own tests; `arbitrate` never consults the watchdog. The design's headline differentiator (demote a lying `working`, catch the Esc/Ctrl-C interrupt hole) would do nothing until the absent daemon tick is written to call it. `STARTUP_GRACE`, `RENOTIFY_INTERVAL`, and `EVAL_COALESCE` are declared but unconsumed — startup-grace muting, persisting-blocker re-notification, and 250 ms early-eval coalescing are unimplemented.
- **Gating holes that strand state:** Codex has no `permission-resolved` vendor-event mapping, so `pending_permission` never clears and every later Codex `Stop` is suppressed to `Blocked` forever. Claude's `AskUserQuestion` `PreToolUse` matcher is not implemented (it maps unconditionally to `tool-start`), losing the question-dialog signal. Amp maps no permission/elicitation event, so Amp can never reach `Blocked` via authority. The per-runtime gating tests pass only because they inject synthetic canonical event strings instead of real vendor event names — the adapters are not actually exercised end to end.
- **Evidence modelled but never read:** `AuthorityGrade::Complete` is never constructed and `grade` is never consulted by arbitration (grade-based weighting from blueprint step 4 is absent); `AuthorityEvidence.seq`, the entire OSC title/`notify_edge`/BEL channel, `root_is_agent`, `child_alive`, and `last_input` are written but never read. So the Codex title-spinner busy/idle channel, the notification-edge attention channel, and the Amp BEL edge — all promised in the adapter table — do nothing even once wired.
- **Rule engine weaker than its own schema claim:** matchers are flat `requires_all`/`requires_any`/`forbids` with one whole-region regex — no nested `all`/`any`/`not`, no per-line regex (so "spinner at line start" is inexpressible beyond the region's first line), the regex is recompiled on every call, and a regex that fails to build is silently treated as non-matching with no diagnostic. `validated_versions` is checked only for non-emptiness (no semver parse, `"*"` accepted), so the co-versioning gate the *Acceptance Criteria* require does not exist. See the Herdr re-review for the concrete expressiveness gaps.
- **Two parallel authority models and a layout violation:** `HookAuthority`/`AuthorityOrigin` (in `agent_status.rs`) and `AuthorityEvidence` (in the evidence module) coexist, only one feeds arbitration; and every file carries an inline `#[cfg(test)] mod tests` block, which violates the crates hard rule (tests live in a sibling `tests.rs`) and would fail review at wire-up time.

None of these are "edge cases to defer": each is a path that produces a wrong public state or a silently-dead channel. They are part of the cost of the feature being correct, and belong in the same work that compiles the tree.

## Pre-Implementation Gaps (resolve before Slice 1)

A final June 25 pass found issues that are not in the tree's code but in the *plan and its surroundings* — they must be settled before the Blueprint is executable, or Slice 1 starts on false footing:

1. **The branch was not actually merge-green (now restored).** `cargo shear` exited non-zero (unused-dep errors from the dead tree's `rustls`/`toml_edit`/`semver`/`toml`, plus the non-blocking unlinked-source warning). Those four premature deps were removed to make the gate green again; they are re-added per-phase below. The structural guard fix still stands: promote `cargo shear`'s unlinked-source from a warning to a hard error so an orphaned tree cannot recur (Phase 0).

2. **The Blueprint is stale against the code it edits.** Slice 1 instructs deleting a `self.apply_raw_state(crate::agent_status::AgentRawState::WorkingVisible)` call in <RepoFile path="crates/jackin-capsule/src/session.rs" />, but no such call exists — the live authoring is `state_after_pty_output` (PTY → `Working`), `state_after_refresh` (3 s silence → `Blocked`), and `mark_operator_input` (keystroke → `Working`). An implementer following Slice 1 verbatim would not find the named site. Slice 1 must be rewritten against the current three functions before it can be followed.

3. **There is no slice for the protocol surface, and the types live in the wrong crate.** The tree imports `jackin_protocol::agent_status::{AgentRawState, AgentStatusReport, AgentStatusConfidence, AgentStatusSource}`, but those types are defined inside the capsule and the protocol crate has no `agent_status` module. The console already expects `PaneSnapshot.agent_status_report` (the phantom field). Yet no Blueprint slice says "create `jackin_protocol::agent_status`, move the shared report/confidence/source types there, add `AgentState::Unknown`, and add the `PaneSnapshot` field + host-snapshot decode." This shared-protocol slice is a prerequisite of Slices 2, 8, and 9 and is currently unwritten. Relatedly, the protocol `AgentState` carries `Done` as a peer of `Idle` but no `Unknown`, while the design treats `done` as *derived* (raw `idle` + `!seen`); the raw-vs-public split must be reconciled — the wire carries public state (gaining `Unknown`), the capsule derives `done` from `seen`.

4. **The Claude/Codex hook-authority question gates the Blueprint; it is mis-filed as a "Future Decision."** Slices 4 (semantic channel) and 9 (adapters) assume Claude/Codex hook events author state. If the Herdr-validated identity-only stance is adopted, the gating table rows for those two runtimes, the installer behaviour, and the adapter tests all change shape. This decision is a **prerequisite to Slice 4**, not a follow-up — it must be made first, or Slice 4 is built twice.

5. **The operator-visible surface is under-specified.** The Blueprint is capsule-centric; the only consumer mention is "console session rows" in Slice 9. But the live flap-prone badge ships through the host console and the capsule tab glyph, and replacing it needs explicit work: the `PaneSnapshot` field (gap 3), host decode, and the console/capsule render swap from the two-state label to the full `working/blocked/done/idle/unknown` vocabulary with the `seen`/`done` distinction. Add a consumer slice; do not leave it implied.

6. **Blast radius: ~20 roadmap items link this as a load-bearing contract** — among them [Agent attention prompts](/reference/roadmap/agent-attention-prompts/), [Autonomous task queue](/reference/roadmap/autonomous-task-queue/), [Desktop Agent Hub](/reference/roadmap/jackin-desktop-agent-hub/), [Token & cost telemetry](/reference/roadmap/token-cost-telemetry/), [Console agent session control](/reference/roadmap/console-agent-session-control/), and [Terminal observation automation](/reference/roadmap/terminal-observation-automation/). Each presumes a reliable `working/blocked/done/idle` signal and an `events.subscribe`/`wait-session-status` surface that do not exist. Their own designs rest on this vapor; the corrected status here must be reflected wherever they assume it is live, so downstream work is not planned on a false premise.

7. **The watchdog will mis-fire off Linux.** `process.rs` stubs return zero CPU jiffies and zero descendants on non-Linux; the watchdog demotes `working` to `unknown` when CPU and child count are both zero. The capsule runs in Linux containers, but tests and any non-Linux execution path would see every real `working` demoted. The watchdog must be explicitly Linux-gated (or treat "physics unavailable" as "no evidence", never as "quiet"), and a test must lock that in — otherwise the design's headline differentiator becomes a false-positive generator on the developer's own machine.

## Architecture Specification

### The one structural rule

Signals never mutate state. Signals update **evidence**; a pure function maps the evidence snapshot to `(raw_state, confidence, evidence_summary)`; a debounce policy turns arbitration results into public transitions; events fire only on debounced transitions. PTY output updates recency timestamps and a damage counter — nothing else. Operator input updates an input timestamp and the seen path — nothing else. `SessionStatus::advance` and its raw-signal enum are deleted.

### Data model (target shapes, original to jackin')

```rust
/// Everything arbitration may look at for one session, assembled per evaluation.
pub struct EvidenceSnapshot {
    pub authority: Option<AuthorityEvidence>, // semantic channel
    pub osc: OscEvidence,                     // agent-authored terminal protocol
    pub screen: ScreenEvidence,               // rule-pack result over the grid
    pub process: ProcessEvidence,             // /proc physics
    pub activity: ActivityEvidence,           // recency timestamps only
    pub subagents_active: u32,
}

pub struct AuthorityEvidence {
    pub source_id: String,
    pub grade: AuthorityGrade,       // Complete (OpenCode-class) | Partial (Claude/Codex-class)
    pub mapped_state: RawAgentState, // produced by daemon-side event mapping, never by the reporter
    pub pending_permission: bool,    // permission event seen without its result event
    pub last_event: Instant,
    pub seq: u64,                    // daemon-assigned arrival order per source
}

pub struct OscEvidence {
    pub title: Option<String>, pub title_changed_at: Option<Instant>,
    pub notify_edge_at: Option<Instant>,         // OSC 9-family notification arrival
    pub progress_active: bool, pub progress_cleared_at: Option<Instant>, // OSC 9;4
    pub bel_at: Option<Instant>, pub bel_count: u64,
}

pub struct ScreenEvidence {
    pub state: Option<RawAgentState>, pub rule_id: Option<String>,
    pub strong: bool,                 // rule carried a strong-evidence flag (live chrome)
    pub freeze: bool,                 // overlay rule matched: keep last state
    pub observed_at: Instant,
}

pub struct ProcessEvidence {
    pub child_alive: bool,
    pub root_is_agent: bool,
    pub foreground_is_agent: bool,
    pub foreground_returned_to_shell: bool,
    pub child_process_count: u32,     // processes under the agent PID
    pub cpu_jiffies_delta: u64,       // over the last sampling window
}

pub struct ActivityEvidence { pub last_output: Option<Instant>, pub last_input: Option<Instant> }
```

`SessionStatus` keeps `effective`, `seen`, `revision`, and gains `raw`, `confidence`, `authority_source`, and the latest `EvidenceSnapshot` summary for events/explain.

### Evaluation scheduling

The existing 1 Hz capsule tick is the floor: every session re-evaluates each tick. PTY damage marks a session dirty and may trigger one early coalesced re-evaluation (minimum 250 ms between evaluations per session), so blocked latency is sub-second without per-byte work. Grid reads dump only the detection window (bottom rows), not the whole grid, and are skipped when the damage counter is unchanged and the state is idle/unknown.

### Semantic channel (reporter protocol)

- Spawn env (daemon, per session): `JACKIN_SESSION_ID`, `JACKIN_STATUS_SOURCE` (unique per session+runtime, e.g. `hook-<runtime>-<session>`), `JACKIN_AGENT_RUNTIME`, `JACKIN_STATUS_SOCKET=/jackin/run/jackin.sock`.
- Reporter is a `jackin-capsule` subcommand (`jackin-capsule report-event --event <name> --payload-stdin`), single fork, no shell/python/netcat chain. Hook and plugin scripts are dumb pipes that pass the vendor event name and payload through.
- **Events, not states.** The daemon owns one mapping+gating table (tested Rust):

| Incoming event (any runtime) | Gate | Mapped effect |
|---|---|---|
| prompt-submitted, tool-start, tool-end, compact-start | — | authority state `working`; entering work cycle resets `seen` |
| permission-requested, question-asked, elicitation | — | authority state `blocked`, `pending_permission = true` |
| permission-resolved (allow/deny), question-answered | — | `pending_permission = false`; state `working` |
| turn-complete / stop | only when `pending_permission == false` AND `subagents_active == 0` | authority state `idle` |
| subagent-start / subagent-stop | — | increment / saturating-decrement `subagents_active`; no state change |
| session-end / agent-exit | — | clear this source's authority; evidence note |
| heartbeat | — | refresh `last_event` only |

- Sequences are assigned by the daemon in arrival order per source; reporter timestamps are used only for staleness diagnostics. Authority expires when `last_event` exceeds `AUTHORITY_TTL`; it is dropped immediately on process exit or when the source's runtime label contradicts the known/detected foreground agent. Reporters never emit decisions that alter agent control flow (no stop-blocking).

### OSC evidence channel

jackin-term already parses every escape sequence; the daemon retains per session: last title + change time, OSC 9-family notification arrivals, OSC 9;4 progress state and set/clear edges, BEL count. Mapping discipline: notification arrival = attention edge (the agent is done **or** needs input — arbitration decides which using the other witnesses); progress-clear = done-ish hint; progress-active = nothing (Claude animates it during approval prompts); title spinner presence/absence = busy/idle channel only for runtimes that document it (Codex defaults to a spinner title item). OSC evidence is accepted only while the foreground process is the detected agent and is wiped on identity change or agent restart.

### Screen rules as data

One engine replaces the five per-agent Rust detector modules. Rule packs are TOML files in-repo, baked into role images, with an operator override directory under `~/.jackin/` (override > image pack). Schema (jackin-original; concept credit Herdr's manifest engine):

```toml
schema_version = 1
agent = "claude"
validated_versions = ">=2.1.0, <2.3"   # image build fails if the pinned CLI is outside every pack's range

[[rule]]
id = "permission-dialog"
state = "blocked"
priority = 900
region = "bottom_nonempty:12"
strength = "strong"
requires_all = ["enter to select", "esc to cancel"]
requires_any = ["to navigate", "↑/↓"]
forbids = []

[[rule]]
id = "transcript-overlay"
state = "freeze"            # keep last state while an overlay hides the live prompt
priority = 1000
region = "bottom:24"
requires_all = ["showing detailed transcript", "ctrl+o to toggle"]
```

- Regions are structural: `bottom:N`, `bottom_nonempty:N`, `prompt_box_body`, `above_prompt_box`, `after_last_rule` (below the last long horizontal rule), `last_nonempty_line`, and virtual regions `osc_title`, `osc_progress`. Blocked rules must anchor to live dialog chrome (affordance co-presence within the dialog region), so an approval question remembered in transcript text is structurally unmatchable.
- Matching: case-insensitive substrings (`requires_all`/`requires_any`/`forbids`) plus optional anchored regexes; highest priority wins; **no match → `unknown`** (deliberately stricter than Herdr's idle fallback).
- Claude-specific corrections the pack must encode (current detector bugs): transcript bullets (`⏺`, `·`, `•`, `●`, `▸`) are not spinner glyphs; a working rule requires the spinner at line start in the live status region together with the interrupt hint; long horizontal rules are not prompt-box evidence; the prompt box is the rounded-corner box containing the input affordance in the bottom region.
- Engine safety caps (rules per pack, gate count, matcher length) guard against pathological packs even though packs are repo-owned.

### Arbitration (pure function, evaluated per session per evaluation)

1. Process exited or foreground returned to shell → publish exit transition (raw `idle`, evidence `process_exited` or `foreground_returned_to_shell`) **before** clearing identity and authority.
2. Screen `freeze` rule matched → keep previous raw state, refresh evidence.
3. Blocked wins: fresh authority `blocked` (pending permission unresolved), or strong screen blocker for the detected agent. A live screen blocker overrides a non-blocked authority only when the screen observation is at least as fresh as the authority's last event.
4. Fresh, grade- and TTL-valid, identity-consistent authority state.
5. Strong screen/OSC evidence: visible working chrome or documented title spinner → `working`; positively matched idle UI → `idle`.
6. Physics-only corroboration (children executing or CPU active, nothing contradicting) → `working` at `Weak` confidence.
7. `unknown`.

The **watchdog** runs orthogonally to precedence: any witness claiming `working` while output, CPU delta, and child-process count are all quiet beyond `WATCHDOG_QUIET` demotes the session to `unknown` and raises a rate-limited `stuck` diagnostic with the full evidence trail. This is what catches the interrupt hole (no hook fires on Esc/Ctrl-C in Claude Code or Codex) and hung plugins.

### Debounce policy (between arbitration and public state)

| Transition | Policy |
|---|---|
| → `blocked` (gated authority event or strong visible blocker) | immediate |
| → `working` | one consistent evaluation |
| `working` → `idle` via positively matched idle UI or gated turn-complete | immediate |
| `working` → `idle` inferred only from absence of working chrome | `IDLE_CONFIRMATIONS` consecutive evaluations AND no OSC progress active AND CPU-quiet |
| any → exit | immediate, ordered before identity clearing |
| agent just spawned | `STARTUP_GRACE`: identity known, screen rules muted, OSC state wiped |
| notifications | fire on debounced public transitions only; a persisting blocker re-notifies at most once per `RENOTIFY_INTERVAL` |

Initial constants (one module, all referenced from tests): `AUTHORITY_TTL = 30 s` (heartbeat or any event refreshes), `WATCHDOG_QUIET = 10 s`, `IDLE_CONFIRMATIONS = 3` evaluations, `STARTUP_GRACE = 3 s`, `CPU_SAMPLE_WINDOW = 2 s`, `RENOTIFY_INTERVAL = 5 min`, early-evaluation coalescing `250 ms`. Future work may adapt `WATCHDOG_QUIET` per runtime as a bounded multiple of observed median cycle time.

### Done, seen, roll-up

Unchanged contract, with wiring named: pane focus and attach call the acknowledgment path (`done` → `idle`, records seen revision); entering a work cycle resets `seen`; roll-up is `blocked > done > working > idle > unknown`.

### Telemetry, explain, capture, replay

- `clog!`: lifecycle transitions, authority grants/expiries/demotions, watchdog degradations, notification emissions. `cdebug!`: per-evaluation evidence snapshots, rule decisions with region previews, sequence/freshness rejections, gating outcomes.
- `jackin-capsule status explain <session>`: dump the current `EvidenceSnapshot`, every rule evaluated with its region text preview, authority state/grade/freshness, and why the winning state won. Compact and debug logs carry transition, telemetry, and stuck breadcrumbs; `status explain` / `status capture` are the full evidence bundle.
- `jackin-capsule status capture <session>`: snapshot the full evidence bundle into a new fixture directory — live mis-detections become regression fixtures in one command.
- Recorded PTY transcripts replay through the parser+engine in CI (trace-driven regression, stronger than curated screens).
- Telemetry counters: state transitions per session-minute (flap rate) and watchdog demotions. A flap-rate regression is the canary that an agent update broke a pack.
- `AgentStateChanged` evidence fields are populated from the actual snapshot (rule id or authority source, real detector booleans, real foreground pgid, confidence, freshness) — never derived from the output state.

## Per-Runtime Adapters (verified June 10, 2026)

The strategy the table implements: **use the semantic surface wherever the vendor provides one** (most accurate while fresh); **rely on the screen/OSC engine wherever they do not** — jackin' is the multiplexer and can always see the live UI, so no runtime blocks on upstream API maturity (Kimi is the worked example); **physics watchdog everywhere**.

> **Decision 0a (June 25, 2026) — Claude and Codex hooks are identity-only, not state authorities.** Their hooks report session identity (which agent owns the pane, the resumable session id) and refresh freshness/liveness; they do **not** author `working`/`blocked`/`idle`. State for both comes from the always-on rule pack + OSC evidence + physics watchdog. Rationale: completion-class hook events for these two CLIs are unreliable in order and timing — a `SubagentStop`, recap, or away-summary can fire *after* the turn's `Stop` and would revive an idle pane (Herdr-validated, June 2026). Because jackin' pins each CLI version in the role image, the co-versioned screen pack is a dependable state owner, so identity-only **eliminates** the late/out-of-order failure class instead of defending it with gates. OpenCode (Complete) and Amp/Kimi are unaffected. This parameterizes Phases 5 and 11. (If a future Claude/Codex release ships a documented, ordered, permission-result-complete event surface, revisit and promote to graded state authority.)

| Runtime | Tier-1 source (container-local install, drift-repaired each launch) | Tier-2/3 cross-checks | Adapter specifics |
|---|---|---|---|
| Claude Code | **Identity + freshness only.** Hook in the container Claude home: `SessionStart`→report session identity (agent slug + resumable session id); lifecycle events (`UserPromptSubmit`, `PreToolUse`, `PostToolUse`, `Stop`, `SubagentStart`/`SubagentStop`, `SessionEnd`) refresh authority freshness/liveness and exit/identity edges — they do **not** map to `working`/`blocked`/`idle`. Grade: Identity-only. | **State owner:** rule pack (primary) + OSC 9-family notification edge + OSC 9;4 clear edge + physics watchdog. | State is never authored from a hook event (per Decision 0a): no `Stop`→idle, no `SubagentStop`→idle, so the post-Stop recap/subagent-revives-idle hazard cannot occur. Reporter is a dumb pipe, never emits stop-blocking decisions. Installer merges additively (repairs its own entries without replacing role-authored hooks). Idle-timer notification is never treated as blocked. |
| Codex | **Identity + freshness only.** Hook in the container `~/.codex/hooks.json`: `SessionStart`→report session identity; other lifecycle events refresh freshness and exit/identity edges only, never state. Grade: Identity-only. | **State owner:** rule pack (primary) + terminal-title spinner item (default on — busy/idle channel) + OSC 9 approval edge (TUI notification setting, OSC method enabled in container config) + physics watchdog. | Per Decision 0a, the hook surface — though real, including `PermissionRequest` — is used for identity/freshness only; the title-spinner and approval OSC plus the rule pack own state, removing dependence on Codex's permission-result event ordering. |
| OpenCode | Container-local plugin (config home, never the project tree) forwarding `session.status`, `session.idle`, `permission.asked`/`permission.replied`, `tool.execute.*`, `session.error`. Grade: Complete. | Rule pack, watchdog. | Replaces the unworkable ACP-bridge approach (separate-instance truth source). Handle known upstream quirks defensively: status can stick after async prompts; idle-event plugin handlers are fire-and-forget. |
| Amp | Container-local plugin on `agent.start`/`tool.call`/`tool.result`/`agent.end`; built-in notifications fire on completion and blocked. Grade: Partial. | BEL edge, rule pack, watchdog. | Promote from screen-only. |
| Kimi | None in the TUI today (wire mode exists but would replace the operator-visible TUI). | **Rule pack is primary** (thinking indicators, approval prompts, idle prompt box), process evidence, watchdog. | The worked example of vendor-independence: do not wait for upstream hooks; the screen engine carries Kimi at full quality, and the co-versioned pack pins it to the image's CLI version. Delete the placeholder hook script that handles events that never fire. |
| Custom role agents | Role-authored reporter over the capsule socket (events-not-states contract). | Process identity, weak activity. | `unknown` remains the default; a future role manifest contract may declare named visible states and ship rule packs. |

Runtime setup constraints (unchanged hard rules): all assets under `/jackin/runtime/agent-status/`, state under `/jackin/state/agent-status/`, socket under `/jackin/run/`; hook/plugin config merged only into container-local agent homes; nothing written to the host or to bind-mounted project trees; drift repaired every launch.

## Execution Plan (authoritative build order)

This is the order to implement, written for a fresh pass that wires the feature from the current (corrected) state. Each phase has a gate; do not start a phase until its predecessors' gates pass. The **Implementation Blueprint** below remains the per-slice detail (its "Slice N" numbers are cross-referenced here), but where the two differ this plan's ordering and the corrections in *Defects inside the unwired tree* and *Pre-Implementation Gaps* win. Run the merge-readiness checks from <RepoFile path="CONTRIBUTING.md" /> at every phase boundary.

**Phase 0 — Decisions & guardrails (no feature code).** Do first.
- *0a — Claude/Codex hook authority decision.* Choose: adopt **identity-only** (recommended — Herdr-validated, and pinned CLI versions make the always-on screen pack the reliable state owner) or keep **Partial-state** with the subagent/permission gates as the sole defence. This parameterizes Phases 5 and 11; record the decision inline in the adapter table before coding them.
- *0b — Harden the guard.* Promote `cargo shear`'s unlinked-source from warning to hard error in CI so an undeclared module tree fails the build (the structural condition that hid this feature).
- *0c — Bind status to CI.* The page may not read "Implemented" until the `agent_status` acceptance tests are discovered and green in CI.
- Gate: decision recorded; a synthetic unlinked file fails CI; no behaviour change.

**Phase 1 — Compile the foundation. [P0 — blocks everything]**
- *1a — Protocol surface (new slice).* Create the `jackin_protocol::agent_status` module; move `AgentRawState`/`AgentStatusReport`/`AgentStatusConfidence`/`AgentStatusSource` there from the capsule; add `AgentState::Unknown`; add `PaneSnapshot.agent_status_report: Option<AgentStatusReport>` plus host-snapshot decode. Reconcile raw-vs-public: the wire carries public `AgentState` (now including `Unknown`); the capsule derives `done` from `seen`.
- *1b — Wire the tree.* Add `mod agent_status` to <RepoFile path="crates/jackin-capsule/src/lib.rs" />; fix the two compile blockers (`Duration::from_mins` → `from_secs(300)`; the protocol imports now resolve); relocate every inline `#[cfg(test)] mod tests` into a sibling `tests.rs` (crate hard rule); collapse the two parallel authority models (`HookAuthority` and `AuthorityEvidence`) into one.
- Gate: `cargo check --workspace --all-targets`, `cargo nextest` (now discovering the agent_status tests), `cargo shear`, and `cargo clippy -D warnings` all green.

**Phase 2 — Kill the flap engine. [P0] (Slice 1, rewritten against real code)**
- In <RepoFile path="crates/jackin-capsule/src/session.rs" />, make `state_after_pty_output`, `state_after_refresh`, and `mark_operator_input` stop authoring state — keep only `last_output_at`/`last_input_at` timestamps; delete the 3 s-silence → `Blocked` inference. The daemon stops broadcasting raw `s.state`.
- Gate: the 30 s permission-dialog redraw soak asserts one transition; `pty_output_does_not_change_state` and `operator_input_does_not_change_state` pass.

**Phase 3 — Evidence + arbitration as the only path. [P0] (Slice 2) + in-tree defect fixes**
- Wire the daemon tick: assemble an `EvidenceSnapshot` per session → `arbitrate` → publish; populate `AgentStateChanged` from the real snapshot.
- Fix now, not later: OSC progress-clear enters as a *hint* at a confidence that still requires idle confirmation (never the `Strong`-idle bypass); arbitration actually consults `grade` (step 4) and `seq`.
- Gate: one test per precedence step; same snapshot → same result; revision increments only on public transitions.

**Phase 4 — Debounce + watchdog. [P0] (Slice 3) + Linux-gate + Herdr mechanics**
- Implement `debounce` and call it from the tick; add `IDLE_HOLD_CAP` (~700 ms) with a faster pending recheck; wire `STARTUP_GRACE` muting and `RENOTIFY_INTERVAL`; call `apply_watchdog` from the tick. **Linux-gate the watchdog**: when `/proc` physics is unavailable (non-Linux stub returns 0), treat it as "no evidence", never "quiet".
- Gate: every debounce-table row; watchdog demotes quiet `working`; watchdog does **not** fire off-Linux or during a network wait with recent output.

**Phase 5 — Semantic channel + gating fixes. [P1] (Slice 4, parameterized by 0a)**
- Add the `report-event` capsule subcommand, the spawn env vars, and the `ReportRuntimeEvent` protocol method. Implement gating and fix the holes: Codex `permission-resolved`, Claude `AskUserQuestion` `PreToolUse` → question-asked, Amp permission/elicitation → blocked. If 0a = identity-only, Claude/Codex emit identity + freshness only and the pack owns their state. Re-add `toml_edit` here (installer config edits).
- Gate: every gate row exercised with **real vendor event names** (not synthetic canonical strings); env vars present in `/proc/<child>/environ`.

**Phase 6 — Process physics. [P1] (Slice 5)** Wire `process.rs` into the tick (Linux); exit-clears-authority-before-recompute ordering; foreground-returned-to-shell handoff.

**Phase 7 — OSC evidence + consume the channels. [P1] (Slice 6)** Retain title/notify/progress/BEL and make arbitration actually **read** them (Codex title-spinner busy/idle, notification attention edge, Amp BEL) — Phase 3 leaves them written-only by design of the current tree.

**Phase 8 — Rule-pack engine + expressiveness. [P1] (Slice 7) + Herdr borrows**
- TOML loader; add `line_regex`, nested `all`/`any`/`not` gates, and `not` inside OSC-title rules; add the prompt-caret region family and one reused `is_horizontal_rule` primitive that also localizes the prompt box (count two rules from the bottom; no corner-glyph assumption); compile each regex once; enforce `validated_versions` as semver ranges (re-add `semver` + `toml` in `jackin-image`) and add a `min_engine_version` floor. Delete the five detector modules once packs reproduce their true positives; the fixture harness must execute.
- Gate: per-region extraction, priority ordering, unknown fallback, caps, the three named Claude false-positive fixtures.

**Phase 9 — Contract completion. [P2] (Slice 8)** ✅ landed June 25, 2026 — seen/ack (the daemon tick acknowledges the focused session each tick, so a pane being viewed never lingers on `done`); subagent counter (Phase 5); `clog!` flap-rate (every public transition with its winning evidence) + `status.stuck` (watchdog demotion) telemetry; `status explain <id>` (evidence bundle from the snapshot) and `status capture <id>` (daemon writes `visible.txt` + `evidence.json` under `/jackin/state/agent-status/captures/`, via a `DaemonCommand` channel). 701 capsule tests green, clippy/shear clean.

**Phase 10 — Operator surface (new slice). [P2]** ✅ landed June 25, 2026 — the producer (`PaneSnapshot.agent_status_report` populated from `SessionStatus::report`, Phase 3) and the render are in place: the host console paints the per-pane state via `AgentState::label`, which now spans the full `working/blocked/done/idle/unknown` vocabulary off the arbitrated signal (no longer the two-state flap, and `done`/`seen` are real via Phase 9 ack). The capsule tab-strip glyph is attention-only by design (flags blocked/done; working/idle/unknown show none). `token_monitor` reattributed to the token-cost-telemetry roadmap item (it is not part of this feature; left grandfathered, owned there).

**Phase 11 — Runtime adapters + hook bookkeeping. [P2] (Slice 9, parameterized by 0a)** ✅ landed June 25, 2026 — `run_agent_setup` installs/repairs the container-local reporter for the launched agent each launch (Claude `settings.json`, Codex `hooks.json`, Amp/OpenCode plugins; verify→install drift repair; failure non-fatal). Claude/Codex install but are gated to identity/freshness (0a); Kimi rule-pack-only. exit-clears-authority-before-recompute landed in Phase 6. Per-source monotonic `seq` rejection and the session-replacement allowlist are **not applicable** here: the daemon assigns arrival-order `seq` (inherently monotonic) and owns the session id, so it never trusts a reporter-supplied seq/session id. `toml_edit` stays removed — installers write JSON via `serde_json`. 701 capsule tests green, clippy/shear clean.

**Phase 12 — Image assets + co-versioning. [P2]** ✅ landed June 25, 2026 — the derived-image builder stages the reporter assets (hooks + packs) into the build context and COPYs them to `/jackin/runtime/agent-status/` (chmod 0755), dockerignore allowlisted; this makes the semantic channel work end to end (the installer's referenced script paths now exist). **Co-versioning architectural finding:** agent CLIs are bind-mounted read-only from the host, not baked/pinned in the image, so there is no pinned CLI version at *build* time — the roadmap's build-time gate assumed baked CLIs. `RulePack::accepts_cli_version` (semver) is the implemented + tested comparison and `validate()` rejects unbounded ranges at load; the correct enforcement is a **runtime** check (compare the running agent's `--version` against the pack range at launch), noted as the remaining co-versioning work. 815 tests across jackin-image + jackin-capsule green, clippy/shear clean.

**Priorities & dependencies.** P0 = Phases 0–4 (no correct signal exists without them). P1 = 5–8 (semantic channel + screen accuracy). P2 = 9–12 (contract surface, operator UI, adapters, image). Edges: 1 → everything; 2 → 3 → 4; 5/6/7/8 depend on 3–4 and may run in parallel after; 9 and 10 depend on the producer (3) and protocol (1a); 11 depends on 5; 12 depends on 8. **Cross-cutting:** update this page's Acceptance Criteria as each phase lands, and when a dependent item ([attention prompts](/reference/roadmap/agent-attention-prompts/), [task queue](/reference/roadmap/autonomous-task-queue/), [Desktop Agent Hub](/reference/roadmap/jackin-desktop-agent-hub/), [token telemetry](/reference/roadmap/token-cost-telemetry/), [session control](/reference/roadmap/console-agent-session-control/), [terminal observation](/reference/roadmap/terminal-observation-automation/)) first consumes a real signal, update it too.

### Implementation Checklist

Work top to bottom. Check an item only when it is in the build and its test is green in CI — never from intent. A phase's `Gate` is itself a checklist item; do not start the next phase until it is checked. Keep this list updated in the same change that lands each item (it is the live progress record).

**Phase 0 — Decisions & guardrails [P0]** ✅ landed June 25, 2026
- [x] 0a — Claude/Codex hook authority decided: **identity-only** (see Decision 0a above the adapter table)
- [x] 0b — `cargo shear --deny-warnings` in CI promotes unlinked-source to a hard error; the two known orphans (`agent_status.rs`, `token_monitor.rs`) are grandfathered in <RepoFile path="Cargo.toml" /> `[workspace.metadata.cargo-shear] ignored-paths`. `agent_status` was wired in Phase 1 and removed from the list; `token_monitor` is a separate token-cost-telemetry orphan (not this feature) and stays grandfathered, attributed to that item (Phase 10 reattribution)
- [x] 0c — `scripts/ci/check-agent-status-truthful.sh` (CI step) fails if the page claims "Implemented" while `agent_status` is unwired or still grandfathered
- [x] Gate 0 — verified: a synthetic unlinked file fails `cargo shear --deny-warnings` (exit 1) while the grandfathered tree stays green (exit 0); workspace still compiles; no behaviour change

**Phase 1 — Compile the foundation [P0, blocks all]** ✅ landed June 25, 2026
- [x] 1a — Created `jackin_protocol::agent_status` (`AgentRawState`, `AgentStatusReport`, `AgentStatusConfidence`, `AgentStatusSource`)
- [x] 1a — Added `AgentState::Unknown` (+ `label()` arm; three live non-exhaustive matches fixed)
- [x] 1a — Added `PaneSnapshot.agent_status_report: Option<AgentStatusReport>` (serde `default`/`skip`); host decodes via serde, populated in Phase 3/10
- [x] 1a — Raw-vs-public reconciled: wire carries public `AgentState` incl. `Unknown`; `SessionStatus` derives `done` from raw `idle` + `!seen`
- [x] 1b — `pub mod agent_status` added to the capsule `lib.rs` (follows the crate's `pub mod` convention; non-Linux `procfs` stub made `pub(crate)`)
- [x] 1b — `Duration::from_mins` was a non-issue: stable on the pinned 1.96 toolchain (already used elsewhere in the workspace) — no change needed
- [x] 1b — All 10 inline `#[cfg(test)] mod tests` relocated to sibling `tests.rs` (the two `agent_status.rs` modules merged into one)
- [x] 1b — Collapsed the dual authority model: deleted dead `HookAuthority`/`AuthorityOrigin`; `AuthorityEvidence` (consumed by `arbitrate`) is the survivor
- [x] Gate 1 — verified: `cargo check --workspace --all-targets` green; `cargo nextest` discovers + passes the **88** agent_status tests (2067 workspace-wide); `cargo shear --deny-warnings` exit 0 (agent_status de-grandfathered); `cargo clippy --workspace --all-targets -D warnings` clean

**Phase 2 — Kill the flap engine [P0] (Slice 1, rewritten)** ✅ landed June 25, 2026
- [x] 2 — `feed_pty` and `mark_operator_input` now update recency only (`last_output_at` / new `last_input_at`); deleted `state_after_pty_output`, `state_after_refresh`, `refresh_state`, and `BLOCKED_AFTER`. Sessions start `Unknown` (was `Working`)
- [x] 2 — Removed the 3 s-silence → `Blocked` inference and the daemon ticker's flap `refresh_state` loop; state is no longer authored on the tick (Phase 3 rewires the tick to assemble evidence → arbitrate → publish)
- [x] Gate 2 — `pty_output_does_not_change_state` and `operator_input_does_not_change_state` pass; `redraw_soak_produces_zero_state_transitions` replays 150 dialog repaints and asserts **zero** transitions. (The "exactly one Blocked transition" soak needs the detection pipeline; it lands with the Phase 8 fixtures.) 683 capsule tests green, clippy/shear clean

**Phase 3 — Evidence + arbitration as the only path [P0] (Slice 2)** ✅ landed June 25, 2026
- [x] 3 — Daemon `state_ticker` now assembles an `EvidenceSnapshot` per session → `arbitrate` → `SessionStatus::publish_raw` → syncs `session.state`; arbitration is the **only** path that authors state. `PaneSnapshot.agent_status_report` populated from `SessionStatus::report` (was always `None`). Evidence sources are recency-only for now (authority P5, process P6, OSC P7, screen P8 fill them in) so arbitration returns `Unknown` until those land — correct per the vocabulary
- [x] 3 — OSC progress-clear fixed to a `Weak` hint (still requires idle confirmation), never the `Strong`-idle bypass
- [x] 3 — Arbitration now consults `grade` (Complete → Authoritative, Partial → Strong); test locks it. `seq` consumption lands with Phase 5 (no authority producer assigns `seq` until runtime events arrive); the push `AgentStateChanged` event also lands with Phase 5's subscription surface — Phase 3 carries the report on the pulled snapshot
- [x] Gate 3 — 18 arbitrate tests (one per precedence step + freshness both directions + freeze + grade); `publish_raw` increments revision only on public transitions (existing `revision_increments_only_on_public_state_change`); 695 capsule+protocol tests green, clippy/shear clean

**Phase 4 — Debounce + watchdog [P0] (Slice 3)** ✅ landed June 25, 2026
- [x] 4 — `debounce` and `apply_watchdog` now run on the tick (`apply_watchdog(arbitrate(..)) → debounce → publish_raw`); added `IDLE_HOLD_CAP` (700 ms) + `started_at`; merged the inferred-idle OSC-progress/CPU-quiet guard into `debounce` and deleted the redundant `should_publish_candidate`. **Faster pending-recheck deferred** to the dirty-session early-evaluation scheduling (`EVAL_COALESCE`), only observable once evidence produces idle (Phases 5–8); at the 1 Hz floor the 700 ms cap governs inferred-idle latency (sub-2 s, never a flap)
- [x] 4 — `STARTUP_GRACE` muting deferred to Phase 8 (no screen candidates to mute yet); `RENOTIFY_INTERVAL` to Phase 9 (notifications); both consts in place
- [x] 4 — Watchdog Linux-gated via a new `physics_sampled` flag: fires only when `/proc` was actually read, so unavailable physics reads as "no evidence", never "quiet"
- [x] Gate 4 — debounce rows + `watchdog_demotes_quiet_working`, `watchdog_does_not_fire_when_physics_unsampled`, recent-output, live-child, OSC/CPU guard; 685 capsule tests green, clippy/shear clean

**Phase 5 — Semantic channel + gating fixes [P1] (Slice 4, per 0a)** ✅ landed June 25, 2026
- [x] 5 — Spawn env vars injected (`JACKIN_SESSION_ID` / `JACKIN_AGENT_RUNTIME` / `JACKIN_STATUS_SOURCE` / `JACKIN_STATUS_SOCKET`; shell panes get only the socket var), keyed on the spawn-time session id; tested
- [x] 5 — `ClientMsg::ReportRuntimeEvent` + `ServerMsg::Ack`; control-socket handler forwards over a `RuntimeEventMsg` channel; daemon select-loop arm calls `Session::apply_runtime_event` (per-source gate state → `AuthorityEvidence` with arrival-order `seq`, fed into the snapshot); `report-event --event <name> [--payload-stdin]` subcommand reads the spawn env, sends, drains stdin, always exits 0. The docker hook scripts that call it now resolve
- [x] 5 — Applied 0a: Claude/Codex hook events map to `Heartbeat` (identity/freshness) only, never state; OpenCode (Complete) + Amp keep full vendor→state mapping. Pre-0a "gating holes" obviated. `toml_edit` re-added with the installer (Phase 11)
- [x] Gate 5 — gating rows tested via OpenCode/Amp + canonical strings + `claude_and_codex_hooks_are_identity_only`; env-var + `apply_runtime_event` unit tests pass; 2740 tests across affected crates green, clippy/shear clean. **Note:** end-to-end authority→published-state is gated on Phase 6 (arbitration's fresh-authority filter needs `foreground_is_agent` from `/proc`); transport + gating + storage are complete

**Phase 6 — Process physics [P1] (Slice 5)** ✅ landed June 25, 2026
- [x] 6 — `Session::sample_process_evidence` reads `/proc` per tick (Linux): `foreground_is_agent`, `foreground_pgid`, `child_process_count`, `cpu_jiffies_delta`, `root_is_agent`, `process_exited`, `foreground_returned_to_shell`, `physics_sampled`. Gated by `process::physics_available()` (off-Linux/no-PID → default, `physics_sampled=false` — "no evidence", never "quiet"). Exit/shell-return publishes via arbitration step 1, then `clear_runtime_authority()` drops authority/gate/counters — exit transition before identity clear. This also unblocks Phase 5 end-to-end: `foreground_is_agent` now lets fresh OpenCode authority reach published state on Linux
- [x] Gate 6 — process `/proc` fixtures (direct/node-wrapped/dead-pid/descendant/CPU-window) pass; `process_evidence_unavailable_without_child_pid`, `clear_runtime_authority_drops_state_and_counters`; 691 capsule tests green, clippy/shear clean

**Phase 7 — OSC evidence consumed [P1] (Slice 6)** ✅ landed June 25, 2026
- [x] 7 — Capture per-session `OscEvidence` from the PTY parse and feed it into the snapshot: title (OSC 0/2, capped 256 chars) + `title_changed_at`; notify edge (plain OSC 9 via passthrough); OSC 9;4 progress (raw scan `scan_osc9_progress` — jackin-term doesn't surface it as a passthrough event); OSC 133 shell marks (`scan_osc133` in `feed_pty`: PreExec→working, PromptEnd/CommandFinished→idle). Wiped on exit via `clear_runtime_authority` → `OscEvidence::clear_agent_signals`. Arbitration already consumes `shell_state` (Strong) and `progress_cleared_at` (Weak hint); the title-spinner channel is consumed by the rule pack's `osc_title` region (Phase 8) and the notify edge by the attention path (Phase 9). **BEL deferred to Phase 11** (needs a jackin-term Bell passthrough variant)
- [x] Gate 7 — `osc_title_captured_and_capped` (256 cap), `osc9_notification_sets_notify_edge`, `osc94_progress_active_then_clear`, `osc133_marks_set_shell_state`; 695 capsule tests green, clippy/shear clean

**Phase 8 — Rule-pack engine + expressiveness [P1] (Slice 7)** ✅ landed June 25, 2026
- [x] 8a — **Wired the engine into the tick** (keystone): `RulePackRegistry::bundled()` loaded once; per session per tick it evaluates the pack over `Session::visible_screen_rows` (scrollback never read) → `ScreenEvidence` in the snapshot. Screen detection was the universal fallback but ran nowhere — identity-only runtimes (Claude/Codex per 0a) and Kimi now get state from the pack. OSC virtual regions fed from captured evidence (`osc_title`, `osc_progress` via new `progress_raw`)
- [x] 8 — TOML loader, regions, priority, unknown fallback, safety caps were already present and tested
- [x] 8 — Added `line_regex` (existential per-line) and `forbids_regex` (anchored negation → OSC-title idle-by-subtraction); `validate()` compile-checks every regex at load (fixes the silent-never-match defect). Regex alternation within `line_regex`/`regex` subsumes most nested `all`/`any`/`not` cases, so a recursive `Gate` struct is deferred unless a pattern provably needs it; a compiled-regex cache (compile-once perf) is a noted follow-up
- [x] 8 — Added the prompt-caret region family (`after_last_prompt_marker`, `before_current_prompt_marker`, `whole_recent_without_current_prompt_marker`) for Codex live-vs-stale discrimination. Existing `after_last_rule` already passes the horizontal-rule false-positive fixture, so its geometry is unchanged; block-marker regions remain a Codex refinement no pack needs yet
- [x] 8 — `validate()` enforces `validated_versions` as a **bounded** semver range (rejects `*` and lower-only); `RulePack::accepts_cli_version` is the image-build co-versioning comparison (semver in the capsule, where the loader is). The construct build step feeding installed CLI versions is the remaining wiring (Phase 12); `min_engine_version` deferred — the exact `schema_version==1` check already hard-gates
- [x] 8 — No old detector modules remained (already gone); the fixture harness `packs_load_and_match_fixtures` executes for all five runtimes incl. the three named Claude false-positive classes
- [x] Gate 8 — per-region extraction, priority, unknown fallback, caps, the false-positive fixtures, `line_regex`/`forbids_regex`, caret regions, and semver bounds all pass; 701 capsule tests green, clippy/shear clean

**Phase 9 — Contract completion [P2] (Slice 8)**
- [ ] 9 — seen/ack on focus + attach; subagent counter; `status explain` / `status capture`; flap-rate + stuck telemetry
- [ ] Gate 9 — focus/attach ack end-to-end through the daemon (idle+unseen→done, focus→idle, re-work→done)

**Phase 10 — Operator surface [P2] (new slice)**
- [ ] 10 — Produce `PaneSnapshot.agent_status_report`; swap host console + capsule renders to the full `working/blocked/done/idle/unknown` vocabulary with seen/done
- [ ] Gate 10 — operator sees correct, non-flapping state per pane; `done` and `unknown` reachable

**Phase 11 — Runtime adapters + hook bookkeeping [P2] (Slice 9, per 0a)**
- [ ] 11 — Per-runtime assets; per-source monotonic `seq` rejection; session-replacement allowlist; exit-clears-before-recompute; Kimi rule-pack-only
- [ ] Gate 11 — per-runtime recorded event sequences assert the public transition sequence, incl. the interrupt hole

**Phase 12 — Image assets + co-versioning [P2]**
- [ ] 12 — Dockerfile/build step copies `runtime/agent-status/` into derived images; build-time pack-version validation rejects unpinned/mismatched CLI
- [ ] Gate 12 — derived image contains the assets and packs; image build fails on a version mismatch

**Cross-cutting (every phase)**
- [ ] Update this page's Acceptance Criteria / Current Implementation State as each phase lands
- [ ] Update each dependent roadmap item when it first consumes a real signal

## Implementation Blueprint

Each slice below is independently shippable, lands in its own PR, and is specified so an implementing agent needs no further design decisions — when something is ambiguous, the Architecture Specification above is the tiebreaker, and this page must be updated in the same PR as any deviation. **Follow the Execution Plan above for ordering and the corrected scope; the slices here are the per-step detail.** Every slice ends with `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo nextest run -p jackin-protocol -p jackin-capsule`, and the capsule smoke-test mandate from <RepoFile path=".github/AGENTS.md" />. New files use the crate's self-named module layout (no `mod.rs`).

### Slice 1 — Kill the flap engine

*Why first*: removes the dominant false-positive source with a two-site deletion; everything else builds on a quiet baseline.

- In <RepoFile path="crates/jackin-capsule/src/session.rs" />, the PTY output handler currently sets `last_output_at` and then calls `self.apply_raw_state(crate::agent_status::AgentRawState::WorkingVisible)` — delete the `apply_raw_state` call, keep the timestamp. In `mark_operator_input`, delete the `apply_raw_state(WorkingVisible)` call, keep `last_output_at` (rename to a dedicated `last_input_at` field in this slice) and the `was_blocked` return.
- Add `last_input_at: Instant` next to `last_output_at`; both are evidence inputs for later slices.
- Interim behavior (until slice 2 lands): state changes come only from the 1 Hz detector tick and reporter messages; sessions start `Unknown` and may stay `Unknown` longer — that is correct per the vocabulary table.
- Tests (in `session/tests.rs`): `pty_output_does_not_change_state` (feed bytes while `Blocked`, assert still `Blocked`); `operator_input_does_not_change_state` (same while `Blocked` and while `Done`). Flap soak test (integration): drive a fake PTY that redraws a Claude-style permission dialog every 200 ms for 30 s; assert exactly one transition total (the initial entry into `Blocked`).
- Do not: add any replacement signal, timer, or lease — silence/activity must author nothing.

### Slice 2 — Evidence model and arbitration as the only path

- New file `crates/jackin-capsule/src/agent_status/evidence.rs`: the `EvidenceSnapshot`, `AuthorityEvidence`, `OscEvidence`, `ScreenEvidence`, `ProcessEvidence`, `ActivityEvidence`, `AuthorityGrade { Complete, Partial }` types exactly as in the Data model section (fields may grow, never shrink). All fields plain data; no methods with I/O.
- Rewrite <RepoFile path="crates/jackin-capsule/src/agent_status/arbitrate.rs" />: signature `pub fn arbitrate(snapshot: &EvidenceSnapshot, previous_raw: RawAgentState, now: Instant) -> ArbitrationResult` where `ArbitrationResult { raw: RawAgentState, confidence: StatusConfidence, winner: EvidenceWinner, notes: Vec<EvidenceNote> }` and `EvidenceWinner` names which step (1–7) decided. Implement the seven-step precedence verbatim from the Arbitration section, including: step 2 freeze returns `previous_raw`; the step-3 freshness rule compares `screen.observed_at` against `authority.last_event`; and the old inverted idle-grace bug must not return — staleness of a *screen observation* lowers its rank, it never raises it.
- Delete `SessionStatus::advance`, the `AgentRawState` signal enum in `agent_status.rs` (the protocol crate's state enum stays), and the `transition` function; `SessionStatus` keeps `effective`, `seen`, `revision` and gains `raw`, `confidence`, `last_snapshot_summary`.
- In <RepoFile path="crates/jackin-capsule/src/daemon.rs" />, the state tick assembles an `EvidenceSnapshot` per session (screen result from the existing detectors until slice 7; authority from the stored `HookAuthority` until slice 4 re-shapes it; process/OSC fields default until slices 5–6), calls `arbitrate`, then applies the debounce policy (slice 3; until then, apply transitions directly), then derives effective from raw + seen, then broadcasts.
- `broadcast_agent_state_changed` is rewritten to serialize the real `ArbitrationResult` + snapshot summary: real detector booleans, real winner, real confidence, real foreground pgid (None until slice 5), seen revision from the acknowledgment bookkeeping (slice 8 wires the callers).
- Tests: one test per precedence step (7); freshness-override both directions; `freeze` keeps previous; property tests — arbitration never panics for any combination of present/absent evidence (use a small hand-rolled combinatorial loop, not a new dependency), same snapshot twice → same result, revision increments only on public transitions.

### Slice 3 — Debounce policy and physical watchdog

- New file `crates/jackin-capsule/src/agent_status/policy.rs`: all constants (`AUTHORITY_TTL = 30 s`, `WATCHDOG_QUIET = 10 s`, `IDLE_CONFIRMATIONS = 3`, `STARTUP_GRACE = 3 s`, `CPU_SAMPLE_WINDOW = 2 s`, `RENOTIFY_INTERVAL = 5 min`, `EVAL_COALESCE = 250 ms`) and `pub fn debounce(prev_public: AgentState, candidate: &ArbitrationResult, pending: &mut PendingTransition, now: Instant) -> Option<AgentState>` implementing the policy table row by row: blocked/working/positively-matched-idle/exit immediate; inferred idle requires `IDLE_CONFIRMATIONS` consecutive candidates with no OSC progress active and CPU-quiet; startup grace mutes screen-derived candidates entirely.
- Watchdog inside the tick, after arbitration: if `raw == Working` and `now - last_output > WATCHDOG_QUIET` and `cpu_jiffies_delta == 0` and `child_process_count == 0`, override the candidate to `Unknown` with an `EvidenceNote::WatchdogDemoted`, and emit a rate-limited stuck diagnostic event.
- Tests: every table row; `watchdog_demotes_quiet_working_authority`; `watchdog_does_not_fire_during_network_wait_with_recent_output`; `inferred_idle_needs_three_confirmations`; `visible_idle_publishes_immediately`; `startup_grace_mutes_screen`.

### Slice 4 — Semantic channel on, safely

- Spawn env: in the `session.rs` spawn path (the function that builds the per-pane `CommandBuilder`), add `cmd.env("JACKIN_SESSION_ID", id.to_string())`, `JACKIN_STATUS_SOURCE = format!("hook-{agent}-{id}")`, `JACKIN_AGENT_RUNTIME = agent slug`, `JACKIN_STATUS_SOCKET = "/jackin/run/jackin.sock"`. Shell panes get only the socket var.
- New subcommand in <RepoFile path="crates/jackin-capsule/src/main.rs" />: `jackin-capsule report-event --event <name> [--payload-stdin]` — reads env for session/source/runtime, reads optional JSON payload from stdin, sends one framed control message `ReportRuntimeEvent { session_id, source_id, runtime, event, payload }` over the socket, exits 0 even on failure (reporters must never break agent hooks). Extend `ClientMsg` in <RepoFile path="crates/jackin-protocol/src/control.rs" /> accordingly; keep `ReportAgentState` for role-authored reporters but mark it lower-trust in gating.
- New file `crates/jackin-capsule/src/agent_status/gating.rs`: `pub fn map_event(event: &RuntimeEvent, state: &mut SourceGateState) -> GateEffect` implementing the gating table verbatim (prompt-submitted/tool-start/tool-end → working; permission-requested/question-asked → blocked + `pending_permission = true`; permission-resolved → clears it + working; turn-complete → idle **only if** `!pending_permission && subagents_active == 0`, else working with a `GateNote::StopSuppressed`; subagent-start/stop → counter only; session-end → clear authority; heartbeat → freshness only). The daemon assigns `seq` by arrival order per source; reporter timestamps go into evidence notes only.
- Rewrite the Claude hook script at <RepoFile path="docker/runtime/agent-status/hooks/claude/report-hook.sh" /> as a dumb pipe: read stdin, extract `hook_event_name` (and notification type for `Notification`), exec `jackin-capsule report-event --event "$EVENT" --payload-stdin`. Delete the Stop-blocking branch, the python framing, the `nc` dependency. Map vendor event names to canonical ones in `gating.rs` (one match arm per runtime), not in shell.
- Installer (<RepoFile path="crates/jackin-capsule/src/agent_status/hook_installer.rs" />): add `Notification` to the registered events; change `merge_hook_entries` to append-or-repair its own command entry inside each event's array without touching other entries; delete registrations for unverified events (`TaskCreated`, `TaskCompleted`) until a live payload confirms them.
- Delete the old OpenCode ACP bridge and the Kimi placeholder hook; Slice 9 names the runtime-specific replacements. Launch nothing at entrypoint scope that needs a session id.
- Tests: every gate row including `stop_with_pending_permission_stays_blocked`, `stop_with_live_subagents_stays_working`, `permission_resolved_unblocks`; arrival-order sequencing under interleaved sources; an integration test that spawns a session and asserts the four env vars are present in `/proc/<child>/environ`.

### Slice 5 — Process physics

- Wire <RepoFile path="crates/jackin-capsule/src/agent_status/process.rs" /> into the tick: known child PID from spawn is the anchor; sample foreground pgid (`tcgetpgrp` equivalent via `/proc/<pid>/stat` tpgid), child-process count (scan `/proc` for ppid chains under the agent PID, cached per tick), and CPU-jiffy delta (utime+stime difference across `CPU_SAMPLE_WINDOW`). Populate `ProcessEvidence`.
- Exit ordering: the existing reaper path publishes the exit transition (raw idle, evidence `process_exited`) **before** session teardown clears identity and authority; foreground-returned-to-shell (root process group owns the foreground again while the child is alive, after prior agent identity observation and with no descendant work) publishes idle with a `foreground_returned_to_shell` note, then clears agent identity/authority and seeds shell-pane evidence.
- Tests: pure process fixtures at the `/proc` boundary (direct binary, `node`-wrapped, dead pid, shell foreground ownership, descendant tree counting); `exit_transition_precedes_identity_clear`; CPU-delta sampling-window math.

### Slice 6 — OSC evidence channel

- At the parser boundary where the capsule already processes per-session escape sequences, retain `OscEvidence` per session: title changes (OSC 0/2), OSC 9-family notification arrivals, OSC 9;4 set/clear with payload state, BEL. Cap stored payload length (256 bytes) — OSC content is untrusted model output. Accept only while `foreground_is_agent`; wipe on identity change and on agent restart.
- Wire the existing `scan_osc133` into the shell-pane path: `PromptEnd → idle`, `PreExec → working` (these enter arbitration as screen-strength evidence for shell panes only).
- Tests: synthetic byte streams set/clear each field; payload cap; `osc_ignored_when_foreground_not_agent`; shell pane goes working on pre-exec and idle on prompt-end.

### Slice 7 — Rule-pack engine

- New file `crates/jackin-capsule/src/agent_status/rules.rs`: TOML pack loader (schema from the Screen-rules section: `schema_version`, `agent`, `validated_versions`, `[[rule]]` with `id`, `state` (`working|blocked|idle|freeze`), `priority`, `region`, `strength`, `requires_all`, `requires_any`, `forbids`, optional `regex`), region extraction (`bottom:N`, `bottom_nonempty:N`, `prompt_box_body`, `above_prompt_box`, `after_last_rule`, `last_nonempty_line`, `osc_title`, `osc_progress`), highest-priority-wins evaluation, `unknown` on no match, and safety caps (≤128 rules, ≤32 matchers/rule, ≤512 chars/matcher).
- Packs live at `docker/runtime/agent-status/packs/<agent>.toml`, installed under `/jackin/runtime/agent-status/packs/`, and loaded at daemon start with operator overrides taking highest precedence. Encode the corrected Claude rules (no transcript-bullet spinners, spinner at line start + interrupt hint, no horizontal-rule prompt fallback, blocked anchored to dialog-affordance co-presence), and translate the existing Codex/Amp/Kimi/OpenCode detector patterns into packs.
- Delete the five `detectors/*.rs` modules and the `Detector` trait once packs reproduce their true positives.
- Fixture harness (new test file): walk `agent_status/screen/fixtures/<agent>/*.txt`; expectation encoded by filename prefix (`working*`, `blocked*`, `idle*` → that state; `false_positive*` → first line of the fixture names the wrong state it must NOT produce, asserted as "anything but"); the three Claude false-positive fixtures must pass. `jackin-capsule status capture <session>` writes a new fixture from the live grid + evidence bundle.
- Co-versioning: pack `validated_versions` checked against the pinned agent CLI version during image build (build-jackin-capsule / construct pipeline emits an error listing pack vs pinned version on mismatch).
- Tests: per-region extraction; priority ordering; unknown fallback; caps enforced; the harness itself.

### Slice 8 — Contract completion

- Seen/ack: call the acknowledgment path when a pane gains focus and when an attach client views it (daemon focus-change and attach handlers); `done → idle` on focus, re-work resets seen (already in the work-cycle rule). End-to-end test through the daemon, not only the unit machine.
- Subagent counter: consumed from gating (slice 4) and from `ReportChildAgentState` for bridge reporters; expose `subagents_active` in evidence and events.
- `jackin-capsule status explain <session>`: print the current snapshot, every rule evaluated with a region preview (240 chars), authority state/grade/freshness, winner step, and active debounce holds; compact/debug logs carry transition and stuck breadcrumbs, while `status capture` writes the full evidence bundle.
- Telemetry counters: transitions per session-minute and watchdog demotions, emitted through the existing `clog!`/diagnostics path.

### Slice 9 — Runtime adapters

- Codex: installer writes container-local `~/.codex/hooks.json` (events from the adapter table) and enables the TUI notification OSC method in container config; title-spinner mapping in `OscEvidence`; dumb-pipe hook script.
- OpenCode: container-local plugin (config home, never project tree) forwarding the five event kinds to `jackin-capsule report-event`; defensive handling for the known stuck-status/dropped-promise upstream quirks (treat missing idle events as TTL expiry, never as proof of work).
- Amp: container-local plugin on the four lifecycle events.
- Kimi: rule pack is primary; ensure no hook assets remain; pack fixtures for moon-phase/braille thinking, approval prompt, idle box.
- Per-runtime gating tests with recorded event sequences (prompt → tools → permission → resolve → stop) asserting the public transition sequence, including the interrupt hole: working authority + Esc (no event) + quiet physics → watchdog → unknown, then prompt box → idle.
- UI polish landed with `stuck` as a diagnostics overlay, token usage on session snapshots/events, capsule status-bar glyphs, and console session rows that display the shared public state vocabulary.

## Acceptance Criteria

- No code path translates PTY output bytes or operator keystrokes into a state value (reviewable invariant; soak test enforces it).
- Reporters forward runtime events; all mapping and gating lives in daemon Rust with a unit test per gate row.
- The fixture harness executes: every built-in runtime has passing working / blocked / idle / false-positive fixtures, including the three named Claude false-positive classes.
- Every `AgentStateChanged` carries evidence computed from inputs, never from the output state.
- Authority demonstrably expires: TTL lapse, process exit, identity contradiction, and watchdog demotion are each tested and leave evidence notes.
- Focus/attach acknowledgment works end-to-end through the daemon: raw idle + unseen → `done`, focus → `idle`, re-work → `done` again.
- Rule packs declare validated agent-version ranges and the image build enforces the pairing.
- A quiet live session never becomes `blocked` from silence, and an unmatched screen never becomes `idle` — both fall to `unknown`.
- Wait semantics hold when the session is already in the requested state; roll-up priority is covered by tests.
- Flap-rate telemetry exists; the blocked-dialog soak test shows one initial transition into `blocked` and zero redraw-driven flaps after that.

## Do Not Do

- Do not copy Herdr, claude-squad, cmux, or coder/mux source or wire formats; concepts only, implemented from scratch, source never open side-by-side.
- Do not let PTY output activity or operator keystrokes author state — not even temporarily.
- Do not let reporters change agent behavior (no stop-blocking, no decision injection).
- Do not map Claude Code's idle-timer notification to blocked; do not treat OSC 9;4 progress-active as working-proof; do not use cursor-probe timeouts as a blocked detector (event-loop TUIs).
- Do not fall back to `idle` when nothing matches — fall back to `unknown`. Do not treat silence as `blocked`.
- Do not silently edit host agent homes, shell rc files, terminal settings, git config, or user repositories; do not create jackin-owned container paths outside `/jackin/`; do not write status hooks/plugins into bind-mounted project directories.
- Do not let every UI surface parse terminal text independently; do not add broad regexes over historical output — rules are narrow, region-anchored, fixture-backed data.
- Do not let cooperative state files or hooks prove health without process/session validation, freshness, and the physical watchdog.

## Future Decisions

- **Claude/Codex hooks: state authority or identity-only?** (raised by the June 25 Herdr re-review). Herdr strips lifecycle→state hooks for these two CLIs and lets the screen pack own state, because completion events fire late/out of order (SubagentStop/recap after Stop). Decide whether jackin' keeps Partial-grade state mapping defended only by the subagent/permission gates, or demotes Claude/Codex hooks to identity + freshness and lets the co-versioned pack + physics watchdog decide. Pinned CLI versions make the identity-only stance cheap.
- **Bounded idle-hold and identity-miss hysteresis** — add an `IDLE_HOLD_CAP` (~700 ms) and faster pending recheck to `policy.rs`, and an explicit consecutive-miss count before identity teardown, per the Herdr re-review.
- **Rule-engine expressiveness** — add per-line `line_regex`, nested `all`/`any`/`not` gates, and `not` inside OSC-title rules to the pack schema. This is the largest detection-accuracy gap; it subsumes the count-token rule ("agent waiting on its own background tasks = `working`") as one `line_regex` case, and unlocks Codex idle-by-subtraction from the title bar.
- **Prompt-caret regions and structural geometry** — add the caret-relative region family and specify one reused `is_horizontal_rule` primitive that also localizes the prompt box by counting two rules from the bottom (no corner-glyph assumption — Claude/Codex draw plain `─` boxes).
- **Debounce mechanics** — specify the dual scan cadence, the 800 ms stable-blocker re-publish, the content-sequence idle-scan dedupe, and the screen-fresher-than-hook override comparison, not just the confirmation count.
- **Hook-authority bookkeeping** — per-source monotonic sequence rejection, exit-clears-before-recompute ordering, and a lifecycle-event-keyed session-replacement allowlist for the Complete-grade reporters (OpenCode), independent of the Claude/Codex decision.
- **Pack engine-version floor** — add `min_engine_version` negotiation so an older capsule safely rejects an operator-override pack it cannot evaluate.
- **Role-authored rule packs and named states** once the role manifest contract exists (nearly free after slice 7).
- **Adaptive watchdog timeouts** (bounded multiple of per-runtime median cycle time) once telemetry exists.
- **Structured headless bridges** — `codex exec` JSONL, OpenCode SSE, Claude supervisor state — for jackin-managed queue/headless modes; higher-grade authorities where they observe the same session.
- **LLM-tier status descriptions** as a rate-limited display layer over the badge; never the badge.
- **Amp/Kimi deeper integrations** as upstream surfaces stabilize.

## Appendix A — Historical Validation Audit (June 10, 2026)

This audit is the pre-implementation defect record that drove the branch. Keep it as regression context, not as the current gap list: the June 11, 2026 implementation replaced the byte-driven state machine with evidence arbitration, live reporter events, `/proc`/OSC corroboration, rule packs, fixture coverage, and `status explain` diagnostics.

1. **PTY byte → working.** The old `Session` PTY output handler and `mark_operator_input` both called `apply_raw_state(WorkingVisible)`. Consequences observed: a Blocked dialog repaint flipped Blocked→Working and the next 1 Hz detector tick flipped it back (one event + redraw per flip, every second); an idle Claude pane repaint flipped Idle→Working, the next tick's prompt-box match produced raw idle, and because entering a work cycle reset `seen`, the public state landed on a false `Done` — repeating forever; arrow keys inside a blocked dialog flipped Blocked→Working and re-notified on re-entry; shell panes (no detector registered) became permanently `Working` after any output.
2. **Arbitration dead.** The old arbitration module's own header said it was "currently used in tests only". The live mutation path was `SessionStatus::advance`, last-signal-wins. The unused function also carried an inverted grace bug: its visible-idle staleness check measured observation age, so an *older* (less trustworthy) screen observation was what overrode a working authority.
3. **Hook channel dead end-to-end.** No Rust code set `JACKIN_SESSION_ID` (zero occurrences in the workspace); every shipped reporter script began with an exit-if-unset guard. The entrypoint exported `JACKIN_STATUS_SOURCE` with a session-id default of `0` at container scope. `heartbeat.sh` shipped and was never launched. Authority `last_seen` was refreshed but no staleness sweep existed, and because arbitration was unwired, stored authority affected only the broadcast's `source` string.
4. **ACP bridge triple-fault.** Launched at entrypoint scope where the session id is unset (guard exits); even if running, it spawns a second `opencode acp` instance — a different session than the pane's TUI, which this page forbids as a truth source; and its `opencode acp | python3 -u - <<'PYEOF'` pipeline lets the heredoc replace python's stdin, so the JSON-RPC stream is never read.
5. **Stop-hook behavior mutation.** The Claude hook returns `{"decision":"block"}` when background tasks are reported running, forcing Claude to continue — agent control-flow mutation inside an observability feature. Also, all report invocations in one hook run share one timestamp-derived sequence value, and timestamp-as-sequence across concurrent hook processes gives no causal order (an async PostToolUse stamped after a sync Stop overwrites idle → stuck Working at turn end).
6. **Installer gaps.** Registered events omitted `Notification` (the channel carrying permission-prompt and elicitation types, which the hook script handles); `TaskCreated`/`TaskCompleted` registrations were unverified against vendor docs; `merge_hook_entries` replaced each event's whole array, destroying role-authored entries; the reporter transport was sh + cat + two python spawns + `nc -U` per event — a fork-heavy latency tax on Claude's synchronous hook path with a BusyBox-vs-OpenBSD `nc` portability hazard.
7. **Detector false-positive generators (Claude).** The spinner glyph list includes transcript bullets (`⏺`, `·`, `•`, `●`, `▸`, `∙`, `○`), and the rule is glyph + later "ing" + ellipsis anywhere in the line — a historical transcript line such as a tool-call bullet with a gerund matches `WorkingVisible` while the agent idles at its prompt. `is_blocked` matches the joined bottom-24-row window, so an approval question that scrolled into transcript keeps firing `BlockedVisible` while the agent works. The prompt-box fallback accepts any row with more than 20 horizontal-rule characters — Claude prints such rules in normal output, producing false `PromptVisible` whenever working chrome is momentarily absent at the sample instant. No debounce exists anywhere, so each single-frame mis-sample publishes.
8. **Fixtures unloaded.** No harness loads `agent_status/screen/fixtures/`; the Claude `false_positive_old_spinner` fixture (stale spinner line above a live prompt box) classifies as Working under the current detector — proof the fixtures were never executed.
9. **Evidence fabricated.** `broadcast_agent_state_changed` sets `visible_blocker := state == Blocked`, `visible_idle := state == Idle`, `visible_working := state == Working`, `process_exited := state == Idle`, `confidence: None`, `foreground_pgid: None`, `last_seen_revision := current revision` — every diagnostic field derived from the output, none from inputs.
10. **Dead modules.** `/proc` identity (`read_tpgid`, `identify_agent`, `detect_foreground_agent` — implemented and unit-tested), `scan_osc133`, cursor-probe raw states, and `seen::acknowledge_session`/`mark_pane_focused` had zero live callers; `ReportChildAgentState` was an empty match arm; one unit test asserted the subagent guard "by simulating the daemon guard by not calling advance" — testing a guard that did not exist.
11. **Misc.** The legacy `Session.state` field initializes to `Working` while `SessionStatus` starts `Unknown`; the capsule and protocol crates each define a different `AgentRawState` (name collision, wrong-import hazard); `visible_lines()` dumps the entire grid per session per tick where the detection window suffices.

## Appendix B — Herdr Reference Notes (concepts and observed facts; AGPL — no code)

Source review of head `1fc5e84`, June 10, 2026; re-reviewed at head `58b9fecc`, June 25, 2026 (see *Herdr re-review* under Research Base for what changed — notably Claude/Codex hooks downgraded to identity-only and a count-token background-task working rule). Facts recorded for design comparison; all jackin' implementation must be original.

- **Signal classes**: foreground-process identity (tpgid from `/proc/<pid>/stat`, process-group scan, wrapper unwrapping for node/bun/python/`sh -c`, symlink canonicalization); bottom-of-buffer screen text (last terminal-height rows — operator scrollback never affects detection); passively captured OSC 0/2 title and OSC 9-family progress (payloads capped at 256 chars, wiped on agent change); Unix-socket lifecycle reports with per-source monotonic sequence. Deliberately absent: CPU sampling, `/proc` state chars, syscall tracing, timers-on-output.
- **Authority model**: agents with complete lifecycle-hook coverage get exclusive authority — screen detection is short-circuited entirely while live. Claude Code and Codex hooks are deliberately *not* state authorities there (they miss permission results and Esc interrupts); for those, screen/OSC manifests decide. A live visible blocker overrides a non-blocked hook only when the screen observation is not older than the hook report.
- **Observed runtime policy facts**: detection tick ~300 ms with an identified agent, ~500 ms without; working→plain-idle requires 3 consecutive idle reads on a ~100 ms recheck capped at ~700 ms, while positively matched visible idle bypasses the hold and blocked is never held; a persisting visible blocker is re-published roughly every 800 ms to keep downstream freshness; startup grace ~3 s with OSC state wiped so a new process cannot inherit the previous title; identity cleared only after 6 consecutive probe misses, except foreground-return-to-shell which publishes an exit transition first; released sources are suppressed from re-acquisition for ~1 s; remote manifest updates poll every 30 min with size caps, dotted-version ordering, engine-version gates, and full validation before acceptance; local override beats remote beats bundled.
- **Manifest engine shape**: priority-ordered rules, each naming a target state, a structural region (whole-recent, bottom-N, prompt-box body, above-prompt-box, after-last-prompt-marker, after-last-horizontal-rule, plus osc-title/osc-progress virtual regions), strong-evidence flags, substring/regex matchers with nested all/any/not gates, and overlay rules that freeze state; unmatched screens fall back to idle (jackin' deliberately diverges: `unknown`). Complexity caps protect the engine from pathological rule data.
- **Era timeline**: hand-coded per-agent matchers (every TUI wording change = code change + release) → PTY-activity-first with keystroke-taint causality, working leases (~1.8 s), and idle/working confirmation debounces (June 8–9) → all deleted June 10 with changelog rationale that output activity no longer publishes working, vetoes blockers, or decides idle, because input echo, resizes, redraw nudges, and agent-spawned background processes defeat byte-level causality → manifest engine + OSC evidence, with a final fix dropping a Codex background-terminal rule because long-running helper terminals kept panes falsely working.
- **Diagnostics**: an explain command dumps final state, manifest provenance, every evaluated rule with matcher/gate counts and a region preview, and the fallback reason; their contributor loop is reproduce → explain → edit rule → hot reload → verify.

## Appendix C — Per-Runtime Signal Surface Reference (verified June 10, 2026)

- **Claude Code** — hooks: <a href="https://code.claude.com/docs/en/hooks">code.claude.com/docs/en/hooks</a>; terminal config: <a href="https://code.claude.com/docs/en/terminal-config">terminal-config</a>; agent view: <a href="https://code.claude.com/docs/en/agent-view">agent-view</a>; monitoring: <a href="https://code.claude.com/docs/en/monitoring-usage">monitoring-usage</a>. Events incl. `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PermissionRequest`, `PermissionDenied`, `PostToolUse`, `PostToolUseFailure`, `Notification` (types `permission_prompt`, `idle_prompt` — fires ~60 s after idle, `elicitation_dialog`, `auth_success`), `SubagentStart`/`SubagentStop`, `Stop`, `StopFailure`, `SessionEnd`; hooks accept `async: true` and a per-hook timeout; all receive `session_id` on stdin. `AskUserQuestion` is a tool, so a `PreToolUse` matcher on it is the question-dialog signal. Known reliability issues: false/missing idle notifications and payload gaps (anthropics/claude-code issues 12048, 13024, 11964, 8320). Emits OSC 9-family notifications per `preferredNotifChannel` and ConEmu OSC 9;4 indeterminate progress; the progress bar animates during approval prompts (issue 12620), so active ≠ working. Does not emit OSC 133 (issue 32635 closed unimplemented). OTEL traces include a blocked-on-user span — audit-grade, too slow for badges. `claude agents --json` exposes native `working`/`blocked`/`done`/`failed`/`stopped` plus a waiting-for reason, supervisor-hosted sessions only.
- **Codex CLI** — hooks: <a href="https://developers.openai.com/codex/hooks">developers.openai.com/codex/hooks</a>; config: <a href="https://developers.openai.com/codex/config-reference">config-reference</a>; non-interactive: <a href="https://developers.openai.com/codex/noninteractive">noninteractive</a>. Hooks shipped and on by default (`~/.codex/hooks.json` or config): `SessionStart`, `UserPromptSubmit`, `PreToolUse`, `PermissionRequest`, `PostToolUse`, `SubagentStart`/`SubagentStop`, `Stop`; only command handlers run; async entries are skipped. The separate `notify` program receives `agent-turn-complete` only (approval gap tracked in openai/codex issue 13478). `tui.notifications` filters `agent-turn-complete` and `approval-requested` with `notification_method = osc9` available; the terminal title defaults to spinner + project items, making it a busy/idle channel. `codex exec --json` emits thread/turn/item lifecycle JSONL.
- **OpenCode** — plugins: <a href="https://opencode.ai/docs/plugins/">opencode.ai/docs/plugins</a>. Events: `session.status` (busy/idle), `session.idle`, `session.error`, `permission.asked`/`permission.replied`, `message.updated`, `tool.execute.before/after`; the same stream is available over the server's SSE endpoint. Known quirks: status stuck after async prompts (anomalyco/opencode issue 12860); `session.idle` plugin handlers fire-and-forget (issue 16879).
- **Amp** — manual: <a href="https://ampcode.com/manual">ampcode.com/manual</a>; plugin API: <a href="https://ampcode.com/manual/plugin-api">plugin-api</a>. Plugin events `session.start`, `agent.start`, `tool.call`, `tool.result`, `agent.end`; built-in notifications on completion and on blocked-waiting; `AMP_FORCE_BEL` forces a BEL.
- **Kimi CLI** — wire mode: <a href="https://moonshotai.github.io/kimi-cli/en/customization/wire-mode.html">wire-mode docs</a> (JSON-RPC stdio with turn/step/status/approval/subagent events — a server mode, not the TUI). No TUI hook surface; no OSC 9 yet (MoonshotAI/kimi-cli issue 1342).
- **Non-built-in runtimes** (reference for the future custom-role reporter contract): Gemini CLI hooks (<a href="https://geminicli.com/docs/hooks/">geminicli.com/docs/hooks</a>) include `BeforeAgent`/`AfterAgent` (turn boundaries), `BeforeTool`/`AfterTool`, and `Notification` with the documented `ToolPermission` type — hooks run synchronously in the agent loop; only `ToolPermission` is concretely documented, treat broader marketing claims as unverified. Aider has no hook system but `--notifications-command` fires when the LLM finished and waits for input (<a href="https://aider.chat/docs/usage/notifications.html">aider docs</a>); as a plain readline CLI it genuinely blocks in tty read while waiting, so the wchan-class check in Appendix F is valid for it alone. Pi exposes extension lifecycle events (`agent_start`/`agent_end`, `turn_start`/`turn_end`, `tool_call`) via pi-mono's extension API.
- **Community corroboration** (secondary sources): hook-notification write-ups at alexop.dev, kane.mx, and martin.hjartmyr.se; Hacker News threads on cmux, Agent Notify, Ccnotifs, and Sculptor document the same false-positive complaints this design eliminates.

## Appendix D — Orchestrator Survey (what to borrow, what to avoid)

| Tool | Mechanism | Lesson for jackin' |
|---|---|---|
| workmux (MIT) | Per-agent hooks assert Working/Waiting/Done; watchdog marks "working + pane output unchanged 10 s" as interrupted, auto-clears on resume | The hook+watchdog cross-check — adopted as the universal demotion rule |
| tmux-agent-status (no license) | Pure hooks; Stop→done gated on a pending-background-tasks claim | Gate turn-complete on pending permission + live subagents; verify the background-tasks payload field before trusting it |
| claude-squad (AGPL) | 500 ms tick, SHA-256 pane hash changed ⇒ Running; literal prompt strings ⇒ Ready | Anti-pattern: animation = work; UI copy change breaks Ready |
| VibeTunnel (MIT) | Output within 5 s ⇒ active; BEL + process-tree attribution; title filter that owns OSC 2 | Recency ≠ semantics; BEL attribution and title ownership are worth borrowing |
| cmux (dual proprietary) | Terminal itself interprets OSC 9/99/777 from panes; installs hooks for 13 agents | Closest cousin; its tmux-passthrough failure cannot happen when the capsule is the terminal |
| unitmux | Title markers + recent-output prompt parsing, EN+JA pattern packs | The maintenance burden of pattern packs without co-versioning |
| Claude agent view (native) | Supervisor owns processes; exact states; Haiku row summaries | Own the process ⇒ get the truth; LLM only for descriptions |
| Crystal / Conductor / omnara / happy | Drive `claude` via stream-JSON / SDK relays instead of PTY scraping | Right answer for headless modes; not applicable to operator-visible PTY panes |
| claude_code_agent_farm (MIT) | Heartbeat files (stale >2 min ⇒ stuck); adaptive idle timeout = 3× median cycle bounded 30 s–600 s | Adaptive watchdog bounds as future work |
| zellaude / claude-code-zellij-status | Hook → pipe → plugin state; ~16 states from four hook kinds | Hook-fed state files work; host-side install is their constraint, not ours |
| coder/mux (AGPL) | Sidebar status from a small model at intervals | LLM-as-classifier: descriptions only |
| tmux native | `monitor-activity` / `monitor-silence` / `monitor-bell` | Documented insufficiency (tmux issues 172, 2047): cannot distinguish waiting from finished from nothing |

## Appendix E — Terminal-Protocol Signal Reliability

| Signal | Agent emission | Notes |
|---|---|---|
| OSC 133 A/B/C/D prompt marks | No agent TUI emits them; shells do (fish 4+, nushell natively; bash/zsh via integration) | Excellent for shell panes (`C` running, `D;exit` done). jackin' synthesizes them in container shell rc. Spec: <a href="https://iterm2.com/documentation-escape-codes.html">iTerm2 escape codes</a> |
| OSC 9 / 99 / 777 notifications | Claude (per `preferredNotifChannel`), Codex (`osc9` method incl. approval-requested), Amp (BEL/SSH), Pi (extension) | Out-of-band, works from alt-screen; semantics = "done OR needs attention" — arbitration decides which. tmux swallows without passthrough — irrelevant when the capsule is the terminal |
| OSC 9;4 progress (ConEmu) | Claude emits indeterminate while working | States: 0 clear, 1 set, 2 error, 3 indeterminate, 4 paused. Clear edge = done-ish hint; active ≠ working (animates during approvals). Reference: <a href="https://rockorager.dev/misc/osc-9-4-progress-bars/">rockorager.dev OSC 9;4</a>, <a href="https://ghostty.org/docs/vt/osc/conemu">Ghostty ConEmu OSC</a> |
| Title OSC 0/2 | Codex: documented spinner/status items, default on. Claude: title-setting mechanism unclear in docs — log OSC traffic from a live container pane before relying on it | Title is a shared mutable resource; attribute to foreground agent, wipe on change |
| BEL | Claude (`terminal_bell`), Amp (SSH), others | Coarse attention edge; attribute via process tree |
| DECSET toggles (1049 alt-screen, 2004 paste, 2026 sync, 1004 focus) | All TUIs at startup | Not state-correlated; alt-screen edges ≈ app start/stop only |

## Appendix F — `/proc` Signal Validity (Linux containers)

- CPU-jiffy deltas (`utime`+`stime` from `/proc/<pid>/stat` over a window): the robust "is it computing" primitive; threshold over a window, never a zero-test (idle TUIs run timers).
- Child-process tree under the agent PID: strong, cheap "tool executing" corroborator; absent during pure generation, so it never gates working on its own.
- Foreground pgid (tpgid): precise "agent or shell owns the pane"; says nothing about state within the agent.
- wchan-style "blocked in tty read = waiting for input": valid only for readline CLIs; every built-in jackin' runtime is an event-loop TUI sitting in `epoll`/`poll` in both states — this also rules out cursor-probe-timeout (CSI 6n) as a blocked detector. Background: <a href="https://josnyder.com/blog/2024/wchan.html">josnyder.com on wchan</a>.
- Provider TLS sockets: keep-alive persists across idle turns; presence is meaningless — skip.

## Related Files

- <RepoFile path="crates/jackin-capsule/src/agent_status.rs" /> — state model; v2 replaces its signal machine with evidence arbitration.
- <RepoFile path="crates/jackin-capsule/src/agent_status/arbitrate.rs" /> — becomes the only state path.
- <RepoFile path="crates/jackin-capsule/src/agent_status/process.rs" /> — `/proc` identity plus CPU/children sampling.
- <RepoFile path="crates/jackin-capsule/src/agent_status/hook_installer.rs" /> — container-local hook/plugin installers.
- <RepoFile path="crates/jackin-capsule/src/session.rs" /> — PTY/process source; loses its state-authoring call sites.
- <RepoFile path="crates/jackin-capsule/src/daemon.rs" /> — event loop, ticker, control handling, evidence recomputation.
- <RepoFile path="crates/jackin-protocol/src/control.rs" /> — status/event protocol surface.
- <RepoFile path="crates/jackin-capsule/src/socket.rs" /> — control socket, event subscription, wait semantics.
- <RepoFile path="crates/jackin-capsule/src/runtime_setup.rs" /> — container-local installation path.
- <RepoFile path="docker/runtime/entrypoint.sh" /> — launch scaffolding and status env.
- <RepoFile path="crates/jackin-core/src/agent.rs" /> — built-in runtime enum and slugs.
