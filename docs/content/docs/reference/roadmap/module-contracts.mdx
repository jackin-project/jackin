---
title: "Module contracts (//! docs)"
---

**Status**: Partially implemented — `//!` module-level docs now cover all production source files in `src/` and `crates/` (2026-06-02); remaining scope is `///` item-level API docs on public crate boundary functions, deferred until Stage 4 crate extraction stabilises the public surfaces.

## Problem

About 46% of source files (53 of 116) had `//!` orientation docs at original analysis. The remaining 54% gave AI agents and contributors no orientation when they open the file — purpose, scope, and key invariants must be reverse-engineered from the code.

## What shipped (2026-06-02)

All priority files from both tables below received `//!` blocks following the `env_model.rs` exemplar format (purpose + scope claim + key invariants):

| File | Status |
|---|---|
| <RepoFile path="src/runtime/launch.rs" /> | ✅ documented |
| <RepoFile path="src/app/mod.rs" /> | ✅ documented |
| <RepoFile path="src/app/context.rs" /> | ✅ documented |
| <RepoFile path="src/instance/auth.rs" /> | ✅ documented |
| <RepoFile path="src/isolation/materialize.rs" /> | ✅ documented |
| <RepoFile path="src/isolation/finalize.rs" /> | ✅ documented |
| <RepoFile path="src/config/mod.rs" /> | ✅ documented |
| <RepoFile path="src/workspace/mod.rs" /> | ✅ documented |
| <RepoFile path="src/runtime/cleanup.rs" /> | ✅ documented |
| <RepoFile path="src/derived_image.rs" /> | ✅ documented |
| <RepoFile path="src/console/tui/state.rs" /> | ✅ documented (stale path fixed) |
| <RepoFile path="src/console/tui/input/global_mounts.rs" /> | ✅ documented |
| <RepoFile path="src/manifest/validate.rs" /> | ✅ documented |

<RepoFile path="src/console/tui/input/save.rs" /> already had a `//!` block (purpose + scope); expanding to full 3-element format is a follow-up.

## Current coverage (2026-06-02)

All production source files in both `src/` and `crates/` are documented. Excluded intentionally: `main.rs`, `bin/role.rs`, `build.rs`, and `*_tests.rs` / `tests/*.rs` files (binary entry points and test modules do not need orientation headers).

Re-verify with: `grep -rL '^//!' src/ crates/ --include='*.rs' | grep -v 'build.rs\|tests/'` — should return zero results for production code.

The next orientation-doc work is `///` item-level docs on public API entry points at crate boundaries, and rustdoc examples on the stable-surface functions — but that pass waits until the crate extraction (Stage 4) stabilises the public surfaces.

## Steps

1. For each file, read the first 50 lines and the public item list.
2. Write a `//!` block at the top: purpose, scope claim, key invariants, what callers can assume.
3. Model on <RepoFile path="src/env_model.rs" /> (the 3-element exemplar: purpose + scope + history).
4. Do not add `///` item-level docs in this pass — only `//!` module-level.

## What a good `//!` block says

- **Purpose:** What does this module do?
- **Scope:** What is it NOT responsible for? (crucial for AI agents to know where not to look)
- **Key invariants:** What must always be true when this module's public functions return?

## Caveats

Purely additive — no behavior change, no CI risk. Do before the behavioral specs since the spec presupposes the module has a stated contract.
