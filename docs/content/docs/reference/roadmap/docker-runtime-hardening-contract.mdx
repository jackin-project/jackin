---
title: "Docker Runtime Hardening Contract"
---

**Status**: Partially implemented — all implementable phases shipped (PR #528). Permanently deferred by documented blockers: rootless DinD (Phase B research, cgroup v2 + compatibility matrix required), `no-new-privileges` for `standard` (blocked by base-image `NOPASSWD:ALL` sudo audit — Open Question #5), Sysbox (Phase C — host-install policy undecided).

## Problem

jackin' is intentionally Docker-first today. That choice is still correct for the current product shape: local operator-controlled agents, explicit mounts, agent state that survives reconnects, and a Docker-compatible development environment that most role authors already understand.

The weakness is not "Docker exists." The weakness is that the current jackin' Docker runtime does not yet expose a precise hardening contract. Operators see "container isolation" but do not get a structured answer to:

- which Docker security controls were requested
- which controls the selected host can actually enforce
- whether the role container, DinD sidecar, and inner containers have different risk postures
- what remains open after launch
- which profile is appropriate for trusted local work, suspicious repos, or long autonomous runs

The current model is coherent but broad:

- build the role image through the selected host Docker engine
- create a per-agent Docker network
- start a privileged `docker:dind` sidecar
- start the role container on that network
- mount only operator-approved host paths
- connect the role container to the DinD sidecar over TLS
- keep the host Docker socket out of the agent container

That is already better than a normal development container with the host Docker socket mounted. But it is not the same thing as a hardened sandbox. This roadmap item defines the Docker-side hardening path before jackin' adds any new backend family.

## Design Philosophy: Capability Grants, Not Restrictions

jackin' is intentionally a no-interruption autonomous agent environment. The agent binaries run with maximum bypass: `claude --dangerously-skip-permissions`, `codex --dangerously-bypass-approvals-and-sandbox`, `amp --dangerously-allow-all`. This is not a weakness — it is the product's reason for existing. An agent that stops to ask permission for every file write is not useful.

The Docker container IS the sandbox. The agent is free inside it. The operator controls what the container can do, not what the agent chooses to do inside the container. These are the same outcome — if the container has no outbound network, it does not matter that the agent might try to exfiltrate data, because the packet never leaves.

This shapes the entire design of the profile system:

> **Capability grants, not restrictions.** A container starts from a minimal locked baseline. The operator (or role) explicitly grants what they need. The profile is a named bundle of grants. Explicit grants can extend any profile. The result is: "this container is exactly as capable as I allowed it to be — nothing more."

This is different from the alternative approach (start permissive, optionally restrict), which has the failure mode of "I thought I restricted X but I forgot to add the flag." A grant model fails safe: if you forget to grant something, it does not work, and the operator knows immediately what to add. A restriction model fails open: if you forget to restrict something, the capability is silently available.

**Practical implications:**
- `compat` is a full-grant profile — it is what you opt into when you need everything, not the default
- `locked` is the starting point for any use case where you want tight control
- profiles are bundles of grants for common use cases; explicit grants compose with any profile
- a role declares minimum grants it requires; the operator can grant more but cannot grant less than the role needs
- the session contract output tells the operator exactly what was granted and what was not

**The agent bypass flags are not configurable** — they are immutable inside the container. The Docker boundary is the only enforceable isolation layer. This is correct: jackin' is not trying to sandbox the agent from itself; it is trying to sandbox the container from the host. The operator's trust model is "I trust the agent to do its job within the limits I set on the container."

## Thesis

The next step should be:

> Keep Docker as the default runtime, but make Docker launches managed by jackin'
> run under an explicit, auditable capability-grant contract. The container has
> exactly the capabilities it was granted — network, Docker, privilege, filesystem
> access, and resources — and no more.

This is different from:

- replacing Docker with microVMs
- claiming hardened containers equal hypervisor isolation
- forking Docker or owning a custom runtime
- silently changing host Docker, host firewall, or host config
- restricting what the agent binary itself chooses to do inside the container (it runs with full bypass; the container boundary is the enforcement layer)

Docker remains the packaging and execution substrate. jackin' becomes the grant policy layer that chooses, applies, reports, and tests a precise Docker capability posture.

## Source Material

This design depends on Docker primitives that exist today:

- [Docker rootless mode](https://docs.docker.com/engine/security/rootless/) - runs the daemon and containers inside a user namespace without daemon root privileges.
- [Docker seccomp profiles](https://docs.docker.com/engine/security/seccomp/) - Docker's default profile is an allowlist that blocks a set of high-risk syscalls while preserving broad compatibility.
- [Docker AppArmor profiles](https://docs.docker.com/engine/security/apparmor/)
  - Docker applies `docker-default` on supported Linux hosts unless overridden.
- [Docker run reference](https://docs.docker.com/reference/cli/docker/container/run/)
  - the CLI surface for capabilities, `--security-opt`, `no-new-privileges`, read-only root filesystems, tmpfs mounts, networking, and ulimits.
- [Docker resource constraints](https://docs.docker.com/engine/containers/resource_constraints/)
  - memory, CPU, swap, and related cgroup controls.
- [Docker daemon attack surface](https://docs.docker.com/engine/security/) - daemon control is privileged enough to mount host paths and create powerful containers, so jackin' must keep the host socket out of agent reach.

Existing jackin' roadmap items folded into this contract:

- [Network egress policy](/reference/roadmap/network-egress-policy/) - outbound policy and enforcement-quality reporting.
- [Declarative resource limits](/reference/roadmap/declarative-resource-limits/)
  - role and launch resource budgets.
- [Session contract and explain mode](/reference/roadmap/session-contract-explain-mode/)
  - preflight explanation of the actual boundary.
- [Process-level sandboxing](/reference/roadmap/process-level-sandboxing/) - per-operation controls inside the runtime.

Concrete Docker upstream references collected during the May 2026 research pass:

- Docker default capability set (the 14 caps granted unless `--cap-drop` overrides) is defined in `moby/moby/daemon/pkg/oci/caps/defaults.go`: `CAP_CHOWN`, `CAP_DAC_OVERRIDE`, `CAP_FSETID`, `CAP_FOWNER`, `CAP_MKNOD`, `CAP_NET_RAW`, `CAP_SETGID`, `CAP_SETUID`, `CAP_SETFCAP`, `CAP_SETPCAP`, `CAP_NET_BIND_SERVICE`, `CAP_SYS_CHROOT`, `CAP_KILL`, `CAP_AUDIT_WRITE`.
- Docker default masked `/proc` and `/sys` paths (from `moby/moby/oci/defaults.go`): masked = `/proc/asound`, `/proc/acpi`, `/proc/kcore`, `/proc/keys`, `/proc/latency_stats`, `/proc/timer_list`, `/proc/timer_stats`, `/proc/sched_debug`, `/proc/scsi`, `/sys/firmware`, `/sys/devices/virtual/powercap`; read-only = `/proc/bus`, `/proc/fs`, `/proc/irq`, `/proc/sys`, `/proc/sysrq-trigger`. `--security-opt systempaths=unconfined` lifts both lists (per `docs.docker.com/reference/cli/docker/container/run/`).
- Docker rootless storage drivers (per `docs.docker.com/engine/security/rootless/`): `overlay2` (kernel 5.11+), `fuse-overlayfs` (4.18+), `btrfs`, `vfs`. AppArmor, overlay networks, and SCTP port exposure are explicitly unsupported in rootless mode.
- `dind` image TLS posture (per Docker Hub `_/docker` image docs): from 18.09+ the dind variants auto-generate `ca/`, `server/`, `client/` cert directories in `DOCKER_TLS_CERTDIR`. No rotation/revocation tooling shipped upstream; an operator-built PKI is the only path beyond first launch.
- `docker sandbox` (Docker Sandboxes) CLI surface as of May 2026 (per `docs.docker.com/reference/cli/docker/sandbox/`): `create`, `exec`, `inspect`, `ls`, `network`, `reset`, `rm`, `run`, `save`, `stop`, `version`. No documented events stream, no `--format json`, no per-sandbox stdout/log hooks. Not yet a viable programmatic backend for jackin'.

## Non-Goals

- Do not claim hostile multi-tenant SaaS safety from Docker hardening alone.
- Do not mount the host Docker socket into agent containers.
- Do not mutate host firewall rules, host Docker daemon config, host git config, or host credential stores silently.
- Do not make every role compatible with the strictest profile on day one.
- Do not replace the future backend work tracked in [Selectable sandbox backends](/reference/roadmap/selectable-sandbox-backends/).
- Do not keep separate one-off roadmap pages for individual Docker hardening flags when they belong to this shared contract.

## Current Docker Boundary

The current Docker backend has useful properties:

| Property | Current behavior |
|---|---|
| Host file visibility | Only explicit workspace/global mounts enter the role container. |
| Mount write access | `:ro` mounts are enforced by Docker and the kernel. |
| Agent state | Agent homes and tool auth state persist separately from the role container writable layer. |
| Host Docker daemon | The role container does not receive the host Docker socket. |
| Inner Docker workflows | A per-agent DinD sidecar provides a private Docker daemon. |
| Agent separation | Each instance gets its own Docker network and state. |
| User mapping | The role process maps to the host UID/GID to avoid host-file ownership damage. |

### Current code state on `main`

A read of the launch path on the docs/docker-hardening-orbstack-backend branch (May 24, 2026) confirms what is and is not enforced today. This grounds the rest of the contract in code that exists, not flags that might exist.

| Concern | Location | Current value |
|---|---|---|
| Role container `docker run` flag assembly | <RepoFile path="src/runtime/launch.rs" /> `launch_role_runtime` fn, `run_args` vec starting at line 790 | No `--cap-add` / `--cap-drop`, no `--security-opt seccomp=`, no `--security-opt apparmor=`, no `--security-opt no-new-privileges`, no `--read-only`, no `--tmpfs`, no `--pids-limit`, no `--memory*`. Docker defaults apply implicitly. The socket-dir bind mount (`-v <socket_dir>:/jackin/run`) is appended last at line 1007–1008 immediately before the image argument. |
| DinD sidecar start | <RepoFile path="src/runtime/launch.rs" /> `dind_args` vec at lines 690–711 | `--privileged`. TLS cert dir at `DOCKER_TLS_CERTDIR=/certs` with `DOCKER_TLS_SAN=DNS:<dind-name>`. Cert volume bound per launch. No rotation. |
| Docker execution path — role container | <RepoFile path="src/runtime/launch.rs" /> line 1018 + <RepoFile path="src/docker.rs" /> `ShellRunner` | Role container is launched via **CLI subprocess** `docker run -d`. Flags are a `Vec<&str>` built in `launch_role_runtime`. The Bollard API path (`create_container` + `start_container` in <RepoFile path="src/docker_client.rs" />) is only used by tests and restore paths — not the primary production launch. All security flags must be added to both paths to stay in sync. |
| Bollard `HostConfig` for `ContainerSpec` | <RepoFile path="src/docker_client.rs" /> line 539 | `HostConfig { network_mode, binds, privileged, ..Default::default() }`. No `cap_add`, `cap_drop`, `security_opt`, `memory`, `nano_cpus`, `pids_limit`, or `read_only_rootfs`. |
| `LaunchContext` struct | <RepoFile path="src/runtime/launch.rs" /> lines 548–578 | Carries `container_name`, `image`, `network`, `dind`, `selector`, `agent_display_name`, `workspace`, `state`, `git`, `debug`, agents, capsule config, `resolved_env`, `github_env`, `paths`. No security profile field. |
| `LoadOptions` struct | <RepoFile path="src/runtime/launch.rs" /> lines 35–76 | Carries `debug`, `rebuild`, `force`, `op_runner`, `host_env`, `agent`, `role_branch`, `restore_container_base`, `restore_role_source_git`, `provider`. No docker profile override field. |
| `WorkspaceConfig` struct | <RepoFile path="src/workspace/mod.rs" /> lines 45–94+ | Carries `version`, `workdir`, `mounts`, `allowed_roles`, `default_role`, `default_agent`, `last_role`, `env`, `roles`, `keep_awake`, `claude`, `codex`, `amp`, `kimi`, `opencode`. No docker security field. |
| `DockerConfig` in `AppConfig` | <RepoFile path="src/config/mod.rs" /> `DockerConfig` struct | Only carries `mounts: DockerMounts`. No default profile, no resource defaults, no security options. |
| `RoleManifest` struct | <RepoFile path="src/manifest/mod.rs" /> lines 11–43 | Fields: `version`, `dockerfile`, `published_image`, `identity`, `agents`, `claude`, `codex`, `amp`, `kimi`, `opencode`, `hooks`, `env`. No `docker`, `min_profile`, or `requires_inner_engine` field. |
| Per-instance metadata struct | <RepoFile path="src/instance/manifest.rs" /> `DockerResources` at lines 88–93 | `{ role_container, dind_container, network, certs_volume }`. Pure name record for cleanup/restore. No active profile recorded. |
| Network creation | <RepoFile path="src/docker_client.rs" /> `create_network` at line 576 | `NetworkCreateRequest { name, labels, ..Default::default() }`. No `internal: true`, no subnet isolation. External egress is open by default. |
| Container user | <RepoFile path="docker/construct/Dockerfile" /> line 113 | `useradd -m -u "$UID" -g "$GID" -s /bin/zsh agent` + `echo "agent ALL=(ALL) NOPASSWD:ALL" > /etc/sudoers.d/agent`. The `agent` user has unconditional passwordless `sudo`. No `--user` flag on `docker run` — image's `USER agent` directive governs. **`NOPASSWD:ALL` sudo is incompatible with `--security-opt no-new-privileges`** (sudo needs setuid root to escalate; `no-new-privileges` prevents that escalation). Any role using `sudo` must stay on `compat` or replace `sudo` calls with file capabilities on specific binaries in the role Dockerfile. |
| Operator env / `op://` resolution | <RepoFile path="src/operator_env.rs" /> | `resolve_operator_env` runs at launch only; resolved values become `-e` flags. No re-resolution on `hardline` or agent restart. |
| Telemetry hook | <RepoFile path="src/tui/mod.rs" /> lines 172–179 | `debug_log!(category, …)` macro, gated on `DEBUG_MODE: AtomicBool` via `is_debug_mode()`. Format + allocation skipped entirely when off. No separate `clog!` macro yet; the two-tier model in AGENTS.md is met through `debug_log!` alone. New profile-related telemetry emits via `debug_log!("launch", …)`. |
| Backend selection CLI | (none) | No `--docker-profile` flag exists. The `clap` definition (`LoadArgs` struct) lives in <RepoFile path="src/cli/role.rs" />; that is where `--docker-profile <PROFILE>` will be added. |
| Schema versions | <RepoFile path="src/config/migrations.rs" /> lines 7–8, <RepoFile path="src/manifest/migrations.rs" /> line 6 | `CURRENT_CONFIG_VERSION = "v1alpha5"`, `CURRENT_WORKSPACE_VERSION = "v1alpha5"`, `CURRENT_MANIFEST_VERSION = "v1alpha4"`. |

The pattern is consistent: jackin' inherits Docker defaults end-to-end. That is the starting baseline for every profile below — `compat` is "what already runs".

The gaps:

| Gap | Why it matters | Practical severity |
|---|---|---|
| Privileged DinD | The sidecar still needs broad privileges today. A DinD compromise has higher blast radius than the role container alone. **While DinD is privileged, capability drops on the role container are largely circumventable** — the agent can run `docker run --privileged` against DinD to regain any dropped capability. | High — structural |
| Agent launch bypasses | Every agent binary is launched with its maximum-bypass flag: `claude --dangerously-skip-permissions`, `codex --dangerously-bypass-approvals-and-sandbox`, `amp --dangerously-allow-all`, `kimi --yolo`. This is intentional design (jackin' is a no-interruption environment), but it means the agent process itself does not enforce its own sandboxing. The Docker boundary is the only enforceable line. | High — by design |
| `NOPASSWD:ALL` sudo | The `agent` user has unconditional passwordless sudo (Dockerfile line 113). This is incompatible with `--security-opt no-new-privileges`. Until every `sudo` call site is replaced with file capabilities, `standard` profile cannot harden the privilege-escalation surface. | High — blocks Phase 2 |
| No named security profile | Operators cannot choose "maximum compatibility" versus "harden this launch." | Medium |
| No capability policy | Docker defaults are implicit. jackin' does not yet drop capabilities according to role needs. Drops only matter under `hardened` with DinD disabled. | Low-Medium |
| No read-only root contract | The role image filesystem is currently mutable during the session. Read-only root is incompatible with `apt install` at runtime (package extraction writes to `/usr/lib/`, `/usr/bin/`, etc., which cannot be tmpfs). Real only for purpose-built pre-installed-everything images. | Medium for restricted roles |
| No resource budget by default | Parallel agents can exhaust CPU, memory, PID count, or file descriptors. This is the most universally useful hardening item — no compatibility issues with any workflow. | High — easy win |
| Open egress by default | Agents can exfiltrate anything they can read from mounted paths or credentials. With DinD active, inner containers also have egress. | High for sensitive repos |
| Mixed enforcement quality | Docker Desktop, OrbStack, Linux Docker, rootless Docker, and remote Docker differ. The launch output does not yet call those differences out. | Medium |

## User Perspective and Practical Impact

This section evaluates each hardening control from the operator's actual usage pattern and answers the question: what does this give a real user and when?

### The baseline user and their workflow

jackin' operators are developers who run AI coding agents against their local or worktree-isolated repositories. The daily workflow is `jackin console` → pick workspace → pick role → agent launches and works autonomously in the background. The operator does not supervise every command. This is by design: jackin' is deliberately a no-interruption, full-permission environment. The agents run with `--dangerously-skip-permissions` / `--dangerously-bypass-approvals-and-sandbox` / `--dangerously-allow-all` at the entrypoint level — these are not flags operators can override per-session. The agents have full shell access plus Docker access via DinD.

**The threat the operator actually cares about** is: "I left an AI agent running against my codebase overnight. What is it allowed to touch that it should not be?" The answer today is: anything in mounted paths, any outbound network, anything in the DinD Docker daemon. The operator wants to reduce that surface without abandoning the reason they use jackin' (autonomous, fast, effective agents).

### Per-control user impact

**Resource limits — `standard` profile, Phase 2**

Impact: universally useful. Zero compatibility breakage. Prevents one agent from exhausting host memory, CPU, or PID table and degrading other containers or the host machine. The operator running three or four agents in parallel against different repos today has no protection against one agent hitting a runaway loop. A declared `memory = "8G"` + `pids = 2048` prevents OOM cascades and fork bombs. This is the item with the best effort-to-impact ratio.

**Host Docker socket guard — Phase 1**

Impact: regression test prevents the single most dangerous mistake. The hard rule already exists; the test just makes it machine-enforceable. If a future change accidentally mounts `/var/run/docker.sock` into the role container, the test fails before merge. No user-visible change; pure safety net.

**DinD TLS cert lifecycle — Phase A**

Impact: makes the current security posture auditable. The certs already die with the launch (per-launch generation today, confirmed in code). Emitting `debug_log!("dind", "TLS certs generated, …")` + storing under `/jackin/run/<instance>/dind-certs/` makes this visible and testable. Low effort, real signal for debugging auth issues.

**DinD-free roles — Phase D / `hardened` profile**

Impact: the highest-value item in the entire contract for roles that genuinely do not need Docker. A code review role, a documentation agent, or a read-only analysis role does not need DinD. Skipping the sidecar eliminates the largest blast-radius component: the privileged container that can reach the host kernel on Linux. Without DinD, capability drops on the role container become genuinely meaningful rather than circumventable.

**Who does this help?** Role authors who build purpose-specific restricted roles. The two current built-in roles (`agent-smith` and `the-architect`) both need DinD for Testcontainers, Docker Compose, and inner build workflows. A future `reviewer` or `researcher` role that only reads code and calls the model API does not. The `dind = "none"` manifest field is what enables that use case.

**`no-new-privileges` — Phase 2, blocked by sudo audit**

Impact when unblocked: prevents the agent from gaining new privileges through setuid/setgid binaries or file capabilities beyond what the container was started with. Marginal benefit while DinD is active (agent can `docker run --privileged` to regain capabilities). Real benefit under `hardened` with DinD disabled. Currently blocked by `NOPASSWD:ALL` sudo in the base image — enabling it today would silently break `apt install`, tool scripts that call `sudo`, and `gh auth` operations that need elevated file access.

**Capability drops — Phase 4, `hardened` profile only**

Impact: only meaningful under `hardened` + DinD disabled. Under `compat`/`standard` with DinD active, a determined agent can run `docker run --privileged` against the DinD daemon and regain any dropped cap inside that inner container. The 8-cap minimum set (`CHOWN`, `DAC_OVERRIDE`, `FOWNER`, `FSETID`, `SETUID`, `SETGID`, `SETFCAP`, `KILL`) is a good starting hypothesis for roles that do not need `MKNOD` (device nodes), `NET_RAW` (raw sockets/ping), `SYS_CHROOT` (chroot calls), or `AUDIT_WRITE` (kernel audit log). Dropping `NET_RAW` is safe for most coding workflows since modern kernels allow ICMP via `ping_group_range` without it.

**Read-only root — Phase 4, `hardened` profile only**

Impact: eliminates the class of attacks where the agent modifies its own runtime binaries or system libraries mid-session. But it is incompatible with `apt install` at runtime: `dpkg` extracts package files to `/usr/bin/`, `/usr/lib/`, `/usr/share/`, etc., which are image-layer paths and cannot be tmpfs without losing the pre-installed binary tree. The tmpfs preset covers `/var/cache/apt/archives/` (download cache) and `/var/lib/dpkg/` (package database), so `apt-get update` works and `apt-get download` works, but `apt install` (extraction step) fails on a read-only root.

**Who does this help?** Roles that are fully pre-built: everything the agent needs is installed in the image at `docker build` time, and the agent never runs `apt install` during a session. For those roles, `--read-only` is meaningful. For the current built-in roles that call `apt install` in hooks or during tasks, `hardened` is incompatible. This is acceptable — `hardened` is an opt-in, and the role's `min_profile = "standard"` declares incompatibility cleanly.

**Network egress — Phase 5**

Impact: high, but the hardest to get right. Blocking outbound connections prevents credential exfiltration over the network — the most realistic post-compromise attack. The complication is DinD: a role container whose egress is blocked can still create inner containers via DinD with unrestricted networking unless the DinD daemon itself is also egress-limited. Any implementation that only filters the role container must explicitly report `partial` enforcement to avoid giving operators false confidence. Under `locked` with DinD disabled, a network deny policy is fully enforceable at the Docker bridge layer.

**`locked` profile — when is it useful?**

The `locked` profile as designed (no DinD, no writable mounts, read-only root, network deny except agent API, no persistent auth) is only usable for a narrow but real class of roles: **pure read-only analysis agents**. An agent that reads code from a `:ro` mount, calls its model API, and outputs analysis (as terminal text or by writing to a single explicitly-writable output path) fits `locked`. Examples: a PR reviewer that reads a diff and posts a comment via the `gh` CLI (but `gh` needs auth, which needs persistent state — so `locked` would need an explicit auth exception), a code quality scanner that reads source and emits a report.

In practice: `locked` is too restrictive for any current built-in role and for most user-authored roles today. Its value is as a future target for specialized read-only roles. Document it clearly so role authors understand what they're designing toward.

**The honest one-sentence summary for users:** The most impactful near-term items are (1) resource limits on `standard`, and (2) `dind = "none"` enabling DinD-free roles under `hardened`. Everything else is either blocked by the sudo audit, only relevant for purpose-built restricted roles, or largely theater while DinD remains privileged. The hardening contract makes sense structurally but its Phase 2 deliverable (resource limits) is what most operators will actually experience; Phases 3–5 are for specialized roles.

## Capability Taxonomy

Six independent capability dimensions define the full grant space. Each dimension has discrete tiers. A profile bundles a choice across all six. Explicit grants (`[docker.grants]`) extend any profile on specific dimensions.

### Dimension 1 — Network

TOML key: `network`. Values: `"none"` | `"allowlist"` | `"open"`.

| Tier | Docker mechanism | What the agent can do |
|---|---|---|
| `none` | `--network none` | No IP at all. Only Unix sockets within the container. Agent cannot reach its model API. Only viable for purely offline file-analysis roles where the API token is irrelevant. |
| `allowlist` | In-container `iptables`/`ipset` OUTPUT allowlist, seeded from `JACKIN_ALLOWED_HOSTS` env var assembled at launch. Requires `CAP_NET_ADMIN` + `CAP_NET_RAW` — both **implicitly granted** when `network = "allowlist"` is selected, even under profiles that otherwise drop those caps. | Agent can reach only the explicitly listed hosts (domains and/or IPs). Cannot exfiltrate to arbitrary URLs. Enforcement degrades to `open` with a session-contract warning if the iptables implementation is unavailable on the host. |
| `open` | Default bridge network with external route | Full outbound internet. No restrictions. |

**`allowlist` implementation — in-container iptables** (see also [Devcontainer Parity](/reference/roadmap/devcontainer-parity/) for the Anthropic reference):

```sh
#!/bin/bash
# /jackin/runtime/init-firewall.sh
# Requires: CAP_NET_ADMIN + CAP_NET_RAW, iptables, ipset, dig
set -euo pipefail

ipset create allowed-hosts hash:net maxelem 65536

for entry in $(echo "$JACKIN_ALLOWED_HOSTS" | tr ',' '\n'); do
    # If entry looks like a CIDR (contains digits and dots/slashes), add directly.
    # Otherwise treat as domain name and resolve to IPs.
    if echo "$entry" | grep -qE '^[0-9]'; then
        ipset add allowed-hosts "$entry" 2>/dev/null || true
    else
        for ip in $(dig +short "$entry" | grep -E '^[0-9]'); do
            ipset add allowed-hosts "$ip" 2>/dev/null || true
        done
    fi
done

iptables -P OUTPUT DROP
iptables -A OUTPUT -o lo -j ACCEPT
iptables -A OUTPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
iptables -A OUTPUT -p udp --dport 53 -j ACCEPT
iptables -A OUTPUT -m set --match-set allowed-hosts dst -j ACCEPT
```

**`JACKIN_ALLOWED_HOSTS`** is assembled by the host CLI at launch from (merged, deduplicated):
1. Auto-detected agent API endpoints for every agent configured in the launch (Claude → `api.anthropic.com` or `ANTHROPIC_BASE_URL` hostname; Codex → OpenAI API base; Amp → `ampcode.com`; Kimi → Kimi service endpoints; OpenCode → configured provider)
2. `github.com` + `api.github.com` when gh auth is forwarded
3. Operator `[docker.grants] allowed_hosts = [...]` in config.toml or workspace
4. Role `[network] allowed_hosts = [...]` in `jackin.role.toml`

The result is injected as a comma-separated env var. jackin' assembles the list; the script enforces it. Unlike the Anthropic devcontainer (hard-coded Anthropic-specific list), jackin' adapts the list to which agents are running and what the operator/role declares.

**Allowlist + sudo interaction:** when `network = "allowlist"` is active, `init-firewall.sh` runs with `agent_scoped_sudo` (sudo for `/jackin/runtime/init-firewall.sh` only). If `sudo = true` is also granted, the agent can run `sudo iptables -F` and undo the firewall — session contract reports `enforcement: partial`. If `sudo = false` and `user = "agent"`, the agent has no path to iptables — session contract reports `enforcement: full`. **Full enforcement requires `sudo = false` and `user = "agent"`.**

**DinD interaction:** inner containers spawned via DinD have their own network path and bypass the role container's iptables rules entirely. Session contract reports `enforcement: partial (dind active)` whenever `dind != "none"` and `network = "allowlist"`. Full enforcement requires both `network = "allowlist"` and `dind = "none"`.

### Dimension 2 — Docker in Docker (DinD)

TOML key: `dind`. Values: `"none"` | `"rootless"` | `"privileged"`.

| Tier | DinD sidecar | `--privileged`? | `DOCKER_HOST` injected? | What the agent can do |
|---|---|---|---|---|
| `none` | Not started | — | No | No `docker` CLI, no `docker build`, no Docker Compose, no Testcontainers. The five DinD env vars (`DOCKER_HOST`, `DOCKER_TLS_VERIFY`, `DOCKER_CERT_PATH`, `JACKIN_DIND_HOSTNAME`, `TESTCONTAINERS_HOST_OVERRIDE`) are NOT set. No certs volume created. |
| `rootless` | `docker:dind-rootless` | No | Yes | Docker available. Daemon runs unprivileged in a user namespace. Most Compose/build workflows work. Kernel exploits inside the sidecar do not propagate to the host as easily as privileged DinD. **Requires cgroup v2 on the host.** |
| `privileged` | `docker:dind` | Yes | Yes | Full Docker daemon. `docker run --privileged`, `docker run -v /:/host`, full kernel namespace access inside the sidecar. On Linux hosts, a privileged container escape is a kernel-level threat. On macOS (Docker Desktop/OrbStack), escape reaches the backend VM, not macOS directly. |

When `dind = "none"`, the entire DinD sidecar block in `launch_role_runtime` (lines 675–724) is skipped: no cert volume, no sidecar container, no DinD env vars on the role container. `DockerResources.dind_container` and `certs_volume` become `Option::None` in the persisted instance manifest (Phase 3 schema change).

### Dimension 3 — User and Privilege

Two orthogonal TOML keys: `user` (which user the container runs as) and `sudo` (whether that user can escalate via passwordless sudo).

#### `user`

TOML key: `user`. Type: string. Default: `"agent"`.

| Value | Docker mechanism | What it means |
|---|---|---|
| `"agent"` (default) | Image-baked `USER agent` directive; host UID/GID remapped at build time | Runs as the mapped `agent` user. Cannot access root-owned files. Standard isolation. |
| `"root"` | `--user root` on `docker run` | Full root inside the container. No `--user` flag — Docker default is root if image doesn't set `USER`, but jackin's images set `USER agent`; `--user root` overrides it. |
| any other string | `--user <value>` on `docker run` | Runs as the named user if it exists in the image. Useful for images with a purpose-built non-root user other than `agent`. |

**Validation:** the combination `user = "root"` + `sudo = true` is a launch error — `"grants.user = \"root\" and grants.sudo = true are mutually exclusive: root does not need sudo escalation; remove one of the two grants"`. Root already has every privilege; adding a sudo binary on top is redundant and confusing.

#### `sudo`

TOML key: `sudo`. Type: bool. Default: depends on profile (see profile table below).

| Value | Sudoers entry in image | `--security-opt no-new-privileges` |
|---|---|---|
| `false` | None (requires base image to not carry `NOPASSWD:ALL`) | Applied — no setuid escalation possible |
| `true` | `agent ALL=(ALL) NOPASSWD:ALL` (current Dockerfile line 113) | Not applied |

**Current state:** the base image unconditionally sets `NOPASSWD:ALL` (Dockerfile line 113). Enforcing `sudo = false` requires the base image to change. This is the sudo audit blocker (Open Question #5). Until resolved, `sudo = false` is accepted in the schema and recorded in the session contract but cannot actually be enforced — `no-new-privileges` cannot be added while the sudoers entry is present. The session contract reports `sudo: schema=false, enforced=no, reason=base-image-audit-pending`.

**`network = "allowlist"` implicit interaction:** when the allowlist network tier is active, `init-firewall.sh` needs `sudo` for `iptables` calls. jackin' handles this via `agent_scoped_sudo` — a sudoers entry scoped to `/jackin/runtime/init-firewall.sh` only, independent of the `sudo` grant. This means `network = "allowlist"` + `sudo = false` works correctly: firewall init runs with the scoped entry, agent sessions do not get NOPASSWD:ALL.

### Dimension 4 — Filesystem

TOML key: `system_writes`. Type: bool. Default: depends on profile.

| Value | Docker mechanism | What the agent can do |
|---|---|---|
| `false` | `--read-only` + tmpfs preset (see Read-Only Root Tmpfs Preset section) | Cannot write to image-layer paths (`/usr/bin/`, `/usr/lib/`, `/etc/`, etc.). `apt install` fails at the extraction step. All bind-mounted paths (workspace mounts, `/jackin/run`, `/jackin/state`, agent home dirs) remain writable regardless — `--read-only` applies only to the overlay image layer. |
| `true` | No `--read-only` flag | Full read-write container root. `apt install`, system library modification, writing anywhere. |

Workspace mounts have their own per-mount `readonly` flag (existing `MountConfig.readonly` field) independent of this dimension. Setting `system_writes = false` does not force workspace mounts to be read-only.

### Dimension 5 — Resources

Five independent fields. All are optional — omitting any field means no limit for that resource. Unlimited is expressed by absence, not by a special value.

| TOML key | Docker flag | Type | Purpose |
|---|---|---|---|
| `memory` | `--memory` | string (`"4G"`, `"512M"`) | Hard ceiling. Container is OOM-killed if exceeded. |
| `memory_reservation` | `--memory-reservation` | string | Soft ceiling. Triggers throttling before OOM-kill. Should be ≤ `memory`. |
| `cpus` | `--cpus` | float | CPU share quota. `4.0` = 4 full CPU cores. |
| `pids` | `--pids-limit` | integer | Maximum number of processes. Prevents fork bombs and limits DinD inner container spawning. |
| `nofile` | `--ulimit nofile=N:N` | integer | Open file descriptor limit. Applies to both soft and hard limit. |

**Naming rationale:** field names match Docker's flag names (`--memory`, `--memory-reservation`, `--cpus`) to minimize surprise for operators who already know Docker. `pids` is a shorthand for `--pids-limit`; `nofile` maps to `--ulimit nofile`.

**Validation:** `memory_reservation` must be ≤ `memory` when both are set — launch error if violated. Size strings are parsed at launch time; invalid values (e.g. `"16 gigabytes"`) produce a clear parse error before any container is started.

### Dimension 6 — Linux Capabilities

Two parts: the **profile base set** (determined by the profile, applied via `--cap-drop ALL --cap-add ...`) and **explicit additions** via `capabilities_add`.

TOML key: `capabilities_add`. Type: array of strings. Each string is a Linux capability name without the `CAP_` prefix (case-insensitive; jackin' normalizes to uppercase).

**Validation:** every entry in `capabilities_add` must be a recognized Linux capability name. Unknown names produce a launch error: `"unknown Linux capability \"CAP_MAGIC\" in grants.capabilities_add; valid capabilities: AUDIT_CONTROL, AUDIT_READ, AUDIT_WRITE, BPF, BLOCK_SUSPEND, CHECKPOINT_RESTORE, CHOWN, DAC_OVERRIDE, DAC_READ_SEARCH, FOWNER, FSETID, IPC_LOCK, IPC_OWNER, KILL, LEASE, LINUX_IMMUTABLE, MAC_ADMIN, MAC_OVERRIDE, MKNOD, NET_ADMIN, NET_BIND_SERVICE, NET_BROADCAST, NET_RAW, PERFMON, SETFCAP, SETGID, SETPCAP, SETUID, SYS_ADMIN, SYS_BOOT, SYS_CHROOT, SYS_MODULE, SYS_NICE, SYS_PACCT, SYS_PTRACE, SYS_RAWIO, SYS_RESOURCE, SYS_TIME, SYS_TTY_CONFIG, WAKE_ALARM"`.

**Profile base cap sets:**
- `compat` / `standard`: Docker 14-cap default (no explicit drop). `--cap-drop` and `--cap-add` flags NOT emitted; Docker applies its defaults.
- `hardened` / `locked`: `--cap-drop=ALL` followed by `--cap-add` for each cap in the 8-cap minimum set.

**8-cap minimum set** (hardened/locked base):

| Cap | Why kept |
|---|---|
| `CHOWN` | `apt`, `tar`, package install change file ownership |
| `DAC_OVERRIDE` | `apt`, `git`, `cargo`, `npm` bypass filesystem permission checks |
| `FOWNER` | `tar`, `unzip` bypass ownership-match checks |
| `FSETID` | `apt`, `dpkg` preserve setuid/setgid bits on installed binaries |
| `SETUID` | `su`, `gosu`, dropping privileges after startup |
| `SETGID` | Group operations, dropping privileges |
| `SETFCAP` | `dpkg`/`apt` set file capabilities on installed binaries |
| `KILL` | Shells, process supervisors, `bash -c "cmd & wait"` |

**6 caps dropped from Docker default under hardened/locked:**

| Cap dropped | Reason |
|---|---|
| `MKNOD` | Role containers do not create device nodes |
| `NET_RAW` | Raw socket access; modern kernels allow ICMP via `ping_group_range` without it. Re-add via `capabilities_add = ["NET_RAW"]` for `ping`/`tcpdump`/`nmap` roles. |
| `SETPCAP` | Allows modifying other processes' capability sets; not needed once caps are set at exec |
| `NET_BIND_SERVICE` | Binding ports < 1024; role containers should use unprivileged ports |
| `SYS_CHROOT` | `chroot(2)` calls; not needed for normal coding workflows |
| `AUDIT_WRITE` | Writing to kernel audit log; reduces audit-stream noise |

**`capabilities_add` is additive on top of the profile's base set** — it cannot remove caps from the profile's base set. To get a cap that the profile drops, add it via `capabilities_add`; to prevent a cap from the default set, choose a stricter profile.

**`CAP_NET_ADMIN` is implicitly added** when `network = "allowlist"` is active (required for iptables). It is NOT in `capabilities_add` — it is a side effect of the network grant, reported separately in the session contract.

**Capability interactions with DinD:** under `compat`/`standard` with DinD active, capability drops on the role container are largely circumventable — the agent can run `docker run --privileged` against the DinD daemon and regain any dropped cap inside an inner container. Capability enforcement is only substantively meaningful under `dind = "none"`.

#### Complete Linux capabilities reference

All capabilities available for `capabilities_add`:

**File system:**

| Cap | What it enables | Common use |
|---|---|---|
| `CHOWN` | Change file UID/GID | `apt`, `tar`, package install |
| `DAC_OVERRIDE` | Bypass read/write/exec permission checks | `apt`, `git`, `cargo`, `npm` |
| `DAC_READ_SEARCH` | Bypass read perm + directory search checks | `find` on restricted directories |
| `FOWNER` | Bypass perm checks when file UID doesn't match process | `tar`, `unzip` |
| `FSETID` | Don't clear SUID/SGID on file modification | `apt`, `dpkg` |
| `MKNOD` | Create device nodes via `mknod(2)` | Rarely needed in containers |
| `SYS_CHROOT` | Use `chroot(2)` | Build tools that chroot |
| `LINUX_IMMUTABLE` | Set immutable/append-only inode flags | Edge cases |
| `LEASE` | Establish leases on arbitrary files | File locking edge cases |

**Process and signals:**

| Cap | What it enables | Common use |
|---|---|---|
| `KILL` | Send signals to any process | Shells, supervisors |
| `SETUID` | Change process UID | `su`, `gosu`, privilege dropping |
| `SETGID` | Change process GID | Group operations |
| `SETPCAP` | Modify bounding capability set of other processes | Usually not needed post-exec |
| `SETFCAP` | Set file capabilities on binaries | `dpkg`/`apt` for setcap binaries |
| `SYS_PTRACE` | `ptrace(2)` arbitrary processes | Debuggers: `gdb`, `strace`, `lldb` |
| `SYS_NICE` | Raise nice value; renice other processes | Build tools adjusting priority |
| `SYS_RESOURCE` | Override resource limits | Rarely needed |
| `SYS_ADMIN` | Mount, namespaces, swapon, keyctl, and ~30 more | DinD-style nested containers; very broad — avoid |

**Network:**

| Cap | What it enables | Common use |
|---|---|---|
| `NET_ADMIN` | Routing, iptables, network interfaces, TUN/TAP | `init-firewall.sh`, VPN tools — **implicitly granted by `network = "allowlist"`** |
| `NET_RAW` | RAW/PACKET sockets, transparent proxying | `ping`, `tcpdump`, `nmap`, `wireshark` |
| `NET_BIND_SERVICE` | Bind ports < 1024 | Web servers on port 80/443 inside container |
| `NET_BROADCAST` | Socket broadcast, multicast listen | Multicast applications |

**Kernel / system:**

| Cap | What it enables | Common use |
|---|---|---|
| `SYS_MODULE` | Load/unload kernel modules | Almost never in containers |
| `SYS_BOOT` | `reboot(2)`, `kexec_load(2)` | Never in containers |
| `SYS_TIME` | Set system clock | CI systems needing time sync |
| `SYS_RAWIO` | I/O port operations (`iopl`, `ioperm`) | Hardware-level access |
| `SYS_TTY_CONFIG` | `vhangup(2)`, privileged TTY ioctls | Terminal multiplexers (rare) |
| `SYS_PACCT` | Process accounting `acct(2)` | Monitoring agents |

**Audit / security:**

| Cap | What it enables | Common use |
|---|---|---|
| `AUDIT_WRITE` | Write to kernel audit log | Audit daemons |
| `AUDIT_READ` | Read kernel audit log | Security monitoring |
| `AUDIT_CONTROL` | Enable/disable kernel auditing | Audit configuration |
| `MAC_ADMIN` | MAC (Smack) configuration | SELinux/AppArmor admin |
| `MAC_OVERRIDE` | Override MAC policy | Bypass Smack |

**Memory / power / performance:**

| Cap | What it enables | Common use |
|---|---|---|
| `IPC_LOCK` | Lock memory (`mlock`, `mlockall`) | Databases, crypto wallets |
| `IPC_OWNER` | Bypass perm checks on SysV IPC objects | Old-style shared memory |
| `BLOCK_SUSPEND` | Block system suspend | Power management agents |
| `WAKE_ALARM` | Set alarm clocks that wake the system | Scheduled task agents |
| `BPF` | Privileged BPF operations | eBPF observability tools |
| `PERFMON` | Performance monitoring | `perf`, profiling tools |
| `CHECKPOINT_RESTORE` | CRIU checkpoint/restore | Container live migration |

## Explicit Capability Grants

Profiles are bundles for common cases. Explicit grants (`[docker.grants]`) let operators extend any profile on specific dimensions without choosing a more permissive profile for everything.

### Complete `[docker.grants]` field reference

```toml
# ~/.config/jackin/config.toml  (global, applies to every launch)
[docker]
default_profile = "hardened"

[docker.grants]
# ── Network dimension ──────────────────────────────────────────────────────
network = "allowlist"          # "none" | "allowlist" | "open"
allowed_hosts = [              # merged with role-declared list + auto agent endpoints
    "registry.npmjs.org",      # domain
    "pypi.org",
    "192.168.1.0/24",          # IPv4 CIDR
    "*.cdn.example.com",       # wildcard subdomain
    "example.com:8080",        # domain with port
]

# ── Docker in Docker dimension ─────────────────────────────────────────────
dind = "none"                  # "none" | "rootless" | "privileged"

# ── User and privilege dimension ───────────────────────────────────────────
user = "agent"                 # "agent" (default) | "root" | any image username
sudo = false                   # NOPASSWD:ALL sudo; ERROR if user = "root"

# ── Filesystem dimension ───────────────────────────────────────────────────
system_writes = false          # true = writable root fs; false = --read-only + tmpfs

# ── Resource dimension ─────────────────────────────────────────────────────
memory = "16G"                 # --memory (hard limit); omit = unlimited
memory_reservation = "12G"    # --memory-reservation (soft); omit = no soft limit
cpus = 4.0                    # --cpus; omit = unlimited
pids = 2048                   # --pids-limit; omit = unlimited
nofile = 8192                 # --ulimit nofile=N:N; omit = system default

# ── Linux capabilities dimension ───────────────────────────────────────────
capabilities_add = ["NET_RAW"] # additive on top of profile base set; validated at launch
```

```toml
# workspace config — override for one workspace
[docker]
profile = "hardened"

[docker.grants]
dind = "rootless"              # this workspace needs Testcontainers
allowed_hosts = ["registry.npmjs.org"]
memory = "32G"                 # big Rust build
```

Role-level minimum requirements (in `jackin.role.toml`):

```toml
[docker]
min_profile = "standard"           # role refuses to run under locked/hardened
dind = "rootless"                  # role needs inner Docker; "none" | "rootless" | "privileged"

[network]
allowed_hosts = ["crates.io", "static.crates.io"]  # role-declared additions to JACKIN_ALLOWED_HOSTS

[capabilities]
capabilities_add = ["SYS_PTRACE"]  # role requires this cap beyond profile base set
```

### Grant validation rules

These are enforced at launch time before any container is started. Violations produce clear, actionable errors — they are never silently ignored or downgraded.

| Rule | Error message |
|---|---|
| `user = "root"` + `sudo = true` | `"grants.user = \"root\" and grants.sudo = true are mutually exclusive: root does not need sudo; remove one"` |
| Unknown capability in `capabilities_add` | `"unknown Linux capability \"X\" in grants.capabilities_add — valid values: AUDIT_CONTROL, AUDIT_READ, …"` |
| `memory_reservation` > `memory` (when both set) | `"grants.memory_reservation (\"14G\") must be ≤ grants.memory (\"12G\")"` |
| Unparseable size string | `"cannot parse \"16 gigabytes\" as a memory size — use format \"512M\", \"4G\", \"32G\""` |
| Profile conflict with role `min_profile` | `"role declares min_profile = \"standard\"; active profile \"hardened\" is below that minimum — add grants to satisfy the role's requirements or choose a higher profile"` |
| `network = "allowlist"` on host where iptables unavailable | Launch proceeds with `enforcement: degraded` in session contract and a visible warning; does not hard-fail (operator can decide) |
| `dind = "rootless"` on cgroup v1 host | `"dind = \"rootless\" requires cgroup v2; this host reports cgroup v1 — use dind = \"privileged\" or upgrade the host kernel"` |

### Grant composition rule

Effective grant for each dimension = max(profile default, explicit grant). "Max" means more capable:

```
network:   none < allowlist < open
dind:      none < rootless < privileged
user:      "agent" < "root"  (for capability ordering; custom usernames are opaque)
sudo:      false < true
system_writes: false < true
resources: lower limits < higher limits < no limit
caps:      minimum_set ⊂ default_set ⊂ default_set ∪ capabilities_add
```

Grants can only elevate a dimension. There is no grant that restricts below the profile's baseline — to get a lower tier, choose a stricter profile.

The session contract output shows each dimension's effective value and its source (`profile`, `workspace_grant`, `cli_grant`, `implicit_dependency`).

**The jackin' philosophy:** Anthropic's devcontainer hard-codes one fixed allowlist for one use case. jackin' ships the same mechanism (iptables, ipset, scoped sudo) but makes every component configurable. The operator who wants npm access adds `registry.npmjs.org` to `allowed_hosts`; the operator who wants strictly model-API-only removes even `github.com`. Nobody is forced into one experience — the tool adapts to what the operator needs.

## Codebase Map

Every file and location that must be touched to implement this contract, organized by what each file owns. Implementors should read these files before writing code.

### Primary implementation sites

**<RepoFile path="src/runtime/launch.rs" />** — The entire launch orchestration. This is where profile-aware flags are injected.

- `LoadOptions` struct (lines 35–76): add `docker_profile: Option<DockerSecurityProfile>` for CLI override.
- `LaunchContext` struct (lines 548–578): add `profile: DockerSecurityProfile` field so `launch_role_runtime` reads it without needing a new parameter.
- `launch_role_runtime` fn (line 622): the only place where `run_args` for the role container and `dind_args` for the sidecar are assembled. All capability drops, seccomp options, `no-new-privileges`, `--read-only`, `--tmpfs`, resource limits, and DinD skipping happen here.
  - DinD args vec: lines 690–711. The `--privileged` flag is at line 697. Profile gating (skip DinD under `hardened`/`locked` when `requires_inner_engine = false`) applies here.
  - Role container `run_args` vec: starts at line 790. Security flags insert between the base flags block (line 809) and the first `-e` block (line 815). Resource limits insert before the image argument (line 1013).
  - Socket-dir bind mount: line 1007–1008 appends `-v <socket_dir>:/jackin/run`. This is already a bind mount (not tmpfs) — the writable-path tmpfs preset must **not** include `/jackin/run` since the bind mount already provides write access.
- Profile precedence resolution: should live in a helper called from `load_role_with`, reading CLI > workspace > role `min_profile` > global default.

**<RepoFile path="src/docker_client.rs" />** — Bollard API wrapper. Must stay in sync with the CLI path.

- `ContainerSpec` struct (lines 85–96): add security fields — `cap_add: Vec<String>`, `cap_drop: Vec<String>`, `security_opt: Vec<String>`, `memory: Option<i64>`, `nano_cpus: Option<i64>`, `pids_limit: Option<i64>`, `read_only_rootfs: Option<bool>`, `tmpfs: Option<HashMap<String, String>>`.
- `create_container` impl (line 526), `HostConfig` construction (line 539): wire all new `ContainerSpec` fields into `HostConfig`.
- `create_network` (line 576): add `internal: Option<bool>` to the `create_network` API to support `locked` network isolation.

**<RepoFile path="src/config/mod.rs" />** — Operator-level config.

- `DockerConfig` struct: extend with `default_profile: Option<DockerSecurityProfile>` (the global fallback profile).
- New `DockerResourceDefaults` struct: default `memory`, `memory_reservation`, `cpus`, `pids`, `nofile` values for `standard`/`hardened`/`locked` profiles when a role declares no limits.
- `AppConfig` itself does not need structural changes beyond the `DockerConfig` extension.

**<RepoFile path="src/workspace/mod.rs" />** — Workspace-level config.

- `WorkspaceConfig` struct (line 45): add `docker: Option<WorkspaceDockerConfig>` where `WorkspaceDockerConfig` carries an optional `profile: Option<DockerSecurityProfile>` override and optional `resources` override.

**<RepoFile path="src/manifest/mod.rs" />** — Role manifest.

- `RoleManifest` struct (line 11): add `min_profile: Option<DockerSecurityProfile>` (roles that need sudo or privileged workflows declare `min_profile = "compat"`), and `requires_inner_engine: Option<bool>` (roles that do not need DinD declare `false` to opt out of the sidecar under `hardened`/`locked`).

**New file: <RepoFile path="src/runtime/docker_profile.rs" />** — Houses the `DockerSecurityProfile` enum, per-dimension grant types, effective grant resolution, and all grant-to-flags translation logic.

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Default, Serialize, Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum DockerSecurityProfile {
    #[default]
    Compat,    // max capability; explicit opt-in for full-privilege workflows
    Standard,  // typical dev work; eventual default
    Hardened,  // DinD-free, read-only root, allowlist network, no sudo
    Locked,    // minimal; model API + read-only workspace only
}

// Per-dimension grant types — each dimension can be overridden independently
pub enum NetworkGrant { None, Allowlist, Open }
pub enum DindGrant { None, Rootless, Privileged }
pub enum FilesystemGrant { ReadOnly, Writable }

pub struct UserGrant {
    pub user: String,   // "agent" | "root" | custom
    pub sudo: bool,     // NOPASSWD:ALL; error if user == "root" && sudo == true
}

pub struct ResourceGrant {
    pub memory: Option<u64>,              // --memory (bytes)
    pub memory_reservation: Option<u64>, // --memory-reservation (bytes)
    pub cpus: Option<f64>,               // --cpus
    pub pids: Option<i64>,               // --pids-limit
    pub nofile: Option<u64>,             // --ulimit nofile
}

pub struct EffectiveGrants {
    pub network: NetworkGrant,
    pub allowed_hosts: Vec<String>,       // merged from all sources
    pub dind: DindGrant,
    pub user: UserGrant,
    pub filesystem: FilesystemGrant,
    pub resources: ResourceGrant,
    pub capabilities_add: Vec<String>,    // validated CAP_* additions beyond profile base
    pub no_new_privileges: bool,
}
```

`PartialOrd`/`Ord` on `DockerSecurityProfile` lets the precedence check be `max(workspace_profile, role_min_profile)`.

This module provides:
- `fn profile_base_grants(profile: DockerSecurityProfile) -> EffectiveGrants` — returns the bundle for a named profile.
- `fn apply_explicit_grants(base: EffectiveGrants, overrides: &DockerGrants) -> EffectiveGrants` — applies per-dimension overrides from `[docker.grants]` config.
- `fn resolve_effective_grants(profile, workspace_grants, role_requires, host_caps) -> Result<EffectiveGrants, GrantConflict>` — full resolution pipeline; returns `Err(GrantConflict)` if operator restricts below role minimum.
- `fn to_docker_flags(grants: &EffectiveGrants) -> Vec<String>` — emits all Docker CLI flags for `run_args`.
- `fn to_host_config(grants: &EffectiveGrants) -> HostConfig` — emits Bollard `HostConfig` for the test path.
- `fn dind_enabled(grants: &EffectiveGrants) -> bool` — whether the DinD sidecar block should run.
- `fn dind_privileged(grants: &EffectiveGrants) -> bool` — whether `--privileged` goes on the DinD args.

### Migration sites

**<RepoFile path="src/config/migrations.rs" />** — `CURRENT_CONFIG_VERSION = "v1alpha5"`, `CURRENT_WORKSPACE_VERSION = "v1alpha5"`. Both bump to `v1alpha6` when `DockerConfig.default_profile` and `WorkspaceDockerConfig` land. They land in the same PR (one bump per file kind per PR). Fixture dirs: `tests/fixtures/migrations/config/from-v1alpha5/` and `tests/fixtures/migrations/workspace/from-v1alpha5/`.

**<RepoFile path="src/manifest/migrations.rs" />** — `CURRENT_MANIFEST_VERSION = "v1alpha4"`. Bumps to `v1alpha5` when `min_profile` and `requires_inner_engine` land in `RoleManifest`. Fixture dir: `tests/fixtures/migrations/manifest/from-v1alpha4/`.

### Secondary files

**<RepoFile path="docker/construct/Dockerfile" />** — The `NOPASSWD:ALL` sudo line (line 113) is incompatible with `--security-opt no-new-privileges`. For the base image to support `standard` profile without breaking every built-in agent, either: (a) drop `NOPASSWD:ALL` and replace each sudo call site with file capabilities in the Dockerfile, or (b) keep it and require roles that need `standard` to rebuild without it. Audit the full list of `sudo` call sites in the base image before deciding. Note that the built-in agents (claude, codex, amp, kimi, opencode) themselves may call `sudo` during tool execution — that call chain must be audited before `no-new-privileges` can be the default.

**<RepoFile path="docker/runtime/entrypoint.sh" />** — Reads `JACKIN_DEBUG`, dispatches per-agent launch. No structural changes needed for Phase 1–2. Phase 4 (read-only root) may require explicit tmpfs paths to exist before `jackin-capsule runtime-setup` runs.

**<RepoFile path="src/tui/mod.rs" />** — `debug_log!` macro (lines 172–179). No changes needed; all new profile telemetry emits through this existing macro.

### Test files requiring updates

All existing tests that call `load_role` / `load_role_with` via `LoadOptions::default()` will continue to pass (default profile = `compat`, no behavior change). New tests that exercise specific profiles should pass explicit `LoadOptions { docker_profile: Some(DockerSecurityProfile::Hardened), ..Default::default() }`.

Tests in `launch.rs` that assert specific `run_args` contents (there are many — search for `run_args` or `docker run` in the test section starting at line 5200) will need updating if any default args change.

## Security Profiles

Profiles are named bundles of capability grants across the six dimensions. They exist for convenience — instead of specifying every dimension individually, an operator picks a profile that matches their intent. Explicit `[docker.grants]` extend any profile on specific dimensions.

**The ordering is ascending capability, not descending restriction.** `locked` grants the least; `compat` grants everything. Grants can only raise a dimension above the profile's default — to go lower, choose a stricter profile.

CLI and config shape:

```sh
jackin load the-architect . --docker-profile standard
jackin load reviewer .     --docker-profile hardened
jackin load researcher .   --docker-profile locked
jackin load fullstack .    --docker-profile compat
```

```toml
[docker]
default_profile = "standard"       # global default for all launches

[workspaces.my-service.docker]
profile = "hardened"               # override for this workspace only
```

### Complete profile defaults table

| Dimension | Key | `locked` | `hardened` | `standard` | `compat` |
|---|---|---|---|---|---|
| Network | `network` | `"allowlist"` | `"allowlist"` | `"open"` | `"open"` |
| Allowed hosts | `allowed_hosts` | auto agent endpoints only | auto + `github.com` | — (open) | — (open) |
| Docker in Docker | `dind` | `"none"` | `"none"` | `"rootless"`* | `"privileged"` |
| User | `user` | `"agent"` | `"agent"` | `"agent"` | `"agent"` |
| Sudo | `sudo` | `false` | `false` | `true`† | `true` |
| System writes | `system_writes` | `false` | `false` | `true` | `true` |
| Memory | `memory` | `"4G"` | `"16G"` | `"16G"` | (none) |
| Memory reservation | `memory_reservation` | `"3G"` | `"12G"` | `"12G"` | (none) |
| CPUs | `cpus` | `2.0` | `4.0` | `4.0` | (none) |
| PIDs | `pids` | `512` | `2048` | `2048` | (none) |
| Open file descriptors | `nofile` | `2048` | `8192` | `8192` | (none) |
| Capabilities base | — | 8-cap minimum | 8-cap minimum | Docker 14-cap default | Docker 14-cap default |
| `no-new-privileges` | — | enforced | enforced | goal†† | not applied |

*`standard` `dind`: `"rootless"` on cgroup v2 hosts; `"privileged"` fallback on cgroup v1 hosts with a session-contract note.

†`standard` `sudo`: `true` while the `NOPASSWD:ALL` sudo audit (Open Question #5) is unresolved. Flips to `false` once the base image drops the unconditional sudoers entry.

††`standard` `no-new-privileges`: goal but blocked by the sudo audit. Reported as `schema=true, enforced=no, reason=sudo-audit-pending` in the session contract until resolved.

### `locked` — minimal grant bundle

**Purpose:** analysis, review, investigation, or any role where the operator wants maximum confidence about what the agent can and cannot do. The agent can read code, call its model API, and write to explicitly-granted workspace paths. Everything else requires an explicit grant.

**Default grants:**
- Network: `allowlist` — only auto-detected agent API endpoints reachable. Agent cannot reach npm, PyPI, GitHub, or any other URL unless added to `allowed_hosts`.
- DinD: `none` — no Docker CLI, no `docker build`, no Compose, no Testcontainers.
- User: `agent` — runs as the mapped `agent` user. No privilege escalation.
- Sudo: `false` — no `NOPASSWD:ALL` entry; `--security-opt no-new-privileges` applied.
- System writes: `false` — `--read-only` image root. `apt install` fails at extraction.
- Resources: `memory = "4G"`, `memory_reservation = "3G"`, `cpus = 2.0`, `pids = 512`, `nofile = 2048`.
- Capabilities: 8-cap minimum set. `NET_RAW`, `MKNOD`, `SYS_CHROOT`, `SETPCAP`, `NET_BIND_SERVICE`, `AUDIT_WRITE` all dropped.

**Who uses `locked`?** A PR reviewer that reads diffs and calls the model. A code quality scanner that emits a report. An agent that reads documentation and answers questions. Any role where the guarantee is "this agent cannot change anything on my machine or reach anything beyond its model API."

**Image requirements:** everything the agent needs must be pre-installed in the role Dockerfile. No runtime package installs. Role must declare `min_profile = "locked"`.

### `hardened` — restricted DinD-disabled bundle

**Purpose:** untrusted or unfamiliar role repos, long autonomous runs, roles that do real coding work with pre-installed toolchains but do not need inner Docker. The strongest practical profile for agents that actually write code.

**Default grants:**
- Network: `allowlist` — auto agent endpoints + `github.com` + `api.github.com` (if gh auth forwarded). `git push`, PRs, and model API work. `apt`/`npm`/`pip` registries blocked unless added to `allowed_hosts`.
- DinD: `none` — no DinD sidecar by default. Roles that need Docker declare `dind = "rootless"` or `"privileged"` in their manifest.
- User: `agent` — mapped agent user. No privilege escalation.
- Sudo: `false` — no `NOPASSWD:ALL`; `no-new-privileges` applied.
- System writes: `false` — `--read-only` root + tmpfs preset. No runtime `apt install`.
- Resources: `memory = "16G"`, `memory_reservation = "12G"`, `cpus = 4.0`, `pids = 2048`, `nofile = 8192`. Enough for typical Rust/Node/Python builds.
- Capabilities: 8-cap minimum set. DinD disabled, so cap enforcement is substantive (no DinD bypass).

**Who uses `hardened`?** An agent running against a repo cloned from an unfamiliar source. A long-running autonomous agent where the operator wants to limit blast radius. Any role whose image is purpose-built and self-contained. When DinD is genuinely not needed, `hardened` provides the strongest practical isolation available while still allowing real coding work.

**Image requirements:** all toolchains pre-installed. No sudo. If the role needs Docker, add `dind = "rootless"` to its manifest (cgroup v2 required) or `"privileged"` with operator confirmation.

### `standard` — typical trusted dev work

**Purpose:** the intended eventual default for everyday coding sessions on trusted role repos. Full dev capability with resource protection. `git push`, `npm install`, `apt install`, Docker Compose, Testcontainers — all work.

**Default grants:**
- Network: `open` — full outbound. Agent can reach any URL.
- DinD: `rootless` (cgroup v2) or `privileged` (cgroup v1) — DinD for Docker-based workflows.
- User: `agent` — mapped agent user.
- Sudo: `true` (current) — `NOPASSWD:ALL`. Flips to `false` + `no-new-privileges` after the sudo audit.
- System writes: `true` — writable root. `apt install` works.
- Resources: `memory = "16G"`, `memory_reservation = "12G"`, `cpus = 4.0`, `pids = 2048`, `nofile = 8192`. **Required** on multi-agent launches — warn if absent.
- Capabilities: Docker 14-cap default. Cap drops deferred until the test matrix confirms the safe minimum for `standard`.

**Who uses `standard`?** The operator who trusts the role repo (built by their team or a built-in role) and wants full dev capability with resource protection. Intended everyday profile.

**Current blocker for making `standard` the default:** the `NOPASSWD:ALL` sudo audit (Open Question #5). `standard` becomes the default once the base image drops unconditional sudo and `no-new-privileges` can be safely applied.

### `compat` — maximum compatibility (today's behavior)

**Purpose:** roles that need everything: privileged DinD, writable system, sudo, open network, no resource limits. The "I know what I'm doing, give the container everything" profile. This is jackin's current behavior before this contract ships.

**Default grants:**
- Network: `open` — full outbound. No filtering.
- DinD: `privileged` — `docker:dind --privileged`. On Linux hosts, approximately kernel-level access inside the sidecar.
- User: `agent` — mapped agent user (but with NOPASSWD:ALL sudo).
- Sudo: `true` — `NOPASSWD:ALL` passwordless sudo.
- System writes: `true` — fully writable root filesystem.
- Resources: **none** — no `--memory`, no `--cpus`, no `--pids-limit`. Parallel agents can exhaust host.
- Capabilities: Docker 14-cap default. No drops.

**Who uses `compat`?** Roles with complex Docker workflows (Compose, Testcontainers, multi-stage builds), roles that run `apt install` during sessions, roles that need privileged operations, and any operator who wants exact parity with today's behavior. `compat` must be explicitly declared — not the intended default after `standard` is validated.

### Single-dimension grants on top of any profile

An operator does not need to switch to a more permissive profile to grant one specific capability:

```toml
[docker]
default_profile = "hardened"

# Grant only DinD on top of hardened — everything else stays at hardened defaults
[docker.grants]
dind = "rootless"

# Or: grant only more memory on top of locked
# [docker.grants]
# memory = "32G"

# Or: grant only net_raw for a debugging role
# [docker.grants]
# capabilities_add = ["NET_RAW"]
```

## Control Matrix

| Control | Docker primitive | First profile | Notes |
|---|---|---|---|
| Seccomp | `--security-opt seccomp=...` | `compat` | Docker default profile stays on unless explicitly disabled. The default is itself an allowlist that blocks high-risk syscalls while preserving compatibility. |
| AppArmor | `--security-opt apparmor=...` | `standard` | Linux-only at the kernel layer. On macOS through Docker Desktop or OrbStack the role container runs inside the backend's LinuxKit/OrbStack VM, so `docker-default` AppArmor is enforced inside that VM, not by macOS. The launch contract must say which layer enforces it on the active host. |
| SELinux label | `--security-opt label=...` | Future | Relevant on SELinux Linux hosts (Fedora, RHEL, openSUSE Tumbleweed with Crio/Podman backends). Not on macOS. |
| Capabilities | `--cap-drop`, `--cap-add` | `hardened` | Hypothesis grounded in Docker's documented default (`CAP_CHOWN`, `CAP_DAC_OVERRIDE`, `CAP_FSETID`, `CAP_FOWNER`, `CAP_MKNOD`, `CAP_NET_RAW`, `CAP_SETGID`, `CAP_SETUID`, `CAP_SETFCAP`, `CAP_SETPCAP`, `CAP_NET_BIND_SERVICE`, `CAP_SYS_CHROOT`, `CAP_KILL`, `CAP_AUDIT_WRITE` — 14 caps). See Capability Minimum Set below. |
| No privilege escalation | `--security-opt no-new-privileges` | `standard` | Breaks `sudo` (errors with "sudo: effective uid is not 0, is sudo installed setuid root?"). Mitigation: install role-required privilege paths via file caps (`setcap cap_setuid,cap_setgid+ep <binary>`) in the role Dockerfile, or keep `sudo`-using roles on `compat`. |
| Masked paths | implicit, `--security-opt systempaths=unconfined` to lift | `standard` | The default masked set (see Source Material) stays on. `systempaths=unconfined` is only allowed under `compat` and must be reported in the launch contract. |
| Read-only root | `--read-only`, `--tmpfs` | `hardened` | Requires the writable-path inventory (see Read-Only Root Tmpfs Preset). |
| Non-root user | `--user`, image user setup | Existing | Today the role process maps to host UID/GID. Sidecars (DinD especially) must be audited separately — DinD runs as root with `--privileged` today. |
| User namespace remap | `--userns-remap`, `--userns=host` | Future | Daemon-level, not per-container. Incompatible with `--pid=host`/`--network=host`/`--privileged`, and with several volume-driver setups. Out of scope for V1; tracked here so it does not get forgotten. |
| Resource limits | `--memory`, `--cpus`, `--pids-limit`, `--ulimit` | `standard` | Translation depends on cgroups version (see Cgroup Detection). Integrate with [Declarative resource limits](/reference/roadmap/declarative-resource-limits/). |
| Network isolation | per-agent network, `--network none` | Existing | Per-agent network exists. Egress policy is open by default; see [Network egress policy](/reference/roadmap/network-egress-policy/). |
| Host socket exclusion | omit `/var/run/docker.sock` | Existing | Hard rule. Backed by a launch-time guard plus regression test. |
| Private engine | DinD/rootless DinD/Sysbox/future backend | Existing | Keep inner Docker workflows away from the host daemon. |
| Credential location | env/file/brokered auth | Future | Coordinate with [Container credential exposure](/reference/roadmap/container-credential-exposure/) and [Host bridge](/reference/roadmap/host-bridge/). |

### Capability Minimum Set

**Critical constraint: capability drops on the role container are only meaningful under `hardened` profile with DinD disabled.** Under `compat` and `standard` (where DinD is active), the agent process has `DOCKER_HOST` pointing to a privileged DinD sidecar. Even if `CAP_NET_RAW` is dropped from the role container, the agent can run `docker run --privileged` against the DinD daemon and get the full capability set inside an inner container. Capability dropping is therefore a defense-in-depth measure for the `hardened` profile where DinD is absent, not a meaningful constraint for `compat`/`standard` with DinD active. This does not make it worthless — defense-in-depth against prompt injection attacks that try to exploit specific syscalls directly is real — but operators should not treat capability drops as a primary security boundary unless DinD is disabled.

Docker's 14-cap default is the upper bound. The validated jackin' starting set, derived from common role workflows during the May 2026 research pass, is:

| Capability | Why needed | Worflows it unblocks |
|---|---|---|
| `CAP_CHOWN` | Change file owner | `apt`, `npm install`, file extraction |
| `CAP_DAC_OVERRIDE` | Bypass file read/write/exec checks | `apt`, `npm install`, `pip`, `cargo`, `git` |
| `CAP_FOWNER` | Bypass perm checks on file owner | `apt`, `tar`, `unzip` |
| `CAP_FSETID` | Clear setuid/setgid on chown | `apt`, `tar` |
| `CAP_SETUID` | `setuid()` calls | drop privileges, `su`, `gosu`, `runuser` |
| `CAP_SETGID` | `setgid()` calls | drop privileges, group ops |
| `CAP_SETFCAP` | Set file caps | `apt`/`dpkg` (sets file caps on installed binaries) |
| `CAP_KILL` | Signal other procs | shells, process supervisors, `bash -c "cmd & wait"` |

Drop from Docker default in the jackin' starting set:

| Capability | Reason to drop |
|---|---|
| `CAP_MKNOD` | Role containers do not create device nodes; jackin' provides `/dev` via Docker defaults. |
| `CAP_NET_RAW` | Allows raw socket creation (`ping`, `tcpdump`). Modern Linux exposes ICMP via `ping_group_range` sysctl without raw sockets. Keep behind an opt-in `network.allow_raw_sockets = true` for roles that need `tcpdump`/`nmap`. |
| `CAP_SETPCAP` | Allows changing other procs' caps. Not needed once jackin' itself sets the cap set at exec. |
| `CAP_NET_BIND_SERVICE` | Allows binding privileged ports (`<1024`). Role containers should bind unprivileged ports inside the container; host port mapping is unaffected. |
| `CAP_SYS_CHROOT` | `chroot()` calls. Not needed for normal role workflows. |
| `CAP_AUDIT_WRITE` | Write to kernel audit log. Not needed; reduces audit-stream noise. |

Result: 8-cap default (down from 14). This is the V1 hypothesis; roles that fail with the smaller set declare `min_profile = "standard"` or `min_profile = "compat"` in their manifest, and `hardened` either drops the role's per-workflow caps to the same set or refuses to launch with a clear message.

DinD inside the role container is a separate concern: BuildKit and Compose empirically require near-full default set plus `CAP_SYS_ADMIN` (for mount namespacing) and `CAP_NET_ADMIN` (for bridge setup). DinD is therefore gated behind `compat` and `standard` by default, and explicitly opt-in under `hardened`.

### Read-Only Root Tmpfs Preset

The minimum writable-path inventory for `--read-only` with `--tmpfs`, derived from the Linux base-image + common package managers:

```text
/tmp                       — POSIX requirement
/run                       — runtime daemons, pid files
/var/run                   — typically a symlink to /run, but explicit on some images
/var/tmp                   — long-lived tmp
/var/cache                 — apt/dpkg/pip
/var/log                   — process logs that some tools (rsyslogd, systemd-journald, agents) write
/var/lib/apt/lists         — apt-get update writes here
/var/cache/apt/archives    — apt-get install downloads here (download phase only)
/var/lib/dpkg              — dpkg state, including /var/lib/dpkg/lock-frontend
$HOME/.cache               — pip, cargo, npm, fastly tools
```

Each path becomes a `--tmpfs <path>:rw,nosuid,nodev,size=<sized>` flag. Sizes need profile-level defaults plus a per-role override. `/tmp` and `$HOME/.cache` are the largest expected consumers (npm/yarn cache, pip wheel cache).

**`apt install` is NOT compatible with read-only root.** The download phase (`apt-get download`, fetching `.deb` files) works because it only writes to `/var/cache/apt/archives/` and `/var/lib/dpkg/`. The extraction phase (`dpkg --unpack` + `dpkg --configure`) writes to `/usr/bin/`, `/usr/lib/`, `/usr/share/`, `/etc/`, and other FHS paths under the image layer. These paths are not in the tmpfs preset because making them tmpfs would shadow the pre-installed binary tree and make tools installed at `docker build` time invisible. Therefore, under `--read-only`, `apt install <pkg>` fails when `dpkg` tries to extract to `/usr/`. This is an expected and accepted tradeoff for `hardened` — roles that need `apt install` at runtime declare `min_profile = "compat"` or `min_profile = "standard"`. Only roles that pre-install everything in the role Dockerfile can use `hardened` with read-only root.

**Shell history and runtime home paths.** The `agent` user's home directory (`/home/agent/`) has several subdirectories that are bind mounts (agent credential files seeded by `jackin-capsule runtime-setup`) and several that are image-layer paths (oh-my-zsh, `.zshrc`, `.config/`, etc.). Under `--read-only`, shell history (`~/.zsh_history`, `~/.bash_history`), zsh session files (`~/.zsh_sessions/`), and oh-my-zsh update markers will fail to write. These need either a tmpfs entry (acceptable for ephemeral zsh session state) or a bind mount (for persistent history). Shell history persistence as a feature is deferred — see [Devcontainer Parity Finding 4](/reference/roadmap/devcontainer-parity/) and [Déjà Vu](/reference/roadmap/deja-vu/) for the full discussion. For Phase 4 read-only root, the minimum fix is adding these to the tmpfs preset so container startup does not break: `$HOME/.zsh_sessions`, `$HOME/.zsh_history`, `$HOME/.bash_history`. An explicit audit of every write target in `$HOME` is required before Phase 4 ships.

**`jackin-capsule runtime-setup` writes under read-only root.** The capsule runtime setup (in `crates/jackin-capsule/src/runtime_setup.rs`) writes auth credential files (`~/.claude/`, `~/.claude.json`, `~/.codex/auth.json`, `~/.local/share/amp/secrets.json`, `~/.kimi-code/`, `~/.local/share/opencode/`) and git hook markers (`/jackin/state/container-init.done`, `/jackin/state/git-hooks/`). All of these are on bind-mounted paths (either the per-container home state volume or `/jackin/state/`), so they are writable regardless of `--read-only`. No capsule changes needed for read-only root support.

Paths that should NOT be tmpfs even under `hardened`:

- jackin'-owned persistent state mounts (under `/jackin/state/` and any agent-home state bind mounts) — explicit volume mounts, already writable.
- `/jackin/run` — already a bind mount at `launch.rs:1007–1008`; adding it as tmpfs would shadow the bind mount. Do not include in the tmpfs preset.
- Workspace mounts — bind mounts from the host, not tmpfs.

Under `locked` the inventory is smaller because the agent is not expected to install packages: `/tmp`, `/run`, `/var/run` only (plus the shell history entries).

### Cgroup Detection

Resource limits translate differently across cgroups v1 and v2. Detection at jackin' launch time:

```sh
# v2 if cgroup2fs; v1/hybrid if tmpfs
stat -fc %T /sys/fs/cgroup/
# or: read /proc/self/cgroup — v2 has a single line starting "0::/..."
```

| jackin' contract | cgroup v2 mapping | cgroup v1 mapping |
|---|---|---|
| `memory_reservation` | `--memory-reservation` → `memory.high` (soft throttle, no OOM-kill) | not supported; downgrade to `--memory-reservation` → `memory.soft_limit_in_bytes` and warn that throttling differs |
| `memory` | `--memory` → `memory.max` | `--memory` → `memory.limit_in_bytes` |
| `cpus` | `--cpus` → `cpu.max` | `--cpus` → `cpu.cfs_quota_us`/`cpu.cfs_period_us` |
| `pids` | `--pids-limit` → `pids.max` | `--pids-limit` → `pids.max` (controller may not be delegated in rootless v1) |
| `nofile` | `--ulimit nofile=N:N` | same; LimitNOFILE differs by host |

`hardened` and `locked` should require cgroups v2 on Linux. On macOS, Docker Desktop and OrbStack both expose v2 inside their VMs by default. Older Linux hosts on v1/hybrid should get a `debug_log!("launch", "cgroup v1 detected, downgrading memory_high to soft limit")` line and degrade gracefully under `standard`, but fail-closed under `hardened`/`locked`.

## DinD Hardening Track

DinD is the highest-priority Docker-specific risk because jackin' keeps Docker available inside the agent environment.

### Phase A — make the current DinD contract explicit

- Report sidecar image, privilege status, transport, cert volume, and network in the launch/session contract.
- Ensure all docs say TLS-authenticated DinD, not plain TCP.
- Keep the host Docker socket blocked from the role container.
- Add regression tests that fail if the host socket is mounted into either the role container or the DinD sidecar by accident.

DinD TLS reality check (May 2026): the upstream `_/docker` image generates `ca/`, `server/`, `client/` cert directories under `DOCKER_TLS_CERTDIR` on first boot. Docker ships no rotation, no revocation, and no CA-management tooling — `docs.docker.com/engine/security/protect-access/` explicitly warns "Using TLS and managing a CA is an advanced topic" and gives no guidance beyond that. jackin' inherits a per-launch self-signed CA today and never rotates. If a DinD client cert leaks (e.g. via `docker inspect` on a misconfigured role), the leaker has full daemon control inside the sidecar — and with `--privileged` the sidecar has kernel access to the outer host. Phase A scope:

- regenerate certs on every launch (already true today; document it)
- store cert volume under `/jackin/run/<instance>/dind-certs/` per the container path convention so `purge` removes them
- emit `debug_log!("dind", "TLS certs generated, lifetime=<launch>")` so the operator can trace the cert lifecycle
- do not invest in CA rotation tooling under Phase A — the certs already die with the launch

### Phase B — evaluate `docker:dind-rootless`

Known-good upstream limits to design against (per `docs.docker.com/engine/security/rootless/troubleshoot/`):

- Storage drivers supported in rootless: `overlay2` (kernel 5.11+), `fuse-overlayfs` (4.18+), `btrfs`, `vfs`. Other drivers fail-closed.
- Explicitly unsupported: AppArmor, checkpointing, overlay networks, exposing SCTP ports.
- Cgroups: resource limits delegated only on cgroup v2 + systemd; only memory + pids controllers by default.
- NFS as data-root unsupported.
- Privileged ports (`<1024`) require explicit setup (`/etc/sysctl.d/87-net.ipv4.ip_unprivileged_port_start.conf` or systemd `AmbientCapabilities=CAP_NET_BIND_SERVICE`).

Research questions remaining:

- Can role Dockerfiles build under rootless DinD? Most Dockerfiles should; ones that need `--add-host`, custom sysctls, or non-`overlay2` drivers will not.
- Does Java Testcontainers work against rootless DinD inside the role container? Reach-out path: try with `testcontainers-java` against the `testcontainers/ryuk` reaper, which has historically had rootless issues.
- Do BuildKit, Compose, bind mounts, and container networking behave acceptably? BuildKit yes (rootless-supported upstream), Compose yes for v2.
- What extra writable paths or sysctls are required? `subuid`/`subgid` maps must exist for the role user.
- Is startup time acceptable for interactive `jackin load`?

Known limitations from `moby#42910` (cgroup-v2 issues) and `docker-library/docker#451` (nested rootless): host-rootless + dind-rootless on cgroup v1 fails entirely. Decision pivot: rootless DinD is only viable on cgroup-v2 hosts. Document the prereq, do not silently downgrade.

Decision rule:

- If rootless DinD passes normal role workflows on cgroup v2 hosts, make it the `standard` DinD implementation on those hosts.
- On cgroup v1 hosts, keep privileged DinD under `standard` and gate `hardened` behind an upgrade path.
- If it only passes some workflows, make it opt-in under `hardened`.
- If it breaks common workflows even on cgroup v2, keep it documented as unsupported and revisit only when Docker changes.

### Phase C — evaluate Sysbox on Linux

Sysbox is a Linux-only container runtime that can run Docker-in-Docker style workloads without the usual privileged-container model. It is not a microVM.

Research questions:

- Can jackin' select Sysbox only for the DinD sidecar?
- Does it require host runtime installation that violates the "never mutate host silently" rule?
- Does it work with the selected Docker contexts jackin' supports?
- Is it useful enough if it cannot work on macOS?

Decision rule:

- Treat Sysbox as a Linux operator opt-in, not a default dependency.
- Surface any host-side installation requirement before launch.

### Phase D — support DinD-free roles

Some roles do not need Docker inside the sandbox. For those roles, `hardened` should skip the sidecar entirely.

Suggested role manifest shape for future design:

```toml
[runtime.docker]
requires_inner_engine = false
```

If this field lands in `jackin.role.toml`, it is a versioned manifest schema change and must ship with a migration.

## Resource Limits

Resource limits remain tracked in
[Declarative resource limits](/reference/roadmap/declarative-resource-limits/),
but this contract defines how profiles consume them.

Minimum Docker translator:

| Contract field | Docker mapping |
|---|---|
| `memory_reservation` | `--memory-reservation` |
| `memory` | `--memory` |
| `cpus` | `--cpus` |
| `pids` | `--pids-limit` |
| `nofile` | `--ulimit nofile=N:N` |

Profile rules:

- `compat`: limits optional.
- `standard`: apply limits when declared; warn for parallel launches with none.
- `hardened`: limits required.
- `locked`: limits required.

Launch output must distinguish "not configured" from "configured but backend cannot enforce."

## Network Policy

Network policy stays in
[Network egress policy](/reference/roadmap/network-egress-policy/), but Docker
profiles should drive defaults:

| Profile | Default egress |
|---|---|
| `compat` | open |
| `standard` | open, clearly reported |
| `hardened` | allowlist or deny required |
| `locked` | deny |

Important Docker-specific constraint: the role container and the DinD sidecar may have separate paths to the network. An implementation that filters only the role container but lets DinD-created inner containers egress freely must report `partial` enforcement.

## Filesystem Policy

Profiles should separate three filesystem concepts:

- host mounts, controlled by workspace/global mount config
- role container root filesystem, created from the role image
- jackin'-owned runtime/state paths inside the container

Rules:

- Host mounts remain explicit. No profile may add host mounts silently.
- `:ro` host mounts stay the primary read-only guarantee.
- `hardened` and `locked` should use read-only role root filesystems.
- Writable paths for jackin'-owned runtime state must remain under `/jackin/`.
- Temporary writable paths must be explicit tmpfs mounts.
- Persisted agent state must remain easy to purge and visible in the session contract.

Likely writable paths for read-only root evaluation:

```text
/tmp
/var/tmp
/run
/jackin/run
/jackin/state
agent home state mounts
```

The exact list must come from a compatibility test matrix, not guesswork.

## Credential Policy

Docker profile hardening must coordinate with:

- [Container credential exposure](/reference/roadmap/container-credential-exposure/)
- [Host bridge](/reference/roadmap/host-bridge/)
- [Live bidirectional auth sync](/reference/roadmap/live-auth-sync/)

Profile defaults:

| Profile | Credential posture |
|---|---|
| `compat` | Existing auth forwarding modes. |
| `standard` | Existing modes, with explicit launch-contract disclosure. |
| `hardened` | Prefer env-only or future brokered credentials; warn on copied persistent auth. |
| `locked` | No persistent copied auth by default. |

Do not hide the residual risk: if a token is placed in a file, env var, or tool-specific config inside the runtime, the agent can potentially read it.

## Session Contract Output

This roadmap item depends on
[Session contract and explain mode](/reference/roadmap/session-contract-explain-mode/).
Every Docker launch should eventually show a compact table like:

```text
Docker profile: hardened
Role container:
  seccomp: docker-default
  apparmor: docker-default
  no-new-privileges: enforced
  capabilities: drop-all + CHOWN,DAC_OVERRIDE,FOWNER
  root filesystem: read-only
  writable tmpfs: /tmp,/run,/jackin/run
DinD:
  status: disabled
Network:
  mode: allowlist
  enforcement: guest-enforced
  uncovered paths: none
Resources:
  memory: 8 GiB
  cpus: 2.0
  pids: 2048
Credentials:
  Claude: env-only
  GitHub CLI: not forwarded
Residual risk:
  shared host kernel; writable workspace mounts can still be changed
```

The output must be factual. If a host cannot enforce AppArmor, rootless Docker does not support a control, or a Docker Desktop backend hides a detail, say so.

## Compatibility Test Matrix

Before any profile becomes a default, test it against common jackin' workflows:

| Workflow | `compat` | `standard` | `hardened` | `locked` |
|---|---|---|---|---|
| launch each built-in agent runtime | required | required | required where no DinD needed | required for read-only roles |
| `apt`, `npm`, `pip`, `cargo` install/build | required | required | evaluate | usually blocked |
| Docker CLI against DinD | required | required | only if enabled | blocked |
| Docker Compose inside agent | required | required | evaluate | blocked |
| Java Testcontainers | required | required | evaluate | blocked |
| read-only mount write attempt fails | required | required | required | required |
| writable worktree mount works | required | required | explicit only | blocked by default |
| network deny/allowlist behavior | n/a | report only | required | required |
| reconnect/hardline/eject/purge | required | required | required | required |

Host matrix:

- macOS with OrbStack Docker context
- macOS with Docker Desktop
- Linux Docker rootful
- Linux Docker rootless
- remote/plain TCP Docker context where currently supported

## Implementation Phases

### Phase 1 — contract-only refactor ✅ Shipped in PR #528

Goal: establish the `DockerSecurityProfile` enum, wire `--docker-profile` CLI flag through the launch pipeline, add host-socket regression test and telemetry. No Docker behavior changes — every launch acts as `compat` regardless of requested profile.

**Delivered:**
- <RepoFile path="src/runtime/docker_profile.rs" /> — `DockerSecurityProfile` enum (`Locked | Hardened | Standard | Compat`) with `PartialOrd`/`Ord` (ascending capability), `FromStr`/`Display`, serde, `ParseProfileError` with full valid-values message, `resolve_profile(cli_override)` (CLI-only in Phase 1).
- `LoadOptions.docker_profile: Option<DockerSecurityProfile>` — in-memory, no schema bump.
- `LaunchContext.profile: DockerSecurityProfile` — populated via `resolve_profile(opts.docker_profile)`.
- `--docker-profile <PROFILE>` CLI flag on `jackin load` via `parse_docker_profile` in <RepoFile path="src/cli/role.rs" />, wired through <RepoFile path="src/app/mod.rs" />.
- `debug_log!("launch", "docker_profile selected={} container={}")` telemetry.
- `debug_assert!` + `debug_log!("launch", "host_socket_check passed=yes")` before every container start.
- Regression test `role_container_never_mounts_host_docker_socket` — fails if any future change mounts `/var/run/docker.sock`.
- Regression test `docker_profile_flag_accepted_and_produces_valid_launch` — verifies all four profiles are accepted and produce no Phase-1-premature security flags.

**Deferred to Phase 2** (intentionally not in Phase 1 — schema migrations and grant types belong with the implementation that uses them):
- Per-dimension grant types (`NetworkGrant`, `DindGrant`, `UserGrant`, `FilesystemGrant`, `ResourceGrant`, `EffectiveGrants`) — added in Phase 2 alongside the code that uses them.
- `DockerConfig.default_profile` field, `WorkspaceDockerConfig`, schema bumps — added in Phase 2.
- `RoleManifest.min_profile` + `dind` fields, manifest schema bump — added in Phase 3.
- `resolve_profile()` reading from config/workspace — Phase 2, after schema fields exist.

### Phase 2 — schema + grants struct + resource limits

Goal: make profiles actually read from config/workspace; add the full `DockerGrants` struct; wire resource limits into Docker flags. The schema for all grant fields lands here so operators can write configs immediately, even while enforcement of individual dimensions rolls out in Phases 3–5.

- **`DockerGrants` struct** in <RepoFile path="src/runtime/docker_profile.rs" /> — 12 fields exactly matching the `[docker.grants]` TOML spec:
  ```rust
  pub struct DockerGrants {
      pub network: Option<NetworkGrant>,           // "none" | "allowlist" | "open"
      pub allowed_hosts: Vec<String>,              // validated at launch
      pub dind: Option<DindGrant>,                 // "none" | "rootless" | "privileged"
      pub user: Option<String>,                    // "agent" | "root" | custom
      pub sudo: Option<bool>,
      pub system_writes: Option<bool>,
      pub memory: Option<String>,                  // "4G", "512M" — parsed to bytes at launch
      pub memory_reservation: Option<String>,
      pub cpus: Option<f64>,
      pub pids: Option<i64>,
      pub nofile: Option<u64>,
      pub capabilities_add: Vec<String>,           // validated against VALID_CAPABILITIES at launch
  }
  ```
- **Per-dimension grant enums** in <RepoFile path="src/runtime/docker_profile.rs" />:
  ```rust
  pub enum NetworkGrant { None, Allowlist, Open }
  pub enum DindGrant { None, Rootless, Privileged }
  ```
- **`EffectiveGrants` struct** — resolved per-dimension result after merging profile defaults with explicit grants. Used by `to_docker_flags()` to produce `run_args` entries.
- **`VALID_CAPABILITIES`** — `&[&str]` const with all 39 cap names (without `CAP_` prefix). Used by grant validation.
- **Validation functions** (all called at launch before any container starts):
  - `validate_grants(grants: &DockerGrants) -> Result<(), GrantError>` — checks user+sudo conflict, unknown caps, memory_reservation ≤ memory, parseable size strings.
  - `parse_memory_bytes(s: &str) -> Result<u64, GrantError>` — parses `"4G"`, `"512M"`, `"2048K"`.
- **`DockerConfig.default_profile`** + `DockerConfig.grants: Option<DockerGrants>` in <RepoFile path="src/config/mod.rs" />. Config schema bump: `v1alpha5 → v1alpha6` (noop migration; rebake 5 config `after.toml` + `meta.toml` pairs; new `tests/fixtures/migrations/config/from-v1alpha5/` fixture).
- **`WorkspaceDockerConfig { profile: Option<DockerSecurityProfile>, grants: Option<DockerGrants> }`** + `WorkspaceConfig.docker: Option<WorkspaceDockerConfig>`. Workspace schema bump: `v1alpha5 → v1alpha6` (noop migration; same rebake + fixture procedure; both config and workspace land in one PR at the same version).
- **`resolve_profile()`** updated to read full precedence chain: CLI > workspace > config default > `Compat`.
- **Resource limits** translated to Docker flags in `launch_role_runtime`: `--memory`, `--memory-reservation`, `--cpus`, `--pids-limit`, `--ulimit nofile=N:N`.
- **Cgroup detection** at launch: `stat -fc %T /sys/fs/cgroup/` → emit `debug_log!("launch", "cgroup_version v={}")`. On v1 hosts under `hardened`/`locked`, fail-close; under `standard`, downgrade `memory_reservation` with a warning.
- **`no-new-privileges` stub**: add the flag path in code, emit `debug_log!("launch", "no_new_privileges enforced=no reason=sudo-audit-pending")`. Do not actually add `--security-opt no-new-privileges` until Open Question #5 (sudo audit) is resolved.
- **`ContainerSpec`** in <RepoFile path="src/docker_client.rs" /> extended with resource fields to keep the Bollard test path in sync.
- **Schema-versions doc entry** for `v1alpha6` in <RepoFile path="docs/content/docs/reference/schema-versions.mdx" />.

### Phase 3 — rootless DinD and DinD-free roles

- Add `DockerResourceLimits` struct to <RepoFile path="src/runtime/docker_profile.rs" />: `memory: Option<u64>`, `memory_reservation: Option<u64>`, `cpus: Option<f64>`, `pids: Option<i64>`, `nofile: Option<u64>`.
- In `launch_role_runtime`, read resolved limits from role manifest `[resources]` (tracked in [Declarative resource limits](/reference/roadmap/declarative-resource-limits/)) and translate to Docker flags via the helper in `docker_profile.rs`:
  - `--memory <memory>` for `memory`
  - `--memory-reservation <memory_reservation>` for `memory_reservation`
  - `--cpus <cpus>` for `cpus`
  - `--pids-limit <pids>` for `pids`
  - `--ulimit nofile=<nofile>:<nofile>` for `nofile`
- Detect cgroup version at launch (run `stat -fc %T /sys/fs/cgroup/` or read `/proc/self/cgroup`) and emit `debug_log!("launch", "cgroup_version v={}")`. On v1 hosts under `hardened`/`locked`, fail-close; under `standard`, downgrade `memory_reservation` and emit a warning.
- Add `--security-opt no-new-privileges` to `standard`+ profiles **after** completing Open Question #5 (sudo audit). Until the audit is done, emit `debug_log!("launch", "no_new_privileges enforced=no reason=sudo-audit-pending")` but do not add the flag.
- Report missing resource budgets: if `standard`/`hardened`/`locked` and no limits declared, emit a `clog!`-tier warning (or `debug_log!` until `clog!` lands) so the operator sees it at launch.
- Update `ContainerSpec` in <RepoFile path="src/docker_client.rs" /> to carry the same resource fields and wire them into `HostConfig` so tests stay in sync with production.

### Phase 3 — rootless DinD and DinD-free roles

Goal: reduce DinD blast radius; let roles opt out of the sidecar entirely.

- Add `requires_inner_engine: Option<bool>` to `RoleManifest` (lands in the same `v1alpha5` bump as `min_profile` from Phase 1).
- In `launch_role_runtime`, skip the DinD sidecar block (lines 690–716) when `profile >= hardened && !requires_inner_engine`. Emit `debug_log!("launch", "dind enabled=no reason=profile-hardened-no-inner-engine-required")`.
- Prototype `docker:dind-rootless`: swap the `docker:dind` image for `docker:dind-rootless` on cgroup v2 hosts under `standard`. Drop `--privileged` from `dind_args`. Evaluate against the compatibility test matrix. Do not make it the default until all rows in the matrix pass.
- Add the DinD cert path note: emit `debug_log!("dind", "TLS certs generated, lifetime=launch, path=/jackin/run/<instance>/dind-certs/")`. Verify cert volume is stored under `/jackin/run/` per the container path convention — the bind mount at `launch.rs:1007` already mounts the socket dir; certs need their own subdirectory under the same base.

### Phase 4 — read-only root and capability policy

Goal: lock down the role container filesystem and drop unnecessary Linux capabilities.

- In `launch_role_runtime`, after the base flags block (line 809), insert capability flags for `hardened`/`locked` via the `capability_flags()` helper:
  ```
  --cap-drop=ALL
  --cap-add CHOWN
  --cap-add DAC_OVERRIDE
  --cap-add FOWNER
  --cap-add FSETID
  --cap-add SETUID
  --cap-add SETGID
  --cap-add SETFCAP
  --cap-add KILL
  ```
- Insert `--read-only` for `hardened`/`locked`.
- Insert `--tmpfs` entries for the read-only root tmpfs preset. Each entry uses `rw,nosuid,nodev`. **Do not include `/jackin/run`** (already a bind mount). Note: `/jackin/state` is also already a bind mount and must not appear in the tmpfs list. The tmpfs list for `hardened` is: `/tmp`, `/run`, `/var/run`, `/var/tmp`, `/var/cache`, `/var/log`, `/var/lib/apt/lists`, `/var/cache/apt/archives`, `/var/lib/dpkg`, `/home/agent/.cache`. Sizes need profile defaults + per-role overrides.
- Emit the full capability telemetry lines defined in the Telemetry Surface section.
- Run the compatibility test matrix against the 8-cap minimum set. If a built-in agent workflow fails, either add the required cap with a documented reason or accept that `hardened` rejects that workflow.
- Update `ContainerSpec` in <RepoFile path="src/docker_client.rs" /> with `cap_add`, `cap_drop`, `security_opt`, `read_only_rootfs`, `tmpfs` fields and wire them into `HostConfig`.

### Phase 5 — network and credential enforcement

Goal: connect profile defaults to the network egress policy; integrate credential broker work.

- In `create_network` (<RepoFile path="src/docker_client.rs" /> line 576): add `internal: Option<bool>` parameter. For `locked`, pass `internal: true` (blocks external egress at the Docker network layer). For `standard`/`hardened`, keep `internal: false` but ensure egress policy from [Network egress policy](/reference/roadmap/network-egress-policy/) is applied.
- Report partial enforcement when DinD or inner containers are not covered by the network policy. The `locked` profile's `--network none` equivalent is `internal: true` on the bridge — verify inner containers created by DinD (if DinD is enabled under `standard`) inherit the same restriction or emit a `partial` enforcement warning.
- Integrate credential posture with [Container credential exposure](/reference/roadmap/container-credential-exposure/): under `hardened`/`locked`, prefer env-only injection over file copies. Emit a `debug_log!("launch", "credential_posture …")` line per agent auth type.

## Telemetry Surface

Every Docker hardening decision must emit `debug_log!("launch", …)` (the existing macro in <RepoFile path="src/tui/mod.rs" />) so the firehose under `JACKIN_DEBUG=1` lets an operator reconstruct what actually got applied. Minimum required lines:

- `profile_selected profile=<name> source=<workspace|cli|role|default>`
- `cap_drop_all` + `cap_add cap=<name> reason=<jackin-default|role-required|opt-in>`
- `no_new_privileges enforced=<yes|no> reason=<profile|role-incompat>`
- `seccomp profile=<docker-default|custom|unconfined>`
- `apparmor available=<yes|no> profile=<docker-default|custom|unconfined> layer=<host|backend-vm>`
- `read_only_root enforced=<yes|no> tmpfs=<list> reason=<profile|role-incompat>`
- `cgroup_version v=<v1|v2|hybrid>`
- `resource_limit kind=<memory|cpus|pids> value=<v> backend_translated=<docker-flag>`
- `dind enabled=<yes|no> mode=<privileged|rootless|sysbox> tls_certs_path=<path>`
- `host_socket_check passed=<yes|no>`

Compact telemetry (the future `clog!` tier in the AGENTS.md two-tier rule, currently overlapping with `debug_log!`) limits to one line per launch:

```text
launch profile=hardened cap_set=8 caps no_new_privileges=on read_only_root=on cgroup=v2 dind=disabled network=allowlist
```

## Image-Build-Time Hardening (Out Of Scope For V1)

Roles build their own images, today on the host Docker engine. The build phase is also code execution. This contract scopes runtime hardening only; build-time hardening (rootless BuildKit, build context constraints, hermetic builders) is tracked separately and will land alongside the [OrbStack isolated machine backend](/reference/roadmap/orbstack-isolated-machine-backend/) and [smolvm backend research](/reference/roadmap/smolvm-backend-research/) work where machine-local builds become possible. V1 of this contract does not change the build path.

## Schema Migration Footprint

This contract introduces three new config surfaces. Per the rules in <RepoFile path="AGENTS.md" />, each lands with a `CURRENT_*_VERSION` bump:

| Surface | File kind | Type touched | Current version | Target version | Action |
|---|---|---|---|---|---|
| `[docker] default_profile = "..."` | `config.toml` | `AppConfig` → `DockerConfig` | `v1alpha5` (<RepoFile path="src/config/migrations.rs" /> line 7) | `v1alpha6` | bump `CURRENT_CONFIG_VERSION`, add fixture `tests/fixtures/migrations/config/from-v1alpha5/`, re-bake all existing `after.toml`s, add Schema Versions entry. |
| `[workspaces.X.docker] profile = "..."` | `~/.config/jackin/workspaces/<name>.toml` | `WorkspaceConfig` | `v1alpha5` (<RepoFile path="src/config/migrations.rs" /> line 8) | `v1alpha6` | bump `CURRENT_WORKSPACE_VERSION`, fixture `tests/fixtures/migrations/workspace/from-v1alpha5/`, same bake + entry procedure. Config and workspace versions track together; one PR bumps both to `v1alpha6`. |
| `[docker] min_profile = "..."` and `[docker] requires_inner_engine = false` | `jackin.role.toml` | `RoleManifest` | `v1alpha4` (<RepoFile path="src/manifest/migrations.rs" /> line 6) | `v1alpha5` | bump `CURRENT_MANIFEST_VERSION`, fixture `tests/fixtures/migrations/manifest/from-v1alpha4/`, same bake + entry procedure. |

All three additions are additive (new optional fields with serde defaults), so each bump is a single migration step. The "one schema version bump per PR" rule in AGENTS.md still applies: each version transition is one PR. The config+workspace bump is one PR (both go to `v1alpha6`); the manifest bump is a separate PR (goes to `v1alpha5`).

The exact TOML shape for the role manifest fields:

```toml
# jackin.role.toml — new optional fields
[docker]
min_profile = "compat"          # default: "standard"; roles needing sudo declare "compat"
requires_inner_engine = false   # default: true; roles that don't need DinD declare false
```

The exact TOML shape for the workspace override:

```toml
# ~/.config/jackin/workspaces/<name>.toml — new optional section
[docker]
profile = "hardened"
```

The exact TOML shape for the global config default:

```toml
# ~/.config/jackin/config.toml — new optional field under [docker]
[docker]
default_profile = "standard"
```

## Decided In This Pass

These are the V1 defaults the contract commits to. Code work that implements them does not need to re-debate the choice:

- **Model: capability grants, not restrictions.** Profiles are capability bundles. `locked` is the tightest; `compat` grants everything. Explicit `[docker.grants]` extend any profile on specific dimensions. Operators grant up, not restrict down.
- Profile enum is `locked | hardened | standard | compat` (ordered ascending capability). The conceptual default from the user's perspective is `locked` as the starting point; the practical default while `standard` is not yet validated is `compat`.
- **`compat` is the initial default for all launches** until the sudo audit (Open Question #5) is resolved and the compatibility test matrix passes. `standard` becomes the default after that. Phase 1 ships only the enum + `compat`-only behavior — no functional change for any user, just bookkeeping.
- **Profile + explicit grant composition:** take the profile's per-dimension tier, apply any explicit grants that elevate a dimension, report the effective per-dimension grant in the session contract. Conflict (operator restricts a dimension below what a role declares minimum) = launch failure with a named-conflict message.
- **Ascending-capability interpretation:** `locked` is not "locked down from compat"; it is "explicitly granted only what's needed for model-API-only agent work." The operator who adds `dind = "privileged"` to a `locked` profile is granting one dimension beyond the locked bundle, not weakening a restriction.
- Capability minimum hypothesis for `hardened`/`locked`: 8 caps (`CHOWN`, `DAC_OVERRIDE`, `FOWNER`, `FSETID`, `SETUID`, `SETGID`, `SETFCAP`, `KILL`). Caps only matter when DinD is disabled — under `hardened`/`locked`, DinD is `none` by default. Validate against the test matrix before the 8-cap set is considered confirmed.
- `no-new-privileges` is required under `hardened` and `locked`. Under `standard` it is the goal but blocked by the sudo audit. Under `compat` it is not applied.
- `locked` and `hardened` default network grant is `allowlist`. `allowlist` requires the allowlist proxy implementation (tracked in [Network egress policy](/reference/roadmap/network-egress-policy/)). Until that is implemented, `locked`/`hardened` network grant degrades to `open` with a visible warning in the session contract — launching with an unimplemented grant is better than refusing to launch.
- Masked-paths default stays on for all profiles; `systempaths=unconfined` allowed only under `compat` and reported.
- Read-only root: only for `locked`/`hardened`, only for purpose-built images (pre-installed tools, no runtime `apt install`). The `apt install` + read-only root conflict is a feature: if the role needs `apt install`, it declares `min_profile = "standard"` and gets a writable root.
- Cgroup v2 required for rootless DinD under `hardened`/`standard`; privileged DinD falls back on cgroup v1 for `standard`/`compat`. `hardened` fails-closed on cgroup v1 if rootless DinD is the only option and rootless fails.
- DinD TLS certs regenerate per launch under `/jackin/run/<instance>/dind-certs/`. No CA rotation under V1 — certs already die with the launch.
- Host Docker socket exclusion is a hard rule; the contract adds a launch-time regression test.
- Profile selection precedence: CLI flag > workspace override > role manifest `min_profile` > global default. CLI cannot go below `min_profile` without explicit operator grant override.
- **The implementation priority order, grounded in user impact:** (1) resource limits (`standard` profile, Phase 2) — universally useful, zero compatibility issues, real protection for parallel agent users; (2) DinD-free roles (`dind = "none"`, Phase 3) — enables `hardened`/`locked` to be meaningful; (3) read-only root + capability minimum set (Phase 4) — for purpose-built roles after DinD is disabled; (4) network allowlist (`allowlist` tier, Phase 5) — completes the `locked` profile's network grant. Phase 1 is infrastructure only.

## Open Before Implementation Can Start

These need answers before Phase 1 code work begins:

1. **Test fixture matrix.** Which exact agent runtimes + workflows are the acceptance set? (claude-code, codex, amp, kimi, opencode × `apt install build-essential`, `npm install <pkg-with-native-deps>`, `pip install <wheel>`, `cargo build`, `git clone+push`, `docker build` in-DinD, Compose, Testcontainers, `gh pr create`). Without this, "validate the cap set" has no objective answer.
2. **`min_profile` field shape.** Role manifest needs a way to say "I need at least `compat`" or "I am `locked`-compatible". Open: enum vs ordered list vs constraint expression. Lowest-friction choice: an ordered enum field, default `standard`. The `DockerSecurityProfile` enum in <RepoFile path="src/runtime/docker_profile.rs" /> (see Codebase Map) should derive `PartialOrd`/`Ord` so precedence is `max(workspace_override, role_min_profile)`.
3. **AppArmor on macOS launch contract.** What exact text does jackin' print under `standard` when running on Docker Desktop or OrbStack? Current code does not know which backend Docker is using; needs a probe (`docker info --format '{{.OperatingSystem}}'` or `docker info --format '{{.SecurityOptions}}'`) at launch. The probe result flows into the session contract output and the `debug_log!("launch", "apparmor available={} layer=…")` telemetry line. The `clap` definition for `--docker-profile` will live in `LoadArgs` in <RepoFile path="src/cli/role.rs" />.
4. **Sysbox host-install policy.** Sysbox needs a host-side runtime registration that violates the "never mutate host silently" rule. Either operator-confirmed opt-in install or hard-skip on hosts without Sysbox. Decide which before Phase C.
5. **`NOPASSWD:ALL` sudo audit.** Before `no-new-privileges` can be applied to any built-in agent runtime, audit every `sudo` call site in <RepoFile path="docker/construct/Dockerfile" /> and all role Dockerfiles. The base image adds `NOPASSWD:ALL` at line 113. With `--security-opt no-new-privileges`, `sudo` fails because setuid elevation is blocked at the kernel level (`sudo: effective uid is not 0, is sudo installed setuid root?`). Resolution path: identify each privileged binary that roles need and replace the `sudo` call with a file capability (`setcap cap_setuid,cap_setgid+ep <binary>`) in the role Dockerfile. Until the audit is complete, `standard` profile cannot safely enable `no-new-privileges` for built-in agent images. Custom role authors who control their own Dockerfile can opt in earlier by declaring `min_profile = "standard"` after removing `sudo` from their image.
6. **Two-Docker-path sync policy.** The production role container uses the CLI subprocess path (`run_args: Vec<&str>` in `launch_role_runtime`); the Bollard `ContainerSpec` / `create_container` path is used by tests and restore code. Both must be kept in sync. Decide whether to: (a) keep both paths and add `ContainerSpec` security fields mirroring the CLI path (more maintenance, but tests can exercise security config), or (b) have the Bollard path defer to the same helper that builds CLI flags (eliminates duplication). Option (b) is cleaner but requires refactoring the Bollard path to accept a pre-built `HostConfig` rather than a `ContainerSpec`. Option (a) is the lower-risk Phase 1 choice.
7. **`/jackin/run` tmpfs exclusion.** The socket-dir bind mount `-v <socket_dir>:/jackin/run` is appended at `launch.rs:1007–1008` and provides write access to `/jackin/run` through the bind mount, not a tmpfs. The read-only root tmpfs preset listed above correctly excludes `/jackin/run`. Confirm before Phase 4 that no other path under `/jackin/` needs an explicit tmpfs entry — the bind mounts for `/jackin/state`, `/jackin/claude/`, `/jackin/codex/` etc. already provide write access to those subtrees.
8. **$HOME write-path audit required before read-only root.** The `agent` user's home directory mixes bind-mount paths (writable regardless of `--read-only`) and image-layer paths (blocked under `--read-only`). Before Phase 4 ships, enumerate every runtime write target under `/home/agent/` that is NOT a bind mount: shell history (`.zsh_history`, `.bash_history`), zsh session files (`.zsh_sessions/`), oh-my-zsh update markers, any `.gitconfig` local overrides, and `.local/share/mise/` shim paths if `mise` activates at runtime. Each needs either a tmpfs entry or a per-container state bind mount. `jackin-capsule runtime-setup` writes are all on bind-mount paths (`.claude/`, `.codex/`, `.local/share/amp/`, etc.) and are fine.
9. **Agent bypass flags are not configurable per profile.** Every agent binary is launched with its maximum-bypass flag in <RepoFile path="docker/runtime/entrypoint.sh" /> (`claude --dangerously-skip-permissions`, `codex --dangerously-bypass-approvals-and-sandbox`, `amp --dangerously-allow-all`, `kimi --yolo`, `opencode` with `OPENCODE_CONFIG_CONTENT='{"permission":"allow"}'`). These are hard-coded in the entrypoint — not configurable per Docker profile. Any security model that assumes the agent binary enforces its own sandboxing is incorrect. The Docker boundary is the only enforceable isolation layer. This is intentional design (jackin' is a no-interruption autonomous environment), but it must be stated clearly in the session contract output so operators understand the trust model: they are trusting the Docker isolation, not the agent's own permission system.

## Risks and Design Traps

- Overstating security. Hardened Docker is still a shared-kernel boundary.
- Breaking role author workflows by making strict profiles the default too soon.
- Adding flags without session-contract reporting, which makes the policy invisible and hard to debug.
- Filtering only the role container's network while inner DinD containers still have open egress.
- Treating Docker Desktop, OrbStack, and Linux Docker as if they expose the same security controls.
- Mutating host Docker daemon settings to make a profile pass.
- **CLI path and Bollard path diverging.** Security flags are built in two separate places (`run_args` vec in `launch_role_runtime` for production, `HostConfig` in `create_container` for the Bollard path). If one is updated and the other is not, tests pass against a different security posture than production runs. Enforce this via a shared `docker_profile.rs` helper that both paths call, or add an integration test that exercises both paths with an explicit non-default profile.
- **`NOPASSWD:ALL` sudo + `no-new-privileges` silent failure.** If `standard` profile enables `no-new-privileges` before the base image removes `NOPASSWD:ALL` sudo, every `sudo` call inside the container silently fails with a non-obvious error. This is worse than the feature not being there — the container starts successfully but tools that call `sudo` (apt, dpkg post-installs, npm scripts) fail at runtime with confusing permission errors. The `standard` profile must not enable `no-new-privileges` until the base-image audit in Open Question #5 is resolved.
- **Profile precedence not enforced before launch.** If the precedence logic (`CLI > workspace > role min_profile > global default`) is only applied after some early-exit path (e.g., image not found, trust gate rejection), operators may see an inconsistent profile in logs vs what actually ran. Resolve the effective profile once at the start of `load_role_with` and record it in `LaunchContext` before any early exits.
- **`docker info` probe adds latency to every launch.** The AppArmor / cgroup version probe requires a `docker info` call which can take 100–500 ms on some Docker Desktop configurations. Cache the result per-session or run it in parallel with the image pull check.
- **`run_args` tests brittle to flag ordering.** Many existing tests (search `docker run` in `launch.rs` test section starting ~line 5200) assert exact `run_args` contents. Adding new security flags changes arg ordering and will break these tests. Budget time for test updates in each phase.
- **`/jackin/run` bind-mount before read-only flag.** Docker processes bind mounts and the `--read-only` flag together. The socket-dir bind mount at `launch.rs:1008` must remain a bind mount (not tmpfs) even under `hardened` because the capsule daemon socket lives there. Confirm that Docker's behavior with `--read-only` + explicit bind mounts does not prevent writes to those bind-mount paths (it does not — `--read-only` applies only to the image layer, not bind mounts — but document this explicitly so future maintainers don't add an unnecessary tmpfs entry for `/jackin/run`).
- **DinD cert volume and `--read-only`.** The DinD sidecar mounts its cert volume at `/certs/client` (from `launch.rs:688`). The role container mounts the same volume read-only at `/certs/client:ro`. Under `hardened`, the DinD sidecar itself is disabled by default, so this is moot. Under `standard`, the cert volume mount at `/certs/client:ro` in the role container is already read-only — no change needed there.

## Lessons from Anthropic's Claude Code Devcontainer

The [devcontainer-parity](/reference/roadmap/devcontainer-parity/) roadmap item documents a side-by-side analysis of jackin' construct vs the upstream [anthropics/claude-code `.devcontainer`](https://github.com/anthropics/claude-code/tree/main/.devcontainer). Several of those findings directly inform this contract. Key extractions:

**What the upstream devcontainer proves works (and jackin' makes configurable instead of fixed):**

| Upstream choice | What it solves | jackin' equivalent | jackin' difference |
|---|---|---|---|
| `iptables`/`ipset` OUTPUT allowlist in `init-firewall.sh` | Prevents arbitrary outbound from agent | `network = "allowlist"` tier in capability taxonomy | Configurable: operator adds/removes domains; default list is assembled from configured agent endpoints; fully toggleable per profile/workspace/role |
| `--cap-add=NET_ADMIN --cap-add=NET_RAW` in `devcontainer.json` | Required for iptables init | Implicit cap grants when `network = "allowlist"` is selected | Surfaced in session contract; not hidden; reverts to default cap set if `allowlist` is not selected |
| Sudo scoped to `/usr/local/bin/init-firewall.sh` only | Firewall init needs root; agent should not | `agent_scoped_sudo` user privilege tier; `network = "allowlist"` implicitly selects it | Fully toggleable; operator can layer `sudo = true` on top for broader sudo if the role needs it |
| Hard-coded allowlist (Anthropic endpoints, GitHub, npm, Sentry, VS Code) | Catches 90% of real Claude Code use cases | Per-session assembled `JACKIN_ALLOWED_DOMAINS` from agent endpoints + operator config + role config | Not hard-coded; adapts to which agents are running and what packages/services the role needs |
| Named Docker volumes for `~/.claude` and shell history | State survives container recreation | `~/.jackin/data/{name}/home/.claude` bind mount (existing). Shell history persistence is a separate concern — see note below. | `~/.claude` bind mount already survives container recreation and eject. Shell history and **agent conversation history** are different in importance: conversation history is tracked by [Déjà Vu](/reference/roadmap/deja-vu/). |
| Default DROP OUTPUT policy before allowlist is applied | Fail-safe: if script fails, no traffic leaves | Same posture: iptables -P OUTPUT DROP runs before any ipset additions | Same semantics; if firewall init fails, container should surface the error (jackin-capsule can detect exit status) |

**What the upstream devcontainer does NOT do that jackin' provides:**
- DinD for inner Docker workflows
- Per-agent namespaced networks (each agent isolated)
- Provider routing (Z.AI, MiniMax, Kimi via provider system)
- Multi-agent, workspace-level configuration
- Configurable profiles instead of a fixed setup

**Key philosophical difference:** the upstream devcontainer makes one set of choices for one use case (Claude Code in VS Code/Codespaces). Those choices are correct for that use case. jackin' operators have different use cases — some want `open` network for unrestricted development, some want `allowlist` for suspicious repos, some want `none` for offline analysis. jackin does not force a single experience; it provides the mechanism and lets the operator choose. The upstream devcontainer is a valuable proof of concept for the `allowlist` + `agent_scoped_sudo` combination specifically.

**The `init-firewall.sh` approach is the V1 implementation of `allowlist`.** It is simpler than a proxy sidecar (no additional container, no TLS interception), proven in production use at Anthropic's scale, and directly implementable as a jackin runtime hook. The proxy sidecar approach (tracked in [Network egress policy](/reference/roadmap/network-egress-policy/)) is the V2 implementation that covers non-HTTP protocols and provides connection logs.

## Related Files

- <RepoFile path="src/runtime/launch.rs" /> - role container, DinD, network, and launch orchestration.
- <RepoFile path="src/docker_client.rs" /> - typed Docker API boundary and container create host config.
- <RepoFile path="src/docker.rs" /> - Docker CLI boundary for build/TTY cases.
- <RepoFile path="src/manifest/mod.rs" /> - role manifest schema if runtime requirements move into roles.
- <RepoFile path="src/config/mod.rs" /> - global/workspace config if profile defaults move into operator config.
- <RepoFile path="docker/runtime/entrypoint.sh" /> - runtime startup and writable path assumptions.
- <RepoFile path="docker/construct/Dockerfile" /> - base runtime image and user assumptions.

## Related Roadmap Items

| Item | Relationship |
|---|---|
| [Selectable sandbox backends](/reference/roadmap/selectable-sandbox-backends/) | Umbrella for Docker, OrbStack, Kubernetes, and future microVM backends. |
| [OrbStack isolated machine backend](/reference/roadmap/orbstack-isolated-machine-backend/) | macOS backend that still needs Docker-compatible inner workflows. |
| [Network egress policy](/reference/roadmap/network-egress-policy/) | Supplies deny/allowlist enforcement and connection logs. |
| [Declarative resource limits](/reference/roadmap/declarative-resource-limits/) | Supplies resource budget schema and parser. |
| [Process-level sandboxing](/reference/roadmap/process-level-sandboxing/) | Adds per-command isolation inside any Docker profile. |
| [Session contract and explain mode](/reference/roadmap/session-contract-explain-mode/) | Makes the active profile inspectable before launch. |
