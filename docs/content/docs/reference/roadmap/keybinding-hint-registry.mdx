---
title: "Unify keybindings and hint bars (one keymap registry)"
---

**Status**: Partially implemented

This item is written as a hand-off plan: an engineer (or a Sonnet-class agent) should be able to execute it mechanically without re-discovering anything. Every divergence is listed with its file and the prescribed resolution. Line numbers are accurate as of the audit (branch `feature/launch-ctrl-c-cancellation`, 2026-06-19) and should be re-confirmed by reading each file before editing, since earlier phases shift later lines.

## Problem

Across every jackin' TUI surface, the keys a screen *handles* and the keys its hint bar *advertises* are maintained as two independent lists. Nothing binds them together, so they drift. The result is a class of bug where a key does something the operator was never told about — or a hint names a key that does nothing, or names one key while a different key fires.

The reported instance: on the console workspaces screen the hint bar reads `↑↓ · ↵ launch   N new · S settings   Ctrl+Q quit`, yet pressing bare `Q` (or `q`, or `Esc`) exits jackin' immediately with **no confirmation** — a path that is both unadvertised and more destructive than the advertised `Ctrl+Q`, which only opens a confirm dialog. The handler is `KeyCode::Esc | KeyCode::Char('q' | 'Q') => Ok(InputOutcome::ExitJackin)` in <RepoFile path="crates/jackin/src/console/tui/input/list.rs" />, while `should_open_quit_confirm()` in <RepoFile path="crates/jackin-console/src/tui/run.rs" /> deliberately does not fire for bare `q` on the main list (because `is_on_main_screen` returns true there), so bare `q`/`Esc` bypass even the confirm that `Ctrl+Q` would open.

An audit of all surfaces (host console, launch cockpit, capsule, shared `jackin-tui`, plus the lookbook) found the same divergence pattern everywhere — dozens of live instances catalogued below.

## Why It Matters

Keybindings are a contract with the operator, and the hint bar is the only place that contract is published. When a key is handled but not advertised, the operator cannot discover it and can trigger it by accident (a reflexive `q` quits the whole app). When a hint advertises a key the widget does not handle, the operator presses it and nothing happens, eroding trust the way an inert `Ctrl+C` does. A correct architecture makes these states unrepresentable; today it permits all of them, and the count of live divergences is the evidence that manual discipline does not hold.

## Design

### Root-cause analysis

There is **no single source of truth binding a key → action → hint label.** Dispatch and advertisement are two disjoint code paths that share facts only by hand.

- `HintSpan::Key(&str)` in <RepoFile path="crates/jackin-tui/src/geometry.rs" /> is free-text presentation. The string `"Ctrl+Q"` has no connection to any `KeyCode`; the hint renderer only measures and styles strings.
- Every component's `handle_key` is a standalone `match key.code` with no reference to any hint list: `ConfirmState::handle_key` in <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" />, `TextInputState::handle_key` in <RepoFile path="crates/jackin-tui/src/components/text_input.rs" />, `SelectListState::handle_key` in <RepoFile path="crates/jackin-tui/src/components/select_list.rs" />, plus every per-surface dispatcher in <RepoFile path="crates/jackin/src/console/tui/input/list.rs" />, <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" />, and <RepoFile path="crates/jackin-capsule/src/tui/input.rs" />.
- Hints are hand-written constants/builders in different files (often different crates) from the handler: <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" />, <RepoFile path="crates/jackin-launch/src/tui/components/prompts.rs" />, <RepoFile path="crates/jackin-capsule/src/tui/components/dialog/hint.rs" />.

No test asserts that a handler's key set matches its hint; the only hint tests check column-width math. So a key can be handled without a hint, a hint can name a key no handler acts on, and a hint can name a different key than the one handled.

The pattern is already proven achievable in this codebase — just not applied to keys. Mouse geometry is unified: `StatusBarPlan` in <RepoFile path="crates/jackin-capsule/src/tui/components/status_bar.rs" /> is the single source for both paint and hit-test of clickable regions, and `box_rect` in <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" /> is shared between render and click hit-testing. Scroll is near-unified: `ScrollAxes` feeds both `scroll_hint_spans(axes)` (advertise) and `handle_key_for_axes` (dispatch) in <RepoFile path="crates/jackin-tui/src/components/dialog_layout.rs" />, so advertised arrows and accepted arrows agree on availability. Keys have no analogue, and that absence is the enabling structure.

### The fix: one binding table per context, feeding both render and dispatch

Replace "free-text hint + independent match" with a binding entry that couples everything currently split. The hint bar renders the `Shown` entries — it can no longer advertise a key with no binding nor omit a `Shown` one. The dispatcher matches the incoming key against the table's chords and returns the action — a key absent from the table is, by construction, not handled, so no hidden behaviour is possible. Aliases are explicit rows (`HiddenAlias`), so `h/j/k/l` and lowercase `y`/`n` keep working but are registered, not silently bolted onto a match arm. `HintSpan::Key` stops being an author-supplied string and is derived from the binding's chord, so labels and handlers cannot disagree.

### Registry API spec (concrete)

A new module `crates/jackin-tui/src/keymap.rs` (self-named, per the crate's no-`mod.rs` rule), declared `pub mod keymap;` in <RepoFile path="crates/jackin-tui/src/lib.rs" /> and re-exported through <RepoFile path="crates/jackin-tui/src/components.rs" />. Tests in `keymap/tests.rs`. It builds on existing `HintSpan` (geometry.rs), `ModalOutcome` (lib.rs), and `ScrollAxes` (scroll.rs) — none replaced; the registry sits beside them and *produces* `HintSpan`, *consumes* `ScrollAxes` for axis-gated visibility.

**Neutral logical key** — both `crossterm::event::KeyEvent` (host/launch) and the capsule's raw-byte parser must construct and match the same type:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum LogicalKey {
    Char(char),
    Enter, Esc, Tab, BackTab,
    Up, Down, Left, Right,
    Home, End, PageUp, PageDown,
    Backspace, Delete,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]
pub struct Mods { /* CTRL | ALT | SHIFT bit flags (u8 mask) */ }

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub struct KeyChord { pub key: LogicalKey, pub mods: Mods }
```

**Binding + map:**

```rust
pub enum Visibility { Shown, HiddenAlias, Internal }

pub struct KeyBinding<A> {
    pub chords: &'static [KeyChord], // first chord drives the hint glyph; rest are aliases
    pub action: A,
    pub hint: Option<&'static str>,  // label after the glyph ("confirm", "cancel", …); None for Internal
    pub visibility: Visibility,
}

pub struct Keymap<A: 'static> { bindings: &'static [KeyBinding<A>] }

impl<A: Copy + 'static> Keymap<A> {
    pub fn dispatch(&self, chord: KeyChord) -> Option<&A>;      // first binding whose chords contain chord
    pub fn hint_spans(&self) -> Vec<HintSpan<'static>>;          // Shown bindings → Key+Text spans, GroupSep-joined
    pub fn hint_spans_for_axes(&self, axes: ScrollAxes) -> Vec<HintSpan<'static>>; // fold in scroll_hint_spans gating
}
```

`A` is a per-widget action enum mapped back to `ModalOutcome<T>` by the widget's thin dispatch shim, so the `ModalOutcome` contract is untouched. Case-folding (`y`/`Y`, `o`/`O`) uses the explicit-alias model: list both `Char('y')` and `Char('Y')` as chords so the `HiddenAlias` visibility stays representable.

**Glyph derivation** — `chord_glyph` must reproduce the exact glyphs already in use so output is byte-identical to today: `Ctrl+Q`; `↑↓` (`\u{2191}\u{2193}`); `↑↓←→`; `←→`; `↵` (`\u{21b5}`); `Esc`; `Y`; `⇥` (`\u{21e5}`). Grouped glyphs (`↑↓`, `↵/Esc`) map from a small binding-level lookup, not a single chord.

**Per-surface adaptation (incremental — no event-loop rewrite):** crossterm surfaces add `impl From<KeyEvent> for KeyChord`; a widget's `handle_key` body becomes `match keymap.dispatch(key.into()) { Some(a) => self.apply(a), None => /* text Char insert fallthrough */ }`. The capsule adds a byte→`KeyChord` parser; `handle_raw_key_for_axes` stays as a migration fallback. Migrate one widget at a time; `ConfirmState` is the pilot (worst divergence). Delete the hand-built footer functions only once every consumer reads `keymap.hint_spans()`.

**Enforcement (so the class cannot return):**

1. The binding table makes dispatch and hint the *same data* — divergence becomes unrepresentable for `Shown`/`HiddenAlias` bindings.
2. Per-context test: drive the real `handle_key` with every chord over the full `LogicalKey × Mods` space; any chord the handler *consumes* (not `ModalOutcome::Continue` and no focus change) must resolve via `keymap.dispatch(chord).is_some()`, and vice-versa. This is exactly the test that catches the ContainerInfo `↵ copy value` phantom (Enter is advertised but `handle_scroll_key` falls through).
3. CI lint/test forbidding `handle_key` match arms outside a binding table on migrated surfaces. The repo already proves this is enforceable for modals: `modal_footer_items` is an exhaustive `match` (compile-enforced) in the console footer code.

## Divergence catalogue and executable checklist

Verdicts: **HIDDEN-KEY** (handled, not advertised) · **PHANTOM-HINT** (advertised, not handled) · **LABEL-MISMATCH** · **DEAD-ARM/DEAD-STRING** (unreachable) · **STALE-COMMENT**. The dominant class is HIDDEN-KEY.

### Console — host manager (`jackin-console` + `jackin/src/console`)

Quit-policy spine: <RepoFile path="crates/jackin/src/console/tui/run.rs" /> calls `should_open_quit_confirm` before any stage handler; that predicate (in <RepoFile path="crates/jackin-console/src/tui/run.rs" />) opens the confirm on `Ctrl+Q` always, or bare `q`/`Q` only when `!on_main_screen && !consumes_letter_input`. `is_on_main_screen` is true on the workspace list, which is why bare `q` there falls through to the silent-exit arm.

**A. Main-list silent quit / policy (highest priority).**
- [x] `input/list.rs` `Esc | Char('q'|'Q') => ExitJackin` arm: already removed (was cleaned by Phase 0 C). `Esc` is a no-op (`Continue`) on the main list; `q/Q` has no arm → falls through to `Continue`. No silent exit.
- [x] `jackin-console` `run.rs` `should_open_quit_confirm`: bare `q/Q` opens quit-confirm on all screens *except* the main workspace list (`on_main_screen` gate). On main list, `q/Q` is silently ignored (no silent exit, no confirm). Policy: main-list `q` is a no-op; `Ctrl+Q` opens confirm everywhere. This is acceptable for now; a future pass can also open confirm on main-list bare `q` if desired.

**B. Inline picker `exit_on_q` symmetry.**
- [x] `input/list.rs` (`~:594` role picker passes `inline_picker_shell_plan(key, true)`; `~:621` agent picker passes `false`): unified to `false` for both — `q` filters, quit relies on `Ctrl+Q`.
- [x] `footer_hints.rs` (`~:86`, `AgentPicker` mode): changed to `include_quit=true` — `Ctrl+Q quit` now shown (parity with role picker).
- [x] `footer_hints.rs` `workspace_picker_footer_items`: added `type to filter` span.

**C. Dead settings arms (delete the `q`/`Q` leg; keep `Esc`).**
- [x] `input/global_mounts.rs` lines `~:272`, `~:349`, `~:434`, `~:517` — `| KeyCode::Char('q' | 'Q')` removed from all four arms.
- [x] `input/global_mounts/auth.rs` `~:19` and `~:49` — `| KeyCode::Char('q' | 'Q')` removed from both arms.
- [x] `input/global_mounts/auth.rs` `~:61` — empty no-op arm `Char('d' | 'D') => {}` deleted.

**D. Vim `h/j/k/l` aliases — register as `HiddenAlias`.** All sites already handle these keys inline (no UX gap, just a registry-coupling gap). Full migration deferred to Phase 2 per-screen dispatch work. Sites for reference: `input/list.rs` (`~:84/89/94/103`); `screens/workspaces/update.rs` preview pane (`~:311/312/313`) plus `→` as alias of `⇥` into preview (`~:277`); `input/editor.rs` (`~:126/146/164/178/192/203/214/228`); `input/global_mounts.rs` (`~:97/214/225/236/247/319/330/408/415/461/472/483/494`) and `auth.rs` (`~:23/30`); inline pickers in `update.rs` (`~:147/148`) and `agent_choice.rs` (`~:51/58`, also add `J`/`K` for parity with list/editor which accept `j|J`/`k|K`).

**E. Instance-row hidden actions.**
- [x] `input/list.rs` `~:212` `i/I` Inspect: added `I info` to `InstanceRow` footer items in `footer_hints.rs` — now discoverable.
- [x] `input/list.rs` `~:197-222` `r/a/x/i/p/t` on a workspace row with no instances: `instance_action_outcome` now silently returns `Continue` when `has_any_instance_for_selection` is false — eliminates unadvertised error popups. Popup is still shown when an instance exists but is in the wrong state (action rejected for that status).
- `r/R` and `a/A` as aliases for Enter/N: left as-is (redundant but not harmful; keeping them unadvertised is acceptable until full keymap migration).

**F. Editor Auth `D reset` coverage.**
- [x] `footer_hints.rs` `~:339` (`AuthRowFooterMode::EditMode`): added `D reset` — WorkspaceMode/RoleMode rows now advertise the `d/D` handled in `auth.rs`.

**G. OpPicker refresh label + Field collapse hints.**
- [x] `op_picker/input.rs` `~:62/118/200/357`: changed `Char('r')` → `Char('r' | 'R')` so the advertised `R refresh` matches the handler.
- [x] `op_picker/input.rs` `~:394/406`: `←/→ collapse/expand section` added to Field-stage footer — `ModalFooterMode::FilteredPicker` gained `include_collapse: bool`; `op_picker_modal_footer_mode` sets `true` for `OpPickerStage::Field`.

**H. File browser + git prompt (found by completeness pass).**
- [x] <RepoFile path="crates/jackin-console/src/tui/components/file_browser/state.rs" /> (`~:54-70`): hint updated to `↑↓/j/k navigate`, `↵/l open`, `H/h/← up` — vim aliases now advertised.
- [x] <RepoFile path="crates/jackin-console/src/tui/components/file_browser/git_prompt.rs" /> (`~:222-240`): `git_prompt_footer_items` extended with `⇥/→ next · ⇤/← prev · ↵ select` group — focus-nav and commit keys now advertised.

**I. Optional discoverability.**
- [x] Editor/create/settings footers: `append_save_and_escape` now appends `· Ctrl+Q quit` after the `Esc back/discard` span — `Ctrl+Q` is advertised on every surface it intercepts.

### Launch cockpit (`jackin-launch`)

Global facts: `Ctrl+C` (HardExit) and `Ctrl+Q` (open quit-confirm) are evaluated in `handle_cockpit_input` before modal routing; the pre-pipeline prompt loops use `read_pressed_key` in <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> which treats both `Ctrl+C` and `Ctrl+Q` as cancel.

**Comment fixes (no behaviour change):**
- [x] `subscriptions.rs` (`~:409`): reworded to "Yes confirmed: graceful cancel".
- [x] `build_log_dialog.rs` (`~:189`) and `subscriptions.rs` (`~:279`): comments updated to `Esc closes`.

**Hint reconciliations:**
- [x] `failure_dialog.rs` `FAILURE_HINT`: added `Ctrl-C abort · Ctrl+Q quit` group.
- [x] `prompts.rs` `CONFIRM_HINT`: replaced with `confirm_hint_spans()` (Phase 2).
- [x] `prompts.rs` `ERROR_POPUP_HINT`: replaced with `error_popup_hint_spans()` (Phase 2).
- [x] `prompts.rs` `TEXT_PROMPT_HINT` / `TEXT_PROMPT_SKIP_HINT`: cancel group updated to `Ctrl-C/Ctrl+Q/Esc cancel`.
- [x] `prompts.rs` `PICKER_HINT`: eliminated — `draw_select` now calls `select_list_hint_spans()` (Phase 2). Ctrl+Q and Esc visibility handled at caller level.
- [x] Cockpit quit-confirm: `draw_confirm` renders `confirm_hint_spans()` via hint bar (Phase 2).
- [x] Container-info overlay: `debug_info_hint_spans` includes `↵ copy value` and `Esc dismiss`; Ctrl-C/Ctrl+Q appended by caller. Vim scroll aliases now shown: `scroll_hint_spans` updated to `↑↓/j/k` / `←→/h/l` / `↑↓/j/k · ←→/h/l` — applies across all dialogs using the shared scroll hint.
- [ ] Cosmetic: `Ctrl-C` (hyphen) vs `Ctrl+Q` (plus) glyph normalisation deferred pending full registry ownership.

### Capsule (`jackin-capsule`)

The capsule decodes raw PTY bytes into `InputEvent` rather than `crossterm::KeyEvent`. The byte-level chords are exact below.

- [x] `dialog/hint.rs` `main_view_hint`: added `Ctrl+Q quit` to both main and scrollback branches.
- [ ] `dialog/hint.rs` `main_view_hint`: add an `Alt+Shift+◆ resize` span (or a help overlay) for `ResizePane` (`input.rs ~:762`, CSI `\x1b[1;4A/B/C/D`).
- [ ] Prefix keymap: the entire opt-in `prefix_binding` set (`input.rs ~:585-602`: `c n p 0-9 " % h j k l z x & Ctrl+L d space :`) is handled but never advertised. Add a discoverable surface (help dialog, or extended footer when `InputParser::prefix_enabled()`); at minimum advertise `prefix space/:` → palette.
- [ ] `input.rs` `~:602`: `b'r' => Redraw` maps to no action (`message.rs ~:217` returns None) — remove or give `PrefixCommand::Redraw` a real action.
- [x] `dialog.rs` ConfirmAction: migrated to `CONFIRM_KEYMAP.dispatch(raw_bytes_to_chord(key))` (Phase 2). `↵ confirm` (CommitFocused), `Y yes`, `N/Esc no`, `⇥ focus` all shown. `Ctrl+C` and arrow/`Tab` toggle are `HiddenAlias`.
- [x] `dialog.rs` SS3 arrows: `raw_bytes_to_chord` decodes both CSI `\x1b[C`/`\x1b[D` and SS3 `\x1bOC`/`\x1bOD` as `LogicalKey::Right`/`Left` — ToggleFocus fires on both terminal modes.
- [x] `dialog.rs` `~:254` and `~:264`: `to_ratatui_snapshot` now passes empty strings for Exit (avoids formatting dead code); title/message arms in `ConfirmKind` kept for non-Exit variants which still need them. Exit width fix: box_rect arm for ConfirmKind::Exit uses 70% of terminal width so warning notes don't truncate.
- [x] Read-only info dialogs: `READ_ONLY_HINT` updated to `q/Esc dismiss` matching actual `is_dismiss_key` accept set.
- [ ] `Ctrl+C` dismiss policy: `is_filter_dismiss_key` / `is_dismiss_key` accept `Ctrl+C` everywhere (palette, pickers, rename, confirm, info) but no hint shows it — decide one policy and reflect it across `PALETTE_HINT`/`PICKER_HINT`/`RENAME_HINT`/`CONFIRM_HINT`.
- [x] ProviderPicker phantom: added dedicated `PROVIDER_HINT` = `↑↓ navigate · ↵ select · Esc cancel`; wired in `dialog.rs hint_spans()`.
- [ ] Status-bar label drift: `status_bar.rs` (`~:114-115`) hardcodes `Ctrl+\` / `Ctrl+B` labels while <RepoFile path="crates/jackin-capsule/src/services/input_bindings.rs" /> makes those keys env-configurable (`JACKIN_PREFIX` / `JACKIN_PALETTE_KEY`); under an override the advertised label diverges from the handled byte. Derive the label from the resolved binding (and verify `palette_label`/`prefix_label` are actually rendered — `button_text_for` at `~:206` may make them dead fields).
- [ ] Optional: advertise that `Ctrl+\` (`0x1C`) closes any open dialog (`palette_toggle_route` → CloseDialog), or accept as a deliberate global.

**Neutral-key requirements (for `LogicalKey`/`KeyChord`):** the capsule parser must produce, and the registry represent: `Char` (printable UTF-8); `Enter` (`\r`/`\n` collapsed); `Esc` (bare `\x1b` + CSI-u `CSI 27…u`); `Backspace` (`\x7f`/`\x08`); `Tab` (`\t`); `Ctrl(letter)` (`0x01-0x1A`, incl. `Ctrl+C`=`\x03`, `Ctrl+Q`=`0x11`, `Ctrl+L`=`0x0c`, `Ctrl+H`=`0x08`); `Ctrl(symbol)` (`0x1C-0x1F`, `Ctrl+\`=`0x1C`); `Arrow{Up,Down,Left,Right}` unifying legacy CSI `\x1b[A-D`, SS3 `\x1bOA-D`, and kitty `\x1b[1;<mod>[:<event>]<final>`; `Arrow+Mods` (Alt/Shift, e.g. Alt+Shift=mod 4 for resize) with an event kind (Press/Repeat/Release) so Release is dropped uniformly; `FocusIn`/`FocusOut` (`\x1b[I`/`\x1b[O`); bracketed-paste boundaries as a paste mode (not chords); mouse events (separate from chords); and a "suppress/no-op" outcome for normalized specials (Shift+Enter modifyOtherKeys, kitty key-release).

### Shared `jackin-tui` widgets

The registry must collapse these per-widget divergences. `ConfirmState::handle_key` (<RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" />, `~:108-127`) handles `y Y n N Tab BackTab ← → h l Enter Esc`; no single hint advertises that set — the console `ConfirmDismiss` mode shows `↵ confirm · Esc cancel` (hides y/Y/n/N) while the `YesNo` mode shows `Y yes · N/Esc no` (hides Enter and the focus keys). `TextInputState` (`text_input.rs ~:247-289`) handles editing keys (Backspace/Delete/Left/Right/Home/End) + a deliberate `Ctrl+m` swallow, none advertised (register `Internal`). `SelectListState` (`select_list.rs ~:104-147`) handles `Left/Right/Home` horizontal scroll + `Backspace` filter-edit unadvertised, and the two console footer modes for it disagree on whether `↵`/`Esc` are shown. `ErrorPopupState` (`error_dialog.rs ~:33-38`) handles `o`/`O` dismiss unadvertised (register `HiddenAlias` — it matches the rendered "OK" button). `ContainerInfoState` (`container_info.rs ~:213-244`) handles `q`/`Q` dismiss + vim scroll unadvertised, and advertises `↵ copy value` it does **not** handle (the caller copies) — the canonical PHANTOM-HINT; resolve by moving copy into the widget keymap as a `CopyValue` action or marking the binding caller-owned.

- [x] All five shared widgets migrated onto per-widget keymaps: `ConfirmState`, `TextInputState`, `SelectListState`, `ErrorPopupState`, `SaveDiscardState` (Phase 2). `ContainerInfoState` copy phantom deferred (see Phase 2 task).
- [x] Dead canonical-hint constants removed from `hint_bar.rs`: `SELECT_CANCEL_HINT`, `DISMISS_HINT`, `DISMISS_CANCEL_HINT`, `CONFIRM_DISMISS_HINT` (Phase 3).
- [ ] Fold `scroll_hint_spans` / `handle_key_for_axes` (`dialog_layout.rs`) into the registry's axis-gated hint path rather than duplicating the gating.

### Lookbook (dev tool, low priority)

- [x] <RepoFile path="crates/jackin-tui-lookbook/src/main.rs" /> `SIDEBAR_HINT` and `PREVIEW_FOCUS_HINT`: updated to include `Home/End first/last` (sidebar) and `PgUp/PgDn page` (preview) in Phase 0. Further registry migration deferred (lookbook is internal tooling).

## Mandatory hint bar on every operational screen

An additional requirement was added after the audit: every operational TUI screen must carry a visible hint bar. Intro and outro animations are the only exceptions. Screens where an operator makes a selection, confirms an action, or navigates a list must always advertise the available keys — the hint bar is the only discovery surface.

An audit of all four surfaces (console, launch cockpit, capsule, lookbook) confirmed no screen was completely missing a hint bar call. The gaps found were incomplete hints — keys handled but not advertised in an existing hint bar. Six sites were fixed in this branch:

| Surface | File | Fix |
|---|---|---|
| Launch | `prompts.rs CONFIRM_HINT` | Added `↵ confirm` |
| Console | `run.rs CONFIRM_HINT` | Added `↵ confirm` |
| Capsule | `dialog/hint.rs CONFIRM_HINT` | Added `↵ select` and `⇥ toggle`; normalised `N/Esc no` |
| Capsule | `dialog/hint.rs main_view_hint` | Added `Ctrl+Q quit` to the non-scrollback branch |
| Lookbook | `main.rs SIDEBAR_HINT` | Added `Home/End first/last` |
| Lookbook | `main.rs PREVIEW_FOCUS_HINT` | Added `PgUp/PgDn page` |

Remaining hint gaps (handled but unadvertised) are in the divergence catalogue above and are addressed as part of Phase 2.

## Tasks

**Phase 0 — Stop the dangerous divergence + visible truncation (correctness, independent of the refactor):**
- [x] Console main-list silent quit (catalogue A): `q/Q` arm removed from all Esc arms in `global_mounts.rs` and `auth.rs`; main list has no `q/Q` arm → silent no-op. Policy: bare `q` on main list is no-op; `Ctrl+Q` opens confirm everywhere.
- [x] Console inline-picker `exit_on_q` symmetry (catalogue B): both role-picker and agent-picker now pass `exit_on_q=false`; `Ctrl+Q` is the sole quit path from inline pickers.
- [x] Launch stale comments: `subscriptions.rs` "Ctrl+Q confirmed" → "Yes confirmed"; `build_log_dialog.rs` and `subscriptions.rs` "`q` closes" → "`Esc` closes".
- [x] Capsule exit-confirm box-width truncation: `dialog.rs box_rect` — `ConfirmKind::Exit` now uses 70% terminal width instead of hardcoded `PALETTE_WIDTH`.
- [x] Mandatory hint bar audit: confirmed all screens have hint bars; fixed six missing-key gaps (see table above).
- [x] Launch hint reconciliations (catalogue launch section): FAILURE_HINT, TEXT_PROMPT_HINT/SKIP, PICKER_HINT; scroll_hint_spans updated with vim aliases.
- [x] Capsule hint reconciliations (catalogue capsule section): main_view_hint Ctrl+Q both branches, READ_ONLY_HINT q/Esc, PROVIDER_HINT dedicated constant, Exit dead-string cleanup.

**Phase 1 — Registry foundation (`jackin-tui`):**
- [x] `LogicalKey` / `Mods` / `KeyChord` neutral key types; `From<crossterm::event::KeyEvent> for KeyChord` for crossterm surfaces; byte→`KeyChord` parser left for capsule migration (Phase 2).
- [x] `KeyBinding<A>`, `Keymap<A>`, `Visibility`, `chord_glyph`, `Keymap::dispatch`/`hint_spans`/`hint_spans_for_axes` in <RepoFile path="crates/jackin-tui/src/keymap.rs" />; 9 unit tests in `keymap/tests.rs`; `pub mod keymap;` in `lib.rs`; re-exported through `components.rs`.

**Phase 2 — Migrate every surface onto the registry:**
- [x] Shared widgets: `ConfirmState`, `ErrorPopupState`, `SaveDiscardState`, `SelectListState`, `TextInputState` all migrated to per-widget `Keymap<A>`; each exports a `*_hint_spans()` function used by all callers.
- [x] `raw_bytes_to_chord(bytes: &[u8]) -> Option<KeyChord>` added to `keymap.rs`; covers Enter/Esc/Tab/Backspace/printable ASCII/Ctrl+letter/CSI+SS3 arrows and arrow+modifier sequences.
- [x] Capsule `ConfirmAction` dispatch (dialog.rs `~:715`) migrated to `CONFIRM_KEYMAP.dispatch(raw_bytes_to_chord(key))`; `CONFIRM_HINT` in `dialog/hint.rs` replaced with `confirm_hint_spans()`.
- [x] Launch `prompts.rs`: `draw_confirm` uses `confirm_hint_spans()`, `draw_select` uses `select_list_hint_spans()`, `draw_error_popup` uses `error_popup_hint_spans()`.
- [x] Console `run.rs`: quit-confirm hint bar uses `confirm_hint_spans()`.
- [x] Console `footer_hints.rs`: `error_popup_footer_items` uses `error_popup_hint_spans()`; `save_discard_cancel_footer_items` uses `save_discard_hint_spans()`; `ConfirmDismiss`/`OpNamingTextInput` modes use `text_input_hint_spans()`.
- [ ] Capsule `input.rs`/`dialog/input.rs` full dispatch (Ctrl+Q, prefix keys, read-only dismiss, resize, palette); `status_bar.rs` env-configurable labels.
- [ ] Launch `subscriptions.rs`/`run.rs` dispatch + failure-popup and pre-pipeline overlay hints.
- [ ] Console `input/*.rs` + `file_browser`/`git_prompt` dispatch and remaining `footer_hints.rs` builders; register vim aliases and workspace-row/instance actions; delete dead settings arms.
- [ ] `ContainerInfoState` phantom `↵ copy value` hint (callers own copy, not widget) — resolve by adding `CopyValue` action to widget keymap or marking caller-owned.
- [ ] Lookbook hints: `SIDEBAR_HINT` and `PREVIEW_FOCUS_HINT` remain hand-written — lookbook navigation is unique (not a standard component), so registry derivation would require new keymaps with no operator-visible benefit. Deferred; acceptable since lookbook is internal tooling.

**Phase 3 — Enforcement and cleanup:**
- [x] Per-context chord-coverage tests in `keymap/tests.rs`: `assert_all_chords_dispatch` + `assert_has_shown_hints` applied to all five shared-widget keymaps; `raw_bytes_to_chord` round-trip tests including `CONFIRM_KEYMAP` dispatch via raw bytes.
- [x] Dead canonical-hint constants removed from `hint_bar.rs`: `SELECT_CANCEL_HINT`, `DISMISS_HINT`, `DISMISS_CANCEL_HINT`, `CONFIRM_DISMISS_HINT` (all had zero callers; consumers migrated to registry-derived functions).
- [ ] CI lint/test forbidding `handle_key` arms outside a binding table on migrated surfaces (remaining console/capsule surfaces — deferred until those surfaces are migrated).
- [ ] Remove now-unused hand-built footer builders once console/capsule migrations complete.

**Validation:**
- [ ] Re-run the four-surface audit + completeness pass; confirm the catalogue is empty.
- [ ] Manual smoke per surface: every advertised key works; no unadvertised key changes app state in a way the operator was not told about.

## Related Files

- <RepoFile path="crates/jackin-tui/src/geometry.rs" /> — `HintSpan`, the free-text hint vocabulary
- <RepoFile path="crates/jackin-tui/src/components/hint_bar.rs" /> — hint-bar render + the dead canonical-hint constants
- <RepoFile path="crates/jackin-tui/src/components/confirm_dialog.rs" /> — `ConfirmState::handle_key` (shared hidden-key set)
- <RepoFile path="crates/jackin-tui/src/components/text_input.rs" /> — `TextInputState::handle_key`
- <RepoFile path="crates/jackin-tui/src/components/select_list.rs" /> — `SelectListState::handle_key`
- <RepoFile path="crates/jackin-tui/src/components/error_dialog.rs" /> — `ErrorPopupState::handle_key`
- <RepoFile path="crates/jackin-tui/src/components/container_info.rs" /> — `ContainerInfoState` (the `↵ copy value` phantom hint)
- <RepoFile path="crates/jackin-tui/src/components/save_discard_dialog.rs" /> — `SaveDiscardState::handle_key`
- <RepoFile path="crates/jackin-tui/src/components/dialog_layout.rs" /> — `scroll_hint_spans` / `handle_key_for_axes` (the existing single-source precedent for scroll)
- <RepoFile path="crates/jackin/src/console/tui/input/list.rs" /> — the reported silent-quit arm
- <RepoFile path="crates/jackin-console/src/tui/run.rs" /> — `should_open_quit_confirm`, the quit policy
- <RepoFile path="crates/jackin-console/src/tui/components/footer_hints.rs" /> — console hint vocabulary
- <RepoFile path="crates/jackin-launch/src/tui/subscriptions.rs" /> — launch cockpit dispatch
- <RepoFile path="crates/jackin-launch/src/tui/run.rs" /> — `read_pressed_key`, pre-pipeline prompt loops
- <RepoFile path="crates/jackin-launch/src/tui/components/prompts.rs" /> — launch hint constants
- <RepoFile path="crates/jackin-capsule/src/tui/input.rs" /> — capsule byte parser, `Ctrl+Q`/prefix chords
- <RepoFile path="crates/jackin-capsule/src/tui/components/dialog.rs" /> — `ConfirmAction`, `box_rect` width, dead Exit strings
- <RepoFile path="crates/jackin-capsule/src/tui/components/dialog/hint.rs" /> — capsule hint vocabulary
- <RepoFile path="crates/jackin-capsule/src/services/input_bindings.rs" /> — env-configurable prefix/palette keys
