---
title: "Behavioral spec: op_picker"
---

**Status**: Partially implemented — spec published (2026-06-02); next step is linking from the `op_picker/mod.rs` `//!` doc to the spec URL

## Problem

`src/console/tui/op_picker/` is now split across root-console adapter files for state, input, load execution, and root commit selection, while the surface-local render/state-plan component lives in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs">crates/jackin-console/src/tui/components/op_picker.rs</RepoFile>. The old single-file analysis was ~1,712 lines; the current split still lacks an INV-format invariant contract. The 4-level drill-down state machine has subtle invariants around the `op://` reference format, the absence of secret values in the picker path, and the collapsible-section navigation layer added to the field stage.

The renderer now lives with the surface-local op-picker component in <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs">crates/jackin-console/src/tui/components/op_picker.rs</RepoFile>; the root `OpPickerState` implements that crate-owned render trait.

## Key invariants

| INV | Description | Verify by |
|---|---|---|
| INV-1 | Field selection commits `OpField::reference` verbatim when present; only fixtures missing `reference` use the synthesized `op://<vault>/<item>/<label>` fallback | selection path returns `field.reference.clone()` first, then falls back to `format!("op://{}/{}/{}"...)` |
| INV-2 | No secret values in picker path — `RawOpField` struct has no `value` field; serde drops it silently | grep `value` in op_picker/mod.rs; exhaustive destructure test at `operator_env.rs:~2055` |
| INV-3 | Loading is async (background worker + channel); key handlers stay synchronous and only advance UI state / `poll_load()` | background loaders use `std::thread::spawn`; key handlers do not call the CLI directly |
| INV-4 | `field_list_state.selected` indexes the *display* row list (returned by `build_field_display_rows()`), not `filtered_fields()` directly. Section-header rows are navigable and selectable; Enter on a header toggles its `collapsed_sections` membership rather than committing a field | display rows built from `FieldDisplayRow` enum; collapse/expand via `collapsed_sections: HashSet<String>`; `reset_selection_for_filter` and Up/Down both use `build_field_display_rows().len()` |

## State machine

4 stages: `Accounts` → `Vaults` → `Items` → `Fields`. Each stage has a background loader and a key handler. Loaders post results via channel; `poll_load()` drains them before each render.

The field stage has an extra navigation layer: fields belonging to named 1Password sections (detected from `OpField::reference` using `parse_op_reference`) are grouped under collapsible `SectionHeader` rows. Unsectioned fields render first. Navigation (Up/Down), collapse (Left), and expand (Right/Enter-on-header) all operate on the full display-row list, not the underlying field slice.

## Status

**Spec published** — see [op_picker Behavioral Spec](/reference/developer-reference/specs/op-picker/).

## Related files

- `src/console/tui/op_picker/` — root-console adapter for state, loader, key handler, and root commit selection
- <RepoFile path="crates/jackin-console/src/tui/components/op_picker.rs">crates/jackin-console/src/tui/components/op_picker.rs</RepoFile> — surface-local op-picker render/state-plan helpers including collapsible section headers
- <RepoFile path="crates/jackin-env/src/resolve.rs">crates/jackin-env/src/resolve.rs</RepoFile> — uses `RawOpField`; exhaustive destructure test; `parse_op_reference` used by `build_field_display_rows`
