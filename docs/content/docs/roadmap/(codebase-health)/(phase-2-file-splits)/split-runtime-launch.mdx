---
title: "Split runtime/launch.rs"
---

**Status**: Partially implemented — launch is split into a root API and child launch modules; remaining work is reducing the still-large launch pipeline and tests

## Current shape

<RepoFile path="crates/jackin-runtime/src/runtime/launch.rs">crates/jackin-runtime/src/runtime/launch.rs</RepoFile> is now a smaller root API file. Child modules exist under `crates/jackin-runtime/src/runtime/launch/`, including `launch_pipeline.rs`, `launch_dind.rs`, and `launch_slot.rs`.

The split removed the old single-file bottleneck, but the launch pipeline remains the dense review surface. The next task is not another top-level move; it is making the pipeline phases and their tests easier to audit.

## Remaining split

Focus on <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs">crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs</RepoFile> and its colocated test surface. Keep Rust 2024 self-named module layout and the crate rule that module tests live in one `tests.rs` file.

Candidate production boundaries:

- workspace/materialization preparation;
- trust and role-source checks if still pipeline-local;
- Docker resource planning and launch plan construction;
- env/auth resolution and diagnostics;
- final attach/cleanup handoff.

## Remaining work

1. Re-measure `launch_pipeline.rs` and its `tests.rs` before splitting.
2. Extract only cohesive pipeline phases; do not reintroduce circular dependencies around trust confirmation.
3. Move repeated Docker naming and launch-plan helpers behind shared runtime helpers where duplication still exists.
4. Add or update behavioral-spec links after each extracted phase has stable invariants.

## Related files

- <RepoFile path="crates/jackin-runtime/src/runtime/launch.rs">crates/jackin-runtime/src/runtime/launch.rs</RepoFile> — public launch API
- <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs">crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs</RepoFile> — remaining dense pipeline surface
- <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_dind.rs">crates/jackin-runtime/src/runtime/launch/launch_dind.rs</RepoFile> — DinD/network launch helper
- <RepoFile path="crates/jackin-runtime/src/runtime/launch/launch_slot.rs">crates/jackin-runtime/src/runtime/launch/launch_slot.rs</RepoFile> — container-slot helper
