---
title: "jackin' Desktop Agent Hub goal prompt"
---

This page is an execution prompt, not the product proposal. Copy the prompt
below when starting the implementation program.

Source of truth:

- [jackin' daemon](/reference/roadmap/jackin-daemon/)
- [jackin' Desktop Agent Hub roadmap](/reference/roadmap/jackin-desktop-agent-hub/)
- [Roadmap overview](/reference/roadmap/)

## Goal prompt

```text
/goal Continue PR #317 on branch `jackin/desktop-attention-routing` and implement the daemon + Desktop Hub scope described by these source-of-truth roadmap items:

- docs/src/content/docs/reference/roadmap/jackin-daemon.mdx
- docs/src/content/docs/reference/roadmap/jackin-desktop-agent-hub.mdx

Pull request: https://github.com/jackin-project/jackin/pull/317

Work only on this PR branch. Do not create stacked or follow-up implementation PRs unless the operator explicitly changes the delivery plan.

Before implementing, read AGENTS.md, PULL_REQUESTS.md, COMMITS.md, and the two roadmap items above. Treat the roadmap details as the spec; do not duplicate them into code comments or PR prose. If implementation reveals a contract mismatch, update the relevant roadmap item in PR #317 before coding around it.

Primary outcome: PR #317 should land the first coherent daemon-backed macOS Desktop Hub. The daemon is the API/data/action layer. The macOS app is the operator-facing surface. Together they must make the in-scope roadmap requirements usable enough for operator smoke testing, especially workspace visibility, running-instance visibility, per-instance branch/PR correlation, and GitHub + DiffsHub PR actions.

Keep the app native macOS and daemon-backed. Desktop must not parse jackin' TOML, Docker output, or Capsule socket frames directly. Keep Ghostty as the v1 terminal target. Keep host-side effects explicit and scoped.

Allowed deferrals are only the deferrals named by the roadmap items and PR #317 description. Keep the PR description current with what shipped, what remains deferred, and exact verification results.

Verification: run focused Rust tests for the daemon/API work, the strongest practical Swift build or smoke validation for the macOS app, and docs checks from docs/ when docs change.
```
