---
title: "Update PROJECT_STRUCTURE.md"
---
import RepoFile from '../../../../components/RepoFile.astro'

**Status**: Partially implemented — most of the original drift has been resolved by rewriting <RepoFile path="PROJECT_STRUCTURE.md" /> into a terse pointer + ecosystem table + code↔docs cross-reference, with the detailed module map delegated to [Codebase Map](/reference/codebase-map/). Only the small workflow / root-files residual gaps remain.

## Background

The original page (October 2025) catalogued a large drift between <RepoFile path="PROJECT_STRUCTURE.md" /> and the actual repo: the `console/` widget list, the `manager/` sub-structure, missing files such as `op_cache.rs`, a stale `commands/cd.mdx` reference, an omitted `guides/environment-variables.mdx`, and an incomplete workflow table.

Most of that drift was resolved by rewriting <RepoFile path="PROJECT_STRUCTURE.md" /> as a short pointer file: it now covers the ecosystem table and the code↔docs cross-reference, and delegates the detailed module map to the new [Codebase Map](/reference/codebase-map/) reference page. The widget list, `manager/` sub-structure, `op_cache.rs`, and docs-page enumerations all moved to the codebase-map page (or were removed entirely), so they can no longer drift in the root file.

## Remaining work

Small residual gaps in <RepoFile path="PROJECT_STRUCTURE.md" />:

- The CI/CD workflows table is brought into line with `.github/workflows/` (`ci.yml`, `preview.yml`, `renovate.yml`, `renovate-validate.yml` added).
- <RepoFile path="build.rs" /> is added to the Root Files table.

Once those land the item can be retired in favour of:

1. The per-PR code↔docs contract baked into the rewritten <RepoFile path="PROJECT_STRUCTURE.md" /> (every structural PR updates the [Codebase Map](/reference/codebase-map/) in the same commit).
2. A potential CI staleness gate — see [CI project-structure gate](/reference/roadmap/ci-project-structure-gate/).

## Caveats

Purely documentation — no code change.
