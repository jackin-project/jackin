---
title: "Update PROJECT_STRUCTURE.md"
---
import RepoFile from '../../../../components/RepoFile.astro'

**Status**: Open — Phase 1, no confirmation needed

## Problem

<RepoFile path="PROJECT_STRUCTURE.md" /> has drifted in several sections. The `console/` entry still lists the pre-PR#171 widget set and omits new widgets plus the fuller `manager/` sub-structure, but the staleness is broader than that: docs navigation and workflow sections also reference files that no longer exist or omit files that now do.

## Specific gaps

Currently listed in `widgets/`: `file_browser`, `text_input`, `confirm`, `confirm_save`, `error_popup`, `mount_dst_choice`, `workdir_pick`, `github_picker`, `save_discard`, `panel_rain`

**Missing:**
- `op_picker/` (1712L, 1Password picker state machine — added in PR #171)
- `agent_picker.rs` (436L, agent disambiguation modal)
- `scope_picker.rs` (201L, workspace-vs-agent choice)
- `source_picker.rs` (244L, plain-or-1Password choice)

**Also stale:** `manager/` description doesn't reflect the actual structure: `state.rs`, `input/` (mouse.rs, editor.rs, save.rs, list.rs, prelude.rs, mod.rs), `render/` (editor.rs, list.rs, mod.rs), `create.rs`, `mount_info.rs`, `agent_allow.rs`, `github_mounts.rs`.

**Also missing:** `op_cache.rs` (session-scoped op metadata cache).

**Outside `src/console/`:**
- `commands/cd.mdx` is still listed even though that page does not exist
- `guides/environment-variables.mdx` exists but is omitted
- <RepoFile path="build.rs" /> exists at the repo root but is omitted from Root Files
- current workflows such as `preview.yml` and `renovate.yml` are omitted

## Steps

1. Update the `console/` section: widgets list, `manager/` sub-structure, and `op_cache.rs`.
2. Fix docs-page listings so only real command pages are listed and current guides are present.
3. Refresh the root-file / workflow tables so they mention current files such as <RepoFile path="build.rs" />, plus the newer workflows (`preview.yml`, `renovate.yml`, etc.).
4. Re-read the file top-to-bottom once after editing; this page is a navigational map, so partial spot-fixes are how it drifts again.

## Caveats

Purely documentation — no code change. Do before enabling the CI staleness gate.
