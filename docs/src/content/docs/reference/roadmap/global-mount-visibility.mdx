---
title: "Global Mount Visibility"
---
import RepoFile from '../../../../components/RepoFile.astro'

**Status**: Open — design proposal

## Problem

`jackin config mount list` shows global mounts, but workspace-oriented surfaces do not show the global mounts that will be applied when that workspace launches.

That makes a valid setup look incomplete. For example, an operator can configure global `Gradle` and `Cargo` caches under `[docker.mounts]`, then open `jackin workspace show chainargos` or the console workspace panel and see only the saved workspace mounts. The effective launch will still mount the global caches, but the operator has no visual proof from the workspace view.

## Why It Matters

- Operators use workspace views to answer "what will this container see?" Global mounts are part of that answer.
- Cache mounts such as `/home/agent/.gradle/caches`, `/home/agent/.gradle/wrapper`, `/home/agent/.gradle/jdks`, `/home/agent/.cargo/registry`, and `/home/agent/.cargo/git` are most useful as global mounts, but their absence from workspace views makes them easy to misdiagnose as missing.
- The console currently puts workspace mounts and role choices next to the launch action. Hiding global mounts there means the launch summary is less complete than the operator expects.

## Desired Behavior

Show effective mounts in workspace inspection and workspace-edit surfaces while preserving the distinction between where each mount is configured.

### CLI

`jackin workspace show <name>` should display workspace mounts and applicable global mounts in separate groups:

```text
Workspace mounts:
  ~/Projects/app -> ~/Projects/app  rw  clone

Global mounts:
  ~/.cache/jackin/global/gradle/caches -> /home/agent/.gradle/caches  rw
  ~/.cache/jackin/global/cargo/registry -> /home/agent/.cargo/registry  rw
```

The global group should include role-scoped global mounts only when the workspace has enough role context to decide applicability:

- If the workspace has one allowed/default/last role, show mounts for that role and label the scope.
- If multiple roles could launch the workspace, either show a role-filtered subsection per role or print a short note that global mounts depend on the selected role.

### Console

The workspace panel and the workspace editor's **Mounts** tab should separate mount origins visually. A single mixed table is too easy to misread because global mounts are not edited from the workspace editor.

Recommended shape:

- Keep the existing **Mounts** panel for saved workspace mounts.
- Add a **Global mounts** panel below it, or add a section break inside the panel with a clear label.
- Include the global mount scope when it is not unscoped, for example `global` or `chainargos/*`.
- Treat global mounts as not editable from the workspace editor. Editing them belongs to `jackin config mount` today and to a future global-config TUI page later.

### Workspace editor

In `edit workspace -> Mounts`, global rows should be visible for orientation but excluded from workspace-edit actions:

- `D remove` applies only to saved workspace mounts.
- `R toggle ro/rw` applies only to saved workspace mounts.
- `I cycle isolation` applies only to saved workspace mounts.
- `+ Add mount` continues to add a workspace mount, not a global mount.
- Selecting a global mount should either show disabled action hints or a short status message such as `global mount — edit from config mounts`.

The visual grouping matters here even more than in the read-only workspace panel because the footer lists mutating actions. The operator must be able to tell which rows those actions can affect before pressing a key.

## Global Mounts TUI

Global mounts need their own first-class TUI surface. They are operator configuration, not workspace configuration, and they apply across many workspaces depending on scope.

### Design direction

Target the latest Apple UI/UX direction, not older macOS admin-tool patterns. The design reference is the current Apple Human Interface Guidelines and the macOS Tahoe / iOS 26 design language: content first, clear sidebars and toolbars, thoughtful control grouping, instantly familiar navigation, and a distinct control layer that does not bury the operator's task.

For jackin's terminal UI, this means adapting the intent rather than copying visual effects the terminal cannot render:

- Prefer a clean sidebar + detail layout over nested modal flows for primary navigation.
- Use grouped controls and concise footer actions; avoid dense command lists that read like legacy terminal admin screens.
- Keep global config visually separate from workspace config, matching Apple's emphasis on clear context and familiar navigation.
- Use current-system mental models: **Settings** / **Global config**, sidebars, tabs, segmented choices, disabled states, and clear detail panels.
- Do not optimize for old macOS conventions or backwards-looking UI compatibility. The target is the latest Apple platform behavior and expectations.

Recommended placement:

- Add a top-level console area outside the workspace editor, for example a left-nav item named **Global config** or **Settings**.
- Inside that area, add a **Global mounts** tab.
- Keep workspace editing focused on workspace-owned fields; do not make the workspace editor the place where global config is changed.

### Global mounts list

The global mounts page should show every configured global mount with:

- Name
- Source
- Destination
- Mode (`rw` / `ro`)
- Scope (`global`, `chainargos/*`, `scentbird/*`, etc.)
- Type (`folder`, `file`, `missing source`, sensitive path warning when applicable)

Suggested table shape:

```text
Global mounts

Name               Source                                  Destination                       Mode  Scope
gradle-caches      ~/.cache/jackin/global/gradle/caches    /home/agent/.gradle/caches        rw    global
gradle-properties  ~/.cache/jackin/global/gradle.properties /home/agent/.gradle/gradle.properties ro global
team-secrets       ~/.company/secrets                      /secrets                          ro    chainargos/*
```

### Editing operations

The global mounts page should support the same conceptual operations as `jackin config mount`:

- Add global mount
- Edit source
- Edit destination
- Toggle `readonly`
- Edit scope
- Rename mount
- Remove mount

The editor should validate the same rules as the CLI:

- Source must exist before saving.
- Destination must be absolute.
- Duplicate destinations within the same effective scope should be rejected before launch.
- Sensitive source paths should use the same warning-and-confirm flow as other mount entry points.

### Scope editing

Scope needs a deliberate UI because it is the main difference between a workspace mount and a global mount.

Recommended control:

- A segmented choice for `All roles` vs `Scoped`.
- When `Scoped` is selected, show a text input for the glob pattern.
- Preserve the current CLI semantics: examples include `chainargos/*` and `chainargos/agent-brown`.

The global mounts list should keep scope visible in the main table so an operator can immediately see why a mount applies to one workspace but not another.

### Relationship to workspace views

Workspace screens should consume global mount data, but not own it:

- Workspace overview: show applicable global mounts in a separate read-only section.
- Workspace editor: show applicable global mounts in a separate read-only section or panel; workspace edit actions do not affect them.
- Global mounts page: the only TUI place where global mounts are created, changed, or removed.

This mirrors the CLI split: `jackin workspace ...` manages workspace config, while `jackin config mount ...` manages global mounts.

## Host-side effects

This feature intentionally writes host-side jackin operator configuration only when the operator explicitly saves changes from the dedicated global mounts TUI. The save target is `~/.config/jackin/config.toml`, the same file managed by `jackin config mount`; workspace screens remain read-only for global mounts and must not mutate global config as a side effect of inspecting or editing a workspace.

## Proposed Codex goal prompt

This roadmap item is intended to be executable as one autonomous Codex Goal. Paste the prompt below from the jackin repository root. The goal runner should read this roadmap item as the source of truth, implement the full behavior in one feature branch, verify it locally, open one draft pull request, and stop for operator review.

```text
/goal Implement the Global Mount Visibility roadmap item completely in one pull request.

Context:
- Work in the jackin repo from a new `feature/<short-description>` branch.
- Read this roadmap first and treat it as the implementation contract:
  - docs/src/content/docs/reference/roadmap/global-mount-visibility.mdx
- Read and follow the repository process files before editing or opening the PR:
  - AGENTS.md
  - PULL_REQUESTS.md
  - COMMITS.md
  - BRANCHING.md
- Read the mount and workspace implementation paths before choosing the code shape:
  - src/config/mounts.rs
  - src/config/editor.rs
  - src/workspace/resolve.rs
  - src/config/workspaces.rs
  - src/console/manager/mod.rs
  - src/console/manager/state.rs

Product goal:
Build global mount visibility and editing so operators can understand and manage effective Gradle, Cargo, and other global cache mounts without confusing global operator config with per-workspace config. Workspace views should answer "what will this container see?" while clearly showing which rows come from the workspace file and which rows come from global config. Global mounts must be editable from a dedicated global-config TUI surface, not from workspace screens.

Required behavior:
- `jackin workspace show <name>` displays saved workspace mounts and applicable global mounts in separate groups.
- Role-scoped global mounts are shown only when the workspace/role context can decide applicability; otherwise the CLI explains that the selected role affects global mount visibility.
- The console workspace overview shows applicable global mounts separately from workspace mounts.
- `edit workspace -> Mounts` shows applicable global mounts for orientation, but workspace edit actions cannot mutate them.
- Selecting a global mount in the workspace editor shows disabled action hints or a short status message such as `global mount - edit from config mounts`.
- A dedicated global-config TUI surface lists all configured global mounts and supports add, edit source, edit destination, toggle readonly, edit scope, rename, and remove.
- The global mounts TUI saves through the same persistence path as `jackin config mount` and writes `~/.config/jackin/config.toml` only after an explicit operator save.
- Duplicate effective destinations still fail instead of silently shadowing one mount with another.
- Existing launch behavior remains compatible with the displayed effective mount set.

Implementation constraints:
- Work autonomously and silently until the goal is complete or a real blocker prevents completion.
- Implement the full roadmap item in one pull request.
- Keep workspace-owned configuration and global operator configuration separate in code, UI labels, TUI state, and persistence.
- Reuse existing config editor and mount-resolution primitives where practical.
- Do not create a second persistence path for `~/.config/jackin/config.toml`.
- Do not make workspace show, workspace overview, or workspace edit screens write global config.
- Do not force-push, rewrite commits, mark the PR ready, or merge unless the operator explicitly asks.
- If implementation changes a versioned schema described in AGENTS.md, ship the required migration artifacts in the same PR.

Tests to add or update:
- CLI rendering test for separate workspace/global mount groups.
- Role-scoped global mount display test.
- Workspace editor read-only behavior test for global mount rows.
- Global mounts TUI persistence test for add/edit/remove/rename/scope/readonly.
- Validation tests for missing source, non-absolute destination, sensitive path confirmation when applicable, and duplicate effective destinations.
- Regression test proving launch conflict behavior still rejects duplicate destinations.

Verification before opening the PR:
- Run:
  - `cargo fmt --check`
  - `cargo clippy --all-targets --all-features -- -D warnings`
  - `cargo check --all-targets`
  - `cargo nextest run --all-features`
- Run manual smoke:
  - `cargo run --bin jackin -- --debug workspace show chainargos`
  - `cargo run --bin jackin -- console --debug`
- In manual smoke, verify workspace views show workspace mounts and global mounts in separate sections.
- In manual smoke, verify global mounts are visible but not editable from the workspace editor.
- In manual smoke, verify the dedicated global mounts TUI can add, edit, scope, rename, and remove a test global mount only after explicit save.

Pull request requirements:
- Open one draft PR and stop for operator review. Do not merge it.
- Follow PULL_REQUESTS.md exactly, including the canonical PR body shape and copy-pasteable local verification commands.
- Use a Conventional Commit PR title because the PR title becomes the squash subject.
- Include user-smoke steps for every CLI/TUI behavior changed by this goal.
- Mention host-side effects explicitly: the implementation reads `~/.config/jackin/config.toml` for display and writes it only from the dedicated global mounts TUI after explicit save.
- Every commit must be signed off with `git commit -s`.
- Every agent-authored commit must include exactly one Codex trailer: `Co-authored-by: Codex <codex@openai.com>`.
- Use normal pushes for follow-up commits. Do not force-push or rewrite review history unless the operator explicitly approves that force push for this PR.

Deliverables:
- One draft PR implementing the full global mount visibility/editing behavior.
- Focused tests and full local verification output summarized in the PR body.
- Updated operator documentation if the implementation adds or changes user-facing commands or TUI flows.
- A concise final report in the PR body naming remaining risks, especially any role-scoped display ambiguity or TUI validation behavior that needs operator attention.
```

## Implementation Notes

- The runtime already resolves global + workspace + load-time mounts for launch. Reuse that resolver for display, but keep display rows tagged with their source: `global`, `workspace`, or `load-time` when relevant.
- Conflict behavior should stay unchanged: duplicate destinations still fail rather than letting one mount silently win.
- The console should not offer edit actions for global mounts inside the workspace editor unless it explicitly switches to a global-config editing flow.
- The global mounts TUI should call through the same config editor primitives used by `jackin config mount add/list/remove` rather than introducing a second persistence path.
- Because global mounts live in `~/.config/jackin/config.toml`, saving from the global mounts page is a global config write, not a workspace file write. The save confirmation and dirty-state prompt should say that explicitly.

## Related Files

- <RepoFile path="src/config/mounts.rs" /> — global mount config model
- <RepoFile path="src/config/editor.rs" /> — config mutation helpers
- <RepoFile path="src/workspace/resolve.rs" /> — launch-time workspace resolution
- <RepoFile path="src/config/workspaces.rs" /> — `workspace show` rendering
- <RepoFile path="src/console/manager/mod.rs" /> — console workspace manager
- <RepoFile path="src/console/manager/state.rs" /> — console workspace state
- [Mounts](/guides/mounts/) — operator-facing mount concepts
