from_version = "legacy"
target_version = "v1alpha1"
target_version_shipped = "2026-05-10"
summary = "Legacy operator config has no top-level `version` field. Migration adds `version = \"v1alpha1\"`; comments and existing keys are preserved (toml_edit inserts the new key at the top of the document)."
