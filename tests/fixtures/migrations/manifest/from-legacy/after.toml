# Sample role manifest carried through the legacy -> v1alpha1 migration test.

dockerfile = "Dockerfile"
agents = ["claude"]
version = "v1alpha1"

[claude]
plugins = []

[identity]
name = "the-architect"
