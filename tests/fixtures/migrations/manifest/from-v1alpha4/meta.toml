from_version = "v1alpha4"
target_version = "v1alpha5"
target_version_shipped = "2026-06-04"
summary = "Restamps role manifest as v1alpha5. Introduces [docker].min_profile (optional string) and [docker].dind (optional string); the migration is a no-op restamp."
