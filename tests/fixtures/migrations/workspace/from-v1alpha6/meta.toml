from_version = "v1alpha6"
target_version = "v1alpha7"
target_version_shipped = "2026-06-04"
summary = "Adds optional [workspaces.X.runtime] section with backend field for per-workspace apple-container backend override. Additive noop migration — existing workspace files parse unchanged."
