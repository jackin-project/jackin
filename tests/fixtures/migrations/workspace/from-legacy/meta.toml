from_version = "legacy"
target_version = "v1alpha1"
target_version_shipped = "2026-05-10"
summary = "Legacy per-workspace file has no top-level `version` field. Migration adds `version = \"v1alpha1\"` and leaves all existing keys, comments, mount arrays, and env tables untouched."
