# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / Main
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      providers:
        description: Comma-separated provider subset of [github-hosted, velnor]
        required: false
        default: github-hosted
        type: string
      scope:
        description: Verification scope
        required: true
        default: full
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-26.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      unit_ids: ${{ steps.plan.outputs.unit_ids }}
      full_units: ${{ steps.plan.outputs.full_units }}
      plan_digest: ${{ steps.plan.outputs.plan_digest }}
      excluded: ${{ steps.plan.outputs.excluded }}
      bun_matrix: ${{ steps.plan.outputs.bun_matrix }}
      docker_matrix: ${{ steps.plan.outputs.docker_matrix }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
      swift_matrix: ${{ steps.plan.outputs.swift_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        id: runtime
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@0dc79895ff1c5e88be7c3822c437e1c5b5282e12
        with:
          rev: 0dc79895ff1c5e88be7c3822c437e1c5b5282e12
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=0dc79895ff1c5e88be7c3822c437e1c5b5282e12" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_PROVIDERS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.providers || 'github-hosted' }}
          VELNOR_EVENT_TRUSTED: ${{ (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot'))) && 'true' || 'false' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 0dc79895ff1c5e88be7c3822c437e1c5b5282e12
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          policy_closure="$("$stage/velnor-workflow-policy" --closure)"
          [[ "$policy_closure" == "${{ steps.runtime.outputs.closure }}" ]] || { echo "::error::policy runtime reports closure $policy_closure, expected ${{ steps.runtime.outputs.closure }}" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg closure "${{ steps.runtime.outputs.closure }}" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_closure "$policy_closure" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, closure: $closure, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_closure: $policy_closure, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-0dc79895ff1c5e88be7c3822c437e1c5b5282e12-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  policy:
    name: Policy
    runs-on: ubuntu-26.04
    timeout-minutes: 20
    # Trust invariant: this job runs the base branch's Stage-0 validator
    # product against the audited tree under pull_request_target. It holds
    # `contents: read` only, references no secrets, persists no credentials,
    # and never compiles. When the audited tree differs from the declared
    # pin's render, it additionally EXECUTES the PR run's prebuilt
    # candidate generator — PR-built code, same-repository runs only, bound
    # to the audited tree by manifest closure plus binary digest before
    # execution — with no secret references, no persisted credentials, the
    # read-only github.token confined to the Acquire/Ruleset API steps,
    # and both candidate exec points tokenless.
    permissions:
      contents: read
    steps:
      - name: Checkout repository history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: policy-checkout
          fetch-depth: 0
          persist-credentials: false
      - name: Check out audited head
        working-directory: policy-checkout
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
        run: |
          set -euo pipefail
          if ! git cat-file -e "$HEAD_SHA^{commit}" 2>/dev/null; then
            git fetch --no-tags "$GITHUB_SERVER_URL/$HEAD_REPOSITORY" "$HEAD_SHA"
          fi
          git checkout --quiet --detach "$HEAD_SHA"
      - name: Set up Velnor workflow runtime
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@0dc79895ff1c5e88be7c3822c437e1c5b5282e12
        with:
          rev: 0dc79895ff1c5e88be7c3822c437e1c5b5282e12
          checkout-path: ${{ github.workspace }}/policy-checkout
      - name: Read declared generator pin
        id: pin
        working-directory: policy-checkout
        run: |
          set -euo pipefail
          pin="$(sed -n -E 's/^[[:space:]]*revision[[:space:]]*=[[:space:]]*"([0-9a-f]{40})".*/\1/p' .github-gen/velnor-workflow.toml | head -n 1)"
          test "$pin" != '' || pin="$(sed -n -E 's/^.*VELNOR_WORKFLOW_POLICY_REVISION:[[:space:]]*([0-9a-f]{40}).*/\1/p' .github/workflows/ci-policy.yml | head -n 1)"
          test "$pin" != '' || { echo "::error::audited tree declares no generator pin" >&2; exit 1; }
          echo "value=$pin" >> "$GITHUB_OUTPUT"
      - name: Set up declared generator product
        id: renderer
        if: steps.pin.outputs.value != '0dc79895ff1c5e88be7c3822c437e1c5b5282e12'
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@0dc79895ff1c5e88be7c3822c437e1c5b5282e12
        with:
          rev: ${{ steps.pin.outputs.value }}
          checkout-path: ${{ github.workspace }}/policy-checkout
      - name: Resolve declared generator product
        if: steps.pin.outputs.value != '0dc79895ff1c5e88be7c3822c437e1c5b5282e12'
        run: |
          set -euo pipefail
          binary="$HOME/.cache/velnor/workflow-runtime/${{ steps.renderer.outputs.closure }}/bin/velnor-workflow"
          test -x "$binary"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$binary" >> "$GITHUB_ENV"
      - name: Resolve required status checks
        env:
          GH_TOKEN: ${{ github.token }}
          DEFAULT_BRANCH: main
          DECLARED_RULESET_CONTEXTS: DCO,Policy,ci-required
        run: |
          set -euo pipefail
          stderr="$(mktemp)"
          trap 'rm -f "$stderr"' EXIT
          if contexts="$(gh api "repos/$GITHUB_REPOSITORY/rulesets?includes_parents=true" 2>"$stderr" \
            | jq -r '.[] | select(.target == "branch" and .enforcement == "active") | .id' \
            | while read -r id; do gh api "repos/$GITHUB_REPOSITORY/rulesets/$id"; done \
            | jq -r --arg branch "refs/heads/$DEFAULT_BRANCH" 'select(.conditions.ref_name.include | any(. == "~DEFAULT_BRANCH" or . == "~ALL" or . == $branch)) | .rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context' \
            | sort -u | paste -sd, -)"; then
            :
          elif grep -qE '(HTTP 403|Upgrade to GitHub Team)' "$stderr"; then
            echo "::warning::rulesets API returned 403; falling back to declared contexts [$DECLARED_RULESET_CONTEXTS]"
            contexts="$DECLARED_RULESET_CONTEXTS"
          else
            cat "$stderr" >&2
            exit 1
          fi
          echo "RULESET_CONTEXTS=$contexts" >> "$GITHUB_ENV"
      - name: Enforce workflow policy
        env:
          WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
          VELNOR_WORKFLOW_POLICY_REVISION: 0dc79895ff1c5e88be7c3822c437e1c5b5282e12
        run: |
          set -euo pipefail
          velnor-workflow policy \
            --workflow-root "$WORKFLOW_ROOT" \
            --head-sha "$HEAD_SHA" \
            --base-sha "$BASE_SHA" \
            --candidate-manifest "${VELNOR_WORKFLOW_CANDIDATE_MANIFEST:-}" \
            --ruleset-contexts "$RULESET_CONTEXTS"

      - name: Set up actionlint
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: actionlint@1.7.12
          cache: false
      - name: Lint caller workflows
        working-directory: policy-checkout
        env:
          MISE_NO_CONFIG: "1"
        run: mise exec actionlint@1.7.12 -- actionlint
  github-hosted-bun-jackin-docs:
    name: "Bun · Bun package (jackin-docs) · github-hosted — bun-jackin-docs"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"bun-jackin-docs"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-jackin-docs
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: "~/.bun/install/cache"
      cache_key_files: |
        docs/package.json
        docs/bun.lock
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-bun-jackin-docs:
    name: "Bun · Bun package (jackin-docs) · velnor — bun-jackin-docs"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"bun-jackin-docs"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-jackin-docs
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      host_warm_layers: cargo
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-docker-docker-construct:
    name: "Docker · Docker (docker/construct) · github-hosted — docker-docker-construct"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"docker-docker-construct"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker-docker-construct
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-docker-docker-construct:
    name: "Docker · Docker (docker/construct) · velnor — docker-docker-construct"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"docker-docker-construct"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker-docker-construct
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  prepare-cargo:
    name: "Control / Prepare Cargo"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (contains(needs.plan.outputs.units, '"unit_id":"rust-arrayref"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-agent-status"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-brand"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-build-meta"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-capsule"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config-fuzz"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-console"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-core"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-dev"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-diagnostics"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-docker"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env-fuzz"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-host"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-image"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-instance"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-isolation"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-launch"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest-fuzz"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-oppicker"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-otlp-testbed"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-pr-trailers"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-process"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol-fuzz"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-runtime"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-telemetry"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-test-support"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-tui"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage-ffi"') || contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-xtask"')) && ((((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot'))))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-arrayref
      provider: control
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-hosted-rust-arrayref:
    name: "Rust · arrayref · github-hosted — rust-arrayref"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-arrayref"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-arrayref
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: dd0cd0f44a70
      mbx_dependency_files: |
        .cargo/**
        rust-toolchain
        rust-toolchain.toml
        vendor/arrayref/Cargo.lock
      mbx_freshness_files: |
        mise.lock
        vendor/arrayref/**/*.rs
        vendor/arrayref/Cargo.lock
        vendor/arrayref/benches/**
        vendor/arrayref/examples/**
        vendor/arrayref/src/**
        vendor/arrayref/tests/**
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        vendor/arrayref/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "vendor/arrayref"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-arrayref:
    name: "Rust · arrayref · velnor — rust-arrayref"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-arrayref"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-arrayref
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin:
    name: "Rust · jackin · github-hosted — rust-jackin"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "6a8762b1d34f"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-console/**/*.rs
        crates/jackin-console/benches/**
        crates/jackin-console/examples/**
        crates/jackin-console/src/**
        crates/jackin-console/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-host/**/*.rs
        crates/jackin-host/benches/**
        crates/jackin-host/examples/**
        crates/jackin-host/src/**
        crates/jackin-host/tests/**
        crates/jackin-image/**/*.rs
        crates/jackin-image/benches/**
        crates/jackin-image/build.rs
        crates/jackin-image/examples/**
        crates/jackin-image/src/**
        crates/jackin-image/tests/**
        crates/jackin-instance/**/*.rs
        crates/jackin-instance/benches/**
        crates/jackin-instance/examples/**
        crates/jackin-instance/src/**
        crates/jackin-instance/tests/**
        crates/jackin-isolation/**/*.rs
        crates/jackin-isolation/benches/**
        crates/jackin-isolation/examples/**
        crates/jackin-isolation/src/**
        crates/jackin-isolation/tests/**
        crates/jackin-launch/**/*.rs
        crates/jackin-launch/benches/**
        crates/jackin-launch/build.rs
        crates/jackin-launch/examples/**
        crates/jackin-launch/src/**
        crates/jackin-launch/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-oppicker/**/*.rs
        crates/jackin-oppicker/benches/**
        crates/jackin-oppicker/examples/**
        crates/jackin-oppicker/src/**
        crates/jackin-oppicker/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-runtime/**/*.rs
        crates/jackin-runtime/benches/**
        crates/jackin-runtime/build.rs
        crates/jackin-runtime/examples/**
        crates/jackin-runtime/src/**
        crates/jackin-runtime/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-test-support/**/*.rs
        crates/jackin-test-support/benches/**
        crates/jackin-test-support/examples/**
        crates/jackin-test-support/src/**
        crates/jackin-test-support/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        crates/jackin-usage/**/*.rs
        crates/jackin-usage/benches/**
        crates/jackin-usage/examples/**
        crates/jackin-usage/src/**
        crates/jackin-usage/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        crates/jackin/**/*.rs
        crates/jackin/benches/**
        crates/jackin/build.rs
        crates/jackin/examples/**
        crates/jackin/src/**
        crates/jackin/tests/**
        docker/construct/Dockerfile
        docker/runtime/agent-status/hooks/claude/report-hook.sh
        docker/runtime/agent-status/hooks/codex/report-hook.sh
        docker/runtime/agent-status/hooks/opencode/plugin.js
        docker/runtime/agent-status/hooks/opencode/report-hook.sh
        docker/runtime/entrypoint.sh
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-config,rust-jackin-console,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-env,rust-jackin-image,rust-jackin-launch,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-runtime,rust-jackin-telemetry,rust-jackin-test-support,rust-jackin-tui,rust-jackin-usage"
      unit_admission: github-hosted
  velnor-rust-jackin:
    name: "Rust · jackin · velnor — rust-jackin"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-config,rust-jackin-console,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-env,rust-jackin-image,rust-jackin-launch,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-runtime,rust-jackin-telemetry,rust-jackin-test-support,rust-jackin-tui,rust-jackin-usage"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-agent-status:
    name: "Rust · jackin-agent-status · github-hosted — rust-jackin-agent-status"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-agent-status"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-agent-status
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "0d8065e348ff"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-agent-status/**/*.rs
        crates/jackin-agent-status/benches/**
        crates/jackin-agent-status/examples/**
        crates/jackin-agent-status/src/**
        crates/jackin-agent-status/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-agent-status"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-protocol"
      unit_admission: github-hosted
  velnor-rust-jackin-agent-status:
    name: "Rust · jackin-agent-status · velnor — rust-jackin-agent-status"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-agent-status"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-agent-status
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-protocol"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-brand:
    name: "Rust · jackin-brand · github-hosted — rust-jackin-brand"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-brand"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-brand
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "8eeaa5687d4c"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-brand"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-jackin-brand:
    name: "Rust · jackin-brand · velnor — rust-jackin-brand"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-brand"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-brand
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-build-meta:
    name: "Rust · jackin-build-meta · github-hosted — rust-jackin-build-meta"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-build-meta"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-build-meta
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "45a7a3202922"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-build-meta"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: github-hosted
  velnor-rust-jackin-build-meta:
    name: "Rust · jackin-build-meta · velnor — rust-jackin-build-meta"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-build-meta"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-build-meta
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-capsule:
    name: "Rust · jackin-capsule · github-hosted — rust-jackin-capsule"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-capsule"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-capsule
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "0b936afd90a3"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-agent-status/**/*.rs
        crates/jackin-agent-status/benches/**
        crates/jackin-agent-status/examples/**
        crates/jackin-agent-status/src/**
        crates/jackin-agent-status/tests/**
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-capsule/**/*.rs
        crates/jackin-capsule/benches/**
        crates/jackin-capsule/build.rs
        crates/jackin-capsule/examples/**
        crates/jackin-capsule/src/**
        crates/jackin-capsule/src/daemon.rs
        crates/jackin-capsule/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        crates/jackin-usage/**/*.rs
        crates/jackin-usage/benches/**
        crates/jackin-usage/examples/**
        crates/jackin-usage/src/**
        crates/jackin-usage/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-capsule"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-agent-status,rust-jackin-brand,rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-tui,rust-jackin-usage"
      unit_admission: github-hosted
  velnor-rust-jackin-capsule:
    name: "Rust · jackin-capsule · velnor — rust-jackin-capsule"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-capsule"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-capsule
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-agent-status,rust-jackin-brand,rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-tui,rust-jackin-usage"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-config:
    name: "Rust · jackin-config · github-hosted — rust-jackin-config"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-config
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "2180fd43365c"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-config"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-config:
    name: "Rust · jackin-config · velnor — rust-jackin-config"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-config
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-config-fuzz:
    name: "Rust · jackin-config-fuzz · github-hosted — rust-jackin-config-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config-fuzz"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-config-fuzz
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "50dfae74b8a0"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        crates/jackin-config/fuzz/Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/fuzz/**/*.rs
        crates/jackin-config/fuzz/Cargo.lock
        crates/jackin-config/fuzz/benches/**
        crates/jackin-config/fuzz/examples/**
        crates/jackin-config/fuzz/src/**
        crates/jackin-config/fuzz/tests/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        crates/jackin-config/fuzz/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-config/fuzz"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-config
      unit_admission: github-hosted
  velnor-rust-jackin-config-fuzz:
    name: "Rust · jackin-config-fuzz · velnor — rust-jackin-config-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-config-fuzz"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-config-fuzz
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-config
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-console:
    name: "Rust · jackin-console · github-hosted — rust-jackin-console"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-console"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-console
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: aae6926ced24
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-console/**/*.rs
        crates/jackin-console/benches/**
        crates/jackin-console/examples/**
        crates/jackin-console/src/**
        crates/jackin-console/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-oppicker/**/*.rs
        crates/jackin-oppicker/benches/**
        crates/jackin-oppicker/examples/**
        crates/jackin-oppicker/src/**
        crates/jackin-oppicker/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-console"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-env,rust-jackin-oppicker,rust-jackin-otlp-testbed,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-tui"
      unit_admission: github-hosted
  velnor-rust-jackin-console:
    name: "Rust · jackin-console · velnor — rust-jackin-console"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-console"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-console
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-env,rust-jackin-oppicker,rust-jackin-otlp-testbed,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-tui"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-core:
    name: "Rust · jackin-core · github-hosted — rust-jackin-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-core"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-core
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "78d4c653ae98"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-core"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-jackin-core:
    name: "Rust · jackin-core · velnor — rust-jackin-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-core"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-core
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-dev:
    name: "Rust · jackin-dev · github-hosted — rust-jackin-dev"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-dev"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-dev
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "99fcfdfce0d6"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-dev/**/*.rs
        crates/jackin-dev/benches/**
        crates/jackin-dev/examples/**
        crates/jackin-dev/src/**
        crates/jackin-dev/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-dev"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-jackin-dev:
    name: "Rust · jackin-dev · velnor — rust-jackin-dev"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-dev"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-dev
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-diagnostics:
    name: "Rust · jackin-diagnostics · github-hosted — rust-jackin-diagnostics"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-diagnostics"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-diagnostics
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: df33e6fcec01
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-diagnostics"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-otlp-testbed,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-diagnostics:
    name: "Rust · jackin-diagnostics · velnor — rust-jackin-diagnostics"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-diagnostics"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-diagnostics
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-otlp-testbed,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-docker:
    name: "Rust · jackin-docker · github-hosted — rust-jackin-docker"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-docker"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-docker
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: a9b1b8a2ccda
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-docker"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-docker:
    name: "Rust · jackin-docker · velnor — rust-jackin-docker"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-docker"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-docker
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-env:
    name: "Rust · jackin-env · github-hosted — rust-jackin-env"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-env
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: e773c1b3e6dc
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-env"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-env:
    name: "Rust · jackin-env · velnor — rust-jackin-env"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-env
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-env-fuzz:
    name: "Rust · jackin-env-fuzz · github-hosted — rust-jackin-env-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env-fuzz"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-env-fuzz
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: c213ddff377c
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        crates/jackin-env/fuzz/Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/fuzz/**/*.rs
        crates/jackin-env/fuzz/Cargo.lock
        crates/jackin-env/fuzz/benches/**
        crates/jackin-env/fuzz/examples/**
        crates/jackin-env/fuzz/src/**
        crates/jackin-env/fuzz/tests/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        crates/jackin-env/fuzz/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-env/fuzz"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-env"
      unit_admission: github-hosted
  velnor-rust-jackin-env-fuzz:
    name: "Rust · jackin-env-fuzz · velnor — rust-jackin-env-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-env-fuzz"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-env-fuzz
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-env"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-host:
    name: "Rust · jackin-host · github-hosted — rust-jackin-host"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-host"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-host
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "159dc75f0d35"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-host/**/*.rs
        crates/jackin-host/benches/**
        crates/jackin-host/examples/**
        crates/jackin-host/src/**
        crates/jackin-host/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-test-support/**/*.rs
        crates/jackin-test-support/benches/**
        crates/jackin-test-support/examples/**
        crates/jackin-test-support/src/**
        crates/jackin-test-support/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-host"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support"
      unit_admission: github-hosted
  velnor-rust-jackin-host:
    name: "Rust · jackin-host · velnor — rust-jackin-host"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-host"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-host
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-image:
    name: "Rust · jackin-image · github-hosted — rust-jackin-image"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-image"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-image
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: fa4431b509d1
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-image/**/*.rs
        crates/jackin-image/benches/**
        crates/jackin-image/build.rs
        crates/jackin-image/examples/**
        crates/jackin-image/src/**
        crates/jackin-image/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        docker/runtime/agent-status/hooks/claude/report-hook.sh
        docker/runtime/agent-status/hooks/codex/report-hook.sh
        docker/runtime/agent-status/hooks/opencode/plugin.js
        docker/runtime/agent-status/hooks/opencode/report-hook.sh
        docker/runtime/entrypoint.sh
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-image"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-image:
    name: "Rust · jackin-image · velnor — rust-jackin-image"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-image"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-image
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-instance:
    name: "Rust · jackin-instance · github-hosted — rust-jackin-instance"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-instance"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-instance
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: eee0b8d3d383
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-instance/**/*.rs
        crates/jackin-instance/benches/**
        crates/jackin-instance/examples/**
        crates/jackin-instance/src/**
        crates/jackin-instance/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-instance"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-manifest,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-instance:
    name: "Rust · jackin-instance · velnor — rust-jackin-instance"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-instance"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-instance
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-manifest,rust-jackin-process,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-isolation:
    name: "Rust · jackin-isolation · github-hosted — rust-jackin-isolation"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-isolation"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-isolation
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: e8d748665d2f
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-isolation/**/*.rs
        crates/jackin-isolation/benches/**
        crates/jackin-isolation/examples/**
        crates/jackin-isolation/src/**
        crates/jackin-isolation/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-test-support/**/*.rs
        crates/jackin-test-support/benches/**
        crates/jackin-test-support/examples/**
        crates/jackin-test-support/src/**
        crates/jackin-test-support/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-isolation"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support"
      unit_admission: github-hosted
  velnor-rust-jackin-isolation:
    name: "Rust · jackin-isolation · velnor — rust-jackin-isolation"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-isolation"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-isolation
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-launch:
    name: "Rust · jackin-launch · github-hosted — rust-jackin-launch"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-launch"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-launch
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "57e35188efb1"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-launch/**/*.rs
        crates/jackin-launch/benches/**
        crates/jackin-launch/build.rs
        crates/jackin-launch/examples/**
        crates/jackin-launch/src/**
        crates/jackin-launch/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-launch"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-telemetry,rust-jackin-tui"
      unit_admission: github-hosted
  velnor-rust-jackin-launch:
    name: "Rust · jackin-launch · velnor — rust-jackin-launch"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-launch"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-launch
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-telemetry,rust-jackin-tui"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-manifest:
    name: "Rust · jackin-manifest · github-hosted — rust-jackin-manifest"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-manifest
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "5e4d259caaba"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-manifest"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core"
      unit_admission: github-hosted
  velnor-rust-jackin-manifest:
    name: "Rust · jackin-manifest · velnor — rust-jackin-manifest"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-manifest
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-manifest-fuzz:
    name: "Rust · jackin-manifest-fuzz · github-hosted — rust-jackin-manifest-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest-fuzz"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-manifest-fuzz
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: dc2f1c515a7e
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        crates/jackin-manifest/fuzz/Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/fuzz/**/*.rs
        crates/jackin-manifest/fuzz/Cargo.lock
        crates/jackin-manifest/fuzz/benches/**
        crates/jackin-manifest/fuzz/examples/**
        crates/jackin-manifest/fuzz/src/**
        crates/jackin-manifest/fuzz/tests/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        crates/jackin-manifest/fuzz/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-manifest/fuzz"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-manifest
      unit_admission: github-hosted
  velnor-rust-jackin-manifest-fuzz:
    name: "Rust · jackin-manifest-fuzz · velnor — rust-jackin-manifest-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-manifest-fuzz"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-manifest-fuzz
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-manifest
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-oppicker:
    name: "Rust · jackin-oppicker · github-hosted — rust-jackin-oppicker"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-oppicker"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-oppicker
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: cc03a6ae27cc
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-oppicker/**/*.rs
        crates/jackin-oppicker/benches/**
        crates/jackin-oppicker/examples/**
        crates/jackin-oppicker/src/**
        crates/jackin-oppicker/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-oppicker"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-oppicker:
    name: "Rust · jackin-oppicker · velnor — rust-jackin-oppicker"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-oppicker"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-oppicker
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-otlp-testbed:
    name: "Rust · jackin-otlp-testbed · github-hosted — rust-jackin-otlp-testbed"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-otlp-testbed"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-otlp-testbed
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: d0fe68d9f74c
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-otlp-testbed"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-telemetry
      unit_admission: github-hosted
  velnor-rust-jackin-otlp-testbed:
    name: "Rust · jackin-otlp-testbed · velnor — rust-jackin-otlp-testbed"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-otlp-testbed"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-otlp-testbed
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-telemetry
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-pr-trailers:
    name: "Rust · jackin-pr-trailers · github-hosted — rust-jackin-pr-trailers"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-pr-trailers"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-pr-trailers
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: c605c1db963d
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-pr-trailers/**/*.rs
        crates/jackin-pr-trailers/benches/**
        crates/jackin-pr-trailers/examples/**
        crates/jackin-pr-trailers/src/**
        crates/jackin-pr-trailers/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-pr-trailers"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: github-hosted
  velnor-rust-jackin-pr-trailers:
    name: "Rust · jackin-pr-trailers · velnor — rust-jackin-pr-trailers"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-pr-trailers"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-pr-trailers
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-process:
    name: "Rust · jackin-process · github-hosted — rust-jackin-process"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-process"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-process
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: afc36c2e72cb
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-process"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-jackin-process:
    name: "Rust · jackin-process · velnor — rust-jackin-process"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-process"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-process
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-protocol:
    name: "Rust · jackin-protocol · github-hosted — rust-jackin-protocol"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-protocol
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "62ed63e57a62"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-protocol"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-protocol:
    name: "Rust · jackin-protocol · velnor — rust-jackin-protocol"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-protocol
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-protocol-fuzz:
    name: "Rust · jackin-protocol-fuzz · github-hosted — rust-jackin-protocol-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol-fuzz"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-protocol-fuzz
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "4dda055f6439"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        crates/jackin-protocol/fuzz/Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/fuzz/**/*.rs
        crates/jackin-protocol/fuzz/Cargo.lock
        crates/jackin-protocol/fuzz/benches/**
        crates/jackin-protocol/fuzz/examples/**
        crates/jackin-protocol/fuzz/src/**
        crates/jackin-protocol/fuzz/tests/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        crates/jackin-protocol/fuzz/Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-protocol/fuzz"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-protocol
      unit_admission: github-hosted
  velnor-rust-jackin-protocol-fuzz:
    name: "Rust · jackin-protocol-fuzz · velnor — rust-jackin-protocol-fuzz"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-protocol-fuzz"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-protocol-fuzz
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-protocol
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-runtime:
    name: "Rust · jackin-runtime · github-hosted — rust-jackin-runtime"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-runtime"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-runtime
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "2efca9caf8bd"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-build-meta/**/*.rs
        crates/jackin-build-meta/benches/**
        crates/jackin-build-meta/examples/**
        crates/jackin-build-meta/src/**
        crates/jackin-build-meta/tests/**
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-docker/**/*.rs
        crates/jackin-docker/benches/**
        crates/jackin-docker/build.rs
        crates/jackin-docker/examples/**
        crates/jackin-docker/src/**
        crates/jackin-docker/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-host/**/*.rs
        crates/jackin-host/benches/**
        crates/jackin-host/examples/**
        crates/jackin-host/src/**
        crates/jackin-host/tests/**
        crates/jackin-image/**/*.rs
        crates/jackin-image/benches/**
        crates/jackin-image/build.rs
        crates/jackin-image/examples/**
        crates/jackin-image/src/**
        crates/jackin-image/tests/**
        crates/jackin-instance/**/*.rs
        crates/jackin-instance/benches/**
        crates/jackin-instance/examples/**
        crates/jackin-instance/src/**
        crates/jackin-instance/tests/**
        crates/jackin-isolation/**/*.rs
        crates/jackin-isolation/benches/**
        crates/jackin-isolation/examples/**
        crates/jackin-isolation/src/**
        crates/jackin-isolation/tests/**
        crates/jackin-launch/**/*.rs
        crates/jackin-launch/benches/**
        crates/jackin-launch/build.rs
        crates/jackin-launch/examples/**
        crates/jackin-launch/src/**
        crates/jackin-launch/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-runtime/**/*.rs
        crates/jackin-runtime/benches/**
        crates/jackin-runtime/build.rs
        crates/jackin-runtime/examples/**
        crates/jackin-runtime/src/**
        crates/jackin-runtime/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-test-support/**/*.rs
        crates/jackin-test-support/benches/**
        crates/jackin-test-support/examples/**
        crates/jackin-test-support/src/**
        crates/jackin-test-support/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        crates/jackin-usage/**/*.rs
        crates/jackin-usage/benches/**
        crates/jackin-usage/examples/**
        crates/jackin-usage/src/**
        crates/jackin-usage/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        docker/runtime/agent-status/hooks/claude/report-hook.sh
        docker/runtime/agent-status/hooks/codex/report-hook.sh
        docker/runtime/agent-status/hooks/opencode/plugin.js
        docker/runtime/agent-status/hooks/opencode/report-hook.sh
        docker/runtime/entrypoint.sh
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-runtime"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-env,rust-jackin-host,rust-jackin-image,rust-jackin-instance,rust-jackin-isolation,rust-jackin-launch,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support,rust-jackin-usage"
      unit_admission: github-hosted
  velnor-rust-jackin-runtime:
    name: "Rust · jackin-runtime · velnor — rust-jackin-runtime"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-runtime"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-runtime
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-brand,rust-jackin-build-meta,rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-docker,rust-jackin-env,rust-jackin-host,rust-jackin-image,rust-jackin-instance,rust-jackin-isolation,rust-jackin-launch,rust-jackin-manifest,rust-jackin-otlp-testbed,rust-jackin-process,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-test-support,rust-jackin-usage"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-telemetry:
    name: "Rust · jackin-telemetry · github-hosted — rust-jackin-telemetry"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-telemetry"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-telemetry
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "2f5df293b7d5"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-telemetry"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-jackin-telemetry:
    name: "Rust · jackin-telemetry · velnor — rust-jackin-telemetry"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-telemetry"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-telemetry
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-test-support:
    name: "Rust · jackin-test-support · github-hosted — rust-jackin-test-support"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-test-support"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-test-support
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "1dbc3c29257a"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-manifest/**/*.rs
        crates/jackin-manifest/benches/**
        crates/jackin-manifest/examples/**
        crates/jackin-manifest/src/**
        crates/jackin-manifest/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-test-support/**/*.rs
        crates/jackin-test-support/benches/**
        crates/jackin-test-support/examples/**
        crates/jackin-test-support/src/**
        crates/jackin-test-support/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-test-support"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-manifest,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-test-support:
    name: "Rust · jackin-test-support · velnor — rust-jackin-test-support"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-test-support"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-test-support
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-core,rust-jackin-manifest,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-tui:
    name: "Rust · jackin-tui · github-hosted — rust-jackin-tui"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-tui"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-tui
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "2ede62de110c"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-brand/**/*.rs
        crates/jackin-brand/benches/**
        crates/jackin-brand/examples/**
        crates/jackin-brand/src/**
        crates/jackin-brand/tests/**
        crates/jackin-tui/**/*.rs
        crates/jackin-tui/benches/**
        crates/jackin-tui/examples/**
        crates/jackin-tui/src/**
        crates/jackin-tui/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-tui"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-brand
      unit_admission: github-hosted
  velnor-rust-jackin-tui:
    name: "Rust · jackin-tui · velnor — rust-jackin-tui"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-tui"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-tui
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-brand
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-usage:
    name: "Rust · jackin-usage · github-hosted — rust-jackin-usage"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-usage
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: e5051076ad08
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage/**/*.rs
        crates/jackin-usage/benches/**
        crates/jackin-usage/examples/**
        crates/jackin-usage/src/**
        crates/jackin-usage/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-usage"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-protocol,rust-jackin-telemetry"
      unit_admission: github-hosted
  velnor-rust-jackin-usage:
    name: "Rust · jackin-usage · velnor — rust-jackin-usage"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-usage
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-diagnostics,rust-jackin-otlp-testbed,rust-jackin-protocol,rust-jackin-telemetry"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-usage-ffi:
    name: "Rust · jackin-usage-ffi · github-hosted — rust-jackin-usage-ffi"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage-ffi"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-usage-ffi
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      mbx_compat: "05207d703ae5"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-config/**/*.rs
        crates/jackin-config/benches/**
        crates/jackin-config/examples/**
        crates/jackin-config/src/**
        crates/jackin-config/tests/**
        crates/jackin-core/**/*.rs
        crates/jackin-core/benches/**
        crates/jackin-core/examples/**
        crates/jackin-core/src/**
        crates/jackin-core/tests/**
        crates/jackin-diagnostics/**/*.rs
        crates/jackin-diagnostics/benches/**
        crates/jackin-diagnostics/build.rs
        crates/jackin-diagnostics/examples/**
        crates/jackin-diagnostics/src/**
        crates/jackin-diagnostics/tests/**
        crates/jackin-env/**/*.rs
        crates/jackin-env/benches/**
        crates/jackin-env/examples/**
        crates/jackin-env/src/**
        crates/jackin-env/tests/**
        crates/jackin-otlp-testbed/**/*.rs
        crates/jackin-otlp-testbed/benches/**
        crates/jackin-otlp-testbed/examples/**
        crates/jackin-otlp-testbed/src/**
        crates/jackin-otlp-testbed/tests/**
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-protocol/**/*.rs
        crates/jackin-protocol/benches/**
        crates/jackin-protocol/examples/**
        crates/jackin-protocol/src/**
        crates/jackin-protocol/tests/**
        crates/jackin-telemetry/**/*.rs
        crates/jackin-telemetry/benches/**
        crates/jackin-telemetry/examples/**
        crates/jackin-telemetry/src/**
        crates/jackin-telemetry/tests/**
        crates/jackin-usage-ffi/**/*.rs
        crates/jackin-usage-ffi/benches/**
        crates/jackin-usage-ffi/examples/**
        crates/jackin-usage-ffi/src/**
        crates/jackin-usage-ffi/tests/**
        crates/jackin-usage/**/*.rs
        crates/jackin-usage/benches/**
        crates/jackin-usage/examples/**
        crates/jackin-usage/src/**
        crates/jackin-usage/tests/**
        crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-usage-ffi"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-env,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-usage"
      unit_admission: github-hosted
  velnor-rust-jackin-usage-ffi:
    name: "Rust · jackin-usage-ffi · velnor — rust-jackin-usage-ffi"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-usage-ffi"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-usage-ffi
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: "rust-jackin-config,rust-jackin-core,rust-jackin-env,rust-jackin-protocol,rust-jackin-telemetry,rust-jackin-usage"
      unit_admission: velnor-trust-gated
  github-hosted-rust-jackin-xtask:
    name: "Rust · jackin-xtask · github-hosted — rust-jackin-xtask"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-xtask"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-xtask
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest github:open-telemetry/weaver"
      mbx_compat: ab9469a835b8
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/jackin-process/**/*.rs
        crates/jackin-process/benches/**
        crates/jackin-process/examples/**
        crates/jackin-process/src/**
        crates/jackin-process/tests/**
        crates/jackin-xtask/**/*.rs
        crates/jackin-xtask/benches/**
        crates/jackin-xtask/examples/**
        crates/jackin-xtask/src/**
        crates/jackin-xtask/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/jackin-xtask"
      cargo_net_offline: true
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: github-hosted
  velnor-rust-jackin-xtask:
    name: "Rust · jackin-xtask · velnor — rust-jackin-xtask"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-jackin-xtask"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-jackin-xtask
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "aqua:nextest-rs/nextest/cargo-nextest github:open-telemetry/weaver"
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-process
      unit_admission: velnor-trust-gated
  github-hosted-rust-policy:
    name: "Rust · Rust dependency policy · github-hosted — rust-policy"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"rust-policy"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: bf492502128c
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        deny.toml
      mbx_freshness_files: |
        .cargo/audit.toml
        .cargo/deny.toml
        Cargo.lock
        audit.toml
        deny.toml
      cargo_bin_tools: "cargo-deny,cargo-audit"
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  velnor-rust-policy:
    name: "Rust · Rust dependency policy · velnor — rust-policy"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(needs.plan.outputs.units, '"unit_id":"rust-policy"') && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      provider: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "cargo:cargo-deny"
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_platform: linux-x64
      unit_trust: untrusted-ok
      unit_admission: velnor-trust-gated
  github-hosted-swift-package-native-design-prototypes-unifiedagentusage:
    name: "Swift · Apple · Swift package (native/Design/Prototypes/UnifiedAgentUsage) · github-hosted — swift-package-native-design-prototypes-unifiedagentusage"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"swift-package-native-design-prototypes-unifiedagentusage"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-swift.yml
    with:
      unit: swift-package-native-design-prototypes-unifiedagentusage
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: "~/.swiftpm"
      cache_key_files: |
        native/Design/Prototypes/UnifiedAgentUsage/Package.swift
        native/Design/Prototypes/UnifiedAgentUsage/Package.resolved
        native/Design/Prototypes/UnifiedAgentUsage/.swiftpm/Package.resolved
      unit_platform: macos-arm64
      unit_trust: untrusted-ok
      unit_admission: github-hosted
  github-hosted-swift-package-native:
    name: "Swift · Apple ·  · github-hosted — swift-package-native"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(needs.plan.outputs.units, '"unit_id":"swift-package-native"') && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-swift.yml
    with:
      unit: swift-package-native
      provider: github-hosted
      selected_units: ${{ needs.plan.outputs.units }}
      selected_unit_ids: ${{ needs.plan.outputs.unit_ids }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      plan_digest: ${{ needs.plan.outputs.plan_digest }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mise_tools: "cargo-binstall rust cargo:sccache cargo:boltffi_cli"
      unit_platform: macos-arm64
      unit_trust: untrusted-ok
      unit_dependencies: rust-jackin-usage-ffi
      unit_admission: github-hosted
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, policy, github-hosted-bun-jackin-docs, velnor-bun-jackin-docs, github-hosted-docker-docker-construct, velnor-docker-docker-construct, prepare-cargo, github-hosted-rust-arrayref, velnor-rust-arrayref, github-hosted-rust-jackin, velnor-rust-jackin, github-hosted-rust-jackin-agent-status, velnor-rust-jackin-agent-status, github-hosted-rust-jackin-brand, velnor-rust-jackin-brand, github-hosted-rust-jackin-build-meta, velnor-rust-jackin-build-meta, github-hosted-rust-jackin-capsule, velnor-rust-jackin-capsule, github-hosted-rust-jackin-config, velnor-rust-jackin-config, github-hosted-rust-jackin-config-fuzz, velnor-rust-jackin-config-fuzz, github-hosted-rust-jackin-console, velnor-rust-jackin-console, github-hosted-rust-jackin-core, velnor-rust-jackin-core, github-hosted-rust-jackin-dev, velnor-rust-jackin-dev, github-hosted-rust-jackin-diagnostics, velnor-rust-jackin-diagnostics, github-hosted-rust-jackin-docker, velnor-rust-jackin-docker, github-hosted-rust-jackin-env, velnor-rust-jackin-env, github-hosted-rust-jackin-env-fuzz, velnor-rust-jackin-env-fuzz, github-hosted-rust-jackin-host, velnor-rust-jackin-host, github-hosted-rust-jackin-image, velnor-rust-jackin-image, github-hosted-rust-jackin-instance, velnor-rust-jackin-instance, github-hosted-rust-jackin-isolation, velnor-rust-jackin-isolation, github-hosted-rust-jackin-launch, velnor-rust-jackin-launch, github-hosted-rust-jackin-manifest, velnor-rust-jackin-manifest, github-hosted-rust-jackin-manifest-fuzz, velnor-rust-jackin-manifest-fuzz, github-hosted-rust-jackin-oppicker, velnor-rust-jackin-oppicker, github-hosted-rust-jackin-otlp-testbed, velnor-rust-jackin-otlp-testbed, github-hosted-rust-jackin-pr-trailers, velnor-rust-jackin-pr-trailers, github-hosted-rust-jackin-process, velnor-rust-jackin-process, github-hosted-rust-jackin-protocol, velnor-rust-jackin-protocol, github-hosted-rust-jackin-protocol-fuzz, velnor-rust-jackin-protocol-fuzz, github-hosted-rust-jackin-runtime, velnor-rust-jackin-runtime, github-hosted-rust-jackin-telemetry, velnor-rust-jackin-telemetry, github-hosted-rust-jackin-test-support, velnor-rust-jackin-test-support, github-hosted-rust-jackin-tui, velnor-rust-jackin-tui, github-hosted-rust-jackin-usage, velnor-rust-jackin-usage, github-hosted-rust-jackin-usage-ffi, velnor-rust-jackin-usage-ffi, github-hosted-rust-jackin-xtask, velnor-rust-jackin-xtask, github-hosted-rust-policy, velnor-rust-policy, github-hosted-swift-package-native-design-prototypes-unifiedagentusage, github-hosted-swift-package-native]
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          PLAN_DIGEST: ${{ needs.plan.outputs.plan_digest }}
          EXCLUDED: ${{ needs.plan.outputs.excluded }}
          PROVIDER_ADMITTED_GITHUB_HOSTED: ${{ (github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch') }}
          PROVIDER_ADMITTED_VELNOR_TRUSTED: ${{ ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot'))) }}
          PROVIDER_ADMITTED_ANY_LOCAL_TRUSTED: ${{ (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "$PLAN_DIGEST" ]]; then
            echo "plan did not freeze a plan digest: the expected set has no identity" >&2
            exit 1
          fi
          echo "verdict binds plan digest $PLAN_DIGEST"
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          plan_expects() {
            [[ "$(jq -r --arg unit "$1" --arg provider "$2" '[.[] | select(.unit_id == $unit) | .providers[] | select(. == $provider)] | length' <<<"$SELECTED_UNITS")" -gt 0 ]]
          }
          plan_expects_local() {
            [[ "$(jq -r --arg unit "$1" '[.[] | select(.unit_id == $unit) | .providers[] | select(. == "velnor")] | length' <<<"$SELECTED_UNITS")" -gt 0 ]]
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job policy)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite policy did not pass: $result" >&2
            exit 1
          fi
          if plan_expects "bun-jackin-docs" "github-hosted"; then
            result="$(result_for_job github-hosted-bun-jackin-docs)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-bun-jackin-docs was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-bun-jackin-docs was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-bun-jackin-docs did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-bun-jackin-docs ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-bun-jackin-docs)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-bun-jackin-docs succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-bun-jackin-docs ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "bun-jackin-docs" "velnor"; then
            result="$(result_for_job velnor-bun-jackin-docs)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-bun-jackin-docs was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-bun-jackin-docs was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-bun-jackin-docs did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-bun-jackin-docs ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-bun-jackin-docs)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-bun-jackin-docs succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-bun-jackin-docs ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "docker-docker-construct" "github-hosted"; then
            result="$(result_for_job github-hosted-docker-docker-construct)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-docker-docker-construct was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-docker-docker-construct was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-docker-docker-construct did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-docker-docker-construct ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-docker-docker-construct)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-docker-docker-construct succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-docker-docker-construct ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "docker-docker-construct" "velnor"; then
            result="$(result_for_job velnor-docker-docker-construct)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-docker-docker-construct was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-docker-docker-construct was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-docker-docker-construct did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-docker-docker-construct ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-docker-docker-construct)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-docker-docker-construct succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-docker-docker-construct ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects_local "rust-arrayref" || plan_expects_local "rust-jackin" || plan_expects_local "rust-jackin-agent-status" || plan_expects_local "rust-jackin-brand" || plan_expects_local "rust-jackin-build-meta" || plan_expects_local "rust-jackin-capsule" || plan_expects_local "rust-jackin-config" || plan_expects_local "rust-jackin-config-fuzz" || plan_expects_local "rust-jackin-console" || plan_expects_local "rust-jackin-core" || plan_expects_local "rust-jackin-dev" || plan_expects_local "rust-jackin-diagnostics" || plan_expects_local "rust-jackin-docker" || plan_expects_local "rust-jackin-env" || plan_expects_local "rust-jackin-env-fuzz" || plan_expects_local "rust-jackin-host" || plan_expects_local "rust-jackin-image" || plan_expects_local "rust-jackin-instance" || plan_expects_local "rust-jackin-isolation" || plan_expects_local "rust-jackin-launch" || plan_expects_local "rust-jackin-manifest" || plan_expects_local "rust-jackin-manifest-fuzz" || plan_expects_local "rust-jackin-oppicker" || plan_expects_local "rust-jackin-otlp-testbed" || plan_expects_local "rust-jackin-pr-trailers" || plan_expects_local "rust-jackin-process" || plan_expects_local "rust-jackin-protocol" || plan_expects_local "rust-jackin-protocol-fuzz" || plan_expects_local "rust-jackin-runtime" || plan_expects_local "rust-jackin-telemetry" || plan_expects_local "rust-jackin-test-support" || plan_expects_local "rust-jackin-tui" || plan_expects_local "rust-jackin-usage" || plan_expects_local "rust-jackin-usage-ffi" || plan_expects_local "rust-jackin-xtask"; then
            result="$(result_for_job prepare-cargo)"
            if [[ "$PROVIDER_ADMITTED_ANY_LOCAL_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI prerequisite prepare-cargo was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI prerequisite prepare-cargo was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI prerequisite prepare-cargo did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI prerequisite prepare-cargo ran outside its provider admission (PROVIDER_ADMITTED_ANY_LOCAL_TRUSTED=$PROVIDER_ADMITTED_ANY_LOCAL_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job prepare-cargo)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI prerequisite prepare-cargo succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI prerequisite prepare-cargo ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-arrayref" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-arrayref)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-arrayref was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-arrayref was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-arrayref did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-arrayref ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-arrayref)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-arrayref succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-arrayref ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-arrayref" "velnor"; then
            result="$(result_for_job velnor-rust-arrayref)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-arrayref was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-arrayref was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-arrayref did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-arrayref ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-arrayref)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-arrayref succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-arrayref ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin" "velnor"; then
            result="$(result_for_job velnor-rust-jackin)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-agent-status" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-agent-status)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-agent-status was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-agent-status was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-agent-status did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-agent-status ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-agent-status)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-agent-status succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-agent-status ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-agent-status" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-agent-status)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-agent-status was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-agent-status was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-agent-status did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-agent-status ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-agent-status)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-agent-status succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-agent-status ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-brand" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-brand)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-brand was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-brand was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-brand did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-brand ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-brand)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-brand succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-brand ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-brand" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-brand)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-brand was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-brand was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-brand did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-brand ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-brand)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-brand succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-brand ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-build-meta" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-build-meta)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-build-meta was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-build-meta was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-build-meta did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-build-meta ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-build-meta)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-build-meta succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-build-meta ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-build-meta" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-build-meta)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-build-meta was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-build-meta was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-build-meta did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-build-meta ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-build-meta)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-build-meta succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-build-meta ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-capsule" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-capsule)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-capsule was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-capsule was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-capsule did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-capsule ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-capsule)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-capsule succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-capsule ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-capsule" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-capsule)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-capsule was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-capsule was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-capsule did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-capsule ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-capsule)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-capsule succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-capsule ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-config" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-config)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-config was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-config was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-config did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-config ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-config)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-config succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-config ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-config" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-config)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-config was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-config was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-config did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-config ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-config)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-config succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-config ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-config-fuzz" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-config-fuzz)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-config-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-config-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-config-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-config-fuzz ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-config-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-config-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-config-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-config-fuzz" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-config-fuzz)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-config-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-config-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-config-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-config-fuzz ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-config-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-config-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-config-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-console" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-console)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-console was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-console was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-console did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-console ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-console)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-console succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-console ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-console" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-console)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-console was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-console was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-console did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-console ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-console)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-console succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-console ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-core" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-core)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-core was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-core was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-core did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-core ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-core)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-core succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-core ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-core" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-core)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-core was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-core was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-core did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-core ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-core)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-core succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-core ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-dev" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-dev)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-dev was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-dev was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-dev did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-dev ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-dev)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-dev succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-dev ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-dev" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-dev)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-dev was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-dev was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-dev did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-dev ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-dev)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-dev succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-dev ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-diagnostics" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-diagnostics)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-diagnostics was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-diagnostics was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-diagnostics did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-diagnostics ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-diagnostics)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-diagnostics succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-diagnostics ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-diagnostics" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-diagnostics)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-diagnostics was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-diagnostics was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-diagnostics did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-diagnostics ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-diagnostics)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-diagnostics succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-diagnostics ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-docker" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-docker)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-docker was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-docker was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-docker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-docker ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-docker)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-docker succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-docker ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-docker" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-docker)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-docker was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-docker was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-docker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-docker ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-docker)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-docker succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-docker ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-env" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-env)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-env was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-env was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-env did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-env ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-env)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-env succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-env ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-env" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-env)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-env was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-env was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-env did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-env ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-env)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-env succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-env ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-env-fuzz" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-env-fuzz)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-env-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-env-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-env-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-env-fuzz ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-env-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-env-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-env-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-env-fuzz" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-env-fuzz)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-env-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-env-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-env-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-env-fuzz ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-env-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-env-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-env-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-host" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-host)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-host was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-host was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-host did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-host ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-host)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-host succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-host ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-host" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-host)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-host was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-host was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-host did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-host ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-host)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-host succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-host ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-image" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-image)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-image was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-image was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-image did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-image ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-image)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-image succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-image ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-image" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-image)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-image was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-image was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-image did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-image ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-image)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-image succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-image ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-instance" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-instance)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-instance was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-instance was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-instance did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-instance ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-instance)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-instance succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-instance ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-instance" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-instance)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-instance was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-instance was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-instance did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-instance ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-instance)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-instance succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-instance ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-isolation" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-isolation)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-isolation was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-isolation was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-isolation did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-isolation ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-isolation)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-isolation succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-isolation ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-isolation" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-isolation)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-isolation was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-isolation was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-isolation did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-isolation ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-isolation)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-isolation succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-isolation ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-launch" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-launch)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-launch was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-launch was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-launch did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-launch ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-launch)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-launch succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-launch ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-launch" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-launch)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-launch was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-launch was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-launch did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-launch ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-launch)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-launch succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-launch ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-manifest" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-manifest)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-manifest was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-manifest was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-manifest did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-manifest ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-manifest)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-manifest succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-manifest ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-manifest" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-manifest)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-manifest was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-manifest was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-manifest did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-manifest ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-manifest)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-manifest succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-manifest ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-manifest-fuzz" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-manifest-fuzz)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-manifest-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-manifest-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-manifest-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-manifest-fuzz ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-manifest-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-manifest-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-manifest-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-manifest-fuzz" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-manifest-fuzz)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-manifest-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-manifest-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-manifest-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-manifest-fuzz ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-manifest-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-manifest-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-manifest-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-oppicker" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-oppicker)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-oppicker was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-oppicker was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-oppicker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-oppicker ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-oppicker)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-oppicker succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-oppicker ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-oppicker" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-oppicker)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-oppicker was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-oppicker was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-oppicker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-oppicker ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-oppicker)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-oppicker succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-oppicker ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-otlp-testbed" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-otlp-testbed)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-otlp-testbed was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-otlp-testbed was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-otlp-testbed did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-otlp-testbed ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-otlp-testbed)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-otlp-testbed succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-otlp-testbed ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-otlp-testbed" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-otlp-testbed)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-otlp-testbed was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-otlp-testbed was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-otlp-testbed did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-otlp-testbed ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-otlp-testbed)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-otlp-testbed succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-otlp-testbed ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-pr-trailers" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-pr-trailers)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-pr-trailers was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-pr-trailers was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-pr-trailers did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-pr-trailers ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-pr-trailers)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-pr-trailers succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-pr-trailers ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-pr-trailers" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-pr-trailers)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-pr-trailers was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-pr-trailers was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-pr-trailers did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-pr-trailers ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-pr-trailers)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-pr-trailers succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-pr-trailers ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-process" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-process)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-process was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-process was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-process did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-process ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-process)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-process succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-process ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-process" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-process)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-process was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-process was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-process did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-process ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-process)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-process succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-process ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-protocol" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-protocol)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-protocol was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-protocol was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-protocol did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-protocol ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-protocol)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-protocol succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-protocol ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-protocol" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-protocol)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-protocol was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-protocol was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-protocol did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-protocol ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-protocol)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-protocol succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-protocol ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-protocol-fuzz" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-protocol-fuzz)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-protocol-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-protocol-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-protocol-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-protocol-fuzz ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-protocol-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-protocol-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-protocol-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-protocol-fuzz" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-protocol-fuzz)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-protocol-fuzz was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-protocol-fuzz was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-protocol-fuzz did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-protocol-fuzz ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-protocol-fuzz)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-protocol-fuzz succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-protocol-fuzz ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-runtime" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-runtime)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-runtime was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-runtime was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-runtime did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-runtime ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-runtime)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-runtime succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-runtime ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-runtime" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-runtime)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-runtime was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-runtime was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-runtime did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-runtime ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-runtime)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-runtime succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-runtime ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-telemetry" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-telemetry)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-telemetry was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-telemetry was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-telemetry did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-telemetry ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-telemetry)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-telemetry succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-telemetry ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-telemetry" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-telemetry)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-telemetry was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-telemetry was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-telemetry did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-telemetry ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-telemetry)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-telemetry succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-telemetry ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-test-support" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-test-support)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-test-support was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-test-support was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-test-support did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-test-support ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-test-support)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-test-support succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-test-support ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-test-support" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-test-support)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-test-support was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-test-support was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-test-support did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-test-support ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-test-support)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-test-support succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-test-support ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-tui" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-tui)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-tui was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-tui was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-tui did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-tui ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-tui)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-tui succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-tui ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-tui" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-tui)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-tui was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-tui was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-tui did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-tui ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-tui)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-tui succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-tui ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-usage" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-usage)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-usage was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-usage was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-usage did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-usage ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-usage)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-usage succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-usage ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-usage" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-usage)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-usage was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-usage was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-usage did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-usage ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-usage)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-usage succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-usage ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-usage-ffi" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-usage-ffi)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-usage-ffi was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-usage-ffi was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-usage-ffi did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-usage-ffi ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-usage-ffi)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-usage-ffi succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-usage-ffi ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-usage-ffi" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-usage-ffi)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-usage-ffi was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-usage-ffi was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-usage-ffi did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-usage-ffi ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-usage-ffi)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-usage-ffi succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-usage-ffi ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-xtask" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-jackin-xtask)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-jackin-xtask was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-jackin-xtask was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-jackin-xtask did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-jackin-xtask ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-jackin-xtask)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-jackin-xtask succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-jackin-xtask ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-jackin-xtask" "velnor"; then
            result="$(result_for_job velnor-rust-jackin-xtask)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-jackin-xtask was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-jackin-xtask was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-jackin-xtask did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-jackin-xtask ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-jackin-xtask)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-jackin-xtask succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-jackin-xtask ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-policy" "github-hosted"; then
            result="$(result_for_job github-hosted-rust-policy)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-rust-policy was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-rust-policy was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-rust-policy ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-rust-policy)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-rust-policy succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-rust-policy ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "rust-policy" "velnor"; then
            result="$(result_for_job velnor-rust-policy)"
            if [[ "$PROVIDER_ADMITTED_VELNOR_TRUSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job velnor-rust-policy was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job velnor-rust-policy was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job velnor-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-policy ran outside its provider admission (PROVIDER_ADMITTED_VELNOR_TRUSTED=$PROVIDER_ADMITTED_VELNOR_TRUSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-policy)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job velnor-rust-policy succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job velnor-rust-policy ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "swift-package-native-design-prototypes-unifiedagentusage" "github-hosted"; then
            result="$(result_for_job github-hosted-swift-package-native-design-prototypes-unifiedagentusage)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-swift-package-native-design-prototypes-unifiedagentusage)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-swift-package-native-design-prototypes-unifiedagentusage ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
          if plan_expects "swift-package-native" "github-hosted"; then
            result="$(result_for_job github-hosted-swift-package-native)"
            if [[ "$PROVIDER_ADMITTED_GITHUB_HOSTED" == true ]]; then
              case "$result" in
                success) ;;
                skipped) echo "expected CI job github-hosted-swift-package-native was skipped: a skipped expected result cannot pass" >&2; exit 1 ;;
                cancelled) echo "expected CI job github-hosted-swift-package-native was cancelled: a cancelled expected result cannot pass" >&2; exit 1 ;;
                *) echo "expected CI job github-hosted-swift-package-native did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-hosted-swift-package-native ran outside its provider admission (PROVIDER_ADMITTED_GITHUB_HOSTED=$PROVIDER_ADMITTED_GITHUB_HOSTED): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-hosted-swift-package-native)"
            case "$result" in
              skipped) ;;
              success) echo "unexpected CI job github-hosted-swift-package-native succeeded outside the expected set: the plan did not declare it" >&2; exit 1 ;;
              *) echo "unexpected CI job github-hosted-swift-package-native ran outside the expected set: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-26.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
