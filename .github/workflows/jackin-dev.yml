name: jackin-dev

on:
  pull_request:
    paths:
      - ".github/actions/sign-and-attest-archive/**"
      - ".github/workflows/jackin-dev.yml"
      - "Cargo.lock"
      - "Cargo.toml"
      - "crates/jackin-dev/**"
      - "mise.toml"
      - "rust-toolchain.toml"
  push:
    branches: [main]
    paths:
      - ".github/actions/sign-and-attest-archive/**"
      - ".github/workflows/jackin-dev.yml"
      - "Cargo.lock"
      - "Cargo.toml"
      - "crates/jackin-dev/**"
      - "mise.toml"
      - "rust-toolchain.toml"
  workflow_dispatch:
    inputs:
      lanes:
        description: |
          Which runner(s) to run on. Default github; use both for parity runs.
        type: choice
        default: github
        options: [github, velnor, both]

jobs:
  validate-version-bump:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0

      - name: Require jackin-dev version bump for artifact changes
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail

          diff_files=$(git diff --name-only "${BASE_SHA}...${HEAD_SHA}")
          source_changed=false
          while IFS= read -r path; do
            [ -z "$path" ] && continue
            case "$path" in
              Cargo.toml|Cargo.lock|crates/jackin-dev/**)
                source_changed=true
                ;;
            esac
          done <<< "$diff_files"

          if [ "$source_changed" != true ]; then
            exit 0
          fi

          base_version=$(
            git show "${BASE_SHA}:crates/jackin-dev/Cargo.toml" |
              sed -n 's/^version = "\(.*\)"/\1/p' |
              head -1
          )
          head_version=$(
            sed -n 's/^version = "\(.*\)"/\1/p' crates/jackin-dev/Cargo.toml |
              head -1
          )

          if [ "$base_version" = "$head_version" ]; then
            echo "::error::jackin-dev artifact inputs changed; bump crates/jackin-dev/Cargo.toml from ${head_version}"
            exit 1
          fi

  matrix-setup:
    name: Select runner lane
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      configs: ${{ steps.set.outputs.configs }}
      has_github: ${{ steps.set.outputs.has_github }}
    steps:
      - id: set
        env:
          LANES: ${{ github.event_name == 'workflow_dispatch' && inputs.lanes || 'github' }}
        run: |
          github='{"lane":"GitHub","runner":"\"ubuntu-24.04\"","runner_os":"Linux","runner_arch":"X64"}'
          velnor='{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64"}'
          case "$LANES" in
            velnor)
              configs="[$velnor]"
              has_github=false
              ;;
            both)
              configs="[$github,$velnor]"
              has_github=true
              ;;
            *)
              configs="[$github]"
              has_github=true
              ;;
          esac
          {
            echo "configs=$configs"
            echo "has_github=$has_github"
          } >> "$GITHUB_OUTPUT"

  version:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - id: version
        run: |
          version=$(sed -n 's/^version = "\(.*\)"/\1/p' crates/jackin-dev/Cargo.toml | head -1)
          test -n "$version"
          echo "version=$version" >> "$GITHUB_OUTPUT"

  assert-version:
    if: github.ref == 'refs/heads/main'
    needs: version
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - name: Assert jackin-dev version is unpublished
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          if gh release view "jackin-dev-v${VERSION}" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "jackin-dev-v${VERSION} already exists; bump crates/jackin-dev/Cargo.toml"
            exit 1
          fi

  build:
    needs: [version, matrix-setup]
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    name: build ${{ matrix.target.target }} (${{ matrix.config.lane }})
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.matrix-setup.outputs.configs) }}
        target:
          - target: aarch64-apple-darwin
            zigbuild_target: aarch64-apple-darwin
          - target: x86_64-apple-darwin
            zigbuild_target: x86_64-apple-darwin
          - target: aarch64-unknown-linux-gnu
            zigbuild_target: aarch64-unknown-linux-gnu.2.17
          - target: x86_64-unknown-linux-gnu
            zigbuild_target: x86_64-unknown-linux-gnu.2.17
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Cache Rust toolchain
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml') }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-
      - uses: jdx/mise-action@e6a8b3978addb5a52f2b4cd9d91eafa7f0ab959d # v4.2.0
        with:
          version: "2026.6.11"
          cache_key_prefix: "mise-v2"
          install_args: ${{ matrix.config.lane == 'Velnor' && 'rust zig cargo-zigbuild cosign syft sccache' || 'rust zig cargo-zigbuild cosign syft' }}
          github_token: ${{ secrets.GH_READONLY_TOKEN }}
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          shared-key: jackin-dev-${{ matrix.target.target }}-workspace-v2
          cache-all-crates: "true"
          cache-workspace-crates: "true"
          env-vars: "CARGO CC CFLAGS CXX CMAKE RUSTC_WRAPPER RUSTFLAGS RUSTDOC RUSTUP"
          # Match release/preview archive builds: one target-scoped workspace
          # cache, shared across branches through GitHub's default-branch
          # fallback, plus the lockfile-bound Cargo registry cache above.
      - run: rustup target add ${{ matrix.target.target }}
      - name: Enable local sccache
        if: matrix.config.lane == 'Velnor'
        run: |
          echo "RUSTC_WRAPPER=sccache" >> "$GITHUB_ENV"
          echo "SCCACHE_BASEDIRS=$GITHUB_WORKSPACE" >> "$GITHUB_ENV"
      - name: Cache macOS SDK
        if: contains(matrix.target.target, 'apple')
        id: sdk-cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: /tmp/macos-sdk
          key: macos-sdk-26.1
      - name: Download macOS SDK
        if: contains(matrix.target.target, 'apple') && steps.sdk-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p /tmp/macos-sdk
          curl -fsSL "https://github.com/joseluisq/macosx-sdks/releases/download/26.1/MacOSX26.1.sdk.tar.xz" \
            | tar -xJ -C /tmp/macos-sdk
      - name: Set macOS SDKROOT
        if: contains(matrix.target.target, 'apple')
        run: |
          echo "SDKROOT=/tmp/macos-sdk/MacOSX26.1.sdk" >> "$GITHUB_ENV"
          echo "MACOSX_DEPLOYMENT_TARGET=12.0" >> "$GITHUB_ENV"
      - name: Build
        env:
          ZIGBUILD_TARGET: ${{ matrix.target.zigbuild_target }}
        run: cargo zigbuild --timings --release --locked -p jackin-dev --target "$ZIGBUILD_TARGET"
      - name: Upload cargo timings
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: cargo-timings-jackin-dev-${{ matrix.config.lane }}-${{ matrix.target.target }}
          path: target/cargo-timings/*.html
          if-no-files-found: ignore
          retention-days: 7
      - name: Write sccache stats
        if: always() && matrix.config.lane == 'Velnor'
        run: |
          stats_file="sccache-stats-jackin-dev-${{ matrix.config.lane }}-${{ matrix.target.target }}.txt"
          sccache --show-stats | tee "$stats_file"
          {
            echo "### sccache jackin-dev ${{ matrix.target.target }} (${{ matrix.config.lane }})"
            echo '```text'
            cat "$stats_file"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Upload sccache stats
        if: always() && matrix.config.lane == 'Velnor'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sccache-stats-jackin-dev-${{ matrix.config.lane }}-${{ matrix.target.target }}
          path: sccache-stats-jackin-dev-${{ matrix.config.lane }}-${{ matrix.target.target }}.txt
          if-no-files-found: ignore
          retention-days: 7
      - name: Package
        env:
          TARGET: ${{ matrix.target.target }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          archive="jackin-dev-${VERSION}-${TARGET}.tar.gz"
          tar -czf "$archive" \
            -C "target/${TARGET}/release" \
            jackin-dev
          sha256sum "$archive" | awk '{print $1}' > "$archive.sha256"
          echo "archive=$archive" >> "$GITHUB_ENV"
      - name: Sign, SBOM, and attest archive
        uses: ./.github/actions/sign-and-attest-archive
        with:
          archive: ${{ env.archive }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: jackin-dev-${{ matrix.config.lane }}-${{ matrix.target.target }}
          path: |
            jackin-dev-*.tar.gz
            jackin-dev-*.tar.gz.sha256
            jackin-dev-*.tar.gz.bundle
            jackin-dev-*.tar.gz.sbom.json

  publish:
    if: github.ref == 'refs/heads/main' && needs.matrix-setup.outputs.has_github == 'true'
    needs: [version, matrix-setup, assert-version, build]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: jackin-dev-GitHub-*
          path: artifacts
          merge-multiple: true
      - name: Publish jackin-dev release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          gh release create "jackin-dev-v${VERSION}" \
            artifacts/jackin-dev-*.tar.gz \
            artifacts/jackin-dev-*.tar.gz.sha256 \
            artifacts/jackin-dev-*.tar.gz.bundle \
            artifacts/jackin-dev-*.tar.gz.sbom.json \
            --repo "$GITHUB_REPOSITORY" \
            --title "jackin-dev v${VERSION}" \
            --notes "jackin-dev v${VERSION}"
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: jackin-project/homebrew-tap
          ref: main
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap
      - name: Rewrite jackin-dev formula
        env:
          VERSION: ${{ needs.version.outputs.version }}
        run: |
          read_sha() {
            cat "artifacts/jackin-dev-${VERSION}-$1.tar.gz.sha256"
          }
          ARM64_MACOS_SHA256=$(read_sha aarch64-apple-darwin)
          X86_MACOS_SHA256=$(read_sha x86_64-apple-darwin)
          ARM64_LINUX_SHA256=$(read_sha aarch64-unknown-linux-gnu)
          X86_LINUX_SHA256=$(read_sha x86_64-unknown-linux-gnu)

          cat > homebrew-tap/Formula/jackin-dev.rb <<EOF
          class JackinDev < Formula
            desc "Developer tooling for local jackin pull request verification"
            homepage "https://github.com/jackin-project/jackin"
            version "${VERSION}"
            license "Apache-2.0"

            on_macos do
              on_arm do
                url "https://github.com/jackin-project/jackin/releases/download/jackin-dev-v${VERSION}/jackin-dev-${VERSION}-aarch64-apple-darwin.tar.gz"
                sha256 "${ARM64_MACOS_SHA256}"
              end
              on_intel do
                url "https://github.com/jackin-project/jackin/releases/download/jackin-dev-v${VERSION}/jackin-dev-${VERSION}-x86_64-apple-darwin.tar.gz"
                sha256 "${X86_MACOS_SHA256}"
              end
            end

            on_linux do
              on_arm do
                url "https://github.com/jackin-project/jackin/releases/download/jackin-dev-v${VERSION}/jackin-dev-${VERSION}-aarch64-unknown-linux-gnu.tar.gz"
                sha256 "${ARM64_LINUX_SHA256}"
              end
              on_intel do
                url "https://github.com/jackin-project/jackin/releases/download/jackin-dev-v${VERSION}/jackin-dev-${VERSION}-x86_64-unknown-linux-gnu.tar.gz"
                sha256 "${X86_LINUX_SHA256}"
              end
            end

            def install
              bin.install "jackin-dev"
            end

            test do
              system "#{bin}/jackin-dev", "--help"
            end
          end
          EOF

          cd homebrew-tap
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/jackin-dev.rb
          git commit -m "Update jackin-dev to ${VERSION}"
          git push origin main

  timing-summary:
    if: always()
    needs: [matrix-setup, version, assert-version, build, publish]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Write workflow timing summary
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash scripts/ci/workflow-timing-summary.sh jackin-dev
