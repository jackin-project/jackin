# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Rust
on:
  workflow_call:
    inputs:
      unit:
        required: true
        type: string
      scope:
        required: true
        type: string
      selection-artifact:
        required: true
        type: string

jobs:
  github-rust-arrayref:
    name: "GitHub / rust-arrayref"
    if: ${{ inputs.unit == 'rust-arrayref' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-57943d4e7fec-${{ runner.os }}-${{ runner.arch }}-rust-arrayref-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml', 'vendor/arrayref/Cargo.toml') }}-${{ hashFiles('mise.lock', 'vendor/arrayref/**/*.rs', 'vendor/arrayref/benches/**', 'vendor/arrayref/examples/**', 'vendor/arrayref/src/**', 'vendor/arrayref/tests/**') }}
          restore-keys: |
            velnor-mbx-v3-57943d4e7fec-${{ runner.os }}-${{ runner.arch }}-rust-arrayref-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml', 'vendor/arrayref/Cargo.toml') }}-
            velnor-mbx-v3-57943d4e7fec-${{ runner.os }}-${{ runner.arch }}-rust-arrayref-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (arrayref)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'vendor/arrayref/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'vendor/arrayref' && cargo fetch --locked
      - name: Run "Rust crate (arrayref)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · arrayref"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (arrayref)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'vendor/arrayref/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-arrayref:
    name: "Velnor / rust-arrayref"
    if: ${{ inputs.unit == 'rust-arrayref' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (arrayref)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'vendor/arrayref/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'vendor/arrayref' && cargo fetch --locked
      - name: Run "Rust crate (arrayref)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · arrayref"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin:
    name: "GitHub / rust-jackin"
    if: ${{ inputs.unit == 'rust-jackin' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-59b22c0cb4e7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-console/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'crates/jackin/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-console/**/*.rs', 'crates/jackin-console/benches/**', 'crates/jackin-console/examples/**', 'crates/jackin-console/src/**', 'crates/jackin-console/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-host/**/*.rs', 'crates/jackin-host/benches/**', 'crates/jackin-host/examples/**', 'crates/jackin-host/src/**', 'crates/jackin-host/tests/**', 'crates/jackin-image/**/*.rs', 'crates/jackin-image/benches/**', 'crates/jackin-image/build.rs', 'crates/jackin-image/examples/**', 'crates/jackin-image/src/**', 'crates/jackin-image/tests/**', 'crates/jackin-instance/**/*.rs', 'crates/jackin-instance/benches/**', 'crates/jackin-instance/examples/**', 'crates/jackin-instance/src/**', 'crates/jackin-instance/tests/**', 'crates/jackin-isolation/**/*.rs', 'crates/jackin-isolation/benches/**', 'crates/jackin-isolation/examples/**', 'crates/jackin-isolation/src/**', 'crates/jackin-isolation/tests/**', 'crates/jackin-launch/**/*.rs', 'crates/jackin-launch/benches/**', 'crates/jackin-launch/build.rs', 'crates/jackin-launch/examples/**', 'crates/jackin-launch/src/**', 'crates/jackin-launch/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-oppicker/**/*.rs', 'crates/jackin-oppicker/benches/**', 'crates/jackin-oppicker/examples/**', 'crates/jackin-oppicker/src/**', 'crates/jackin-oppicker/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-runtime/**/*.rs', 'crates/jackin-runtime/benches/**', 'crates/jackin-runtime/build.rs', 'crates/jackin-runtime/examples/**', 'crates/jackin-runtime/src/**', 'crates/jackin-runtime/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'crates/jackin/**/*.rs', 'crates/jackin/benches/**', 'crates/jackin/build.rs', 'crates/jackin/examples/**', 'crates/jackin/src/**', 'crates/jackin/tests/**', 'docker/construct/Dockerfile', 'docker/runtime/agent-status/hooks/claude/report-hook.sh', 'docker/runtime/agent-status/hooks/codex/report-hook.sh', 'docker/runtime/agent-status/hooks/opencode/plugin.js', 'docker/runtime/agent-status/hooks/opencode/report-hook.sh', 'docker/runtime/entrypoint.sh', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-59b22c0cb4e7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-console/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'crates/jackin/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-59b22c0cb4e7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin' && cargo fetch --locked
      - name: Run "Rust crate (jackin)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin:
    name: "Velnor / rust-jackin"
    if: ${{ inputs.unit == 'rust-jackin' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin' && cargo fetch --locked
      - name: Run "Rust crate (jackin)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-agent-status:
    name: "GitHub / rust-jackin-agent-status"
    if: ${{ inputs.unit == 'rust-jackin-agent-status' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-843ffcda479c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-agent-status-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-agent-status/**/*.rs', 'crates/jackin-agent-status/benches/**', 'crates/jackin-agent-status/examples/**', 'crates/jackin-agent-status/src/**', 'crates/jackin-agent-status/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-843ffcda479c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-agent-status-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-843ffcda479c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-agent-status-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-agent-status)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-agent-status' && cargo fetch --locked
      - name: Run "Rust crate (jackin-agent-status)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-agent-status"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-agent-status)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-agent-status:
    name: "Velnor / rust-jackin-agent-status"
    if: ${{ inputs.unit == 'rust-jackin-agent-status' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-agent-status)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-agent-status' && cargo fetch --locked
      - name: Run "Rust crate (jackin-agent-status)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-agent-status"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-brand:
    name: "GitHub / rust-jackin-brand"
    if: ${{ inputs.unit == 'rust-jackin-brand' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-5cc3418b01d0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-brand-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-5cc3418b01d0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-brand-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-5cc3418b01d0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-brand-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-brand)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-brand' && cargo fetch --locked
      - name: Run "Rust crate (jackin-brand)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-brand"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-brand)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-brand:
    name: "Velnor / rust-jackin-brand"
    if: ${{ inputs.unit == 'rust-jackin-brand' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-brand)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-brand' && cargo fetch --locked
      - name: Run "Rust crate (jackin-brand)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-brand"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-build-meta:
    name: "GitHub / rust-jackin-build-meta"
    if: ${{ inputs.unit == 'rust-jackin-build-meta' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-ace0b06b7a31-${{ runner.os }}-${{ runner.arch }}-rust-jackin-build-meta-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-ace0b06b7a31-${{ runner.os }}-${{ runner.arch }}-rust-jackin-build-meta-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-ace0b06b7a31-${{ runner.os }}-${{ runner.arch }}-rust-jackin-build-meta-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-build-meta)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-build-meta' && cargo fetch --locked
      - name: Run "Rust crate (jackin-build-meta)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-build-meta"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-build-meta)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-build-meta:
    name: "Velnor / rust-jackin-build-meta"
    if: ${{ inputs.unit == 'rust-jackin-build-meta' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-build-meta)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-build-meta' && cargo fetch --locked
      - name: Run "Rust crate (jackin-build-meta)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-build-meta"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-capsule:
    name: "GitHub / rust-jackin-capsule"
    if: ${{ inputs.unit == 'rust-jackin-capsule' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-dc1b69c8b47d-${{ runner.os }}-${{ runner.arch }}-rust-jackin-capsule-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-capsule/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-agent-status/**/*.rs', 'crates/jackin-agent-status/benches/**', 'crates/jackin-agent-status/examples/**', 'crates/jackin-agent-status/src/**', 'crates/jackin-agent-status/tests/**', 'crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-capsule/**/*.rs', 'crates/jackin-capsule/benches/**', 'crates/jackin-capsule/build.rs', 'crates/jackin-capsule/examples/**', 'crates/jackin-capsule/src/**', 'crates/jackin-capsule/src/daemon.rs', 'crates/jackin-capsule/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-dc1b69c8b47d-${{ runner.os }}-${{ runner.arch }}-rust-jackin-capsule-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-agent-status/Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-capsule/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-dc1b69c8b47d-${{ runner.os }}-${{ runner.arch }}-rust-jackin-capsule-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-capsule)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-capsule/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-capsule' && cargo fetch --locked
      - name: Run "Rust crate (jackin-capsule)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-capsule"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-capsule)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-capsule/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-capsule:
    name: "Velnor / rust-jackin-capsule"
    if: ${{ inputs.unit == 'rust-jackin-capsule' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-capsule)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-capsule/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-capsule' && cargo fetch --locked
      - name: Run "Rust crate (jackin-capsule)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-capsule"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-config:
    name: "GitHub / rust-jackin-config"
    if: ${{ inputs.unit == 'rust-jackin-config' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-a4a85bcc8c64-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-a4a85bcc8c64-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-a4a85bcc8c64-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-config)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-config' && cargo fetch --locked
      - name: Run "Rust crate (jackin-config)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-config"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-config)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-config:
    name: "Velnor / rust-jackin-config"
    if: ${{ inputs.unit == 'rust-jackin-config' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-config)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-config' && cargo fetch --locked
      - name: Run "Rust crate (jackin-config)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-config"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-config-fuzz:
    name: "GitHub / rust-jackin-config-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-config-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-5937afda161c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-config/fuzz/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/fuzz/**/*.rs', 'crates/jackin-config/fuzz/benches/**', 'crates/jackin-config/fuzz/examples/**', 'crates/jackin-config/fuzz/src/**', 'crates/jackin-config/fuzz/tests/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-5937afda161c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-config/fuzz/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-5937afda161c-${{ runner.os }}-${{ runner.arch }}-rust-jackin-config-fuzz-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-config-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-config/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-config-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-config-fuzz"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-config-fuzz)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-config-fuzz:
    name: "Velnor / rust-jackin-config-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-config-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-config-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-config/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-config/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-config-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-config-fuzz"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-console:
    name: "GitHub / rust-jackin-console"
    if: ${{ inputs.unit == 'rust-jackin-console' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-77ae4336eac2-${{ runner.os }}-${{ runner.arch }}-rust-jackin-console-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-console/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-console/**/*.rs', 'crates/jackin-console/benches/**', 'crates/jackin-console/examples/**', 'crates/jackin-console/src/**', 'crates/jackin-console/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-oppicker/**/*.rs', 'crates/jackin-oppicker/benches/**', 'crates/jackin-oppicker/examples/**', 'crates/jackin-oppicker/src/**', 'crates/jackin-oppicker/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-77ae4336eac2-${{ runner.os }}-${{ runner.arch }}-rust-jackin-console-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-console/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-77ae4336eac2-${{ runner.os }}-${{ runner.arch }}-rust-jackin-console-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-console)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-console/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-console' && cargo fetch --locked
      - name: Run "Rust crate (jackin-console)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-console"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-console)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-console/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-console:
    name: "Velnor / rust-jackin-console"
    if: ${{ inputs.unit == 'rust-jackin-console' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-console)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-console/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-console' && cargo fetch --locked
      - name: Run "Rust crate (jackin-console)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-console"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-core:
    name: "GitHub / rust-jackin-core"
    if: ${{ inputs.unit == 'rust-jackin-core' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-45a90aeb37a3-${{ runner.os }}-${{ runner.arch }}-rust-jackin-core-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-45a90aeb37a3-${{ runner.os }}-${{ runner.arch }}-rust-jackin-core-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-45a90aeb37a3-${{ runner.os }}-${{ runner.arch }}-rust-jackin-core-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-core)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-core' && cargo fetch --locked
      - name: Run "Rust crate (jackin-core)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-core"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-core)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-core:
    name: "Velnor / rust-jackin-core"
    if: ${{ inputs.unit == 'rust-jackin-core' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-core)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-core' && cargo fetch --locked
      - name: Run "Rust crate (jackin-core)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-core"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-dev:
    name: "GitHub / rust-jackin-dev"
    if: ${{ inputs.unit == 'rust-jackin-dev' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-727f37873aed-${{ runner.os }}-${{ runner.arch }}-rust-jackin-dev-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-dev/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-dev/**/*.rs', 'crates/jackin-dev/benches/**', 'crates/jackin-dev/examples/**', 'crates/jackin-dev/src/**', 'crates/jackin-dev/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-727f37873aed-${{ runner.os }}-${{ runner.arch }}-rust-jackin-dev-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-dev/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-727f37873aed-${{ runner.os }}-${{ runner.arch }}-rust-jackin-dev-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-dev)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-dev/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-dev' && cargo fetch --locked
      - name: Run "Rust crate (jackin-dev)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-dev"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-dev)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-dev/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-dev:
    name: "Velnor / rust-jackin-dev"
    if: ${{ inputs.unit == 'rust-jackin-dev' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-dev)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-dev/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-dev' && cargo fetch --locked
      - name: Run "Rust crate (jackin-dev)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-dev"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-diagnostics:
    name: "GitHub / rust-jackin-diagnostics"
    if: ${{ inputs.unit == 'rust-jackin-diagnostics' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-6f344bcdd904-${{ runner.os }}-${{ runner.arch }}-rust-jackin-diagnostics-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-6f344bcdd904-${{ runner.os }}-${{ runner.arch }}-rust-jackin-diagnostics-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-6f344bcdd904-${{ runner.os }}-${{ runner.arch }}-rust-jackin-diagnostics-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-diagnostics)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-diagnostics' && cargo fetch --locked
      - name: Run "Rust crate (jackin-diagnostics)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-diagnostics"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-diagnostics)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-diagnostics:
    name: "Velnor / rust-jackin-diagnostics"
    if: ${{ inputs.unit == 'rust-jackin-diagnostics' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-diagnostics)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-diagnostics' && cargo fetch --locked
      - name: Run "Rust crate (jackin-diagnostics)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-diagnostics"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-docker:
    name: "GitHub / rust-jackin-docker"
    if: ${{ inputs.unit == 'rust-jackin-docker' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-e57a5b202e12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-docker-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-e57a5b202e12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-docker-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-e57a5b202e12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-docker-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-docker)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-docker' && cargo fetch --locked
      - name: Run "Rust crate (jackin-docker)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-docker"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-docker)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-docker:
    name: "Velnor / rust-jackin-docker"
    if: ${{ inputs.unit == 'rust-jackin-docker' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-docker)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-docker' && cargo fetch --locked
      - name: Run "Rust crate (jackin-docker)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-docker"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-env:
    name: "GitHub / rust-jackin-env"
    if: ${{ inputs.unit == 'rust-jackin-env' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-34c64edca3ef-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-34c64edca3ef-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-34c64edca3ef-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-env)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-env' && cargo fetch --locked
      - name: Run "Rust crate (jackin-env)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-env"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-env)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-env:
    name: "Velnor / rust-jackin-env"
    if: ${{ inputs.unit == 'rust-jackin-env' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-env)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-env' && cargo fetch --locked
      - name: Run "Rust crate (jackin-env)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-env"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-env-fuzz:
    name: "GitHub / rust-jackin-env-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-env-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-14922bb8b41a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-env/fuzz/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/fuzz/**/*.rs', 'crates/jackin-env/fuzz/benches/**', 'crates/jackin-env/fuzz/examples/**', 'crates/jackin-env/fuzz/src/**', 'crates/jackin-env/fuzz/tests/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-14922bb8b41a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-env/fuzz/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-14922bb8b41a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-env-fuzz-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-env-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-env/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-env-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-env-fuzz"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-env-fuzz)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-env-fuzz:
    name: "Velnor / rust-jackin-env-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-env-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-env-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-env/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-env/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-env-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-env-fuzz"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-host:
    name: "GitHub / rust-jackin-host"
    if: ${{ inputs.unit == 'rust-jackin-host' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-1691020b2fa8-${{ runner.os }}-${{ runner.arch }}-rust-jackin-host-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-host/**/*.rs', 'crates/jackin-host/benches/**', 'crates/jackin-host/examples/**', 'crates/jackin-host/src/**', 'crates/jackin-host/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-1691020b2fa8-${{ runner.os }}-${{ runner.arch }}-rust-jackin-host-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-1691020b2fa8-${{ runner.os }}-${{ runner.arch }}-rust-jackin-host-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-host)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-host/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-host' && cargo fetch --locked
      - name: Run "Rust crate (jackin-host)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-host"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-host)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-host/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-host:
    name: "Velnor / rust-jackin-host"
    if: ${{ inputs.unit == 'rust-jackin-host' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-host)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-host/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-host' && cargo fetch --locked
      - name: Run "Rust crate (jackin-host)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-host"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-image:
    name: "GitHub / rust-jackin-image"
    if: ${{ inputs.unit == 'rust-jackin-image' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-a6ca594bc2f1-${{ runner.os }}-${{ runner.arch }}-rust-jackin-image-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-image/**/*.rs', 'crates/jackin-image/benches/**', 'crates/jackin-image/build.rs', 'crates/jackin-image/examples/**', 'crates/jackin-image/src/**', 'crates/jackin-image/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'docker/runtime/agent-status/hooks/claude/report-hook.sh', 'docker/runtime/agent-status/hooks/codex/report-hook.sh', 'docker/runtime/agent-status/hooks/opencode/plugin.js', 'docker/runtime/agent-status/hooks/opencode/report-hook.sh', 'docker/runtime/entrypoint.sh', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-a6ca594bc2f1-${{ runner.os }}-${{ runner.arch }}-rust-jackin-image-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-a6ca594bc2f1-${{ runner.os }}-${{ runner.arch }}-rust-jackin-image-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-image)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-image/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-image' && cargo fetch --locked
      - name: Run "Rust crate (jackin-image)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-image"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-image)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-image/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-image:
    name: "Velnor / rust-jackin-image"
    if: ${{ inputs.unit == 'rust-jackin-image' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-image)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-image/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-image' && cargo fetch --locked
      - name: Run "Rust crate (jackin-image)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-image"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-instance:
    name: "GitHub / rust-jackin-instance"
    if: ${{ inputs.unit == 'rust-jackin-instance' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-ff532092f3ab-${{ runner.os }}-${{ runner.arch }}-rust-jackin-instance-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-instance/**/*.rs', 'crates/jackin-instance/benches/**', 'crates/jackin-instance/examples/**', 'crates/jackin-instance/src/**', 'crates/jackin-instance/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-ff532092f3ab-${{ runner.os }}-${{ runner.arch }}-rust-jackin-instance-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-ff532092f3ab-${{ runner.os }}-${{ runner.arch }}-rust-jackin-instance-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-instance)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-instance' && cargo fetch --locked
      - name: Run "Rust crate (jackin-instance)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-instance"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-instance)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-instance:
    name: "Velnor / rust-jackin-instance"
    if: ${{ inputs.unit == 'rust-jackin-instance' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-instance)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-instance' && cargo fetch --locked
      - name: Run "Rust crate (jackin-instance)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-instance"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-isolation:
    name: "GitHub / rust-jackin-isolation"
    if: ${{ inputs.unit == 'rust-jackin-isolation' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-b6ce87903477-${{ runner.os }}-${{ runner.arch }}-rust-jackin-isolation-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-isolation/**/*.rs', 'crates/jackin-isolation/benches/**', 'crates/jackin-isolation/examples/**', 'crates/jackin-isolation/src/**', 'crates/jackin-isolation/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-b6ce87903477-${{ runner.os }}-${{ runner.arch }}-rust-jackin-isolation-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-b6ce87903477-${{ runner.os }}-${{ runner.arch }}-rust-jackin-isolation-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-isolation)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-isolation' && cargo fetch --locked
      - name: Run "Rust crate (jackin-isolation)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-isolation"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-isolation)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-isolation:
    name: "Velnor / rust-jackin-isolation"
    if: ${{ inputs.unit == 'rust-jackin-isolation' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-isolation)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-isolation' && cargo fetch --locked
      - name: Run "Rust crate (jackin-isolation)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-isolation"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-launch:
    name: "GitHub / rust-jackin-launch"
    if: ${{ inputs.unit == 'rust-jackin-launch' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-25f335fe2594-${{ runner.os }}-${{ runner.arch }}-rust-jackin-launch-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-launch/**/*.rs', 'crates/jackin-launch/benches/**', 'crates/jackin-launch/build.rs', 'crates/jackin-launch/examples/**', 'crates/jackin-launch/src/**', 'crates/jackin-launch/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-25f335fe2594-${{ runner.os }}-${{ runner.arch }}-rust-jackin-launch-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-25f335fe2594-${{ runner.os }}-${{ runner.arch }}-rust-jackin-launch-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-launch)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-launch' && cargo fetch --locked
      - name: Run "Rust crate (jackin-launch)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-launch"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-launch)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-launch:
    name: "Velnor / rust-jackin-launch"
    if: ${{ inputs.unit == 'rust-jackin-launch' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-launch)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-launch' && cargo fetch --locked
      - name: Run "Rust crate (jackin-launch)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-launch"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-lints:
    name: "GitHub / rust-jackin-lints"
    if: ${{ inputs.unit == 'rust-jackin-lints' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-a3ea4f0ea42b-${{ runner.os }}-${{ runner.arch }}-rust-jackin-lints-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-lints/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-lints/**/*.rs', 'crates/jackin-lints/benches/**', 'crates/jackin-lints/examples/**', 'crates/jackin-lints/src/**', 'crates/jackin-lints/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-a3ea4f0ea42b-${{ runner.os }}-${{ runner.arch }}-rust-jackin-lints-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-lints/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-a3ea4f0ea42b-${{ runner.os }}-${{ runner.arch }}-rust-jackin-lints-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin_lints)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-lints/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-lints' && cargo fetch --locked
      - name: Run "Rust crate (jackin_lints)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin_lints"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin_lints)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-lints/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-lints:
    name: "Velnor / rust-jackin-lints"
    if: ${{ inputs.unit == 'rust-jackin-lints' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin_lints)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-lints/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-lints' && cargo fetch --locked
      - name: Run "Rust crate (jackin_lints)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin_lints"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-manifest:
    name: "GitHub / rust-jackin-manifest"
    if: ${{ inputs.unit == 'rust-jackin-manifest' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-6cb2e1e3dd53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-6cb2e1e3dd53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-6cb2e1e3dd53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-manifest)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-manifest' && cargo fetch --locked
      - name: Run "Rust crate (jackin-manifest)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-manifest"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-manifest)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-manifest:
    name: "Velnor / rust-jackin-manifest"
    if: ${{ inputs.unit == 'rust-jackin-manifest' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-manifest)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-manifest' && cargo fetch --locked
      - name: Run "Rust crate (jackin-manifest)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-manifest"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-manifest-fuzz:
    name: "GitHub / rust-jackin-manifest-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-manifest-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-c0d2f1a5ec91-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-manifest/fuzz/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/fuzz/**/*.rs', 'crates/jackin-manifest/fuzz/benches/**', 'crates/jackin-manifest/fuzz/examples/**', 'crates/jackin-manifest/fuzz/src/**', 'crates/jackin-manifest/fuzz/tests/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-c0d2f1a5ec91-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-manifest/fuzz/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-c0d2f1a5ec91-${{ runner.os }}-${{ runner.arch }}-rust-jackin-manifest-fuzz-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-manifest-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-manifest/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-manifest-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-manifest-fuzz"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-manifest-fuzz)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-manifest-fuzz:
    name: "Velnor / rust-jackin-manifest-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-manifest-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-manifest-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-manifest/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-manifest/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-manifest-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-manifest-fuzz"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-oppicker:
    name: "GitHub / rust-jackin-oppicker"
    if: ${{ inputs.unit == 'rust-jackin-oppicker' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-e8fbc033abe6-${{ runner.os }}-${{ runner.arch }}-rust-jackin-oppicker-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-oppicker/**/*.rs', 'crates/jackin-oppicker/benches/**', 'crates/jackin-oppicker/examples/**', 'crates/jackin-oppicker/src/**', 'crates/jackin-oppicker/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-e8fbc033abe6-${{ runner.os }}-${{ runner.arch }}-rust-jackin-oppicker-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-e8fbc033abe6-${{ runner.os }}-${{ runner.arch }}-rust-jackin-oppicker-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-oppicker)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-oppicker' && cargo fetch --locked
      - name: Run "Rust crate (jackin-oppicker)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-oppicker"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-oppicker)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-oppicker:
    name: "Velnor / rust-jackin-oppicker"
    if: ${{ inputs.unit == 'rust-jackin-oppicker' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-oppicker)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-oppicker/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-oppicker' && cargo fetch --locked
      - name: Run "Rust crate (jackin-oppicker)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-oppicker"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-otlp-testbed:
    name: "GitHub / rust-jackin-otlp-testbed"
    if: ${{ inputs.unit == 'rust-jackin-otlp-testbed' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-9f20d3bfdb22-${{ runner.os }}-${{ runner.arch }}-rust-jackin-otlp-testbed-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-9f20d3bfdb22-${{ runner.os }}-${{ runner.arch }}-rust-jackin-otlp-testbed-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-9f20d3bfdb22-${{ runner.os }}-${{ runner.arch }}-rust-jackin-otlp-testbed-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-otlp-testbed)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-otlp-testbed' && cargo fetch --locked
      - name: Run "Rust crate (jackin-otlp-testbed)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-otlp-testbed"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-otlp-testbed)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-otlp-testbed:
    name: "Velnor / rust-jackin-otlp-testbed"
    if: ${{ inputs.unit == 'rust-jackin-otlp-testbed' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-otlp-testbed)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-otlp-testbed' && cargo fetch --locked
      - name: Run "Rust crate (jackin-otlp-testbed)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-otlp-testbed"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-pr-trailers:
    name: "GitHub / rust-jackin-pr-trailers"
    if: ${{ inputs.unit == 'rust-jackin-pr-trailers' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-30920da7eb30-${{ runner.os }}-${{ runner.arch }}-rust-jackin-pr-trailers-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-pr-trailers/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-pr-trailers/**/*.rs', 'crates/jackin-pr-trailers/benches/**', 'crates/jackin-pr-trailers/examples/**', 'crates/jackin-pr-trailers/src/**', 'crates/jackin-pr-trailers/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-30920da7eb30-${{ runner.os }}-${{ runner.arch }}-rust-jackin-pr-trailers-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-pr-trailers/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-30920da7eb30-${{ runner.os }}-${{ runner.arch }}-rust-jackin-pr-trailers-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-pr-trailers)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-pr-trailers/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-pr-trailers' && cargo fetch --locked
      - name: Run "Rust crate (jackin-pr-trailers)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-pr-trailers"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-pr-trailers)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-pr-trailers/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-pr-trailers:
    name: "Velnor / rust-jackin-pr-trailers"
    if: ${{ inputs.unit == 'rust-jackin-pr-trailers' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-pr-trailers)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-pr-trailers/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-pr-trailers' && cargo fetch --locked
      - name: Run "Rust crate (jackin-pr-trailers)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-pr-trailers"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-process:
    name: "GitHub / rust-jackin-process"
    if: ${{ inputs.unit == 'rust-jackin-process' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-b41e1fbb617a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-process-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-b41e1fbb617a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-process-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-b41e1fbb617a-${{ runner.os }}-${{ runner.arch }}-rust-jackin-process-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-process)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-process' && cargo fetch --locked
      - name: Run "Rust crate (jackin-process)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-process"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-process)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-process:
    name: "Velnor / rust-jackin-process"
    if: ${{ inputs.unit == 'rust-jackin-process' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-process)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-process' && cargo fetch --locked
      - name: Run "Rust crate (jackin-process)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-process"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-protocol:
    name: "GitHub / rust-jackin-protocol"
    if: ${{ inputs.unit == 'rust-jackin-protocol' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-c6d1b5394509-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-c6d1b5394509-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-c6d1b5394509-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-protocol)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-protocol' && cargo fetch --locked
      - name: Run "Rust crate (jackin-protocol)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-protocol"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-protocol)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-protocol:
    name: "Velnor / rust-jackin-protocol"
    if: ${{ inputs.unit == 'rust-jackin-protocol' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-protocol)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-protocol' && cargo fetch --locked
      - name: Run "Rust crate (jackin-protocol)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-protocol"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-protocol-fuzz:
    name: "GitHub / rust-jackin-protocol-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-protocol-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-bf78a71aacb0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-protocol/fuzz/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/fuzz/**/*.rs', 'crates/jackin-protocol/fuzz/benches/**', 'crates/jackin-protocol/fuzz/examples/**', 'crates/jackin-protocol/fuzz/src/**', 'crates/jackin-protocol/fuzz/tests/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-bf78a71aacb0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-fuzz-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-protocol/fuzz/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-bf78a71aacb0-${{ runner.os }}-${{ runner.arch }}-rust-jackin-protocol-fuzz-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-protocol-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-protocol/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-protocol-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-protocol-fuzz"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-protocol-fuzz)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-protocol-fuzz:
    name: "Velnor / rust-jackin-protocol-fuzz"
    if: ${{ inputs.unit == 'rust-jackin-protocol-fuzz' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-protocol-fuzz)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-protocol/fuzz/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-protocol/fuzz' && cargo fetch --locked
      - name: Run "Rust crate (jackin-protocol-fuzz)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-protocol-fuzz"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-runtime:
    name: "GitHub / rust-jackin-runtime"
    if: ${{ inputs.unit == 'rust-jackin-runtime' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-4f496b864589-${{ runner.os }}-${{ runner.arch }}-rust-jackin-runtime-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-host/**/*.rs', 'crates/jackin-host/benches/**', 'crates/jackin-host/examples/**', 'crates/jackin-host/src/**', 'crates/jackin-host/tests/**', 'crates/jackin-image/**/*.rs', 'crates/jackin-image/benches/**', 'crates/jackin-image/build.rs', 'crates/jackin-image/examples/**', 'crates/jackin-image/src/**', 'crates/jackin-image/tests/**', 'crates/jackin-instance/**/*.rs', 'crates/jackin-instance/benches/**', 'crates/jackin-instance/examples/**', 'crates/jackin-instance/src/**', 'crates/jackin-instance/tests/**', 'crates/jackin-isolation/**/*.rs', 'crates/jackin-isolation/benches/**', 'crates/jackin-isolation/examples/**', 'crates/jackin-isolation/src/**', 'crates/jackin-isolation/tests/**', 'crates/jackin-launch/**/*.rs', 'crates/jackin-launch/benches/**', 'crates/jackin-launch/build.rs', 'crates/jackin-launch/examples/**', 'crates/jackin-launch/src/**', 'crates/jackin-launch/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-runtime/**/*.rs', 'crates/jackin-runtime/benches/**', 'crates/jackin-runtime/build.rs', 'crates/jackin-runtime/examples/**', 'crates/jackin-runtime/src/**', 'crates/jackin-runtime/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'docker/runtime/agent-status/hooks/claude/report-hook.sh', 'docker/runtime/agent-status/hooks/codex/report-hook.sh', 'docker/runtime/agent-status/hooks/opencode/plugin.js', 'docker/runtime/agent-status/hooks/opencode/report-hook.sh', 'docker/runtime/entrypoint.sh', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-4f496b864589-${{ runner.os }}-${{ runner.arch }}-rust-jackin-runtime-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-build-meta/Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-docker/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-host/Cargo.toml', 'crates/jackin-image/Cargo.toml', 'crates/jackin-instance/Cargo.toml', 'crates/jackin-isolation/Cargo.toml', 'crates/jackin-launch/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-4f496b864589-${{ runner.os }}-${{ runner.arch }}-rust-jackin-runtime-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-runtime)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-runtime' && cargo fetch --locked
      - name: Run "Rust crate (jackin-runtime)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-runtime"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-runtime)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-runtime:
    name: "Velnor / rust-jackin-runtime"
    if: ${{ inputs.unit == 'rust-jackin-runtime' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-runtime)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-runtime/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-runtime' && cargo fetch --locked
      - name: Run "Rust crate (jackin-runtime)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-runtime"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-telemetry:
    name: "GitHub / rust-jackin-telemetry"
    if: ${{ inputs.unit == 'rust-jackin-telemetry' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-c03b3f4ffe23-${{ runner.os }}-${{ runner.arch }}-rust-jackin-telemetry-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-c03b3f4ffe23-${{ runner.os }}-${{ runner.arch }}-rust-jackin-telemetry-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-c03b3f4ffe23-${{ runner.os }}-${{ runner.arch }}-rust-jackin-telemetry-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-telemetry)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-telemetry' && cargo fetch --locked
      - name: Run "Rust crate (jackin-telemetry)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-telemetry"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-telemetry)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-telemetry:
    name: "Velnor / rust-jackin-telemetry"
    if: ${{ inputs.unit == 'rust-jackin-telemetry' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-telemetry)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-telemetry' && cargo fetch --locked
      - name: Run "Rust crate (jackin-telemetry)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-telemetry"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-test-support:
    name: "GitHub / rust-jackin-test-support"
    if: ${{ inputs.unit == 'rust-jackin-test-support' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-e77214541a53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-test-support-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-e77214541a53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-test-support-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-manifest/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-e77214541a53-${{ runner.os }}-${{ runner.arch }}-rust-jackin-test-support-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-test-support)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-test-support' && cargo fetch --locked
      - name: Run "Rust crate (jackin-test-support)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-test-support"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-test-support)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-test-support:
    name: "Velnor / rust-jackin-test-support"
    if: ${{ inputs.unit == 'rust-jackin-test-support' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-test-support)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-test-support/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-test-support' && cargo fetch --locked
      - name: Run "Rust crate (jackin-test-support)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-test-support"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-tui:
    name: "GitHub / rust-jackin-tui"
    if: ${{ inputs.unit == 'rust-jackin-tui' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-96c5c29c8c12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-tui-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-96c5c29c8c12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-tui-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-brand/Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-96c5c29c8c12-${{ runner.os }}-${{ runner.arch }}-rust-jackin-tui-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-tui)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-tui' && cargo fetch --locked
      - name: Run "Rust crate (jackin-tui)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-tui"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-tui)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-tui:
    name: "Velnor / rust-jackin-tui"
    if: ${{ inputs.unit == 'rust-jackin-tui' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-tui)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-tui/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-tui' && cargo fetch --locked
      - name: Run "Rust crate (jackin-tui)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-tui"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-usage:
    name: "GitHub / rust-jackin-usage"
    if: ${{ inputs.unit == 'rust-jackin-usage' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-f62fef3e61b7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-f62fef3e61b7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-f62fef3e61b7-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-usage)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-usage' && cargo fetch --locked
      - name: Run "Rust crate (jackin-usage)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-usage"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-usage)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-usage:
    name: "Velnor / rust-jackin-usage"
    if: ${{ inputs.unit == 'rust-jackin-usage' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-usage)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-usage' && cargo fetch --locked
      - name: Run "Rust crate (jackin-usage)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-usage"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-usage-ffi:
    name: "GitHub / rust-jackin-usage-ffi"
    if: ${{ inputs.unit == 'rust-jackin-usage-ffi' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-f3432b2af2cc-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-ffi-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-usage-ffi/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-usage-ffi/**/*.rs', 'crates/jackin-usage-ffi/benches/**', 'crates/jackin-usage-ffi/examples/**', 'crates/jackin-usage-ffi/src/**', 'crates/jackin-usage-ffi/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-f3432b2af2cc-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-ffi-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-config/Cargo.toml', 'crates/jackin-core/Cargo.toml', 'crates/jackin-diagnostics/Cargo.toml', 'crates/jackin-env/Cargo.toml', 'crates/jackin-otlp-testbed/Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-protocol/Cargo.toml', 'crates/jackin-telemetry/Cargo.toml', 'crates/jackin-usage-ffi/Cargo.toml', 'crates/jackin-usage/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-f3432b2af2cc-${{ runner.os }}-${{ runner.arch }}-rust-jackin-usage-ffi-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-usage-ffi)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage-ffi/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-usage-ffi' && cargo fetch --locked
      - name: Run "Rust crate (jackin-usage-ffi)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-usage-ffi"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-usage-ffi)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage-ffi/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-usage-ffi:
    name: "Velnor / rust-jackin-usage-ffi"
    if: ${{ inputs.unit == 'rust-jackin-usage-ffi' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-usage-ffi)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-usage-ffi/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-usage-ffi' && cargo fetch --locked
      - name: Run "Rust crate (jackin-usage-ffi)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-usage-ffi"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-jackin-xtask:
    name: "GitHub / rust-jackin-xtask"
    if: ${{ inputs.unit == 'rust-jackin-xtask' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: aqua:nextest-rs/nextest/cargo-nextest
          cache: true
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-e36d44c875aa-${{ runner.os }}-${{ runner.arch }}-rust-jackin-xtask-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-xtask/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-${{ hashFiles('crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-xtask/**/*.rs', 'crates/jackin-xtask/benches/**', 'crates/jackin-xtask/examples/**', 'crates/jackin-xtask/src/**', 'crates/jackin-xtask/tests/**', 'mise.lock') }}
          restore-keys: |
            velnor-mbx-v3-e36d44c875aa-${{ runner.os }}-${{ runner.arch }}-rust-jackin-xtask-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml', 'crates/jackin-process/Cargo.toml', 'crates/jackin-xtask/Cargo.toml', 'rust-toolchain', 'rust-toolchain.toml') }}-
            velnor-mbx-v3-e36d44c875aa-${{ runner.os }}-${{ runner.arch }}-rust-jackin-xtask-
          save-on-workflow-dispatch: true
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust crate (jackin-xtask)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-xtask/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-xtask' && cargo fetch --locked
      - name: Run "Rust crate (jackin-xtask)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-xtask"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust crate (jackin-xtask)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-xtask/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-rust-jackin-xtask:
    name: "Velnor / rust-jackin-xtask"
    if: ${{ inputs.unit == 'rust-jackin-xtask' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (jackin-xtask)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/jackin-xtask/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/jackin-xtask' && cargo fetch --locked
      - name: Run "Rust crate (jackin-xtask)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · jackin-xtask"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  github-rust-policy:
    name: "GitHub / rust-policy"
    if: ${{ inputs.unit == 'rust-policy' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'github' || github.event.inputs.lanes == 'both'))) }}
    runs-on: ubuntu-26.04
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-984396e67ef674189cf15f4e5bff3c8785aa3180-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 984396e67ef674189cf15f4e5bff3c8785aa3180
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-mbx-v3-84cd56273a76-${{ runner.os }}-${{ runner.arch }}-rust-policy-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml') }}-${{ hashFiles('./**/*.rs') }}
          restore-keys: |
            velnor-mbx-v3-84cd56273a76-${{ runner.os }}-${{ runner.arch }}-rust-policy-${{ hashFiles('.cargo/**', 'Cargo.lock', 'Cargo.toml') }}-
            velnor-mbx-v3-84cd56273a76-${{ runner.os }}-${{ runner.arch }}-rust-policy-
          save-on-workflow-dispatch: true
      - name: Set up cargo-deny
        uses: taiki-e/install-action@0758d235715de2f3551eacc980d9ae8fce9342c3 # v2.87.3
        with:
          tool: cargo-deny
          fallback: none
      - name: Set up cargo-audit
        uses: taiki-e/install-action@0758d235715de2f3551eacc980d9ae8fce9342c3 # v2.87.3
        with:
          tool: cargo-audit
          fallback: none
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --silent --show-error --location --retry 3 --retry-delay 2 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
            sudo ln -sf /usr/local/bin/mold "$(realpath /usr/bin/ld 2>/dev/null || printf /usr/bin/ld)"
          fi
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main'))) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Restore "Rust dependency policy" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Run "Rust dependency policy" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · Rust dependency policy"
          VELNOR_LANE_NAME: GitHub
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0
      - name: Save "Rust dependency policy" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'Cargo.lock') }}

  velnor-rust-policy:
    name: "Velnor / rust-policy"
    if: ${{ inputs.unit == 'rust-policy' && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.lanes == 'velnor' || github.event.inputs.lanes == 'both'))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust dependency policy" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Run "Rust dependency policy" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · Rust dependency policy"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

