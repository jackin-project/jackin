# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "Desktop merge cadence"
run-name: "Desktop merge cadence · ${{ github.event_name }}"

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-merge-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  desktop-merge:
    name: "Desktop merge cadence"
    runs-on: macos-26
    timeout-minutes: 90
    env:
      CARGO_TERM_COLOR: always
      DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
      MACOSX_DEPLOYMENT_TARGET: "26.0"
      MISE_NOT_FOUND_AUTO_INSTALL: "false"
      MISE_TASK_RUN_AUTO_INSTALL: "false"
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: "cargo-binstall rust aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify ripgrep"
          cache: true
          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
      - name: Run desktop-merge
        run: mise run desktop-merge
