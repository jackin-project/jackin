# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "Desktop scheduled cadence"
run-name: "Desktop scheduled cadence · ${{ github.event_name }}"

on:
  schedule:
    - cron: "41 4 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-scheduled-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  desktop-scheduled:
    name: "Desktop scheduled cadence"
    runs-on: macos-26
    timeout-minutes: 90
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
      DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
      MACOSX_DEPLOYMENT_TARGET: "26.0"
      MISE_NOT_FOUND_AUTO_INSTALL: "false"
      MISE_TASK_RUN_AUTO_INSTALL: "false"
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "true"
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: "cargo-binstall rust aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify periphery ripgrep"
          cache: true
          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
      - name: Run desktop-scheduled
        run: mise run desktop-scheduled
