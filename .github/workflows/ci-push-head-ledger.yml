# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "CI push-head ledger"
run-name: "CI push-head ledger · ${{ github.event_name }}"

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-push-head-ledger-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci-push-head-ledger:
    name: "CI push-head ledger"
    runs-on: ubuntu-26.04
    timeout-minutes: 10
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false
      - name: Run ci-push-head-ledger
        run: mise run ci-push-head-ledger
      - name: Upload ci-push-head-ledger artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ci-push-head-ledger
          path: |
            target/ci-push-head-ledger/
          if-no-files-found: warn
