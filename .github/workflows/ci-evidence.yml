# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "CI first-attempt evidence"
run-name: "CI first-attempt evidence · ${{ github.event_name }}"

on:
  schedule:
    - cron: "47 4 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-evidence-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci-evidence:
    name: "CI first-attempt evidence"
    runs-on: ubuntu-26.04
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
    env:
      GH_TOKEN: "${{ github.token }}"
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false
      - name: Run ci-evidence
        run: mise run ci-evidence
      - name: Upload ci-evidence artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ci-evidence
          path: |
            target/ci-evidence/
          if-no-files-found: warn
