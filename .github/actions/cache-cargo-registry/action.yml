name: Cache Cargo registry
description: Restore the shared Cargo registry/index/git DB cache.

outputs:
  cache-hit:
    description: Whether the exact Cargo registry cache key was restored.
    value: ${{ steps.restore.outputs.cache-hit }}
  cache-primary-key:
    description: Primary Cargo registry cache key.
    value: ${{ steps.restore.outputs.cache-primary-key }}

runs:
  using: composite
  steps:
    - name: Restore Cargo registry
      id: restore
      uses: actions/cache/restore@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git/db
        key: cargo-registry-v3-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', '.cargo/config.toml') }}
        restore-keys: |
          cargo-registry-v3-${{ runner.os }}-${{ runner.arch }}-
