# SPDX-FileCopyrightText: 2026 Alexey Zhokhov
# SPDX-License-Identifier: Apache-2.0

name: Cache Cargo registry
description: Restore the shared Cargo registry/index/git DB cache.

outputs:
  cache-hit:
    description: Whether the exact Cargo registry cache key was restored.
    value: ${{ steps.restore.outputs.cache-hit }}
  cache-primary-key:
    description: Primary Cargo registry cache key.
    value: ${{ steps.restore.outputs.cache-primary-key }}

runs:
  using: composite
  steps:
    - name: Restore Cargo registry
      id: restore
      uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git/db
        key: cargo-registry-v5-${{ hashFiles('Cargo.lock', 'crates/**/fuzz/Cargo.lock', 'rust-toolchain.toml', '.cargo/config.toml') }}
        restore-keys: |
          cargo-registry-v5-
          cargo-registry-v4-
    - name: Fetch Cargo registry
      if: steps.restore.outputs.cache-hit != 'true'
      shell: bash
      env:
        CARGO_NET_OFFLINE: "false"
        CARGO_NET_RETRY: "10"
        RUSTC_WRAPPER: ""
      run: |
        set -euo pipefail
        attempt=1
        until cargo fetch --locked; do
          if [ "$attempt" -ge 3 ]; then
            exit 1
          fi
          sleep "$((attempt * 10))"
          attempt="$((attempt + 1))"
        done

        while IFS= read -r manifest; do
          attempt=1
          until cargo fetch --manifest-path "$manifest" --locked; do
            if [ "$attempt" -ge 3 ]; then
              exit 1
            fi
            sleep "$((attempt * 10))"
            attempt="$((attempt + 1))"
          done
        done < <(find crates -path '*/fuzz/Cargo.toml' -type f | sort)
    - name: Save Cargo registry
      if: steps.restore.outputs.cache-hit != 'true'
      continue-on-error: true
      uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git/db
        key: ${{ steps.restore.outputs.cache-primary-key }}
