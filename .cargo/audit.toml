[advisories]
ignore = [
    # Transitive through sigstore/openidconnect. No patched rsa exists; jackin'
    # uses sigstore for image verification, not network-observable RSA decrypt.
    "RUSTSEC-2023-0071",
]

[database]
fetch = true
stale = false

[output]
deny = ["warnings"]
quiet = false

[yanked]
enabled = true
