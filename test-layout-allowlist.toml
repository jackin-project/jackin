# Test-layout ratchet — grandfathered violations of the one-tests.rs rule.
# The gate (`cargo xtask lint tests`) fails on any violation NOT listed here.
# Delete an entry when its file is fixed; the list may only shrink.
files = [
    "crates/jackin-capsule/src/tui/selection/tests.rs",
    "crates/jackin-console/src/tui/console/tests.rs",
    "crates/jackin-console/src/tui/op_picker/tests.rs",
    "crates/jackin-console/src/tui/screens/workspaces/view/list/tests.rs",
    "crates/jackin-instance/src/auth/tests.rs",
]

