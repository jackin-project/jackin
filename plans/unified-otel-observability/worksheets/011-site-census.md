# Plan 011 per-site semantic-boundary census

This worksheet expands the reviewed file classifications in [`011-census.md`](011-census.md) to the exact production sites required by the reopened audit. Generic macro anchors come from commit `21e16e11`, the snapshot whose filtered totals exactly reconcile to 501 invocations. Scoped diagnostic standard-error anchors come from the same snapshot. Raw-tracing anchors reflect the final tree and remain permitted only where the syntax-aware registry policy recognizes an exact composition-root or governed-facade implementation construct.

## Generic prose macro sites (501)

| Site | Mechanism | Landed decision and semantic owner |
|---|---|---|
| `crates/jackin-capsule/src/alloc_telemetry.rs:48` | `telemetry_info!` | COMPLETE — developer-only DHAT state stays outside production telemetry and emits no generic lifecycle log |
| `crates/jackin-capsule/src/alloc_telemetry.rs:57` | `telemetry_info!` | COMPLETE — developer-only DHAT state stays outside production telemetry and emits no generic lifecycle log |
| `crates/jackin-capsule/src/attach_protocol.rs:146` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:154` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:181` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:189` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:197` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:215` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:228` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:247` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:264` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:280` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:290` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:297` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:301` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:339` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:396` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:400` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:476` | `telemetry_debug!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:479` | `telemetry_error!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:490` | `telemetry_warn!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:498` | `telemetry_error!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:520` | `telemetry_warn!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:527` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:558` | `telemetry_warn!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:560` | `telemetry_error!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/attach_protocol.rs:588` | `telemetry_info!` | COMPLETE — detached handshake/control failures automatically emit one bodyless typed error from `DetachedCompletion`; persistent attach failures retain one owning `rpc_error`; expected shutdown/channel closure is silent; timeout and join failures use bounded types |
| `crates/jackin-capsule/src/client.rs:42` | `telemetry_info!` | COMPLETE — swallowed hook-report failures emit one bodyless `rpc_error` through `ResultTelemetryExt` |
| `crates/jackin-capsule/src/client_writer.rs:154` | `telemetry_info!` | COMPLETE — expected receiver closure is silent and per-frame structural narration is deleted; bounded render metrics retain byte, cursor-move, and painted-cell health without terminal content or geometry |
| `crates/jackin-capsule/src/client_writer.rs:171` | `telemetry_debug!` | COMPLETE — expected receiver closure is silent and per-frame structural narration is deleted; bounded render metrics retain byte, cursor-move, and painted-cell health without terminal content or geometry |
| `crates/jackin-capsule/src/clipboard.rs:38` | `telemetry_info!` | COMPLETE — cleanup failures emit bodyless `io_error`, non-file recovery emits one governed warning, and stale transfers coalesce into one bodyless recovered-degradation warning per cleanup pass; paths, transfer identity, format, and sizes are excluded |
| `crates/jackin-capsule/src/clipboard.rs:200` | `telemetry_debug!` | COMPLETE — cleanup failures emit bodyless `io_error`, non-file recovery emits one governed warning, and stale transfers coalesce into one bodyless recovered-degradation warning per cleanup pass; paths, transfer identity, format, and sizes are excluded |
| `crates/jackin-capsule/src/clipboard.rs:274` | `telemetry_info!` | COMPLETE — cleanup failures emit bodyless `io_error`, non-file recovery emits one governed warning, and stale transfers coalesce into one bodyless recovered-degradation warning per cleanup pass; paths, transfer identity, format, and sizes are excluded |
| `crates/jackin-capsule/src/container_context.rs:66` | `telemetry_info!` | COMPLETE — identity-source and expected-absence fallback narration deleted; container identity is prohibited telemetry data |
| `crates/jackin-capsule/src/container_context.rs:81` | `telemetry_info!` | COMPLETE — identity-source and expected-absence fallback narration deleted; container identity is prohibited telemetry data |
| `crates/jackin-capsule/src/container_context.rs:87` | `telemetry_info!` | COMPLETE — identity-source and expected-absence fallback narration deleted; container identity is prohibited telemetry data |
| `crates/jackin-capsule/src/daemon.rs:535` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:703` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:714` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:726` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:754` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:758` | `telemetry_debug!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:855` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:876` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:978` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1031` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1040` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1100` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1141` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1164` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1306` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1338` | `telemetry_debug!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1349` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1364` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1379` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1398` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1426` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1475` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1505` | `telemetry_debug!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon.rs:1596` | `telemetry_info!` | COMPLETE — registered agent-state signals own transitions; final daemon errors and handled degradations are typed; raw workdir/terminal/session/PTY/clipboard/error detail and duplicate lifecycle/debug narration are removed |
| `crates/jackin-capsule/src/daemon/compositor.rs:82` | `telemetry_info!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:90` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:103` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:179` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:189` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:391` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:417` | `telemetry_debug!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/compositor.rs:548` | `telemetry_info!` | COMPLETE — Ratatui draw failure emits one bodyless `io_error` at the rendering owner; duplicate failure narration and per-frame pane/session firehose are deleted; registered render metrics own duration and volume |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:75` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:87` | `telemetry_info!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:94` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:103` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:126` | `telemetry_info!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:160` | `telemetry_info!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:184` | `telemetry_info!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:199` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:264` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:308` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:338` | `telemetry_info!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:359` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/context_mgmt.rs:404` | `telemetry_debug!` | COMPLETE — branch and PR workers emit validated bounded `background.cycle` operations; delivered lookup failures and closed result channels classify independently and automatically emit typed errors; raw branch, HEAD, request, cache, and error narration is deleted |
| `crates/jackin-capsule/src/daemon/control.rs:97` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:145` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:151` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:187` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:227` | `telemetry_debug!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:236` | `telemetry_debug!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:292` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:296` | `telemetry_debug!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/control.rs:304` | `telemetry_info!` | COMPLETE — invalid correlation, repeated handshake, unknown control messages, absent capture sessions, capture I/O, and host clipboard failures emit bodyless typed errors; registered RPC operations own response outcomes; raw paths, session/request IDs, geometry, input sizes, and peer payloads are deleted |
| `crates/jackin-capsule/src/daemon/file_export.rs:44` | `telemetry_info!` | COMPLETE — export validation/open/read failures emit one bodyless `io_error` through `ResultTelemetryExt`; operator notices retain useful detail; path, basename, digest, transfer identity, size, and policy narration are deleted from telemetry |
| `crates/jackin-capsule/src/daemon/file_export.rs:92` | `telemetry_debug!` | COMPLETE — export validation/open/read failures emit one bodyless `io_error` through `ResultTelemetryExt`; operator notices retain useful detail; path, basename, digest, transfer identity, size, and policy narration are deleted from telemetry |
| `crates/jackin-capsule/src/daemon/file_export.rs:176` | `telemetry_debug!` | COMPLETE — export validation/open/read failures emit one bodyless `io_error` through `ResultTelemetryExt`; operator notices retain useful detail; path, basename, digest, transfer identity, size, and policy narration are deleted from telemetry |
| `crates/jackin-capsule/src/daemon/file_export.rs:187` | `telemetry_info!` | COMPLETE — export validation/open/read failures emit one bodyless `io_error` through `ResultTelemetryExt`; operator notices retain useful detail; path, basename, digest, transfer identity, size, and policy narration are deleted from telemetry |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:69` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:71` | `telemetry_info!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:180` | `telemetry_info!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:282` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:458` | `telemetry_info!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:557` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:592` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:608` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:623` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:635` | `telemetry_debug!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/input_dispatch.rs:937` | `telemetry_info!` | COMPLETE — unknown provider fallback emits one recovered-degradation warning and split failures use typed `launch_failed`; per-action, key, session, agent, mouse, scrollback, terminal-mode, and encoded-byte narration is deleted; PTY writer metrics own delivered bytes |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:239` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:340` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:385` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:430` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:522` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:566` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:578` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:590` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:608` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:630` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:637` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:644` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:651` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:666` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:699` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:708` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:751` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:762` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:772` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:780` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:793` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:802` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:813` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:821` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:829` | `telemetry_debug!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:851` | `telemetry_info!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/mouse_input.rs:861` | `telemetry_info!` | COMPLETE — the registered `ui.action` owns successful host-link dispatch; rejected links retain the operator notice; mouse routing, selection, URL/token, session, coordinate, geometry, and clipboard narration is deleted |
| `crates/jackin-capsule/src/daemon/multiplexer_utils.rs:16` | `telemetry_info!` | COMPLETE — env/provider/render narration and dynamic values are deleted; the usage-worker join owner emits one bodyless `panic` or `dependency_cancelled` error |
| `crates/jackin-capsule/src/daemon/multiplexer_utils.rs:137` | `telemetry_info!` | COMPLETE — env/provider/render narration and dynamic values are deleted; the usage-worker join owner emits one bodyless `panic` or `dependency_cancelled` error |
| `crates/jackin-capsule/src/daemon/multiplexer_utils.rs:212` | `telemetry_debug!` | COMPLETE — env/provider/render narration and dynamic values are deleted; the usage-worker join owner emits one bodyless `panic` or `dependency_cancelled` error |
| `crates/jackin-capsule/src/daemon/multiplexer_utils.rs:327` | `telemetry_info!` | COMPLETE — env/provider/render narration and dynamic values are deleted; the usage-worker join owner emits one bodyless `panic` or `dependency_cancelled` error |
| `crates/jackin-capsule/src/daemon/pane_layout.rs:94` | `telemetry_info!` | COMPLETE — reaped split races emit one recovered-degradation warning; registered PTY/session/UI signals own successful split, close, focus, and resize behavior; pane/session/agent/provider labels and geometry are deleted |
| `crates/jackin-capsule/src/daemon/pane_layout.rs:103` | `telemetry_info!` | COMPLETE — reaped split races emit one recovered-degradation warning; registered PTY/session/UI signals own successful split, close, focus, and resize behavior; pane/session/agent/provider labels and geometry are deleted |
| `crates/jackin-capsule/src/daemon/pane_layout.rs:162` | `telemetry_info!` | COMPLETE — reaped split races emit one recovered-degradation warning; registered PTY/session/UI signals own successful split, close, focus, and resize behavior; pane/session/agent/provider labels and geometry are deleted |
| `crates/jackin-capsule/src/daemon/pane_layout.rs:213` | `telemetry_debug!` | COMPLETE — reaped split races emit one recovered-degradation warning; registered PTY/session/UI signals own successful split, close, focus, and resize behavior; pane/session/agent/provider labels and geometry are deleted |
| `crates/jackin-capsule/src/daemon/ports.rs:100` | `telemetry_debug!` | COMPLETE — stale reporter events retain the explicit always-ACK contract and remain silent; registered agent-state signals own applied transitions, and raw session identity is deleted |
| `crates/jackin-capsule/src/daemon/resource_metrics.rs:85` | `telemetry_debug!` | migrated to standard governed process metrics |
| `crates/jackin-capsule/src/daemon/resource_metrics.rs:98` | `telemetry_debug!` | migrated to standard governed process metrics |
| `crates/jackin-capsule/src/daemon/resource_metrics.rs:108` | `telemetry_debug!` | migrated to standard governed process metrics |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:95` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:121` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:155` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:265` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:364` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:389` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:464` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/daemon/session_lifecycle.rs:516` | `telemetry_info!` | COMPLETE — registered PTY/session/UI signals own successful lifecycle; labels, identifiers, counts and provider values are deleted; the two mutually exclusive spawn-dialog owners emit one bodyless `launch_failed` error |
| `crates/jackin-capsule/src/debug_panic.rs:19` | `telemetry_info!` | COMPLETE — the shared panic hook owns one registered `app.crash`; pre-panic narration is deleted |
| `crates/jackin-capsule/src/firewall.rs:116` | `telemetry_info!` | COMPLETE — selected policy is owned by the isolation event; invalid/unresolvable allowlist members emit typed recovered-degradation WARN; raw hosts and counts are omitted |
| `crates/jackin-capsule/src/firewall.rs:139` | `telemetry_info!` | COMPLETE — selected policy is owned by the isolation event; invalid/unresolvable allowlist members emit typed recovered-degradation WARN; raw hosts and counts are omitted |
| `crates/jackin-capsule/src/firewall.rs:153` | `telemetry_info!` | COMPLETE — selected policy is owned by the isolation event; invalid/unresolvable allowlist members emit typed recovered-degradation WARN; raw hosts and counts are omitted |
| `crates/jackin-capsule/src/firewall.rs:178` | `telemetry_info!` | COMPLETE — selected policy is owned by the isolation event; invalid/unresolvable allowlist members emit typed recovered-degradation WARN; raw hosts and counts are omitted |
| `crates/jackin-capsule/src/git_context.rs:61` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:115` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:126` | `telemetry_debug!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:134` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:141` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:195` | `telemetry_debug!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:207` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:241` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:250` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:257` | `telemetry_debug!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:266` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:463` | `telemetry_debug!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:498` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:559` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:575` | `telemetry_info!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/git_context.rs:653` | `telemetry_debug!` | COMPLETE — typed return paths own expected absence/malformed metadata; consumed filesystem/thread/cache failures emit bodyless recoveries; the inotify watcher owns bounded open/close operations without retaining a lifetime span; paths, Git content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/pid1.rs:47` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:64` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:93` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:113` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:127` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:176` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:189` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pid1.rs:208` | `telemetry_info!` | COMPLETE — mutex poisoning emits `panic`; PID conversion and OS signal/wait/thread failures emit bodyless `io_error`; continued `sigwait` attempts emit `retry.scheduled`; the reaper owns one bounded open phase and no lifetime span; PID and errno values are omitted |
| `crates/jackin-capsule/src/pr_context.rs:146` | `telemetry_info!` | COMPLETE — process/background owners retain terminal failures; best-effort checks, unknown buckets, and rollup fallback emit bodyless recovered-degradation WARNs without URLs, buckets, command material, or raw errors |
| `crates/jackin-capsule/src/pr_context.rs:187` | `telemetry_debug!` | COMPLETE — process/background owners retain terminal failures; best-effort checks, unknown buckets, and rollup fallback emit bodyless recovered-degradation WARNs without URLs, buckets, command material, or raw errors |
| `crates/jackin-capsule/src/pr_context.rs:198` | `telemetry_debug!` | COMPLETE — process/background owners retain terminal failures; best-effort checks, unknown buckets, and rollup fallback emit bodyless recovered-degradation WARNs without URLs, buckets, command material, or raw errors |
| `crates/jackin-capsule/src/pr_context.rs:404` | `telemetry_debug!` | COMPLETE — process/background owners retain terminal failures; best-effort checks, unknown buckets, and rollup fallback emit bodyless recovered-degradation WARNs without URLs, buckets, command material, or raw errors |
| `crates/jackin-capsule/src/runtime_setup.rs:305` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:666` | `telemetry_debug!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:678` | `telemetry_debug!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:771` | `telemetry_debug!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:779` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:1283` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:1293` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:1455` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/runtime_setup.rs:1464` | `telemetry_info!` | COMPLETE — bounded process/auth owners retain terminal outcomes; expected existing config and optional catalog fallback are silent; reporter and DCO cache read/write fallbacks emit bodyless recovered degradation; paths, identity, provider content, errno, and raw errors are omitted |
| `crates/jackin-capsule/src/services/input_bindings.rs:33` | `telemetry_info!` | COMPLETE — parser return values own invalid-input fallback; raw environment values and key material are prohibited telemetry data, so generic narration is deleted |
| `crates/jackin-capsule/src/services/input_bindings.rs:51` | `telemetry_info!` | COMPLETE — parser return values own invalid-input fallback; raw environment values and key material are prohibited telemetry data, so generic narration is deleted |
| `crates/jackin-capsule/src/session.rs:471` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:480` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:496` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:505` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:517` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:535` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:544` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:560` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:571` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:578` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:599` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:643` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:654` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:815` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:829` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:919` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1163` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1217` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1353` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1366` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1373` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1412` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1448` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1454` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1480` | `telemetry_debug!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1504` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/session.rs:1510` | `telemetry_info!` | COMPLETE — PTY/lock/resize failures use bounded `ResultTelemetryExt`; PTY exit owns wait classification; expected closure/EOF and raw byte/identity/detail chatter deleted |
| `crates/jackin-capsule/src/socket.rs:149` | `telemetry_info!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:158` | `telemetry_debug!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:163` | `telemetry_info!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:177` | `telemetry_info!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:186` | `telemetry_info!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:191` | `telemetry_info!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/socket.rs:209` | `telemetry_debug!` | COMPLETE — bounded autonomous open/close operations replace listener narration; every setup/accept failure emits bodyless `io_error`, every continued accept attempt emits `retry.scheduled`, and no span covers listener idle time |
| `crates/jackin-capsule/src/tui/run.rs:141` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/tui/run.rs:146` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/tui/run.rs:151` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/tui/run.rs:156` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/tui/run.rs:161` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/tui/run.rs:168` | `telemetry_debug!` | COMPLETE — host-only frames are intentionally ignored by the in-Capsule client; generic narration is duplicate behavior and URL/path/export context is prohibited |
| `crates/jackin-capsule/src/util.rs:26` | `telemetry_debug!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:38` | `telemetry_debug!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:47` | `telemetry_debug!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:96` | `telemetry_info!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:120` | `telemetry_info!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:153` | `telemetry_info!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:164` | `telemetry_debug!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:174` | `telemetry_info!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-capsule/src/util.rs:182` | `telemetry_info!` | COMPLETE — bounded metadata open/read failures and truncation emit bodyless recovered-degradation warnings; file paths, metadata labels, limits, errno, and raw errors are excluded |
| `crates/jackin-console-oppicker/src/state.rs:222` | `telemetry_debug!` | DELETE — returned reference owns fallback behavior; vault/item names and field identifiers prohibited |
| `crates/jackin-console/src/services/role_source.rs:55` | `telemetry_debug!` | COMPLETE — typed return values own parse/resolve outcomes; raw selectors, role keys, repository URLs, trust state, and raw errors are prohibited telemetry data, so all five generic narrations are deleted |
| `crates/jackin-console/src/services/role_source.rs:57` | `telemetry_debug!` | COMPLETE — typed return values own parse/resolve outcomes; raw selectors, role keys, repository URLs, trust state, and raw errors are prohibited telemetry data, so all five generic narrations are deleted |
| `crates/jackin-console/src/services/role_source.rs:64` | `telemetry_debug!` | COMPLETE — typed return values own parse/resolve outcomes; raw selectors, role keys, repository URLs, trust state, and raw errors are prohibited telemetry data, so all five generic narrations are deleted |
| `crates/jackin-console/src/services/role_source.rs:68` | `telemetry_debug!` | COMPLETE — typed return values own parse/resolve outcomes; raw selectors, role keys, repository URLs, trust state, and raw errors are prohibited telemetry data, so all five generic narrations are deleted |
| `crates/jackin-console/src/services/role_source.rs:81` | `telemetry_debug!` | COMPLETE — typed return values own parse/resolve outcomes; raw selectors, role keys, repository URLs, trust state, and raw errors are prohibited telemetry data, so all five generic narrations are deleted |
| `crates/jackin-console/src/tui/input/auth.rs:53` | `telemetry_debug!` | COMPLETE — expected unavailable rows/pickers are silent; missing modal return paths emit one bodyless typed instrumentation fault without credential, path, function, or UI-state detail |
| `crates/jackin-console/src/tui/input/auth.rs:76` | `telemetry_debug!` | COMPLETE — expected unavailable rows/pickers are silent; missing modal return paths emit one bodyless typed instrumentation fault without credential, path, function, or UI-state detail |
| `crates/jackin-console/src/tui/input/auth.rs:263` | `telemetry_debug!` | COMPLETE — expected unavailable rows/pickers are silent; missing modal return paths emit one bodyless typed instrumentation fault without credential, path, function, or UI-state detail |
| `crates/jackin-console/src/tui/input/editor.rs:1118` | `telemetry_debug!` | COMPLETE — the operator popup owns display and one bodyless `config_error` owns telemetry; raw input, repository URL, and raw error are omitted |
| `crates/jackin-console/src/tui/input/editor/agents.rs:32` | `telemetry_debug!` | COMPLETE — modal state owns the action; trusted role count and generic narration are deleted |
| `crates/jackin-console/src/tui/input/editor/modal.rs:271` | `telemetry_debug!` | COMPLETE — the shared role-input error popup emits one bodyless `config_error`; raw input narration is deleted |
| `crates/jackin-console/src/tui/input/global_mounts/auth.rs:441` | `telemetry_debug!` | COMPLETE — missing global-settings auth return paths emit one bodyless typed instrumentation fault without token, folder, 1Password reference, function, path, or modal detail |
| `crates/jackin-console/src/tui/input/global_mounts/auth.rs:468` | `telemetry_debug!` | COMPLETE — missing global-settings auth return paths emit one bodyless typed instrumentation fault without token, folder, 1Password reference, function, path, or modal detail |
| `crates/jackin-console/src/tui/input/global_mounts/auth.rs:522` | `telemetry_debug!` | COMPLETE — missing global-settings auth return paths emit one bodyless typed instrumentation fault without token, folder, 1Password reference, function, path, or modal detail |
| `crates/jackin-console/src/tui/input/global_mounts/auth.rs:573` | `telemetry_debug!` | COMPLETE — missing global-settings auth return paths emit one bodyless typed instrumentation fault without token, folder, 1Password reference, function, path, or modal detail |
| `crates/jackin-console/src/tui/state.rs:417` | `telemetry_debug!` | COMPLETE — editor/role popups remain operator output while telemetry emits one bodyless `config_error`; displayed messages and raw errors are omitted |
| `crates/jackin-console/src/tui/state.rs:428` | `telemetry_debug!` | COMPLETE — editor/role popups remain operator output while telemetry emits one bodyless `config_error`; displayed messages and raw errors are omitted |
| `crates/jackin-console/src/tui/state/manager.rs:421` | `telemetry_debug!` | COMPLETE — selection clamp fallback emits a bodyless recovered-degradation WARN; missing async auth parents emit bodyless instrumentation faults; indices, workspace/modal state, and credential references are omitted |
| `crates/jackin-console/src/tui/state/manager.rs:571` | `telemetry_debug!` | COMPLETE — selection clamp fallback emits a bodyless recovered-degradation WARN; missing async auth parents emit bodyless instrumentation faults; indices, workspace/modal state, and credential references are omitted |
| `crates/jackin-console/src/tui/state/manager.rs:1169` | `telemetry_debug!` | COMPLETE — selection clamp fallback emits a bodyless recovered-degradation WARN; missing async auth parents emit bodyless instrumentation faults; indices, workspace/modal state, and credential references are omitted |
| `crates/jackin-console/src/tui/state/manager.rs:1197` | `telemetry_debug!` | COMPLETE — selection clamp fallback emits a bodyless recovered-degradation WARN; missing async auth parents emit bodyless instrumentation faults; indices, workspace/modal state, and credential references are omitted |
| `crates/jackin-core/src/worktree_dirty.rs:118` | `telemetry_debug!` | COMPLETE — no live invocation remains; contributor guidance now delegates diagnostic lines to operator output or registered privacy-safe signals rather than prescribing the generic macro |
| `crates/jackin-diagnostics/src/lib.rs:4` | `telemetry_debug!` | COMPLETE — legacy debug-log re-exports are gone and crate-level guidance names registered facades rather than the transitional generic macro |
| `crates/jackin-host/src/caffeinate.rs:408` | `telemetry_debug!` | DELETE — command result and typed error own teardown semantics; process identifiers prohibited |
| `crates/jackin-host/src/host_clipboard.rs:185` | `telemetry_debug!` | DELETE — return value owns implicit-paste resolution; host paths prohibited |
| `crates/jackin-host/src/host_clipboard.rs:211` | `telemetry_debug!` | DELETE — return value owns implicit-paste resolution; host paths prohibited |
| `crates/jackin-image/src/agent_binary.rs:874` | `telemetry_debug!` | DELETE generic fallback — registered cache/download boundaries and active-run compact records own semantics; agent/version/URL/path/error detail prohibited |
| `crates/jackin-image/src/capsule_binary.rs:179` | `telemetry_debug!` | DELETE generic fallback — registered cache boundary and active-run compact records own semantics; version/architecture/path detail prohibited |
| `crates/jackin-instance/src/auth.rs:353` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:360` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:376` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:379` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:402` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:443` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/auth.rs:1093` | `telemetry_debug!` | DELETE — typed auth outcome owns semantics; credential paths, CLI stderr, and credential-file parse details prohibited |
| `crates/jackin-instance/src/manifest.rs:322` | `telemetry_debug!` | DELETE — typed persistence result owns semantics; state paths and container identifiers prohibited |
| `crates/jackin-instance/src/manifest.rs:331` | `telemetry_debug!` | DELETE — typed persistence result owns semantics; state paths and container identifiers prohibited |
| `crates/jackin-instance/src/manifest.rs:352` | `telemetry_debug!` | DELETE — typed persistence result owns semantics; state paths and container identifiers prohibited |
| `crates/jackin-instance/src/manifest.rs:360` | `telemetry_debug!` | DELETE — typed persistence result owns semantics; state paths and container identifiers prohibited |
| `crates/jackin-instance/src/manifest.rs:529` | `telemetry_debug!` | DELETE — typed persistence result owns semantics; state paths and container identifiers prohibited |
| `crates/jackin-isolation/src/cleanup.rs:45` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:56` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:81` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:87` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:111` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:134` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:154` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:184` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/cleanup.rs:241` | `telemetry_debug!` | DELETE — cleanup result and typed errors own semantics; paths, mount/container names, branches, and command material prohibited |
| `crates/jackin-isolation/src/finalize.rs:275` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/finalize.rs:291` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/finalize.rs:307` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/finalize.rs:381` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/finalize.rs:551` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/finalize.rs:563` | `telemetry_debug!` | DELETE — finalizer decision owns semantics; container/mount names and shared diagnostic prose prohibited |
| `crates/jackin-isolation/src/materialize.rs:311` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:333` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:353` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:395` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:401` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:414` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:426` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:604` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:624` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:692` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:703` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:732` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:742` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:756` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:764` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:779` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:797` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:818` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:834` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:867` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:913` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:945` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:976` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:989` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:1058` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:1080` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/materialize.rs:1103` | `telemetry_debug!` | DELETE — incidental materialization chatter; launch/isolation boundary owns semantics; paths, workspace/container names, branches, commits, and URLs prohibited |
| `crates/jackin-isolation/src/state.rs:156` | `telemetry_debug!` | DELETE — persistence return value owns semantics; state paths and mount names prohibited |
| `crates/jackin-isolation/src/state.rs:187` | `telemetry_debug!` | DELETE — persistence return value owns semantics; state paths and mount names prohibited |
| `crates/jackin-isolation/src/state.rs:202` | `telemetry_debug!` | DELETE — persistence return value owns semantics; state paths and mount names prohibited |
| `crates/jackin-isolation/src/state.rs:209` | `telemetry_debug!` | DELETE — persistence return value owns semantics; state paths and mount names prohibited |
| `crates/jackin-launch-tui/src/progress.rs:413` | `telemetry_debug!` | DELETE — recovered lock poison has no operator action; returned acknowledgement state owns behavior |
| `crates/jackin-runtime/src/apple_container_client.rs:114` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:123` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:130` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:163` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:176` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:181` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/apple_container_client.rs:223` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:71` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:126` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:137` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:141` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:156` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:216` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:238` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/exec_host.rs:304` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/host_daemon.rs:220` | `telemetry_debug!` | COMPLETE — registered agent state plus dispatcher result own behavior; attention container/session/label/mute narration is deleted |
| `crates/jackin-runtime/src/reactive_daemon.rs:89` | `telemetry_debug!` | COMPLETE — diagnostic notifier is intentionally silent because registered agent state owns transitions; container/session/label narration is deleted |
| `crates/jackin-runtime/src/runtime/apple_container.rs:104` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:113` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:165` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:195` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:251` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:258` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:293` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:323` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:358` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:374` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:410` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:428` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:442` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/apple_container.rs:529` | `telemetry_debug!` | governed DEBUG detail |
| `crates/jackin-runtime/src/runtime/attach.rs:89` | `telemetry_warn!` | COMPLETE — manifest-title fallback and handled attach EOF/failure each emit a bodyless recovered-degradation warning at the decision boundary; container identity, paths, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/attach.rs:315` | `telemetry_debug!` | COMPLETE — manifest-title fallback and handled attach EOF/failure each emit a bodyless recovered-degradation warning at the decision boundary; container identity, paths, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/attach.rs:899` | `telemetry_debug!` | COMPLETE — manifest-title fallback and handled attach EOF/failure each emit a bodyless recovered-degradation warning at the decision boundary; container identity, paths, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/cleanup.rs:221` | `telemetry_warn!` | COMPLETE — corrupt-manifest and orphan-lock removal failures use bodyless recovered-degradation WARNs; successful cleanup narration and the raw path/error `eprintln!` are deleted |
| `crates/jackin-runtime/src/runtime/cleanup.rs:849` | `telemetry_debug!` | COMPLETE — corrupt-manifest and orphan-lock removal failures use bodyless recovered-degradation WARNs; successful cleanup narration and the raw path/error `eprintln!` are deleted |
| `crates/jackin-runtime/src/runtime/docker_profile.rs:636` | `telemetry_debug!` | COMPLETE — the session contract is explicit operator `--debug` output, not exported telemetry; the stale generic-macro documentation reference is removed |
| `crates/jackin-runtime/src/runtime/host_attach.rs:112` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:127` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:342` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:350` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:359` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:392` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:400` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:411` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:419` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:430` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:438` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:526` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:539` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:554` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:565` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:592` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:626` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:716` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:817` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:831` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:859` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:1132` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:1138` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:1261` | `telemetry_debug!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:1331` | `telemetry_warn!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/host_attach.rs:1339` | `telemetry_warn!` | COMPLETE — host notice, file-export, desktop-action, and terminal failures emit bodyless `io_error`; clipboard probe fallback emits recovered degradation; attach-control write failures remain owned exactly once by RPC spans; success/abort narration containing transfer IDs, paths, filenames, hashes, sizes, URLs, image metadata, and raw errors is deleted |
| `crates/jackin-runtime/src/runtime/image.rs:161` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:165` | `telemetry_warn!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:195` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:248` | `telemetry_warn!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:263` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:280` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:286` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:986` | `telemetry_warn!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:998` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:1015` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:1091` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:1128` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:1185` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image.rs:1315` | `telemetry_debug!` | COMPLETE — registered image reuse/invalidation and prewarm-plan owners replace duplicate narration; lookup/inspect/version and binary-download fallback emit bodyless recovered degradation; operator fallback warning remains local; image references, agent identity, paths, URLs, install commands, and raw errors are omitted from export |
| `crates/jackin-runtime/src/runtime/image/build.rs:128` | `telemetry_debug!` | COMPLETE — governed cache/image decisions own reuse; local base-image identity narration is deleted |
| `crates/jackin-runtime/src/runtime/image/version.rs:158` | `telemetry_debug!` | COMPLETE — handled GitHub-token probe failure emits one bodyless recovered-degradation WARN without token, command, stderr, path, or raw error |
| `crates/jackin-runtime/src/runtime/launch/exit_diagnosis.rs:169` | `telemetry_debug!` | COMPLETE — conservative dead/unavailable-container preservation emits a bodyless recovered-degradation warning; container identity and Docker detail are excluded |
| `crates/jackin-runtime/src/runtime/launch/exit_diagnosis.rs:180` | `telemetry_debug!` | COMPLETE — conservative dead/unavailable-container preservation emits a bodyless recovered-degradation warning; container identity and Docker detail are excluded |
| `crates/jackin-runtime/src/runtime/launch/git_pull.rs:55` | `telemetry_debug!` | COMPLETE — explicit compact operator output retains pull starts; governed process ownership covers pull outcomes; repository source/path and duplicate debug narration are excluded from telemetry |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:376` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:385` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:399` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:557` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:590` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:632` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:643` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_dind.rs:652` | `telemetry_debug!` | COMPLETE — every consumed prewarm state, network, readiness, cleanup, directory, file, and lock failure emits one bodyless recovered-degradation warning; registered adoption outcomes retain the bounded decision; paths, resource names, state payloads, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:331` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:338` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:432` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:730` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:746` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:782` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:802` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:976` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:986` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:1059` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline.rs:1515` | `telemetry_debug!` | COMPLETE — cache decisions and launch/restore stages replace duplicate narration; cached-manifest parse and best-effort purge failures emit bodyless recovered degradation; selector, container identity, paths, and raw errors are omitted |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline/launch_core/orchestrate.rs:216` | `telemetry_debug!` | COMPLETE — registered image and launch-stage owners cover build/materialization decisions; expected preserved-container absence is silent; unavailable session inspection emits bodyless recovered degradation without container identity or raw reason |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline/launch_core/orchestrate.rs:286` | `telemetry_debug!` | COMPLETE — registered image and launch-stage owners cover build/materialization decisions; expected preserved-container absence is silent; unavailable session inspection emits bodyless recovered degradation without container identity or raw reason |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline/launch_core/orchestrate.rs:838` | `telemetry_debug!` | COMPLETE — registered image and launch-stage owners cover build/materialization decisions; expected preserved-container absence is silent; unavailable session inspection emits bodyless recovered degradation without container identity or raw reason |
| `crates/jackin-runtime/src/runtime/launch/launch_pipeline/launch_core/orchestrate.rs:1429` | `telemetry_debug!` | COMPLETE — registered image and launch-stage owners cover build/materialization decisions; expected preserved-container absence is silent; unavailable session inspection emits bodyless recovered degradation without container identity or raw reason |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:323` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:439` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:443` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:453` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:458` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:459` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:464` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:474` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:481` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:487` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:494` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:495` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:726` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:954` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:1024` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_runtime.rs:1160` | `telemetry_debug!` | COMPLETE — security/session-contract, post-run, socket-setup, and clean-shutdown detail is explicit local operator `--debug` output, not OTLP; AppArmor probe fallback additionally emits bodyless recovered degradation; registered launch/process/session owners retain semantic outcomes |
| `crates/jackin-runtime/src/runtime/launch/launch_slot.rs:80` | `telemetry_debug!` | COMPLETE — lock contention followed by another bounded attempt emits the registered bodyless `retry.scheduled` WARN; container names, attempt counts, lock paths, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/launch/launch_slot.rs:170` | `telemetry_debug!` | COMPLETE — lock contention followed by another bounded attempt emits the registered bodyless `retry.scheduled` WARN; container names, attempt counts, lock paths, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/launch/trust.rs:97` | `telemetry_debug!` | COMPLETE — governed launch trust owns the decision; workspace count and Codex config path narration are deleted |
| `crates/jackin-runtime/src/runtime/repo_cache.rs:364` | `telemetry_debug!` | COMPLETE — registered cache decisions own stale and fresh reuse; a failed fast-forward followed by reset emits one bodyless recovered-degradation warning, while process owners retain command failure; repository URLs, paths, selectors, branches, age, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/repo_cache.rs:508` | `telemetry_debug!` | COMPLETE — registered cache decisions own stale and fresh reuse; a failed fast-forward followed by reset emits one bodyless recovered-degradation warning, while process owners retain command failure; repository URLs, paths, selectors, branches, age, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/repo_cache.rs:558` | `telemetry_debug!` | COMPLETE — registered cache decisions own stale and fresh reuse; a failed fast-forward followed by reset emits one bodyless recovered-degradation warning, while process owners retain command failure; repository URLs, paths, selectors, branches, age, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/repo_cache.rs:608` | `telemetry_debug!` | COMPLETE — registered cache decisions own stale and fresh reuse; a failed fast-forward followed by reset emits one bodyless recovered-degradation warning, while process owners retain command failure; repository URLs, paths, selectors, branches, age, and raw errors are excluded |
| `crates/jackin-runtime/src/runtime/universe.rs:219` | `telemetry_debug!` | COMPLETE — unexpected exit-claim rename failure emits one bodyless recovered-degradation WARN; marker path and raw OS error are omitted |
| `crates/jackin-usage/src/telemetry.rs:62` | `telemetry_info!` | REPLACE remaining init failure — typed health plus best-effort `config_error`; DELETE duplicate active lifecycle containing session identity |
| `crates/jackin-usage/src/telemetry.rs:69` | `telemetry_info!` | REPLACE remaining init failure — typed health plus best-effort `config_error`; DELETE duplicate active lifecycle containing session identity |
| `crates/jackin-usage/src/token_monitor.rs:253` | `telemetry_debug!` | REPLACE — `ResultTelemetryExt` emits typed `io_error`; collector absence owns behavior; provider labels, host paths, and raw errors prohibited |
| `crates/jackin-usage/src/token_monitor/opencode.rs:16` | `telemetry_debug!` | REPLACE — `ResultTelemetryExt` emits typed `db_error`; collector false outcome owns behavior; database path and raw errors prohibited |
| `crates/jackin-usage/src/token_monitor/opencode.rs:28` | `telemetry_debug!` | REPLACE — `ResultTelemetryExt` emits typed `db_error`; collector false outcome owns behavior; database path and raw errors prohibited |
| `crates/jackin-usage/src/token_monitor/opencode.rs:41` | `telemetry_debug!` | REPLACE — `ResultTelemetryExt` emits typed `db_error`; collector false outcome owns behavior; database path and raw errors prohibited |
| `crates/jackin-usage/src/usage.rs:1004` | `telemetry_info!` | REPLACE remaining credential failures — typed `io_error`/`config_error`; expected absence stays silent; credential paths and raw errors prohibited |
| `crates/jackin-usage/src/usage.rs:1016` | `telemetry_info!` | REPLACE remaining credential failures — typed `io_error`/`config_error`; expected absence stays silent; credential paths and raw errors prohibited |
| `crates/jackin-usage/src/usage/codex.rs:131` | `telemetry_debug!` | REPLACE remaining provider failures — typed `http_error`/`rpc_error`/`io_error`; config paths and raw dependency errors prohibited |
| `crates/jackin-usage/src/usage/codex.rs:792` | `telemetry_debug!` | REPLACE remaining provider failures — typed `http_error`/`rpc_error`/`io_error`; config paths and raw dependency errors prohibited |
| `crates/jackin-usage/src/usage/codex.rs:1049` | `telemetry_info!` | REPLACE remaining provider failures — typed `http_error`/`rpc_error`/`io_error`; config paths and raw dependency errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:88` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:141` | `telemetry_error!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:174` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:180` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:185` | `telemetry_debug!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:304` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:312` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:378` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage/refresh.rs:390` | `telemetry_info!` | REPLACE remaining refresh/persistence failures — owning spans plus typed `timeout`/`panic`/provider/`io_error`/`config_error` events and one governed WARN on recovery; cache keys, paths, reasons, and raw errors prohibited |
| `crates/jackin-usage/src/usage_snapshot_store.rs:379` | `telemetry_debug!` | REPLACE — `ResultTelemetryExt` emits typed `db_error`; original upsert error remains the returned owner; raw rollback error prohibited |
| `crates/jackin/src/console/effects.rs:248` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:288` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:294` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:300` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:312` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:356` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:370` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:377` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:384` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:394` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:426` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:439` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:527` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/effects.rs:921` | `telemetry_debug!` | COMPLETE — role resolution and registration control-flow narration is deleted; the registration boundary owns ordinary failures as bodyless `io_error` and caught panics as bodyless `panic`, while operator popups retain useful detail and telemetry excludes selectors, role keys, repository URLs, paths, and raw errors |
| `crates/jackin/src/console/services.rs:382` | `telemetry_debug!` | COMPLETE — one bodyless recovered-degradation warning owns a refresh with any handled manifest or snapshot failure; container identity and raw errors are excluded, and repeated failures are coalesced per refresh |
| `crates/jackin/src/console/services.rs:409` | `telemetry_debug!` | COMPLETE — one bodyless recovered-degradation warning owns a refresh with any handled manifest or snapshot failure; container identity and raw errors are excluded, and repeated failures are coalesced per refresh |
| `crates/jackin/src/console/tui/run.rs:684` | `telemetry_debug!` | COMPLETE — registered `ui.action` and render operations own keyboard/mouse semantics; raw key, pointer, coordinate, and dynamic screen-location narration is deleted |
| `crates/jackin/src/console/tui/run.rs:789` | `telemetry_debug!` | COMPLETE — registered `ui.action` and render operations own keyboard/mouse semantics; raw key, pointer, coordinate, and dynamic screen-location narration is deleted |

## Scoped diagnostic standard-error sites (25)

| Site | Decision and semantic owner |
|---|---|
| `crates/jackin-instance/src/auth.rs:597` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:747` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:965` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:986` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:1140` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:1361` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:1371` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-instance/src/auth.rs:1379` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:152` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:264` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:322` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:341` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:371` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:379` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:399` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:426` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:446` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:476` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:491` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:729` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:735` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:780` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:805` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:807` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |
| `crates/jackin-runtime/src/runtime/cleanup.rs:856` | Operator-visible detail stays on the typed operator-output port when actionable; otherwise the migrated semantic owner emits one bodyless bounded error or recovered-degradation WARN without paths, identity, credentials, stderr, or raw error material. |

## Final raw-tracing implementation sites

| Site | Decision and enforcement |
|---|---|
| `crates/jackin-telemetry/src/event.rs:233` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:234` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:235` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:236` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:237` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:241` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:242` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:243` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:244` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/event.rs:245` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:383` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:386` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:389` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:392` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:395` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:398` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:401` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:404` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:407` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:410` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:419` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:422` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:425` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:428` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:431` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:434` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:437` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:440` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:443` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:452` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:455` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:458` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:461` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:464` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:467` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:470` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:473` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:476` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:479` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:488` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:491` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:494` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:497` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:500` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:503` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:506` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:509` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-telemetry/src/operation.rs:512` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |
| `crates/jackin-diagnostics/src/run.rs:559` | Exact governed-facade or composition-root implementation construct; syntax-aware `cargo xtask telemetry-registry` policy rejects every unrecognized raw-tracing call and alias form. |

## Reconciliation

- Generic macro snapshot: 501 sites = 330 `telemetry_debug!` + 157 `telemetry_info!` + 10 `telemetry_warn!` + 4 `telemetry_error!`.
- Scoped diagnostic standard error: 25 sites in the two plan-owned files.
- Final production invocation census: zero invocations of all ten prohibited legacy/generic macros.
- The generated current-tree ownership census (`011-error-ownership-census.md`) separately pins every propagated carrier, immediate consumed-error recorder, and typed configuration owner by exact line.
