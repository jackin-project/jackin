# Unified OpenTelemetry observability reopened gap audit

Audited independently by three read-only agents on 2026-07-16 and re-audited after the `ad00e3cd6` main-branch convergence merge at `2aa779f6e`. Every row is binding until implementation and requirement-matched verification prove it complete.

| Plan | Reopened residuals |
|---:|---|
| 001 | **Implementation residuals closed.** Offline Weaver generation owns exact schema metadata and the runtime event/span/metric descriptor inventories; `events.yaml` owns canonical severity through Weaver-preserved structured notes. Runtime facades include only generated constants and `ALL` tables, including explicit generated API aliases. Exact bidirectional and single-byte mutation tests cover checked-in drift, missing/invalid severity, attributes, enums, values, names, types, units, descriptions, boundaries, requirements and facade inventories. Final workspace/strict-lint gates remain shared acceptance evidence. |
| 002 | Focused implementation is closed; integrated workspace/feature-power-set/lint gates remain. Host and Capsule initialize traces, logs, and metrics atomically; validated headers, per-signal timeouts, TLS, and standard environment configuration feed all three providers; health reports the active signals and owned flush observations; validation and shutdown share coordinated five-second budgets. |
| 003 | **Implementation residuals closed.** `conformance_single_delivery_preserves_native_shape` proves native EventName, canonical severity, body, typed fields, immutable Resource, matching TraceId/SpanId and zero duplicate span events; the exact ERROR/WARN/INFO/DEBUG/TRACE matrix proves gating and no automatic status/exception inference; registry-wide generated definitions drive canonical-severity evidence; the compatibility path preserves single delivery. Final workspace/lint gates remain shared acceptance evidence. |
| 004 | Redact-before-UTF-8-truncate is closed on the production body, `exception.message`, and `exception.stacktrace` path with dynamic exporter proof. Observable callback enforcement is closed: source policy requires inline closures, rejects every non-snapshot function/method call, indirect callee and macro expansion with bypass fixtures, while the exact production registration executes through an in-memory SDK reader with a real Tokio handle, emits all five process/runtime instruments, and bounds every collection. Generated event enforcement is closed registry-wide for required/allowed keys, exact value types and enum domains; live reattach export proves `session.previous_id` is retained. Cumulative span mutation reserves outcome/error slots, rejects the first attribute overflow, caps links at eight, and moves trace/size-limit health exactly once for each overflow. Real processor bypass tests prove unknown names/attributes, forged severity, invalid types, private values, and oversized logs/spans are dropped into their exact signal/reason cells; array and link limit validators have direct negatives. A generic production exporter wrapper now proves raw SDK Meter batches reject unknown names/attributes, wrong instrument shapes, invalid array types, private/oversized dimensions, and 257-point cardinality before the inner exporter, with exact metric health cells; rejected batches also move attempt/failure health once without a success. Abandoned guards export `telemetry_instrumentation_fault`; recovered degradation exports one bodyless governed WARN. Syntax-aware lint fixtures cover raw Meter construction and generic/raw signal APIs. Disabled event, completed operation, and counter paths have a zero-allocation test and an executing Criterion benchmark. Expected versus deadline/dependency cancellation status is closed at `351a95bfb`. Remaining shared evidence is the integrated workspace/strict-lint gate. |
| 005 | Remove every enter/context guard held across `.await`; enforce tracing-enter invalid types in Clippy/source policy; cover joined/detached/cycle/stream, JoinSet, local/blocking/thread/subscription/timer helpers and their parent/link/disabled semantics; catch all raw spawn forms with synthetic tests. The testbed raw-spawn exemption and last exact-census hit are closed by governed stream spawning plus a rejection fixture. A production Git-pull exporter test found and closed a deeper gap: joined named/unnamed OS threads now install the captured dispatcher as well as entering the captured span, so operations created inside the worker export with correct context. |
| 006 | Focused implementation is closed; integrated workspace/protocol/conformance gates remain. The envelope is on daemon, control, attach, jobs, and `jackin-exec`; CLIENT/SERVER ownership and bounded RPC metrics exist. Exec framing/auth/correlation/resolution/write failures terminate once without raw payload/error formatting. Attach handshake, detach, focus, and single/chunked clipboard controls are contextual bounded methods; response guards complete after actual writes, enqueue failures cannot strand them, invalid correlation creates one typed local failure after extraction, and legacy controls are rejected. Exporter matrices prove propagation/sampling/rejection, framing, bad-ID, peer-close, invalid handshake/control, no-side-effect, delivery-failure, and payload privacy behavior. |
| 007 | Focused implementation is closed; integrated workspace/strict-lint and one-shot smoke gates remain. Host and `jackin-role` parse/help/version outcomes share a pre-dispatch boundary that initializes export, completes one governed `help` root with exit/outcome/stable error and CLI metrics, then flushes before clap exits; developer exclusion is explicit. Exhaustive dotted mapping and centralized E001–E016/common/cancellation classification are proven. Exclusive scoped session guards reject concurrent owners, safely transfer console ownership into attachment, preserve pairing, and mint reattach `session.previous_id`. Capsule identity precedes exporter/startup fallibility, valid launch context links to the bounded startup root, listener readiness completes it once, failure clears ownership, and the PID-1 owner records terminal stage errors while the provider guard is live. Lifetime conformance proves roots do not cover idle session time, identity appears on roots/logs but not Resource, and metrics reject it. |
| 008 | Capsule, image, host CLI/desktop/clipboard, instance-auth, and Docker-context subprocess inventories are closed with privacy-proven owners; runtime and isolation process owners exist. Host viewers preserve inherited terminal behavior with typed absent-tool fallback, while detached daemon startup owns only the bounded spawn-to-readiness interval and records readiness failure or abandonment without a daemon lifetime span. Host and runtime child guards explicitly close read, wait, overflow cancellation, exit, spawn, timeout, and abandonment outcomes; Docker shell execution distinguishes `process_spawn_error` from post-spawn `io_error`; credential and Docker-context lookups retain no token, service, context, socket/TLS location, command, argument, output, or raw failure. Both environment Claude paths have fixed `claude` identity and stable privacy-safe errors. The version probe has a finite deadline; the interactive PTY owns only bounded spawn/setup, immediate read failure, and post-EOF reap phases, never operator idle time, and exports neither custom path nor PTY content. The 1Password state machines share a fixed `op` owner for spawn retry, success, nonzero, timeout, missing pipes, stdout/stderr read failure, wait failure, write I/O, and abandonment, with an exporter privacy matrix. Reader errors and panics can no longer degrade into false success through `unwrap_or_default`; injected transport failures prove one `io_error` owner without pipe content or raw failure export. Create/edit secret-stdin commands use the finite shared process deadline and retain one operation across transient spawn retries. The shared usage CLI runner owns Claude/Amp spawn, pipe, bounded read, wait, external-reap, timeout, success, and nonzero outcomes, with stable errors and an exporter privacy matrix. Managed Codex/Grok children and their external RPC phases have fixed executable/system/method vocabulary, exact outcome counts, timeouts, and payload/path privacy proof. Docker exec response consumption is now inside the bounded `/exec/{id}/start` HTTP owner, so detached responses and stream-read failures cannot occur after a false-success span; exporter proof excludes container, command, output, and raw failure material. The launch root and all eleven production stage guards now have a representative live-wire mixed-outcome proof with exact root/stage counts, parentage, every governed stage value, terminal events and stage metrics; the proof found and fixed terminal events being rejected by their accidental metric-only `launch.target.kind` attribute, and excludes private role, stage detail and failure-surface content. Docker/provider HTTP owners also have live-wire shape and privacy proof. All five governed image download templates now have exact live-wire shape/outcome coverage around a real HTTP exchange; the complete five-by-five cache name/result matrix exports exact events and all three metric instruments, and a production metadata retry proves one event per actual backoff without URL, address, query, payload, or raw error material. The roadmap and registry explicitly govern `db.operation.name=connect`; a real live-wire local Turso flow now covers connect, update, upsert, select and failing select with exact span count, typed failure and duration delivery while excluding database path, SQL, table, account, value and raw driver material. Host cache schema update, metadata/account upserts, table/read/count queries, and row iteration use the same typed DB owner. Config load, global/workspace validation, bootstrap/editor save, and migrate operations use the registered event; a transparent typed ownership marker suppresses outer duplicates, and exporter tests prove exactly-once failures without paths, values, or raw errors. Launch-time grants emit success only after persistence; prompt failures and operator rejection emit one typed failure without role identity, repository location, or raw prompt errors. Shared tonic channels now instrument every physical Parallax DNS/TCP/TLS attempt without recursive self-export, including timeout, reconnect, TLS and mTLS proof. The host attach client now owns bounded open and close phases: `Welcome` completes open, `Shutdown` or acknowledged detach completes close, transport/frame failure closes with `rpc_error`, and task cancellation closes as cancellation without a lifetime span. Remaining work is the literal full integrated launch exercise and fresh integrated gates. |
| 009 | Focused implementation is closed: host reducer and launch cockpit/build-log classifications are compile-time exhaustive; action roots bound synchronous effects, host transitions, and one host/launch render; continuous frames remain metric-only; screen/focus lifecycle, the one-second sliding-window jank crossing, and privacy-safe crash plus shutdown flush are implemented with focused exporter tests. Integrated host/launch/Capsule exporter scenarios must still prove keyboard/mouse equivalence, lifecycle/focus/dwell/action/render causality, and payload/metric privacy before closure. |
| 010 | Focused implementation and conformance are closed; integrated workspace/strict-lint gates remain. Connection ownership and process/Tokio worker/task/queue health have real host/daemon/Capsule OTLP delivery evidence; the audit found and fixed an exact registry-description mismatch that had rejected the whole metric batch. PTY spawn/exit pairing covers all five reasons, typed errors, and raw-error/content privacy. Capsule pane/tab-bar/palette/modal focus uses `WidgetFocusTracker` with paired events/duration metrics and no pane-action spans on the production Multiplexer path. Provider discovery, host/Capsule auth provisioning, Capsule-safe endpoint/auth gating and coverage health, complete privacy-safe Capsule crash shape plus emergency flush, host instance/agent cycles, and standard privacy-safe token-usage metrics are proven. Helper plus real-OTLP tests prove prewarm producer/consumer identity, kind, root/link, fan-out, and outcome semantics; Capsule production-path tests prove quiet cycles export no span while substantive work exports one bounded autonomous cycle with no `job.id`. |
| 011 | **CLOSED.** The exact historical census records all 501 generic macro sites and 25 scoped diagnostic stderr sites as `path:line → decision → semantic owner/operator port`; the final raw-tracing inventory is restricted to governed-facade and composition-root implementation constructs by syntax-aware policy. The generated current-tree census pins every propagated carrier, immediate consumed-error recorder, and typed configuration owner. The final production census is zero invocations of all ten prohibited macros. Generic propagated errors carry an idempotent `TelemetryError`: the first owner emits one bounded type, nested ownership preserves it without another record, and the original source remains available for operator reporting without being formatted during capture. Live exporter proof covers nested ownership and privacy. |
| 012 | Focused implementation is closed; live-receiver, workspace, and lint gates remain. Validation compares per-signal attempt/success/failure deltas rather than force-flush alone; both daemon protocols expose sanitized effective per-signal fingerprints, typed configuration/exporter/flush/shutdown health, and Capsule endpoint/auth coverage; daemon Resource identity is canonical `jackin-daemon`. |
| 013 | **CLOSED at `d466eab2b`.** Independently verified the usage-snapshot rename, real isolated host/daemon/Capsule no-local-artifact lifecycle, explicit PTY fixture exception, in-memory-only diagnostics state, exact namespace exemptions, removed CLI surfaces, and legitimate provider-usage/test-tool exceptions. |
| 014 | Receiver detector coverage is closed: namespace and prohibited-value scans traverse Resource/schema metadata, instrumentation scopes, trace links, log bodies, every metric datapoint family and exemplars, with injected canaries; every acceptance-map test name now resolves. Host, daemon, and Capsule wire suites install real invocation/session context, prove it arrives on spans/logs, reject missing Resources, prove exact Resource equality across all three providers, enforce the fixed Resource allowlist, and exclude the complete correlation family from every metric datapoint/exemplar dimension. The authenticated Capsule-safe child-process case now uses the standard header environment path against a receiver that rejects missing metadata, accepts all three signals only with the exact header and proves the secret is absent from decoded payloads; endpoint-only classification is no longer the authentication evidence. Serialized daemon and Capsule-control matrices prove sampled parentage, unsampled suppression, absent/malformed safe roots, bad product-ID rejection, no side effects on rejection, and server completion after response writes; daemon/control/exec peer-close tests own write failures. A real Capsule status control request now crosses serialization, the production coordinator and response-delivery completion over live OTLP with exact remote parentage. A real daemon Unix-socket status request proves the production serialized client/server/connection chain over live OTLP with exact three-span parentage and no build-ID or socket-path leakage. Concurrent-session evidence runs two overlapping product processes against one live receiver and proves distinct invocation→session mappings remain consistent across logs and spans; sequential reattach linkage remains a separate test. The real host binary now proves a consumed private parse error exits 2 with exactly one bounded `cli.command` help/config-error span, no argv/home leakage and no local artifact. The production Docker and provider HTTP owners now have direct live-wire success/failure evidence for bounded templates, outcomes, typed errors, namespace cleanliness, and container/command/output/provider-response/token/account/query privacy. A real invalid `AppConfig::load_or_init` flow now proves exactly one wire-level configuration failure with bounded global/load/failure/config-error fields and no path, key, value, or parser-input leakage. A real Capsule process spawn failure now proves exactly one `process.command` wire span with fixed executable/outcome/error and no executable path, argument, or token material. A real `Session::spawn` PTY flow proves one spawn/exit pair across actual open, stream and reap behavior, with typed nonzero exit and no terminal bytes, shell command, executable path or private tab label anywhere on the wire. A real Capsule Unix listener now proves successful open, receiver-driven close after peer-confirmed EOF, and failed setup as exactly three bounded live-wire stream phases with one typed failure owner and no socket path, parent path, content or raw error leakage. Slow-export conformance now proves shutdown stays inside the coordinated five-second budget, clears the runtime, and records the deadline timeout explicitly; ordered tracer/logger/meter shutdown remains unit-proven. The full second-line matrix is current and executable: raw logs/spans reject unknown names, forged severity, unknown attributes, invalid types, private values and size overflow into exact health cells; raw SDK metrics reject unknown names/attributes, wrong shapes/types, privacy, size and the 257th cardinality set before inner export, and rejected batches increment attempts/failures without false successes. The scheduled soak is no longer a disabled-facade structural loop: a 10,000-iteration live-OTLP run now exports exactly 10,000 actions, 1,000 autonomous cycles, 1,000 connections, 100 producer/consumer job pairs and 100 linked reattachments, periodically flushes below queue capacity, proves every span closes below one second, checks producer links resolve, preserves invocation identity only where intended, delivers cycle/connection/job duration and active/count instruments plus process-memory/Tokio-queue gauges, excludes `job.id` from metrics, and completes locally in under 60 seconds. The representative production launch controller emits one live root plus all eleven exactly-once child stages across success, skip and typed failure, with exact terminal event counts, stage metrics and private role/detail/failure-content exclusion. Every generated registered event also traverses a live OTLP receiver exactly once at its canonical severity with native EventName and valid TraceId/SpanId, while the parent span carries no duplicated span events; the isolated child explicitly exercises the real trace-level configuration precedence. Historical aggregate output has been removed from the acceptance map; only fresh post-merge command/CI evidence can close it. Exercise representative privacy, cardinality and volume properties. Replace the uncontrolled single-median performance comparison with calibrated evidence; keep `jackin-otlp-testbed` and detector self-tests in required CI. |
| 015 | The diagnostics and usage crate contributor contracts now require the governed facade/direct providers and prohibit the deleted generic macros, file renderers, and parallel sinks; the canonical diagnostics reference distinguishes landed schema, identity, propagation, UI/cycle, metric governance and wire proof from the genuinely remaining integrated acceptance work. Regenerated crate pages plus tracked chrome links pass the RepoFile gate, roadmap/sidebar/research audits and `cargo xtask ci --only docs` are green, and the full production docs build succeeds with 1,092 prerendered routes. A fresh fragment/link scan found and closed the sole internal broken route; its remaining 226 failures are external connection failures across unrelated research sources and need a successful-network rerun before closure. Continue removing false/stale JSONL, file fallback, local-history and unsupported-health claims from source comments, READMEs and docs. Keep canonical docs truthful. Do not mark the roadmap resolved or delete anything yet. |
| 016 | Independently re-read the original roadmap at `fa8194882`, every plan and acceptance row; verify each with requirement-matched current evidence; prove the full expected absence of Collector/gateway/agent/sidecar/backend/dashboard/alert/saved-query configuration, a backend-neutral viewer API, disk-backed telemetry queues, and production profiles; update every roadmap cross-reference to canonical shipped docs; remove the roadmap from indexes/sidebar; require a conflict-free PR with complete CI/conformance/docs/performance evidence; delete the roadmap file and this plan directory only after every row is proven complete. |

Post-row 014 evidence supersedes its performance residual: the active-path gate now normalizes all four workload medians against a same-run calibration benchmark, rejects missing/non-finite/non-positive calibration, accepts uniform runner slowdown, and rejects a calibrated regression above five percent. The download/cache wire case now proves a real private cache root reaches the production cache consumer and its returned path remains absent from every OTLP structure; it also proves a deceptive suffix hostname is not classified or exported as an allowlisted authority. The Codex transcript case proves an arbitrary model name is parsed and consumed by local pricing while only bounded provider/token dimensions reach all three OTLP signals. A real console `MouseEvent` now crosses coordinate hit-testing into a tab switch, while the resulting span and metric expose only the semantic action/screen/widget contract and no pointer-coordinate keys. A real generated agent codename now crosses wordlist selection, live/tab/history state, shell environment injection, and child output while remaining absent from every OTLP structure. The production Docker create request now proves private image, label, environment, bind, network, hostname, entrypoint, workdir, and container-name values are consumed while absent from every OTLP structure. The representative launch case now resolves a private role owner, name, combined ID, and source URL through the production selector, then proves the URL, hostname, query token, and identifiers remain absent from all three signals. The production TLS resolver reads private CA, client-certificate, and client-key paths and contents before a three-signal wire export that excludes every sentinel. Every generated metric stream independently accepts 256 exact series, preserves existing series at the cap, and rejects its 257th series. The shared host, daemon, Capsule-safe, and authenticated Capsule-safe live-receiver scenario attempts canonical DEBUG and TRACE events under default configuration, proves neither reaches the receiver, requires every received record to be INFO or higher, and bounds decoded logs, spans, and metric streams. The reopened representative Plan 014 privacy, cardinality, performance, and volume residuals are closed; fresh integrated gates remain required before closure.

Post-row evidence supersedes Plan 008's launch residual: `conformance_wire_public_launch_controller_exports_complete_pipeline` drives the public launch controller across the canonical injected process and Docker ports, then proves one launch root, all eleven stage children, and governed subprocess and Docker HTTP children occupy the same private, namespace-clean trace. Fresh integrated gates remain required.

Post-row evidence supersedes Plan 009's integrated host-console residual: `conformance_wire_console_reducer_preserves_ui_causality` drives the production manager reducer through workspace-open and mouse-equivalent tab-select messages, then the production adapter owns screen transition, widget focus/unfocus, dwell/focus metrics, and one render beneath each semantic action over live OTLP without the private display label. The production coordinate hit-test and keyboard/mouse semantic equivalence remain separately proven by the named Plan 014 cases. Fresh integrated gates remain required.

## Rejected audit noise

- Executor instructions in operator-authored plan files are intentional workflow data, not a product security vulnerability.
- Non-telemetry `jackin.*` Docker/image labels and code/config identifiers are outside the telemetry namespace ban unless they cross an exported telemetry boundary.
