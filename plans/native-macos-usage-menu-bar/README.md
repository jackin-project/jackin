# Native macOS usage menu-bar program plans → jackin❯ Desktop

Generated by the improve skill on 2026-07-21 against commit `7a52a273`; extended 2026-07-21 against commit `3c49fff0` after an operator-directed concept review against CodexBar (github.com/steipete/codexbar) and OpenUsage (github.com/robinebers/openusage); extended 2026-07-22 against commit `be6fb79e` with the **jackin❯ Desktop v1** track (plans 007–012) implementing the product spec recorded in the roadmap item (`docs/content/docs/roadmap/(operator-surface)/native-macos-usage-menu-bar.mdx`). The roadmap's phases 0–5 and tracks 1–2 (cache unification, Tahoe design) are shipped; distribution engineering (001–003) is done with activation blocked on Apple secrets. The active track is **jackin❯ Desktop v1**: identity rename, Rust-owned view/FFI extensions, status-item display modes, the Liquid Glass Usage window, the glance popover, and parity acceptance. No track reopens provider scope or moves probe logic into Swift.

## Recommended execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| [005](005-host-global-usage-cache.md) | Unify the host-global usage cache so one refresh feeds every surface | P1 (first) | L | — | DONE — re-verified 2026-07-21 HEAD `4833d1b3`: nextest 742/742, clippy, docs audits, `cargo xtask ci --fast` → `ci gate OK` |
| [006](006-native-tahoe-design-refresh.md) | Bring the menu bar app to native Tahoe-grade look and feel | P1 | M | 005 preferred first | DONE — re-verified: `swift build -c release` local; display-only greps clean; GHA Swift tests run 29818901390 (local XCTest unavailable on CLT) |
| [001](001-universal-static-app.md) | Make one universal static app assembly path and gate it on PRs | P1 | L | — | DONE — re-verified: arm64 Apple Silicon app + `verify-usage-menu-bar-app` OK; shellcheck/actionlint 0 |
| [002](002-homebrew-cask-guardrails.md) | Prepare the tap to validate native casks without write credentials | P1 | M | — | DONE — tap merge `e091a86f0da93e865982f68efd9bf8359025f039`; `cask-validation.yml` present |
| [003](003-notarized-release-and-cask.md) | Publish immutable notarized release assets and reconcile the stable cask | P1 | L | 001, 002, **007 (identity rename before first publish)**, operator decisions and credentials | BLOCKED — Apple secrets in `release-macos` (README §6; env secrets total_count=0; 0 codesigning identities); offline fixtures ALL PASS; validate green run 29833722203 |
| [004](004-production-proof-and-roadmap-retirement.md) | Prove the first production install and retire the completed roadmap item | P1 | M | 003, one real stable release, operator-approved merged first cask | BLOCKED — named input: plan 003 must ship notarized ZIP + operator merges first cask PR |
| [007](007-jackin-desktop-identity-rename.md) | Rename the app to jackin❯ Desktop and ship the logomark status-item icon | P1 (first of the v1 track; hard gate before 003 `mode=publish`) | M | — | DONE — `JackinDesktop.app` / `com.jackin-project.desktop` / `jackin-desktop` asset+cask; logomark template resource; local build+verify+fixtures+shellcheck+release_verify green (swift test needs full Xcode; CLT-only here) |
| [008](008-rust-view-ffi-extensions.md) | Extend the Rust view-model and FFI so every v1 string is Rust-owned | P1 | L | — (parallel with 007) | DONE — provider remap lifted; compact pinned/strip/depleted; format prefs; overview_rows; next_refresh_label; estimate_caption + FFI; nextest 1043 green; bindings regenerated; swift build green |
| [009](009-status-item-modes-and-settings.md) | Status-item display modes and the v1 Settings window (S1 + S6) | P1 | M | 007, 008 | DONE — display modes + format prefs + screen-share hide in Settings; depleted via Rust labels (no Swift checkbox flag); swift build green |
| [010](010-usage-window.md) | Usage window — Liquid Glass sidebar/content restating the Capsule dialog (S3 + S4) | P1 | L | 007, 008 (009 preferred first — shared store edits) | DONE — Window scene + NavigationSplitView; full provider card projection; Open Usage… from popover; glass gates centralized; swift build + app verify green |
| [011](011-glance-popover-and-design-contract.md) | Glance popover overview strip + v1 design contract (S2 + S5) | P1 | M | 008, 010 | TODO |
| [012](012-v1-parity-acceptance.md) | v1 parity acceptance and roadmap closure | P2 | S | 007–011 | TODO |

## Dependency notes

- **005 runs first** (operator directive, 2026-07-21): a single source of truth for usage on the host — refresh anywhere, visible everywhere, at most one prober per account host-wide — outranks all cosmetic and distribution work. It is pure Rust + docs and conflicts with nothing else.
- 006 prefers 005 landed so the UI copy ("numbers match every jackin❯ surface") is true when written, and touches only Swift + a small additive FFI display API.
- 001 and 002 can run in parallel with each other (different repositories) and with 006 (001 owns packaging/scripts, 006 owns Swift sources; only a trivial `Package.swift` target listing may overlap — coordinate that one hunk).
- 003 must not begin credentialed publication until 001 and 002 land. 004 stays a separate production-evidence gate. The jackin❯ program owner — not the external tap executor — reconciles this README after the tap PR lands.
- **v1 track (2026-07-22).** 007 (identity rename) is a hard gate before 003's first `mode=publish` — no published artifact may carry the old identity. 007 and 008 are independent and may run in parallel (007 = Swift/scripts/CI/docs identity; 008 = Rust + bindings). 009–011 consume 008's FFI exports; 010 runs before 011 so the popover never loses detail before the Usage window can show it (no regressive intermediate state). 012 is the cross-cutting acceptance after 007–011. Recommended order: `007 ∥ 008 → 009 → 010 → 011 → 012`; 003/004 activation remains ops-blocked and can proceed any time after 007.
- New display strings requested during 009–011 land Rust-first following 008's step pattern (Rust fn + FFI export + golden) — never composed in Swift.

## Operator decisions (recorded 2026-07-21)

Approved as the permanent distribution policy for this program (recommended values adopted as recorded decisions). Secret **values** are never stored in git — only names, types, and governance.

1. **App identity** — bundle ID `com.jackin-project.usage-menu-bar`; visible name `jackin usage`; app product `JackinUsageMenuBar.app`; cask token `jackin-usage-menu-bar`. Team ID and certificate SHA-256 fingerprint are repository **variables** (non-secret) `JACKIN_DEVELOPER_ID_TEAM_ID` and `JACKIN_DEVELOPER_ID_CERT_SHA256`, set when the Developer ID cert is provisioned. **Superseded 2026-07-22 (operator):** the app is renamed **jackin❯ Desktop** — visible name `Jackin Desktop`, product `JackinDesktop.app`, bundle ID `com.jackin-project.desktop`, cask `jackin-desktop`, asset `jackin-desktop-<VERSION>-aarch64-apple-darwin.zip`; rename must land before the first notarized publish (no published artifact carries the old identity). Full spec: roadmap item `docs/content/docs/roadmap/(operator-surface)/native-macos-usage-menu-bar.mdx`. Team/cert variables unchanged.
2. **CFBundleVersion** — full-history `git rev-list --count HEAD` with stable `X.Y.Z` marketing version; reject `-dev` and non-numeric forms; do not use GHA run numbers.
3. **Immutable recovery** — never replace a published signed ZIP; conflicting bytes require a new version; missing sidecars may be regenerated only after validating the existing ZIP.
4. **App Store Connect key** — **team** API key (issuer ID required). notarytool contract: `--key <p8> --key-id <id> --issuer <issuer>` (CI) or `--keychain-profile` (local).
5. **Secret governance** — GitHub environment **`release-macos`**, GitHub-hosted **macOS-14** runners only (never Velnor), secrets listed by name under decision 6.
6. **Credential inventory (names only)** — environment secrets: `DEVELOPER_ID_APPLICATION_P12_BASE64`, `DEVELOPER_ID_APPLICATION_P12_PASSWORD`, `APP_STORE_CONNECT_API_KEY_P8`, `APP_STORE_CONNECT_KEY_ID`, `APP_STORE_CONNECT_ISSUER_ID`. Owner: jackin❯ program owner; rotate on cert/API-key expiry. Tap: existing `HOMEBREW_TAP_TOKEN` (PR open/update only).
7. **Tap authority** — writer may open/update the deterministic PR; **first cask is never auto-merged**; prefer a narrowly scoped GitHub App when available.
8. **Launch proof (Plan 004)** — require **Apple Silicon** (arm64) macOS 14+ runtime launch before claiming runtime proof; Intel/x86_64 is deferred. Architecture inspection alone is insufficient.

## Program boundaries

- Provider scope is frozen by ADR-011: `Agent::ALL` + Z.AI/GLM + MiniMax. CodexBar and OpenUsage are **concept references only** (status-item conventions, cache honesty semantics, glass adoption patterns) — never a provider list to import, never code to copy.
- Rust owns probes, cache, cooldown, severity, and view shaping; Swift is display-only. The one cache lives on the host and is bind-mounted into containers; no daemon and no new IPC channel in this program.
- Sparkle, DMG/PKG packaging, app sandboxing, Intel/x86_64 menu-bar builds (deferred), daemon integration, new providers, WidgetKit, multi-status-item layouts, and speculative entitlements are out of scope. The roadmap accepts cask-only updates.
- The release artifact is `jackin-desktop-<VERSION>-aarch64-apple-darwin.zip` containing exactly `JackinDesktop.app`, created after stapling (identity superseded 2026-07-22 by operator decision 1's supersession note; plan 007 implements; historical plan texts 001–004 carry the old strings and are read through that supersession).
- The app remains macOS 14+, `LSUIElement`, no root daemon; Tahoe Liquid Glass arrives via the macOS 26 SDK with centralized availability fallbacks, not a raised floor.
- The final release ZIP receives three file sidecars — `.sha256`, `.bundle`, and `.sbom.json` — plus a GitHub-hosted build-provenance attestation record under the existing release trust policy.

## Considered and rejected

- **Rewrite probes in Swift to make the app "pure Swift" end to end:** rejected. Display parity with jackin❯ and the single-cache invariant both require the shared Rust core; a Swift probe matrix is exactly the parallel-copy problem this program exists to eliminate (and ADR-011 forbids it). "Pure Swift" is satisfied at the application layer: the app is a native SwiftPM SwiftUI app, no web views, no cross-platform shells.
- **Daemon-owned usage cache with a socket API (v1):** deferred. The filesystem coordination layer (account-keyed snapshots + cooldowns + best-effort locks on one shared root) already exists and satisfies refresh-anywhere semantics; the roadmap keeps the daemon as an optional later upgrade.
- **Migrate `MenuBarExtra` to AppKit `NSStatusItem` + `NSPanel` now:** deferred until evidence. CodexBar/OpenUsage did it for key-window and positioning control, but jackin❯'s popover is simpler; Plan 006 carries an explicit STOP condition if `MenuBarExtra(.window)` misbehaves on macOS 26.
- **Keep the dylib-based local build and add a separate static release build:** rejected because two assembly architectures would drift and the dylib path does not implement ADR-011's staticlib/XCFramework decision.
- **Publish the current pre-stapling ZIP:** rejected because the staple changes the app after archive creation; Homebrew must receive a ZIP created from the final stapled app.
- **Treat release + formula version as one `published` boolean:** rejected because partial releases need repair without regenerating timestamped Apple signatures.
- **Auto-merge the first cask from the secrets-bearing release job:** rejected because the tap has no cask precedent and installation validation must run without tap write credentials.
- **Ship Sparkle now:** not worth doing; the roadmap says cask-only distribution is acceptable.
