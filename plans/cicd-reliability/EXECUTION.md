# CI/CD Reliability Execution Record

Goal: Jackin + upstream Velnor CI/CD — green pre-merge predicts green main, separate fmt/clippy/test steps, 120s pipelines, no repeated setup.

## Current continuation — 2026-09-22

| Agent | Assignment | Result | Independent status |
|---|---|---|---|
| `/root/live_coordination` | Live PR/check/review inventory | #1053 Policy failure isolated to stale generator sidecar; #1044/#1052 overlap and have no reviews | parent rechecked GitHub run/logs |
| `/root/policy_challenge` | Adversarial policy diagnosis | Historical `01bc16b2` runtime changed only the ownership-sidecar scan hash; current D3 pin `7576f40f` passes `--check` after regeneration | parent reproduced with the exact published runtime in a clean checkout |
| `/root/apple_migration` | Migration convergence | #1044 and #1052 are competing shapes; one current-runtime carrier must retain generic Apple discovery and Renovate | pending independent implementation/review |
| `/root/product_openrouter` | OpenRouter incident | String-derived `401` classifier remains; typed transport/HTTP boundary required | implementation delegated separately |
| `/root/product_clock_daemon` | Render/daemon incidents | Render samples wall clock repeatedly; daemon failure is export completion/ownership, not socket RPC deadline | implementation delegated separately for render; daemon lifecycle slice pending |
| `/root/desktop_parity` | Candidate/Desktop evidence | Desktop remains main-only and supersedable; candidate coverage absent | pending generic Velnor capability |
| `/root/rust_performance` | Rust/cache/Docker evidence | phased steps landed; repeated bootstrap, cache churn, Docker E2E absence and reuse gaps remain | pending measured implementation |
| `/root/velnor_lifecycle` | Velnor phase/promotion lifecycle | regeneration clears phase identity; runtime publication and promotion contracts exist but self composition remains wrong | pending Velnor fix |
| `/root/evidence_rollup` | First-attempt evidence | historical ledger is stale; no automated collector/rollup exists | pending implementation |

Live repair: PR #1053 body was replaced (template removed). Its generated-tree
failure was reproduced with the published pinned runtime, repaired only by a
state-sidecar regeneration, and rechecked with `--plain --check`. This is not
closure for the report findings.

## 2026-09-22 continuation — parent branch `codex/ci-reliability-20260922`

- Jackin commits `67f89e95`, `7c178a8a`, and `900d7b6f` close the fixed-clock
  render and OTLP testbed lifecycle defects with focused regression tests.
- `762ea61e` converges the schema-2 Apple migration on published Velnor
  `816046893ef55359ee2f41903d1f1f893d59303e`: generic Swift/XcodeGen/BoltFFI
  discovery, typed XCFramework producer-consumer transport, Xcode 26.6 pin,
  Renovate writer/validator, and generated state all pass exact regeneration.
- `63c8e8b1` wires sccache only where generated Apple profiles provision
  `cargo:sccache`; Rust MBX and release jobs remain wrapper-free. This is
  static/configuration evidence only. No hosted speedup claim is made.
- `b6b3744d` preserves typed HTTP status across Codex/Claude provider fetches;
  transport/decode text cannot synthesize auth or rate-limit classes. Usage,
  broker, FFI, diagnostics, OTLP, projection, xtask tests, clippy, format,
  actionlint, and exact generator checks pass locally.
- Collector `7c57de6c` live probe (window `2026-09-22T10:00:00Z` →
  `15:10:54Z`) collected `18` obligations/attempts and recorded `2` missing
  push-event gaps. Rollup: `11` successes, `7` non-success outcomes; Desktop
  had cancellations/data-quality; qualified green and six-nines remain false.
  Independent review found the scheduled 31-day event-cap, retired-workflow
  identity, inferred-obligation, artifact-retention, and mutable-derived-field
  gaps. Collector hardening is still active.
- Velnor selection PR #1075 merged as `aa2345bd`; runtime publication/pin
  promotion is pending. Phase-retention PR #1076 passed local Velnor tests but
  its first live PR run failed before candidate publication because base
  runtime `37e7814` rejected generated `precondition` phases. A bootstrap fix
  is delegated; Jackin does not pin or claim the unproven phase change.

## Post-#1053 live verification — 2026-09-22

Agent `/root/report_postmerge_correction` reconciled the merged report with
the live candidate evidence. PR #1053 merged as `df4671e4`. Its only generated
state change was the scan digest in
`.github/ci/.github-actions-generator-state`:
`7229bed326311884` → `bbbfa7f57b91cb3b`. The change is a global generator
input, and the live Control / Planning result selected 40/40 units with the
explicit fail-closed fallback. This is a correctness pass for conservative
selection, not a zero-work result.

Evidence:

- [CI / PR run 35651713880](https://github.com/jackin-project/jackin/actions/runs/35651713880), [Swift job 106505596038](https://github.com/jackin-project/jackin/actions/runs/35651713880/job/106505596038): 22m01s; 78 Swift tests passed. The job report recorded repeated native work (`desktop-release` compile, 380 compiler-output lines) and no validated cross-workflow product reuse.
- [Post-merge Desktop run 35656263744](https://github.com/jackin-project/jackin/actions/runs/35656263744), [job 106520585495](https://github.com/jackin-project/jackin/actions/runs/35656263744/job/106520585495): 35m01s; 19 UI tests passed; the independent Apple path rebuilt native products.

Both Apple correctness verdicts are green. The 120-second objective remains
FAIL. Reuse remains unproven and is not credited as a gain. No-work remains
unproven/FAIL for this state-sidecar-changing documentation candidate. The
historical first-attempt rows below are retained unchanged.

Independent review status: this documentation-only correction is prepared on
`docs/report-live-results`; parent review is required before merge. No generated
`.github` file was edited by this change.

Pinned-render check: Velnor source revision `4dec6b9ec28b0d51cb370fd8f5d5401c6186adf0`
was built from source and run with `--plain --check` against this tree. It
reported `Generated files are current`; no generated file or state-sidecar
change was needed for this documentation-only correction.

## Baselines (2026-09-21, parent-observed)

- Jackin root: `/Users/donbeave/Projects/tailrocks/jackin-project/jackin`, branch `main`, HEAD `fce94cea`, tree clean.
- Velnor root: `/Users/donbeave/Projects/tailrocks/velnor-project/velnor`, branch `codex/rolling-preview-legacy-migration-20260920` (NOT main), 1 commit ahead, untracked work present (`.firecrawl/`, `goal-finish-and-merge-ci-runtime-products.md`, `velnor-bastion-final-plan.md`, `__pycache__/`). DO NOT disturb; coordinate before checkout/branch moves.
- `gh` authenticated as `donbeave` (repo, workflow scopes).
- Mandatory jobs: run 35521080097/job 106105226160 (test failure `conformance_partial_success_is_not_retried`), run 35515575859/job 106090835001 (desktop-merge cancelled ~35m28s).

## Workstreams (workflow `cicd-discovery`, 6 agents + synthesis)

| Stream | Scope |
|---|---|
| failure-inventory | all retained runs/attempts, failure ledger |
| parity-gate | PR vs merge_group vs main vs desktop/release parity, final gate |
| hooks-hk-prek | hk vs prek decision + staged-snapshot hook design |
| velnor-arch | generator model, phase split sites, schema compat |
| perf-cache | critical path, cache audit, 120s budget per class |
| baseline-state | SHAs, pins, toolchains, regen procedure |

## Parent-verified facts (2026-09-21)

- Main HEAD `fce94cea` CI run 35521080097 FAILED; only failing job is 106105226160 `Rust · jackin-diagnostics`, step 29 `Run unit checks` (single opaque step — confirms combined fmt/clippy/test).
- Failure signature (log, /tmp/joblog.txt): `conformance_partial_success_is_not_retried` panicked at `crates/jackin-diagnostics/tests/wire_failure_support/mod.rs:51`, left 7 right 1. Fail-fast: 108/122 shown, rest cancelled.
- Prior main `a5e10227` run 35517379726 green → regression introduced by `fce94cea` (#1012 security fix). Prime parity case: why didn't PR validation catch it.
- Desktop merge cadence on `fce94cea` (35521079960) succeeded; cancelled 35m28s run was on `0163d1b7` (35515575859, event push).
- Open PRs: 1013 (active, in-progress run), 1002, 1004, 1005, 1007 (draft, `ci: pin the attested workflow runtime and regenerate` — directly relevant), 1009 (draft).
- Log fetch note: `gh api .../logs` needs `--allow-escape-sequences`.
- #1012 diff (a5e10227..fce94cea): 12 files, capsule/runtime/protocol + docs; NO diagnostics-crate changes → cross-crate behavioral regression. Failing assert is line 51 `testbed.logs().len()` (7 actual vs 1 expected) — extra exports.
- Generation input `.github-gen/velnor-workflow.toml`: schema 1, pin `4fa7a3a`; generated `project.toml` header says schema 2 (output version ≠ input schema). Rust unit commands render fmt → nextest → clippy (wrong order, one step).
- `ci-unit-rust.yml` (502 lines): single job `verify-github`; step `Run unit checks` shells `velnor-workflow run --config .github/ci/project.toml --scope … --unit …`. Phase split needs runtime CLI phase selection + generator multi-step emit.
- `desktop-merge.yml` (42 lines): concurrency `desktop-merge-<repo>-<ref>` cancel-in-progress → 14:43 push (a5e10227) cancelled 14:08 run (0163d1b7, job 106090835001). Same-ref serialization; perf/parity agents confirm + fix.
- Gating: `ci-required` (always(), needs all units) validates selected-must-pass / unselected-must-not-fail; no `merge_group` trigger anywhere. PR jobs gated on `plan.outputs.units` (change selection).
- Parity finding (PARENT-VERIFIED, diagnoser confirming mechanism): PR #1012 run 35519793543 RAN diagnostics, 122/122 PASS (job 106101859590, /tmp/prdiag.log). PR head tree == fce94cea tree; pr/full commands identical. NOT a selection gap: same test green on PR, red on main (105/122 position both). Leads: test nondeterminism (Testbed ports? global exporter? timing) or job env diff. Diagnoser redirected.
- #1012 metadata: head 0a053b15, base a5e10227 (= prior main), squash-merged fce94cea 15:55Z; PR run 15:30Z success.
- Diagnoser verdict (harvested): TRANSIENT FLAKE, #1012 exonerated (zero dep-closure overlap; vendor OTLP treats partial-success as Ok, no retry; unique 127.0.0.1:0 ports rule out cross-talk; 60ms test vs retry backoff incompatible). Local: 25/25 single, 5x122 suite, 60+24 instrumented — all green. Mechanism UNRESOLVED; next probe: instrument testbed LogsService::export with timestamp+payload-hash if recurs. Enabler: exact wire-count asserts over async batching pipeline with zero content inspection.
- Diagnostic rerun: run 35521080097 attempt 2 (failed-jobs-only) GREEN — transient confirmed; attempt-1 failure preserved in ledger.
- Slice `diag-harden`: `assert_wire_requests` helper (+73/-3, test-only) keeps exact counts, adds log-record content assert + rich mismatch dump (per-request spans/records/metrics, span/event/metric names, health snapshot). Parent-reviewed, fmt-clean, target test green with CI flags. Branch `cicd/integration`.
- Hooks ground truth: NO hooks exist (stock samples only, no core.hooksPath, no hk/prek config). prek 0.5.3 present as user-global mise shim; hk absent; neither pinned in repo.
- Perf ground truth: mise `[tools]` lists 15 `cargo:`-backed tools with `cargo.binstall=true` (binstall-first, source fallback) — source-fallback path is the desktop-merge compile-time suspect.
- Runtime model (Velnor main `runtime.rs`): `CiUnit.commands(lane, scope)` over 4 string arrays; `run --config/--scope/--unit`, NO phase selector. Phase split = typed phase model + runtime phase flag + generator multi-step emit (velnor-arch detail pending synthesis).
- Fragile pattern to remove: `prerequisite_commands` matches `command.contains(" clippy ")` + `replacen(" clippy ", " check ")` — exactly the substring detection the goal forbids. Non-full affected-scope units run only check prerequisites (no tests); #1012 diagnostics ran tests ⇒ was full unit.
- Velnor main RED: root = PR #968 regenerated tree without promoting pin (stuck at 38dbf85e); push-event Policy polls 15 min for candidate keyed by unmatchable merge SHA (futile by design); Preview fails generated-tree fast. Fix = `promote --rev HEAD` at tip (velnor-promote-fix in flight, /tmp/velnor-promote, no push); structural follow-up = fail-fast Acquire on non-PR events. Runtime product for 59396040 exists (run 35535449251).
- Velnor main RED (superseded note): CI/main 35535449676 (59396040) + Preview failed; Runtime products green. Policy job 106143544517: committed tree differs from base-pin render (`project.toml`, `ci-main.yml`, `ci-pr.yml`, `preview.yml`, `release.yml`, generator-state — "rerun gen"), candidate-path fallback then 15-min timeout waiting for `velnor-workflow-candidate-8881e50b88c80c40-Linux-X64` (never published). Preview run 35535449623: `Resolve preview identity` failed. Runtime releases exist (latest `velnor-workflow-runtime-v1-92e3ae31fd276e74`); Jackin pin 4fa7a3a → af140ad4 exists. Upstream regen + candidate publication owned as follow-up (needs slot).

## Slices landed

- 2026-09-21: PR #1007 blocker fixed — `33b91891` fmt-only (`crates/jackin-xtask/src/desktop/tests.rs`), pushed to `codex/ci-performance-campaign`; xtask unit failed rustfmt --check → ci-required FAILURE. Enabling condition: no pre-commit fmt enforcement. Comment: PR #1007 comment 5752641444. VERIFIED: rerun 35537475111 xtask job success; zero failed jobs at 21:08Z check.
- 2026-09-21: Slice `diag-harden` → PR #1014 (branch `cicd/integration`, Jackin's integration branch): test-only wire-count actionability + EXECUTION.md. Attempt-2 green confirms transient; attempt-1 preserved.
- 2026-09-21: PR #1014 CI caught `clippy::panic` on the new helper's `panic!` (job 106151006979) — fixed with `assert!` + same message (`1f12d1a5`), verified with exact CI clippy/fmt/nextest flags. PROCESS LESSON: every implementer brief must require local verification with exact CI flags (fmt, clippy `-D warnings`, nextest); pre-commit hooks would have caught this. Ledger + child payloads persisted under plans/cicd-reliability/.
- 2026-09-21: PR #1014 Policy FAIL `generated-tree` (run 35538495995): branch changed scanned inputs without regenerating `.github/ci/.github-actions-generator-state`. NOT main drift — passing PRs #1002/#1013 (same base `fce94cea`) all commit regenerated state. Root cause: implementer slice omitted the regen step; enabling condition = no local gate runs `velnor-workflow --check` before push. Fix: regen with the pinned published runtime only (never hand-edit). PROCESS LESSON: every branch touching the Jackin tree must end with pinned-generator regen + commit of the state file; verify with `--check`.
- 2026-09-21: PR #1014 regen fix LANDED + VERIFIED: `ccbbebf4` (bookkeeping) + `6474f6fb` (state-only regen, scan `79aad67c→eaca5ab7`, 18 outputs byte-identical). Renderer = published `velnor-workflow-runtime-v1-af140ad4d8d84326` macOS-ARM64, SHA `7bee5aab…` matches manifest, `--revision 4fa7a3a` = declared pin, `--check` green pre-push. Policy PASS run 35539394262. Unit jobs pending.
- 2026-09-21: Velnor promote fix INTEGRATED → PR tailrocks/velnor#984 (branch `cicd/promote-tip-59396040`, commit `b6767850`, /tmp/velnor-promote): pin `38dbf85e→59396040`, 13 files 94+/94- pure pin+metadata. Runtime product `velnor-workflow-runtime-v1-92e3ae31fd276e74` verified. CI pending. Phase-split (agent 12, /tmp/velnor-phase) + hooks-implement (agent 14) still running.
- 2026-09-21: Velnor PR #984 MERGED (`b4d7661`, --merge per repo convention): 22 pass + 48 change-selection skips, Codex review no findings, state CLEAN. New-main verification runs: CI/Main 35540073650 + Preview 35540073593 (in progress at merge+1m; old-main runs 35535449676/35535449623 failed as diagnosed). Parity designer (agent 16) DONE → plans/cicd-reliability/parity-design.md: ruleset `protect-main` verified (DCO+Policy+ci-required, strict, squash-only, no merge_queue rule, admin bypass hole named); slices (a) merge_group (ir.rs sites + DCO/Policy queue-capability blocker), (b) empty-selection guard (exact bash condition, legitimate-empty class), (c) desktop/release-drill pre-merge + Jackin-only hardening steps.
- 2026-09-21: #984 main-verification: Policy green in 32s on post-merge main (was 15m34s timeout-fail) — stale-pin mode eliminated. BUT main run 35540073650 failed on unrelated Velnor product test `velnor-runner::scaleset_allocator thundering_herd_grants_exactly_one_per_permit` (scaleset_allocator.rs:103, left 1 right 2; 2507/2556 pass). Same tree green on PR #984 → flake hypothesis; #984 touched zero Rust source. Failed-job rerun triggered to test; Preview 35540073593 in progress. Velnor product flakes are out of goal scope (don't block generation/runtime/adoption) — ledger entry only unless it recurs on phase-split validation. RERUN VERDICT: failed-job rerun on same tree GREEN → transient confirmed (attempt-1 preserved). Preview 35540073593: `Resolve preview identity` now PASSES (#984 fixed that stage); remaining failures are downstream product-packaging, out of scope: arm64 missing `aarch64-linux-gnu-gcc` cross toolchain (aws-lc-sys build), amd64 `expected exactly one .deb to rename, found 2`. Neither blocks generation/Runtime-products/Policy (all green) or phase-split validation. Perf designer (agent 15) DONE → plans/cicd-reliability/perf-design.md: (i) affected-selection PROVEN DEAD — every regen'd branch commits `.github/ci/.github-actions-generator-state` → FULL_SELECTION_PREFIXES fallback → 40/40 units on PRs #1007 + #1014; plans/*.md unmatched → same fallback; (ii) Actions cache 30 entries/11.14GB = 111% cap → LRU churn, mbx 0%-effective (per-unit 2GB blobs miss every commit) + 124s/job post-export; (iii) desktop mise cache saved BEFORE 26-tool auto-install → 448-573s unconditional; (iv) sccache unwired by construction (mbx-excluded for Rust, no Swift arm, no check-profile env). Slices: S1 MISE_*=false on check-profiles (~8min, Jackin-config-only, precedent in-file) → NEXT; S2 sccache env macOS; S3 mbx→sccache transport; S4 narrow state-file/unmatched selection; S5 prebuilt boltffi + fingerprint align. 120s verdicts: PR plausible iff narrow+warm (needs S3+S4); main/desktop/release need gate redefinition (attestation model / shard split / exempt), not speedup — recorded as structural constraints.
- 2026-09-21: PR #1014 Codex P2 ADDRESSED (`8f2d72ac`, pushed): helper now asserts per-request log-record distribution (each of N requests carries exactly one matching record; flattened-total-only could hide 0/1/2 corruption). Implementation note: first cut with nested loops hit `clippy::excessive_nesting` under `-D warnings` → rewrote with flat_map chains (matches testbed style). Verified CI-exact locally: mbx fmt ✓, clippy ✓, nextest 122/122 ✓. Regen protocol: clean-worktree `--check` GREEN → .rs-only edits don't move the scan (earlier delta came from plans/*); no regen needed. PROCESS REFINEMENT: regen-check via detached worktree at HEAD (immune to concurrent uncommitted agent work in main checkout).
- 2026-09-21: PR #1014 MERGED (squash → Jackin main `d0d4ee09`): 46 pass + CLEAN, P2 addressed + 👍, no new threads. Main verification runs: CI 35542103804 + desktop 35542103447 (in progress), Renovate green. Post-merge: `cicd/integration` branch now behind; next Jackin slice rebases onto `d0d4ee09` (note: squash changed SHAs — branch commits bd819099/b8767616/1f12d1a5/ccbbebf4/6474f6fb/8f2d72ac all landed as one).
- 2026-09-21: Phase-split DELIVERED (agent 12, /tmp/velnor-phase `cicd/rust-phases`, uncommitted, 38 files +3905/-267, report /tmp/phase-split-result.md): typed ValidationPhase s1+s2, run --phase, per-phase steps sharing job/cache, substring rewrite deleted from production (2 remaining sites = test needles, reviewer to confirm), conditional doctest (8 Velnor units 4-phase, bin-only/tests-only 3-phase), suite 1849+20 green. PARENT REVIEW FINDING — BOOTSTRAP CONSTRAINT: old pinned runtime rejects `--phase` (parse_options `unsupported CI argument`, proven on base 5939604) AND new TOML keys (deny_unknown_fields), while `checked_in_workflows_match_the_generator_byte_for_byte` forces in-PR regen ⇒ impl+regen cannot land as one green PR. Sequence: PR-A (runtime capability, legacy render, green on old runtime) → merge → runtime products publish → PR-B (pin promote + phased render) → Jackin PR-C (pin adopt + regen). Independent reviewer (agent 17) spawned: correctness verdict + exact A/B split spec + Jackin impact (lints-cdylib 3-phase, declared-rows tag alignment). `mbx test --doc` verified supported locally (CI action-version risk noted for PR live-check).
- 2026-09-21: Phase-split REVIEW DONE (agent 17 → /tmp/phase-review.md): SHIP-WITH-FIXES. Implementer's "green" claim FALSE (2 red integration tests: rust_order_render doctest-append, velnor_first_ci legacy-step count — both untouched files, missed expectation updates → F2, PR-B). F1 MAJOR (demonstrated): unphased rust prereq tier silently no-ops via commands_for_phase empty-short-circuit (breaks old-TOML→new-binary + regen-gated/ci_tasks units) → restore base rewrite as fallback + regression test. F3 split-blocking: unconditional VALIDATION_PHASES decl (4 junk lines on non-rust reusables) → conditional skip. F4/F5 should-fix, F6 optional skipped, F7 claim corrections (incl. jackin-lints row = NO-OP, excluded crate — cdylib 3-phase proven by unit tests only). Jackin regen predicted 28×4-phase + 7×3-phase (bin-only xtask/dev/pr-trailers + 4 fuzz), no misalignment (no ci_tasks/workspace_check on rust rows). mbx risk CLOSED (CI mbx 1.12.0 ≥ assumed 1.12.0). Fixup agent (18) owns /tmp/velnor-phase: rebase b4d7661 → F1/F3/F4/F5 → save /tmp/phase-B-activation.patch → PR-A deactivation → gates (suite/fmt/clippy/empty-generated-diff).
- 2026-09-21: S1 VALIDATED for post-#1015 implementation (parent): no `[mise]` section exists in Jackin's input — the supported path is `[check_profile.env]` (renders to job env; proven: renovate-upstream-sources.yml:24 carries `MISE_TASK_RUN_AUTO_INSTALL=false`, green in 12s). S1a = MISE_* env on desktop-merge/desktop-scheduled profiles (input-only) BUT requires tools-coverage audit (profile lists 9 tools; task needs ~26 — auto-install-off with missing tools fails the task) + cache-save-after-install timing (generator-side? implementer to determine). S1b (unit-job mise env) needs generator support. Implementer agent after #1015 merges.
- 2026-09-21: Hooks slice HARVESTED (agent 14 → /tmp/hooks-result.md) + parent-reviewed. hk 2.0.1 pre-commit (fmt→clippy→actionlint→swift, review mode, stash=git, `mise run hooks-install`) + `cargo xtask clippy-affected` (proven closure, 326/326 xtask tests). Deviations accepted: (1) local-not-global bootstrap (global hardcodes --staged=no-stash, proven; staged-snapshot REQUIRES local); (2) xtask alias +--locked (hook builds rewrote Cargo.lock); (3) direct tool calls not `mise run` (GUI minimal-PATH lacks mise; textually identical bodies — drift risk noted). Parent reversals/corrections: (a) clippy excludes mise.toml/mise.lock/hk.pkl CORRECT (toolchain = rust-toolchain.toml-pinned, covered by both steps; my early flag withdrawn with evidence); (b) xtask-lint step REMOVED (umbrella red on main both modes + enforced by zero CI jobs → new ledger class XTASK-GATE COVERAGE GAP; hook must not exceed all green CI). Parent E2E on shipped ae993f77 (throwaway worktree): fmt-defect commit BLOCKED (fix unstaged, exit 1), dead_code clippy BLOCKED, staged-clean+unstaged-dirty PASSED with dirt preserved (commit 5c4aa5eb), hk validate + check --all 5/5 green. Known: ~1/30 silent hook run (agent-flagged, CI remains backstop).

## Integrated synthesis (parent, 2026-09-21 — workflow synthesis dropped 4 payloads; recovered from session logs)

- Inventory (complete): 40k runs 05-31..09-20, classes C1–C15 in FAILURE_LEDGER.md + gaps GAP-1..4. Main CI 42 runs (21F/10C/11S), desktop 13 (5F/1C/7S).
- Parity: merge queue unsupported (generator test forbids merge_group; ruleset has no MQ rule); gate passes vacuously on empty selection (NO min-units guard — gap); desktop-ci referenced by no workflow (main-only); scope_for_event already handles merge_group→full.
- Hooks: hk-first decision (mise-native, one Pkl, stash isolation, global --mise install); nextest/desktop stay out of hooks. VERIFICATION PENDING (hk-verify agent).
- Velnor-arch: split sites mapped (scan/rust.rs phase tags, Unit/CiUnit phase vec, ir.rs per-phase steps or run --phase, replace :2569 substring rewrite); HEAD already emits fmt-clippy-nextest order (pin renders old order); s1 (Jackin) vs s2 (Velnor) compat open.
- Perf: desktop mise HIT yet 26 tools rebuilt ~9.5min EVERY run (cache ineffective); sccache installed but unwired; bindings-check boltffi 7m56s; cancel attribution confirmed.
- Baseline: regen live-verified read-only; runtime release covers linux-x64/arm64 + macos-arm64; toolchains recorded.
- #1012 parity answer (parent): flaky test passed on PR (122/122, job 106101859590), failed attempt-1 main on identical tree+commands, green attempt-2. NOT selection.

## Decisions

- D1: hk-first CONFIRMED (independent hk-verify vs live docs 2026-09-21): prek capable but hk wins — mise-env provisioning via global launcher (prek isolated-env ignores mise.toml pins; system hooks need ambient PATH, fails GUI git), one Pkl shared editor/hook/CI + profiles/diagnostics, stash isolation incl. untracked + backup patches. Narrowings: PREK_HOME cost applies to managed hooks only; CI-parity shape is a wash. Corrections: no as-is builtins for clippy/swiftlint (customize to exact CI flags); swift-format custom (no builtin); non-macOS swift skip strategy needed; lockfile pins hk version + mise.lock; CI parity via generator inputs, never hand-edit.
- D1a (clippy placement): verifier proposed pre-push-only for clippy (cold cost) — REJECTED by goal §7 (pre-push-only insufficient). Resolution: pre-commit clippy with PROVEN dependency-closure scoping (changed packages + reverse dependents from cargo metadata); workspace fallback documented. Implementer must prove closure correctness.
- D2: Velnor work in fresh /tmp clones on branch `cicd/rust-phases`; existing velnor checkout untouched (foreign branch + uncommitted work).
- D3: Jackin integration branch `cicd/integration` (PR #1014 open); parent owns git for both repos.
- D4: Phase split = typed phase model + `run --phase` + per-phase steps; kills `contains(" clippy ")` rewrite.

## Slices

(none yet)

## Evidence

- Discovery synthesis ref: pending.
- 2026-09-21: #1017 MERGED (squash f664f4d3): S1 mise whole-toolset auto-install off on desktop profiles. 46/46 green on 17bf8abc (rebased over #1016 docs; base-current at merge; only review note = Codex usage-limit notice). Post-merge main: Renovate 35550272669 SUCCESS; Desktop 35550272723 in-progress (S1 live verification target); CI/main 35550272891 queued. Prior main CI 35548774180 (2e4489cf) SUCCESS.
- 2026-09-21: Post-#1017 main CI 35550272891 (f664f4d3) SUCCESS first attempt (all jobs incl. Swift green, final gate passed). Desktop 35550272723 still in progress at +25min; S1 verification pending its logs.
- 2026-09-21: S1 LIVE-VERIFIED on post-#1017 desktop 35550272723 (f664f4d3) SUCCESS. Wall 24m36s (created 01:14:49Z → updated 01:39:25Z) vs 46.5min pre-S1 SUCCESS baseline ≈ 22min saved. Setup installed exactly 9 tools (141.2s); gap tools (cargo-audit, cargo-dylint, codebook-lsp, dylint-link) listed "(missing)", never installed; ZERO tool-install signals in Run desktop-merge step (24 compile lines = project Swift/Rust builds only). MISE_*=false env present. Empty-gap audit proven correct: run green without the 4 tools. All 3 post-merge runs SUCCESS (Renovate 35550272669, CI/main 35550272891, desktop 35550272723).
- 2026-09-21: Velnor PR-A OPENED (#993, tailrocks/velnor, branch cicd/rust-phases @ ae013d0f, base 7131195a): typed ValidationPhase + run --phase, legacy render, F1/F3/F4/F5. Agent 18 harvested (gates green, then stalled 2.5h without submitting; parent cancelled, merged main #987-#990 clean, re-verified fmt/clippy/suite/regen-0byte, squashed 8 commits to 1 signed with identical tree, pushed).
- 2026-09-21: Velnor PR-A #993 MERGED (dc6674f5): 11 pass / 40 legit-skip / 0 fail on a7b27107 (base incl. #991), MERGEABLE+CLEAN, no substantive comments. Post-merge main runs + runtime product publication pending; PR-B blocked on published runtime.
- 2026-09-21: Velnor Preview main-run failure is PRE-EXISTING, not PR-A-caused: identical `generated-tree` FAIL (.github/ci/.github-actions-generator-state vs pin 4c12824f render) on ad652d56 (run 35552208624, pre-PR-A) and dc6674f5 (run 35553026741). Drift dates to #987-#991 era. Not blocking runtime products (SUCCESS on ad652d56). Revisit after PR-B pin promotion; possible self-heal.
- 2026-09-21: Velnor CI/main post-PR-A failure also PRE-EXISTING: identical `Acquire candidate generator product` 15-min timeout (no candidate product published) + symlinked .github/CLAUDE.md refusal on ad652d56 (run 35552208635) and dc6674f5 (run 35553026761); Policy→ci-required→Control/Required cascade both. PR #993's own Policy PASSED → main-only parity gap. Non-blocking for PR-B (Runtime products SUCCESS). Ledgered for follow-up.
- 2026-09-21: Velnor PR-B OPENED (#995, branch cicd/rust-phases-b @ 7d2cbaf1, base dc6674f5): phased scan activation + F2 updates + regen + D19 pin to dc6674f5. Agent 20 harvested (7 commits, gates green); parent spot-verified (regen deterministic 0-byte, byte-identical gate test ok, fmt ok, generated ci-unit-rust has fmt→clippy→tests→doctest steps with legacy fallback only for unphased), squashed to 1 signed (identical tree), pushed.
- 2026-09-21: PR-B #995 contract failure diagnosed + fixed. Root cause: phased scan adds doctest cmd to rust unit recipes; recipe feeds mbx compat digest (snapshot_compatibility, ir.rs:1734), so 9 recorded cache keys changed and parameterized_callees test failed (CI job 106197474774 + local repro). Fix: rotate fixture (9 hashes, each verified single-segment diff), full contract suite 6/6 green locally. Committed 47806d22 (signed), pushed. Enabling condition: recipe-coupled fixture must rotate on any unit-command change (precedent 9de09111); PR-B agent gates covered velnor-workflow pkg only, CI contract unit caught it.
- 2026-09-21: PR-B #995 Policy failure diagnosed: NOT pre-existing. `generated-tree` FAIL on run 35555855347 (job 106199237163): committed tree is byte-identical to candidate render (local dry-run 0 changes) and candidate acquisition SUCCEEDED, but policy's resolve_pinned_binary (policy.rs:1490→1210) fatally rejects the env-slot candidate binary ('is not the declared pin') before render_with_candidate is consulted. Enabling condition: generator change + D19 pin bump combined in one commit (7d2cbaf1) — candidate closure != pin closure, so pin resolution dies first. PR-A passed only because its tree matched its pin render (no candidate needed).
- 2026-09-21: PR-B fix per D19 procedure (commit 42c75604, signed, pushed): pin reverted to base 4c12824f, tree regenerated with branch renderer; diff verified pin-literals-only (13 files, 127+/127-, state-file hashes derived) with fmt→clippy→test→doctest steps intact; contract suite 6/6 green locally; dry-run 0 changes. Expected CI path: Acquire candidate → Enforce resolves pin via running base validator (slot 1) → candidate exception verifies tree. Pin advances in a single pin commit after merge+publish.
- DEFERRED (validator hardening, own slice): resolve_pinned_binary should skip (not fatal) the env-slot binary when the candidate manifest binds it as this tree's in-flight candidate (digest match + closure == audited candidate closure), falling through to other pin sources. Preserves fail-closed; unblocks combined generator+pin PRs. Not needed while D19 two-step procedure is followed; base validator is immutable at PR time so the fix only helps future PRs after merge+publish.
- 2026-09-21: PR-B DOUBLE-BIND proven. (a) 47806d22 (pin dc6674f5): Policy FAIL — resolve_pinned_binary fatally rejects env-slot candidate before candidate exception (policy.rs:1490→1210). (b) 708ea71c (pin 4c12824f, merged main 80777836): Control/Planning FAIL — base runtime schema lacks `phases`+`check_commands` (unknown-field; BTreeMap order reports check_commands first). PR-A slipped through with legacy render (old-schema tree) but left pin stale; back-to-back schema+render breaks with stale base pin make combined PRs unmergeable. PR-A passed via candidate path only because its tree stayed old-schema.
- 2026-09-21: Main drift ROOT-CAUSED: tree (agent instruction files AGENTS.md/CLAUDE.md + state) != pin-4c12824f render (proven: pin render to scratch = 21 files vs tree; --check refuses symlinked stale .github/CLAUDE.md). Drift forces main ci-main down candidate path -> 15-min acquire timeout (pre-existing failure explained, not just timed-out infra). Fix = D19 single pin commit, NOT deletion (files are load-bearing agent guidance re-emitted by newer generator).
- 2026-09-21: SEQUENCE: PR-1 #997 (pin 4c12824f->80777836, 70b51de3, no source change; candidate==pin closure so Policy passes; runtime published so Planning parses; heals main Preview+ci-main) -> PR-B #995 rework (phase source + phased tree, pin UNCHANGED at new base; Policy via candidate path slot-1+candidate; Planning via PR-A-schema runtime) -> PR-3 (Velnor pin->PR-B merge + regen) + Jackin PR-C (adopt PR-B merge pin). PR-1 verified locally: regen-0, pin-literals-only 13 files, plan parses, contract 6/6.
- 2026-09-21: #997 retargeted to 8b8f1cbe (merge 06d92321) after #992 landed; green 22/0 twice; SQUASH-MERGED as b145e770 (repo blocks merge commits; learned at merge time). Post-merge: products SUCCESS; CI/main failed ONLY rust-velnor-runner lane (structural Acquire/parse classes gone; all 4 lane commands pass locally incl. nextest 2552 — flake-or-linux-specific, log pending); Preview verdict pending.
- 2026-09-21: PR-B #995 REWORKED onto b145e770 (merge 301d129f, pin->b145e770, regen idempotent, phases intact, base-runtime plan parses, contract 6/6, velnor-workflow suite 1875+ green) and went FULLY GREEN 22/0 with Policy PASS via candidate path (14m25s). SQUASH-MERGED as fff18da8 (squash title kept stale original PR title mentioning dc6674f5 — cosmetic only). Known debt: main Preview will fail generated-tree until PR-3 (pin->fff18da8 + regen); products must publish first.
- 2026-09-21: PR-3 #999 opened (Velnor pin->fff18da8). Jackin PR-C #1018 opened (d1da55bf: pin 4fa7a3a->fff18da8, phases verified fmt->clippy->test->doctest with fail-fast + per-unit validation_phases, S1 guards intact, desktop diff = safe upstream mise dedup, template->docs/ with GitHub-docs proof, 2 dead files dropped with zero refs, runtime release verified 4-asset parity). Local gates: regen-0, plan parses, manifest 112 + xtask 328 + image 132 green.
- 2026-09-21: PR-C Policy FAIL root-caused (run 35565296848): ownership-state scan digest recorded BEFORE the template relocation, so pin-product render differed in exactly one digest line. Reproduced with the published product (digest-verified download); fix = regen on final tree (1-line state change), pin-product dry-run 0. Committed 27939e77. LESSON: always regen LAST after every worktree edit; verify with the published pin product, not just the local build.
- DEFERRED (Velnor enhancement, own slice): #992 full-tree replacement deletes unowned hand-written files under .github/ with --force and no keep-list (s2/mod.rs:6766-6787 + lib.rs twin). Consumers with live hand files (PR templates etc.) must relocate outside .github/. A typed input keep-list would remove the footgun; relocation to docs/ preserves function meanwhile.
- 2026-09-21: PR-3 #999 (Velnor pin->fff18da8) MERGED as 9f437e96. Post-merge main: CI/main 35566258047 SUCCESS + Runtime products 35566257687 SUCCESS first attempt (D19 self-hosting lag closed; phased render live on Velnor main). Preview 35566257840 FAILED — pre-existing class (see Preview root-cause entry), not PR-3-caused.
- 2026-09-21: PR-C #1018 (Jackin adopt fff18da8 + regen) MERGED as fd18e95c. Post-merge main: CI/main 35568814633 SUCCESS (phased fmt->clippy->test->doctest live on Jackin main), Renovate 35568814270 SUCCESS. Desktop cadence 35568814352 SUCCESS — PR-C tree fully green on main (all 3 runs first-attempt). Main has since moved to 9ee50f6a (#1013 multi-account feature, others-owned), also fully green (Renovate 35575539440, CI/main 35575539502, Desktop 35575539424 all SUCCESS): phased tree composes cleanly with the next merge.
- 2026-09-21: fff18da8 CI/main 35564570736 disposition: Policy + ci-required + Control/Required FAILED (predicted D19 self-hosting-lag class: phased tree vs legacy-render pin dc6674f5). RESOLVED by successor #999 — 9f437e96 CI/main SUCCESS first attempt. Attempt-1 failure preserved. Exact Policy lines (job 106223740445, /tmp/policy-fff.log): `error: generated files differ: .github/ci/project.toml, .github/workflows/ci-main.yml, ci-pr.yml, ci-unit-rust.yml, preview.yml, release.yml, .github/ci/.github-actions-generator-state; rerun generate` (BASE_PIN b145e770 legacy render vs phased tree — squash title's dc6674f5 confirmed cosmetic), then `no candidate product velnor-workflow-candidate-eeae77040653a669-Linux-X64 was published within 15 minutes` (push event has no PR candidate). D19 self-hosting lag as predicted; #999 closed it.
- 2026-09-21: Velnor Preview RED root-caused, PRE-EXISTING, NON-BLOCKING. Failing test `policy::tests::cli_requires_the_base_validator_revision` (run 35566257840/job 106230735664, nextest 189/2044 fail-fast, panic src/policy/tests.rs:1106): asserts CLI-without---base-revision fails with a '--base-revision' error, but generated release jobs export VELNOR_WORKFLOW_POLICY_REVISION job-wide ('Set trusted workflow policy revision' step, present ONLY in release-*/debian/identity/metadata jobs), so the policy.rs:272 env fallback resolves the revision and the CLI proceeds to an IO error ('read workflow directory .../.github/workflows: No such file or directory') -> assert fails. CI lanes (no export) pass the same test — explains the success/failure lane split. Identical failure on pre-change de5a1c46 (run 35562973341: same rust job + both deb builds); Preview red 11 straight main runs 7131195a->9374a4d3 incl. PR-A dc6674f5. Does NOT block generation, Runtime products (SUCCESS), or Jackin adoption (fd18e95c CI-main SUCCESS); Preview is not a merge gate (#999/#1000/#1001 merged through red). Proposed fix (own slice, NOT taken: Preview additionally fails deb-build + identity stages; release-packaging healing is parallel others-owned work on a racing main): hermetic subprocess test per the velnor_first_ci.rs child-env pattern (crate forbids unsafe, cannot unset env in-process) and/or scope the policy-revision export to policy-enforcement steps only. Local repro blocked: cargo test OOM-SIGKILLed x2 (box load 17+, foreign rustc fleet), prebuilt binary stalls on launch; CI log + source + workflow archaeology conclusive without it. Log: /tmp/preview-rust-fail.log (1460 lines).
- 2026-09-21: §8 GAP named (Velnor-self only; Jackin PASS): unit rust-velnor-workflow carries NO phases key, so its release job renders the legacy single `Run "Rust crate (velnor-workflow)" checks` step. Mechanism is explicit code, not drift: typed `regen-gate` declaration (.github-gen/velnor-workflow.toml:222-228) prepends the generator self-check command, and s2/primitives/regen.rs:40-49 calls `unit.clear_phases()` because phases are a positional parallel vec ("Inserting shifts every position, so a unit that gains the gate drops its phase tags"). Structural fix = per-command phase tags (gate renders as its own prerequisite step + fmt/clippy/test/doctest steps); touches contract schema + runtime + renderer + fixtures s1+s2 — OWN SLICE, kept in goal, deferred with cause: (a) local verification impossible (OOM-blocked builds), (b) Velnor main racing with others' planner-adjacent work (#1001), (c) Preview red for independent reasons so the slice is unverifiable live. Jackin unaffected: input declares no regen-gate; all 35 rust crate units PHASED, only rust-policy (dependency advisories `mbx deny check`/`mbx audit`, intentional untyped gate per rust.rs:711) unphased by design.
- 2026-09-21: NOTE: run id 35572948188 cited in pre-compaction notes 404s (stale transcription); all dispositions above use fresh API-verified ids (3556/3557/35568814633-series).

- 2026-09-21: §14 NEGATIVE CONTROLS green. Probe A #1022 (fmt defect, jackin-build-meta): Formatting=failure, Clippy/Tests/Doctests=skipped, ci-required FAIL, merge BLOCKED — closed unmerged. Probe B #1023 (clippy unused-var, fmt-clean): Formatting=success, Clippy=failure, Tests/Doctests=skipped, ci-required FAIL, BLOCKED — closed unmerged. Branches deleted. §7 hook proof: both defect commits blocked pre-push (fmt: auto-fix + fail_on_fix, staged defect intact + fix left unstaged; clippy: unused-var error across 10-package closure). HK=0 bypass used ONLY to stage the CI probes (defects never merged). Structural: ci-unit-rust.yml:438-527 = 4 separate `--phase` steps, no always()/continue-on-error, rc-propagating pipefail — fail-fast gating by construction.
- 2026-09-21: ee1da0c6 FULL DISPOSITION: push CI/main 35581113129 FAILURE (Swift watchdog only; all else green) — attempt-1 preserved; manual dispatch re-run 35581117100 on identical tree SUCCESS 22m27s (transient confirmed); Desktop 35581112849 SUCCESS 31m47s; Renovate SUCCESS. Main was red ~1h on the Swift flake; healed by #1024 merge (1daf30b4 post-merge runs in flight at record time).
- 2026-09-21: C4 FIX #1024 merged (1daf30b4): builder child implemented streaming pre-build (desktop.rs:37-38,725-737), independent reviewer child clean, hk hook requested one rustfmt chain expansion (accepted), PR CI green incl Swift 22m10s with 381 Compiling lines + 14s incremental pack. Ledger C4 CLOSED (cargo window); bindings-window residual (452s silent, 1.33×) named as follow-up slice.
- 2026-09-21: CHILD EVIDENCE HARVEST (8 subagents, all read-only except builder): (a) timing: last-5 CI/main walls 1136-1347s ALL FAIL 120s; Desktop 2170s SUCCESS / 334s cancelled / in-progress; queue samples 4-6s; n=5+3, ~3h window. (b) Velnor @5bfc9d5b (#1005 pin; #996/#1002/#1003/#1004 in): CI/main SUCCESS, Preview red with 3 INDEPENDENT causes — rust-velnor-tools NEW test failure (github_raw_store cleanup), rust-velnor-workflow SAME env-test class (cli_requires...:1106), arm64 deb NEW (missing aarch64-linux-gnu-gcc). (c) regen-gate slice scoped: ~20 files across s1+s2 twins (contract schema, runtime commands_for_phase, release/IR renderer, scan producers, velnor_first_ci + fixtures). (d) §14 11-row gap map: bullet 11 PROVEN, 1-10 partial with exact next experiments designed (test-defect probe, 2×2 staging, filter-evasion, stale-green, gate-negation incl. vacuous-pass documentation, parity drill, fork probe, poison-cache, checksum, overlap).

- 2026-09-21: §14 BULLET 1 COMPLETE. Probe C #1028 (test defect `assert_eq!(1+1, 3)` in jackin-build-meta, fmt+clippy clean): Formatting=success, Clippy=success, Tests=failure (41s), Doctests=skipped, ci-required FAIL, merge BLOCKED — closed unmerged, branches deleted. Local hooks PASSED without bypass (test defects aren't commit-gated locally by design — full suites too slow for pre-commit; CI is the enforcement point). With #1022 (fmt leg) + #1023 (clippy leg), all three §14 bullet-1 legs proven live: defects fail their separate step, downstream never starts, gate rejects, merge blocked.
- 2026-09-21: 03d1fe8d (#1026) FULL DISPOSITION: CI/main 35590036716 SUCCESS; Renovate 35590036245 SUCCESS; Desktop 35590036278 CANCELLED 21m in — superseded by b569b1b5 push (concurrency cancel, not a failure; no Desktop verdict exists for 03d1fe8d).
- 2026-09-21: EXTERNAL MERGES assessed (independent investigator child, read-only; parent verified pin claims). #1027 (382018b6) = pure runtime (account_config guard/quarantine/atomic write), zero .github touch, no CI interaction. #1019 (b569b1b5) = Velnor pin fff18da8→44243ed4 (change-aware minimal-work + lane-scope fix), 11 files +342/-56: plan gains expected-work.json artifact + planned_no_work/no_work_reason outputs; ci-required gains Download/Verify/Add-runtime + aggregate(expected vs reported unit results, FAIL-CLOSED: real-work plan with zero records fails); unit workflows gain Record/Upload unit result; project.toml +2 pty fixture bins (structural rescan). Pin VALID: rev on Velnor main (ahead 3), Runtime-products run 35584453657 SUCCESS with Linux-X64/ARM64 + macOS-ARM64 artifacts. §8 PRESERVED: ci-unit-rust.yml:438-501 still Formatting→Clippy→Tests→Doctests with identical validation_phases gating (6 fuzz/dev units fmt+clippy+test as before). planned_no_work has NO consumer branch — no-work passes through aggregate empty-vs-empty; nothing skipped beyond pre-existing affected-selection. #1019's own PR ran FULL scope (all ~40 units + ci-required SUCCESS), so CI-tree changes trigger full validation. §12 SATISFIED pending b569b1b5 main-CI verdict (in flight).
- 2026-09-21: b569b1b5 CI/main 35591885154 SUCCESS — #1019 adoption downstream-validated on main; §12 FULLY SATISFIED (pin valid, products green, regen, PR full-scope + main CI green). b569b1b5 Desktop 35591884968 CANCELLED (superseded by ebf5d5cd push — second consecutive main head with no Desktop verdict; Desktop concurrency cancels the older head). ebf5d5cd (#1025 others-owned multi-account runtime, zero .github touch) is tip; its mains in flight.
- 2026-09-21: §14 BULLET 2 COMPLETE (independent experimenter child, isolated worktree /tmp/jackin-stageproof, since removed; main checkout untouched, nothing pushed). hk 2.0.1 pre-commit (`stash="git"`, `fix=true`, `stage=false`, `fail_on_fix=true`; fmt `cargo fmt`, clippy `cargo xtask clippy-affected`). (1) staged-clean + unstaged-fmt-defect: commit exit 0, unstaged sha256 identical, defect preserved. (2b) staged-fmt-defect: exit 1 sole cause fail_on_fix, index blob identical (fix NOT restaged), unstaged + untracked byte-identical, REVIEW mode `MM`; clippy closure = staged crate only. (3) staged clippy unused-var (fmt-clean): exit 1 `-D unused-variables -p jackin-pr-trailers`, index + unstaged identical. (4) `mise run fmt-fix`: no restage (index blob identical), unstaged markers intact, tree fmt-clean after. Untracked survived hook failure byte-identical (stashed + restored). Full logs /tmp/stageproof-t2b.log, /tmp/stageproof-t3.log (child-side).
- 2026-09-21: TIMINGS (monitor child): b569b1b5 CI/main 35591885154 SUCCESS 21m39s (44 jobs); 03d1fe8d CI/main 35590036716 SUCCESS 27m9s; #1029 run 35592676084 (6ac1b769) SUCCESS 18m13s all 43 green incl both Swift. 120s budget still FAIL across all classes — no recoloring.
- 2026-09-21: #1030 FULL-SCOPE VERDICT = BY-DESIGN (planner-selection source probe @Velnor 44243ed4, schema-1 root pipeline: Jackin input is schema=1 so runtime.rs+reuse.rs serve plan). AGENTS.md-only diff: no watch matches (zero .md in project.toml), not .github → github_verdict None; bun-jackin-docs + swift-package-native (mise) units OPAQUE → classify_path Unknown → full_selection all 40 units (reuse.rs:627-636,1055-1066; runtime.rs:2733-2735). No instruction-file or manifest global class exists; FULL forcers = Scope::Full events, no-base, git failure, unparseable entry, .github globals/unknown (fail-closed), unmatched+opaque. planned_no_work emitted iff selection empty AND reason Some (empty-without-reason = plan ERROR); no Jackin job branches on the output — no-work path = empty `units=` skips workload gates + aggregate reads marker from artifact (empty+marker PASS, empty-without-marker FAIL). PREDICTION for #1031 (root Cargo.toml comment-only): NARROW 37/40 (all rust watches own Cargo.toml literally; swift-native joins via ffi dependent edge; bun/docker/design-prototypes excluded; fallback_full=false). §9 NOTE: unmatched-path full fallback is a documented fail-closed property — instruction-only changes pay full workload by design; narrowing requires transparent unit table (bun/mise opacity removal = own Velnor slice).

- 2026-09-21: §14 BULLET 3 COMPLETE. Probe #1031 (root Cargo.toml comment-only, zero .rs; run 35593268806, closed unmerged, branch deleted): plan scope=affected selected 37/37 required (36 rust + swift-native), all 36 Rust checks ran+passed, exactly 3 skipped (bun-docs, docker-construct, swift-prototypes), ci-required PASS, merge CLEAN (green because validation ran, not skipped). Local clippy-affected printed "no Rust changes; skipping" on the same diff — benign precision divergence for comment-only, but exposed the real T2a gap (below).
- 2026-09-21: 37/40 ADJUDICATION (parent, vs 120s-child "universe drift" claim): NARROW IS GENUINE. #1031 plan log: pin 44243ed4 (16 hits), scope=affected, units=37=full_units (identical sets), NO fallback_reason; #1030 same era: units=40=full_units WITH fallback_reason "unmatched path AGENTS.md selects every unit". Source: full_units = affected set running full commands (runtime.rs:2876,3045; test comment :7084-7085), NOT the universe; fallback path sets full_units=all+reason (:2783-2784). Killer check-set evidence: #1029 vs #1031 both 48 checks, identical names except the 3 units carry " / GitHub" suffix when run (lane-admitted) and lack it when gate-skipped — drift would show 37 checks, not 40+3-skipping. The 3 exclusions match the pre-registered source prediction (no rust edges) exactly. 120s child's wall-time point (3 skips save ~0 wall under Swift) stands; its mechanism claim is refuted. Suffix semantics: " / GitHub" = lane-admitted execution marker.
- 2026-09-21: §14 BULLET 5 (gate negation) PROVEN live+code (synthesis child, read-only). FAILED leg: #1022/#1023/#1028 runs all ci-required FAIL + Control/Required FAIL + BLOCKED (`selected CI job ... did not pass: failure`). CANCELLED leg: #1029 run 35591101264 gates cancelled (40 partial greens ignored), successor 35592676084 fully re-validated. SKIPPED-legitimate: #1031 expected-work artifact 10635481049 (37 required, planned_no_work=false) → aggregate collected 37/37 PASS → Validate via unselected branch. Code (ci-pr.yml@ebf5d5cd): presence-only marker L53-58; per-unit contains() gates L123; zero-records-on-real-work fails L2811-2814+L2847; selected+admitted admits only success L2872-2875; final mirror L3665-3674. GAP (live): missing-record, unexpected-skip, incomplete-matrix legs are code-proven only — no dedicated live probes. Caveat: aggregate binary pinned-external; in-repo contract = comment + exit codes.
- 2026-09-21: PROBES 6a+9a PASS, LOCAL ZERO-CI (experimenter child, worktree since removed). 6a: github_pr==github_full 40/40, velnor_pr==velnor_full 40/40; only runner split swift→macos-26; actual phases 28×4+7×3+5unphased (phases exist only on rust kind: 3-phase = 4 fuzz + dev/pr-trailers/xtask; unphased = bun, docker, rust-policy, 2 swift) — expectation text corrected, no equivalence break. 9a: Verify block verbatim-extracted (ci-pr.yml:2783-2791, byte-identical across pr/main/unit-bun/docker/rust; swift unit has none — fresh pinned install, separate trust path): repo-swap→exit1, run_id-swap→exit1, flipped-byte→exit1 at recompute, malformed-closure→exit1; valid-hex-but-wrong closure→Verify exit0 caught by NEXT step (closure--binary binding correctly ordered after digest verify). Findings (not failures): job_id unbound (only !=""), head_branch recorded-not-verified — within-run replay not cryptographically excluded.
- 2026-09-21: 120s CRITICAL-PATH MEASURED (child, read-only gh APIs). b569b1b5 CI 21m39s = 74s queue + Swift 19m21s + 64s tail; #1029 PR 18m13s = 125s queue + Swift 15m50s; Desktop newest success 35581112849 31m47s (bindings-check 11.6m incl silent boltffi regen 10.4m, staticlib#1 7.5m, UI tests 5.7m). Swift inner: staticlib desktop-release 6m22s + silent bindings-gen ~7.4m + SwiftPM ~73s; Swift unit cache SKIPPED (none exists), Mise miss +~3m (boltffi rebuild). Rust fan-out never wall-critical (max job 8m45s; per-job restore ~713MB, mbx save 2.26GB/sample — all in Swift's shadow). macOS queue 74s→296s volatile; Swift exec variance 15.3m→22.3m on cache hits. Per-class: warm PR 18m13s (9×), manifest PR 24m33s, instruction PR 28m35s, main 21m39s/20m54s, desktop 31m47s, cold unobserved. Bottom line: every class misses 9–20×; wall = queue + swift-package-native (15.8–23m, ~8× budget alone). Needs NEW slices: Swift split/cache, boltffi-gen speedup, queue capacity, Desktop↔CI-Swift dedup. Correction applied: child's "0 jobs saved / universe drift" claim refuted (see 37/40 adjudication); "can never touch wall until swift deselected" stands.
- 2026-09-21: T2a STAGED-ONLY MANIFEST MISS FOUND+BEING FIXED (investigator child → builder child, PR #1036 open). clippy-affected semantic refinement compared HEAD↔worktree only; staged breaking `[workspace.lints] missing_docs=deny` with worktree=HEAD → empty selection → exit 0, while real clippy exits 101 with 800+ errors (proven E2E T2a/T2b, no commits). Shipped hook accidentally safe via hk stash="git" collapse; CLI contract broken for manual xtask/mise/hk-check invocations. Fix: three-blob read (HEAD, index via `git show :path`, worktree), changed-sets over HEAD↔index ∪ index↔worktree, unprovable→widen; preserves T1 comment-only skip. Also noted: classify fails open on unknown non-doc paths (hardening: forward to widen).
- 2026-09-21: #1029 MERGED as 65e9dfbd (46 pass, CLEAN, 0 reviews/1 comment); post-merge mains 35596986705 (CI) + 35596986361 (Desktop) queued at record time, Renovate SUCCESS. NEW RECURRING CLASS: `Renovate · schedule` EACCES `mkdir '/tmp/renovate/cache/containerbase'` — 5 of last 8 red (35597217333, 35505690982, ...). Non-blocking (not a required check) but main-red; fix builder spawned on fix/renovate-cache-eacces (verdict: generated-vs-hand-owned determines Velnor-vs-Jackin slice).
- 2026-09-21: #1036 MERGED (squash `7e223f8c`): T2a clippy-affected staged-manifest fix. Head `80404681`, 2 files (`clippy_affected.rs` +39/-9, `tests.rs` +87): three-blob read (HEAD/index via `git show :path`/worktree), HEAD↔index ∪ index↔worktree unions, either-pair-unprovable→widen. Independent review = donbeave COMMENTED APPROVE-equivalent (self-approval impossible by platform; 29/29 xtask tests verified at head, T1/T2a/lock-twin bodies inspected). PR CI: narrow selection, Swift green, ci-required SUCCESS.
- 2026-09-21: #1037 MERGED (squash `c90bf147`): docs evidence batch (§14 bullets 3+5, 37/40 adjudication, 120s model, new classes). No code.
- 2026-09-21: Velnor #1022 MERGED (`f406baff`, `renovate_renderer.rs` ONLY file): Renovate ownership fix runs unconditionally. Runtime products 35600683439 SUCCESS (Linux-X64/ARM64 + macOS-ARM64, release `velnor-workflow-runtime-v1-64813ff2fdf17dad`). Adopted by Jackin #1041 MERGED (squash `799f5774`): 11 files +60/-61, pin `44243ed4→f406baff`, exactly one functional line (renovate.yml `cache-matched-key` gate removal); ci-unit-rust diff = pin literals only, phases intact (Formatting→Clippy→Tests→Doctests + validation_phases gating). `799f5774` verdicts: CI/Main 35608115267 SUCCESS + Renovate 35608114761 SUCCESS; Desktop structurally unavailable (cancelled by #1043, see chain entry). §12 satisfied.
- 2026-09-21: Velnor `f406baff` CI/Main 35600683746 + Preview 35600683931 failures TRIAGED PRE-EXISTING D19 SELF-HOSTING PIN-LAG, #1022 exonerated. Policy log (job 106335785491): `generated files differ: ci-unit-bun/docker/docs/opentofu/rust.yml + generator-state` — BYTE-IDENTICAL line (modulo timestamp) on parent `447c0f83` run 35600604077 (job 106335526425); both fail Policy→ci-required→Control/Required only. #1022 touched zero workflow files. No fix opened; owner = Velnor workflow pin-advance chore (same class as fff18da8→#999).
- 2026-09-21: §14 BULLET 4 COMPLETE (stale-green). Probe #1032 (closed unmerged): push A `55b1c66e` 46/46 success run 35596185556 → push B `5f73b141` merge BLOCKED (mergeStateStatus still BLOCKED at close) with ZERO carried checks — fresh full-matrix run 35598570563 (40 pass + 3 fail: runtime unit + both gates). B terminal red via FLAKY `host_daemon::tests::conformance_wire_real_daemon_socket_exports_bounded_parented_rpc` (10.02s, tests.rs:263; job 106329218385) — strengthens conclusion: B re-executed the full matrix rather than inheriting A's green.
- 2026-09-21: §14 BULLET 7 COMPLETE (fork validation). Probe #1035 from deleted fork (head repo null, forkprobe author; closed unmerged): push A `b5547bcf` green full unprivileged CI run 35597160258 — CORRECTION: 34 pass/12 skip (46 checks; 28 units + Policy/Planning/ci-required/Control-Required/Renovate/DCO), not 33/12. Push B `e89f8433` (intentional failing assert) enforced: build-meta FAILURE + ci-required FAILURE + Control/Required FAILURE, mergeStateStatus BLOCKED. Logs: CI/PR-B extracted 4.6MB/64 files (CORRECTION: not 6.4MB) with 0 raw-secret hits (no ghp_/gho_/pat/token/key material; only expected `***` mask placeholders on checkout token lines). Live merge evaluation: workflows resolved `@refs/pull/1035/merge` → `2b1d067a` ≠ head (no stale merge). release.yml = push-tags + workflow_dispatch only (no pull_request trigger); renovate.json = no automerge key.
- 2026-09-21: §14 BULLET 6b COMPLETE (PR↔main parity). Probe #1034 (closed unmerged, `98b64132`): PR run 35596828177 planned 28 units vs `workflow_dispatch scope=full` 40 units on the same SHA — PR ⊂ full exactly (full-only = 12: bun/docs, docker, arrayref, dev, xtask, policy, design-prototypes, pr-trailers, 4 fuzz). GAP PROVEN: 0 of 44 PR check contexts mention desktop — `desktop-merge.yml` has no `pull_request` trigger (main-push + dispatch only), so Desktop validation is main-only by construction. Release validate run 35596835902: build job reached in_progress (runs ✓), sign job skipped ✓ (no secrets on untrusted path).
- 2026-09-21: §14 BULLET 8 COMPLETE (cache reuse/invalidation). Probe #1038 (closed unmerged): R1 (`0bcfc842` brand comment, run 35599045487, 28 units SUCCESS) vs R2f empty-commit (`66227a00`, run 35611817500, 28 IDENTICAL units SUCCESS, identical restore keys) — no verdict diff on identical inputs; R3 (Cargo.lock +newline, run 35615962582) → 37 units (36 lock-watchers + swift-native via dep) with unit-cargo primary MISS → `cargo fetch --locked` RAN (0 occurrences in R1/R2f) — invalidation correct. Save-gate: 0 cache entries under PR merge-ref/branch refs. LIVE CONFOUND (root-caused): GitHub event `base.sha` lags main tip 20–30+ min while merge ref is fresh → leaked `plans/**` file (unwatched) → fail-open FULL scope on R2/R2b; de-confounded via rebase (R2a/R2c both 28-identical green). Proves the classify fail-open hardening note live. Infra flake: R3 attempt 1 arrayref+telemetry on 3× cache-service 504 (rerun→green).
- 2026-09-21: §14 BULLET 10a COMPLETE (PR self-cancel isolation). Probe #1047 (closed unmerged): A `766ef5d2` + B `936b6e37` at +50s → R_A 35621055453 `completed/cancelled` mid-run (plan success, bun cancelled) while R_B 35621147934 `completed/success` green on head B; 0 references to R_A in R_B's ci-required log (run-id-scoped download); R_A wrote 0 caches; cancel blast radius = R_A only repo-wide in 15:40–15:56 (siblings untouched).
- 2026-09-21: DESKTOP SUPERSEDE-CANCEL PATTERN CONFIRMED STRUCTURAL. Every main head's Desktop run cancelled by the next main push under rapid cadence: `65e9dfbd→7eb2105d→7e223f8c→c90bf147→08713c9b→820ed5d6→799f5774→82ff0593→87521a95→68edddab`, ALL Desktop=cancelled except the tip. Only the latest main's Desktop can complete; intermediate heads have no Desktop verdict by construction (concurrency cancel, not failure). `799f5774` final: CI/Main + Renovate SUCCESS, Desktop cancelled by #1043.
- 2026-09-21: EXTERNAL MERGES assessed (usage-lane .rs + docs, PR gates green at merge): #1033 (`7eb2105d`, broker discovery), #1040 (`08713c9b`, broker lifecycle), #1039 (`820ed5d6`, cursor/codex), #1043 (`82ff0593`, docs ledger addendum), #1046 (`68edddab`, launch credential fence; CI/Main + Renovate SUCCESS, Desktop tip-running). POST-MERGE NOTE: `08713c9b` CI/Main 35605917622 FAILED on openrouter flake (`openrouter_snapshot_with_refused_base_is_error_never_fabricated`, NeedsLogin vs Error, tests.rs:329; file untouched by #1040/#1039; green on #1040's PR and on successor `820ed5d6`) — main red one merge, healed by #1039. TRANSIENT, attempt preserved. Also: `65e9dfbd` carried `Renovate · schedule` FAILURE (EACCES class recurring, non-blocking). OPEN + UNTOUCHED: #1044 (Apple CI→generic recipe), #1045 (D1 instruction-only re-proof), #1030, #1007, #1004.
- 2026-09-21: EXTERNAL #1042 MERGED (`87521a95`, repromote generator 01bc16b2 = f406baff+4, CONTAINS the renovate fix; current main renovate.yml has 0 `cache-matched-key` occurrences — fix survives). Its CI/Main 35614729832 FAILED on 100% EXTERNAL/FLAKE causes, pin exonerated: bun + diagnostics + dev + process on GitHub cache-service HTTP 504s in setup (mbx restore, 3×504 + retries), docker on external tirith-release download curl-22, runtime on the KNOWN-FLAKY `host_daemon ... bounded_parented_rpc` socket test (tests.rs:263, 10.06s — 3rd occurrence today: b4-B + here + earlier flag; product-test timing flake, owner = product team, recorded not fixed). Gates failed correctly as consequence. Main healed by `68edddab` (CI/Main SUCCESS). No action; attempt preserved.
- 2026-09-21: #1049 MERGED (squash `52c5236c`, docs evidence batch 4, independently reviewed CLEAN). Its CI/Main 35626321731 FAILED in `jackin-usage` Tests only (`parity_console_render_smoke_overview`, "must contain expires in 30d", job 106421702380) — TRIAGED FLAKE, NOT the docs change: `68edddab→52c5236c` diff = EXECUTION.md ONLY (`crates/` byte-identical, proven via git diff), and `68edddab` CI/Main was SUCCESS on the identical usage tree. Same-code green→red = render nondeterminism or environment; owner = usage-lane/product session. No rerun (attempt preserved per §5), no product edit (foreign lane). `52c5236c` final: CI failure / Desktop superseded-cancelled / Renovate SUCCESS. External #1048 merged after (`5639cdc3`, launch lease port; mains in flight at record time).

## Evidence matrix (2026-09-21, parent — this session)

| Requirement (goal §15) | Verdict | Evidence |
|---|---|---|
| Failure inventory w/ disposition; main/PR blockers fixed | PASS | Jackin fd18e95c + 9ee50f6a all 3 SUCCESS; ee1da0c6 Swift flake root-caused (C4, ledger) + fixed by #1024; Velnor fff18da8 gates resolved by #999; Preview pre-existing non-blocking w/ root cause; transients preserved (attempt-1s kept) |
| Preventable post-merge classes structurally addressed; stale evidence blocked | PARTIAL | D19 two-step + candidate-path proven (#995/#997/#999); merge_group unsupported by generator, no empty-selection guard (parity-design) — open |
| Local hooks enforce staged fmt+clippy | REPORTED | Pre-compaction: #1015 merged + E2E in record; not re-verified this session |
| Separate fmt/clippy/test steps, ordered, gating | PASS (Jackin) / GAP (Velnor-self) | Jackin: 35/35 rust crate units PHASED, live green on fd18e95c; phases preserved under 44243ed4 adoption (ci-unit-rust.yml:438-501 ordered, gating identical); Velnor rust-velnor-workflow unphased via regen-gate clear_phases (code site + fix named above) |
| Shared changes integrated/published/pinned/regen/exercised | PASS | PR-A/B/3 + PR-C merged; Runtime products SUCCESS; Jackin regen on published fff18da8 pin, then external #1019 advanced to 44243ed4 (pin valid, products green, #1019 PR full-scope SUCCESS); byte-identical regen verified |
| 120s across scenarios | FAIL (honest, open) | Measured @44243ed4: warm PR 18m13s, manifest PR 24m33s, instruction PR 28m35s, main 21m39s/20m54s, desktop 31m47s (9–20× over); wall = macOS queue + swift-package-native 15.8–23m; new slices named (Swift split/cache, boltffi-gen, dedup); none recolored |
| Reuse measured/trustworthy/invalidated, no avoidable repeats | PARTIAL | S1 live-verified (-22min, 9-tool setup); affected-selection proven dead (FULL fallback); mbx 0%-effective; S2-S5 open |
| Reviews addressed, slices merged, mains verified | PASS | #995/#997/#999/#1014/#1017/#1018/#1021/#1024/#1026/#1029/#1033/#1036/#1037/#1039/#1040/#1041/#1042/#1043/#1046 merged via normal controls (all-green PR gates); #1032/#1034/#1035/#1038/#1047 probes + #1022/#1023/#1028/#1031 closed unmerged; `08713c9b` main-CI red = openrouter flake healed by successor, `87521a95` main-CI red = cache-504/tirith/flaky-socket all external (attempts preserved); `799f5774` CI/Main + Renovate SUCCESS, Desktop structurally unavailable under supersede-cancel; open #1044/#1045/#1030/#1007/#1004 untouched |
| Independent verification, no remaining actionable defect | PARTIAL | 25+ child verifications + 13 live probes this session; §14: bullets 1, 2, 3, 4, 5, 6b, 7, 8, 10a, 11 proven live, 6a/9a local legs pass, 9b/10b drills pass; full matrix not yet run |
