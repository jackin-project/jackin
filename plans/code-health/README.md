# Code-health implementation plans

Generated by the `improve` skill (deep audits) following the roadmap item [Codebase health: Rust strictness, structure, and AI reviewability](/roadmap/codebase-health-enforcement/). First wave 2026-07-09 against `a4761957d` (plans 003-009: concrete bug/perf/test instances). Second wave 2026-07-09 against `47dd5fca0` (plans 010-018: the roadmap program itself — one plan per phase cluster, selected per operator instruction to cover all phases 0-8). Third wave 2026-07-09 against `e80d5cc0a` (plans 019-023: top deferred findings promoted — no re-audit; the second-wave audit data was hours old and the ledger below was the source). Fourth wave 2026-07-09 against `b42c97d4c` (plans 024-027: next deferred tranche — clock seam, test-support crate, two highest-ranked perf fixes). Fifth wave 2026-07-09 against `c856acc9d` (plans 028-032: the remaining concrete executor-safe ledger items — dependency/docs debt sweeps, console view-models, op-probe typed error, the two missing behavioral specs). Sixth wave 2026-07-09 against `0971da66d` (plans 033-040: per operator instruction, a roadmap↔plans coverage gap analysis — the full matrix is below — then plans for every uncovered roadmap sub-item that is concrete and not decision-blocked; two open decisions were resolved by the operator the same day and are recorded under "Operator decisions"). **After wave 8 every roadmap sub-item is either planned/executed (003-056), decided, or explicitly deferred with a measured reason in the coverage matrix and [RESIDUAL_LEDGER.md](RESIDUAL_LEDGER.md) — zero open SEQ debt.** Seventh wave 2026-07-09 against `fabe88406` (plans 041-053, per operator instruction: a six-agent deep verification of this matrix against the roadmap page — every phase-cluster claim re-checked against the cited plan text and code — followed by gap closure with telemetry as the declared priority; the matrix below carries the corrections). Wave 7 planned the Phase 8 facade program (041-044), closed the verified coverage gaps (045-053), and extended plans 010/011/013/017 in place where a matrix claim over-credited them. **Wave 8 (055–056) closed residual footnotes and converted all matrix SEQ debt to DEFER + [RESIDUAL_LEDGER.md](RESIDUAL_LEDGER.md); program planning+execution on this branch is complete.**

## Reconcile log

- **2026-07-12 plan 067** — thiserror mid-tranche for `jackin-docker` (`DockerError`).
- **2026-07-12 plan 066** — thiserror mid-tranche for `jackin-isolation` (`IsolationError`).
- **2026-07-12 plan 065** — thiserror mid-tranche for `jackin-instance` (`InstanceError` + `SyncSourceValidationError`).
- **2026-07-12 plan 064** — auth_error mode/env traces + token_setup revoke/expiry take WorkspaceName; materialize_workspace measured dual-semantics DEFER (path labels).
- **2026-07-12 plan 063** — isolation list_records + drift detect take WorkspaceName.
- **2026-07-12 plan 062** — AppConfig require/edit/remove take WorkspaceName.
- **2026-07-12 plan 061** — console providers/save-diff + ConfigEditor set_workspace_*/edit/remove take WorkspaceName.
- **2026-07-12 plan 060** — env operator-resolve APIs take `Option<&WorkspaceName>`; launch pipeline + console providers call sites parse at the boundary.
- **2026-07-11 plan 059** — roles resolve APIs (`resolve_mode*`, github, sync dir, github env layers) take `Option<&WorkspaceName>`; launch/console/app call sites updated.
- **2026-07-11 residual wave R1b (plan 058)** — cognitive-complexity-threshold 60→58; env `run_doctor` takes `WorkspaceName`; `jackin-test-support` snapshot helpers.
- **2026-07-11 residual wave R1 (plan 057)** — closed R-014-materialize-bench (`materialize_accounts` criterion + path seam), R-export-volume-ratchet (`ratchet.toml` export-volume family), R-map-metadata-gate (`cargo xtask docs map-check`).
- **2026-07-11 parallel dispatch playbook** — added [DISPATCH.md](DISPATCH.md): orchestrator/worker roles, hard serialization resources, T0/T1/T2 verify tiers, residual waves R1–R4 fan-out, worker prompt template. Residual ledger and VERIFICATION point at it for non-serial residual drains.
- **2026-07-11 wave-8 residual closure at `e9c3a8260`** — plan 055 closed named residual footnotes (host turso + docs repo-link generator in tree at `e1eacdf44`; 014/023/033/038 DEFER measured). Plan 056 converted every coverage-matrix SEQ marker to DEFER + [RESIDUAL_LEDGER.md](RESIDUAL_LEDGER.md). Plan inventory second pass includes 051 with evidence columns. `cargo xtask lint --strict` green; `ci --fast` red only for documented executor-env waivers (4 docker manager_flow + RUSTSEC-2026-0204).

- **2026-07-09 executor-env finding** — `cargo xtask ci --fast` is red at base `a61d9d391` itself in executor environments; three failures proven pre-existing by a base-state re-run (plan 014's executor): `cargo audit` + `cargo deny advisories` fail on RUSTSEC-2026-0204 (crossbeam-epoch 0.9.18 via turso→jackin-usage, locked at base), and two jackin-diagnostics tests (`observability::otlp::tests::debug_kind_is_debug_severity_and_filtered_at_info`, `tests::writes_jsonl_events`) fail under the capsule-exported `JACKIN_TELEMETRY_LEVEL=info`. Reviews treat exactly these three as pre-existing waivers; the `ci gate OK` done-criterion is otherwise unattainable until a dep-bump/ignore chore lands and those two tests become env-hermetic (defect→gate ledger candidate for [017]). Plan 010's executor additionally saw three `crates/jackin/tests/manager_flow` disk-persistence tests fail (`auth_claude::auth_form_save_persists_mode_and_credential_to_disk`, `auth_github::github_auth_form_save_persists_token_mode_and_gh_token_to_disk`, `secrets::secrets_delete_key_saves_to_disk`) with zero changes under `crates/jackin` — suspected same env-sensitivity class; treat as waiver candidates when the failure list matches exactly, and fold into the same hermeticity chore. Plan 018's executor then base-verified the FULL set via git-stash cycles — 12 pre-existing workspace failures in executor environments: the 2 jackin-diagnostics env tests, 4× `jackin::manager_flow` disk-persistence (Docker daemon unavailable), 4× `jackin::profile_matrix tier1_*` (e2e-gated, demand a running Docker daemon), and 2× `jackin-docker shell_runner` debug-capture (same env-leak class). Plan 023's executor added a 5th manager_flow member (`secrets::secrets_edit_value_saves_to_disk`, identical `timed out waiting for config save worker` signature, deterministic at base). The authoritative waiver set is now 13 items; all share two root causes — capsule-exported `JACKIN_*` env and no Docker daemon — one hermeticity chore (defect→gate ledger candidate for [017]) closes the family.
- **2026-07-09 tranche-1 dispatch at `a61d9d391`** — plans 003/004/007/010/014/018 (Batch 1 subset: both spines + instance fixes + bench pair) dispatched to isolated worktree executors; statuses set IN PROGRESS. Operational note for future dispatches: harness-provisioned worktrees start from the main lineage, not this branch — every executor must run `git checkout -B exec-plan-0NN a61d9d391` (plan-base commit) as its first action; this tranche was redirected mid-flight after plan 010's drift check caught the mismatch.
- **2026-07-10 operator halt at `05dc62d63`** — operator stopped execution mid-tranche. Executor states at halt: **025 COMPLETE** (`exec-plan-025` @ `ead83e524`, executor-verified: 714/714 isolation+runtime tests, clippy/shear/arch/agents/docs gates green; deviations documented — `install_all_test_stubs` stays in runtime by consumer-count branch, dead `test-support` feature removed; REVIEW PENDING); **015 partial** (`exec-plan-015` @ `4b06680f0`, killed awaiting its CI run; push rejected — token lacks `workflow` scope for `.github/workflows/docs.yml` — branch is LOCAL-ONLY in the reviewer checkout); **040** (`exec-plan-040` @ `0f4c5c051`, two commits, clean tree, killed while checking CI output; review pending); **053** (`exec-plan-053` @ `f5015c9bc`, one commit, clean tree; the executor pushed its own branch on the operator's halt directive). All four had stalled once on the same harness failure mode — ending their turn to wait on a background verification job — and were nudged with a foreground-only rule; future dispatch prompts should include it up front. 026/027/037/048 remain with operator-run external agents (prompts delivered; no reports received). Next session: review 025/040/053 diffs, resume-or-redispatch 015, collect external-agent reports.
- **2026-07-10 executor-session reset at `05dc62d63`** — the reviewer session hosting the 015/025/053 executors was cleared; all three died mid-flight with dirty worktrees and zero commits (015: xtask docs gate edits; 025: jackin-test-support scaffold; 053: tui runtime edits). Their worktrees were removed and no-commit branches deleted; all three re-dispatched fresh from `05dc62d63` alongside newly dispatched 040. Plans 026 and 027 handed to two operator-run external agents (same executor contract, branches `exec-plan-026`/`exec-plan-027` from `05dc62d63`).
- **2026-07-09 at `0eaa86391` (post-wave-7)** — zero drift. The only non-plans changes since the wave-1 base `a4761957d` are docs/CI-config (`47dd5fca0`: `.codebook.toml`, `jackin-dev.yml`, `lychee.toml`, roadmap MDX) plus the separate `security-review/` folder; no Rust code was touched, so every plan's "Current state" excerpts still hold at HEAD. All 53 plans TODO; no stale IN PROGRESS entries; no executor worktrees. Next leverage is execution per the dispatch schedule below.

## Parallel dispatch (read first for execution speed)

**Authoritative playbook:** [DISPATCH.md](DISPATCH.md) — orchestrator vs worker roles, hard serialization resources, worker prompt template, verification tiers (T0 package / T1 lint / T2 ci-fast per wave), residual waves R1–R4, anti-stall rules.

Throughput rule: spawn every **ready, write-disjoint** plan in **one turn**; never wait for worker A before starting worker B when both are ready. Full `ci --fast` is **once per merge wave**, not per plan. Done criteria are never skipped.

## Proposed dispatch schedule (post-wave-7)

Derived from the per-wave dependency notes below; each batch is a safe-parallel set (disjoint file scopes), and any subset of a batch may be dispatched. Serialization rules that bind regardless of batch: one owner at a time for the root `Cargo.toml` lint table (011 → 019 → 034 → 047), for `ci.rs`/`ci.yml` (011 → 022 → 036), and for `hygiene.yml` appends (022/035/046/048/052 — append-only, so any order). 042 and 026 are capsule PRs (smoke block mandatory). Detail + residual fan-out → [DISPATCH.md](DISPATCH.md).

1. **Batch 1 — spine openers + instance fixes + independents**: [010] (program spine), [018] (telemetry spine — wave-7 declared priority), [003]+[014] (bench lands with the fix), [004], [007], [008], [009], [023], [024], [028], [029], [031], [049], [053].
2. **Batch 2 — after batch 1**: [011] (needs 010's baseline; owns the lint table and `ci.rs` this batch), [041]+[042] (parallel, after 018), [043] (after 018), [015], [025], [026], [027], [040] (benches exist post-014), [048].
3. **Batch 3 — after batch 2**: [012] then [016]; [019] then [021]; [034] then [047] (lint-table serialization); [022] then [036] (`ci.rs` serialization); [044] (after 041+042); [013], [020], [030], [032]+[033] (pair), [035], [037], [038], [039], [045], [046], [050], [051], [052].
4. **Last**: [017] (after 010+011; the ratchet engine absorbs every budget family the earlier plans left in-source). Former SEQ items are **DEFER** with measured reasons in [RESIDUAL_LEDGER.md](RESIDUAL_LEDGER.md) via [056](056-matrix-seq-to-defer.md) — not silent open debt.

## Execution order & status

Ordered by leverage (urgency ÷ effort, weighted by confidence) within each wave.

### First wave — instance fixes (no hard dependencies; any order)

| Plan | Title | Priority | Effort | Category | Status |
|------|-------|----------|--------|----------|--------|
| [003](003-materialize-accounts-borrow.md) | Stop deep-cloning usage views on every refresh | P1 | S | perf | DONE (in-tree on `chore/rust-code-health-roadmap`) |
| [004](004-resize-coalesce-drops-frames.md) | Stop dropping the frame queued behind a coalesced resize | P1 | S | bug | DONE (in-tree on `chore/rust-code-health-roadmap`) |
| [007](007-osc8-hyperlink-map-bound.md) | Bound OSC 8 hyperlink maps; clear on reset | P2 | S-M | bug | DONE (in-tree on `chore/rust-code-health-roadmap`) |
| [008](008-launch-finalization-cleanup-guard.md) | Tear down DinD when a post-success finalization step fails | P2 | M | bug | DONE (in-tree on `chore/rust-code-health-roadmap`) |
| [009](009-protocol-decoder-fuzz-and-truncation-tests.md) | Fuzz + truncation tests for protocol wire decoders | P2 | M | tests | DONE (in-tree on `chore/rust-code-health-roadmap`; fuzz smoke ~9.9M execs clean with `--sanitizer none`) |

### Second wave — program infrastructure (respect the dependency notes)

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [010](010-health-dashboard-and-baselines.md) | Code-health dashboard, suppression inventory, verification matrix | 0 | P1 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; `cargo xtask health` + `code-health-baseline.toml` + TESTING matrix) |
| [011](011-lint-strictness-silent-failures.md) | Silent-failure lints, rustdoc gates, doc tests in PR CI, reason-gate | 1 | P1 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; `assertions_on_result_states` closed by [054](054-assertions-on-result-states.md)) |
| [012](012-tier-graph-arch-gate.md) | Tier-graph arch gate (replaces empty forbidden-edge list) | 2 | P1 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; 27 crates in `TIERS`; DEV_CYCLE_ALLOWLIST empty post-025) |
| [013](013-test-infra-flakes-fuzz-idempotence.md) | Flake detection, timing artifacts, migration idempotence, parser fuzz | 3 | P2 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; nextest ci profile + junit + flaky-tests.toml + migration golden/idempotence + 4 fuzz targets) |
| [014](014-hot-path-bench-coverage.md) | Compile-check all benches; cover 4 unbenchmarked hot paths; measured lane | 4 | P2 | M | DONE (in-tree; materialize bench CLOSED [057](057-residual-wave-r1-bench-ratchet-map.md); launch-pipeline still R-014-launch-pipeline-bench) |
| [015](015-docs-gates-brand-specs-readme.md) | Brand-prose lint, spec↔test citations, README presence gate | 5 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; freshness-vs-diff residual stays with [050](050-readme-freshness-gate.md)) |
| [016](016-ownership-headers-blame-revs.md) | Ownership headers everywhere + headers gate + blame-ignore-revs | 6 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; headers gate + `.git-blame-ignore-revs`) |
| [017](017-unified-ratchet-engine.md) | Unified ratchet engine (`ratchet.toml`) + defect→gate ledger | 7 | P2 | L | DONE (in-tree on `chore/rust-code-health-roadmap`; engine + `lint ratchet` + DEFECT_LEDGER; legacy budget TOMLs retained as adapters) |
| [018](018-telemetry-drift-proofing.md) | One OTLP builder, semconv registry, correlatable sinks, honest failures | 8 | P2 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; shared `build_otlp_providers`, `otel_metrics`/`otel_events` registry, OTel hex JSONL ids + capsule context banner, `expected_shutdown` outcome, backend-query failure rows; 041/042/043 mint from its registry/builder) |

### Third wave — promoted deferred findings

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [019](019-slice-index-lints-pure-crates.md) | Slice/index panic-coverage lints on the 4 pure crates (wave 1) | 1 | P1 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; `#![deny]` on protocol/config/manifest/core + clippy.toml valves; ≥3 malformed decode tests) |
| [020](020-container-path-chokepoint-policy.md) | Container-path chokepoint + executable `/jackin/`-only policy | 3 | P1 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; chokepoint + gate + shrink-only allowlist) |
| [021](021-protocol-missing-docs-typed-errors.md) | `missing_docs` on jackin-protocol + typed clipboard wire error | 1/2 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; `ClipboardImageError` enum + `#![deny(missing_docs)]`) |
| [022](022-ci-lanes-powerset-canary-partitions.md) | Scoped powerset PR gate, beta clippy canary, `xtask ci --only` partitions | 1/4 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; scoped PR powerset + beta canary + `--only` partitions) |
| [023](023-docs-command-drift-gate.md) | Documented-command drift gate (docs fences ↔ clap tree) | 5 | P2 | M | DONE (in-tree; operator flags closed as DEFER in [055](055-residual-footnote-closure.md) / R-023-*) |

### Fourth wave — clock seam, test-support extraction, top perf fixes

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [024](024-clock-seam-clipboard-expiry.md) | `Clock` seam in jackin-core; first consumer: clipboard expiry | 3/6 | P1 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; capsule-touching — smoke block mandatory at PR time) |
| [025](025-test-support-crate-break-dev-cycle.md) | Extract `jackin-test-support`; break isolation⇄runtime dev cycle | 2/3 | P2 | M | DONE (cherry-picked `ead83e524` onto `chore/rust-code-health-roadmap`) |
| [026](026-scrollback-range-snapshot.md) | Range-scoped scrollback snapshots (per-mouse-event full-scrollback alloc) | 2/4 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; borrowed zero-copy row accessor residual) |
| [027](027-diagnostics-jsonl-typed-streaming.md) | Typed borrowed JSONL streaming; stop double-parsing detail | 4 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`) |

### Fifth wave — remaining concrete ledger items

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [028](028-dependency-hygiene-sweep.md) | Dependency hygiene: turso store boundary, ring exception, pin rationale | 1 | P3 | M | DONE (in-tree; host turso residual **CLOSED** by [055](055-residual-footnote-closure.md) via `store_backend`) |
| [029](029-docs-drift-reconciliation.md) | Docs drift: README links, Apple status, reserved envs, capsule README, codebase map | 5 | P3 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; reserved list is 19 names — plan's 20 overcounted `JACKIN_OPEN_LINKS`, which is deliberately settable) |
| [030](030-console-view-model-structs.md) | Console editor/settings view-model structs (kills the 42-site type_complexity cluster) | 2 | P2 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; `form_model` FieldRow/FormSection; editor/settings type_complexity allows removed; residue state.rs + auth handler) |
| [031](031-op-probe-typed-error.md) | Typed `op` probe errors (`OpProbeError` in jackin-core; downcast classification) | 2 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; enum in jackin-core; env constructs typed sources; oppicker downcasts first, substring fallback retained) |
| [032](032-daemon-console-behavioral-specs.md) | Behavioral specs: capsule daemon + operator console (cite-or-MISSING) | 3 | P2 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; capsule-daemon + operator-console specs) |

### Sixth wave — roadmap-coverage gap closure

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [033](033-characterization-launch-displace-pty.md) | Characterization: launch-core teardown, client displace seams, PTY fault recovery | 2/3 | P1 | M-L | DONE (suites B+C in-tree; suite A → [055](055-residual-footnote-closure.md) / R-033-suite-a DEFER) |
| [034](034-numeric-and-easy-lint-families.md) | Numeric + easy-to-avoid lint families (census: near-zero candidates; sign-loss fix wave) | 1 | P1 | S-M | DONE (in-tree on `chore/rust-code-health-roadmap`; truncation/precision/wrap remain allow) |
| [035](035-advisory-verification-lanes.md) | Scheduled advisory lanes: llvm-cov, Miri, ASan fuzz, cargo-mutants, hakari timing | 1 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; hygiene coverage/miri/ASan/mutants/hakari jobs + mise pins) |
| [036](036-process-boundary-xtask-cmd-and-timeout.md) | Process boundary: one xtask cmd module; `RunOptions.timeout` honored by ShellRunner | 2 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`) |
| [037](037-thiserror-foundational-core-env.md) | thiserror for jackin-core concrete errors + jackin-env resolution taxonomy | 2 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; ParseProfileError/EnvCycleError/PathsError + OperatorEnvError/ResolveEnvError; port traits keep anyhow) |
| [038](038-workspace-name-newtype.md) | `WorkspaceName` newtype at config/instance/launch boundaries | 2 | P2 | M-L | DONE (in-tree; frontier ~62 env/console tail → [055](055-residual-footnote-closure.md) / R-038-env-console-tail) |
| [039](039-jackin-env-pub-surface-pilot.md) | Pub-surface pilot: jackin-env sealed behind curated root re-exports | 2 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; modules sealed; host_claude+env_layer internal; token_setup facade trimmed; thin resolve wrappers kept root-public after dead_code) |
| [040](040-grid-resize-in-place.md) | In-place grid resize; same-size/height-only fast paths + resize_storm bench | 4 | P2 | M | DONE (cherry-picked onto `chore/rust-code-health-roadmap`) |

### Seventh wave — telemetry facade program + verified-gap closure

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [041](041-telemetry-operation-facade.md) | Typed operation facade; collapse duplicate `debug_log!`; adopt at ShellRunner | 8 | P1 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; `operation_*` + ShellRunner; `debug_log!` collapsed to core port) |
| [042](042-high-frequency-metrics.md) | High-frequency internals become metrics (9 instruments; firehose rows demoted) | 8 | P1 | M-L | DONE (in-tree on `chore/rust-code-health-roadmap`; 9 instruments; send/render demoted; residuals db-statement + docker-inspect) |
| [043](043-per-sink-filters-jackin-debug-retirement.md) | Per-sink telemetry filters; retire `JACKIN_DEBUG` to one alias shim | 8 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; per-sink levels + JACKIN_DEBUG alias shim; dual-inject temporary) |
| [044](044-telemetry-conformance-lane.md) | Telemetry conformance suite (dossier acceptance checks as a permanent gate) + build-trace link | 8 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; 7 acceptance tests + derived-image link) |
| [045](045-protocol-env-corpus-closure.md) | Corpus closure: protocol goldens + capability-skew, term fuzz seeds, env fuzz, unknown-field assert | 3 | P2 | M | DONE (in-tree; residual: Hello short-payload soft-default skew still hard-errors — fail-closed pinned) |
| [046](046-dind-chaos-lane.md) | Scheduled dind-E2E chaos variant (seeded faults; survival invariants) | 3 | P2 | M | DONE (in-tree; local Docker unavailable — compile-check only; hygiene dind-chaos job added) |
| [047](047-maintainability-lint-census.md) | Census the 7 allowed maintainability lints; deny quiet ones, document noisy ones | 1 | P2 | S-M | DONE (in-tree on `chore/rust-code-health-roadmap`; 2026-07: needless_pass_by_value=28 allow; other 6 promoted warn @0 residual) |
| [048](048-advisory-lanes-wave-2.md) | Advisory lanes wave 2: hyperfine cold-start, rust-analyzer clean, build-time measurement | 4/6 | P3 | S-M | DONE (in-tree on `chore/rust-code-health-roadmap`; cold-start-bench / rust-analyzer-clean / build-time-measure) |
| [049](049-crate-readme-fumadocs-pipeline.md) | Crate-README → Fumadocs generated section; slim PROJECT_STRUCTURE.md | 5 | P2 | M | DONE (in-tree; repo-link generator residual **CLOSED** by [055](055-residual-footnote-closure.md)) |
| [050](050-readme-freshness-gate.md) | README-freshness gate (structural src change ⇒ README touched same PR) | 5 | P2 | S-M | DONE (in-tree on `chore/rust-code-health-roadmap`; advisory CI first) |
| [051](051-xtask-gate-reporter-core.md) | Machine-readable gate output core (human\|json\|github reporter; 2 exemplar gates) | 6 | P2 | S-M | DONE (in-tree on `chore/rust-code-health-roadmap`; remaining gates = mechanical rollout) |
| [052](052-dylint-scaffold-render-purity.md) | dylint scaffold: `crates/jackin-lints` + render-thread-purity lint, advisory nightly lane | 1 | P3 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; workspace-excluded lints crate + render_thread_purity + dylint-advisory) |
| [053](053-tui-half-layer-spike.md) | TUI half-layer spike: prototype shared View dispatcher, finish-or-drop verdict | 2 | P3 | S-M | DONE (cherry-picked `f5015c9bc` onto `chore/rust-code-health-roadmap`) |

### Residual after plan 011 + wave-8 residual program

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [054](054-assertions-on-result-states.md) | Adopt `assertions_on_result_states` after mass test conversion | 1 | P2 | M | DONE (in-tree on `chore/rust-code-health-roadmap`; ~200 test sites → unwrap/expect_err; lint denied) |
| [055](055-residual-footnote-closure.md) | Close named residual footnotes (014/023/028/033/038/049) | 0–8 | P1 | M | DONE (in-tree; 028/049 CLOSED; 014/023/033/038 DEFER in RESIDUAL_LEDGER) |
| [056](056-matrix-seq-to-defer.md) | Convert coverage-matrix SEQ debt to DEFER + residual ledger | 0–8 | P1 | S-M | DONE (in-tree; zero open SEQ; RESIDUAL_LEDGER authoritative) |
| [057](057-residual-wave-r1-bench-ratchet-map.md) | Residual R1: materialize bench + export-volume ratchet + map-check | 4/5/8 | P1 | M | DONE (in-tree; R-014-materialize / R-export-volume / R-map-metadata CLOSED) |
| [058](058-residual-complexity-env-snapshot.md) | Residual R1: complexity floor + env doctor WorkspaceName + snapshot helpers | 1/2/3 | P2 | S-M | DONE (cognitive 58; run_doctor typed; test-support snapshot) |
| [059](059-workspace-name-roles-resolve.md) | WorkspaceName on config roles resolve APIs | 2 | P2 | M | DONE (Option&WorkspaceName on resolve_mode/github/sync/env layers) |
| [060](060-workspace-name-env-resolve.md) | WorkspaceName on env operator-resolve APIs | 2 | P2 | M | DONE (Option&WorkspaceName on lookup/resolve/has/collect/print) |
| [061](061-workspace-name-console-editor.md) | WorkspaceName on console save/launch + ConfigEditor writers | 2 | P2 | M | DONE (services + set_workspace_*/edit/remove) |
| [062](062-workspace-name-appconfig-crud.md) | WorkspaceName on AppConfig require/edit/remove | 2 | P2 | S-M | DONE (typed AppConfig CRUD + CLI parse at boundary) |
| [063](063-workspace-name-isolation-drift.md) | WorkspaceName on isolation list + drift detect | 2 | P2 | S | DONE (list_records + detect_workspace_edit_drift) |
| [064](064-workspace-name-auth-error-token.md) | WorkspaceName on auth_error traces + token revoke/expiry | 2 | P2 | S-M | DONE (materialize path-label dual-semantics DEFER measured) |
| [065](065-thiserror-jackin-instance.md) | thiserror mid-tranche for jackin-instance | 2 | P2 | S | DONE (`InstanceError` + `SyncSourceValidationError`) |
| [066](066-thiserror-jackin-isolation.md) | thiserror mid-tranche for jackin-isolation | 2 | P2 | S | DONE (`IsolationError`) |
| [067](067-thiserror-jackin-docker.md) | thiserror mid-tranche for jackin-docker | 2 | P2 | S-M | DONE (`DockerError`) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Dependency notes (second wave)

- **010 before 011**: the suppression reason-gate's budget file is generated from 010's scanner/baseline, not hand-counted.
- **012 before 016**: the headers gate cross-checks each `lib.rs` tier against 012's `TIERS` table in arch.rs.
- **010 + 011 before 017**: the ratchet engine ports 011's suppression budget and reuses 010's metric functions as providers.
- **014 alongside 003**: land the `materialize_accounts` bench before/with plan 003 so the optimization's win is measured.
- 013, 015, 018 are independent and can run in parallel with anything.

## Dependency notes (third wave)

- **019 after 011** (soft): both edit the lint config area; land 011 first to avoid conflicts. 019's valves are independent of 011's lints.
- **021 after 019** (soft): both touch `jackin-protocol/src/attach.rs` and `lib.rs`; sequence to avoid conflicts.
- **020 Step 5**: if plan 017's `ratchet.toml` engine has landed, the literal gate is a ratchet family, not a standalone gate — the plan branches on that.
- **022 conflicts with 011** on `ci.rs`/`ci.yml` — coordinate ordering; either first is fine.
- 023 is fully independent.

## Dependency notes (fourth wave)

- **026 and 027 after 014** (soft): 014's benches are their before/after instruments; each plan creates its bench per 014's spec if 014 hasn't landed.
- **025 interacts with 012**: post-012 it deletes the isolation⇄runtime `DEV_CYCLE_ALLOWLIST` entry and adds a `TIERS` row; pre-012 it just breaks the cycle.
- **024 is independent**; its `ManualClock` should later be re-exported from the 025 crate (noted in both plans' maintenance sections).
- **026 conflicts with nothing in flight** but is a capsule PR (smoke block mandatory); **027 shares jackin-diagnostics with 018** (disjoint files: summary.rs vs run.rs/observability.rs).

## Dependency notes (fifth wave)

- **030 after 011** (soft): 030 burns the biggest bare-allow cluster; regenerate 011's `suppression-budget.toml` after it lands (the ratchet will demand the shrink).
- **032 after 015** (soft): uses 015's Tests-citation format either way; the `docs specs` gate validates it only once 015 lands. 032's MISSING worklists feed the Phase 2 daemon-decomposition characterization queue.
- **031 is independent**; establishes the typed-error-at-boundary pattern for the ~40 remaining substring-classification sites.
- **028/029 are independent chores**; 029 touches `codebase-map.mdx`, which the deferred README→Fumadocs pipeline later supersedes.

## Dependency notes (sixth wave)

- **033 pairs with 008 and 032**: if 008 landed, characterize its landed finalization behavior; 033's untestable-seam notes (select-loop drain, initial-burst order) feed 032's MISSING worklists.
- **034 soft-conflicts with 011/019** on the root `Cargo.toml` lint table — any order, append entries matching the landed state.
- **035 appends jobs to `hygiene.yml`** which plan 022's beta canary also edits — append, don't restructure; either order.
- **036 re-routes `ci.rs` helper bodies** that 011/022 also touch — mechanical rebase whichever lands second. 036 must not touch `op_cli.rs` (031's).
- **037 coordinates with 031**: 031 owns `op_cli.rs` + creates `op_probe_error.rs`; 037 matches its idiom and never edits that file. 037 before or after 039 both work (039 is visibility-only).
- **038 is independent**; its launch_slot step may be eased by 033's characterization landing first (soft).
- **040 after 014** (soft): 014's `resize_storm` bench is the instrument; 040 creates it to 014's spec if absent and reports 014's scrollback-fixture correction.

## Dependency notes (seventh wave)

- **Telemetry order: 018 → 041 → 042 → 043 → 044.** 041/042 mint names from 018's registry and its single builder; 043 attaches per-sink filters to that builder; 044 asserts 041+042's outputs (hard dep on both) and migrates its volume budgets into 017's engine when that lands. 041 and 042 are parallelizable after 018; 043 is independent of 041/042.
- **042 is a capsule PR** (smoke block mandatory) and must keep the `render_allocation` dhat budgets green — no per-frame allocation from instrument recording.
- **045 coordinates with 009/013**: extends whichever landed, never duplicates; its unknown-field assert merges into 013's fixture loop if 013 lands first.
- **046/048/052 append jobs to `hygiene.yml`** which 022/035 also edit — append beside, never restructure; any order.
- **047 soft-conflicts with 011/019/034** on the root Cargo.toml lint table (append entries matching landed state).
- **050/051 touch jackin-xtask alongside 010/011/017/036** — reporter (051) sets the violation/JSON schema; 010's health `--format json` and 051 must converge on ONE schema (whichever lands second adopts the first's).
- **049 is independent**; 029's capsule-README fix improves its output but is not a blocker. **053 is independent**; rebase around 030 if simultaneous.

## Operator decisions (2026-07-09)

- **`#[non_exhaustive]` sweep — DECIDED: keep exhaustive matching.** The roadmap Phase 2 item 3 premise is revised: workspace-internal compile-time exhaustiveness stays the drift-guard (`Provider` doc, `protocol/lib.rs:210-215`); `non_exhaustive` is scoped to genuinely external surfaces only — currently none exist. Per-type judgment calls (e.g. new error enums like `SelectorError`) remain allowed; no sweep will be planned. The roadmap text edit rides the next plan touching that page (034 Step 5 or 011).
- **`cargo-public-api`/`cargo-semver-checks` — DECIDED: dossier Skip wins** (`rust-ci-tooling.mdx:148`: no published library crate → nothing meaningful to diff). No public-API snapshot tooling will be built; roadmap line 60 gets the Skip note via plan 011 (plan 035 explicitly excludes the report from the advisory lanes). Internal API discipline = tier gate (012) + pub-surface ratchet (010) + curated re-exports (039).

## Roadmap coverage matrix (audited 2026-07-09 at `0971da66d`; re-verified by six agents + corrected at `fabe88406`, wave 7)

Per operator instruction, every numbered sub-item of the roadmap page mapped to its plan or its recorded disposition. Legend: **[NNN]** = planned/executed; **DECIDED** = operator decision above; **DEFER(reason)** = deliberately not executed this program (measured reason; see [RESIDUAL_LEDGER.md](RESIDUAL_LEDGER.md)); **SHIPPED** = verified already built. Former **SEQ** debt was converted by [056](056-matrix-seq-to-defer.md) — zero open SEQ markers remain.

- **Phase 0**: 1 suppression inventory [010] · 2 crate-graph vs map [010 marks open]+[029 one-time] · 3 dashboard [010 incl. duplicate-helpers + advisory sections] · 4 TESTING matrix [010] · 5 budget baselines [010; suite-wall-time seeded via 013's junit artifacts].
- **Phase 1 PR gates**: powerset [022] · doc tests [011] · snapshot hygiene/pending-snap [011].
- **Phase 1 advisory**: coverage [035] · Miri [035] · sanitizers [035] · mutants [035] · public-API DECIDED-Skip · hakari [035].
- **Phase 1 ratchets/suppressions**: allow→expect conversion — [030] burns the console cluster; remainder DEFER(L; console 51% first, then flip `allow_attributes*` to deny) · reason-gate [011] · revisit allowed lints one-at-a-time — `float_cmp` [034]; `map_err_ignore` documented-allow (vet verdict below); the 7 maintainability lints [047 census + promote-or-document] · complexity-threshold ratchets DEFER(RESIDUAL_LEDGER R-complexity-threshold; baseline floors measured, lowering is shrink-only chore) · per-lint `#[expect]` suppression ratchet [011, extended wave 7 with the `[[expect]]` per-lint table].
- **Phase 1 lint families**: slice/index [019 wave 1; wave 2+ DEFER until wave-1 measured] · silent-failure [011] · async deadlock pair [011] · numeric + easy-to-avoid [034; truncation/precision/wrap tier DEFER(optional per roadmap line 78)].
- **Phase 1 meta-suppression**: `allow_attributes`/`allow_attributes_without_reason` deny — DEFER(RESIDUAL_LEDGER R-allow-attributes-deny; ratchet caps bare-allow debt [011]).
- **Phase 1 rustdoc/missing_docs**: rustdoc table + `missing_debug_implementations` + doc tests [011] · `missing_docs` [021 protocol first; manifest→env→term→config→core DEFER(one crate per PR behind 021's pattern)].
- **Phase 1 dylint**: [052] (scaffold + render-thread-purity lint + advisory nightly lane; the "second candidate" condition was judged soft — three follow-up candidates are recorded in 052's maintenance note).
- **Phase 1 stricter-er track**: allowlist pilot DEFER(run AFTER 034+019 land so the pilot measures only the residual restriction set on one pure crate).
- **Phase 1 canaries**: beta clippy [022] · timing artifacts SHIPPED+[013].
- **Phase 2 runtime/launch**: runtime split + launch phase-contract extraction + image/build context structs + typestate — DEFER(RESIDUAL_LEDGER R-launch-typestate; [033] B+C shipped, full extract multi-PR) · test fakes out of runtime [025].
- **Phase 2 daemon**: decomposition DEFER(RESIDUAL_LEDGER R-daemon-decomp; [032] specs + [033] partial char) · scrollback range API [026] · characterization [033 covers 3/7: displace, input routing, PTY recovery; session-lifecycle, status-publication, persistence/reattach, cleanup-outcomes DEFER(RESIDUAL_LEDGER R-daemon-char-remainder; 032 MISSING worklists)].
- **Phase 2 shared contracts**: process boundary [036 bounded; sync/async engine unification DEFER(design: PID-1 sync vs tokio async)] · pub-mod narrowing [039 pilot; core rollout = decision after pilot metrics] · arch tests strict [012].
- **Phase 2 API/dep gates**: sealed traits DEFER(port-marker taxonomy first — 13/15 unsealed traits are intentional ports; no signal for a lint yet) · public-API snapshot DECIDED-Skip · barrel discipline [039] · tier graph [012] · pub-surface report [010].
- **Phase 2 type-system**: newtypes [038 WorkspaceName; SessionId DEFER(u64 wire form + String record form need a design decision — census in 038); TokenCount DEFER(protocol `control.rs:130-131` u64 fields are wire-format — same protect-the-wire rule as 038's persisted-serde fields; the jackin-usage in-memory totals are a later candidate behind 038's pattern); RoleRef/ContainerId/MountPath later candidates behind 038] · error taxonomy [037 core+env-resolve; config/isolation/docker/image/instance tranche DEFER(RESIDUAL_LEDGER R-thiserror-mid-tranches; measured config~66/isolation~14/docker~17/image~23/instance~7); runtime/console/capsule long tail DEFER] · non_exhaustive DECIDED · Result-first ctors [019 + the already-shipped unwrap/expect/panic denials — wave-7 verify: 034 was mis-cited here] · AFIT Send story DEFER(M design decision; bundle with any port-trait signature change) · typestate DEFER(RESIDUAL_LEDGER R-typestate-general).
- **Phase 2 TUI/console**: half-layer [053 spike — binding finish-or-drop criteria; the roadmap already states the "finish" preference, so the decision needed evidence, not an open question] · settings/editor unification item — view-models [030]; full edit-model convergence DEFER(RESIDUAL_LEDGER R-edit-model-convergence) · oppicker continuation DEFER(no measured residue; re-audit after 030/031).
- **Phase 3**: test-support crate [025] · daemon/console specs [032] · property tests — env pure slice [045]; broader proptest adoption DEFER(rides 013's generators; keep with sim below) · coverage-map [010 report] · mega-test breakup = principle (rides decompositions) · snapshot standardization — shared helpers DEFER(RESIDUAL_LEDGER R-snapshot-helpers; [025] shipped) · pending-snap gate [011 Step 5] · sim/turmoil + proptest-state-machine DEFER(RESIDUAL_LEDGER R-sim-turmoil; needs daemon ports) · failpoints — PTY edge delivered by [033]'s FaultMasterPty, Docker edge by existing FakeDockerClient.fail_with; fs/process fail-crate residual DEFER(S-M) · chaos lane [046 — wave-7 verify: the old 024/daemon-port blockers do not apply to external fault injection] · loom/kani DECIDED-out-of-scope (roadmap's own scoping; legend corrected from SHIPPED).
- **Phase 3 policy/corpora**: `/jackin/` policy [020] · schema five-artifact SHIPPED+[013 idempotence]+[045 unknown-field replay] · spec↔test linkage [015]/[032] · flake quarantine + wall-time artifacts [013, wall-time baseline seeding added wave 7] · protocol skew/truncation unit tests [009]; golden frame corpus + capability-negotiation test [045 — wave-7 verify: 009 explicitly de-scopes these] · migration corpus SHIPPED+[013] · fuzz-corpus management [013 new-target seeds]+[045 term backfill + env seeds; crash-promote rule in both].
- **Phase 4**: measured perf reporting [014; launch-pipeline bench DEFER R-014-launch-pipeline-bench] · iai-callgrind DEFER(RESIDUAL_LEDGER R-iai-callgrind) · perf budgets DEFER(RESIDUAL_LEDGER R-perf-budgets; 017 reserves family only) · dhat literals→ratchet DEFER(R-dhat-budgets-ratchet) · materialize borrow [003] · resize fast paths [040] · parallel mount materialization DEFER(design-gated by roadmap itself: partition by dependency group + host repo first) · diagnostics streaming [027] · CI partitions [022] · dhat budgets DEFER(RESIDUAL_LEDGER R-dhat-budgets-ratchet) · hyperfine cold-start [048 — wave-7 verify: the iai coupling was spurious]; input-to-frame latency harness DEFER(needs a headless TUI driver; candidate after 025 matures).
- **Phase 5**: map — one-time reconcile [029] ([010] only marks it open — wave-7 verify: the old row over-credited 010); recurring map↔`cargo metadata` audit gate [057 `docs map-check`] · roadmap-leaf hygiene = editorial · specs location SHIPPED · no-internals-in-user-docs = editorial gate · per-crate authority SHIPPED · README→Fumadocs pipeline + PROJECT_STRUCTURE slim [049 — wave-7 verify: M not L; the build-hook precedent already ships in `prerender-static.ts`] · README freshness-vs-diff [050 — wave-7 verify: needs only the git diff, not the pipeline; presence gate stays in 015] · brand gate [015] · spec↔test gate [015] · command drift [023; config-key half DEFER(needs a schema key artifact + docs-table parser — keys live in `|`-tables, not code fences, so 023's fence-walker is inapplicable; the old reason was wrong)].
- **Phase 6**: ownership headers [016] · narrowest-verification map [010, wave-7 extension: `verification_map` json key generated from `cargo metadata` + completeness test] · build-time budget — measurement lane [048; existing `--timings` artifacts are HTML-only]; budget half DEFER(RESIDUAL_LEDGER R-build-time-budget) · type-system-decides = umbrella (038/037/012) · determinism [024]+principle · machine-readable gates — shared reporter core + 2 exemplar gates [051]; remaining per-gate rollout = mechanical follow-ups after 051 (old L-DEFER retired) · context-economy budgets [016]+[017, wave-7 extension: the `agent-doc-bytes` family now covers all three roadmap-named surfaces — AGENTS, crate READMEs, `lib.rs` headers] · blame-ignore-revs [016] · rust-analyzer lane [048].
- **Phase 7**: defect→gate ledger [017] · ratchet engine [017] · self-tightening lane DEFER(RESIDUAL_LEDGER R-self-tightening; engine shipped, bot not) · golden agent tasks DEFER(L; a no-spend design spike — task fixtures + scoring schema + mock-agent validation — is writable anytime, but held pending operator spend framing; the live lane needs budget sign-off) · health-history JSONL DEFER(RESIDUAL_LEDGER R-health-history-jsonl; json schema from 010) · agent-operated hygiene DEFER(RESIDUAL_LEDGER R-agent-hygiene; [051]+[017] prerequisites).
- **Phase 8** (wave-7 deep census corrected the counts: clog-family 289 sites — clog 170 / cdebug 113 / ctrace_payload 6 — not 312; `debug_log!` 283 sites across 53 files with the two duplicate defs confirmed at `core/debug_log.rs:65` + `diagnostics/lib.rs:70`; `JACKIN_DEBUG` readers 19 production files, not 21; the in-memory rig is `observability.rs:911-952`, not :943; and the level/categories/`JACKIN_OTEL_INTERNAL` env model is already SHIPPED — only per-sink filters and the `JACKIN_DEBUG` retirement remain of item 2): facade + macro unification [041 API + ShellRunner choke point + `debug_log!` collapse; adoption waves HTTP→Docker→launch-timing→capsule-attach follow per 041's maintenance note] · per-sink filters + `JACKIN_DEBUG` retirement [043] · correlation/one-builder [018] · span links [018 real-ids half; build-subtrace link [044]; remaining subsystems ride 041's waves] · per-crate/step/screen dims SHIPPED (`EXPORT_TARGETS`, taxonomy, `screen.rs` — the old per-screen DEFER was stale); per-feature decision events DEFER(rides 041 maturity + a toggle census; disposition recorded in 044) · metric set [042; db-statement + docker-inspect residuals recorded there] · error semantics SHIPPED+[018 residuals] · semconv registry [018]; conformance lane [044]; export-volume ratchet [044 constants + 057 `export-volume` ratchet family] · run-id contract SHIPPED+[018].

## How this maps to the roadmap

These are the concrete, vetted routine instances the roadmap program asks for — the evidence that prioritizes which lints/gates to adopt next:

- 004 / 007 / 008 are the panic/silent-failure/resource-leak defect classes Phase 1's restriction-lint families (`let_underscore_*`, indexing/slice, async) and Phase 7's defect→gate ledger are meant to catch.
- 009 is the Phase 3 “property/fuzz for protocol decoding” item, scoped to the one crate with zero fuzz coverage today.
- 003 is the Phase 4 “borrowed records / avoid full clones” performance item.

## Findings considered and deferred (not yet planned — recorded so they aren't re-audited)

High-value findings that are real but were not turned into first-wave plans (larger blast radius, a design decision, or lower leverage).

### Correctness / bugs

- **BUG-color-hex-slice** — `host_colors.rs:161-166` (duplicated in `jackin-tui` and `jackin-runtime`) slices a `#RRGGBB` OSC reply by byte under a byte-length guard; a 6-byte multibyte string panics. Guard with `is_ascii()` or parse from `as_bytes()`; dedupe the two copies. S. Low reachability but a concrete `string_slice` instance.
- **BUG-pty-unbounded-channel** — capsule `event_tx/event_rx` (`daemon.rs:202-203`) is unbounded; a runaway agent (`yes`) has no PTY backpressure and can inflate capsule RSS. Investigate depth under a flood soak before switching to a bounded/coalescing channel. LOW-MED, investigate-first.

### Performance

- ~~PERF-scrollback-snapshot~~ → **shipped as [026](026-scrollback-range-snapshot.md)** (range-scoped API; the borrowed zero-copy row accessor remains a recorded follow-up).
- ~~PERF-resize-clone~~ → **planned as [040](040-grid-resize-in-place.md)** (in-place `RowStore::resize`, same-size/height-only fast paths, equivalence oracle, `resize_storm` bench per 014's spec with its scrollback-fixture note corrected).
- ~~PERF-diag-double-parse~~ → **shipped as [027](027-diagnostics-jsonl-typed-streaming.md)** (typed borrowed record + kind-gated detail parse; reused line buffer only if measured).
- **PERF-benches-missing** — 014 delivered in-tree (2026-07-11): `resize_storm` + `summarize_jsonl` benches, `--benches --workspace` compile gate, advisory bench-run lane; scrollback-snapshot with [026](026-scrollback-range-snapshot.md). **`materialize_accounts` bench CLOSED** by [057](057-residual-wave-r1-bench-ratchet-map.md). Remaining: R-014-launch-pipeline-bench **DEFER** (full LaunchCore harness).
- Low-leverage (recorded, likely not worth doing soon): usage-monitor whole-file re-read per poll (documented tradeoff), usage upsert without a cached prepared statement (verify turso caching first), `preserve_visible_rows_to_scrollback` double-clone.

### Tests

- ~~TEST-clock~~ → **shipped as [024](024-clock-seam-clipboard-expiry.md)** (seam + clipboard consumer in-tree; remaining raw `now()` sites convert one consumer per PR; next: git/PR-watch throttle).
- ~~TEST-launch-core~~ → **planned as [033](033-characterization-launch-displace-pty.md)** suite A (failure-path-first: grant-failure teardown ordering + mid-pipeline FailedSetup; `run_launch_core` is `pub(super)` so tests live in `launch_pipeline/tests.rs`).
- ~~TEST-displace-policy~~ → **planned as [033](033-characterization-launch-displace-pty.md)** suite B (the reachable seams: detach helper, ClientWriter latch, post-takeover input routing; the select-loop drain + initial-burst order stay MISSING → 032's worklist; the proptest-state-machine remains the deferred sim-track home).
- ~~TEST-pty-recovery~~ → **planned as [033](033-characterization-launch-displace-pty.md)** suite C (new `FaultMasterPty` scriptable double; census confirmed `NullMasterPty` cannot fail, so all four recovery branches are currently unreachable).
- ~~TEST-support-crate~~ → **planned as [025](025-test-support-crate-break-dev-cycle.md)** (moves the runtime test_support surface, breaks the dev cycle; the remaining duplicate fakes — jackin/tests/common.rs FakeRunner, host/caffeinate pair, console StubRunners — stay a recorded dedupe follow-up).
- **TEST-clipboard** — expiry half **shipped in [024](024-clock-seam-clipboard-expiry.md)**; image-chunk-assembly tests remain deferred (S). **TEST-git-watch** (S-M) — git/PR-watch throttle; next Clock-seam consumer after 024.

### Tech debt / architecture

- ~~DEBT-op-typed-error~~ → **shipped as [031](031-op-probe-typed-error.md)** (in-tree). Ownership: `OpProbeError` lives in `jackin-core` (shared ancestor of jackin-env + jackin-console-oppicker; beside the port vocabulary). Residue: ~40 other `.contains()` classification sites in different subsystems.
- **DEBT-anyhow-in-libs** — first tranche **planned as [037](037-thiserror-foundational-core-env.md)** (core's four concrete surfaces + env's resolution taxonomy; port traits stay anyhow by recorded decision — seams whose errors are reported, not handled). Remaining tranches deferred: config 14 files, isolation 5, docker 3, image 7, instance 3, then the runtime 39 / console 35 / capsule 22 long tail. L total, phased behind 037's pattern.
- ~~DEBT-devdep-cycle~~ → **planned as [025](025-test-support-crate-break-dev-cycle.md)** (isolation⇄runtime cycle). The `jackin-term` dev-dep on `jackin-diagnostics` is upward-but-acyclic and allowed under the tier gate's dev-edge rule (plan 012); no action.
- ~~DEBT-type-complexity~~ → **planned as [030](030-console-view-model-structs.md)** (verified current: frame.rs 19, settings/view.rs 14, four tabs 2 each; ~13 sites outside console stay as recorded residue).
- **DEBT-pub-surface** — pilot **planned as [039](039-jackin-env-pub-surface-pilot.md)** (jackin-env: census mapped every external use-site; whole `host_claude` module + 14 other items are external-dead; `unreachable_pub = deny` makes the sweep compiler-verified). jackin-core rollout (39 pub mods, 312 items) is a decision gated on the pilot's Step 6 metrics. jackin-manifest/protocol keep leaky duals meanwhile.
- ~~DEBT-tui-half-layer~~ → **planned as [053](053-tui-half-layer-spike.md)** (bounded spike: convert the console loop, measure glue cost, binding finish-or-drop criteria; snapshots are the oracle).
- **DEBT-newtypes** — `WorkspaceName` **planned as [038](038-workspace-name-newtype.md)** (fresh census: 117 sites, not 58; one validator at config load only; persisted-serde fields explicitly stay `String` to protect on-disk manifests). **SessionId stays deferred pending design**: the census split it into a `u64` mux/wire id (protocol frames — wire compat) and a `String` `SessionRecord.session_id` with **no in-Rust mint site** (populated from manifest JSON written outside Rust) — neither is a mechanical newtype. `RoleRef`/`ContainerId`/`MountPath` are later candidates behind 038's pattern.

### Dependencies

- ~~DEP-turso-wrap, DEP-ring-license, DEP-pin-rationale~~ → **shipped as [028](028-dependency-hygiene-sweep.md)** (in-tree). Turso chokepoint is `jackin-usage::store_backend` (host CLI via [055](055-residual-footnote-closure.md)); ring@0.17.14 exception deleted (unencountered); five runtime `=` pins annotated with archaeology-derived rationale. Pin relaxations recorded as comments only. Host turso residual **CLOSED**.

### DX / tooling (agent-contributor leverage)

- **DX-arch-gate-noop** — `cargo xtask lint arch` has an empty `FORBIDDEN_EDGES` list (`arch.rs:42`), so `--strict` passes while checking zero edges; the umbrella `lint --strict` advertises architecture enforcement it isn't performing. Also `arch.rs:135` misspells the doc as `codebases-health-enforcement`. Populate the gate (start with the DEBT-devdep-cycle edge or a tier graph) or stop over-promising. S-M.
- **DX-doctests-pr** — `cargo test --doc` runs only in scheduled hygiene, not PR CI or `cargo xtask ci`; a broken doctest merges green and reddens `main` up to 24h later, violating the repo's own PR/main-parity hard rule. Add to PR CI + `ci.rs`. S.
- **DX-ci-parity** — `cargo xtask ci` (billed as merge-readiness) omits fuzz, bench-build, `check-agent-status-truthful.sh`, and the docs gate trio, yet runs feature-powerset (a scheduled-only lane). Make it a faithful union of PR gates; demote powerset behind a flag. M.
- ~~DX-gate-json~~ → core **planned as [051](051-xtask-gate-reporter-core.md)** (shared human|json|github reporter, file-size + agents gates converted, live-annotation proof; remaining gates = mechanical rollout). Baseline was 0/8.
- **DX-gate-messages** — bring the six weaker xtask gates up to the `lint tests` bar (each failure states the exact rerun command + fix); `lint tests` is the template. S.
- **DX-fast-tier** — `cargo xtask ci --fast` only drops feature-powerset (still full all-features clippy + nextest + audit + deny + an MSRV toolchain fetch); no lightweight “re-check what I touched” tier. Add `--quick`. S-M.
- **DX-redundant-jobs** — `schema-check` and `file-size-gate` are separate CI jobs each recompiling `jackin-xtask` into its own rust-cache namespace for pure file inspection; collapse into one `xtask-gates` job. S. Cache-quota pressure.
- **DX-onboarding** — no `rust-analyzer.toml` / `.env.example` / TESTING verification matrix; sccache is a pinned tool wired only in CI with no fast-local-loop doc (mold/sccache/`-p` targeting). M. Workspace is RA-friendly — no proc-macro crates — so a 5-line RA config is cheap.

### Docs (concrete drift — sizes the planned freshness/drift gates)

- ~~DOCS-codebase-map, DOCS-capsule-readme, DOCS-reserved-env, DOCS-readme-deadlinks, DOCS-apple-status-drift~~ → **all five shipped as [029](029-docs-drift-reconciliation.md)** (in-tree; reserved list is 19 names — `JACKIN_OPEN_LINKS` is deliberately settable and not in the reserved table).

## Second-wave deferred findings (2026-07-09 audit against `47dd5fca0` — recorded so they aren't re-audited)

Real gaps measured by the phase audit, deliberately not planned this wave (larger blast radius, blocked on a dependency, or an unresolved design decision). Grouped by roadmap phase.

### Phase 1 (lints/CI) — next adoption candidates, with measured volumes

- ~~Slice/index panic family~~ → **planned as [019](019-slice-index-lints-pure-crates.md)** (wave 1: protocol/config/manifest/core). Wave 2+ crates remain deferred: jackin-term (12 range-slices), env, diagnostics, then capsule 31 / tui 28 / runtime 24 / xtask 22.
- **`map_err_ignore` — VERDICT: documented-allow, do not adopt now.** Third-wave vet: the 33 production sites are dominated by `u16::try_from(...).map_err(|_| …)` wire-length conversions (`protocol/attach.rs:631,632,648,670`) where `TryFromIntError` carries no information; adoption would add ~25 boilerplate `#[expect]`s for near-zero catch value. Revisit only via the allowlist-track pilot.
- **Bare-`#[allow]` conversion** — 235 bare allows (of 369; all 182 `#[expect]`s are already reasoned); 51% sit in `jackin-console`. Convert crate-by-crate, then flip `allow_attributes` + `allow_attributes_without_reason` to deny. L. Plan 011's ratchet caps the debt meanwhile.
- **`missing_docs` on foundational crates** — first crate (jackin-protocol) **planned as [021](021-protocol-missing-docs-typed-errors.md)**. Remaining: jackin-manifest (41 items — trivially next), jackin-env (99), jackin-term (154), jackin-config (196), jackin-core (312 — consider splitting). L total.
- **Numeric-cast tier** — the four roadmap-named loud lints (`float_cmp_const`, `lossy_float_literal`, `cast_sign_loss`, `invalid_upcast_comparisons` + `float_cmp` re-eval) plus the easy-to-avoid family → **planned as [034](034-numeric-and-easy-lint-families.md)** (sixth-wave census: easy family zero candidates workspace-wide; float family zero grep-visible; real work = sign-loss cluster in jackin-usage). The full truncation/precision/wrap tier (426 ` as ` casts, 351 unsigned-target) stays the deferred optional tier per roadmap line 78. L, optional.
- ~~Feature-powerset PR promotion + beta-toolchain clippy canary~~ → **planned as [022](022-ci-lanes-powerset-canary-partitions.md)**. ~~Dylint crate~~ → **planned as [052](052-dylint-scaffold-render-purity.md)** (workspace-excluded `crates/jackin-lints`, pinned nightly, render-thread-purity first lint, advisory hygiene lane).
- ~~OPEN DECISION — `cargo-public-api`/`cargo-semver-checks`~~ → **DECIDED 2026-07-09 (operator): dossier Skip wins.** See "Operator decisions" above; plan 011 writes the roadmap note; plan 035 excludes the report from the advisory lanes.

### Phase 2 (architecture) — design-decision items

- ~~OPEN DECISION — `#[non_exhaustive]` on wire/schema enums~~ → **DECIDED 2026-07-09 (operator): keep exhaustive matching.** The third-wave counter-evidence stands (`Provider` doc, `protocol/lib.rs:210-215`: exhaustive matching IS the drift-guard; zero external consumers to protect). No sweep; per-type judgment stays allowed. Roadmap Phase 2 item 3 premise to be revised in the next PR touching that page. See "Operator decisions" above.
- **Port-marker/sealing taxonomy** — 13 of 15 foundational pub traits are unsealed *intentional ports* (DockerApi, CommandRunner, the sink traits); nothing distinguishes them from accidental API, so the planned "flag unsealed pub traits" lint has no signal. Define a marker convention first. M.
- **AFIT Send story** — native `async fn` in 6 pub traits with inconsistent `Send` supertraits and no `Send`-bounded returns (`LaunchOutputSink::print_deploying` boxes without `+ Send`, launch_progress.rs:259). Decide RPITIT+Send vs trait-variant, apply uniformly. M.
- **Stringly-typed foundational errors** — `ClipboardImageError(String)` **planned as part of [021](021-protocol-missing-docs-typed-errors.md)**. Vet corrections: `ParseMountIsolationError(String)` (core/isolation.rs:5-8) already derives thiserror and the String is the offending input — idiomatic, NOT a violation; `ParseProfileError` (core/docker_security.rs:59-71) hand-rolled→thiserror conversion → **planned as [037](037-thiserror-foundational-core-env.md)** Step 1.
- **Launch typestate** — no phase-output structs (`ValidatedProfile → … → RunningContainer`); `#[must_use]` appears once in the launch tree. L; sequence after characterization (TEST-launch-core) per the roadmap.

### Phase 3 (tests) — blocked or design-first

- ~~`/jackin/`-path policy test~~ → **planned as [020](020-container-path-chokepoint-policy.md)** (chokepoint module + policy suite + shrink-only literal gate; census refreshed to 152 literals).
- **Property tests** — the pure env slice (reserved-env consistency + no-panic over `validate_reserved_names`/`resolve_env`) → **planned as [045](045-protocol-env-corpus-closure.md)** Step 4; broader proptest adoption still rides 013's generators. **Sim (turmoil/proptest-state-machine)** stays blocked on the daemon port decomposition; **failpoints** narrowed (PTY edge = [033]'s FaultMasterPty, Docker edge = existing fakes; fs/process fail-crate residual DEFER); ~~chaos lane~~ → **planned as [046](046-dind-chaos-lane.md)** (the clock-seam/daemon-port blockers were inapplicable to external fault injection).
- ~~Missing behavioral specs~~ → **planned as [032](032-daemon-console-behavioral-specs.md)** (cite-or-MISSING format; the MISSING worklists become the daemon-decomposition characterization queue).
- **Snapshot standardization** — shared golden helpers (fixed dims/theme) stay SEQ behind TEST-support-crate [025]; the pending-snap gate half was already planned in [011] Step 5 (wave-7 verify reconciled the two rows). M.
- **Coverage-map enforcement** — plan 010 ships the report (61 production files >300 lines with no sibling tests.rs); flipping to a gate for new modules is a later ratchet.

### Phase 4 (perf) — measurement follow-ons

- **iai-callgrind adoption** for deterministic PR-gateable instruction counts (valgrind runner constraints), **input-to-frame latency harness** (needs a headless TUI driver), **dhat inline literals → ratchet entries** (render_allocation.rs asserts `blocks <= 3`/`bytes <= 1024` as source literals — improvements don't tighten them), **full launch-pipeline bench** through FakeDockerClient. ~~hyperfine cold-start lane~~ → **planned as [048](048-advisory-lanes-wave-2.md)**. ~~`cargo xtask ci` partition aliases~~ → **planned as [022](022-ci-lanes-powerset-canary-partitions.md)**.

### Phase 5/6 (docs/navigation)

- ~~Documented-command drift gate~~ → **planned as [023](023-docs-command-drift-gate.md)**. The config-key half of roadmap item 11 (docs tables ↔ schema artifacts) remains deferred.
- ~~Crate-README→Fumadocs extraction pipeline + PROJECT_STRUCTURE slim~~ → **shipped as [049](049-crate-readme-fumadocs-pipeline.md)**; full Codebase-Map slimming follows as a later pass. ~~README freshness-vs-diff CI check~~ → **planned as [050](050-readme-freshness-gate.md)** (presence gate stays in 015). ~~rust-analyzer cleanliness lane~~ → **planned as [048](048-advisory-lanes-wave-2.md)**. ~~Machine-readable gate output~~ → core reporter + 2 exemplar gates **planned as [051](051-xtask-gate-reporter-core.md)**; per-gate rollout = mechanical follow-ups.

### Phase 7/8 (loop/telemetry)

- **Self-tightening ratchet lane** (scheduled recompute + auto-PR; `--print-budget`/`--print-allowlist` are invoked by nothing today), **health-history JSONL/data-branch**, **golden-agent-tasks benchmark**, **agent-operated hygiene lane** (dependency chain: machine-readable gates + unified engine first).
- ~~Telemetry facade~~ → **planned as [041](041-telemetry-operation-facade.md)** (API + `debug_log!` collapse + ShellRunner adoption; corrected census: 289 clog-family + 283 debug_log! sites). ~~Per-sink filters + JACKIN_DEBUG retirement~~ → **planned as [043](043-per-sink-filters-jackin-debug-retirement.md)** (19 reader files; level/categories/`JACKIN_OTEL_INTERNAL` env model verified already shipped). ~~The 9-instrument metric set~~ → **planned as [042](042-high-frequency-metrics.md)**. ~~Launch conformance suite + export-volume ratchet~~ → **planned as [044](044-telemetry-conformance-lane.md)** (rig verified at observability.rs:911-952; volume budgets in-source until 017).

## Findings considered and rejected (do not re-audit)

- **Duplicate-version `bans.skip` ledger** — `cargo deny check bans` is clean with zero unused skips; the mechanism is settled. Not a finding (a new unrecorded duplicate would be).
- **`-D warnings` CI-only, `allow`-listed pedantic lints, mega `tests.rs` files, pre-release breaking changes** — all documented, deliberate roadmap positions.
- **`await_holding_lock` debt** — the roadmap's claim of "non-`Send` guards held across `.await` in places" was refuted by measurement: zero real hits (capsule has no file with both `.lock()` and `.await`; runtime's one case is a tokio Mutex the lint ignores). Adopted in plan 011 purely as a regression guard; roadmap text corrected there.
- **Schema five-artifact rule & migration fixture corpus as gaps** — verified already enforced/existing (`jackin-xtask/src/schema.rs`, `crates/jackin/tests/fixtures/migrations/` per-version corpus); only idempotence/golden assertions were missing (plan 013).
- **Phase 8 error semantics + run-id contract as unbuilt** — verified largely shipped (`error.type`, span status, outcome taxonomy, wrapper-run-id-wins); roadmap prose overstated the gap; plan 018 reconciles and closes the true residuals.
- **deny.toml/.codebook.toml as ratchet-engine candidates** — deliberately excluded from `ratchet.toml` (cargo-deny/codebook own their semantics; the spell allowlist grows by design).

## Direction findings (maintainer's call — options, not defects)

Forward-looking bets grounded in repo evidence, presented for the maintainer to weigh. Not ranked against the fixes above; each would become a design/spike plan, not a build-everything plan.

1. **Finish the Apple Container backend into a Docker-Desktop-free macOS tier.** A real `ContainerBackend` trait with `DockerBackend` + `AppleContainerBackend` impls and config surface already exist; `AppleContainerBackend::finalize` is a Phase-0 stub. `APPLE_CONTAINER_RESEARCH.md` says apple/container 1.0.0 shipped and the code targets v0.11.0, missing 1.0.0's `--publish-socket` and `--internal` networks. The only true blocker is Phase-0 validation on macOS 26 ARM hardware. Highest strategic leverage; the code is ~80% there. L.
2. **Agent-to-agent orchestration (“Conductor view”).** The capsule multiplexer already owns every agent's PTY, tracks per-session state, and records agent/provider/codename; a dedicated design page argues “mostly reuse.” Turns jackin❯ from “safe place to run one agent” into “visible control room for a team of agents.” L; substrate exists.
3. **Budget enforcement (spend caps), not just telemetry.** `jackin-usage` meters cost but explicitly cannot enforce a per-task/day cap (grep for `budget|enforce|kill` is clean). Trigger (status authority) + actuator (daemon/capsule) exist. With parallel autonomous agents, runaway spend is the top operational risk. M.
4. **First-party eval / outcome-scoring harness.** jackin❯ uniquely runs the same task across runtime × provider × role but measures only cost, never success. Cost telemetry + `jackin-dev pr` verification already exist to join. Category-defining for a multi-runtime tool and the signal a Conductor view needs. M-L.
5. **Local/self-hosted model as a first-class provider with hybrid routing.** Provider catalog + per-session model override + the DinD sidecar pattern a local engine would mirror all exist; fits the isolation/privacy thesis and powers cheap workers for #2. M.
