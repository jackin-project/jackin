# Code-health implementation plans

Generated by the `improve` skill (deep audits) following the roadmap item [Codebase health: Rust strictness, structure, and AI reviewability](/roadmap/codebase-health-enforcement/). First wave 2026-07-09 against `a4761957d` (plans 003-009: concrete bug/perf/test instances). Second wave 2026-07-09 against `47dd5fca0` (plans 010-018: the roadmap program itself — one plan per phase cluster, selected per operator instruction to cover all phases 0-8). Third wave 2026-07-09 against `e80d5cc0a` (plans 019-023: top deferred findings promoted — no re-audit; the second-wave audit data was hours old and the ledger below was the source).

## Execution order & status

Ordered by leverage (urgency ÷ effort, weighted by confidence) within each wave.

### First wave — instance fixes (no hard dependencies; any order)

| Plan | Title | Priority | Effort | Category | Status |
|------|-------|----------|--------|----------|--------|
| [003](003-materialize-accounts-borrow.md) | Stop deep-cloning usage views on every refresh | P1 | S | perf | TODO |
| [004](004-resize-coalesce-drops-frames.md) | Stop dropping the frame queued behind a coalesced resize | P1 | S | bug | TODO |
| [007](007-osc8-hyperlink-map-bound.md) | Bound OSC 8 hyperlink maps; clear on reset | P2 | S-M | bug | TODO |
| [008](008-launch-finalization-cleanup-guard.md) | Tear down DinD when a post-success finalization step fails | P2 | M | bug | TODO |
| [009](009-protocol-decoder-fuzz-and-truncation-tests.md) | Fuzz + truncation tests for protocol wire decoders | P2 | M | tests | TODO |

### Second wave — program infrastructure (respect the dependency notes)

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [010](010-health-dashboard-and-baselines.md) | Code-health dashboard, suppression inventory, verification matrix | 0 | P1 | M | TODO |
| [011](011-lint-strictness-silent-failures.md) | Silent-failure lints, rustdoc gates, doc tests in PR CI, reason-gate | 1 | P1 | M | TODO |
| [012](012-tier-graph-arch-gate.md) | Tier-graph arch gate (replaces empty forbidden-edge list) | 2 | P1 | M-L | TODO |
| [013](013-test-infra-flakes-fuzz-idempotence.md) | Flake detection, timing artifacts, migration idempotence, parser fuzz | 3 | P2 | M-L | TODO |
| [014](014-hot-path-bench-coverage.md) | Compile-check all benches; cover 4 unbenchmarked hot paths; measured lane | 4 | P2 | M | TODO |
| [015](015-docs-gates-brand-specs-readme.md) | Brand-prose lint, spec↔test citations, README presence gate | 5 | P2 | M | TODO |
| [016](016-ownership-headers-blame-revs.md) | Ownership headers everywhere + headers gate + blame-ignore-revs | 6 | P2 | M | TODO |
| [017](017-unified-ratchet-engine.md) | Unified ratchet engine (`ratchet.toml`) + defect→gate ledger | 7 | P2 | L | TODO |
| [018](018-telemetry-drift-proofing.md) | One OTLP builder, semconv registry, correlatable sinks, honest failures | 8 | P2 | M-L | TODO |

### Third wave — promoted deferred findings

| Plan | Title | Phase | Priority | Effort | Status |
|------|-------|-------|----------|--------|--------|
| [019](019-slice-index-lints-pure-crates.md) | Slice/index panic-coverage lints on the 4 pure crates (wave 1) | 1 | P1 | M-L | TODO |
| [020](020-container-path-chokepoint-policy.md) | Container-path chokepoint + executable `/jackin/`-only policy | 3 | P1 | M-L | TODO |
| [021](021-protocol-missing-docs-typed-errors.md) | `missing_docs` on jackin-protocol + typed clipboard wire error | 1/2 | P2 | M | TODO |
| [022](022-ci-lanes-powerset-canary-partitions.md) | Scoped powerset PR gate, beta clippy canary, `xtask ci --only` partitions | 1/4 | P2 | M | TODO |
| [023](023-docs-command-drift-gate.md) | Documented-command drift gate (docs fences ↔ clap tree) | 5 | P2 | M | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Dependency notes (second wave)

- **010 before 011**: the suppression reason-gate's budget file is generated from 010's scanner/baseline, not hand-counted.
- **012 before 016**: the headers gate cross-checks each `lib.rs` tier against 012's `TIERS` table in arch.rs.
- **010 + 011 before 017**: the ratchet engine ports 011's suppression budget and reuses 010's metric functions as providers.
- **014 alongside 003**: land the `materialize_accounts` bench before/with plan 003 so the optimization's win is measured.
- 013, 015, 018 are independent and can run in parallel with anything.

## Dependency notes (third wave)

- **019 after 011** (soft): both edit the lint config area; land 011 first to avoid conflicts. 019's valves are independent of 011's lints.
- **021 after 019** (soft): both touch `jackin-protocol/src/attach.rs` and `lib.rs`; sequence to avoid conflicts.
- **020 Step 5**: if plan 017's `ratchet.toml` engine has landed, the literal gate is a ratchet family, not a standalone gate — the plan branches on that.
- **022 conflicts with 011** on `ci.rs`/`ci.yml` — coordinate ordering; either first is fine.
- 023 is fully independent.

## How this maps to the roadmap

These are the concrete, vetted routine instances the roadmap program asks for — the evidence that prioritizes which lints/gates to adopt next:

- 004 / 007 / 008 are the panic/silent-failure/resource-leak defect classes Phase 1's restriction-lint families (`let_underscore_*`, indexing/slice, async) and Phase 7's defect→gate ledger are meant to catch.
- 009 is the Phase 3 “property/fuzz for protocol decoding” item, scoped to the one crate with zero fuzz coverage today.
- 003 is the Phase 4 “borrowed records / avoid full clones” performance item.

## Findings considered and deferred (not yet planned — recorded so they aren't re-audited)

High-value findings that are real but were not turned into first-wave plans (larger blast radius, a design decision, or lower leverage).

### Correctness / bugs

- **BUG-color-hex-slice** — `host_colors.rs:161-166` (duplicated in `jackin-tui` and `jackin-runtime`) slices a `#RRGGBB` OSC reply by byte under a byte-length guard; a 6-byte multibyte string panics. Guard with `is_ascii()` or parse from `as_bytes()`; dedupe the two copies. S. Low reachability but a concrete `string_slice` instance.
- **BUG-pty-unbounded-channel** — capsule `event_tx/event_rx` (`daemon.rs:202-203`) is unbounded; a runaway agent (`yes`) has no PTY backpressure and can inflate capsule RSS. Investigate depth under a flood soak before switching to a bounded/coalescing channel. LOW-MED, investigate-first.

### Performance

- **PERF-scrollback-snapshot** — `pane_content_from_damagegrid` (`jackin-capsule/src/tui/pane_snapshot.rs:182-194`) snapshots the whole 10k-row scrollback (owned `String` per cell) to read one cell / a few rows, and fires per mouse-motion event during Ctrl/Alt link-hover (`daemon/mouse_input.rs:653`). Add a borrowed row accessor + range-scoped snapshot. M, highest-leverage remaining perf item.
- **PERF-resize-clone** — `resize_grid` (`jackin-term/src/grid/write.rs:45-59`) allocates a fresh grid and deep-clones every retained cell on every `set_size` (both primary + alternate), with an O(rows×pool) arena scan; row storage is freed, not reused. Resize in place. M.
- **PERF-diag-double-parse** — `summary.rs:153-206` parses every JSONL line into an owned `serde_json::Value` and re-parses `detail` a second time; runs reach hundreds of MB. Typed borrowed deserialize + `RawValue` + reused line buffer. M.
- **PERF-benches-missing** — no bench covers resize, scrollback-snapshot/copy, `materialize_accounts`, or `summarize_reader`; the Phase-4 perf-budget lane can't gate them. Add one bench per hotspot. M.
- Low-leverage (recorded, likely not worth doing soon): usage-monitor whole-file re-read per poll (documented tradeoff), usage upsert without a cached prepared statement (verify turso caching first), `preserve_visible_rows_to_scrollback` double-clone.

### Tests

- **TEST-clock** — no injected clock; 97 raw `Instant/SystemTime/Utc::now()` sites make expiry/throttle/deadline logic untestable and force 10 sleep-based tests. Root blocker for all planned sim/failpoint/proptest work. Introduce a `Clock` seam (start with one consumer, e.g. `clipboard.rs` expiry). L, high leverage but touches many sites — scope to a first consumer.
- **TEST-launch-core** — `run_launch_core` has zero direct test; docker-run + wait-for-state are E2E-only. Drive it with `FakeDockerClient` (`runtime/test_support.rs:200`) for a happy path + a mid-pipeline-failure teardown assertion (pairs with plan 008). M.
- **TEST-displace-policy** — capsule multi-client displace / active-client policy has one reattach test; no “second client displaces, input routes to B” test. M. Natural home for the planned proptest-state-machine.
- **TEST-pty-recovery** — `session.rs:470-555` writer-lock-failure / closed-channel / PTY-EOF recovery paths are unexercised. Extend the mock PTY to return Err/EOF. M.
- **TEST-support-crate** — ≥3 `FakeRunner`, 2 `FakeDockerClient`, 4 `StubRunner`, duplicated `fake_with_outputs` across 8+ crates. Extract `jackin-test-support` with one canonical set. L. Roadmap Phase 3 item; sized here.
- **TEST-clipboard** (S) and **TEST-git-watch** (S-M) — untested clipboard image-chunk assembly / time-expiry and git/PR-watch throttle; both need TEST-clock first.

### Tech debt / architecture

- **DEBT-op-typed-error** — `op_cli::run_op_json` rewrites stderr into an `anyhow!` string that `oppicker::classify_probe_error_message` re-parses by substring across a crate boundary (~40 such `.contains()` classification sites workspace-wide). Define an `OpProbeError` thiserror enum. M. **Open decision:** which crate owns the enum (jackin-core vs a shared type) given the oppicker(pure)/jackin-env(side-effects) split — resolve before planning.
- **DEBT-anyhow-in-libs** — `anyhow` saturates library crates (runtime 39 files, console 35, capsule 22…); only 5 of 21 have any `thiserror`. Convert foundational crates first (`jackin-core` 8, `jackin-env` 12). L, phased.
- **DEBT-devdep-cycle** — `jackin-isolation` dev-deps `jackin-runtime` while runtime prod-deps isolation = a cycle through dev-deps; `jackin-term` dev-deps `jackin-diagnostics` (upward). Blocks a tier-graph arch gate. Move the shared test helpers into a neutral testkit crate. S-M. See also DX-arch-gate.
- **DEBT-type-complexity** — 47 `type_complexity` allows, 34 in `jackin-console` editor/settings view builders (19 in `editor/view/frame.rs` alone). Replace nested closure/tuple return types with named `FieldRow`/`SectionBuilder` structs. M.
- **DEBT-pub-surface** — foundational crates expose internals as broad `pub mod` (`jackin-core` 39 pub mods, `jackin-env` 11) rather than curated root re-exports, so every internal refactor risks a breaking change. Flip to `pub(crate)` + `pub use`. M per crate. Roadmap Phase 2.
- **DEBT-tui-half-layer** — the shared `jackin_tui::runtime` `View` trait has 3 thin implementors and 0 real dispatchers (each loop still calls `render` directly, self-documented as a follow-up). Either finish the loop-through-trait migration or drop the trait + context structs until a shared driver exists. M.
- **DEBT-newtypes** — `workspace`/`session`/`instance` ids cross crate boundaries as bare `&str` (58 workspace-name sites; adjacent swappable `&str` params in `launch_slot.rs`). Introduce `WorkspaceName`/`SessionId` newtypes (exemplars: `jackin-core::selector::RoleSelector`, `OpRef`). M. Roadmap Phase 2.

### Dependencies

- **DEP-turso-wrap** — pre-release `turso =0.7.0-pre.17` on the usage-accounting path, but only 2 files import `turso::`. Wrap behind one store trait so a stabilize-or-swap is a one-module change. S.
- **DEP-ring-license** — stale `ring@0.17.14` license exception in `deny.toml` (`license-exception-not-encountered` warning); delete the line. S.
- **DEP-pin-rationale** — 5 runtime `=` pins (tokio, serde_json, toml, turso, ratatui-core) carry no inline rationale; annotate each with reason + revisit trigger. Consider relaxing `ratatui-core =0.1.2` to `"0.1"` (lockstep-conflict risk with caret ratatui 0.30) and tokio `=1.52.3` to `~1.52` for patch flow. S.

### DX / tooling (agent-contributor leverage)

- **DX-arch-gate-noop** — `cargo xtask lint arch` has an empty `FORBIDDEN_EDGES` list (`arch.rs:42`), so `--strict` passes while checking zero edges; the umbrella `lint --strict` advertises architecture enforcement it isn't performing. Also `arch.rs:135` misspells the doc as `codebases-health-enforcement`. Populate the gate (start with the DEBT-devdep-cycle edge or a tier graph) or stop over-promising. S-M.
- **DX-doctests-pr** — `cargo test --doc` runs only in scheduled hygiene, not PR CI or `cargo xtask ci`; a broken doctest merges green and reddens `main` up to 24h later, violating the repo's own PR/main-parity hard rule. Add to PR CI + `ci.rs`. S.
- **DX-ci-parity** — `cargo xtask ci` (billed as merge-readiness) omits fuzz, bench-build, `check-agent-status-truthful.sh`, and the docs gate trio, yet runs feature-powerset (a scheduled-only lane). Make it a faithful union of PR gates; demote powerset behind a flag. M.
- **DX-gate-json** — 0/8 xtask gates emit `--format json` or GitHub `::error file=,line=` annotations; an agent reading a red CI regexes prose. Add a shared human|json|github report helper + a clippy/rustc problem matcher. L. Roadmap “diagnostics are prompts”; baseline = zero.
- **DX-gate-messages** — bring the six weaker xtask gates up to the `lint tests` bar (each failure states the exact rerun command + fix); `lint tests` is the template. S.
- **DX-fast-tier** — `cargo xtask ci --fast` only drops feature-powerset (still full all-features clippy + nextest + audit + deny + an MSRV toolchain fetch); no lightweight “re-check what I touched” tier. Add `--quick`. S-M.
- **DX-redundant-jobs** — `schema-check` and `file-size-gate` are separate CI jobs each recompiling `jackin-xtask` into its own rust-cache namespace for pure file inspection; collapse into one `xtask-gates` job. S. Cache-quota pressure.
- **DX-onboarding** — no `rust-analyzer.toml` / `.env.example` / TESTING verification matrix; sccache is a pinned tool wired only in CI with no fast-local-loop doc (mold/sccache/`-p` targeting). M. Workspace is RA-friendly — no proc-macro crates — so a 5-line RA config is cheap.

### Docs (concrete drift — sizes the planned freshness/drift gates)

- **DOCS-codebase-map** — the “canonical” tier DAG omits 4 crates (`jackin-build-meta`, `jackin-dev`, `jackin-pr-trailers`, `jackin-tui-lookbook`); stale extraction-ledger counts (claims 94 files/21,382 LOC, actual 77/24,417); lists a root `src/`+`tests/` that no longer exist. S.
- **DOCS-capsule-readme** — `jackin-capsule/README.md` is 3 intro paragraphs, missing the mandatory Structure/tier/API/verify sections the `crates/AGENTS.md` hard rule requires for a 31-file crate (the `lint agents` gate only checks file existence). M.
- **DOCS-reserved-env** — `guides/environment-variables.mdx` lists 11 of the 19 names `RESERVED_RUNTIME_ENV_VARS` (`env_model.rs:65-84`) actually rejects; operators setting `JACKIN_ALLOWED_HOSTS`/`JACKIN_SUDO` hit an undocumented hard rejection. S.
- **DOCS-readme-deadlinks** — root `README.md:77` links 3 non-existent doc routes (`/reference/architecture/`, `/reference/codebase-map/`, `/reference/roadmap/`); correct to `/reference/getting-oriented/…` + `/roadmap/`. S.
- **DOCS-apple-status-drift** — `roadmap/index.mdx:93` says Apple-backend lifecycle dispatch is still TODO; the item page and code (`cleanup.rs`/`attach.rs` dispatch through `backend_for_state`) say it's wired. Reconcile. S.

## Second-wave deferred findings (2026-07-09 audit against `47dd5fca0` — recorded so they aren't re-audited)

Real gaps measured by the phase audit, deliberately not planned this wave (larger blast radius, blocked on a dependency, or an unresolved design decision). Grouped by roadmap phase.

### Phase 1 (lints/CI) — next adoption candidates, with measured volumes

- ~~Slice/index panic family~~ → **planned as [019](019-slice-index-lints-pure-crates.md)** (wave 1: protocol/config/manifest/core). Wave 2+ crates remain deferred: jackin-term (12 range-slices), env, diagnostics, then capsule 31 / tui 28 / runtime 24 / xtask 22.
- **`map_err_ignore` — VERDICT: documented-allow, do not adopt now.** Third-wave vet: the 33 production sites are dominated by `u16::try_from(...).map_err(|_| …)` wire-length conversions (`protocol/attach.rs:631,632,648,670`) where `TryFromIntError` carries no information; adoption would add ~25 boilerplate `#[expect]`s for near-zero catch value. Revisit only via the allowlist-track pilot.
- **Bare-`#[allow]` conversion** — 235 bare allows (of 369; all 182 `#[expect]`s are already reasoned); 51% sit in `jackin-console`. Convert crate-by-crate, then flip `allow_attributes` + `allow_attributes_without_reason` to deny. L. Plan 011's ratchet caps the debt meanwhile.
- **`missing_docs` on foundational crates** — first crate (jackin-protocol) **planned as [021](021-protocol-missing-docs-typed-errors.md)**. Remaining: jackin-manifest (41 items — trivially next), jackin-env (99), jackin-term (154), jackin-config (196), jackin-core (312 — consider splitting). L total.
- **Numeric-cast tier** — 392 ` as ` casts/85 files under the four currently-allowed cast lints; the roadmap's "documented invariant on every lossy cast" tier. L, optional.
- ~~Feature-powerset PR promotion + beta-toolchain clippy canary~~ → **planned as [022](022-ci-lanes-powerset-canary-partitions.md)**. Dylint crate (`crates/jackin-lints`) remains deferred: S-M to scaffold, first lint = render-thread purity, scheduled-lane advisory.
- **OPEN DECISION — `cargo-public-api`/`cargo-semver-checks`**: roadmap line 60 asks for an informational public-API report, but the research dossier records a **Skip** decision (`docs/content/docs/reference/research/ci/rust-tooling/rust-ci-tooling.mdx:148`: "a CLI workspace with no published library crate has nothing meaningful to diff"). Reconcile before building anything (plan 011 adds the roadmap note).

### Phase 2 (architecture) — design-decision items

- **OPEN DECISION — `#[non_exhaustive]` on wire/schema enums (downgraded from actionable).** Third-wave vet found in-code counter-evidence: `Provider`'s doc (`protocol/lib.rs:210-215`) states exhaustive matching IS the drift-guard ("the host console, the wire, and the in-container daemon all match on this enum so the provider catalog cannot drift across sites"). `non_exhaustive` forces `_` arms in every consumer crate, silently routing new variants to catch-alls — trading workspace-internal compile-time exhaustiveness (relied upon, valuable pre-release with zero external consumers) for external API stability (no external users; cargo-public-api itself is a disputed Skip). Do not apply without an operator decision; the roadmap's Phase 2 item 3 premise should be revisited alongside the public-API-snapshot decision.
- **Port-marker/sealing taxonomy** — 13 of 15 foundational pub traits are unsealed *intentional ports* (DockerApi, CommandRunner, the sink traits); nothing distinguishes them from accidental API, so the planned "flag unsealed pub traits" lint has no signal. Define a marker convention first. M.
- **AFIT Send story** — native `async fn` in 6 pub traits with inconsistent `Send` supertraits and no `Send`-bounded returns (`LaunchOutputSink::print_deploying` boxes without `+ Send`, launch_progress.rs:259). Decide RPITIT+Send vs trait-variant, apply uniformly. M.
- **Stringly-typed foundational errors** — `ClipboardImageError(String)` **planned as part of [021](021-protocol-missing-docs-typed-errors.md)**. Vet corrections: `ParseMountIsolationError(String)` (core/isolation.rs:5-8) already derives thiserror and the String is the offending input — idiomatic, NOT a violation; `ParseProfileError` (core/docker_security.rs:59-71) only needs a mechanical hand-rolled-impl→thiserror conversion. S, remains deferred.
- **Launch typestate** — no phase-output structs (`ValidatedProfile → … → RunningContainer`); `#[must_use]` appears once in the launch tree. L; sequence after characterization (TEST-launch-core) per the roadmap.

### Phase 3 (tests) — blocked or design-first

- ~~`/jackin/`-path policy test~~ → **planned as [020](020-container-path-chokepoint-policy.md)** (chokepoint module + policy suite + shrink-only literal gate; census refreshed to 152 literals).
- **Property tests** (proptest generators for manifest/config/env: no-panic, unknown-field policy, reserved-env consistency) and **sim/failpoints/chaos** (turmoil/fail; blocked on first-wave TEST-clock and the daemon port decomposition). M / L.
- **Missing behavioral specs** — capsule daemon and operator console state machine (the two highest-risk stateful subsystems; console `state/manager.rs` is 1228 lines with no sibling tests). M; must use plan 015's Tests-column format.
- **Snapshot standardization** — insta only in capsule+console; lookbook SVG renderer unused by tests; promote to a shared golden helper with fixed dims/theme after TEST-support-crate. M.
- **Coverage-map enforcement** — plan 010 ships the report (61 production files >300 lines with no sibling tests.rs); flipping to a gate for new modules is a later ratchet.

### Phase 4 (perf) — measurement follow-ons

- **iai-callgrind adoption** for deterministic PR-gateable instruction counts (valgrind runner constraints), **hyperfine cold-start lane**, **input-to-frame latency harness**, **dhat inline literals → ratchet entries** (render_allocation.rs asserts `blocks <= 3`/`bytes <= 1024` as source literals — improvements don't tighten them), **full launch-pipeline bench** through FakeDockerClient. ~~`cargo xtask ci` partition aliases~~ → **planned as [022](022-ci-lanes-powerset-canary-partitions.md)**.

### Phase 5/6 (docs/navigation)

- ~~Documented-command drift gate~~ → **planned as [023](023-docs-command-drift-gate.md)**. The config-key half of roadmap item 11 (docs tables ↔ schema artifacts) remains deferred.
- **Crate-README→Fumadocs extraction pipeline** + Codebase-Map slimming; **README freshness-vs-diff CI check** (presence lands in plan 015); **rust-analyzer cleanliness lane**; **machine-readable gate output everywhere** (DX-gate-json: `--format json` + problem-matcher annotations across all xtask gates — plan 010's health lane sets the pattern).

### Phase 7/8 (loop/telemetry)

- **Self-tightening ratchet lane** (scheduled recompute + auto-PR; `--print-budget`/`--print-allowlist` are invoked by nothing today), **health-history JSONL/data-branch**, **golden-agent-tasks benchmark**, **agent-operated hygiene lane** (dependency chain: machine-readable gates + unified engine first).
- **Telemetry facade** (`operation_span/log/error/metric` — 312 clog!/cdebug!-family sites + 286 debug_log! sites across two duplicate macro definitions), **per-sink filters + JACKIN_DEBUG retirement** (21 files still read it), **the 9-instrument metric set** (render/terminal/DB counters currently debug log rows, client_writer.rs:126-143), **launch conformance suite + export-volume ratchet** (in-memory exporter rig already exists, observability.rs:911-943).

## Findings considered and rejected (do not re-audit)

- **Duplicate-version `bans.skip` ledger** — `cargo deny check bans` is clean with zero unused skips; the mechanism is settled. Not a finding (a new unrecorded duplicate would be).
- **`-D warnings` CI-only, `allow`-listed pedantic lints, mega `tests.rs` files, pre-release breaking changes** — all documented, deliberate roadmap positions.
- **`await_holding_lock` debt** — the roadmap's claim of "non-`Send` guards held across `.await` in places" was refuted by measurement: zero real hits (capsule has no file with both `.lock()` and `.await`; runtime's one case is a tokio Mutex the lint ignores). Adopted in plan 011 purely as a regression guard; roadmap text corrected there.
- **Schema five-artifact rule & migration fixture corpus as gaps** — verified already enforced/existing (`jackin-xtask/src/schema.rs`, `crates/jackin/tests/fixtures/migrations/` per-version corpus); only idempotence/golden assertions were missing (plan 013).
- **Phase 8 error semantics + run-id contract as unbuilt** — verified largely shipped (`error.type`, span status, outcome taxonomy, wrapper-run-id-wins); roadmap prose overstated the gap; plan 018 reconciles and closes the true residuals.
- **deny.toml/.codebook.toml as ratchet-engine candidates** — deliberately excluded from `ratchet.toml` (cargo-deny/codebook own their semantics; the spell allowlist grows by design).

## Direction findings (maintainer's call — options, not defects)

Forward-looking bets grounded in repo evidence, presented for the maintainer to weigh. Not ranked against the fixes above; each would become a design/spike plan, not a build-everything plan.

1. **Finish the Apple Container backend into a Docker-Desktop-free macOS tier.** A real `ContainerBackend` trait with `DockerBackend` + `AppleContainerBackend` impls and config surface already exist; `AppleContainerBackend::finalize` is a Phase-0 stub. `APPLE_CONTAINER_RESEARCH.md` says apple/container 1.0.0 shipped and the code targets v0.11.0, missing 1.0.0's `--publish-socket` and `--internal` networks. The only true blocker is Phase-0 validation on macOS 26 ARM hardware. Highest strategic leverage; the code is ~80% there. L.
2. **Agent-to-agent orchestration (“Conductor view”).** The capsule multiplexer already owns every agent's PTY, tracks per-session state, and records agent/provider/codename; a dedicated design page argues “mostly reuse.” Turns jackin❯ from “safe place to run one agent” into “visible control room for a team of agents.” L; substrate exists.
3. **Budget enforcement (spend caps), not just telemetry.** `jackin-usage` meters cost but explicitly cannot enforce a per-task/day cap (grep for `budget|enforce|kill` is clean). Trigger (status authority) + actuator (daemon/capsule) exist. With parallel autonomous agents, runaway spend is the top operational risk. M.
4. **First-party eval / outcome-scoring harness.** jackin❯ uniquely runs the same task across runtime × provider × role but measures only cost, never success. Cost telemetry + `jackin-dev pr` verification already exist to join. Category-defining for a multi-runtime tool and the signal a Conductor view needs. M-L.
5. **Local/self-hosted model as a first-class provider with hybrid routing.** Provider catalog + per-session model override + the DinD sidecar pattern a local engine would mirror all exist; fits the isolation/privacy thesis and powers cheap workers for #2. M.
