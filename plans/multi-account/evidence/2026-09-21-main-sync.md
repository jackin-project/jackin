# Main synchronization evidence — 2026-09-21

These are focused working-tree results during the normal merge of main
`fce94cea` into feature head `2a318440ce2e02a15a76530812f375ee4998a0f3`.
They are not final PR candidate or post-merge evidence. The tested source includes
uncommitted merge resolutions; the final commit and affected checks must be
recorded after the merge is committed.

Environment: Apple Silicon arm64, macOS 27.0, Rust 1.97.1, nextest 0.9.140.
The exact macOS 26 acceptance requirement remains unverified on this host.

| Command | Observed result | Execution handle |
|---|---|---|
| `rtk mise install` | Passed: all 36 pinned tools already installed | `76f684`, exit 0 |
| `rtk cargo nextest run -p jackin-telemetry --locked --no-fail-fast` | Passed: 65 tests, 2 binaries, 0.171 seconds | Session 79998, exit 0 |
| `rtk cargo nextest run -p jackin-diagnostics --locked --no-fail-fast` | Passed: 96 tests, 1 binary, 0.502 seconds | Session 42629, exit 0 |
| `rtk cargo nextest run -p jackin-core -p jackin-telemetry -p jackin-diagnostics --locked --no-fail-fast` | Passed after reserving the supervisor PID environment variable: 293 tests, 4 binaries, 1.137 seconds | Session 40363, exit 0 |
| `rtk cargo nextest run -p jackin-protocol --locked --no-fail-fast` | 120 passed, 1 skipped, 2 binaries, 0.228 seconds | Session 88288, exit 0 |
| `rtk cargo nextest run --locked -p jackin-config --lib --no-fail-fast` | Passed: 458 tests, 1 binary, 24.107 seconds | Accounts owner session 62958; reported terminal success |

The skipped protocol entry is the explicitly ignored corpus-rewrite utility in
`crates/jackin-protocol/tests/corpus_decode.rs:314`; its purpose is to mutate binary
fixtures when deliberately invoked. This is not a skipped acceptance scenario.

The Cargo results are the observed RTK summaries, not fabricated full runner logs.
Their scope is telemetry and diagnostics integration from main. They do not prove
account functionality, credential boundaries, native UI, or container/provider
acceptance. Subsequent relevant edits require fresh verification.
