version = "v1alpha10"
# Operator comment preserved across the bootstrap-stamp migration.
[accounts.personal]
name = "Personal"
provider = "anthropic"
[accounts.personal.credential]
type = "api_key"
value = "$ANTHROPIC_API_KEY"
