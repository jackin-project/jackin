from_version = "v1alpha10"
target_version = "v1alpha12"
target_version_shipped = "2026-09-17"
summary = "Stamp an already-initialized [bootstrap] sentinel so upgrades never rescan; agent configurations and default launch lists arrive via serde defaults."
