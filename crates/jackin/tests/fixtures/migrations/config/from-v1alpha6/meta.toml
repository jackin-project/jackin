from_version = "v1alpha6"
target_version = "v1alpha7"
target_version_shipped = "2026-06-09"
summary = "Restamps config as v1alpha7. Adds optional [runtime].default_backend for selectable container backends; additive with serde default, no transformation needed."
