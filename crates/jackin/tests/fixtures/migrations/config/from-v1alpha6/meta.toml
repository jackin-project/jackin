from_version = "v1alpha6"
target_version = "v1alpha7"
target_version_shipped = "2026-06-24"
summary = "Restamps config as v1alpha7. Adds optional dirty_exit_policy to AppConfig (global) and WorkspaceConfig (per-workspace override). Additive with serde default; no transformation needed."
