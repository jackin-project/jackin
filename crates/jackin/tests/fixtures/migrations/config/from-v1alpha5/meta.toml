from_version = "v1alpha5"
target_version = "v1alpha7"
target_version_shipped = "2026-06-04"
summary = "Restamps config as v1alpha7. Adds optional sync_source_dir to AgentAuthConfig (Defect 46 Phase B.1); additive with serde default, no transformation needed."
