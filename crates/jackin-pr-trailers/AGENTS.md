- Trailer order is fixed: `Signed-off-by` first, then `Co-authored-by`, then others in first-appearance order. Do not reorder without updating the squash-merge guidance under `.github/`.
- Use `gh` for PRs; fall back to local `git log` only when no PR exists. Delegate trailer parsing to `git interpret-trailers` — do not hand-roll a parser.
